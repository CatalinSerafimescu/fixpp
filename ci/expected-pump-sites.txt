# ci/expected-pump-sites.txt -- the #289 DIRECT-shape census pin.
#
# ⚠️ THIS PIN IS DELIBERATELY EMPTY, AND EMPTY DOES NOT MEAN #289 IS DONE.
#
# It pins ONE population: a lexically present `run_for(...)` window with an
# unconditional `.get()` within six following lines. That population is now
# migrated out, so the correct reading is zero rows. The scanner is licensed to
# report that zero by its own seeded-positive control (ci/pump-census.sh) -- not
# by anyone's confidence -- and set equality still catches a new site appearing.
#
# WHAT IS STILL OUT THERE, AND HOW TO SEE IT -- do not quote a number from here,
# it is a property of a moving tree:
#
#   bash ci/pump-get-sweep.sh     # every `.get()` on a co_spawn future, asking
#                                 # whether a guard precedes it and NAMES it.
#
# That sweep is the instrument for blind spot (c) (the window indirected behind
# a helper, which has no `run_for` for this census to anchor on) and for blind
# spot (b) (a `.get()` further than six lines below its window). It reported
# hundreds of unguarded candidates on the tree that emptied this pin. A
# candidate is not a defect -- a self-driving executor is correct with a bare
# `get()` -- but the set is where the remaining #289 work is chosen from.
#
# Adding a row here is a REGRESSION unless the change also explains it.
