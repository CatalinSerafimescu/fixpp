# API Checklist: Generated Public Write Surface (Requirements Quality)

**Purpose**: Validate that the requirements defining the generated write surface — the `build_<Msg>(out, args)` signature, the `<Msg>Args` aggregate shape, the exact-33 MsgType set, `validate_<Msg>`, the required-set derivation rules, and the generated-wrapper fail-closed guarantees — are complete, unambiguous, consistent, and measurable BEFORE implementation. These are unit tests for the REQUIREMENTS, not for the emitted code.
**Created**: 2026-07-10
**Feature**: [spec.md](../spec.md) · **Focus**: the generated public builder/args/validate surface (US1/US2/US3, FR-001/FR-004/FR-006/FR-006a/FR-007/FR-007a)

**CHK numbering**: restarts at CHK001 in each of the three 067 checklists (api / abi / nfr).

## Requirement Completeness

- [x] CHK001 Is the generated builder's full signature specified — free function `build_<Msg>(std::span<std::byte> out, const <Msg>Args&)`, return `expected_t<std::span<std::byte>>`, `noexcept` — rather than just "a typed builder"? [Completeness, Spec §FR-001] — PASS: full signature (free function `build_<Msg>(std::span<std::byte> out, const <Msg>Args&)`, return `expected_t<std::span<std::byte>>`, `noexcept`) given verbatim in spec.md FR-001 and data-model.md §1.2.
- [x] CHK002 Is the `<Msg>Args` per-kind member mapping enumerated for EVERY field kind `kind_of` computes (Decimal/Char/Int32/Bool/String/Length+Data/Skip) plus optional-group and required-group members, with member type and presence semantics for each? [Completeness, Spec §FR-007a] — PASS: data-model.md §1.1 table enumerates Decimal/Char/Int32/Bool/String/Length+Data-pair/Skip plus optional-group (`std::optional<std::span>`) and required-group (`std::span`) rows with member type + presence semantics.
- [x] CHK003 Is the exact set of 33 OFFICIAL distinct MsgTypes enumerated verbatim (`D E F G H 8 9 q r AF AC t u · V W X Y c d e f g h i b S R AG Z a · J P AS`) with an authoritative source (FIX44.xml + A/M/P catalogue rows) AND an exact-set (not subset-presence) completeness requirement? [Completeness, Spec §FR-004] — PASS: research.md R6 enumerates the exact 33-element set verbatim (A13/M17/P3) verified against `dictionaries/FIX44.xml`; contract §G4 repeats the literal set; spec.md `## Normative References` cites the A-001..A-013/M-001..M-012/P-001..P-003 catalogue rows; FR-004 states exact-set (not subset) completeness.
- [x] CHK004 Is `validate_<Msg>` specified as a complete surface — a separate routine off the serialize path, the generic `wire::validate_required<T>()` core, and the `writer_traits<Msg>` binding that locates the level-scoped tables? [Completeness, Spec §FR-006] — PASS: FR-006 + data-model §1.3/§1.4 + contract §G5 specify `validate_<Msg>` as a separate off-serialize-path routine over the generic `wire::validate_required<T>()` core bound via `writer_traits<Msg>`.
- [x] CHK005 Are the generated-wrapper fail-closed guarantees specified for GENERATED output (G1 inheriting 061 C1–C4/C6; G4/INV-4 atomic commit — `out` untouched on any error), not merely asserted of the hand builders? [Completeness, Spec §FR-005] — PASS: contract §G1 states generated output inherits 061 C1-C4/C6 verbatim; §G9 lists the discriminating generated-wrapper fail-closed witnesses (undersized out, control-byte, Bool Y/N, Length+Data, required-group-zero, W-vs-X delimiter) distinct from the hand-builder pins.
- [x] CHK006 Are the required-set derivation rules complete and internally consistent — level-scoped (`group_no_tag==0` body set vs per-group set), header/framing exclusion set `{8,9,10,34,35,49,52,56}`, per-occurrence (not version-wide per `no_tag`), and the N3 `append_run` dedup-collapse fallback authority (codegen-local declaration walk carrying per-member `rule`)? [Completeness, Spec §FR-006a] — PASS: FR-006a is exhaustive — level-scoped body-vs-group sets, header/framing exclusion `{8,9,10,34,35,49,52,56}`, per-occurrence (not version-wide per `no_tag`, with the W/X 268 example), and the N3 `append_run` dedup-collapse fallback authority (codegen-local declaration walk carrying per-member `rule`), consistent with data-model §2.

## Clarity

- [x] CHK007 Is Bool routing stated unambiguously as the existing `body_builder` char overload emitting `Y`/`N` (never the int64 path emitting `1`/`0`), with a named example (`LocateReqd(114)`)? [Clarity, Spec §FR-007a] — PASS: FR-007a(a) states Bool routes through the existing char overload (`args.x ? 'Y' : 'N'`), never int64, with named example `LocateReqd(114)`; contract §G9 repeats the same named example.
- [x] CHK008 Is the Length+Data pair modeled unambiguously as ONE coupled `<Msg>Args` member (the Data value) with the Length auto-derived at emit time — not two independent optionals a caller can desync? [Clarity, Spec §FR-007a] — PASS: FR-007a(b) + data-model §1.1 row specify Length+Data as ONE coupled `std::optional<string_view>` member with Length auto-derived at emit time, explicitly "not two independent optionals a caller can desync".
- [x] CHK009 Is the completeness gate's keying basis unambiguous — set-equality over a MsgType→builder REGISTRY keyed by the MsgType wire string (multi-char `AF/AC/AG/AS` included), explicitly NOT `build_<Identifier>` symbol names (different namespaces)? [Clarity, Spec §FR-004] — PASS: FR-004 states the gate keys on a MsgType→builder REGISTRY (wire strings, multi-char included), explicitly NOT `build_<Identifier>` symbol names — "different namespaces"; contract §G4 repeats verbatim.
- [x] CHK010 Is the `<Msg>Args` member-naming rule specified (`to_accessor` snake_case, collision-uniquified; group member via `strip_no_prefix`), so member identity is not left to the emitter's discretion? [Clarity, Spec §FR-007] — PASS: data-model §1.1 states "Member name = `to_accessor(field_name)` (snake_case, collision-uniquified). Group member = `to_accessor(strip_no_prefix(...))` + span"; FR-007 confirms `to_accessor`/`to_identifier` reuse.
- [x] CHK011 Is "no per-field setter surface and no caller-driven open-group API" stated unambiguously (the caller supplies group entries as a span in `<Msg>Args`)? [Clarity, Spec §FR-001] — PASS: FR-001 states verbatim "there is no per-field setter surface and no caller-driven 'open group' API (the caller supplies group entries as a span in `<Msg>Args`)"; Key Entities section repeats.
- [x] CHK012 Is the required-group vs optional-group representation (`std::span` vs `std::optional<std::span>`) specified WITH its classification rule (the `No<G>` field's dict `rule == Required` vs `!= Required`), not left implicit? [Clarity, Spec §FR-007a] — PASS: data-model §1.1 table rows state the classification rule explicitly ("Classified optional/required by the `No<G>` field's dict `rule != Required` / `rule == Required`").

## Consistency

- [x] CHK013 Is the `validate_<Msg>` / `wire::validate_required<T>()` naming AND its return disposition `error::wire_required_field_missing` (=38) consistent across spec (§FR-006), data-model (§1.3), and contract (§G5)? [Consistency, Spec §FR-006] — PASS: `validate_<Msg>`/`wire::validate_required<T>()` naming and `error::wire_required_field_missing` disposition are identical across spec §FR-006, data-model §1.3, contract §G5; value confirmed =38 at `include/fixpp/core/error.hpp:69`.
- [x] CHK014 Is the field-order two-regime rule (top-level tag-ascending from the tag-sorted `m.fields`; group-entry DECLARATION order from `group_order`) stated identically across spec (§FR-007), data-model (INV-ORDER), contract (§G3), and research (R1/R9)? [Consistency, Spec §FR-007] — PASS: the two-regime order rule (top-level tag-ascending from tag-sorted `m.fields`; group-entry declaration order from `group_order`) is stated identically in spec §FR-007, data-model INV-ORDER, contract §G3, research R1/R9 — cross-checked verbatim across all four.
- [x] CHK015 Is the SET-vs-ORDER source split kept consistent everywhere — the required SET always from `m.fields` filtered by `group_no_tag ∧ rule`, the delimiter+member ORDER always from `MessageIR.group_order` — so `build_` and `validate_` cannot silently diverge? [Consistency, Spec §FR-006a] — PASS: the SET-from-`m.fields`-vs-ORDER-from-`group_order` split is stated identically in FR-006a/FR-007, data-model §2 ("Provenance" + INV-ORDER), and contract §G5's closing bullet, which explicitly names the axis split ("the two are different axes (SET vs ORDER)").
- [x] CHK016 Are all THREE purpose-specific framing/exclusion sets reconciled rather than latent — the INV-2 body-framing set `{8,9,34,49,52,56,10}` (FR-005), the validation exclusion set `{8,9,10,34,35,49,52,56}` which additionally excludes `35` (FR-006a), and the `shape_oracle_profile()` exclusion set `{8,9,10,34,52}` (FR-003/G2) — with each difference (notably `35`, emitted structurally by the ctor) intentional and documented? [Consistency, Conflict] — PASS: the three sets serve different purposes and the one non-trivial delta is documented — INV-2's `{8,9,34,49,52,56,10}` (7 tags, no 35) is what `commit()` must NOT emit, since 35 is legitimately body-emitted (`35=<MsgType>` first, contract §G1); the validation exclusion set adds 35 (research.md R3 note: "35 (MsgType) is in the exclusion set because `body_builder` emits it structurally from its ctor; required-presence for it is always satisfied and not the caller's responsibility" — echoed verbatim in FR-006a and data-model §2.1); `shape_oracle_profile()`'s `{8,9,10,34,52}` is the FROZEN 061 external-golden-diff profile, reused as-is per spec.md Dependencies ("The 061 shape-oracle is frozen and authoritative... this feature does not re-derive them") and contract §G1 preamble — a different comparison domain (full-message byte-diff vs golden, not required-presence), not obligated to align element-for-element with the other two.

## Acceptance Criteria Quality

- [x] CHK017 Is the US1 Independent Test measurable — for each of the 33, a builder compiles, accepts a populated `<Msg>Args`, and emits a body that parses back via `dict::reify()` to the seeded field values under the parameterized witness? [Acceptance Criteria, Spec §SC-003] — PASS: spec.md US1 "Independent Test" states verbatim: for each of the 33, a builder compiles, accepts a populated `<Msg>Args`, and emits a body that parses back via `dict::reify()` to the seeded field values under the parameterized witness.
- [x] CHK018 Is SC-001 stated with an objectively checkable basis (exact-set equality over the registry keys, multi-char included), not "builders exist for the OFFICIAL messages"? [Acceptance Criteria, Spec §SC-001] — PASS: SC-001 states "exact-set completeness gate over the generated MsgType→builder registry passes — keyed by MsgType wire string, multi-char AF/AC/AG/AS included; not by `build_*` identifier symbol names" verbatim.
- [x] CHK019 Is SC-004 scoped precisely — `validate_` fires only at the levels each message actually has (many of the 33 are group-free), with named representatives (D missing `Symbol(55)`; E missing `ClOrdID(11)` in `NoOrders`; W-vs-X `NoMDEntries`) and a zero-required-field clean pass? [Acceptance Criteria, Spec §SC-004] — PASS: SC-004 names representatives verbatim (D missing `Symbol(55)`; E missing `ClOrdID(11)` in `NoOrders`; W-vs-X `NoMDEntries`) plus the zero-required-field clean-pass clause and the "fires only at the levels each message actually has" scoping.

## Scenario & Edge Coverage

- [x] CHK020 Is the "engaged empty span ⇒ `No<G>=0`" vs "`nullopt` ⇒ `No<G>` omitted entirely" distinction specified as a requirement (not collapsible to a plain span), and reconciled with the read-side accept (C3 / B-004-7)? [Coverage, Spec §Edge-Cases] — PASS: spec.md Edge Cases states both cases explicitly ("N==0 optional group" vs "Group entirely absent"), ties the distinction to why the type is `std::optional<std::span>` not a plain span, and cross-refs read-side accept C3/B-004-7.
- [x] CHK021 Is the message-with-no-required-fields case covered as a requirement (`validate_` is a no-op returning success; `build_` always commits on well-formed input)? [Coverage, Spec §Edge-Cases] — PASS: spec.md Edge Cases last bullet states verbatim "Message with no required fields: required-presence routine is a no-op; builder always commits on well-formed input"; tasks.md T022(e) is a direct test pin for this case.
- [x] CHK022 Is the multi-char MsgType write path a stated coverage requirement (`35=AS\x01` first, AS/AF/AC/AG round-trip via 057 dispatch)? [Coverage, Spec §FR-004] — PASS: US2 Acceptance Scenario 3 covers the multi-char path verbatim ("begins `35=AS\x01` and matches both oracles (multi-char MsgType path preserved)"); Dependencies section cites 057 for multi-char MsgType dispatch.

## Dependencies & Assumptions

- [x] CHK023 Is the single-serialization-core dependency stated as a requirement (all builders serialize exclusively through `wire::body_builder`, including its LIFO group API; no second/third serialization or group-write idiom)? [Dependencies, Spec §FR-002] — PASS: FR-002 states verbatim "Generated builders MUST serialize exclusively through the existing `wire::body_builder` core (its LIFO group API included). The feature MUST NOT introduce a second/third serialization or group-write idiom."
- [x] CHK024 Is the shared-helper reuse assumption stated with its guard — `kind_of`/`to_accessor`/`to_identifier` reused (not reimplemented), the per-message group planner NOT the read emitter's version-wide `MemberMap`, with the read-emitter determinism golden as the standing regression guard? [Assumption, Spec §FR-007] — PASS: FR-007 states the shared-vs-per-message split ("Which type/name helpers are shared vs the per-message group planner is settled here... the read emitter's determinism golden stays green as the guard"); plan.md Open Items repeats the same guard; research R7 records the decision table.

## Ambiguities & Conflicts

- [x] CHK025 Is the required-set completeness claim (SC-004: "table-derived at both levels, zero hand-authored lists") provisional-until-census, and is that dependency surfaced — the N3 `append_run` dedup-collapse census (plan §N3 / T009) is UNRUN at spec time, and a collapsed tag switches the affected required set's source to the extended declaration walk? [Ambiguity, Spec §SC-004] — DD-DECIDED §plan.md N3 / research.md R3 caveat / tasks.md T009: the N3 `append_run` dedup-collapse census is UNRUN at spec time but its provisionality IS an explicit, scheduled, mandatory /tasks HARD task (T009 "flag, don't hand-wave") with a defined fallback authority already written into FR-006a, data-model §2.2 Caveat, and contract §G5 ("the codegen-local declaration walk, extended to carry per-member `rule`") — this is a settled conditional-branch design, not an unaddressed gap; Completeness/Consistency-adjacent item closed via frozen-authority traceability ref per pipeline instruction, not re-spec'd.
- [x] CHK026 Is the `writer_traits<Msg>` emitted SHAPE ("fully unrolled `validate_<Msg>` vs thin binding") an acknowledged /implement-deferred decision with the observable contract still fixed (data-model §1.3), so "fully described" does not over-claim a settled internal shape? [Ambiguity, Spec §FR-006] — DD-DECIDED §data-model.md §1.4: the `writer_traits<Msg>` emitted shape ("fully unrolled `validate_<Msg>` vs thin binding") is explicitly marked "decided at /implement" while the OBSERVABLE contract is fixed at §1.3 (`validate_<Msg>` signature + behavior) — a legitimate impl-detail deferral, not an under-specified requirement; recorded as a frozen-authority traceability ref.

## Notes

- Requirements-quality checklist (probes the SPEC/design bundle, not the emitted code). Dispositioned by the pipeline step-9 checklist audit before `/speckit-implement`.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 24 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 2 |
| WAIVED | 0 |
| **Total** | 26 |

### SPEC-FIXED items

None.

### DD-DECIDED items

- CHK025 — anchor `plan.md §N3` / `research.md R3 caveat` / `tasks.md T009`; rationale: N3 dedup-collapse census is unrun at spec time but its provisionality is a scheduled, mandatory HARD `/tasks` item with a defined fallback authority already written into FR-006a/data-model §2.2/contract §G5 — a settled conditional design, not an unaddressed gap.
- CHK026 — anchor `data-model.md §1.4`; rationale: `writer_traits<Msg>` internal shape is explicitly deferred to `/implement` while the observable contract (§1.3) is fixed — legitimate impl-detail deferral.

### WAIVED items

None.

Anchors spot-verified: `dictionaries/FIX44.xml:3023` (MDFullGrp → `MDEntryType(269)` first) / `:3060` (MDIncGrp → `MDUpdateAction(279)` first); `src/dictionary/xml_loader.cpp:695-702` (`append_run` tag-sort + tag-dedup); `include/fixpp/core/error.hpp:69` (`wire_required_field_missing = 38`); `specs/061-typed-app-messages/contracts/builder-shape-oracle.md` C1–C6 present — all resolve in the Gate-A-converged bundle (converged commit `a4cb5624`).
