# Session ResetOn{Logon,Logout,Disconnect} Lifecycle Knobs — Requirements Quality Checklist

**Purpose**: Unit-tests-for-English over the 024 requirements — validate completeness, clarity, consistency, measurability, and coverage of spec/plan/contracts/data-model BEFORE implementation. These test whether the requirements are written correctly, NOT whether the implementation works.
**Created**: 2026-06-06
**Feature**: [spec.md](../spec.md) (session lifecycle seqnum-reset domain; G3 slice 3, completes S-017)

## Requirement Completeness

- [x] CHK001 Are all three knobs' default AND on-behavior fully specified (`reset_on_logon`/`reset_on_logout`/`reset_on_disconnect`, each default `false`, on ⇒ reset both inbound+outbound seqnums to 1 at the named event)? [Completeness, Spec §FR-001/FR-002/FR-004/FR-005, data-model §1] — PASS: spec.md FR-001 names all three knobs with explicit default `false`; FR-002/FR-004/FR-005 each specify the on-behavior ("reset both … to 1") for their respective events; data-model §1 table enumerates all three fields with type/default/meaning. Complete and unambiguous.
- [x] CHK002 Is the all-off behavior specified as a pure no-op (byte/semantics-identical to today, including the existing 013 `ResetSeqNumFlag(141)` flow)? [Completeness, Spec §FR-001/SC-003, research §D4] — PASS: spec.md FR-001 states "with all three off, a session MUST be behavior-identical to today's (zero regression to existing 013 ResetSeqNumFlag(141) behavior and all merged sessions)"; SC-003 is the measurable gate; research D4 grounds the default-false decision. T004(5) is the all-off+inbound-141+store-failure regression witness in tasks.md.
- [x] CHK003 Is the initiator `141=Y` emission predicate fully specified as the OR-of-three (`send_reset_flag || ((reset_on_logon||reset_on_logout||reset_on_disconnect) && {1,1})`)? [Completeness, Spec §FR-007, contract §C2.1, data-model table] — PASS: spec.md FR-007 states the full predicate verbatim, grounded against QFcpp Session.cpp:1037 `shouldSendReset()` and QFJ Session.java:919 `isResetNeeded()`; contract C2.1 repeats it identically; data-model §"Reset-trigger disposition table" initiator row carries the OR-of-three formula including the `{1,1}` guard; tasks.md T007 pins the post-reset live-manager evaluation requirement.
- [x] CHK004 Is the durable-reset helper's per-cause store-failure disposition fully specified (knob-driven Logon = fatal/blocks-Active; existing 013-only received-`141` = I-07 logged-then-proceed; teardown = logged)? [Completeness, Spec §FR-008, research §D2, contract §C2.6] — PASS: spec.md "Key Entities" durable-reset-helper description enumerates all three causes with dispositions; contract C2.6 binds each cause explicitly including the 013-only I-07 path staying unchanged; research D2 grounds the cause-keyed (not location-keyed) design. The all-off I-07 witness is T004(5); the knob-fatal witness is T005(9) — both in tasks.md.
- [x] CHK005 Is the acceptor reset-ordering requirement stated (reset MUST run BEFORE `check_inbound`, since too-low is fatal in handshake state)? [Completeness, Spec §FR-002/FR-003, contract §C2.2, research §D3] — PASS: spec.md FR-002 states "MUST run before inbound-Logon sequence validation (check_inbound, session.cpp:1437)… because in the handshake state a too-low sequence is fatal"; FR-003 elaborates the gap-suppression mechanism; contract C2.2 binds "BEFORE inbound-Logon sequence validation (check_inbound, session.cpp:1437) — NOT after"; research D3 grounds the ordering with QFcpp/QFJ reference. Confirmed: session.cpp:1437 is the real `check_inbound` call site.
- [x] CHK006 Are requirements defined for `reset_on_logout` firing in BOTH directions (Logout sent locally AND Logout received from the peer, the latter arriving via inline `Disconnected`→`close(terminal)`)? [Completeness, Spec §FR-004, contract §C3.1, research §D3] — PASS: spec.md FR-004 states "Logout sent or received → terminal"; contract C3.1 is the binding clause spelling out exactly two sites for `logout_seen_` — (a) `close(close_mode::graceful) session.cpp:880` and (b) inbound `35=5` at `session.cpp:~2095` — and explicitly excludes `close_mode::graceful` alone and `onLogout_fired_`; research D3 grounds the peer-initiated path that never enters `close(graceful)`. T010(2) in tasks.md is the peer-initiated witness.
- [x] CHK007 Is the `reset_on_disconnect` abnormal-drop arm specified (the reset MUST fire on a raw transport EOF/error with no Logout, not only on graceful close)? [Completeness, Spec §FR-005, contract §C4.2] — PASS: spec.md FR-005 states "ANY transport disconnect/teardown, including an abnormal connection drop with no graceful Logout"; contract C4.2 is the binding abnormal-arm clause; data-model table "Any disconnect/teardown (incl. abnormal drop)" row explicitly covers read-pump EOF/error → `close(terminal)`; spec Edge Cases paragraph on `ResetOnDisconnect` re-states "MUST still fire on a raw connection loss." T010(3) in tasks.md is the abnormal-drop witness.
- [x] CHK008 Is the single-fire teardown guard specified (a logout+disconnect double-trigger performs exactly one durable `store_->reset()`)? [Completeness, Spec §FR-009, contract §C5.1] — PASS: spec.md FR-009 specifies the single-fire flag requirement by name with the rationale that `FileStore::reset()` "performs a full atomic-rename + fdatasync + dir-fsync on every call regardless of the current counter value"; contract C5.1 binds "MUST be collapsed by a single-fire guard"; US2 AC3 (spec.md:63) carries the fix-forward language "the durable MessageStore::reset() is not a value no-op and must not run twice; see FR-009"; T010(5) in tasks.md is the double-trigger → exactly-one-store-reset witness with the `reset_call_count()` seam.
- [x] CHK009 Is the gap-suppression requirement specified (after a `reset_on_logon` reset, no ResendRequest for seqnums below the reset point)? [Completeness, Spec §FR-003, contract §C2.5] — PASS: spec.md FR-003 specifies the no-ResendRequest requirement for both initiator and acceptor, explaining the mechanism precisely (acceptor: "the pre-validation reset prevents the too-low disconnect" rather than "a ResendRequest is suppressed"); contract C2.5 binds "MUST NOT issue a ResendRequest for any seqnum below the reset point"; US1 AC3 and the "ResetOnLogon and gap recovery" edge case repeat the requirement; T004(4) in tasks.md is the no-ResendRequest-below-reset witness.
- [x] CHK010 Is the `RefreshOnLogon`/S-018 descope stated explicitly with its rationale (manager not store-seeded at `open()`), so the slice boundary is unambiguous? [Completeness, Spec §Context/Clarifications Q3, contract §C7.1] — PASS: spec.md Context second paragraph + Clarifications Q3 both state the descope with full rationale (`SeqnumManager` always starts at 1; `session.cpp:1423` comment; sole `set_next_inbound` caller is SequenceReset handler at `:3663`; store→manager hydrate-on-open path does not exist); contract C7.1 explicitly records "RefreshOnLogon (S-018) is NOT implemented"; FR-011 reinforces S-018 stays `backlog`; research D6 grounds the decision.

## Requirement Clarity

- [x] CHK011 Is "before `check_inbound`" defined against a concrete code anchor (`session.cpp:1437`) and tied to the fatal-too-low-in-handshake reason, rather than left vague? [Clarity, contract §C2.2, research §D3] — PASS: contract C2.2 cites `session.cpp:1437` explicitly; spec.md FR-002 and FR-003 both state the too-low-fatal-in-handshake rationale; research D3 elaborates "in the handshake (LogonReceived) state a too-low or too-high sequence is fatal (:1438-1441 → record_state_transition_(Disconnected))". Anchor verified: `check_inbound` is at session.cpp:1437 in the live source.
- [x] CHK012 Is the `{1,1}` guard in the 141 predicate defined to evaluate POST-reset live manager state (not a value sampled before the reset / before `peek_outbound`)? [Clarity, Spec §FR-007, tasks §T007, contract §C2.1] — PASS: tasks.md T007 states the `{1,1}` guard "MUST be evaluated against the post-reset live manager state (e.g. `seqnum_mgr_.peek_outbound() == seqnum_min` AND `next_inbound == seqnum_min`) AFTER `reset_seqnums_to_one_durable()` completes — never via a value sampled before the reset (analyze B1)"; plan.md states "after the logon-time reset"; FR-007 establishes the predicate order (`reset_seqnums_to_one_durable()` first, then the flag into `build_logon`).
- [x] CHK013 Is `logout_seen_` defined as a DEDICATED flag set only at the two real Logout sites (local graceful-sent + inbound `35=5`), explicitly NOT derived from `onLogout_fired_` (which fires on any `Active→!Active`)? [Clarity, contract §C3.1, research §D3] — PASS: contract C3.1 names the two exact Logout-specific sites ((a) `close(close_mode::graceful), session.cpp:880` and (b) inbound peer-Logout `35=5` at `session.cpp:~2095`) and explicitly states "MUST NOT be derived from `onLogout_fired_`" with the reason; research D3 RC2 repeats the same with `record_state_transition_` analysis; plan.md and tasks.md T013 both carry "NOT `onLogout_fired_`" with explanation. No artifact derives `logout_seen_` from `onLogout_fired_` — confirmed by grep across all artifacts.
- [x] CHK014 Is the durable reset defined as TWO operations (`reset_to_one()` mutates live counters + a SEPARATE `store_->reset()` for persistence), not conflated into one idempotent primitive? [Clarity, research §D2, contract §C2.6] — PASS: research D2 explicitly states "two operations, not one: `SeqnumManager::reset_to_one()` … mutates only the live counters … persistence is a separate `co_await store_->reset()` call"; spec.md "Key Entities" durable-reset-helper definition states "wraps the existing two operations"; `SeqnumManager::reset_to_one()` confirmed at `seqnum_manager.hpp:113`, `MessageStore::reset()` confirmed virtual pure at `message_store.hpp:133` — both exist and are distinct.
- [x] CHK015 Is the acceptor-overlap collapse defined precisely (a **cause-dependent split** — knob-driven arm and 013-only arm — each → one durable reset with the appropriate disposition)? [Clarity, contract §C2.2 (corrected), data-model precedence rules] — PASS (disposition updated per verify-caught correctness fix): contract C2.2 now describes the **cause-dependent split**: (a) knob-driven (`reset_on_logon == true`): `reset_seqnums_to_one_durable(fatal)` BEFORE `check_inbound` (`session.cpp:1559`); (b) 013-only received-`141` (`peer_sent_reset && !reset_on_logon`): `reset_seqnums_to_one_durable(logged)` AFTER `check_inbound` (`session.cpp:1715`). The two arms are mutually exclusive → exactly one `store_->reset()` per path. The earlier "single combined pre-validation decision" language in spec.md Clarifications Q1 / research D3 / plan.md §Summary was superseded by /speckit-verify catching a byte-identity regression in the unified pre-check reset; those records are annotated as superseded. C2.2 and FR-007 are the authoritative binding anchors.
- [x] CHK016 Is the relationship between knob-driven `141=Y` EMISSION and the existing 013 `reset_seqnum_policy_field` ECHO-VALIDATION stated as orthogonal axes? [Clarity, Spec §Clarifications Q1, research §D1] — PASS: spec.md Clarifications Q1 states "the `013` policy (`bilateral_strict`/`bilateral_lenient`/`unilateral`) then governs how the peer's `141` echo is validated … emission vs echo-validation are orthogonal"; spec.md Assumptions "emission and echo-validation are orthogonal axes"; research D1 explicitly documents "The existing `013` `reset_seqnum_policy_field` continues to govern validation of the peer's `141` echo (emission vs echo-validation are orthogonal axes)"; data-model precedence rule 2 restates this.
- [x] CHK017 Is the teardown reset's placement clear relative to the `close()` drains (before BOTH `write_gate_.cancel_and_drain()` and `seqnum_mgr_.drain()`, so `reset_to_one()` does not hit a drained mutex)? [Clarity, contract §C4.1, tasks §T014] — PASS: contract C4.1 states "The reset MUST run before the SeqnumManager async-mutex drain (~:1002)"; tasks.md T014 states "placed before BOTH drains: before `write_gate_.cancel_and_drain()` (the first drain, ~:997) AND before `seqnum_mgr_.drain()` (~:1013), so `reset_to_one()` does not hit a drained seqnum mutex"; research D3 open risk R4 explains the drained-mutex silent-no-op hazard; plan Complexity Tracking row 3 repeats. The drain calls at session.cpp:997 and :1015 were confirmed in source.

## Requirement Consistency

- [x] CHK018 Is the knob naming consistent (C++ snake_case `reset_on_logon`/etc. vs the QuickFIX cfg-key spelling `ResetOnLogon`/etc.) across spec/plan/data-model/contract, with cfg-key parity scoped to NAMING only? [Consistency, Spec §Clarifications Q2, contract §C1.1/C1.2] — PASS: all artifacts use `reset_on_logon`/`reset_on_logout`/`reset_on_disconnect` (C++ snake_case) for the fields and `ResetOnLogon`/`ResetOnLogout`/`ResetOnDisconnect` (PascalCase) for the QuickFIX cfg keys; spec Clarifications Q2 and Assumptions both state the naming-only scope; contract C1.1 names the three fields, C1.2 explicitly scopes cfg-key parsing as out-of-scope; data-model §1 table has the `QuickFIX cfg key` column; no artifact inconsistently calls a field by the PascalCase key name. research D5 grounds the naming decision.
- [x] CHK019 Is the OR-of-three 141 predicate stated identically across spec FR-007, plan, data-model table, and contract C2.1 (no residual `reset_on_logon`-only form anywhere)? [Consistency, Spec §FR-007, contract §C2.1] — PASS: all four loci state `send_reset_flag || ((reset_on_logon || reset_on_logout || reset_on_disconnect) && seqnums=={1,1})` — checked spec.md FR-007, plan.md §Summary and Complexity Tracking, data-model §"Reset-trigger disposition table" initiator row, contract C2.1. No single-knob or `reset_on_logon`-only form found in any artifact. The `reset_on_logout`/`reset_on_disconnect` arms' role (next-Logon after teardown) is explained consistently in research D1, plan, and data-model precedence rule 2.
- [x] CHK020 Is the cause-keyed disposition split stated consistently across research/data-model/contract/plan (no site claiming the existing 013 received-`141` reset became fatal)? [Consistency, research §D2, contract §C2.6] — PASS: research D2 is the authoritative source ("013-only received-`141`… logged-then-proceed (I-07) — UNCHANGED from today"); contract C2.6 repeats it and explicitly guards "The 024 fatal disposition MUST NOT regress this path"; spec.md FR-001 guarantees all-off byte-identity; data-model precedence rule 1 states "013-only received-`141` with knobs off → I-07 logged-then-proceed, UNCHANGED from today"; plan.md §Summary confirms. No artifact claims the 013 path became fatal. T004(5) is the regression witness in tasks.md.
- [x] CHK021 Is the `RefreshOnLogon`/S-018 descope consistent across ALL artifacts (no residual "four knobs", S-018-in-scope, or refresh witness)? [Consistency, Spec §Context, contract §C7.1] — PASS: full artifact grep found no "four knobs" text and no S-018-in-scope claim; spec.md Context, Clarifications Q3, Assumptions, and FR-011 all state descope consistently; plan.md Article VI row states "S-018 stays backlog"; contract C7.1 records the out-of-scope status; tasks.md notes `RefreshOnLogon / S-018 is OUT of scope` twice (scope reality block and Notes section). No refresh witness exists. The branch name contains "refresh" but this is recorded in plan.md Gate A Round 1 disagreements as a non-actionable P3 — not a spec artifact defect.
- [x] CHK022 Is `FileStore::reset()` consistently characterized as non-idempotent durable I/O (so the single-fire guard and single-combined-reset are justified the same way everywhere)? [Consistency, research §D2/D3, contract §C5.1, data-model precedence] — PASS: the "full atomic-rename + fdatasync + dir-fsync on every call" characterization appears consistently in research D2, D3, spec.md FR-009, spec.md FR-007 (Key Entities), spec.md US2 AC3, contract C2.2, contract C5.1, data-model precedence rule 1 and rule 3, and plan.md Complexity Tracking. All artifacts give the same rationale ("NOT a value no-op") for the single-fire guard and single-combined-decision requirements.

## Acceptance Criteria Quality & Measurability

- [x] CHK023 Are the success criteria measurable without implementation detail (e.g., "100% of Logons from non-1 seqnums leave {1,1} persisted")? [Measurability, Spec §SC-001/SC-002] — PASS: SC-001 states "100% of Logons from a session with non-1 persisted sequence numbers leave both sequence numbers at 1 (persisted), reach Active, and emit no ResendRequest" — observable via wire capture + store read + FSM state; SC-002 states "100% of logouts/disconnects … leave the persisted sequence numbers at 1" — observable via store read after teardown; SC-003 states "byte/semantics-identical to the pre-feature baseline (zero regression)" — observable via existing test suite unchanged; SC-004 and SC-005 are similarly output-observable. No success criterion refers to internal implementation details.
- [x] CHK024 Is SC-004 (initiator emits `141=Y` when seqnums `{1,1}`) tied to an objectively checkable artifact (the captured outbound Logon bytes)? [Measurability, Spec §SC-004, contract §C2.1] — PASS: SC-004 states "An initiator … emits `ResetSeqNumFlag(141)=Y` on its outbound Logon in 100% of cases where seqnums are `{1,1}`"; tasks.md T004(1) specifies the witness using `extract_field`/`peek_outbound` to assert the presence of `141=Y` in the captured outbound Logon bytes — an objectively checkable artifact. Contract C2.1 binds the predicate. The outbound-Logon capture approach matches the 021/022 pattern using `session_test_support` fixtures.
- [x] CHK025 Is the single-store-reset claim (C5.1) measurable against a concrete signal (exactly one observable `store_->reset()` I/O via a counter seam)? [Measurability, Spec §FR-009, contract §C5.1] — PASS: tasks.md T001 specifies the `StoreDouble::reset_call_count()` seam (a counter incremented in `reset()`); T005(7) and T010(5) assert "exactly one observable `store_->reset()` I/O (via the T001 `reset_call_count()` seam)" for the acceptor overlap and the double-trigger cases respectively. The seam is a new addition to `tests/support/store_double.hpp` — not assumed to exist already, but explicitly specified in T001.
- [x] CHK026 Is SC-003 (all-off byte/semantics-identity) measurable as a concrete regression gate (existing session+seqnum suites unchanged + the all-off+inbound-141+store-failure→still-Active witness)? [Measurability, Spec §SC-003, contract §C2.6] — PASS: SC-003 stated in spec.md; tasks.md T004(5) is the dedicated all-off+inbound-141+store-failure→still-Active witness named "ResetOnLogon_Off_Inbound141_StoreFailure_StillActive"; tasks.md T009 includes "`ctest -L 'session|seqnum'` no regression" as the gate; the witness asserts the existing 013 I-07 path is unchanged by the 024 fatal disposition. No ambiguity about what "byte/semantics-identical" means in this context.
- [x] CHK027 Does each US acceptance scenario state a Given/When/Then evaluable from observable wire/persisted-state output? [Acceptance Criteria, Spec §US1/US2] — PASS: all 8 acceptance scenarios (US1 ACs 1-4; US2 ACs 1-4) use Given/When/Then form; the "Then" clauses reference observable outputs: persisted counter values ("both sequence numbers are reset to 1 and the reset is persisted"), FSM state ("reaches Active"), wire content ("carries `ResetSeqNumFlag(141)=Y`"), and absence of wire messages ("no ResendRequest"). No AC relies on internal state invisible at the test boundary.

## Scenario & Edge-Case Coverage

- [x] CHK028 Are requirements defined for the acceptor fresh-`34=1`-at-local-expected>1 case (admitted, no too-low disconnect, no ResendRequest)? [Coverage, Spec §FR-002/FR-003, contract §C2.2] — PASS: spec.md FR-002 and FR-003 both address this case; FR-003 states "check_inbound sees next_inbound_==1, so a fresh peer Logon 34=1 is admitted with no too-low disconnect and no ResendRequest"; contract C2.2 binds the pre-validation placement; tasks.md T005(6) is the named witness "ResetOnLogon_Acceptor_AdmitsFresh34eq1_LocalExpectedGt1" that directly tests this case.
- [x] CHK029 Are the local-initiated AND peer-initiated Logout cases BOTH covered as distinct `reset_on_logout` requirements? [Coverage, contract §C3.1] — PASS: contract C3.1 binds both directions; spec.md FR-004 states "Logout sent or received → terminal"; US2 AC1 covers both ("Logout sent or received → terminal"); tasks.md T010(1) is the local-initiated witness and T010(2) is the peer-initiated witness as distinct named test cases. The peer-initiated path that never enters `close(graceful)` is explicitly called out in the requirement.
- [x] CHK030 Is the abnormal-drop (no Logout) case covered as a distinct `reset_on_disconnect` requirement vs the graceful case? [Coverage, contract §C4.1/C4.2] — PASS: contract C4.2 is the dedicated "abnormal arm" clause; spec.md FR-005 states "including an abnormal connection drop with no graceful Logout"; spec Edge Cases paragraph re-states "MUST still fire on a raw connection loss, not only on a graceful Logout (this is what distinguishes it from `ResetOnLogout`)"; tasks.md T010(3) is the dedicated "ResetOnDisconnect_AbnormalDrop_Resets" witness exercising raw transport EOF specifically.
- [x] CHK031 Is the logout/disconnect-reset → `141=Y`-on-next-initiator-Logon case covered (the OR-of-three arm for the non-`reset_on_logon` knobs)? [Coverage, Spec §FR-007, contract §C2.1, data-model precedence rule 2] — PASS: spec.md FR-007 explicitly states "the `reset_on_logout`/`reset_on_disconnect` arms matter because a session … that reset to `{1,1}` at a prior teardown MUST announce that reset with `141=Y` on its next initiator Logon"; contract C2.1 repeats "Consequently a session with `reset_on_logout` or `reset_on_disconnect` … MUST also set `141=Y` on its NEXT initiator Logon while seqnums are `{1,1}`"; data-model precedence rule 2 covers it; tasks.md T011(7) and T011(8) are the named witnesses for `reset_on_logout` and `reset_on_disconnect` respectively.
- [x] CHK032 Is the `reset_on_logon`-under-every-`reset_seqnum_policy_field` case covered (strict/lenient/unilateral all log on cleanly)? [Coverage, contract §C2.4] — PASS: contract C2.4 binds "MUST log on cleanly under every `reset_seqnum_policy_field` value"; spec.md FR-010 says all knobs must work; tasks.md T004(3) is the explicit "ResetOnLogon_AllPolicies_LogonClean" witness iterating over {strict, lenient, unilateral}; research D2/D1 explain why the knob-driven `141=Y` emission and policy-driven echo validation compose without wedging (orthogonal axes).
- [x] CHK033 Is role-symmetry (initiator AND acceptor) stated as a requirement for all three knobs, not assumed? [Coverage, Spec §FR-010, contract §C5.2] — PASS: spec.md FR-010 states "All three knobs MUST behave identically whether fixpp is the initiator or the acceptor"; spec Edge Cases ends with "Both roles: every knob's behavior MUST hold"; contract C5.2 is the dedicated "both roles" clause witnessed per knob; tasks.md T004/T005 split initiator and acceptor as distinct test classes for US1; T010(6) covers both roles for US2; T008 covers T005(8) "BothRoles_Resets."
- [x] CHK034 Is the all-off zero-regression case explicitly covered including the existing 013 received-`141` path under a store-reset failure (still reaches `Active`)? [Coverage, Spec §FR-001, contract §C2.6] — PASS: spec.md FR-001 guarantees all-off byte-identity; contract C2.6 names the I-07 path and states the session "MUST still reach Active on a tolerated store failure"; tasks.md T004(5) is the dedicated named witness "ResetOnLogon_Off_Inbound141_StoreFailure_StillActive" — knobs OFF, inbound `141=Y`, forced `store_->reset()` failure → session still reaches Active. This is the zero-regression guard for the cause-keyed disposition split, explicitly flagged in tasks.md Notes as "must stay GREEN through T008."

## Interop Requirements Quality

- [x] CHK035 Do the interop requirements specify both counterparty engines (QFcpp v1.16.0, QFJ 3.0.1) AND both roles (fixpp-initiator C6.1 + fixpp-acceptor C6.2), with the skip-without-counterparty contract? [Completeness, Spec §FR-010/SC-005, contract §C6.1/C6.2] — PASS: contract C6.1 names "live QuickFIX-cpp v1.16.0 and QuickFIX-J 3.0.1 acceptors" for the initiator role; C6.2 names the same engines for the acceptor role; both clauses together are noted as satisfying "FR-010/SC-005 both-roles live-interop requirement"; tasks.md T016 and T017 each carry the skip-without-counterparty guard phrasing "use the SAME skip-without-counterparty guard as the 018 cells." SC-005 binds "both initiator and acceptor roles."
- [x] CHK036 Is the fixpp-acceptor interop assertion objectively stated (a live QF initiator sends `141=Y`+fresh `34=1`; fixpp resets-before-validation and accepts, resync from 1)? [Clarity, contract §C6.2] — PASS: contract C6.2 states the assertion verbatim: "a live QFcpp/QFJ initiator sends a Logon with `141=Y` + fresh `34=1`; fixpp acceptor … resets-before-validation and accepts (no disconnect, no ResendRequest), reaches Active, and the dialogue resyncs from seqnum 1"; tasks.md T017 mirrors this assertion with the same specific details. The assertion is entirely in terms of observable wire/FSM outputs.

## Dependencies & Assumptions

- [x] CHK037 Is the reuse of the existing 013 reset primitive (`SeqnumManager::reset_to_one()` + `MessageStore::reset()`) stated, with no parallel seqnum path introduced? [Assumption, Spec §FR-006/FR-008, research §D2] — PASS: spec.md FR-006 states "MUST share the same single reset primitive as the existing 013 ResetSeqNumFlag(141) handshake reset (no parallel seqnum path)"; FR-008 states "Every reset MUST go through the existing SeqnumManager + MessageStore reset/persist primitives — no new sequence-number storage"; research D2 grounds the reuse decision; plan.md §Summary states "The reset reuses the existing `013` reset primitive"; both symbols confirmed in live source (`seqnum_manager.hpp:113` for `reset_to_one()`, `message_store.hpp:133` for `reset()`).
- [x] CHK038 Is the "no new persistent store / no codegen / no new error slot / no new wire field (141 already emitted by 013)" boundary stated and bounded? [Assumption, Spec §Assumptions, plan Constitution §X] — PASS: spec.md Assumptions states "No new persistent store, codegen, error slot, or wire field is anticipated (the `141` field is already emitted by `013`)"; plan.md §Summary and plan Article X row both confirm; contract C1.1 confirms "No other public surface is added (no error slot, no new method, no codegen)"; plan Complexity Tracking concludes "No new module, codegen, error slot, or C-ABI"; the `141` field reuse is confirmed at session.cpp:523-526 (existing bilateral_strict emission path) and the spec extends only the predicate.
- [x] CHK039 Is the `cfg_loader` NON-extension stated explicitly (this slice adds the C++ fields only; parsing the cfg-keys is an out-of-scope follow-up)? [Assumption, Spec §Clarifications Q2, contract §C1.2] — PASS: spec.md Clarifications Q2 states "This slice does NOT parse those keys in the `008` `cfg_loader`"; spec.md Assumptions explicitly states "this slice does not add those keys to the `008` `cfg_loader`"; contract C1.2 is the dedicated "out of scope — cfg-key parsing" clause; plan.md §Summary states "C++ fields ONLY." The cfg_loader restriction is stated consistently across four separate artifact sections.
- [x] CHK040 Is the `RefreshOnLogon`/S-018 descope grounded against the real source (manager starts at 1; `set_next_inbound` only called from the inbound SequenceReset handler; `open()` mints but never reads store counters)? [Assumption, research §D6, contract §C7.1] — PASS: research D6 is the authoritative grounding with all three specific facts: "session.cpp:1423 comment 'seqnum_mgr_ starts at 1'"; "sole `set_next_inbound` caller is the inbound SequenceReset handler :3663"; "`open()`'s store resolution :700-711 mints the store but never reads its counters into the manager"; spec.md Clarifications Q3 repeats the first two cites verbatim. All three anchors verified in live source: `session.cpp:1423` contains the comment; `set_next_inbound` caller at session.cpp:3663 confirmed; `check_inbound` call at :1437.

## Ambiguities & Conflicts

- [x] CHK041 Does any artifact still carry the superseded "acceptor reset invoked twice / shared and idempotent" model that conflicts with the single-combined-reset fix (a Gate-A round-3 fix-forward)? [Conflict, Spec §Clarifications Q1, plan Gate A] — PASS: no stale text found. Grep across all artifacts for "invoked twice" and "shared and idempotent" returned zero hits in spec.md (and zero in any other spec artifact). The fix-forward confirmed by plan.md Gate A Round 3 narrative: "rewrote the Clarifications Q1 acceptor clause (spec.md:28) from the superseded 'invoked twice / shared and idempotent' model to the single-combined `need_logon_reset` decision." spec.md:28 (read verbatim) confirms the single-combined-reset language is present. No conflict persists.
- [x] CHK042 Does any artifact still carry a "second reset onto an already-1 state is a no-op" claim that conflicts with the non-idempotent-store / single-fire-guard requirement (FR-009)? [Conflict, Spec §US2, contract §C5.1] — SPEC-FIXED (orchestrator spot-check): US2 AC3 (spec.md:63) was already correct (single-fire-guard framing). The Edge Cases "Reset idempotency" bullet (spec.md:71) still led with "applying a reset onto an already-`{1,1}` sequence state is a no-op" — defensible as referring to the counter-value result, but the same loose-phrasing class that triggered the Gate-A round-3 fix-forward. Tightened spec.md:71 to lead with "the sequence-number *result* … is unchanged, but the durable `MessageStore::reset()` is NOT a value no-op (FR-009) — so overlapping triggers MUST reset at most once observably via the single-fire guard." Now no artifact carries a bare "is a no-op" claim; consistent with FR-009 / US2 AC3 / the durable-helper entity.
- [x] CHK043 Does any artifact derive `logout_seen` from `onLogout_fired_` (which would collapse `reset_on_logout` into `reset_on_disconnect`)? [Conflict, contract §C3.1, plan Gate A r2] — PASS: no artifact derives `logout_seen_` from `onLogout_fired_`. Grep across all artifacts for "onLogout_fired_" found it only in: plan.md (correctly states "NOT `onLogout_fired_`" with explanation), research D3 (same exclusion with source analysis), contract C3.1 (explicit "MUST NOT be derived from `onLogout_fired_`"), tasks.md T013 ("NOT derived from `onLogout_fired_`"). Every occurrence is a prohibition, not a derivation. The fix was applied in Gate A Round 2.
- [x] CHK044 Are the normative references aligned to the catalogue S-017 row (`[FIX-SL §4.4]` primary), with `[FIX-SL §4.5.3]/§4.5.5` clearly labeled secondary-explanatory (not claimed as the catalogue match)? [Traceability, Spec §Normative References, catalogue §S-017] — PASS: spec.md Normative References section leads with `[FIX-SL §4.4] Extended features for FIX session initiation` labeled "catalogue-match primary (the authoritative S-017 row, spec/feature-catalogue.md:37, cites this section)"; the §4.5.3 and §4.5.5 entries are labeled "*(secondary, explanatory)*" and explicitly noted "these describe the mechanism but are not the catalogue citation." The catalogue at `spec/feature-catalogue.md:37` confirms S-017's Spec ref is `[FIX-SL §4.4] Extended features for FIX session initiation` — anchors align exactly.
- [x] CHK045 Are all `[FIX-SL §X]` / `[const §X]` / `session.cpp:NNN` references exact (not vague), per `[const §VI.5]`? [Traceability, Spec §Normative References] — PASS: all cited `session.cpp:NNN` anchors verified against live source — `session.cpp:1437` (`check_inbound`), `:522` (`peek_outbound`), `:523-526` (bilateral_strict 141 flag), `:1584` (existing 141-receipt reset), `:880` (close graceful), `~:2095-2174` (peer-Logout inline transition), `~:935-1025` (close() teardown region), `:175-191` (`onLogout_fired_`), `:1423` (seqnum_mgr_ starts at 1), `:3663` (`set_next_inbound`). All resolve. `[const §VI.5]` and `[const §XII.5]` are established project conventions (verified in constitution). `[FIX-SL §4.4]`, `[FIX-SL §4.5.3]`, `[FIX-SL §4.5.5]` match the catalogue's Spec-ref format for the authoritative external standard. No vague anchors.

---

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 44 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **45** |

### SPEC-FIXED items

- **CHK042** (orchestrator spot-check) — `spec.md:71` Edge Cases "Reset idempotency" bullet led with a bare "applying a reset onto an already-`{1,1}` sequence state is a no-op" (the loose-phrasing class that triggered the Gate-A round-3 fix-forward at the sibling line :63). Tightened to lead with the sequence-number-*result* framing + the explicit "durable `MessageStore::reset()` is NOT a value no-op (FR-009) → single-fire guard" caveat. No requirement/SC/task/entity changed — a non-binding Edge Cases summary's wording only; the binding FR-009 / US2 AC3 / durable-helper entity were already correct. **`/speckit-analyze` re-run NOT required** (no FR/SC/task mapping or cross-artifact relationship changed; the prior drift check remains valid).

### DD-DECIDED items

None.

### WAIVED items

None.

Anchors spot-verified:

- `session.cpp:1437` — `check_inbound` call confirmed in live source.
- `session.cpp:522` — `peek_outbound()` call confirmed in `open()` initiator arm.
- `session.cpp:523-526` — `bilateral_strict` → `send_reset_flag` confirmed.
- `session.cpp:1584` — existing `141`-receipt reset (store_rst_r) confirmed.
- `session.cpp:880` — referenced as `close(graceful)` local-Logout-sent site; plan anchor.
- `session.cpp:~2095-2174` — peer-Logout inline transition; approximate anchor as documented in plan/research.
- `session.cpp:997` — `write_gate_.cancel_and_drain()` confirmed.
- `session.cpp:1015` — `seqnum_mgr_.drain()` confirmed.
- `session.cpp:1423` — comment "seqnum_mgr_ starts at 1" confirmed.
- `session.cpp:3663` — `set_next_inbound` confirmed as sole caller (inbound SequenceReset handler).
- `session.cpp:175-191` — `onLogout_fired_` / `record_state_transition_` region; approximate anchor as documented.
- `seqnum_manager.hpp:113` — `reset_to_one()` declaration confirmed.
- `message_store.hpp:133` — `reset()` pure-virtual declaration confirmed.
- `spec/feature-catalogue.md:37` — S-017 row with `[FIX-SL §4.4]` Spec ref confirmed.
- `[const §VI.5]` — constitution Article VI §5 (Normative References requirement) confirmed.
- `[const §XII.5]` — established project-wide convention for "no-implicit-default"; constitution Article XII §5 is the TLS-profile no-default rule; the broader convention is confirmed by consistent use across 008/012/016/021/022 specs.
- `[FIX-SL §4.4]`, `[FIX-SL §4.5.3]`, `[FIX-SL §4.5.5]` — external standard sections; format matches catalogue canonical form; primary S-017 anchor aligns with catalogue:37 Spec ref exactly.

Anchors spot-verified against live source and catalogue — all resolve in the current signed-off revision (research.md D1–D6 + contracts/reset-knobs.md C1–C7 as the feature-local authority, in lieu of a separate Phase-2 design-doc anchor).
