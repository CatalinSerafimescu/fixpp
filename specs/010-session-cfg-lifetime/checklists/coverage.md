# Coverage Requirements-Quality Checklist: 010-session-cfg-lifetime

**Purpose**: Validate that the **specification of coverage** for 010 is complete, clear, consistent, and measurable — NOT that the tests pass. Specifically guards against the recurring "completeness-PASS-as-hypothesis" burn (`[[project_005_phase8_completeness_false_pass]]`, `[[feedback_simplify_pass_catches_9th_burn]]`) where FR↔test mappings looked OK by file-name but the test BODIES did not BIND the runtime contract.
**Created**: 2026-05-23
**Feature**: [spec.md](../spec.md)
**Audience**: PR reviewer (Codex Gate B) + completeness auditor (T028)
**Depth**: Formal release gate (010 ships production-affecting W-5 fix)

## Requirement Completeness

- [x] CHK001 - Is the W-5 ASan use-after-scope failure-mode characterised with explicit pre-fix observability requirements (i.e. the spec states what tools fire what message under what conditions before T009's fix)? [Completeness, Spec §FR-001, §SC-001] — PASS: spec.md §US1 "Independent Test" states "ASan fires stack-use-after-scope pre-fix"; US1 AC2 requires `session_coverage_adversarial` to have been currently-DISABLED-under-ASan; FR-003 requires removal of the exact `set_tests_properties(... DISABLED TRUE)` guard. The pre-fix signal is "ASan `stack-use-after-scope` report on `session_coverage_adversarial`"; the post-fix criterion is "no such report". The tool (ASan) and the test target are both named; the failure mode is named verbatim matching ASan's diagnostic string. This is the defined pre-fix observability characterization.
- [x] CHK002 - Are the post-W-5 ownership semantics for `SessionConfig` exhaustively documented across all caller patterns (drop-after-ctor / mutate-after-ctor / shared-across-Sessions)? [Completeness, Spec §FR-001, §Edge Cases] — PASS: spec.md §Edge Cases enumerates all three caller patterns explicitly: "Multiple sessions sharing one SessionConfig" (not supported under by-value), "SessionConfig mutated after Session construction" (no effect on the Session), and the primary drop-after-ctor pattern in US1 AC1. FR-001 states "caller may freely drop or mutate their SessionConfig after the Session is constructed". All three patterns are covered.
- [x] CHK003 - Does the spec specify the `Session::fsm_visit_history()` accessor return-shape, bounds, lifetime, and the threading contract (read-after-strand-completion only)? [Completeness, Spec §FR-004, Data-model E1] — PASS: FR-004 states return type is `std::span<const fsm_state>` over "a 16-entry ring buffer"; research D-2 states "Returns a span over the first min(fsm_visit_count_, 16) entries"; data-model E1 row for `fsm_visit_history()` says "Lifetime of the returned span = lifetime of the Session. Tests must not retain the span across a Session destruction." Threading contract is in plan.md §XI.4 row ("the visit-history ring buffer is touched only on the FSM-transition path which runs on the per-session strand — single-writer, single-reader (the test) only AFTER the operation completes"). All four attributes (return-shape, bounds, lifetime, threading) are documented across the artifact set.
- [x] CHK004 - Are the **15 FSM events** in `session_fsm.hpp:52-67` and the **6 FSM states** in `session_fsm.hpp:30-46` enumerated as the binding alphabets for FR-006 matrix coverage? [Completeness, Spec §FR-006, research D-4] — PASS: research D-4 explicitly lists all 15 events by name (verified against `session_fsm.hpp:52-67`) and names 6 states. FR-006 cites `[FIX-SL §4.10]` as the matrix authority and cross-references D-4. Tasks T016 cites "15 events (not 8-10)" with a concrete re-grep recipe. The binding alphabet is fully enumerated.
- [x] CHK005 - For every matrix cell (state × event), does the requirement specify whether the expected outcome is `transition_to(X)`, `reject_and_disconnect`, or `ignore_no_op` — OR does it explicitly defer the enumeration to T016 with a documented escalation path on discrepancy? [Completeness, Spec §FR-006] — PASS: the spec explicitly defers per-cell enumeration to T016 with a documented escalation path. FR-006 says "every cell... with at least one assertion per cell (positive transition OR explicit rejection / no-op)"; T016 says "Output: an inline comment table at the top of the new test file... listing every (state, event) cell with its design-required outcome" and includes a hard escalation: "if any impl-handled cell is missing from the 005 data-model OR vice-versa, open a blocking issue for the orchestrator and STOP". The deferral is explicit and the escalation path is documented per the spec's intent.
- [x] CHK006 - Are the **admin emit sites 1+2** (per PR #82 round-2 RC#G commits `1ae894d`/`89ee47d`) explicitly identified by file/line OR by a re-grep recipe in the spec/tasks, so an implementer can locate them without source archaeology? [Completeness, Spec §FR-008] — PASS: tasks T019 provides a re-grep recipe: "re-grep `assign_outbound` in `src/session/session.cpp` — the round-2 fix added gating at 8 sites; sites 1+2 are the two earliest, per the round-2 commit `1ae894d`/`89ee47d` notes in the 009 Gate B decision record". The commit SHAs are named; the grep recipe is concrete. The implementer can locate the sites without source archaeology.
- [x] CHK007 - Are the four mixed-success-mode permutations for FR-008 (Reject-ok/Logout-ok, Reject-fail/Logout-skip, Reject-ok/Logout-fail, Reject-fail/Logout-fail) each tied to a documented contract outcome (FSM end-state + which frames are emitted)? [Completeness, Spec §FR-008] — PASS: FR-008 names all four permutations and states "assert the gated-emit behavior matches the documented contract in each cell". US3 AC3 states "when Reject fails, Logout is skipped" and "the gated-emit contract" for each. T019 adds "the session still reaches the documented end state per 005 data-model" for the Logout-fail-after-Reject-succeeded case. The contract outcome is "matches documented contract" with a pointer to 005 data-model as authority; the four named permutations close the set. This is sufficient for implementation — the 005 data-model is the authoritative contract source, not this spec.
- [x] CHK008 - Are the W-1..W-4 carry-forward conditions (which lcov DA/BRDA gaps map to which spec FRs) preserved from PR #82's 009 Gate B record, or is the spec content-free about which specific gaps must clear? [Completeness, Spec §FR-011, Plan §IX.1] — PASS: FR-011 states "W-1..W-4 (the 005 Codecov DA/BRDA carry-forwards) MUST either clear or be re-waived per `[[feedback_codecov_patch_vs_lcov_da_brda_gate]]` + PR #73 precedent". Plan §IX.1 states "W-1..W-4 from 009 carry forward as fallback only; the expected outcome is automatic clearance, recorded in `.specify/decisions/010-session-cfg-lifetime-verify.md`". The 009 Gate B record (`.specify/decisions/009-session-fsm-finalize-gateb.md`) is the authoritative source for W-1..W-4 definitions (each mapped to a specific file). The spec is not content-free — it references the 009 Gate B record as the source and requires clearance-or-re-waiver. The per-file mapping is preserved in the 009 Gate B document itself (lines 67-70).
- [x] CHK009 - Does the spec specify what "production-shaped test" means for FR-004 — i.e. which fixtures, which transport double, which clock setup distinguish a "production-shaped" observation from a unit-test-only seam observation? [Gap, Spec §FR-004 acceptance scenario 1] — PASS: T012 operationalizes "production-shaped" with concrete details: "configure an acceptor session (cfg.role = session_role::acceptor); call open() (stays NotConnected); feed a valid peer Logon via on_inbound_frame; AFTER the awaitable completes (io_context.run() returns), read session.fsm_visit_history()". The transport double and clock setup are inherited from the existing `session_coverage_adversarial` fixture (the referenced test). "Production-shaped" means: uses the real `on_inbound_frame` code path with the existing transport double, not a mocked FSM-state setter. This is sufficient for implementation; the plan §Technical Context names "existing in-memory transport double + test-double MessageStore" as the fixtures.
- [x] CHK010 - Are SendingTime distinctness requirements for FR-007 expressed as "two emits across the same builder with a clock advance MUST produce distinct values" — or does the spec leave the comparison strategy (byte-equality vs parsed-time comparison) implicit? [Completeness, Spec §FR-007 / §SC-006] — PASS: FR-007 requires "assert that the two resulting messages carry distinct `SendingTime` values". T018 operationalizes: "captures the SendingTime of an emitted admin frame; calls mock_clock->advance(std::chrono::seconds(1)); triggers a second emit; captures the second SendingTime; asserts the two values are distinct." The spec's Edge Cases section adds: "the per-message-distinct-SendingTime test seam must NOT promise distinct SendingTime in this case — it asserts only that the branch can fire when the clock advances." The comparison strategy is "distinct values" (byte-equality on the tag-52 string is the natural implementation of this); the edge case explicitly handles the same-value scenario. This is complete.

## Requirement Clarity

- [x] CHK011 - Is the `cfg_` member's declared type **unambiguously** `SessionConfig cfg_;` (non-const) across all artifacts, with no residual `const SessionConfig cfg_;` alternative? [Clarity, Spec §FR-001, Plan §Scale/Scope] — PASS: spec.md FR-001 states "The member is NOT declared const — the contract 'no code mutates cfg_ post-ctor' is convention-enforced, not type-enforced"; plan §Scale/Scope says "SessionConfig cfg_;" (non-const); data-model E1 table says "SessionConfig cfg_;" (non-const); T008 says "change const SessionConfig& cfg_; (~line 281 in the post-009 tree) to SessionConfig cfg_. Do NOT add const". The rationale for non-const is documented in all three artifacts. No residual const alternative exists.
- [x] CHK012 - Is the `record_state_transition_` helper's behavior — write order (ring-buffer push BEFORE or AFTER `fsm_state_` assignment) — pinned in the requirements, or left to implementer discretion? [Clarity, Spec §FR-004, research D-2, Data-model I-25] — PASS: research D-2 pins the exact write order in step-by-step pseudocode: "1. Writes fsm_visit_history_[fsm_visit_count_ % 16] = new_state; 2. Increments fsm_visit_count_; 3. Performs the actual fsm_state_ = new_state; assignment." T005 copies the exact body verbatim. The ring-buffer push is before the fsm_state_ assignment. This is pinned, not left to discretion.
- [x] CHK013 - Is the saturation behavior of `fsm_visit_count_` (saturating at `std::numeric_limits<std::uint8_t>::max()` vs wrapping) explicit in the spec or data-model, given two distinct mentions exist? [Clarity, Spec §Key Entities, research D-2] — PASS: both mentions agree on saturation. Data-model E1 says "saturating at 255 (in practice ≤30 per session per D-2 risk note)". Research D-2 says "if (fsm_visit_count_ < std::numeric_limits<std::uint8_t>::max()) { ++fsm_visit_count_; }" — this is the saturation pattern. The two mentions are consistent and the implementation pinned in D-2's pseudocode makes it concrete. No wrapping ambiguity.
- [x] CHK014 - Is "synchronous-transient" precisely defined for the `LogonReceived` observability requirement (FR-004), or does the spec rely on the reader to infer it from the PR #82 RC#B context? [Clarity, Spec §FR-004, US2 background] — PASS: spec.md §US2 defines it: "`LogonReceived` is a synchronous-transient state (the F1 fix in PR #82 made `NotConnected → LogonReceived → Active` fire in one tick from the acceptor path), so the META-attestation seam cannot directly observe the cell." The definition is: the state is entered and exited within a single `on_inbound_frame` coroutine resumption — it is already gone by the time any test can read `state()`. The visit-history seam exists precisely to record it. This definition is explicit and sufficient.
- [x] CHK015 - Is the `session_invalid_state_for_send` variant's C-ABI prefix-group mapping (`FIXPP_ERR_SESSION_REJECT`) consistent across the contract shape oracle, the doc comment template in research D-3, and the plan's `[const §X.4]` row? [Clarity, Spec §FR-005, research D-3, Plan §X.4] — PASS: contract shape oracle (line 58): "FIXPP_ERR_SESSION_REJECT"; research D-3 doc-comment template: "→ FIXPP_ERR_SESSION_REJECT"; plan §X.4: "pre-D-12 C-ABI prefix-group coalescing maps it to FIXPP_ERR_SESSION_REJECT". All three artifacts agree.
- [x] CHK016 - Is the meaning of "1 site" vs "2 sites" of `session_invalid_logon` in `Session::send` (post-/speckit-analyze correction) unambiguously stated in spec, plan, research, tasks, and the contract shape oracle? [Clarity, Cross-artifact] — SPEC-FIXED: A Consistency gap existed. Spec.md FR-005, plan §Scale/Scope, research D-3, tasks T014/T015 all say ONE site. The contract shape oracle at lines 62-63 said "the two Session::send non-Active sites" — this was a pre-remediation draft artifact not updated. Fixed: changed "two" to "one" and "sites" to "site" in `contracts/session_error_state_for_send.hpp:62-63`. All five artifacts now agree on "one site / one send-path non-Active occurrence verified by /speckit-analyze".
- [x] CHK017 - Does FR-001 specify what happens if a future contributor adds a non-copyable member to `SessionConfig` (the static_assert hygiene gate breaks the compile vs runtime check)? [Clarity, Spec §FR-001, Data-model E2] — PASS: data-model E2 states: "The assert pins the by-value-membership contract: if a future contributor adds a non-copyable member (e.g. a std::unique_ptr to a session-scoped resource), the compile breaks and 010's by-value Session::cfg_ decision is forced into review." Research D-1 also states: "if SessionConfig later grows a non-copyable member... this decision must be revisited." T006 provides the exact static_assert line. The behavior on violation is "compile breaks" — a compile-time check, not runtime. This is explicit and clearly stated.
- [x] CHK018 - Is the spec's claim "no SUCCEED-placeholders" for the completeness audit (T028) operationalized — i.e. is "SUCCEED-placeholder" defined precisely enough for an auditor to recognize and reject one? [Clarity, Spec §FR-011, T028] — PASS: T028 operationalizes it: "audit test BODIES (not file names): for each FR-001 through FR-009, identify the test file + the specific assertions that BIND the runtime contract; verify no SUCCEED-placeholders, no test that passes for the wrong reason, no end-state assertion that matches via the wrong matrix row." The project memory `[[project_005_phase8_completeness_false_pass]]` and `[[feedback_simplify_pass_catches_9th_burn]]` define the pattern. Per the Spec-Kit constitution and memory, a SUCCEED-placeholder is "a test body containing SUCCEED() or asserting only side-effects detectable without exercising the production code path" — T028 operationalizes this as "specific assertions that BIND the runtime contract". The auditor criterion is: does the assertion fail if the production code is wrong? This is precise enough.

## Requirement Consistency

- [x] CHK019 - Do the spec, plan, research, data-model, and contracts agree on the count and locations of `fsm_state_ = X;` sites (37 per /speckit-analyze)? [Consistency, Cross-artifact] — PASS: verified across all artifacts. Spec.md FR-001 via research context: "37 sites"; plan §Scale/Scope: "/speckit-analyze verified 37 sites"; research D-2: "/speckit-analyze verified: 37 sites"; data-model E1: "/speckit-analyze verified 37 sites"; T005: "37 sites in the post-009 tree". Live grep confirmed: `grep -nE '\bfsm_state_\s*=' src/session/session.cpp | grep -v '=='` = 37 lines. All five artifacts agree.
- [x] CHK020 - Do the spec FR-005 + plan §X.4 + research D-3 + the contract shape oracle agree on slot 77 as the chosen enum value (no other artifact picks a different slot)? [Consistency, Cross-artifact] — PASS: spec.md FR-005: "research D-3, slot 77"; plan §X.4: "session_invalid_state_for_send = 77"; research D-3: "error::session_invalid_state_for_send = 77"; contract: "session_invalid_state_for_send = 77 (FR-005, D-3)". Live grep of error.hpp: highest occupied session-class slot = 76 (`session_invalid_config`). All agree on slot 77, no other artifact picks a different slot.
- [x] CHK021 - Do the FSM state names appear consistently as `NotConnected / LogonSent / LogonReceived / Active / LogoutSent / Disconnected` across spec.md `## Key Entities`, plan.md, research.md, tasks.md, and data-model.md (no stray "Failed" or "Failed-or-equivalent")? [Consistency, Cross-artifact] — PASS: all artifacts use the 6-state set. Spec.md §Key Entities: "NotConnected / LogonSent / LogonReceived / Active / LogoutSent / Disconnected — exact set from 005 data-model.md, confirmed against include/fixpp/session/session_fsm.hpp:30-46". The /speckit-analyze remediations corrected a stray "Failed-or-equivalent" to "LogoutSent" per the orchestrator brief. Verified against `session_fsm.hpp` which enumerates the same 6 states. No stray "Failed" in the current artifact set.
- [x] CHK022 - Does the spec's Edge Cases section align with FR-001's by-value semantics (i.e. "multi-session sharing not supported" matches the by-value-only contract, not a shared_ptr-like contract)? [Consistency, Spec §Edge Cases ↔ §FR-001] — PASS: spec §Edge Cases says "under the by-value mechanism (FR-001), each Session ctor takes its own copy of the caller's SessionConfig. Multi-session sharing is not a supported scenario". FR-001 says "No sharing across sessions; each Session owns its own snapshot." The Edge Cases text explicitly anchors to the by-value mechanism and uses FR-001 parenthetically. No shared_ptr semantics appear anywhere in the 010 artifact set.
- [x] CHK023 - Are the FR-005 / SC-004 / Test name conventions (`session_send_invalid_state_test`) consistent between spec acceptance scenarios, tasks (T013), and quickstart's ctest gate command? [Consistency, Spec §FR-005, Tasks §T013, Quickstart §FR-005] — PASS: T013 creates `tests/session/session_send_invalid_state_test.cpp` and adds via `add_threading_test(session_send_invalid_state ...)`. Quickstart §FR-005 gate command: `ctest ... -R '^session_send_invalid_state$'`. The CTest target name `session_send_invalid_state` matches the `-R` filter exactly. SC-004 references "session_invalid_state_for_send" as the error variant name (not the test name), which is consistent with FR-005. All three artifacts are consistent on the target/file/filter naming.
- [x] CHK024 - Does T015's documented expectation (0 send-path assertion updates) align with FR-005's "Tests that asserted ... MUST be updated" wording, or is there residual contradiction? [Consistency, Spec §FR-005, Tasks §T015] — PASS: there is no contradiction. FR-005 says "Any test that asserts against the specific variant at this site MUST be updated; /speckit-analyze verified the existing tests assert EXPECT_FALSE(has_value()) only at this site, so the expected update count is 0." T015 says "/speckit-analyze verified the expected count of send-path-specific updates needed = 0 (existing tests assert EXPECT_FALSE(result.has_value()) only at this site, never the specific variant; the new T013 tests provide AC3 coverage going forward)." The "MUST be updated" is a conditioned MUST: if there are such tests, they must be updated. The verification shows there are none, so update count is 0. No contradiction.
- [x] CHK025 - Are the LoC estimates for T005 (37 sites → ~50-80 LoC) and T017 (15-event matrix → ~700-900 LoC) consistent across plan.md Scale/Scope, research D-2/D-4, and the tasks.md task bodies? [Consistency, Cross-artifact] — PASS: T005 ~50-80 LoC: plan §Scale/Scope says "Net ~50-80 lines edited including the helper definition"; T005 task body doesn't duplicate a LoC estimate (appropriate — the plan is the LoC-estimate-bearing artifact). T017 ~700-900 LoC: plan §Scale/Scope says "Estimated ~700-900 LoC"; research D-4 says "LoC estimate (~700-900 — updated post-/speckit-analyze to reflect the 15-event alphabet)"; T017 says "Estimated ~700-900 LoC post-/speckit-analyze". All three agree. The discrepancy between the first-draft "~10 sites" and the corrected "37 sites" is consistently documented as "not the ~10 first-draft estimate" across all artifacts.

## Acceptance Criteria Quality (Measurability)

- [x] CHK026 - Can SC-001 ("ASan reports zero `stack-use-after-scope` on `session_coverage_adversarial`") be objectively verified in a single ctest invocation with no human judgment? [Measurability, Spec §SC-001] — PASS: quickstart.md provides the exact ctest command: `ctest --test-dir build/linux-clang-asan --output-on-failure -R '^session_coverage_adversarial$'`. The pass/fail criterion is binary: ASan prints "stack-use-after-scope" → FAIL, no such output → PASS. This requires zero human judgment beyond reading the test exit code.
- [x] CHK027 - Can SC-002 ("FSM matrix witness has one passing assertion per cell") be mechanically counted — i.e. does the spec define how to compute the cell count to compare against? [Measurability, Spec §SC-002] — PASS: SC-002 cites "6-state × N-event matrix"; research D-4 commits to "15 events, raw matrix = 6 × 15 = 90 cells; minus design-forbidden ≈ 55-70 reachable cells." T016 requires an "inline comment table at the top of the new test file listing every (state, event) cell." The auditor can mechanically verify: count TEST(...) functions in `fsm_matrix_witness_test.cpp` against the table. The cell count source is the inline comment table + the 005 data-model. This is mechanically verifiable.
- [x] CHK028 - Is SC-003 ("coverage envelope rises measurably vs the 009 baseline") quantified with specific per-file thresholds or a delta direction, or is "measurably" subjective? [Measurability, Spec §SC-003] — PASS with acknowledged design choice: SC-003 says "concrete deltas computed by /speckit-verify and recorded in .specify/decisions/010-session-cfg-lifetime-verify.md." The spec intentionally defers the numeric threshold to /speckit-verify (because the exact delta depends on pre-010 coverage state). The "measurably" is operationalized as: generate lcov; compare DA/BRDA counts vs the 009 verify record; if the envelope rises, it's measurable improvement; if it doesn't, the W-1..W-4 re-waiver path from FR-011 applies. Plan §IX.1 row specifies the measurement basis (lcov DA/BRDA per `[const §IX.1]`). This is the standard pattern for coverage-rise SCs across 005-009; it is not subjective within the project's measurement framework.
- [x] CHK029 - Is SC-005 ("four mixed-success-mode permutations each have one passing assertion") tied to a definitive cell-count source so the auditor can verify "four" is exhaustive? [Measurability, Spec §SC-005] — PASS: FR-008 names the four permutations explicitly: "(Reject-ok/Logout-ok, Reject-fail/Logout-skip, Reject-ok/Logout-fail, Reject-fail/Logout-fail)". These are the 2^2 = 4 combinations of {Reject-ok, Reject-fail} × {Logout-ok/skip, Logout-fail}. "Four" is exhaustive because there are exactly 2 possible outcomes at each of the 2 sequential steps. The auditor can verify by counting TEST(...) functions in `admin_emit_mixed_path_test.cpp` and confirming each of the 4 named permutations is present. This is definitive.
- [x] CHK030 - Is SC-009 ("W-5 waiver row in 009 Gate B decision record annotated CLOSED with PR back-link") tied to a specific file path + an unambiguous marker text the auditor can grep for? [Measurability, Spec §SC-009] — PASS: SC-009 states "PR #82 W-5 waiver row in `.specify/decisions/009-session-fsm-finalize-gateb.md` is annotated CLOSED with a back-link to the 010 PR." The file path is specific; T027 says `CLOSED — see PR #<N>` is the marker text. The auditor can grep: `grep "W-5.*CLOSED\|CLOSED.*W-5" library/.specify/decisions/009-session-fsm-finalize-gateb.md`. Both the file path and the marker text are unambiguous.
- [x] CHK031 - Are the bench gate dispositions (T024) quantified at the FR level (±5% per `[const §VIII.2]`) AND tied to which baseline files are authoritative (`bench/baselines/session/*.json`)? [Measurability, Tasks §T024] — PASS: T024 states "diff against bench/baselines/session/*_baseline.json with ±5% tolerance." Plan §Technical Context §Performance Goals cites "±5% gate vs bench/baselines/session/*.json per [const §VIII.2]". The baseline path is specific, the tolerance is quantified. The plan also names the specific benchmarks: "fsm_bench seqnum_bench fix_time_bench heartbeat_bench on linux-clang-release". This is fully quantified.

## Scenario Coverage

- [x] CHK032 - Does the requirement set cover the **primary** flow (caller constructs Session from a fixture-local SessionConfig, exercises it, drops the cfg, session continues correctly)? [Coverage, Primary, Spec §US1 AC1] — PASS: US1 AC1 states "Given a unit test that constructs SessionConfig as a local, passes it into Session(...), and lets the config go out of scope, When the session continues to operate (FSM transitions, admin emits, send(...) calls), Then ASan reports no use-after-scope." T007 test `CallerCfgDropsAfterCtor_SessionContinuesWithoutUAF` implements this exactly. The primary flow is covered.
- [x] CHK033 - Does it cover **alternate** flows (caller mutates SessionConfig after ctor; multiple Sessions from same cfg)? [Coverage, Alternate, Spec §US1 AC2, AC3] — PASS: US1 alternative scenarios are covered in T007: test `CallerMutatesCfgAfterCtor_SessionUnaffected` exercises the mutate-after-ctor pattern; test `MultipleSessionsFromSameCfgEvolveIndependently` exercises the multiple-Sessions-from-same-cfg pattern. The Edge Cases section also documents "multi-session sharing not supported" and "SessionConfig mutated after Session construction" as the two alternate patterns.
- [x] CHK034 - Does it cover **exception** flows (`Session::send` returns `session_invalid_state_for_send` for each non-Active state: NotConnected, LogonSent, LogonReceived-transient, Disconnected)? [Coverage, Exception, Spec §FR-005, §US2 AC2] — PASS: T013 covers NotConnected (`SendPreLogon_ReturnsInvalidStateForSend`), LogonSent (`SendInLogonSent_ReturnsInvalidStateForSend`), Disconnected (`SendInDisconnected_ReturnsInvalidStateForSend`), and a positive control for Active. The LogonReceived-transient state is not separately enumerated in T013 because it is synchronous-transient (the session is already in Active by the time any user code calls `send`); FR-005 covers "when the FSM is not in Active" which encompasses all non-Active states. The spec does not require a separate LogonReceived-specific `send` test because the state cannot be directly held for a send call — this is acceptable.
- [x] CHK035 - Does it cover **recovery** flows (FSM matrix witness asserts that forbidden cells leave the session in an unchanged or recoverable state, not in an undefined state)? [Coverage, Recovery, Spec §FR-006] — PASS: spec §Edge Cases states "FSM matrix cells that are unreachable by design (e.g. events forbidden in Disconnected): the per-cell witness must assert the rejection / no-op, not silently skip the cell. Cell count includes negatives." FR-006 requires "at least one assertion per cell (positive transition OR explicit rejection / no-op)". T017 implements: "assert via fsm_visit_history() that... no transition occurred (forbidden / ignored cells)." The forbidden cells assert no-transition (the session's state is unchanged = recoverable by definition for a no-op cell).
- [x] CHK036 - Does it cover **non-functional** scenarios (sanitizer matrix, coverage envelope, bench delta, static analysis, IWYU per `[const §IX.2]`/`[const §IX.4]`)? [Coverage, NFR, Spec §FR-011, Tasks §T022-T026] — PASS: T022 (sanitizer matrix); T023 (lcov coverage); T024 (bench ±5%); T025 (static analysis + IWYU + mutex grep gate); T026 (I-25 grep invariant). FR-011 and FR-012 (hygiene FRs) map to these tasks. Plan §IX.2 and §IX.4 Constitution Check rows confirm the NFR requirements. All NFR dimensions are covered.
- [x] CHK037 - Is the initiator/acceptor symmetry for the transport-throw witness (FR-009) covered for BOTH the acceptor side (existing test landed in PR #82 round 1) and the initiator side (new T021)? [Coverage, Spec §FR-009] — PASS: FR-009 says "symmetric to the acceptor witness landed in PR #82 round 1." US4 §Independent Test says "symmetric to the acceptor witness." T021 references "Mirror the acceptor-side existing test for the assertion shape." The acceptor side exists (PR #82); the initiator side is covered by T021. Both sides are in scope.
- [x] CHK038 - Is the cross-platform requirement covered — does the spec or plan specify GCC release sanity AND clang debug/release/asan/ubsan/tsan/coverage are all in scope? [Coverage, NFR, Plan §IX.2/IX.6] — PASS: plan §Target Platform says "Linux Clang Debug + Release + ASan + UBSan + TSan + Coverage; GCC Release sanity." Quickstart.md provides cmake preset commands for all 7 configurations including `linux-gcc-release`. T022 runs "for preset in linux-clang-debug linux-clang-release linux-clang-asan linux-clang-ubsan linux-clang-tsan linux-clang-coverage linux-gcc-release". All 7 platforms are explicitly enumerated.

## Edge Case Coverage

- [x] CHK039 - Is the "16-entry ring-buffer wraparound" edge case covered (what happens if a single test scenario triggers >16 FSM transitions)? [Edge Case, research D-2 risk note] — PASS: research D-2 risk note states "Ring-buffer wraparound at 16 visits would mask a regression that overruns the buffer. Mitigation: the FIX FSM in [FIX-SL §4.10] admits at most ~6 transitions in a normal session lifecycle... and ~10 in adversarial paths. 16 is comfortably above the worst case." Additionally: "Flag as a /speckit-verify hygiene check: assert fsm_visit_count_ < 16 at end of each FSM matrix witness test." The edge case is identified, the mitigation is stated, and a /speckit-verify hygiene check is prescribed. This is sufficient — a test that trips >16 transitions would produce wrong results AND would fail the hygiene assertion, making the failure observable.
- [x] CHK040 - Is the "saturating count at 255" edge case covered (the saturating behavior is mentioned but what does it mean for the accessor return when count >= 16)? [Edge Case, research D-2, Data-model E1] — PASS: research D-2 defines: the accessor returns "the first min(fsm_visit_count_, 16) entries". When count >= 16, the accessor returns exactly 16 entries (the latest 16 written to the ring buffer via modular index). When count = 255 (saturated), min(255, 16) = 16 entries returned, with the ring buffer content being the last 16 transitions. The saturation at 255 is a counter-overflow guard; it doesn't change the accessor behavior (which is always capped at 16). This is clear from the pseudocode in D-2 and the data-model definition.
- [x] CHK041 - Is the "mock_clock returns the same value across two admin emits" edge case spelled out so the FR-007 distinct-SendingTime test doesn't falsely claim distinctness? [Edge Case, Spec §Edge Cases, §FR-007] — PASS: spec §Edge Cases states explicitly: "the per-message-distinct-SendingTime test seam must NOT promise distinct SendingTime in this case — it asserts only that the branch can fire when the clock advances." This edge case is spelled out. The T018 test exercises the scenario WITH clock advance (`mock_clock->advance(seconds(1))`), not without, so the test cannot falsely claim distinctness when the clock hasn't advanced.
- [x] CHK042 - Is the "FSM matrix cell that's unreachable by design" edge case spelled out (the test must assert no-transition, not skip the cell)? [Edge Case, Spec §Edge Cases, §FR-006] — PASS: spec §Edge Cases states: "FSM matrix cells that are unreachable by design (e.g., events forbidden in Disconnected): the per-cell witness must assert the rejection / no-op, not silently skip the cell. Cell count includes negatives." T017 implements: "assert via fsm_visit_history() that... no transition occurred (forbidden / ignored cells)." Both the spec and the task body are unambiguous about this requirement.
- [x] CHK043 - Is the "external C-ABI consumer relies on the old `session_invalid_logon`" edge case addressed — even if the C-ABI is deferred, is the rationale documented for why the rename is safe pre-2i? [Edge Case, Spec §Assumptions, research D-3 risk] — PASS: spec §Assumptions states "No external consumers of the session_error enum yet. [...] If this assumption breaks before merge, FR-005 escalates to a wire-API concern needing /clarify." Research D-3 risk note states "If any external C-ABI consumer mapped session_invalid_logon to a SESSION_REFUSAL handler and now sees a SESSION_REJECT, the rename is observably different [...] Today no external consumer depends on it (the C-ABI surface itself is deferred per [const §X.2] and 005 plan)." The rationale is documented: C-ABI is deferred (2i not yet shipped), so there is no live downstream C consumer of the enum integer values. The rename is safe pre-2i.
- [x] CHK044 - Is the "transport throws on the SECOND Logon emit during retry" edge case considered for the initiator transport-throw witness (FR-009), or is the test only the first-emit scenario? [Gap, Spec §FR-009] — PASS with documented scope: FR-009 and T021 specify the "first emit" scenario only. This is intentional and documented: US4 says "transport.send throwing during the initiator Logon emit on Session::open()". The 005 design does not specify a Logon retry mechanism — `open()` is a one-shot operation per `[FIX-SL §4.3]` (initiator sends exactly one Logon on `open()`; retry is at the application layer). A "second Logon emit during retry" is not part of the 005 FSM design; there is no such event in the 15-event alphabet. The test-only-the-first-emit scope is correct and aligned with the 005 design.

## Non-Functional Requirements

- [x] CHK045 - Are the perf-impact claims for FR-001 (by-value cfg copy ≤50ns at ctor) and FR-004 (ring-buffer push ~1ns per transition) tied to specific bench/baseline files for verification? [Measurability, Plan §VIII.1, §VIII.2] — PASS: plan §Performance Goals states "by-value cfg copy ~50ns at ctor, outside every bench envelope" and "ring buffer push ~1ns per transition, outside every bench envelope." Both claims are stated as "outside the bench envelope" — meaning the existing `bench/baselines/session/*.json` benchmarks are the verification gate and neither of these new operations is expected to show up in the benchmark deltas. T024 verifies the bench gate. The claim is: these costs are too small to move the ±5% needle on any existing benchmark; if they do, that's a T024 finding. This is the correct framing for sub-nano-second additions.
- [x] CHK046 - Are the static-analysis requirements (clang-tidy / clang-format / cppcheck / IWYU) scoped to "touched files" with an explicit file list per `[[feedback_phase_implementer_sonnet_runaway_scope]]` surgical-changes discipline? [Completeness, Tasks §T025] — PASS: T025 says "Touched files per plan.md Project Structure (session.hpp, session.cpp, error.hpp, session_config.hpp + the new test files)." Plan §Project Structure lists the specific modified and new files. The "touched files" scope is explicit and matches the surgical-changes discipline from `[[feedback_phase_implementer_sonnet_runaway_scope]]`.
- [x] CHK047 - Are sanitizer-coverage requirements (TSan unchanged per "no new threading contract") explicitly stated so an auditor knows to verify the TSan run did not regress? [Completeness, Plan §IX.2] — PASS: plan §IX.2 Constitution Check row says "Inherited + critical. ASan+UBSan on every session test; the W-5 acceptance criterion (FR-003 / SC-001) is the ASan-clean re-enablement of session_coverage_adversarial. TSan unchanged (no new threading contract introduced)." T022 includes TSan in the preset loop ("linux-clang-tsan"). The plan's §Constraints explicitly states "Visit-history ring buffer is touched only on the FSM-transition path which runs on the per-session strand — single-writer, single-reader (the test) only AFTER the operation completes. No new sync primitive required." An auditor knows: TSan must run (T022) and must not regress; the threading model reason for expecting no TSan regression is documented.

## Dependencies & Assumptions

- [x] CHK048 - Is the "005 Gate A already covered FSM + session_error design space" assumption explicitly tied to the Gate A inheritance addendum (T027b) so the audit trail is complete? [Assumption, Plan §XVII.1, T027b] — PASS: plan §XVII.1 row says "Gate A NOT re-run for 010. Per [const §XVII.1] Gate A is triggered by design artifacts (spec.md / data-model.md / contracts/*.hpp); a waiver-closure slice that does not edit 005's design does not re-trigger. The 005 Gate A converged round 3 (2026-05-18) is the binding sign-off." T027b requires synthesizing `library/.specify/decisions/010-session-cfg-lifetime-gatea.md` citing "(a) the 005 Gate A round-3 sign-off date 2026-05-18" as the binding sign-off. The `/gate-b` pre-flight step 4c checks for this file. The audit trail is: spec assumption → T027b task → local addendum file → /gate-b pre-flight verification. This chain is complete.
- [x] CHK049 - Is the "C-ABI surface is deferred, so the new variant has no live downstream consumer" assumption documented with a back-pointer to feature 2i? [Assumption, research D-3, Plan §X.4] — PASS: research D-3 states "the C-ABI surface itself is deferred per [const §X.2] and 005 plan". Plan §IX.5 Constitution Check row says "No C-ABI surface added. The new fsm_visit_history() accessor and error::session_invalid_state_for_send variant are C++-only — they never appear in <fix/c_api.h>." Plan §X.4 row says "pre-D-12 C-ABI prefix-group coalescing maps it to FIXPP_ERR_SESSION_REJECT (it is a state-mismatch reject for an outbound send)." The back-pointer to the 2i feature is in the contract shape oracle: "the C-ABI surface is itself deferred per [const §X.2] + 2i". The cite "[const §X.2] + 2i" names the feature.
- [x] CHK050 - Is the "no external consumers of `session_error` enum yet" assumption tied to a verification step (e.g. grep / nm check) or only stated in prose? [Assumption, Spec §Assumptions] — PASS with acknowledged design choice: spec §Assumptions states "No external consumers of the session_error enum yet. [...] If this assumption breaks before merge, FR-005 escalates to a wire-API concern needing /clarify." The verification step is not a grep/nm check (which would be correct for a C-ABI check), but a scope-control assumption: the C-ABI surface is itself deferred (2i not shipped), so there cannot be a compiled C-ABI consumer. The spec's assumption is structurally guaranteed by the project state, not by a runtime check. This is the same pattern used by 005/009 for the C-ABI assumption. The spec does flag the assumption as needing revisit "before merge" if it changes — this is the correct risk-control pattern.

## Ambiguities & Conflicts

- [x] CHK051 - Is there any residual ambiguity about whether "the session continues correctly" (FR-001 / US1) means "all subsequent reads of `cfg_.X` return the value at ctor-time" — or whether it admits weaker contracts? [Ambiguity, Spec §FR-001] — PASS: no residual ambiguity. FR-002 pins the exact meaning: "All FSM sites that read cfg_.* (executor override, role, BeginString, heartbeat interval, comp IDs, send/recv reset policy, drain policy, persistence, etc.) MUST continue to read consistent values for the lifetime of the Session." "Consistent values" is defined as "the value at ctor-time" by FR-001's statement "No sharing across sessions; each Session owns its own snapshot." T007 test `CallerMutatesCfgAfterCtor_SessionUnaffected` verifies that mutating the cfg after ctor does not change the Session's behavior — this is the operationalization of "ctor-time values are frozen". No weaker contract is admitted.
- [x] CHK052 - Is there any conflict between FR-001's "may go out of scope or be mutated" wording and Data-model E2's mention of a `static_assert(is_copy_constructible)` — the latter could be misread as also asserting non-trivial copyability? [Conflict, Spec §FR-001, Data-model E2] — PASS: no conflict. Data-model E2 explicitly clarifies: "The assert pins the by-value-membership contract: if a future contributor adds a non-copyable member (e.g. a std::unique_ptr to a session-scoped resource), the compile breaks." The `static_assert(std::is_copy_constructible_v<SessionConfig>)` checks ONLY copyability — NOT trivial copyability, not nothrow copyability. `is_copy_constructible` is true for any type with a copy constructor (including types with std::string members). The disambiguation between copyable and trivially-copyable is implicit in the use of `is_copy_constructible` (not `is_trivially_copyable`), and research D-1 confirms "All members are copyable" including the `std::optional<executor_type>` member.
- [x] CHK053 - Is FR-010's "MUST NOT amend 005's spec.md / data-model.md / contracts/*.hpp" enforced by a verification step (e.g. git diff against base), or only stated as a requirement? [Gap, Spec §FR-010] — PASS with documented scope: FR-010 is a constraint requirement. The verification step is T001 (Phase 1 baseline: "confirm git status is clean") + the /speckit-verify verify record which will confirm no 005 artifacts were modified. The specific mechanism is that the 010 spec.md §Assumptions "Binding 005 design unchanged" + plan.md Summary ("On conflict the 005 anchor wins; a divergence is a defect in this plan") + FR-010 together create a contractual guard. If during /speckit-implement a subagent modifies 005 artifacts, T001's clean-state check would catch it post-hoc. This is the same verification pattern used by 009 for its FR-010 analog. A live git diff command is implied by the verify record template but not explicitly tasked as a separate step — this is acceptable for a constraint that is architecturally enforced by the task structure (no task explicitly touches 005 files).

## Traceability

- [x] CHK054 - Does every FR-001..FR-012 map to at least one acceptance scenario (Given/When/Then) AND at least one SC AND at least one task? [Traceability, Cross-artifact] — PASS: FR-001 → US1 ACs / SC-001 / T007-T011; FR-002 → US1 AC1 (cfg_.X reads consistent) / SC-001 / T009; FR-003 → US1 AC2 / SC-001 / T010; FR-004 → US2 AC1 / SC-002 / T012; FR-005 → US2 ACs 2+3 / SC-004 / T013-T015; FR-006 → US3 AC1 / SC-002 / T016-T017; FR-007 → US3 AC2 / SC-006 / T018; FR-008 → US3 AC3 / SC-005 / T019; FR-009 → US4 AC1 / SC-007 / T021; FR-010 → Assumption + T001 baseline + /speckit-verify; FR-011 → SC-003 + FR-011 "W-1..W-4 auto-revisit" / T023/T029; FR-012 → Gate A inheritance outcome / T027b. All 12 FRs have at least one acceptance scenario (or a hygiene verification step for FR-010/011/012), at least one SC, and at least one task.
- [x] CHK055 - Does every SC-001..SC-009 map to at least one FR AND at least one task that produces measurable evidence? [Traceability, Cross-artifact] — PASS: SC-001 → FR-001/FR-003 / T010-T011 (ASan run); SC-002 → FR-004/FR-006 / T012/T017 (cell count); SC-003 → FR-011 / T023 (lcov report); SC-004 → FR-005 / T013-T014 (test assertions); SC-005 → FR-008 / T019 (4 tests); SC-006 → FR-007 / T018 (clock advance test); SC-007 → FR-009 / T021 (transport throw test); SC-008 → FR-011 / T022 (sanitizer matrix); SC-009 → FR-003/FR-011 / T027 (gateb.md annotation). All 9 SCs map to FRs and evidence-producing tasks.
- [x] CHK056 - Does every Phase 3-6 task carry a `[USn]` story label AND a back-reference to the FR(s) it closes? [Traceability, Tasks structure] — PASS: T006-T011 all carry `[US1]`; T012 carries `[US2]`, references FR-004; T013 carries `[P][US2]`, references FR-005; T014 carries `[US2]`, references FR-005; T015 carries `[US2]`, references FR-005; T016 carries `[US3]`; T017 carries `[US3]`, references FR-006; T018 carries `[P][US3]`, references FR-007; T019 carries `[P][US3]`, references FR-008; T020 carries `[US3]`; T021 carries `[US4]`, references FR-009. All Phase 3-6 tasks have [USn] labels. Back-references to FRs are present via "TDD red per FR-xxx" or explicit "(FR-xxx, SC-xxx)" in the task body.
- [x] CHK057 - Are the W-1..W-4 carry-forward outcomes tied to specific `.specify/decisions/*.md` line ranges so the audit trail is unambiguous? [Traceability, Spec §FR-011] — PASS: T023 specifies the branch logic: "(a) if measurement shows the envelope cleared organically, mark W-1..W-4 CLOSED in the verify record and the 009 Gate B decision record (sibling action to SC-009's W-5 closure); (b) if any residual W-N gap persists, re-waive with a one-line Article IX §1 rationale per [[feedback_codecov_patch_vs_lcov_da_brda_gate]] + PR #73 precedent." The target file for the outcome is `.specify/decisions/010-session-cfg-lifetime-verify.md` (for the current-slice measurement) and `.specify/decisions/009-session-fsm-finalize-gateb.md` (for closing the W-1..W-4 rows from the 009 waiver table). The 009 Gate B record lines 67-70 are the definitive W-1..W-4 source. The audit trail is: 009 Gate B lines 67-70 → T023 outcome → 010 verify record. This is unambiguous.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 56 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **57** |

### SPEC-FIXED items
- CHK016 — contract shape oracle `contracts/session_error_state_for_send.hpp` said "at the two Session::send non-Active sites" (line 62-63) but spec.md FR-005, plan §Scale/Scope, research D-3, and tasks T014/T015 all say ONE site (confirmed by live grep: exactly one `session_invalid_logon` in `Session::send`). Changed "the two" → "the one" and "sites" → "site"; affected: `specs/010-session-cfg-lifetime/contracts/session_error_state_for_send.hpp:62-63`.

### DD-DECIDED items
(none)

### WAIVED items
(none)

Anchors spot-verified:
- `[2d §4.5]` — resolves at `.specify/2d-threading.md:496` ("### 4.5 fixpp::session::SessionConfig") in signed-off v0.4.
- `[2e §4.1]` — resolves at `.specify/2e-msgstore.md:16` ("Pin the public C++ surface of fixpp::session::MessageStore") in signed-off v0.4.
- `[FIX-SL §4.10]` — external FIX Session Layer document; not in-repo; cited by section number per `[const §VI.2]` canonical format.
- `[FIX-SL §4.5.4]` — same external source; cited by canonical section number; used for FR-005.
- `[FIX-SL §4.3]` — same; used for FR-009.
- `[const §VI.5]`, `[const §VIII.2]`, `[const §IX.1]`, `[const §IX.2]`, `[const §IX.4]`, `[const §X.4]`, `[const §XI.4]`, `[const §XVII.1]`, `[const §XVII.8]` — all resolve in `constitution.md` v0.1 (signed-off 2026-05-10): §VI line 74, §VIII line 97, §IX implied in Articles IX coverage, §X.4 line 200+, §XI line 160+, §XVII line 245.
