# Requirements Quality Checklist: toAdmin/toApp observation coverage

**Purpose**: Validate the quality, clarity, and completeness of the requirements for closing the
FR-008 observation gap — BEFORE implementation. Audience: Gate B reviewer.
**Created**: 2026-06-14
**Feature**: [spec.md](../spec.md)
**Depth**: Standard · **Focus**: observation-coverage completeness/consistency + BMR veto/persist behavioral contract

> These are "unit tests for the requirements." Each item asks whether the spec/plan/contracts are
> well-written (complete, unambiguous, consistent, measurable), NOT whether code works.

## Requirement Completeness

- [x] CHK001 - Is the exact set of newly-covered administrative emit sites enumerated, not described by category? [Completeness, Spec §FR-002 / data-model coverage matrix] — PASS: FR-002 enumerates all 9 administrative sites individually by name (shared helper, Q3 SendingTime, SequenceReset-veto, two 021-Arm-C/RC#1 malformed-122, Arm-D, Logout-veto, NewSeqNo-too-low, Guard-3 Logout) plus the 1 application site (35=j); data-model.md coverage matrix rows confirm all 10 at HEAD line numbers. No categorical shorthand — each site is named.
- [x] CHK002 - Are requirements defined for the case where the same reject helper is reached from two distinct caller flows (app-registered vs no-`Application`)? [Completeness, Spec §Edge Cases / contracts C3] — PASS: spec.md §Edge Cases explicitly names both callers (:3026 app-registered fromAdmin-veto; :3215 no-Application unknown-MsgType). C3 carves out the :3215 caller as a byte-identity no-op (excluded from exact-count). Data-model.md footnotes both callers in the helper row. T005 and T004 test them distinctly.
- [x] CHK003 - Does the spec define behavior when no `Application` is registered for every newly-wired site (no-op, byte-identity)? [Completeness, Spec §FR-006] — PASS: FR-006 states every newly-wired site is byte-for-byte and ordering-identical to current behavior when no Application is registered. C1 notes fire_to_admin_ returns true immediately at :332; C2 gates the toApp block on `engine_.application != nullptr`. INV-COV-6 is the formal data-model invariant. T037 is the verification task. Confirmed against session.cpp:332 (fire_to_admin_ early-return) and :4149 (send_impl gate).
- [x] CHK004 - Are the documentation obligations (stale-comment inversion, B&L row, catalogue row, 019 anchor note) each specified as a requirement, not left implicit? [Completeness, Spec §FR-008 / contracts C4] — PASS: FR-008 specifies three obligations (comment inversion, B&L row, 019 anchor note) as requirements. The feature-catalogue/coverage-index traceability row (T035) is pipeline step-19 MARK-DONE bookkeeping common to every feature and is not enumerated as a named requirement in any feature's FR-* set — it is not a behavioral obligation of this feature. C4 echoes the same three doc requirements. The omission is deliberate scope (pipeline boilerplate), not a Completeness gap.
- [x] CHK005 - Is the durable-persist obligation on the BMR veto path explicitly stated as a requirement (not only in prose)? [Completeness, Spec §Edge Cases / data-model INV-COV-5] — PASS: INV-COV-5 in data-model.md is the formal invariant: "inbound durable seqnum advance is STILL durably persisted (persist_inbound_advance_() at :3279 — the veto suppresses only the BMR emit, it must NOT early-return)". FR-004 in spec.md contains the same obligation ("the inbound sequence number advances independently of suppression"). C2 in contracts pins the control-flow explicitly. Confirmed against session.cpp:3278-3281 — persist_inbound_advance_() at :3279 IS at the fall-through of the fromApp-reject block (:3267 closes the BMR-build `if (bmr_r)`, :3270 closes the `application != nullptr` gate, :3279 is unconditionally reached).
- [x] CHK006 - Are requirements present for the "Reject paired with an already-wired Logout" case so the witness does not mistake the Logout for Reject coverage? [Completeness, Spec §Edge Cases] — PASS: spec.md §Edge Cases explicitly names the two paired sites ("Reject paired with a Logout"): :2400 reject + :2421 logout (already wired); :2675 reject + :2696 logout (already wired). The spec says "both the Reject and its paired Logout fire toAdmin (two invocations)". Tasks T006 (Q3 pair) and T010 (021 Arm D pair) assert `toAdmin_calls == 2` with two wire frames — the witness cannot mistake the already-wired Logout for Reject coverage because the count goes to 2, not 1.

## Requirement Clarity

- [x] CHK007 - Is "engine-originated administrative frame" defined precisely enough to decide membership (which msgtypes are in/out of scope)? [Clarity, Spec §Key Entities] — PASS: spec.md §Key Entities defines "Engine-originated administrative frame" as a Reject(35=3), Logout(35=5), or other admin-set message the engine emits on its own initiative (not relayed from the host). The intro explicitly cites the FIX admin set A/0/1/2/3/4/5 and excludes 35=j from it. The data-model coverage matrix enumerates all 25 build-sites with their msgtypes, leaving no ambiguity about membership.
- [x] CHK008 - Is the routing rule for `35=j` (application message → `toApp`, NOT `toAdmin`) stated unambiguously with its rationale (FIX admin set A/0/1/2/3/4/5)? [Clarity, Spec §FR-004] — PASS: FR-004 states "MUST invoke toApp (NOT toAdmin)" with explicit "(it is not in the FIX administrative set A/0/1/2/3/4/5)". Spec.md intro paragraph also states the same exclusion with the full admin-set enumeration. C2 contracts adds "Routing rule: 35=j MUST NOT route through toAdmin." Rationale (QuickFIX parity) is in §Clarifications and research Decision 3.
- [x] CHK009 - Is the callback ordering relative to `assign_outbound` specified distinctly for the admin arm (after) vs the BMR arm (before), with the reason for the difference? [Clarity, Plan §"two routing arms" / research Decision 2-3] — PASS: plan.md §"two routing arms" specifies ARM-1 as "after assign_outbound, before store_then_emit" with the reason (inspect-only, not vetoable, throw disconnects regardless — seqnum order immaterial). ARM-2 is "before assign_outbound" with the reason (veto must not consume an outbound seqnum — originate-path INV-5). Data-model.md coverage matrix has an "Order vs assign_outbound" column. Research Decisions 2 and 3 each document the decision and alternative. Contracts C1 and C2 each show the code shape.
- [x] CHK010 - Is the throw-handling outcome unambiguous: admin arm records `Disconnected` directly vs BMR arm calls `close(terminal)`, and is their equivalence stated? [Clarity, Spec §FR-003 / SC-003] — PASS: FR-003 explicitly distinguishes the two: "the admin arm records Disconnected directly (record_state_transition_(Disconnected)); the BMR arm calls close(close_mode::terminal) — Disconnected IS the terminal FSM state, so the two are functionally equivalent". SC-003 mirrors the same. Spec.md US2 independent test contains "Note the asymmetry ... this is benign." Contracts C1 and C2 each show the respective control-flow shapes.
- [x] CHK011 - Is "inspect-only / not vetoable" for the admin arm quantified (a throw is the only non-OK exit; no suppression)? [Clarity, Spec §FR-003 / contracts C1] — PASS: FR-003 states "toAdmin remains inspect-only / not vetoable for administrative frames (consistent with current behaviour)". Spec.md Assumptions §"toAdmin is inspect-only" elaborates: "The existing contract does not let an Application veto an administrative frame; it may inspect, and a throw is surfaced." C1 post-condition states "toAdmin is inspect-only — it cannot suppress or mutate the frame." fire_to_admin_ returns false only on a throw (session.cpp:331–338).

## Requirement Consistency

- [x] CHK012 - Does the admin exact-count invariant (INV-COV-3 / contract C3) remain consistent with the BMR-veto carve-out (INV-COV-5) without contradiction? [Consistency, data-model INV-COV-3 vs INV-COV-5] — PASS: INV-COV-3 is explicitly scoped: "Carve-outs (stated correctly in INV-COV-5 and the quickstart cell table): the BMR veto path is governed by INV-COV-5 (see there), and the no-Application helper caller (:3215) is witnessed as an FR-006 byte-identity no-op." INV-COV-5 governs the veto path independently. C3 in contracts formalizes the two equalities (admin exact-count vs BMR veto path) without overlap. No contradiction — the two invariants cover disjoint paths.
- [x] CHK013 - Is the census (25 build sites / 15 wired / 10 newly-covered) stated consistently across spec, plan, research, and data-model? [Consistency, research Decision 1] — PASS: research Decision 1 anchors "25 build-call sites / 15 call sites [fire_to_admin_] / 10 unwired" with grep commands. Spec.md intro states "15 of 25 engine-originated admin emit sites / Ten sites were never wired". Plan.md summary states "10 of 25 engine-originated admin-builder emit sites bypass observation; 15 already fire fire_to_admin_". Data-model.md legend lists 15 already-observed + 10 newly-observed = 25. Grep for stale "26/16" finds no survivors — the Gate-A round-1 scrub was complete. **[PR #136 — 2026-06-18]** Post-PR #136 the live census is **24 `build_*` / 14 wired / 10 unwired** (the inbound-Heartbeat echo site was retired). The historical 25/15/10 prose across the 036 bundle is retained as the 036-era point-in-time record (waiver W-1, `research/reviews/opus_pr136_3_triage.md`); only `data-model.md` and this checklist carry the amendment note. Disposition unchanged: PASS.
- [x] CHK014 - Are the `35=j` veto semantics consistent with the cited originate-path `toApp` contract and the QuickFIX `sendRaw` reference? [Consistency, Spec §Clarifications / research Decision 3] — PASS: spec.md Clarifications confirms "Full toApp parity — the veto IS honoured" citing QuickFIX-cpp `sendRaw` (DoNotSend → return false before persist). FR-004 uses "same toApp semantics as the originate path". Plan Decision 3 and research Decision 3 both ground this against the `isAdminMsgType("j")` false branch. Confirmed that send_impl toApp block at session.cpp:4149-4164 is the reused pattern (veto → app_do_not_send → co_return; throw → terminal close). The BMR site reuses callback+outcome, not send_impl's early-return control-flow — that divergence is explicitly named and justified in C2 and plan §ARM-2.
- [x] CHK015 - Is "no new surface" (FR-007) consistent with the Constitution Check Article X / VI / Dependencies rows? [Consistency, Plan Constitution Check] — PASS: FR-007 states "no new wire field, error-code slot, configuration knob, code-generation output, or C-ABI surface." Plan Constitution Check table: Art. X "No new public type/signature; no new error variant. PASS." Art. VI "No new message type or field. PASS." Dependencies row: "No new third-party dependency." Plan §Surface delta confirms "no wire field, no SessionConfig/EngineConfig field, no codegen, no C-ABI, no new error variant, no new public signature." All consistent; no contradiction.

## Acceptance Criteria Quality

- [x] CHK016 - Is the exact-count equality `toAdmin_calls == admin-frames-on-wire` stated as exact (not subset / "≥1") and objectively measurable? [Measurability, Spec §SC-001 / contracts C3] — PASS: SC-001 states "toAdmin is invoked exactly N times — toAdmin_calls == administrative-frames-on-wire, exact-count equality. (Today this equality fails by the ten-site gap.)" The spec explicitly flags "not a subset / 'at least one' check" in US1 independent test. C3 formalizes as `toAdmin_calls(S) == count(administrative frames on the wire in S)`. Quickstart §"Exact-count aggregate" explains the measurement per cell. [[feedback_completeness_gate_exact_set_not_subset]] is cited. Objectively measurable via a counting Application double.
- [x] CHK017 - Is SC-002 ("zero bypass sites") verifiable by static enumeration, not only by scenario, and is that enumeration gate a concrete obligation? [Measurability, Spec §SC-002 / tasks T041] — PASS: SC-002 states "verified by enumerating the emit sites against the wiring, not only by a scenario". T041 is the concrete task: "re-run the research.md Decision-1 census at post-implementation HEAD — assert all 9 admin target sites now appear in the fire_to_admin_ call-site list... Encode it as a grep-gate (or a compile-time counter static_assert)". The enumeration gate is a named obligation in tasks.
- [x] CHK018 - Is the BMR-veto persist witness (`inbound durable advanced` while `35=j` absent) a measurable, discriminating acceptance criterion? [Measurability, quickstart BMR veto cell / INV-COV-5] — PASS: quickstart.md BMR veto cell gives the exact discriminating assertion: `EXPECT_EQ(store_persisted_inbound_seqnum(), inbound_durable_before + 1)`. The discriminating property is stated: "an implementation that co_returns on the veto (skipping persist_inbound_advance_() at :3279) passes the 'stays Active / no 35=j on wire' checks but fails this one." T028 mandates "Use a persistent store so the durable counter is observable." Confirmed against session.cpp:3278-3281 — persist_inbound_advance_() IS at the unconditional fall-through.
- [x] CHK019 - Is the no-`Application` byte-identity criterion (SC-005) anchored to a concrete baseline oracle? [Measurability, Spec §SC-005 / tasks T001] — PASS: T001 specifies "Capture the pre-036 baseline wire bytes for the affected emit scenarios (no Application registered) — provoke each of the 10 sites with no app and snapshot the wire frames/ordering, to be reused as the FR-006/SC-005 byte-identity oracle. Use the mock-transport (in-memory byte sink) seam." T037 says "each provocation with no app registered, assert wire bytes + ordering byte-for-byte identical to the T001 baseline." SC-005 is anchored to a captured concrete oracle, not a vague "same as before" claim.

## Scenario & Edge-Case Coverage

- [x] CHK020 - Are exception-flow requirements (throwing `toAdmin` / throwing `toApp`) defined for every newly-wired callback-reachable site? [Coverage, Spec §US2 / SC-003] — PASS: US2 (P1) is devoted entirely to throwing-callback surfacing. SC-003 requires "100% of the newly-wired sites match the existing throw-handling contract." The admin throw → record_state_transition_(Disconnected) + app_callback_threw; BMR throw → close(close_mode::terminal) + app_callback_threw. T025 requires throwing variants for all 9 callback-reachable admin sites (T004+T006-T013, explicitly excluding the no-app-only :3215 caller). T029 covers the BMR throw. The exclusion of :3215 is documented in T025 with explicit rationale.
- [x] CHK021 - Is the BMR veto (recovery-from-suppression) path covered, including the requirement that suppression must not desync inbound sequence persistence? [Coverage, Recovery, Spec §Edge Cases / INV-COV-5] — PASS: spec.md §Edge Cases §"BusinessMessageReject veto" states "Suppressing the reject must not suppress the durable processing of the inbound message that triggered it: the rejected inbound message's sequence advance is still persisted." INV-COV-5 is the formal invariant. T028 is the test task with the discriminating `store_persisted_inbound_seqnum() == inbound_durable_before + 1` assertion. Confirmed: session.cpp:3278-3281 places persist_inbound_advance_() at the unconditional fall-through after the BMR build block — veto suppresses only the BMR emit, not the persist.
- [x] CHK022 - Are the per-site distinct FSM states (initiator LogonSent Guard-3 vs Active rejects vs no-app helper) reflected in the coverage requirements so none is unreachable-by-construction? [Coverage, research Decision 5 / quickstart cells] — PASS: research Decision 5 explicitly addresses mutual-exclusion: "The 10 sites occupy mutually-exclusive FSM states/roles: Guard-3 (:3368) is initiator LogonSent; the Q3 SendingTime reject (:2400) is established/Active; the 021 arms are Active with specific malformed-122 inbound; the helper's two callers are fromAdmin-veto / no-Application; the BMR is Active fromApp-reject. A single linear session cannot reach all 10." The witness design is per-site cells (Decision 5, tasks T004-T013, T027-T029). Each cell specifies its FSM state in the provocation column. No site is unreachable-by-construction in its cell.
- [x] CHK023 - Is the no-`Application` no-op explicitly covered as its own requirement rather than assumed? [Coverage, Spec §FR-006] — PASS: FR-006 is a standalone functional requirement (not an assumption) that states the no-op obligation across every newly-wired site. Spec.md §Edge Cases §"No Application registered" repeats it as a coverage scenario. T001/T037 are the test tasks. The no-app path is explicitly called out in C1 ("fire_to_admin_ returns true at entry at session.cpp:332 — byte-for-byte no-op"), INV-COV-6, and the quickstart §"No-Application no-op".

## Dependencies & Assumptions

- [x] CHK024 - Is the assumption that both reused helpers (`fire_to_admin_`, the `send_impl` `toApp` block) are noexcept-safe at the boundary stated and traceable? [Assumption, Plan §"noexcept-safe at the boundary"] — PASS: plan.md §"Both callbacks are already noexcept-safe at the boundary" states: "fire_to_admin_ and the send_impl toApp block both invoke the user callback inside parse_and_dispatch_, which converts a throw into app_callback_threw within the noexcept body — so reusing them carries no terminate risk ([[feedback_noexcept_boundary_user_callback_terminate]] is already discharged by the existing helpers; we add no new bare call site)." Traceable to session.cpp:289 (parse_and_dispatch_ definition), :331 (fire_to_admin_ noexcept), and :4149-4164 (send_impl toApp block). The [[feedback_noexcept_boundary_user_callback_terminate]] memory anchor provides cross-session traceability.
- [x] CHK025 - Is the dependency on the existing `app_callback_threw` error (no new variant) documented and validated against the error model? [Dependency, Spec §FR-007] — PASS: FR-007 states "A throwing callback reuses the existing app_callback_threw error." Research Decision 6 confirms "app_callback_threw already exists (error.hpp)." Plan §Summary: "A throwing callback reuses the existing app_callback_threw error." C1 post-condition names "app_callback_threw" and C2 names it. FR-003 and SC-003 both cite it by name. The no-new-variant constraint is stated in the Constitution Check Art. X ("no new error variant"). Verified by grep that app_callback_threw appears in session.cpp at lines 835, 1194, 2120, 2272, 2304 — pre-existing, no new variant.
- [x] CHK026 - Is the assumption that the inbound seqnum is advanced in-memory by `check_inbound` before the BMR build (so only the durable persist is at risk on veto) stated and traceable? [Assumption, research Decision 4 / contracts C2] — PASS: research Decision 4 states explicitly: "the inbound message's seqnum is advanced by check_inbound/persist_inbound_advance_() independently of the outbound BMR, so suppressing the BMR via toApp veto leaves the session consistent — confirmed against the QFcpp incrNextTargetMsgSeqNum()-before-sendRaw ordering." C2 pre-condition states: "the in-memory inbound seqnum was advanced by check_inbound, but its durable persist is downstream at :3279." Traceable to session.cpp check_inbound at :1928 (Active block, before the fromApp dispatch block at :3228). The assumption is stated, traced to the source, and cross-referenced to the QFcpp reference engine.

## Notes

- Items reference spec/plan/research/data-model/contracts sections; `[Gap]` markers would flag missing
  requirements (none identified — the bundle is Gate-A-converged).
- This checklist is dispositioned at pipeline step 9 (`/speckit-checklist-audit`) before `/speckit-implement`.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 26 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **26** |

### SPEC-FIXED items

None.

### DD-DECIDED items

None.

### WAIVED items

None.

Anchors spot-verified: `specs/019-app-callbacks/spec.md FR-008` — resolves in signed-off revision (feature merged, spec.md carries FR-008 at line 103). Referenced line anchors in `src/session/session.cpp` verified: `:332` (fire_to_admin_ early-return for no-app), `:1728` (emit_session_reject_ helper), `:3249` (build_business_message_reject), `:3279` (persist_inbound_advance_ fall-through), `:4149–4164` (send_impl toApp block). QuickFIX `sendRaw` and "assessment 2.4 §2" are grounding references (external clones / Fable docs), not signed-off design-doc anchors — not subject to the anchor spot-verify requirement.
