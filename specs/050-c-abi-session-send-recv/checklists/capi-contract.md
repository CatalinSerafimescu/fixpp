# Checklist — C-ABI contract (050-c-abi-session-send-recv, Feature B)

Quality gate for the design bundle before `/speckit-implement`. Each CHK item is dispositioned by `/checklist-audit` (PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED:<reason>). Categories mirror the ABI/threading/error risk surface of a C-ABI feature.

## A. ABI stability & symbol hygiene (Article X)

- [x] CHK001 All 21 new exported symbols are `extern "C"` `fixpp_*` and appear in `tests/abi/golden/fixpp_capi_symbols.txt`; the per-PR nm gate verifies exported-set == golden AND 0 C++ symbols leak (FR-018; SC-003). — PASS: FR-018 mandates the nm golden gate; T014 appends all 21 symbols to `tests/abi/golden/fixpp_capi_symbols.txt`; T034 runs `check_capi_reentrancy.sh`+nm gate as SC-003; plan §X.6 `[const]` CI gate is cited; symbol count enumerated in plan.md line 28 and tasks.md line 14.
- [x] CHK002 The 5 new error codes are appended at their reserved `[2i §4.3]` slots with NO re-use/re-number of an existing slot (Article X §4); `tools/abi_history/error_codes_v1.txt` appended with `introducing_minor = 3`; `tools/check_capi_occupancy.sh` green (Check A layout, Check B sibling counts). — PASS: T009 appends codes 119/77/129/130/131 at reserved [2i §4.3] slots; T011 records `introducing_minor = 3` in `error_codes_v1.txt` + runs occupancy gate; plan.md line 38 confirms "Check A layout, Check B sibling counts" pass; no slot re-use (all are new slots consistent with the existing Feature A assignments).
- [x] CHK003 C-ABI version is an additive MINOR bump 0.2.0 → 0.3.0 (`version.h` + `c_api.h`); `fixpp_version()` reflects 0.3.0 (FR-020); the `0→1` major freeze stays deferred. — PASS: spec.md FR-020 mandates 0.2.0 → 0.3.0; T006 bumps `FIXPP_C_ABI_VERSION_MINOR` 2→3; T035 asserts `fixpp_version()` returns 0.3.0; major-0 freeze stated in spec.md limitations; no major-bump claim anywhere in the bundle.
- [x] CHK004 `fixpp_decimal_t` boundary PoD is untouched (Article X §3); no struct layout exposed for any config builder (opaque handles only, FR-014). — PASS: T035 asserts `fixpp_decimal_t` PoD untouched; FR-014 mandates opaque handles for all config builders; contracts/config-builders.md confirms all builder families are opaque forward typedefs; data-model E-1 confirms no struct layout exposed; codegraph confirms engine_config.hpp/session_config.hpp are wrapped behind opaque handles.
- [x] CHK005 Codex Gate A ran on every `c_api.h`/`error.h`/`error.cpp` change (`[const §X.6]`) — satisfied by this feature's Gate A (converged round 3). — PASS: plan.md `## Gate A` records three rounds (round 1: Codex+Opus; round 2: Codex+Opus; round 3: orchestrator-inline); review artifacts in `research/reviews/codex_050-*_gate_a_{1,2,3}_review.md` + `opus_050-*_gate_a_{1,2,3}_adversarial_review.md`; converged with P1=0 after round 3 (plan.md line 113).

## B. Reentrancy classification (Article X §5 / [2i §4.10])

- [x] CHK006 Each of the 21 symbols carries exactly ONE reentrancy class, 0 unannotated, verified by `tools/check_capi_reentrancy.sh` (FR-017; SC-002). — PASS: FR-017 mandates exactly one class per symbol; T008 tasks reentrancy doc-blocks on every new symbol; T033/T034 run `check_capi_reentrancy.sh` → green; all 21 symbols enumerated in tasks.md line 14 with their classes pinned in spec.md FR-017; SC-002 is a named success criterion witnessed by T034.
- [x] CHK007 `fixpp_session_send` = `FIXPP_THREAD_SAFE` is recorded as a Gate-A deviation from `[2i §4.10]`'s stale `REQUIRES_SESSION_LOCK` example, in `plan.md ## Gate A`; `[2i]` itself is NOT edited; the carve-out (not from the receive callback) is in the header doc-block. — PASS: plan.md `## Gate A → Round 1` line 117 records RC#1 deviation (`THREAD_SAFE` vs stale `REQUIRES_SESSION_LOCK`); spec.md Clarifications note 29 + FR-017 both state the deviation + flag [2i §4.10]:1176 as stale for erratum; T008 puts the carve-out in the `register_callback` doc-block (FR-013a); `[2i]` is NOT edited (CHK030 cross-verified).
- [x] CHK008 `fixpp_session_close` = `FIXPP_SINGLE_THREAD` (non-callback / non-strand caller; no concurrent close on the same handle); the receive callback's on-strand dispatch = `FIXPP_REQUIRES_SESSION_LOCK`; `fixpp_session_is_established` = `THREAD_SAFE`; `fixpp_engine_create` + config-builder mutators = `SINGLE_THREAD`. — PASS: spec.md FR-017 + contracts/lifecycle-surface.md pin all four classifications; `close` = SINGLE_THREAD because the thunk posts onto the session strand and blocks (on-strand/callback caller would deadlock); the receive-callback on-strand dispatch = REQUIRES_SESSION_LOCK (it runs on the session strand per FR-013, source-verified at engine.cpp:1182-1192 + research D-4); `is_established` = THREAD_SAFE (atomic-safe lookup); `engine_create` + config mutators = SINGLE_THREAD (one-shot construction).

## C. Thunk split & exception safety ([2i §5.2] / §4.2.3)

- [x] CHK009 Every symbol is on exactly one side of the construction-vs-steady split (FR-016): create/open/start/register_callback/config-builders = construction-time (catch→`*_CONFIG`); `send` = steady-state (escaping exception → fatal-log + `std::abort`, NOT translated). — PASS: FR-016 mandates this split; contracts/send-and-receive.md:8 annotates `send` as "steady-state (any escaping exception → fatal-log + abort, NOT translated)"; contracts/lifecycle-surface.md marks create/open/start as construction-time; T012 config-builders are construction-time; T031 marks register_callback as construction-time thunk; SC-006 (T022) witnesses both sides; [2i §5.2] is the authoritative anchor verified to resolve.
- [x] CHK010 No exception crosses any `extern "C"` boundary (FR-019); witnessed by the SC-006 trap fixture (steady-state→SIGABRT; construction-time→`*_CONFIG`+no abort). — PASS: FR-019 mandates zero exception crossing; SC-006 named success criterion; T022 implements the trap fixture in `thunk_split_test.cpp` (steady-state: injected exception → SIGABRT; construction-time: injected exception → `*_CONFIG` + no abort); [2i §5.2] + [2i §4.2.3] anchors verify to resolve.
- [x] CHK011 The `CapiApplication::fromApp` trampoline wraps the C callback in `try/catch(...)` → fatal-log + abort (steady-state on-strand invariant), with zero-global-heap allocation (`[const §VIII.5]`) — verified under mallocnesia (T034a). — PASS: contracts/send-and-receive.md:29 specifies the `try { cb(); } catch (...) { fatal-log + abort }` policy; data-model E-5 has the pseudocode; T032 implements `fromApp` with `try/catch(...) { fatal-log + std::abort }`; T034a mandates mallocnesia interception (not PMR alone, per `feedback_tracking_pmr_resource_false_pass`); the inbound `fixpp_msg_t` is a stack wrapper (D-6), source-verified via research D-4 + contracts/send-and-receive.md:28; [const §VIII.5] anchor cited and plausibly resolves; realizability confirmed CLEAN (CapiApplication complete via application.hpp, not a deferred-spec type).

## D. Error surface (FR-015 / L-049-2)

- [x] CHK012 Exactly 5 reachable UNKNOWN arms are newly published (119/77/129/130/131); `translate()` stays a TOTAL switch, no `default`; each re-pointed arm maps to its specific code (mutation: flip one → RED). — PASS: source-verified in `src/capi/error.cpp` — 119 at line 142, 77 at line 138, 129 at line 212, 130 at line 213, 131 at line 214 — all currently at UNKNOWN; T010 re-points all 5; plan.md `## Gate A → Round 2 + 3` (line 125) confirms "exactly 5"; T010 keeps `translate()` TOTAL with no `default`; T023 mutation-oracle per-row (flip one → RED); [2i §4.3] anchor resolves.
- [x] CHK013 The slot-47-55 threading-block arms (51/52/53/48/54) are LEFT on their existing `THREAD_*` codes — NOT re-pointed to a SESSION block (LEAVE decision, plan ## Gate A Round 2); non-reachable session/app/log/otel/oom variants stay UNKNOWN (residual L-049-2, documented). — PASS: source-verified — `error.cpp` lines 91-104 show 51/52/53/48/54 on THREAD_SESSION_LIFECYCLE/THREAD_CONFIG; plan.md `## Gate A → Round 2` line 127 documents the LEAVE decision with two rationales (L-049-2 scope + taxonomic consistency); T010 explicitly instructs to leave threading-block arms unchanged; residual L-049-2 documented in plan.md line 127.
- [x] CHK014 The send-path reachable set is EXACT (data-model E-4): includes `wire_frame_too_large`(30→WIRE_LIMIT_EXCEEDED), `store_seqnum_overflow`(60→STORE_RUNTIME), `session_already_closed`(52→THREAD_SESSION_LIFECYCLE via the send/close TOCTOU); EXCLUDES `store_io_failure` (I-07-swallowed on send, L-050-3). — PASS: source-verified — (1) `store_io_failure` swallowed at `session.cpp:4790` via `(void)store_r; // store errors → logged-then-proceed (I-07)`; (2) `wire_frame_too_large`(30) at `error.cpp:70` → WIRE_LIMIT_EXCEEDED (existing); (3) `store_seqnum_overflow`(60) at `error.cpp:111` → STORE_RUNTIME (existing); (4) `session_already_closed`(52) at `seqnum_manager.cpp:106` (assign_outbound lock-fail); data-model E-4 authoritative table; L-050-3 documents I-07 swallow; contracts/send-and-receive.md:22 lists the exact return set.
- [x] CHK015 `fixpp_strerror` gains a non-null static (zero-alloc) string for each new code. — PASS: T010 adds static strings per new code (CHK010 re-point task also covers strerror strings per task wording "Add a non-null `fixpp_strerror` static string per new code (zero-alloc)"); zero-alloc is satisfied by static string literals (no heap allocation); `error.cpp:229-235` confirms `translate_for_consumer` exists and the pattern established in Feature A.

## E. Lifecycle correctness (FR-001..007, 021, 022)

- [x] CHK016 The register-then-start model is honoured: `create` → `open`×N (= `register_session`, before start) → `start` (once) → drive → `close` → `destroy`; `open` after `start` → domain error; open ≠ connected (poll `is_established`, FR-022). — PASS: spec.md FR-004 mandates session-open MUST return domain error if called after engine started; FR-021 mandates start-once; FR-022 mandates `is_established` poll for connection status; T015 tests `session_open` after `engine_start` → domain error; T015 tests `is_established` polling; research D-1 confirms C-ABI enforces register-before-start even though C++ layer leaves late registration inert; the constraint is enforced, not merely documented.
- [x] CHK017 `fixpp_engine_destroy` drives `co_await Engine::stop()` UNCONDITIONALLY before delete (never-started engine does NOT abort — FR-003); idempotent + NULL-safe + never-throws; joins the worker thread(s). — PASS: contracts/lifecycle-surface.md explicitly states "unconditionally co_await Engine::stop()" with the `stopped_=false` rationale; source-verified — engine.hpp:199 "stop() idempotent from any state"; engine.hpp:406 `stopped_ = false` (initialized false so stop() on never-started engine is safe); engine.hpp:213 "Destructor: STRICT assert(stopped())" confirms unconditional stop before delete; T015 tests never-started destroy does NOT abort (FR-003); T015 tests double-destroy + NULL-destroy no-op.
- [x] CHK018 `fixpp_session_close` posts `Session::close(graceful)` onto the session domain and bridges back; invalidates the handle on return; no separate session-destroy; uses ASIO native cancellation that closes the transport (FR-005; `[const §XI.2]`). — PASS: research D-5 specifies the post+block bridge design; contracts/lifecycle-surface.md specifies "posts Session::close(graceful) onto session strand + bridges"; FR-005 mandates close invalidates the handle; T020 implements the thunk; data-model E-6 models both cross-thread blocking bridges; [const §XI.2] anchor not independently verified (not a primary audit target for this feature — inherited from Feature A constitution); the close→transport-close chain is source-verified via engine.hpp:280-285.
- [x] CHK019 The internal io_context + worker-thread(s) + work-guard are owned by the C-ABI engine (D-2), constructed inside the worker; the `fork()`-after-create limitation is documented (L-050-2). — PASS: source-verified — engine.hpp:222 "the engine owns NO worker threads" (C++ Engine); the C-ABI CapiEngine wraps it AND adds the io_context+worker+work-guard (research D-2; T013 implements this scaffold); spec.md FR-021 mandates the worker thread; L-050-2 documented in spec.md limitations section; T036 adds L-050-2 to behaviors-and-limitations.md; research D-2 evidence cites engine.hpp:222 explicitly.
- [x] CHK020 NULL handle → `FIXPP_ERR_NULL_HANDLE`; dead/corrupted → `FIXPP_ERR_INVALID_HANDLE` (reused Feature A codes, FR-006). — PASS: FR-006 mandates both codes for null and dead handles; codes were shipped in Feature A (CA-001..004, merged PR #146); T015 tests NULL/dead handle → FIXPP_ERR_NULL_HANDLE/INVALID_HANDLE; contracts/lifecycle-surface.md references FR-006; data-model E-1 notes handle invalidation on close.

## F. Threading & lifetime (Article XI / [arch §4.x])

- [x] CHK021 BOTH cross-thread blocking bridges (close AND send) are modelled (E-6) and TSan-gated on a multi-threaded harness (`feedback_single_threaded_harness_masks_strand_races`); neither is witnessed only single-threaded. — PASS: data-model E-6 explicitly models both bridges; T016 SC-007 witness requires "Multi-threaded harness, TSan-gated (cross-thread close+send bridges, E-6)" — literal phrasing in tasks.md:57; T028 SC-001 headline round-trip is two C-ABI engines on separate threads; send bridge THREAD_SAFE is tested via multi-threaded T028; close bridge SINGLE_THREAD TSan-gated via T016; feedback_single_threaded_harness_masks_strand_races acknowledged in T016 task text.
- [x] CHK022 The `CapiApplication` `SessionId→{cb,userdata}` map is populated at construction-time (before `start`) only — no mutation after start, so no `std::mutex` in a coroutine context (Article XV §9); the `Application` outlives the Engine + every Session. — SPEC-FIXED: The no-mutex argument rests on "no map mutation after start," but neither spec.md FR-011 nor T031 mandated that `fixpp_session_register_callback` actively reject calls made after `fixpp_engine_start` (unlike FR-004 which explicitly mandates a domain error for `session_open` after start). Without enforcement, a post-start `register_callback` races the on-strand `fromApp` read — the data race CHK022 claims to avoid. Fixed: (1) spec.md FR-011 amended to mandate `FIXPP_ERR_CAPI_CONFIG_INVALID` (the existing Feature A construction-time config code, error.h:69=value 10) if called after engine start, mirroring FR-004; (2) contracts/send-and-receive.md doc-comment and prose updated to state enforcement + correct code; (3) tasks.md T031 amended to include reject-after-start check; (4) tasks.md T015 amended to add the register_callback-after-start rejection test. ORCHESTRATOR NOTE: This changes "documented limitation" (tasks.md:157) to "actively enforced" — a new normative requirement. Ratify or revert before unblocking /speckit-implement. Affected: `spec.md:FR-011`, `contracts/send-and-receive.md:§register_callback`, `tasks.md:T031`, `tasks.md:T015`.
- [x] CHK023 The forward-compat downgrade is live: `create` records `consumer_minor`; every fallible return runs `translate_for_consumer` (L-049-1); SC-004 witnesses minor-2 consumer downgrades a minor-3 code → UNKNOWN. — PASS: source-verified — `translate_for_consumer` exists at `src/capi/error.cpp:229` with `constexpr kIntroducingMinor = 2` (uniform Feature A value); T010 adds per-code `introducing_minor=3` for new codes; T017 records `consumer_minor` at engine create; T023 (SC-004) witnesses a minor-2 consumer receiving a minor-3 code downgrades to UNKNOWN; L-049-1 discharge is explicitly modelled; the `kIntroducingMinor` will become per-code lookup per the function's own comment — T023 will fail if not done, ensuring it is not silently skipped.

## G. Receive callback (FR-011..013a)

- [x] CHK024 The receive callback is dispatched synchronously ON the session strand (FR-013, mirrors `fromApp`/`[2d §7.6]`); no `fixpp_session_poll` drain symbol. — PASS: source-verified — `fromApp` callback runs on the per-session strand established at engine.cpp:1163 (`entry.session_strand.emplace(asio::make_strand(exec_))`), with all role loops co_spawned ON that strand at engine.cpp:1182-1192; research D-4 explicitly traces the on-strand proof (application.hpp:67-68 alone insufficient; the proof is from the role-loop spawn location); FR-013 mandates synchronous on-strand dispatch; no `fixpp_session_poll` symbol appears in any contract or the 21-symbol enumeration; [2d §7.6] anchor is in the design doc hierarchy.
- [x] CHK025 The inbound `fixpp_msg_t` is valid ONLY inside the dispatch window (`[2i §4.6]`); the consumer must not retain/destroy it; SC-008 catches a use-after-return under ASan; no callbacks fire after the session closes (FR-012). — PASS: FR-012 mandates both the dispatch-window lifetime + no callbacks after close; contracts/send-and-receive.md:31 specifies "inbound `fixpp_msg_t` lifetime = callback invocation only" + "consumer must NOT retain … after `cb` returns the handle is invalidated (ASan-caught use-after-return, SC-008)"; T029 implements SC-008 as ASan negative test + asserts no callbacks after close; [2i §4.6] anchor verified to resolve.
- [x] CHK026 The FR-013a no-blocking-call-from-callback rule is in the `register_callback` header doc-block; the SUPPORTED reply path (copy-out-then-send-from-a-drain-thread) is witnessed (D-11/T030); no fragile hang-watchdog test. — PASS: FR-013a mandates the rule in the header doc-block; T008 task says "The `register_callback` doc-block MUST state the FR-013a no-blocking-call-from-callback rule"; T030 witnesses the SUPPORTED path (copy-out-then-send-from-drain-thread completing end-to-end); contracts/send-and-receive.md DEADLOCK CONTRACT box explicitly forbids blocking calls from callback; hang-watchdog test deliberately avoided (spec.md FR-013a explains rationale — fragile + no real clients yet).

## H. Test coverage & inheritance fidelity

- [x] CHK027 Each SC-001..008 has a witnessing test task; the headline round-trip (SC-001) drives BOTH ends through the C ABI over loopback plaintext TCP (no mock transport — a mock hides SC-007), with a test-supplied dictionary (L-050-1). — PASS: SC-001 → T028 (headline round-trip, loopback TCP, real transport, test-supplied dictionary); SC-002 → T034 (`check_capi_reentrancy.sh`); SC-003 → T034 (nm gate); SC-004 → T023 (downgrade witness); SC-005 → T021 (config-builder smoke); SC-006 → T022 (thunk-split trap); SC-007 → T016 (close-breaks-blocked-read, LIVE transport, TSan-gated); SC-008 → T029 (use-after-return ASan). Every SC has exactly one witnessing task; no mock transport for SC-001 (task text "over loopback plaintext TCP" explicitly); L-050-1 acknowledged via test-supplied dictionary seam in T003.
- [x] CHK028 The error-arm oracle is mutation-tested per-row by disposition (newly-published → flip-off-UNKNOWN→RED; existing-published → assert unchanged code) — not a "returns a published code" proxy. — PASS: tasks.md line 55 CHK028 description is mirrored in T023 task text which mandates "mutation oracle per-row (D-11)": newly-published arms (flip UNKNOWN off → RED) + existing-published arms (assert unchanged code → not silently re-pointed); data-model E-4 is the per-row authoritative table; spec.md SC-005 (error_block_test) is a named success criterion; the "returns a published code" proxy is explicitly NOT the witness (CHK028 text + task wording both require per-row discriminating mutation).
- [x] CHK029 SC-001's dictionary descope is honest (no pure-C dictionary path claimed; L-050-1 records the Feature-C block); the deferred items (lifecycle callbacks, app-reject, multi-thread executor, post-start registration, outbound construction) are recorded so the completeness audit reads them as intentional, not gaps. — PASS: spec.md Out of Scope + Limitations explicitly lists L-050-1 (dictionary path blocked on Feature C); contracts/config-builders.md confirms Dictionary descoped; tasks.md line 157 lists all deferred items verbatim: "lifecycle callbacks (L-050-x), app-reject-from-callback (D-4), multi-thread executor (D-2), post-start registration (D-1/D-4), outbound `fixpp_msg_*` construction (Feature C)" as intentional deferrals; T036 adds B-050-* + L-050-1/2/3 to behaviors-and-limitations.md; the completeness audit (T039) explicitly reads these as intentional.
- [x] CHK030 `[2i]` is NOT reopened/edited; every inherited decision is either honoured or carried as a recorded Gate-A deviation (reentrancy) with `[2i]` flagged-stale-for-erratum. — PASS: plan.md `## Gate A → Round 1` (line 117) explicitly states "[2i] is NOT reopened or edited by this feature" and flags the §4.10 send example as stale for a future erratum; the plan also says "§4.3 is appended at its reserved slot" — this refers to appending in error.h (not editing [2i] itself); `[2i §4.3]` is only extended by adding new error enum values in the header, which is the normal additive extension the design doc anticipated; [2i] itself (`.specify/2i-capi.md`) has no edits by this feature; the deviation (send=THREAD_SAFE) is recorded per CHK007.

---

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 29 |
| SPEC-FIXED | 1 (with 2 anchor-correction sub-edits) |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **30** |

### SPEC-FIXED items
- CHK-22 — `fixpp_session_register_callback` post-start enforcement gap: FR-011 now mandates `FIXPP_ERR_CAPI_CONFIG_INVALID` (existing Feature A code, error.h:10) if called after engine start; contracts/send-and-receive.md doc-comment and prose updated; tasks.md T031 amended to enforce reject-after-start; tasks.md T015 amended to test the rejection. **ORCHESTRATOR NOTE: This changes tasks.md:157's "documented limitation" (post-start registration) to "actively enforced" — a new normative requirement. Ratify or revert before unblocking /speckit-implement.** Affected: `spec.md:FR-011`, `contracts/send-and-receive.md:§register_callback`, `tasks.md:T031`, `tasks.md:T015`.
- Anchor sub-fix (no CHK re-number) — `[2i §3.13]` cited in spec.md FR-004 and Normative References as "config-by-value at session open + session-handle validity window" was a dangling reference: `[2i §3.13]` is actually "store errors from `[2e §6.7]`." Corrected both cites to `[2i §3.4]` (verified: `.specify/2i-capi.md` line 154 = "§3.4 From `[arch §5.6]` — frozen-at-open rule" which contains the config-by-value + handle validity text). Affected: `spec.md:FR-004`, `spec.md:Normative References`.

### DD-DECIDED items
(none)

### WAIVED items
(none)

Anchors spot-verified: `[2i §3.4]` (line 154 — frozen-at-open / config-by-value / handle validity), `[2i §4.2]` (line 346), `[2i §4.3]` (line 473), `[2i §4.4]` (line 610), `[2i §4.5]` (line 659), `[2i §4.6]` (line 723), `[2i §4.9]` (line 1152), `[2i §4.10]` (line 1168), `[2i §5.2]` (line 1222), `[2i §7.9]` (line 1498), `[2i §8]` (line 1546), `[2i §10]` (line 1605) — all resolve in signed-off revision `.specify/2i-capi.md`. Previously-cited `[2i §3.13]` was a dangling ref (store errors) — corrected to `[2i §3.4]` (see SPEC-FIXED anchor sub-fix above).
