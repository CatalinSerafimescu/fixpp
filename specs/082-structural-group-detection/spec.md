# Feature Specification: Structural Repeating-Group Detection for Legacy FIX Dictionaries

**Feature Branch**: `082-structural-group-detection`

**Created**: 2026-07-29

**Status**: Draft

**Input**: Replace the datatype-gated repeating-group detection predicate (`FieldRef::type == field_data_type::NumInGroup`) with a **structural** one derived from the `<group>` element itself, at every detection site — the runtime `Dictionary::as_table_view()` registration loops and the codegen emitters — and re-instate the `fixpp::v42` typed builder tier that feature 077 had to descope. Closes GitHub **#196**; resolves **L-063-1**, **L-061-1**, **L-066-1**, **L-077-1**.

## Context

Repeating-group detection is gated on the **XML-declared field datatype** at every site on the **read/validate and codegen** paths:

- runtime — `Dictionary::as_table_view()`, both the legacy bare-`no_tag` registration loop (`src/dictionary/dictionary.cpp:398`) and the 063 context-scoped loop (`:441`/`:446`);
- codegen — `emit_messages.cpp:166/234/337/347/425`, `emit_builders.cpp:606`, `emit_reify.cpp:217/227`.

**It is not gated on datatype everywhere, and that asymmetry is the strongest corroboration of this feature's thesis.** The C-ABI **outbound write** path already decides group-ness **structurally**, at four call sites, with precisely the accessor this feature adopts: `is_group_collision()` (`src/capi/message_write.cpp:157` — `h->dict_->group_first_field(tag) != 0`), the delimiter resolve at `:719`, and the `fixpp_msg_group_begin` / nested-group entry gates at `:812` / `:923`. So the pre-082 library is **asymmetric**, not uniformly group-blind: structural on the C-ABI write side, datatype-gated on the read/validate side. `fixpp_msg_group_begin(268)` on a FIX 4.2 dictionary **already succeeds today**. The structural predicate is therefore already in production and already correct; this feature converges the read/validate side onto it (see FR-006, C1.3 P4, and research D-1/D-7).

FIX 4.0 / 4.1 / 4.2 declare **every** `<group>` count field with the legacy XML type `INT`, never `NUMINGROUP`. Those three dictionaries therefore register **zero** repeating groups: the shipped `fixpp::v42` read flyweight has zero `class G_` accessors, `as_table_view()`-driven group parsing and validation are inert, and the 077/078 typed builder tier had to exclude `v42` at the codegen driver (`tools/codegen/fixpp-codegen/main.cpp`, `ir.ns != "v42"`) — because a scalar-only `build_<Msg>` would **silently omit a `required='Y'` repeating group and emit invalid FIX 4.2**, violating Article VI (100% FIX / no silent omissions).

The fix is a predicate replacement: derive group-ness from the `<group>` element, which both loaders already track independent of the field's datatype.

### Pre-spec census (evidence for the FR shape)

Comparing, per dictionary, the **type set** `{tag : the field definition's type is NUMINGROUP}` against the **structural set** `{tag : a <group name=N> element exists, N → tag}`:

| dictionary | type | struct | verdict |
|---|---:|---:|---|
| FIX40 | 0 | 4 | structural-only (+4) |
| FIX41 | 0 | 7 | structural-only (+7) |
| FIX42 | 0 | **18** | structural-only (+18) — the #196 target |
| **FIX43** | 34 | 34 | **sets DIVERGE on two tags**; effective delta **+1 tag (576)** (US3) |
| FIX44 | 59 | 59 | equal — no-op |
| FIX50 | 69 | 69 | equal — no-op |
| FIX50SP1 | 99 | 99 | equal — no-op |
| FIX50SP2 | 507 | 507 | equal — no-op |
| FIXT11 | 1 | 1 | equal — no-op |
| Orchestra FIX Latest | 524 | 524 | equal — no-op (after codeset resolution) |

No `<group>` element in any vendored dictionary has zero members (`predicate_census.py` emits no zero-member warning for any of the ten, and an independent walk of FIX40/41/42/43 confirms it), so a member-derived structural predicate loses nothing **on today's data**. That is a statement about the data, not about the predicate's algebra — see § Edge Cases, FR-023, and research D-1a for the zero-member case's disposition (rejected at load).

This table is reproducible: `contracts/predicate_census.py` is the checked-in, non-circular raw-XML oracle that produced it (it loads neither `Dictionary`/`table_view` nor the codegen IR, so it is independent of the predicate under change). FR-018 requires it to become the source the 063 census helper reads.

**Orchestra / `vlatest` is unaffected.** A raw-XML count initially read 523 typed vs 524 structural because tag **552 `NoSides`** is declared `type='NoSidesCodeSet'`; that codeset itself is `type='NumInGroup'`, so the loader already resolves it. The shipped 076 golden confirms it: `G_552` is present and there are 524 distinct `class G_`. This is also why byte-identity of the unaffected tiers is written below as a **requirement pinned by a golden diff**, not as an assumption — a raw-XML census is not loader truth.

### The FIX43 divergence — both cases are upstream dictionary typos

- **tag 576 `NoClearingInstructions`** is typed `INT` (`FIX43.xml:4069`) but **is** a `<group required='N'>` with member `ClearingInstruction` (`FIX43.xml:1918`). FIX44 types it `NUMINGROUP` (`FIX44.xml:5637`) and declares the same group. Today FIX43 is **group-blind on a real repeating group** — this is a live defect and the one effective FIX43 delta.
- **tag 82 `NoRpts`** is typed `NUMINGROUP` (`FIX43.xml:2596`) but is **never** a `<group>` anywhere; it is used as a plain field inside `<message name='ListStatus'>` (`FIX43.xml:728`). FIX44 types the same tag `INT` (`FIX44.xml:4095`) and likewise uses it as a plain field. The datatype gate *nominates* tag 82 as a group — but **both registration stores already reject it downstream**: the legacy bare store via `group_first_field(82) == 0` (`dictionary.cpp:402`; `group_first_field_impl` binary-searches the structural `groups_` table and returns 0 for a non-`<group>` tag, `dictionary.cpp:92`), and the context-scoped store via its `members.empty()` guard (`dictionary.cpp:463`). **Tag 82 is therefore already unregistered today, and this feature does not change that.** What changes is *why*: the rejection becomes principled (the dictionary does not declare a `<group>`) instead of incidental (a downstream guard happens to catch it).

**Effective FIX43 delta: exactly one tag — `+1 tag (576)`.** Tag 82 is a **no-regression pin**, not a behavior change. *(Notation: `+1 tag (576)` names a cardinality delta of one and identifies the tag. The bare form `+576` is not used, because it sits beside integer count columns where `+4`/`+7`/`+18` **are** cardinalities.)*

**Why pure structural rather than a union (`type OR structural`).** The honest case is *not* that a union misbehaves today — the same downstream guards that already reject tag 82 would reject it under a union too, so union and structural produce identical results on all ten dictionaries as they stand. The case is design integrity: a union keeps two sources of truth for one property and leaves a latent trap (a future dictionary declaring a `NUMINGROUP`-typed non-group tag that *does* acquire members would register spuriously, with no guard left to catch it), keeps `FieldRef::type` semantically overloaded as both datatype and group marker, and costs more code for zero present benefit. One predicate, one source.

### Structural truth is already available on both sides — this is a re-point, not new plumbing

Both group-declaration walks already key on the **element name**, never on the datatype:

- runtime — `src/dictionary/xml_loader.cpp:580` (`else if (tag_name == "group")`) builds the `Dictionary` group table, and `Dictionary::group(no_tag)` / `group_fields(no_tag)` expose it. The raw structural accessors are correct for FIX40/41/42 **today** (L-063-1 notes `group_fields(382)` on FIX42 returns its 4 members); only `as_table_view()`'s registration loops re-derive group-ness from the datatype and therefore register nothing.
- codegen — `tools/codegen/fixpp-codegen/ir.cpp:80` (`walk_level`, `else if (tag_name == "group")`) builds each message's declaration-order `group_order` (the 067/R9 `GroupOrderEntry` structure). So `MessageIR::group_order` is **already correctly populated for FIX42 today**. The emitters nonetheless re-derive group-ness from `FieldRef::type` over the tag-deduped `MessageIR::fields` run — `emit_builders.cpp:606` (`top_level_synthetic_members`) and `emit_messages.cpp:425` (`group_tags` collection) are the two discovery sites.

The work is therefore to re-point each consumer at a structural source that already exists, not to add dictionary parsing. `FieldRef::type` is **not** changed by this feature — it keeps carrying the declared datatype (`INT` for FIX42's count fields), which is what makes the byte-identity claims in FR-015/FR-016 checkable.

### Two existing tests encode the descope and must invert

`tests/codegen/test_077_builder_no_emit.cpp` (`V42EmitsNoBuilders` — asserts `v42/all.hpp` and `v42/messages/` are **absent**) and `tests/codegen/test_077_v42_vt11_completeness_and_c4.cpp` (defines the v42 builder-completeness expected set as **∅ by policy**, while recording that FIX42 really has 39 application messages) are the 077 descope pins. They are the natural RED→GREEN witnesses for US2.

## Clarifications

### Session 2026-07-29

- Q: Registering groups on FIX40/41/42 changes (1) inbound parse/field-addressing **unconditionally** — `session.cpp:992` builds `inbound_tv_ = dictionary->as_table_view()` in `open()` regardless of `validate_inbound_messages`, and 066 flipped the parser onto it — and (2) group-membership + 079 per-group required-member enforcement, reached only when strict validation is already opted into. What compat posture should the spec take? → A: **Accept both, no new gate.** The parse/addressing correction ships unconditionally (today's behavior is documented-wrong — absent or positionally-wrong reads); the newly-activated strictness rides the **existing** `validate_inbound_messages` opt-in rather than a second knob. Both are recorded as a named behavior change plus a release note in `spec/behaviors-and-limitations.md`.
- Q: US4 — the `v42` grouped/nested write exemplar closing L-061-1 needs a QuickFIX-derived wire golden plus exemplar-suite wiring on top of US1+US2. Keep it in this feature or split it out? → A: **Keep in 082 at P3.** L-061-1 is one of the four limitations #196 names for closure, and the exemplar is the concrete proof that the `v42` builder tier is *usable* rather than merely emitted. Cost is bounded — one message, one golden capture, reusing the existing 061 exemplar harness.

### Session 2026-07-30 (Gate A round 1)

- Q: `/speckit-clarify` closed the zero-member-`<group>` question as "moot — both structural sources are already member-independent", but the runtime source refutes that (`xml_loader.cpp:608-649` stores `first_field_tag = 0`; `dictionary.cpp:92-99`'s sentinel is ambiguous). What contract does the predicate actually owe? → A: **Not member-independence.** The predicate owes the property that discriminates the rejected alternative: it MUST **not be derived from a message's own field-run membership** (`{fr.group_no_tag : != 0}` / a `members.empty()` test), because that conflates "**D** declares a group with count tag T" with "T has members **in this message**". The zero-member `<group>` case is recorded as an explicit **non-property with its representational reason** — `table_view` cannot express a delimiter-less group (`set_group_first(t, 0)` would register member tag 0), and the 063 context store's `members.empty()` guard defeats the property in one of the two stores under *any* predicate. No vendored dictionary declares one. Integrated into § Edge Cases, FR-006, C1.3 P1 and research D-1a. `Dictionary::group(t).has_value()` was considered and **rejected** — it delivers the property at the producer, breaks it at the consumer, and forks the runtime tier's predicate away from the four already-structural C-ABI write sites. **[The non-property half of this answer was SUPERSEDED on 2026-07-30 by the user's OD-1 decision: the member-less `<group>` is now a *load error* in both loaders (FR-023), so the state is unreachable rather than tolerated. The "not derived from a message's own field-run membership" absolute is unaffected and still binds. See § Open decisions OD-1 and § Edge Cases.]**
- Q: `/clarify` also concluded that "exactly one detection path" (FR-006) is a property the design delivers. Is it? → A: **Not as originally scoped.** There is a **fourth** group-ness decision path: the C-ABI outbound write path already decides structurally at `src/capi/message_write.cpp:157/719/812/923`. This *strengthens* the design — the chosen predicate is already in production — but it must be named, or "one predicate" is asserted rather than established. FR-006 now names the runtime tier's single predicate as `group_first_field(t) != 0`, shared by `as_table_view()` and the capi write path. Integrated into § Context, FR-006, research D-1/D-7, and C4.4 (split into read and write legs).

## User Scenarios & Testing *(mandatory)*

### User Story 1 - FIX 4.0/4.1/4.2 repeating groups become visible (Priority: P1)

An integrator working against a FIX 4.2 counterparty loads the vendored `FIX42.xml` and reads an inbound `MarketDataSnapshotFullRefresh`. Today the `NoMDEntries(268)` repeating group is invisible: the generated `fixpp::v42` class has no typed group accessor, and a C-ABI / typed group query on what **is** a real wire-level repeating group returns `TYPE_MISMATCH`/absent rather than a membership-bounded read (L-066-1). This story makes all 18 FIX42 groups (and FIX40's 4, FIX41's 7) materialize — typed read accessors in the regenerated `v42` tier, and real group registration in `as_table_view()` for all three dictionaries.

**Why this priority**: It is the root cause. Every other story in this feature is blocked on it, and it alone converts three shipping dictionaries from group-blind to membership-correct.

**Independent Test**: Load `FIX42.xml`, call `as_table_view()`, and assert all 18 group tags are registered with their declared members (today: 0). Separately, regenerate the `v42` read tier and assert the emitted header contains typed group accessors for those 18 tags (today: 0 `class G_`).

**Acceptance Scenarios**:

1. **Given** the vendored `FIX42.xml`, **When** `as_table_view()` is built, **Then** exactly the 18 structurally-declared group tags are registered — {33, 73, 78, 124, 136, 146, 199, 215, 267, 268, 295, 296, 382, 384, 386, 398, 420, 428} — each with its declared member set, and no others.
2. **Given** the vendored `FIX40.xml` / `FIX41.xml`, **When** `as_table_view()` is built, **Then** exactly their 4 / 7 structurally-declared group tags are registered.
3. **Given** a FIX 4.2 inbound `MarketDataSnapshotFullRefresh` carrying a populated `NoMDEntries(268)` group, **When** a typed or C-ABI group read is issued, **Then** it returns a membership-bounded read of the group's entries rather than `TYPE_MISMATCH`/absent.
4. **Given** the regenerated `v42` read tier, **When** the nested `MassQuote` structure is inspected, **Then** the `NoQuoteSets(296) → NoQuoteEntries(295)` nesting is expressed as a nested typed group (5 nested group occurrences exist in FIX42 messages), not flattened.
5. **Given** the six dictionaries whose type set and structural set are equal (FIX44 / FIX50 / FIX50SP1 / FIX50SP2 / FIXT11 / Orchestra FIX Latest), **When** `as_table_view()` is built, **Then** the registered group set is **exactly** what it was before this feature.

---

### User Story 2 - The `fixpp::v42` typed builder tier is re-instated (Priority: P1)

A developer constructing outbound FIX 4.2 messages wants the same typed `build_<Msg>` / `validate_<Msg>` / `<Msg>Args` surface that `v44`, `v50sp2`, and `vlatest` already ship (077/078). Today `v42` is excluded at the codegen driver, so the only option is the untyped runtime `wire::body_builder` path. With groups materialized (US1), `v42` can emit a builder tier whose `Args` carry its repeating groups — so a `required='Y'` group can no longer be silently omitted.

**Why this priority**: This is the deliverable issue #196 actually asks for. It is the user-visible outcome; US1 is the enabling mechanism.

**Independent Test**: Run the codegen driver without the `v42` exclusion and assert the 078 split builder/validator file set is emitted for `v42`; then build a `NewOrderList` via `build_NewOrderList` with a populated `NoOrders(73)` group and assert the emitted bytes match a QuickFIX-derived golden.

**Acceptance Scenarios**:

1. **Given** the codegen driver, **When** it runs over `FIX42.xml`, **Then** it emits the full 078 split builder/validator layout for `v42` (`messages/<Msg>.hpp`, `groups/<PlanName>.hpp` + `groups.hpp`, `validators/traits.hpp`, `all.hpp`) covering its application messages — 21 of the 39 carry at least one repeating group, and the shared plan region is **28** `groups/<PlanName>.hpp` headers (226 files total) under the default `--families all` mode.
2. **Given** a `v42` message whose group is declared `required='Y'` (14 such message/group pairs across 12 messages, e.g. `NewOrderList`/`NoOrders`, `MarketDataRequest`/`NoRelatedSym`, `MassQuote`/`NoQuoteSets`), **When** `validate_<Msg>` runs on an `Args` value that omits that group, **Then** it **rejects** — the group is representable in `Args`, so its absence is detectable rather than silent.
3. **Given** a populated `v42` `Args` value, **When** `build_<Msg>` emits, **Then** the wire bytes match an independently-derived (QuickFIX) golden for that message, groups included.
4. **Given** the `v42` builder tier, **When** the per-version completeness census runs, **Then** every application message and every declared group plan is accounted for, with **no** silently-skipped message or group.

---

### User Story 3 - FIX43's two mis-typed group-count tags are corrected (Priority: P2)

An operator running a FIX 4.3 session under dictionary-backed parsing is affected by a latent dictionary-typo defect that the predicate change resolves: a **real** repeating group, `NoClearingInstructions(576)`, is typed `INT` and is therefore currently unregistered and group-blind. The companion typo — plain field `NoRpts(82)` typed `NUMINGROUP` — is already rejected by both stores' downstream guards, so it carries a **no-regression pin** rather than a behavior change.

**Why this priority**: It is unavoidable once the predicate changes — it cannot be deferred to a later feature — but it is a discovered latent-defect fix, independent of what #196 asks for. It is separated so its delta and pins are visible in their own right and are not buried under the `v42` story.

**Independent Test**: Load `FIX43.xml`, build `as_table_view()`, and assert tag 576 **is** registered as a group with member `ClearingInstruction` (it is not today); separately assert tag 82 remains **un**registered as a group and `ListStatus` still reads it as a plain required field.

**Acceptance Scenarios**:

1. **Given** the vendored `FIX43.xml`, **When** `as_table_view()` is built, **Then** tag **576** **is** registered as a repeating group with member `ClearingInstruction` (it is not registered today).
2. **Given** a FIX 4.3 inbound message carrying a populated `NoClearingInstructions(576)` group, **When** a typed or C-ABI group read is issued, **Then** it returns a membership-bounded read rather than `TYPE_MISMATCH`/absent.
3. **Given** the same view, **When** tag **82** is queried, **Then** it is **not** registered as a repeating group — unchanged from today, now because the dictionary declares no `<group>` for it rather than because a downstream guard rejects it.
4. **Given** the same view, **When** a `ListStatus` message carrying tag 82 is validated, **Then** tag 82 is still accepted and enforced as a **plain required field** — the predicate change must not make the field unknown or optional.
5. **Given** FIX43's other 33 group tags, **When** `as_table_view()` is built, **Then** their registration is unchanged. Exactly one tag moves.

---

### User Story 4 - A grouped/nested FIX 4.2 write exemplar becomes expressible (Priority: P3)

The exemplar suite (feature 061) has no FIX 4.2 grouped or nested **write** exemplar — all five exemplars are forced to `fixpp::v44` because `v42` emits zero typed groups (L-061-1). With US1 + US2 delivered, a grouped market-data write becomes expressible in `v42`.

**Why this priority**: It is the demonstration that closes L-061-1, and the concrete evidence that the builder tier is usable rather than merely emitted. It depends on US1 and US2 and delivers no value without them, so it is sequenced last.

**Independent Test**: Add a `v42` exemplar that constructs a nested-group message — `MassQuote` with `NoQuoteSets(296)` containing `NoQuoteEntries(295)` — and assert its bytes against an independently-derived golden plus a read round-trip.

**Acceptance Scenarios**:

1. **Given** the `v42` builder tier, **When** a `MassQuote` with a populated `NoQuoteSets(296) → NoQuoteEntries(295)` nesting is constructed and emitted, **Then** the wire bytes match an independently-derived golden.
2. **Given** those emitted bytes, **When** they are parsed back through the `v42` read tier, **Then** every field and both group levels round-trip field-for-field.

---

### Edge Cases

- **A group-count tag reused across contexts with different member sets.** The 063 context-scoped registration keys per `(message, parent path, no_tag)`. A newly-visible FIX42 group tag must register **per context** with the *right* members, not collapse to a single bare-`no_tag` entry — and the legacy bare-`no_tag` loop and the context-scoped loop must move **together** (a half-restructure would leave the two views disagreeing). The pin must use a tag whose member sets genuinely **diverge**, or a collapse is unobservable and the pin cannot fail. The example is **`NoRelatedSym(146)`**: 6 occurrences across `News`, `Email`, `QuoteRequest`, `MarketDataRequest`, `SecurityDefinitionRequest`, `SecurityDefinition`, carrying **4 distinct direct-member lists** (verified by an independent walk of `dictionaries/FIX42.xml`) — e.g. `MarketDataRequest`'s 20 members vs `QuoteRequest`'s 31 vs `SecurityDefinitionRequest`'s 22 (an almost entirely disjoint member set — `SDR ∩ MDR = ∅`). `NoOrders(73)` is the secondary: 3 occurrences, 3 distinct lists (74 / 5 / 10 members). **The observable to assert** is that the context store holds *distinct member sets* per `(msg_type, path)` for tag 146, while the bare store holds the loader's first-seen set. *(`LinesOfText(33)` was the example in the previous revision and has been replaced: its two occurrences, `News` and `Email`, have **identical** member lists `{58, 354, 355}`, so a context collapse on tag 33 is unobservable — the bare entry and both context entries carry the same members.)*
- **A `<group>` element with zero members — REJECTED at load (FR-023; OD-1 resolved by the user 2026-07-30).** None exists in any vendored dictionary today. It is **not** the case that both structural sources are member-independent, and the previous revision's claim to that effect was false at the source: `xml_loader.cpp:608-649` initialises `first_field_tag = 0` (`:610`) and records the `GroupDef` unconditionally (`:644`/`:649`), while `group_first_field_impl` (`dictionary.cpp:92-99`) returns 0 both for "not a group" **and** for "declared group whose first field is 0" — an ambiguous sentinel. So a member-less `<group>` would collapse to "not a group" at the runtime tier under **any** predicate keyed on the delimiter. The reason that state is now **rejected at the loader rather than documented around** is **representational**: `table_view` cannot express a delimiter-less group — `set_group_first(t, 0)` (`table_view.hpp:570-575`) would set the group bit *and* insert **member tag 0**, a malformed registration the parser and validator would consume — and the 063 context store's `if (members.empty()) continue;` guard (`dictionary.cpp:463`) defeats visibility in one of the two stores regardless. A declaration with no usable representation downstream is a **malformed dictionary**, and `xml_loader.cpp` already says exactly that for both of its sibling group cases (`:584`, `:1017`) — FR-023. **Do not overstate the consequence**: `group_first_field`'s sentinel is still ambiguous *in isolation*; what FR-023 changes is that the ambiguous **input** can no longer reach it, because no `Dictionary` the loaders admit carries a member-less group. What the predicate MUST satisfy independently of that is the property that actually discriminates the rejected alternative: it must **not be derived from a message's own field-run membership** (`{fr.group_no_tag : != 0}` / a `members.empty()` test), because that conflates "**D** declares a group with count tag T" with "T has members **in this message**". That absolute binds the **detection predicate** only: `dictionary.cpp:463`'s guard is a *post-detection* registration guard, unchanged by this feature and outside its scope (contract C1.3 P1 / P1-NON).
- **A FIX40/41/42 consumer reading a tag that lives inside a repeating group.** Because the parse-side change is ungated (FR-006a), such a read changes shape for **every** FIX40/41/42 session, not only strict-validation ones — today it resolves flat/positionally, afterwards it is group-scoped. This is the intended correction, and it needs a before/after pin taken with `validate_inbound_messages` **off** so the two axes are not conflated.
- **A tag that is a group count in one dictionary and a plain field in another.** Tags 82 and 576 are exactly this across FIX43/FIX44. Detection must be resolved **per dictionary**, never globally by tag.
- **The 063 census helper is built on the predicate being changed.** `tests/dictionary/reused_tag_census.hpp:74,80` established the L-063-1 carve-out using `fr.type == NumInGroup`. If it is simply flipped to the new predicate it moves in lockstep with the code under test and witnesses nothing — it must be re-pointed to an independent source.
- **`vt11` (FIXT.1.1) has one group and zero application messages.** It must stay self-skipping in the builder tier (it is excluded by an empty registry, not by a version predicate) and its read golden must not move.
- **Nested group depth.** FIX42 has 5 nested group occurrences. The emitter's existing depth bound and its deterministic group-emission ordering must hold for the newly-visible legacy groups.

## Requirements *(mandatory)*

### Functional Requirements

**Core — structural detection**

- **FR-001**: Repeating-group **detection** MUST be derived from the dictionary's **structural** group declaration (the `<group>` element / the loader's group table), **not** from the group-count field's declared datatype, at **every detection site**: the `as_table_view()` legacy bare-`no_tag` registration loop, the `as_table_view()` 063 context-scoped registration loop, and the codegen emitters `emit_messages`, `emit_builders`, `emit_reify`. **No *detection* site may retain the datatype gate.** The absolute is scoped to detection: `field_data_type::NumInGroup` remains the correct source wherever the question genuinely *is* the datatype, and those occurrences MUST NOT be changed — `emit_manifest.cpp:73` (the datatype→token name emitted into the manifest's datatype column), `gen_util.hpp:162` (`TypeKind` mapping), the two loaders' type-name tables, `field_type.hpp:70`, and `src/capi/message_write.cpp:134-137` (`is_int_category`). See research D-7 for the full line-site census with dispositions. **`emit_manifest` is not a detection site**: it is a single exhaustive `field_data_type → snake_case token` switch (`emit_manifest.cpp:65-130`, `case NumInGroup: return "num_in_group";` at `:73-74`) with no group branching. The manifest's group axis is `OccurrenceIR::group_path` (`ir.hpp:79`), which is **already structural** — populated from the structural walk at `ir.cpp:354`/`:396`, never from a datatype. Note that `MessageIR::occurrences` is populated **only on the Orchestra path** (`populate_orchestra_projection`, `ir.cpp:476`), so `emit_manifest` returns empty for every `<fix>`-schema version (`emit_manifest.cpp:154-166`) and `main.cpp:29`'s empty-content skip writes no file: **`Manifest.txt` is a `vlatest`-only artifact and `v42` has none** — see FR-016.
- **FR-002**: The predicate MUST be a **replacement**, not a union with the datatype gate. A tag that is declared `NUMINGROUP` but is never a `<group>` MUST NOT be registered as a group (FIX43 tag 82); a tag that is a `<group>` but is declared `INT` MUST be registered (FIX43 tag 576, and every FIX40/41/42 group tag).
- **FR-003**: Detection MUST be resolved **per dictionary**. The same tag may be a group count in one dictionary and a plain field in another; no global tag-keyed group set may be introduced.
- **FR-004**: The two `as_table_view()` registration loops (legacy bare-`no_tag` and 063 context-scoped) MUST be changed **together** and remain mutually consistent — no configuration or code path may leave one structural and the other datatype-gated. **The pin MUST assert the named post-condition directly, not a tag-set projection.** The two stores are keyed differently (bare = `no_tag`; context = `(msg_type, parent path, no_tag)`), so "the two stores agree" can only mean a tag-set projection — which passes while every per-context member set is wrong. The pin MUST therefore assert, for a **divergent-signature** tag (`NoRelatedSym(146)`, 4 distinct member lists across 6 occurrences — § Edge Cases), that the **context store holds the distinct member set for each `(msg_type, path)`** matching the FR-018 oracle per context, **and** that the bare store holds the loader's first-seen set. Exact-set equality in FR-005/FR-014/SC-001/SC-002 binds the **bare** store's tag set; the context store is pinned **separately** by per-context member-set equality against the oracle.
- **FR-005**: For FIX40 / FIX41 / FIX42, `as_table_view()` MUST register exactly the structurally-declared group set — 4 / 7 / 18 tags respectively — with each group's declared member set, in place of today's empty registration.

**Legacy-session behavior change (accepted, ungated)**

- **FR-006**: The FIX40/41/42 behavior change MUST ship **ungated** — no new configuration key, and no per-dictionary opt-in for structural detection. There MUST remain exactly one detection path per tier, and it MUST be a **named, single-sourced** one: the **runtime tier's** predicate is `Dictionary::group_first_field(t) != 0`, shared by `as_table_view()`'s two registration loops **and** the four already-structural C-ABI write sites (`src/capi/message_write.cpp:157/719/812/923`); the **codegen tier's** is membership in `VersionIR::group_tags`. Adopting any *other* accessor in `as_table_view()` — including `Dictionary::group(t).has_value()` — is non-conforming, because it would leave two divergent structural realizations inside the runtime tier (research D-1, C1.3 P4).
- **FR-006a**: The **inbound parse / field-addressing** change is unconditional and MUST be treated as a correctness fix. Verified end-to-end, both ends of the path: `Session::open()` builds `inbound_tv_` from `as_table_view()` regardless of `validate_inbound_messages` (`src/session/session.cpp:992`), and `parse_and_dispatch_` **consumes** it unconditionally — `Parser<access_mode::Index> pd_parser{*inbound_tv_}` (`src/session/session.cpp:328`, guarded only by an `assert` on the invariant, with no strict-flag condition anywhere on the path), which wires `group_member_fn` into the group context (`include/fixpp/wire/parser.hpp:306`). So once groups register, a FIX40/41/42 session's reads of tags inside a repeating group become **membership-bounded** where they were previously absent or positionally-wrong. This MUST be pinned by a before/after behavior test with strict validation **off**, demonstrating the corrected read.
- **FR-006b**: The newly-activated group-membership and 079 per-group required-member **enforcement** MUST ride the **existing** `validate_inbound_messages` opt-in — no additional gate. A FIX 4.0/4.1/4.2 session that has opted into strict inbound validation MAY consequently reject messages it previously accepted (FIX42 declares `required='Y'` members inside groups at 14 message/group pairs); with the flag off (the default), no new rejection path is reachable.
- **FR-006c**: Both changes MUST be recorded in `spec/behaviors-and-limitations.md` as a **named behavior change with an operator-facing release note**, stating explicitly that FIX40/41/42 inbound group reads change shape and that strict-validation deployments on those versions may see new rejects.

**`v42` typed builder tier (issue #196)**

- **FR-007**: The codegen driver's `v42` builder-tier exclusion (`ir.ns != "v42"`) MUST be removed, and `v42` MUST emit the full 078 split builder/validator layout — per-message declaration headers, the shared per-plan groups region plus its umbrella, the validator traits header, and the `all.hpp` umbrella — on the same terms as `v44` / `v50sp2` / `vlatest`.
- **FR-008**: Every `v42` `<Msg>Args` MUST represent the message's declared repeating groups. `validate_<Msg>` MUST reject an `Args` value that omits a group declared `required='Y'` — no group may be unrepresentable in `Args` and therefore silently omitted (Article VI). The pin MUST cover all **14** message/group pairs, and MUST account for the fact that **one of the 14 is structurally different**: 13 sit at message top level, but `MassQuote` / `NoQuoteEntries(295)` is **nested inside** the required `NoQuoteSets(296)`. In 077's emitted validator that pair is checked **per-entry** (`gc.validate_entry(args, i)`, recursing into the parent's entries), not by a top-level `group_checks` row — so for that pair "omission" means *an entry of 296 carrying an empty 295 span*, a different construction from the other 13. A pin that only builds top-level omissions covers 13 plus a lookalike, not 14.
- **FR-009**: `v42` builder output MUST be pinned on the **same terms as the existing versions**, which are two distinct instruments, not one: (a) a new checked-in **golden set** in the 078 golden region for the default `--families all` mode — the 078 split file set, wired into the codegen golden-set-matching test; and (b) for `--families official`, a **structural witness**, *not* a golden. 078 deliberately retired the pre-078 official-mode byte-identity gate (`tests/codegen/determinism_test.cpp:898-909`: "no `v44-official/` golden set is checked in") and replaced it with `OfficialModeBuildersStructuralShape` (`:920-948`), which pins the per-**mode** emitted file-SET shape and the `builder_registry` array cardinality against a fresh isolated `--families official` run — the leg that catches a wrong-subset regression the default-mode golden cannot see. `v42`'s obligation is an instantiation of that witness with the derived figures: `all` → **226** files (39 × 5 + **28** `groups/<PlanName>.hpp` + 3 shared), registry **39**; `official` → **147** files (25 × 5 + **19** plan headers + 3), registry **25**. See research D-9a/D-10 for the derivation and its validation against the three shipped tiers.
- **FR-010**: `vt11` MUST remain excluded from the builder tier by its **empty application-message registry** (self-skip), not by any version-name predicate. The removal of the `v42` predicate MUST NOT introduce a replacement version-name predicate for any version.

**FIX43 corrections**

- **FR-011**: `as_table_view()` for FIX43 MUST register tag **576 `NoClearingInstructions`** as a repeating group with member `ClearingInstruction`, making its inbound reads membership-bounded. This is the one effective FIX43 behavior change.
- **FR-012**: `as_table_view()` for FIX43 MUST continue to leave tag **82 `NoRpts`** unregistered as a group, and `ListStatus` MUST continue to accept and enforce tag 82 as a plain **required** field. This is a **no-regression pin**: tag 82 is already unregistered today (rejected by `group_first_field(82) == 0` in the legacy store and by `members.empty()` in the context-scoped store), so the requirement is that the predicate change preserves that outcome while making it principled rather than incidental.
- **FR-013**: FIX43's other 33 group registrations MUST be unchanged. **Exactly one tag moves** — `+1 tag (576)`.

**Non-regression**

- **FR-014**: For the six dictionaries whose type and structural sets are equal (FIX44, FIX50, FIX50SP1, FIX50SP2, FIXT11, Orchestra FIX Latest), the registered group set from `as_table_view()` MUST be **exactly** unchanged in both directions (set equality, not containment).
- **FR-015**: The `v44`, `v50sp2`, and `vt11` **read** goldens and the `vlatest` read golden MUST stay **byte-identical**, and the existing `vlatest` / `v50sp2` / `v44` **builder** goldens MUST stay byte-identical. This MUST be demonstrated by an actual regeneration + golden diff, not asserted from a source-level census.
- **FR-016**: The `v42` read tier regenerates. **All five** emitted `v42` artifacts — `Fields.hpp`, `Messages.hpp`, `Validator.hpp`, `Reify.hpp`, `NormativeReferences.md` — MUST be classified explicitly as either byte-identical or changed-with-explanation; none may be left unexamined. `Messages.hpp` goes from 0 to **18** `class G_` (one per group **tag**, including `384 NoMsgTypes` whose only host is the admin message `Logon`), its 46 message classes unchanged. **`Manifest.txt` is NOT a `v42` artifact** — `MessageIR::occurrences` is populated only on the Orchestra path (`ir.cpp:476`), so `emit_manifest` returns empty for every `<fix>`-schema version (`emit_manifest.cpp:154-166`) and no file is written (`main.cpp:29`); verified empirically — the generated `v42` directory contains exactly the five artifacts above, and only `vlatest` carries a `Manifest.txt`. Extending `occurrences` population to the `<fix>` schema is **out of scope** (it is new IR plumbing, not a detection re-point). Every count that moves MUST carry a **by-construction** explanation reconciling the emitted delta to FIX42's declared structure — "golden regenerated" alone is not sufficient evidence. Of the five, exactly **one** (`Messages.hpp`) has a checked-in golden; the by-construction instrument for the group axis of the other four is FR-021, and the remaining axes are pinned by the regeneration diff recorded in the `/speckit-verify` record.
- **FR-016a**: Because `FieldRef::type` is unchanged (FR-001 re-points detection, it does not re-type any field), the `v42` artifacts whose content is a function of field datatype rather than group structure are expected to be byte-identical — specifically `Fields.hpp` (the constexpr `FieldRef` arrays) and `Validator.hpp` (per-message rule tables plus the Length+Data pair table; the validator emitter has no group axis at all). This expectation MUST be **verified by regeneration diff**, and any artifact that moves against it is a finding to be explained, not absorbed.
- **FR-016b**: The two existing `v42` builder-descope pins — `tests/codegen/test_077_builder_no_emit.cpp` (`V42EmitsNoBuilders`) and `tests/codegen/test_077_v42_vt11_completeness_and_c4.cpp` (v42 expected set defined as ∅ by policy) — MUST be inverted to assert the delivered `v42` builder tier, not deleted. `vt11`'s companion assertions in the same files MUST be unchanged. Because the second is an **exact-set** completeness gate, its expected `v42` set MUST be **derived by construction from `emit_builders`' own `(no_tag, recursive signature)` interning rule** (research D-9a), **never transcribed from the first emitter run** — a transcribed set is a corpus built from the read it checks and would enshrine whatever the emitter happened to produce. The derived targets are **28** distinct plans over **17** tags under `--families all` and **19** plans over **11** tags under `--families official`, reproducible via the checked-in, self-validating `contracts/builder_plan_census.py`; plan *names* are mode-dependent (a tag bare in one mode can be ordinaled in the other, since `assign_plan_names()` keys on the final per-`no_tag` count over the in-scope set), so the two modes' expected sets MUST NOT be cross-compared by name.
- **FR-017**: No C-ABI change. The frozen `1.5.0` surface and its symbol golden are untouched — verified by the `nm`-based `CabiSymbolSetUnchanged` / `ErrorEnumUnchanged` gate (`.github/workflows/abi-golden.yml`), which **is** this repo's current abidiff-equivalent check; abidiff itself was retired 2026-06-22 (libabigail asserts on the debug-info-less release archive) and its suppr/golden/baseline files are retained only as a future Phase-5 hook, not an active gate. No abidiff regeneration is expected or permitted by this feature.
- **FR-018**: The 063 reused-tag census helper MUST be re-pointed to a source **independent** of the detection predicate under change — a raw-dictionary-XML walk — so that it can witness the FIX40/41/42 and FIX43 deltas rather than moving in lockstep with the code it checks. The pre-spec census logic that produced this feature's evidence base (§ Context) MUST be **checked in** as that oracle, so SC-001 / SC-002 / SC-003 are self-verifying in CI rather than resting on a one-off transcript. Two constraints on *how*:
  - **Extend `tests/dictionary/required_scope_oracle.hpp`; do NOT fork a third XML walker.** That header already is this instrument — a from-scratch `pugixml` walk of the vendored XML under an explicit non-circularity banner (`:19-30`), with `<component>` resolution (`:169-179`), `<header>`/`<trailer>` inclusion as a parameter (`:196-197`, `:223-232`), a `GroupContextKey{msg_type, path, no_tag}` mirroring `table_view`'s convention (`:64-72`), a per-context `group_members` census (`:81`), and an Orchestra sibling. Its own banner records 079's rule verbatim: "**do NOT duplicate/fork the walker logic, that would break the single-oracle guarantee**". Adding a fresh derivation inside `reused_tag_census.hpp` would make three independent walkers in `tests/dictionary/` — the same single-source failure this feature is about, one tier up. Overriding 079's rule requires a stated reason; there is none.
  - **The oracle MUST reproduce the reachability restriction.** C2's registered counts are `set ∩ reachable`: a group registers only if transitively reachable from a `<message>`, including via `<header>`/`<trailer>`, which `xml_loader.cpp:926-931` expands into every message's run. FIX50 / FIX50SP1 / FIX50SP2 **declare** 69 / 99 / 507 group tags but **register** 67 / 97 / 505 (`NoHops(627)` unreachable behind their empty `<header/>`; `NoMsgTypes(384)` belongs to FIXT11's `Logon`). An oracle that compares *declared* sets fails SC-002's both-directions equality on three dictionaries. Its ten-dictionary output MUST equal C2's "registered after" column **exactly** — that is what makes "both directions" meaningful rather than a formula that gets weakened until it stops pinning.

**Documentation and governing-document closure**

- **FR-019**: `spec/behaviors-and-limitations.md` MUST record the resolution of **L-063-1**, **L-061-1**, **L-066-1**, and **L-077-1**, and MUST record the FIX43 tag-82/tag-576 corrections as a named behavior with their evidence. While rewriting the L-066-1 row, its own **stale internal citation MUST be refreshed**: `behaviors-and-limitations.md:1749` cites "`dictionary.cpp:335`'s NumInGroup gate"; the gate is now at `dictionary.cpp:398`. (Pre-existing drift, not introduced by this feature, but FR-019 rewrites that row anyway.) The same B&L pass MUST also carry **FR-023**'s named behavior change + release note for the member-less-`<group>` load rejection — specified normatively in FR-023 and referenced here so the closure task covers it.
- **FR-020**: **Governing-document closure MUST reach `.specify/constitution.md`, not stop at B&L.** Article XVIII §7 (`:386`) states in bold "**`fixpp::v42` builders remain DEFERRED** … blocked on the L-063-1 structural-group-detection fix … tracked as issue #196", and the Status banner (`:85`) records the same at v0.9 — both assert a false fact about the shipped library once 082 lands. Article XVIII §4 makes roadmap changes constitution amendments; Article XX §1 forbids silent violation ("resolved by amending the article first, with rationale committed in the same PR"); Article XX §2 requires Codex Gate A review on the amendment **and user sign-off**. The required change is **annotation-only** and **permissive** — Article I §1's codegen scope (`:94`) already reads "FIX 4.2, FIX 4.4, FIX 5.0 SP2, FIXT.1.1" per `[2c §1.3]`, so 082 moves the library *toward* Article I §1 with no scope widening (the FIX40/41 out-of-scope assumption below stays true). Scope of the amendment: (a) Article XVIII §7's "v42 builders remain DEFERRED" sentence becomes a delivered-by-082 record; (b) the Status banner gains a **v0.11** line; (c) Article I §1 is confirmed unchanged. It MUST be **folded into this feature's own branch**, per the unbroken precedent of v0.5 (069), v0.6 (074), v0.7 (075), v0.8 (076), v0.9 (077), v0.10 (078) — **not** a standalone `Constitution: amend …` PR. The v0.9 amendment-log entry at `:18` carries the same sentence as *historical record* and MUST be left intact. **User ratification is required and is an open item — see § Open decisions.**
- **FR-021**: The 0 → 18 `class G_` delta MUST carry a **by-construction reconciliation instrument**, not a regeneration transcript, so that SC-004's "reconciled by construction" has something behind it and the delta is durable. The instrument is a `v42` **class-side ⟷ raw-XML** consistency gate that composes two *different* derivations: the class side parsed purely from the text of the shipped generated `Messages.hpp` (class bodies, `view_.template get<N>()` accessor calls, and `group_view<...G_N>` return types marking a group reference), per the version-agnostic extraction rule documented in `tests/codegen/vlatest_manifest_class_consistency_test.cpp:33-63`; the structural side from FR-018's oracle. **Note the 076 V-1/V-1b *manifest*↔class pair cannot be instantiated for `v42` as-is**: V-1b keys on `Manifest.txt`, which `v42` does not emit (FR-016). Instantiating the full V-1/V-1b pair for the `<fix>`-schema versions is therefore blocked on `occurrences` population and is **out of scope**; the class-side ⟷ raw-XML leg delivers the by-construction property FR-016/SC-004 actually need without it. Ideally the gate is version-parameterised (it would then close the same hole for `v44` / `v50sp2` / `vt11`), but `v42` is the requirement.
- **FR-022**: This is an Article VIII **§2 intentional perf change**, not a §3 N/A (§2 and §3 are not scoped to hot-path cost — `.specify/constitution.md:185-186`; §5 is the only hot-path clause). **Three** benchmark obligations MUST land in the **same PR**, per research D-12 §1's bench census — re-based on *which bench profiles measure something 082 changes*, over a **closed** enumeration of all **34** profiles registered under `bench/` (32 `add_executable` binary targets ∪ the 2 script harnesses registered via `add_test`, which have no `.cpp` at all):
  - **(a) The `as_table_view()` build-time profile — the leg that measures the diff directly.** `bench/dictionary/table_view_footprint_bench.cpp` times `Dictionary::as_table_view()` **itself** (`:113-116`, `:129-132`; banner `:5`, `:40`) — the exact function 082 edits at all three detection sites. Its cost moves on **every** dictionary, including the C2 EQUAL ones, because the per-field test goes from one enum compare (`dictionary.cpp:398`) to an O(log G) `groups_` binary search (`G = 507` on FIX50SP2). `BM_TableView_BuildFix44` and `BM_TableView_BuildFix50SP2` MUST be re-measured, a **FIX 4.2 row MUST be added** (the bench has none, and FIX 4.2 is where the registered set also changes) with its **pre-change** figure captured first so the post-change number has something to compare against — mirroring 075's own T011/T032 pre/post pattern. `BM_TableView_Sizeof` MUST be re-reported: it is expected **unchanged** (082 adds no member, so `sizeof` is a fixed compile-time constant), and the footprint axis that *does* grow — `group_bits_` from empty to `(max no_tag >> 6) + 1` words per `table_view` **copy** on FIX40/41/42 — MUST be stated explicitly alongside it, because `sizeof` cannot capture a heap-side growth. The result MUST be checked in as **`bench/baselines/dictionary/table_view_footprint_bench.json`**: 075 recorded this profile only as an **in-file comment block** (`:16-41` pre-change, `:42-62` post-change), and §2's ±5% budget is measured "vs `bench/baselines/`", so today the moving profile has **no baseline to compare against** — updating the comment block does **not** discharge this. Note the profile is known-tight: 075's T032 measured `BM_TableView_BuildFix44` at **+5.06%**, already at/over §2's budget.
  - **(b) The FIX 4.2 group-bearing *parse* profile — the leg that measures the downstream consequence.** Because registration flips FIX40/41/42 from a `group_bits_` clear-bit short-circuit to real group-context resolution **per message**, a **FIX 4.2 group-bearing parse benchmark plus a fresh baseline** MUST land in the same PR, reusing the existing 061/067 harnesses. No existing baseline covers FIX4x group parsing: `bench/wire/{parser,offset_table}_bench.cpp` run against a **test-double** `table_view` (`support/mock_dict_table.hpp`) and load no dictionary at all; `bench/wire/validator_bench.cpp` uses a real FIX44 `table_view` but builds it *outside* its measured window (`:250` vs `:302-310`) on a C2 EQUAL row; and the one FIX42 bench, `BM_XmlLoader_LoadFix42` (`bench/dictionary/xml_loader_bench.cpp:47-57`), measures `XmlLoader::load` only and never calls `as_table_view()` or parses a message.
  - **(c) The `v42` read-tier compile-time ceiling — run and record, no new bench.** `bench/codegen/compile_time_bench/` is a script-registered profile (no `.cpp`, no `add_executable`; `add_test` + `LABELS bench`) that measures `clang++ -fsyntax-only` wall time for one TU per version — `#include <fixpp/<ver>/Messages.hpp>` + `Reify.hpp` over `VERSIONS=(v42 v44 v50sp2 vt11)` (`compile_time_bench.sh:56`, `:69-83`). 082 adds **18** `class G_` classes to `v42/Messages.hpp` plus group accessors to `v42/Reify.hpp` (SC-004), so the `v42` TU grows. Its `SINGLE_CEILING=3` s is **load-bearing** and **only `v50sp2` is exempted** as a KNOWN_OVERAGE (`:93-99`); for `v42` an overage sets `PASS=false` and `:139-143` really does `exit 1`. **No CI job runs it**, though — `tier1.yml`'s `bench` job (`:1099-1106`) is soft and runs only `placeholder_bench` under `continue-on-error: true`, and no workflow invokes `ctest -L bench` — so a `v42` overage exits non-zero locally and is **invisible in CI**. The existing harness MUST therefore be **run and the `v42` figure recorded** in the PR body; the all-versions figure MUST also be reported, noting it is **WARN-only** (`:132-135` sets a WARN status and never exits), so it is a report and not a second gate. No new bench and no baseline file: this is a ceiling check, not a `bench/baselines/` comparison. **No profile covers the `v42` *builder* tier's compile cost** (226 new files) — recorded as a stated gap on research D-11's "measure, don't assume" risk row, not an FR-022 obligation, because 078 made builder tests link the prebuilt per-version library.

  All three MUST carry their figures/rationale in the PR body (§2). Asserting any leg unmoved instead of measuring it is the "no hot-path change asserted from the diff" anti-pattern (research D-11).

**Loader fail-closed rejection of a member-less `<group>` (OD-1, resolved by the user 2026-07-30)**

- **FR-023**: A `<group>` element that declares **no member** MUST be **rejected at load time**, in **both** loaders — it becomes a load error, not a silent skip. (Numbered after FR-022 because it was added post-Gate-A-convergence; thematically it belongs with FR-001..005's structural core, and § Edge Cases carries the link.)
  - **The rejected state, stated precisely.** "No member" is the case in which the loader's own member scan resolves no `field` / `group` / `component` child — i.e. exactly the case where `first_field_tag` would remain **0** and be recorded as such. Runtime `<fix>` loader: the `<group>` walk's member scan at `src/dictionary/xml_loader.cpp:610-641`, whose result is recorded at `:644` and pushed at `:649`. Orchestra loader: `first_member_tag(group_node)` returning 0 at `src/dictionary/orchestra_loader.cpp:629` (helper at `:467`), inside the `OrchestraGroupDef` record block `:626-635`.
  - **The rejection MUST fire on ANY member-less occurrence, not only the first-seen one.** Both `GroupDef` records sit inside a first-seen-wins dedup guard — `if (!group_index_by_no_tag_.contains(no_tag))` (`xml_loader.cpp:609`, `orchestra_loader.cpp:626`) — so a check placed *inside* that guard would admit a member-less **second** occurrence of a tag whose first occurrence had members, making the rule order-dependent and therefore not fail-closed. The validation MUST be evaluated per `<group>` occurrence, independently of the dedup. (This is why the OD-1 entry's original "roughly a one-line addition per loader" cost estimate does not hold; the shape is a per-occurrence check, not a one-liner.)
  - **Error type: the existing per-loader parse-error class; NO new subclass.** `src/dictionary/xml_loader.cpp` MUST throw **`fixpp::dict::xml_parse_error`** (`include/fixpp/dict/error.hpp:44`) and `src/dictionary/orchestra_loader.cpp` MUST throw **`fixpp::dict::orchestra_parse_error`** (`error.hpp:98`). Grounds: (i) `xml_loader.cpp:584` — the **sibling** case, a `<group name="X">` with no matching `<field>` declaration — already throws a bare `xml_parse_error`, so the new rejection mirrors an existing disposition for the same class of defect rather than inventing one; (ii) `error.hpp:18-27` documents the house rule for when a *derived* class is warranted — a caller needs to discriminate **by catch type**, since `code()` is non-virtual — and nothing needs to discriminate a member-less group from any other malformed-structure load failure; (iii) `orchestra_parse_error` is the Orchestra loader's standing disposition for malformed structure (e.g. `parse_orchestra_id`, `orchestra_loader.cpp:169-176`, under its own stated "T013/T014/T016 fail-closed contract") and **derives from `xml_parse_error`**, so one `catch (fixpp::dict::xml_parse_error&)` covers both loaders. No `fixpp::core::error` variant is appended, so `error_message()`'s exhaustive switch, `tests/core/test_020_error_completeness.cpp`'s slot pin, and FR-017/SC-009's frozen C-ABI surface are all untouched.
  - **Diagnostic content.** The message MUST name the **offending group's `name` attribute** and its **`no_tag`**, and state that no member resolved — following `group_delimiter_collision_error::make`'s convention of naming "the three facts an operator needs to fix the offending dialect" (`error.hpp:73`, impl `:75-84`). `no_tag` is always available at the rejection point: `xml_loader.cpp:584` has already thrown if the group name resolves to no `<field>`.
  - **No-regression leg.** All **ten** vendored dictionaries MUST continue to load clean. This is not merely expected but *measured*: `contracts/predicate_census.py` emits no zero-member-`<group>` warning for any of the ten (quickstart S0), and an independent walk of FIX40/41/42/43 confirms it — so **zero vendored dictionaries are affected** and there is no regression against the shipped set.
  - **Behavior-change obligation.** Because this is a **loader** behavior change with a reachable consequence for third-party XML — a dictionary containing a member-less `<group>` loads today and will stop loading — it MUST be recorded in `spec/behaviors-and-limitations.md` as a **named behavior change with an operator-facing release note**, on the same terms as FR-006c's, stating explicitly that no vendored dictionary is affected. This is the normative home of that obligation; FR-019 points at it rather than restating it.
  - **C-ABI-observable consequence.** `fixpp_dict_load_from_xml` (`src/capi/dictionary.cpp:48-68` (the generic `catch (...)` at `:64-67`) already wraps every `Dictionary`/Orchestra load in a generic `catch (...)` and returns `FIXPP_ERR_CAPI_CONFIG_INVALID` (`*out_dict = nullptr`) for any exception, so a C-ABI consumer sees the new `xml_parse_error` / `orchestra_parse_error` surface exactly as it already sees every other malformed-dictionary rejection — **no new error code, no C-ABI behavior change** (FR-017). This is pre-existing generic handling, not new code, so it needs no dedicated FR-023 implementation task; T009–T011's C++-level throw pins are the operative witness.
  - **Contract consequence (the payoff).** With the member-less state unreachable at load, `group_first_field(t) != 0` is exactly correct for every `Dictionary` the loaders admit, with **no** caveat — contract C1.3 P1-NON is rewritten from a tolerated non-property into a retired one. **Do not overstate**: the sentinel is still ambiguous *in isolation* (`dictionary.cpp:92-99`); what changes is that the ambiguous input cannot reach it.

### Key Entities

- **Dictionary group table**: The per-dictionary structural record of declared `<group>` elements — group tag, member list, parent group — populated by both loaders directly from the `<group>` / `<fixr:group>` element, independent of the count field's declared datatype. This is the intended source of truth for detection.
- **`FieldRef::type`**: The field's declared datatype. Today it doubles as the group-ness marker; after this feature it carries datatype only.
- **`table_view`**: The runtime projection the validator and typed reads probe, holding the registered group set (both the bare-`no_tag` and the 063 context-scoped views) and the per-group required-member store.
- **Codegen IR / emitters**: The build-time representation from which the per-version read, reify, manifest, and builder/validator outputs are emitted; each currently re-derives group-ness from `FieldRef::type`.
- **Golden corpus**: The checked-in per-version emitted outputs — the 003 read goldens, the 076 `vlatest` read golden, and the 078 split builder/validator golden set — that pin byte-identity for unaffected versions and record the intended delta for `v42`.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: `as_table_view()` registers exactly **4 / 7 / 18** repeating groups for FIX40 / FIX41 / FIX42 (today **0 / 0 / 0**), each with its declared member set, verified against a non-circular raw-XML oracle by exact-set equality in both directions. The exact-set equality binds the **bare** store's tag set; the 063 context store is pinned separately by **per-context member-set** equality against the same oracle (FR-004).
- **SC-002**: For FIX44, FIX50, FIX50SP1, FIX50SP2, FIXT11, and Orchestra FIX Latest, the registered group set is exact-set-equal to the pre-feature set in both directions — **0 additions, 0 removals**. The oracle side is **reachability-restricted**, so the comparison is against 59 / 67 / 97 / 505 / 1 / 524 (registered), never 59 / **69** / **99** / **507** / 1 / 524 (declared) — FR-018.
- **SC-003**: The FIX43 registered group set differs from the pre-feature set by **exactly one** tag — `+1 tag (576)`. A populated `NoClearingInstructions(576)` group reads membership-bounded where it previously did not. Tag 82 stays unregistered and `ListStatus` still enforces it as a plain required field (no-regression pin, not a delta).
- **SC-004**: The regenerated `v42` read tier contains typed group accessors for all **18** FIX42 group tags (today **0**), including the `296 → 295` nesting, and every emitted-count delta in its `Messages.hpp` and `Reify.hpp` is reconciled **by construction** to FIX42's declared structure by FR-021's class-side ⟷ raw-XML gate — not by a regeneration transcript. (`Manifest.txt` is not a `v42` artifact — FR-016.)
- **SC-005**: Regeneration produces **byte-identical** `v44`, `v50sp2`, `vt11`, and `vlatest` read goldens and **byte-identical** existing `v44` / `v50sp2` / `vlatest` builder goldens — demonstrated by an actual diff over regenerated output.
- **SC-006**: `fixpp::v42` ships a typed builder tier covering its **39** application messages, with repeating groups represented in `Args` for the **21** that declare them, emitted as **28** distinct `groups/<PlanName>.hpp` plan headers over **17** group tags (`--families all`; **19** over **11** under `--families official`) — derived by construction from `emit_builders`' interning rule, not transcribed (FR-016b, research D-9a). `validate_<Msg>` rejects omission of a `required='Y'` group at all **14** message/group pairs that declare one, including the nested `MassQuote`/`NoQuoteEntries(295)`-inside-`NoQuoteSets(296)` pair, whose omission construction is a 296 **entry** with an empty 295 span (FR-008).
- **SC-007**: A `v42` grouped **and** nested write exemplar (`MassQuote`, `NoQuoteSets(296) → NoQuoteEntries(295)`) emits bytes matching an independently-derived golden and round-trips field-for-field through the `v42` read tier.
- **SC-008**: A FIX 4.2 inbound message carrying a real wire-level repeating group returns a membership-bounded typed / C-ABI group read where it previously returned `TYPE_MISMATCH` or absent — demonstrated with `validate_inbound_messages` **off**, confirming the parse/addressing correction is independent of the strict-validation opt-in.
- **SC-008a**: With `validate_inbound_messages` **off** (the default), no FIX40/41/42 inbound message is newly **rejected** — the ungated change alters read shape only, never acceptance. With the flag **on**, the new group-required rejections are exactly those derivable from the dictionary's `required='Y'` group members, enumerated and pinned rather than merely observed.
- **SC-009**: The C-ABI surface (`1.5.0`) and its symbol golden are unchanged — the `nm`-based `abi-golden.yml` gate (this repo's current abidiff-equivalent check, abidiff itself retired 2026-06-22) stays green with no regeneration.
- **SC-010**: `spec/behaviors-and-limitations.md` carries no remaining open L-063-1 / L-061-1 / L-066-1 / L-077-1 carve-out, and the FIX43 corrections are recorded with their evidence.
- **SC-011**: `.specify/constitution.md` Article XVIII §7 no longer asserts that `fixpp::v42` builders are deferred, the Status banner carries a **v0.11** annotation naming feature 082, Article I §1 is unchanged, and the amendment is recorded with user ratification in the same branch (FR-020).
- **SC-012**: FR-022's **three** legs are discharged. Two produce new checked-in baselines: `bench/baselines/dictionary/table_view_footprint_bench.json` for the re-measured `as_table_view()` build-time profile (`BM_TableView_BuildFix44` / `BM_TableView_BuildFix50SP2` + a new FIX 4.2 row, with `BM_TableView_Sizeof` re-reported unchanged and the `group_bits_` heap growth stated), and a baseline for the new FIX 4.2 group-bearing **parse** benchmark — both with rationale in the PR body (legs (a) and (b)). The third (leg (c)) produces **no baseline**: the existing `compile_time_bench` harness is **run** and its `v42` TU figure recorded under the load-bearing ≤3 s ceiling — a ceiling check no CI job executes, so recording it is the obligation. Separately, the pre-existing profiles that could be affected are re-checked within their ±5% budgets. Selection rule for that re-check set: every checked-in `bench/baselines/` profile whose bench executes dictionary, wire-parse, or codegen-emitted code — the only three subsystems 082 touches. **The "dictionary" leg spans the whole of `src/dictionary/`, `XmlLoader`/`OrchestraLoader` included** — stated explicitly because FR-023 edits the loaders, and the rule was first written when the load path was dispositioned NO MOVE, so a reader who checks the rule before the list must find it covering the load-path case too. That is these **8** files, and no others: `bench/baselines/wire/{framer_bench,offset_table_bench,parser_bench,validator_bench,writer_bench}.json`, `bench/baselines/codegen/typed_accessor_bench.json`, `bench/baselines/dictionary/{reify_bench,xml_loader}.json`. The remaining 12 checked-in baselines (`decimal_baseline.json`, `log/log_enqueue.json`, `session/*`, `sync/async_mutex_baselines.json`, `threading/threading_baselines.json`, `placeholder.json`) are excluded by the rule — no dictionary, no `table_view`, no codegen artifact on their timed paths. (`dictionary/reify_bench.json` is in the set by subtree; its bench loads no dictionary — research D-12 §1 — so it is enumerated as a no-move member rather than silently dropped.) Note FR-023 adds a member scan **inside `XmlLoader::load`**, which `bench/baselines/dictionary/xml_loader.json` already covers — it is in this 8-file set, so the added load-path work is measured by the ±5% re-check and needs no fourth FR-022 leg (research D-12 §1).
- **SC-013**: A dictionary declaring a `<group>` with **no member** **fails to load**, in both loaders — `xml_parse_error` from the `<fix>` loader and `orchestra_parse_error` from the Orchestra loader — with a diagnostic naming the offending group's `name` and its `no_tag` and stating that no member resolved. The rejection fires on **any** member-less occurrence, not only a tag's first-seen one. All **ten** vendored dictionaries continue to load clean (0 affected), and the rejection is recorded in `spec/behaviors-and-limitations.md` as a named behavior change with an operator-facing release note (FR-023).

## Open decisions — **BOTH RESOLVED 2026-07-30** *(raised at Gate A round 1; kept as audit trail)*

Both items were the user's call and both were answered on **2026-07-30**, after Gate A converged at
round 3. The bundle now **encodes the answers** — the entries below are retained as the audit trail
of what was asked, what was decided, and on what evidence. Neither is open.

- **OD-1 — Zero-member `<group>`: documented non-property, or fail-closed loader rejection?
  → RESOLVED 2026-07-30: fail-closed loader rejection.** The user chose the **alternative** over the
  bundle's default. A member-less `<group>` is now a **load error** in both loaders, specified as
  **FR-023** and pinned by **SC-013**.
  - *What was asked.* *Default (as originally specified):* a bounded, source-verified **non-property**
    — a member-less `<group>` silently skipped at the runtime tier, recorded in § Edge Cases and
    research D-1a with its representational reason, no vendored dictionary declaring one.
    *Alternative:* reject it at load time in both loaders, turning an unrepresentable case into a
    load error. The alternative was **not** the default because it appeared to widen 082 from
    "re-point detection" to "add a loader validation rule", changing the load-failure surface for
    every dictionary — including third-party XML a consumer loads at runtime.
  - *Deciding evidence (gathered after the review loop closed; no review pass censused it).* The
    `<fix>` loader is **already aggressively fail-closed** on malformed dictionary structure — **27**
    `throw` sites in `src/dictionary/xml_loader.cpp` — and **two of them are group-specific**:
    `xml_loader.cpp:584` already throws `xml_parse_error` for a `<group name="X">` with **no matching
    `<field>` declaration**, so a structurally-broken *group declaration* is already a load error;
    and `xml_loader.cpp:1017` throws `group_delimiter_collision_error` for a nested group whose
    delimiter collides with its parent's, under the guard
    `if (g.first_field_tag != 0 && g.first_field_tag == parent.first_field_tag)` — **that `!= 0`
    means the zero-delimiter case is already special-cased into silence at the one site whose entire
    purpose is fail-closed delimiter validation.** So a member-less `<group>` is the **sibling** case
    of `:584`'s and the **excluded** case of `:1017`'s, and was the *only* structurally-broken group
    form passing silently. `include/fixpp/dict/error.hpp:67-85` records the house standard for the
    diagnostic — `group_delimiter_collision_error::make` names "the three facts an operator needs to
    fix the offending dialect" (`:73`). Rejection therefore **mirrors two existing dispositions**
    instead of inventing a rule.
  - *Why the objection does not hold.* The default's justification was a **reachability** claim ("no
    vendored dictionary declares one" — true, verified across all ten). Reachability does not cover
    third-party XML a consumer loads at runtime; and OD-1's stated objection — that rejection widens
    the load-failure surface — is largely **already true** via `:584`, which rejects the adjacent
    malformation today.
  - *Where it is encoded.* **FR-023** (both loaders, error classes, per-occurrence rule, diagnostic,
    no-regression leg, B&L/release-note obligation) · **SC-013** · § Edge Cases · contract C1.1 /
    C1.3 P1-NON / K11 · research D-1a · `data-model.md` I-1a + the FR → pin map · `plan.md`
    § Implementation Sequencing step **2b** · quickstart **S1b**.

- **OD-2 — Constitution amendment ratification (FR-020). → RESOLVED 2026-07-30: RATIFIED.** The user
  **ratifies** the **annotation-only** Article XVIII §7 + Status-banner **v0.11** amendment, folded
  into the **082 branch** rather than a standalone `Constitution: amend …` PR.
  - *What was asked.* Article XVIII §7 currently states that `fixpp::v42` builders "remain
    DEFERRED", which is exactly what 082 delivers; Article XX §1 forbids silent violation and §2
    requires Codex Gate A review on the amendment **plus user sign-off**. The bundle specifies the
    amendment as annotation-only, folded into this feature's own branch per the v0.5/v0.9/v0.10
    precedent (FR-020, research D-13).
  - *What the ratification changes, and what it does not.* **FR-020's obligation is unchanged** and
    remains an **implementation-time** task; `.specify/constitution.md` is **not** edited by this
    design bundle. What changed is that **Article XX §2's user-sign-off precondition is now
    satisfied**, and the separate **Appendix-A user `/plan` sign-off was given 2026-07-30** —
    `plan.md`'s Constitution Check rows are updated accordingly (Appendix-A row flipped from
    "REQUIRED — pending user" to "**GIVEN 2026-07-30**").

## Assumptions

- The structural group declaration is already tracked on both sides independent of the count field's datatype — the loaders' `<group>`-element walk (`xml_loader.cpp:580`) feeding the `Dictionary` group table, and the codegen IR's `walk_level` (`ir.cpp:80`) feeding `MessageIR::group_order`. **No new dictionary parsing is introduced**; detection is re-pointed at data that already exists (see § Context). The expected default is that **neither side needs a new public accessor**: `as_table_view()` is a `Dictionary` member with access to `dictionary_internal.hpp`, so it can read the private group table directly, and the codegen emitters already have `MessageIR::group_order`. An additive public accessor should be introduced only if a site genuinely cannot reach either source — confirming that is a `/speckit-plan` task, and avoiding it keeps FR-017 trivially satisfied.
- `FieldRef::type` is **not** modified. FIX42's count fields keep their declared `INT` datatype; only what the detection sites *consult* changes. This is what makes FR-016a's byte-identity expectation for the datatype-derived artifacts a meaningful, falsifiable check rather than a tautology.
- Only **FIX42, FIX44, FIX50SP2, and FIXT11** are code-generated (`cmake/Codegen.cmake`). FIX40, FIX41, FIX43, FIX50, and FIX50SP1 are runtime dictionaries only, so this feature's codegen/golden impact is confined to **`v42`**; FIX40/41/43 impact is runtime-`as_table_view()`/validator only, with no golden to regenerate and therefore a need for **direct** regression pins.
- Byte-identity of unaffected tiers is treated as a **requirement pinned by regeneration diff**, never as an assumption — the `NoSidesCodeSet` finding above is the standing evidence that a raw-XML census does not equal loader truth.
- **Adding FIX40 / FIX41 (or FIX43 / FIX50 / FIX50SP1) to the code-generated version set is explicitly out of scope.** Structural detection makes typed `v40`/`v41` tiers newly *possible*, but this feature changes which dictionaries register groups, not which are code-generated. The codegen version set stays FIX42 / FIX44 / FIX50SP2 / FIXT11.
- The `v42` builder tier is emitted in the default `--families all` coverage mode (all **39** application messages; `main.cpp:69` sets `CoverageMode::All`). The `--families official` frozen-subset mode selects **25** FIX42 messages (those whose MsgType is in `emit_builders.cpp`'s frozen 33-element `kOfficial33` set; note official mode is *not* `is_application`-gated, but no FIX42 admin MsgType is in that set) and is pinned by a **structural witness**, not a golden — no `--families official` golden set is checked in for any version (FR-009).
- Verification uses the established tooling of the preceding dictionary features: non-circular raw-XML oracles compared by exact-set equality in both directions, RED→GREEN behavior pins, QuickFIX-derived wire goldens, and the codegen golden-matching + determinism tests. `tools/codegen/**` is touched, so the codegen test label is in scope for local verification.
- The FIX40/41/42 compat posture is **settled** (Clarifications, 2026-07-29): ungated, one detection path, strictness riding the existing `validate_inbound_messages` opt-in, recorded as a named behavior change with a release note. The library is pre-v1.0 GA, so correcting documented-wrong legacy reads now is cheaper than preserving them behind a permanent knob; this follows 081's precedent of shipping a validation-behavior flip as a straight correction with pins.
- **The pre-existing group extraction/slicing residuals are unaffected as *code*, but their reachability widens — and that is expected, not a regression.** `spec/behaviors-and-limitations.md` L-065-1 (typed-path depth-≥2 context-threading) is **FIXED by 072**; L-065-2 (C-ABI nested-read arena-exhaustion silent-truncation) is **ADDRESSED by 073** (issue #184, fail-loud on both consumer paths); the residual still deliberately open in that family is **L-073-1** — TOP-LEVEL (non-nested) group reads retain the same silent-truncation-on-arena-exhaustion that 073 made fail-loud for the nested path, excluded by 073's own FR-009. All three concern the **slicing/extraction** machinery (`src/wire/offset_table.cpp`, `src/capi/message_read.cpp`, `group_view`), which this feature does **not** touch: 082 changes only group **detection** (registration in `as_table_view()` and codegen discovery). **The honest nuance:** because FIX40/41/42 register *zero* groups today, that machinery is currently unreachable for them — no group read can occur. Once 082 registers their groups, the extraction path becomes reachable for those three dictionaries at their existing quality, L-073-1 included. That is the intended consequence of making the groups visible at all, not a defect 082 introduces, and it is bounded: no extraction code changes, and the behaviour matches what the six already-registering dictionaries have shipped since 073. A reviewer should therefore not read US1/US4's newly-visible legacy groups as regressing or re-opening these rows — but should equally not read "unaffected" as meaning FIX40/41/42 are exempt from L-073-1 after this feature.

## Normative References

Per Article VI §5, the coverage-index and behaviour-record entries that inform this spec. **No new OFFICIAL catalogue rows are introduced** — this feature makes existing FIX 4.0/4.1/4.2 (and FIX 4.3) coverage correct rather than adding new spec-section coverage; traceability is via existing rows plus B&L L-row closures.

- **`[FIX50SP2 §3] Message validator — required fields, type conformance, enum values, group structure`** — `spec/coverage-index.md:189` (catalogue row W-014). The `group structure` clause is what this feature makes real for FIX40/41/42 and corrects for FIX43.
- **`[FIX42 §] MarketDataSnapshotFullRefresh`** — `spec/coverage-index.md:239` (row M-002, Application Messages — Market Data, FIX 4.2+). Currently satisfied only via `v44` codegen; US1/US4 extend it to `v42`.
- **`[FIX42 §] NewOrderList`** — `spec/coverage-index.md:307` (row A-002). Its "full-field + all-version coverage deferred" note is what US2 discharges for `v42`.
- **`spec/behaviors-and-limitations.md` L-063-1** (`behaviors-and-limitations.md:1695`) — the root-cause record: FIX40/41/42 type group-count fields `INT`, so `as_table_view()` and the codegen emitter register zero groups. Its own stated fix ("relax group detection to structural") is FR-001. **Resolved by this feature.**
- **`spec/behaviors-and-limitations.md` L-061-1** (`behaviors-and-limitations.md:1736`) — no `v42` grouped/nested write exemplar is expressible; all five exemplars forced to `v44`. **Resolved by US4.**
- **`spec/behaviors-and-limitations.md` L-066-1** (`behaviors-and-limitations.md:1749`) — FIX 4.0/4.1/4.2 sessions are strict-but-group-blind under dict-backed inbound parsing; 066's membership-correctness claims are scoped to the six group-registering dictionaries. **Resolved by US1**, which also widens that scope note.
- **`spec/behaviors-and-limitations.md` L-077-1** (`behaviors-and-limitations.md:1817`) — no typed `build_<Msg>` for `fixpp::v42`, descoped from 077 and tracked as issue **#196**, blocked on the L-063-1 fix. **Resolved by US2.**
- **`spec/behaviors-and-limitations.md` B-077-1** (`behaviors-and-limitations.md:1819`) — the 077 structural-key safety pin: a new structural variant of an existing group tag is absorbed as a new ordinaled `Args` plan and surfaces in the builder golden diff, never a silent mis-share. `v42`'s newly-visible groups enter the builder tier under this same guarantee — **and this is the first version where the ordinaling fires on *newly-visible* groups, which has a concrete count consequence the guarantee alone does not convey**: 7 of the 18 tags carry divergent member signatures across contexts (`146` → 4, `73` → 3, `295` → 3, `78`/`268`/`296`/`420` → 2 each), so the 17 tags reaching the builder tier produce **28** distinct `(no_tag, signature)` plans, not 17 or 18. See research D-9a; the arithmetic, not just the citation, is what FR-016b and SC-006 bind.
