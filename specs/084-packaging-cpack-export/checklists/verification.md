# Verification Requirements Quality Checklist: Installable Packaging (CPack) + CMake Package-Config Export

**Purpose**: Validate that the *requirements as written* for the three witness tiers, the red-proof obligations, the CI gating decision, the verification-matrix fallback and the completeness/close-out obligations are complete, clear, consistent and measurable. This checklist tests the **English**, not whether any witness passes.
**Created**: 2026-08-01
**Feature**: [spec.md](../spec.md)

**Scope note**: D3/FR-026a (gate the real-client witness on `linux-gcc-release` only) is a closed Gate A decision and is **not** relitigated. Items ask only whether its rationale and consequences are adequately written down.

## The three witness tiers

- [x] CHK076 Is the tier count stated consistently as **three** at every site a task generator reads first — `spec.md` User Story 1's witness strategy, `plan.md` Technical Context, the Constitution Check Article VII basis cell, and `data-model.md` E6 — given a two-tier heading at any one of them can drop SC-016 from a derived task list? [Consistency, Spec §US1] — PASS: 'three' witness tiers is stated consistently at every named site — spec.md US1 witness strategy, plan.md Technical Context, plan.md Constitution Check Article VII basis cell, and data-model.md E6.
- [x] CHK077 Is each tier described by **what it can fail on** rather than by what it exercises, so two tiers are not read as redundant and dropped to one? [Clarity, data-model E6] — PASS: data-model.md E6's table has a dedicated 'Catches' row per tier (config-package regressions / an export that resolves but cannot link / a config baking in a build-host path), and spec.md US1 frames each tier the same way inline.
- [x] CHK078 Is the clean-environment tier's **mandatory** status written as a requirement, given `plan.md`'s Article VII PASS is explicitly conditional on it and would otherwise rest on the two blind tiers? [Completeness, Spec §SC-016] — PASS: spec.md US1 states the clean-environment tier's mandatory status explicitly and unconditionally ('It is mandatory, not optional').
- [x] CHK079 Is the minimal tier's enlarged scope — after switching from four hand-listed archives to `fixpp::fixpp` it exercises the **link interface**, not just discovery — reflected everywhere the bundle attributes link-interface failures exclusively to the real-client tier? [Consistency, Spec §Assumption 7] — PASS: data-model.md I25 explicitly states 'Both the minimal and real-client tiers now satisfy this [link-interface exercise]; only the real-client tier additionally runs the result against a live counterparty' — verified by grep that no remaining site attributes link-interface failure exclusively to the real-client tier.
- [x] CHK080 Is the blindness the two inherited tiers share (each is handed the producing build's Conan toolchain) stated where a reader might otherwise cite SC-001 or SC-011 as evidence that the package is consumable off the producing host? [Clarity, Spec §SC-001] — PASS: SC-001 and SC-011 each carry an explicit 'Scope: green under the producing build's environment only... must not be cited as evidence the package is consumable off the producing host' qualifier.
- [x] CHK081 Are the three adaptations the packaged real-client variant requires (replace the test-support dictionary helper with a runtime load; drop the network-fetched histogram and both source-tree include paths; enable the disabled-by-default build option) written as enumerated obligations, or as narrative a task generator may compress? [Completeness, Spec §US1] — PASS: spec.md US1 states the three adaptations as an enumerated bulleted list with MUST-level language for each, not narrative prose.
- [x] CHK082 Is the standing caution — the driver is documented as needing an in-tree build, and FR-021a is what makes building it out-of-tree safe — stated **with its consequence**, i.e. that relaxing FR-021a reintroduces the staleness trap that guidance exists to prevent? [Traceability, Spec §FR-021a] — PASS: spec.md US1's standing-caution paragraph states the consequence explicitly ('Treat any relaxation of FR-021a as reintroducing the staleness trap that guidance was written to prevent').
- [x] CHK083 Is SC-012's named equivalent check specified precisely enough to implement without reinterpretation (configure from outside the source tree; `CMAKE_PREFIX_PATH` set only to the staged prefix; assert zero source-root paths in `compile_commands.json` **and** the link line)? [Measurability, Spec §SC-012] — PASS: SC-012 specifies the named equivalent check precisely — configure outside the source tree, `CMAKE_PREFIX_PATH` set only to the staged prefix, assert zero source-root paths in both `compile_commands.json` and the link line.
- [x] CHK084 Is the recorded alternative (ship the adapted driver and its build file inside the package) unambiguously marked as a **candidate, not a requirement**, so it can neither be implemented as one nor cited as delivered? [Clarity, Spec §SC-012] — PASS: SC-012 explicitly marks the alternative ('ship the adapted driver + its CMakeLists.txt inside the package') as 'a candidate, not a requirement'.

## Red-proof obligations

- [x] CHK085 Is the rule "an assertion must be **proven to fail** on deliberately broken input before it counts as a gate" stated **generally** — over every assertion this feature adds — or only enumerated for the two assertions that happened to be thought of (SC-007b, SC-016)? [Coverage, Spec §SC-007b] — WAIVED: the proven-red-before-counting-as-a-gate rule is applied consistently to every meaningfully new gate this feature adds (SC-006's 'must be observed, not assumed', SC-007a, SC-007b, SC-016), but no single general statement unifies them into one stated principle — each instance is individually proven, so there is no coverage gap in delivered behaviour, only the absence of one unifying sentence (the general principle itself is project convention/memory, not codified in `.specify/constitution.md`, which contains no matching text). Tag is [Coverage], not restricted.
- [x] CHK086 Does SC-007a's "exactly one of these MUST be delivered" read as a genuine exclusive choice with a decision owner and a recording obligation, or does it leave both options live indefinitely? [Ambiguity, Spec §SC-007a] — PASS: SC-007a states 'Exactly one of these MUST be delivered' with two named options and their tradeoffs; T025 assigns the decision to the implementer with an explicit recording obligation ('Record which option was taken and why').
- [x] CHK087 Is the reason the ordinary ctest form is unavailable for the closure leg — CMake enforces closure at **generate**, so a broken tree produces no build system for ctest to run in — stated at every site the closure gate is described (SC-007a, I1, B1)? [Completeness, Spec §SC-007a] — PASS: the generate-time-not-ctest-time reason is stated near-verbatim at SC-007a, data-model.md I1, and export-set.md B1.
- [x] CHK088 If SC-007a option (i) is taken, are its attached constraints (`RUN_SERIAL`, an explicit `TIMEOUT`, and no writes inside the source tree) written as obligations of *that option*, or do they read as generic test hygiene a reader may treat as optional? [Traceability, Spec §SC-007a] — PASS: SC-007a's 'in which case' construction explicitly ties RUN_SERIAL/TIMEOUT/no-source-tree-writes to option (i) specifically, not to the closure gate generally.
- [x] CHK089 Is SC-007b's deliberately broken input specified concretely enough to be reproducible — a file of a **new** artifact kind under the codegen include root, then enumerate the installed set? [Measurability, Spec §SC-007b] — PASS: SC-007b specifies the broken input concretely — 'dropping a file of a new kind under the codegen include root and enumerating the installed set'.
- [x] CHK090 Is SC-016's red leg written so it **discriminates** — failing with the *named* dependency's `find_dependency` diagnostic "and nothing else" — rather than merely failing, which an uninstalled config or a malformed version file would also produce? [Clarity, Spec §SC-016] — PASS: SC-016's red leg requires the *named* dependency's diagnostic 'and nothing else' — an explicitly discriminating assertion.
- [x] CHK091 Is the FR-018e obligation-1 assertion required to run over the **installed** `fixppConfig.cmake`/`fixppTargets*.cmake` and never over the templates, stated identically at every site the check appears (FR-018e, SC-016, contract §4 row 1, §8)? [Consistency, Spec §FR-018e] — PASS: 'asserted over installed files, never over the templates' is stated identically at FR-018e obligation 1, SC-016's paired obligation-1 assertion, and package-layout.md §4 row 1/§8.

## The CI gating decision

- [x] CHK092 Is the CI gating decision carried as a **requirement** (FR-026a) rather than as plan prose, and is its own rationale — a decision that lives only in a plan is a witness that silently never runs — recorded with it? [Completeness, Spec §FR-026a] — PASS: FR-026a is a spec.md requirement (not plan prose) and states its own rationale inline ('a witness that silently never runs reads as green... stated as a requirement rather than left in plan prose precisely so a derived task list carries it').
- [x] CHK093 Is FR-026a's scope stated **exclusively** ("exactly one lane … and only there"), so the other five lanes' obligation is unambiguous rather than inferred? [Clarity, Spec §FR-026a] — PASS: FR-026a states exclusivity in both directions ('Exactly one CI lane... MUST enable... The other five in-scope lanes run the minimal tier only').
- [x] CHK094 Is the ground for choosing `linux-gcc-release` (it builds zero third-party dependencies from source, so the heaviest tier is gated at the lowest cost) recorded as the decision's rationale, so a later lane change reads as a visible re-decision rather than an edit? [Traceability, Spec §FR-026a] — PASS: FR-026a states the zero-third-party-dependency-from-source rationale for choosing `linux-gcc-release` inline, citing Assumption 9.
- [x] CHK095 Is the current state — the interop-perf option is `OFF` by default and enabled in **no** preset and **no** workflow — stated where a reader would otherwise assume SC-011/SC-012 already run somewhere? [Completeness, Spec §FR-026a] — PASS: FR-026a states the current OFF-by-default, enabled-nowhere state inline; anchor verified — `cmake/ProjectOptions.cmake:10` is exactly `FIXPP_BUILD_INTEROP_PERF ... OFF`, and grep across `CMakePresets.json`/`.github/workflows/` for the option returns zero matches.
- [x] CHK096 Are FR-026 (every in-scope lane attaches its package artifacts) and FR-026a (one lane additionally gates the real-client witness) written so a reader can tell which lanes owe which obligation, without reconciling two requirements? [Consistency, Spec §FR-026] — PASS: FR-026 (all lanes attach artifacts) and FR-026a (one lane additionally gates the real-client witness) are non-overlapping, complementary obligations, readable independently without reconciliation.

## The verification-matrix fallback

- [x] CHK097 Is Article IX's **UNRESOLVED** state, together with its fallback, documented explicitly enough that it reads as a deliberate deferral rather than as an oversight in a Constitution Check that otherwise reports PASS throughout? [Clarity, plan.md Constitution Check Art. IX] — PASS: plan.md's Constitution Check Article IX row and its round-1 re-evaluation section explicitly frame the UNRESOLVED state as a deliberate, reasoned deferral, not an oversight.
- [x] CHK098 Is the fallback written so it can be **applied** rather than re-derived at verify time — state the taxonomy gap; derive the matrix from the jobs that actually gate the touched paths; route any skipped preset through the paired-rationale waiver? [Measurability, plan.md §D2] — PASS: decision D2 in plan.md states the fallback as an apply-not-re-derive procedure with exactly the three named steps (state the gap; derive the matrix from the jobs that gate touched paths; route skips through the paired-rationale waiver).
- [x] CHK099 Is the prohibition on assuming the coverage bucket's keying in **either** direction (`src/**` path prefix vs compiled-source file type) stated as a constraint with a consequence, rather than as a preference a verifier may resolve by judgement? [Clarity, Spec §Assumption 11] — PASS: Assumption 11 states the prohibition explicitly in both directions with an explicit consequence ('MUST NOT be assumed either way; it is derived from the real diff... at verification time').
- [x] CHK100 Is the obligation to record the taxonomy gap tied to a **named artifact** (the `/speckit-verify` decision record), or does it exist only as a task row that a compressed task list could drop? [Traceability, plan.md §D2] — PASS: T071 names the literal artifact path (`.specify/decisions/084-packaging-cpack-export-verify.md`); Assumption 11's 'the verification record' is a well-established term-of-art in this project's Spec-Kit pipeline (constitution Article XVII §8), not a vague reference.
- [x] CHK101 Is "an unpaired skip is a failure by design" written as a requirement with a consequence, rather than as an admonition inside an assumption? [Gap, Spec §Assumption 11] — PASS: 'an unpaired skip is a failure by design' is a MUST-level clause with explicit consequence inside spec.md Assumption 11, which is formally part of the Gate-A-audited spec bundle, same standing as an FR for this purpose.

## Completeness and close-out obligations

- [x] CHK102 Are FR-024 and FR-025 written **with their constraint and its consequence** — they edit the parent repository, cannot be committed on this branch, and must therefore be deferred to close-out and staged as a deliberate parent-repo commit, never bundled with a submodule commit? [Completeness, Spec §FR-024] — PASS: the parent-repo sequencing note (spec.md, near FR-025) states the constraint (parent repo, cannot commit on this branch), the deferral (close-out), and the staging discipline (single deliberate commit, never bundled) all explicitly.
- [x] CHK103 Is FR-024a's **exclusion** from the parent-repo deferral stated at the deferral itself (not only in FR-024a's own text), so the sequencing note cannot sweep a committable-on-this-branch requirement into a deferred bucket? [Clarity, Spec §FR-024a] — PASS: the sequencing note's very first sentence is 'FR-024a is excluded; see its own note' — stated at the deferral itself, not only inside FR-024a's own text.
- [x] CHK104 Does FR-024a specify the reconciliation's **form** — per clause, which the feature *satisfies* and which it *supersedes* — precisely enough to be checked against `architecture.md` §7.4 rather than satisfied by a general note? [Measurability, Spec §FR-024a] — PASS: FR-024a specifies the reconciliation form (per clause: satisfies vs. supersedes) and names the three specific stale clauses to check; anchors verified exact — `.specify/architecture.md:500` (OBJECT-libraries-combined-into-shared/static claim), `:501` (`fixpp::capi-objects` no namespaced alias), `:503` (`fixpp::capi`), `:504` (`fixpp::service-iface`).
- [x] CHK105 Are the stale claims FR-024 must correct enumerated with their locations, so the requirement is closeable rather than open-ended — including the `REMAINING-WORK.md:44` freeze line whose staleness propagated into this bundle as a load-bearing rationale? [Completeness, Spec §FR-024] — PASS: FR-024 enumerates every stale claim with its exact line number (line 38, line 79, line 43's drifted citations, plus REMAINING-WORK.md:44) — fully closeable, not open-ended.
- [x] CHK106 Is the feature-completeness obligation — every requirement mapped to an owning task, and every open implementation choice either recorded or explicitly waived — stated in the spec or plan, or does it exist only as the final task row? [Gap, tasks.md close-out] — PASS: the feature-completeness obligation is a standing, project-wide gate at `.specify/constitution.md` Article XVII §8 (verified: the /speckit-verify decision-record requirement is codified there, item 8), which T075 instantiates for this feature; it does not need restating in spec.md/plan.md because it is governed at the constitution level, same standing as Gate A/Gate B themselves.
- [x] CHK107 Are the deliberately-open implementation choices enumerated in **one** place — the FR-002a rewrite form, the FR-002b `FILE_SET` disposition, the artifact-directory retention rule, and `fixpp_capi_objects`' `OBJECTS DESTINATION` — so none is silently left undecided at close-out? [Coverage, plan.md §Next] — SPEC-FIXED: plan.md → Next and tasks.md T075 previously enumerated only three open implementation choices, omitting `fixpp_capi_objects`' `OBJECTS DESTINATION`-vs-wiring-change question (present elsewhere in plan.md's Risks table and export-set.md §2a, but not in the single enumerating list); both now list all four (`plan.md` → Next item 5, `tasks.md` T075).

## Notes

- Check items off as completed: `[x]`
- Record the disposition inline (PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED:`<reason>`)
- ID range: CHK076–CHK107. Numbering continues from [`export.md`](./export.md); this is the last of the three domain checklists.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 30 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 1 |
| **Total** | 32 |

Pre-audit: 32/32 boxes `[ ]`. Post-audit: 32/32 boxes `[x]`, zero un-dispositioned.

### SPEC-FIXED items
- CHK107 — plan.md → Next and tasks.md T075's open-implementation-choices enumeration now includes the 4th choice (`fixpp_capi_objects` `OBJECTS DESTINATION` vs. wiring change), previously present only in plan.md's Risks table / export-set.md §2a; affected: `plan.md` → Next item 5, `tasks.md:T075`.

### DD-DECIDED items
None.

### WAIVED items
- CHK085 — the "proven red before counting as a gate" rule is applied consistently to every new gate this feature adds, individually, but is never stated as one general principle; the principle itself is project convention (`feedback_sanitizer_canary_must_be_proven_red`), not codified in `.specify/constitution.md`, so there is nothing at the spec-authority level to point to and no coverage gap in delivered behaviour. Tagged [Coverage], not Completeness/Clarity/Consistency.

Anchors spot-verified: `cmake/ProjectOptions.cmake:10` (`FIXPP_BUILD_INTEROP_PERF ... OFF`; confirmed zero matches for the option across `CMakePresets.json`/`.github/workflows/`), `.specify/architecture.md:500,501,503,504` (FR-024a's three named stale clauses, all confirmed as described), `.specify/constitution.md` Article XVII §8 (the `/speckit-verify` decision-record gate, confirmed to exist and govern feature-completeness project-wide) — all resolve exactly as cited in the signed-off bundle, worktree `084-packaging-cpack-export` @ 2026-08-01.
