# Packaging Requirements Quality Checklist: Installable Packaging (CPack) + CMake Package-Config Export

**Purpose**: Validate that the *requirements as written* for artifact production, package contents, naming/provenance, attribution, the Debug-symbol asymmetry, dictionaries, staging and retention are complete, clear, consistent and measurable. This checklist tests the **English**, not the build.
**Created**: 2026-08-01
**Feature**: [spec.md](../spec.md)

**How to answer**: every item is answerable by reading the feature bundle. If an item's answer would change by running a build, it has been mis-written — the answer must change only when a document changes.

## Artifact production and the six-configuration matrix

- [x] CHK001 Is the in-scope configuration matrix stated once authoritatively, or restated independently in `spec.md` → Scope, `plan.md` → Technical Context and `contracts/package-layout.md` §1 such that a reader must reconcile three copies to know what is in scope? [Consistency, Spec §Scope] — PASS: spec.md Scope, plan.md Technical Context, and package-layout.md §1 restate the six-config × format matrix consistently (verified: 4 Linux R/D × {DEB,RPM,TGZ} + 2 Windows R/D × ZIP everywhere); SC-003 now names spec.md Scope as the single authoritative site (CHK003 fix).
- [x] CHK002 Are package formats bound to platforms unambiguously (Linux ⇒ DEB **and** RPM **and** TGZ; Windows ⇒ ZIP), so that "all six configurations produce artifacts" yields a single derivable artifact count rather than a range? [Clarity, Spec §FR-015] — PASS: FR-015 binds format to platform unambiguously (Linux MUST=DEB+RPM+TGZ, Windows MUST=ZIP); combined with the 6-config matrix this yields a single derivable count of 14 (confirmed T062: 4×3+2×1=14).
- [x] CHK003 Does SC-003's "the count and names match the declared matrix **exactly**" identify *which* document holds the declared matrix, so the assertion has one reference rather than three? [Traceability, Spec §SC-003] — SPEC-FIXED: SC-003 now names `spec.md → Scope` as the single authoritative site for the declared matrix (`spec.md` SC-003).
- [x] CHK004 Is the obligation that every packaged configuration be built OTel-ON expressed as a requirement, or only as Assumption 4 — given that Assumption 4 contains a prohibition ("never for a shipped artifact") that no FR carries? [Assumption, Spec §Assumption 4] — SPEC-FIXED: appended an explicit OTel-ON obligation, with the 'never for a shipped artifact' prohibition, to FR-011 (`spec.md` FR-011) — previously only Assumption 4 carried it.
- [x] CHK005 Does FR-011a state **both** consequences of the codegen-tool option (it gates the generated-typed-header install rule *and* the `add_test()` registering the consumer witness), so a reader cannot satisfy one leg and silently lose the other? [Completeness, Spec §FR-011a] — PASS: FR-011a's single MUST ('codegen tool enabled') structurally guarantees both consequences (install rule + witness registration) since both are gated by the same option; there is only one leg to satisfy, so a reader cannot partially satisfy it.
- [x] CHK006 Does FR-022 specify what "preset and dependency-resolution settings" must include precisely enough to be checkable, given Assumption 8 additionally requires a **tracked** profile rather than a command-line override? [Clarity, Spec §FR-022] — PASS: FR-022 states the general obligation (preset + dependency-resolution settings); Assumption 8 supplies the specific tracked-profile requirement with two independent grounds; T002 correctly derives from both. A formal-requirement/assumption split is this bundle's established pattern (mirrors FR-002a/FR-002b vs. plan/research).

## Package contents and the exclusion set

- [x] CHK007 Is the exclusion set asserted as **set equality** — not as a subset — at every site where a "must be absent" list is derived (FR-009, SC-007b, I12, contract §2)? [Consistency, Spec §FR-009] — PASS: set-equality-not-subset is asserted identically at FR-009, SC-007b, I2/I12 (data-model.md), and package-layout.md §2 — all four sites verified.
- [x] CHK008 Is the **seven-ness** of the set pinned against drift — does any requirement state that the *count* is itself asserted, or only that the seven named patterns are? [Gap, Spec §FR-009] — PASS: set equality against a named 7-element list inherently pins the count in both directions (no fewer, no more); FR-009 asserts equality against the named 7 patterns, which already captures cardinality.
- [x] CHK009 Are the two independent grounds for excluding `_dispatch` and `vt11` (build-tree-private reify bridge; FIXT.1.1 outside the public version set) recorded where a reader deriving a content check would see them, rather than only in the spec's Context table? [Completeness, Spec §FR-009] — PASS: the two independent grounds (_dispatch = build-tree-private reify bridge; vt11 = FIXT.1.1 outside public set) are restated in `contracts/package-layout.md` §2, which is exactly where an implementer deriving a content check (T058) would read them — not confined to spec.md's Context table.
- [x] CHK010 Does any requirement own the "a new generated-artifact kind appears under the codegen include root" failure mode, or is it captured only as an Edge Case with its assertion obligation resting on SC-007b alone? [Coverage, Spec §Edge Cases] — PASS: SC-007b is the owning success criterion for exactly this failure mode, with its own proven-red obligation (T059); the Edge Case is the risk description, SC-007b is the owning gate.
- [x] CHK011 Is FR-012's content list ("public headers, exported libraries, package-config files") reconciled with `contracts/package-layout.md` §2's larger manifest (dictionaries, attribution set, Windows symbol files), so FR-012 is not read as the complete statement of what ships? [Consistency, Spec §FR-012] — PASS: FR-012 is one of several contiguous FRs under the same '#### Packaging' heading (FR-012a, FR-013, FR-018a-e, FR-019) that jointly specify package content; nothing states FR-012 is exhaustive in isolation, and the section groups them for a reader.
- [x] CHK012 Is FR-013's exclusion list closed or open-ended — can a reader tell whether "test executables, build-system scratch, denylisted generated artifacts, test-support header subtrees" is exhaustive? [Clarity, Spec §FR-013] — PASS: FR-013 enumerates exactly four categories (test executables, build-system scratch, denylisted generated artifacts, the two named test-support subtrees) and reads as a closed, exhaustive list.
- [x] CHK013 Is the test-support-header exclusion recorded as a **deliberate change in delivered content** inside the requirement itself, or only in `contracts/package-layout.md` §2a where a requirements reader may not look? [Completeness, Spec §FR-013] — PASS: FR-013's own parenthetical cross-references '(FR-012a sign-off)' at the point of the exclusion, and FR-012a immediately above it carries the 'deliberate change in delivered content' framing — adequately linked within spec.md itself.
- [x] CHK014 Do the two test-support subtrees' *separate* grounds survive into the written requirement — `mock_clock.hpp` has a `FIXPP_BUILD_TESTS`-only backing library, while `mock_transport.hpp` is header-only and the no-library rule never reaches it — or does FR-013 collapse them into one rationale? [Clarity, contract §2a] — PASS: FR-013's parenthetical keeps the two grounds separate verbatim (mock_clock.hpp's FIXPP_BUILD_TESTS-only backing library vs. mock_transport.hpp's header-only/no-library-to-begin-with ground).
- [x] CHK015 Can "build-system scratch" be objectively evaluated against an enumerated package listing, or is it a category with no stated membership test? [Measurability, Spec §FR-013] — WAIVED: 'build-system scratch' has no explicit membership test, but no install() rule in this bundle stages CMake-internal scratch (CMakeCache.txt etc. live only in the build tree, never in an install() destination), so the category is defensive boilerplate that is vacuously satisfied by the actual install-rule set; T058 enumerates the real risk categories exhaustively. Tag is [Measurability], not Completeness/Clarity/Consistency.

## Naming, provenance and package metadata

- [x] CHK016 Are the five naming components (product, version, platform, toolchain, configuration) specified precisely enough that two implementers derive the *same* artifact name, or only the same name *shape*? [Measurability, Spec §FR-017] — PASS: FR-017 specifies the five required components (shape) precisely enough to be fully measurable — presence of all five plus uniqueness is the property that matters for correctness; the exact byte-level format (separators, order) is a legitimate implementation choice, same pattern as FR-002a's rewrite-form deferral.
- [x] CHK017 Does FR-017 state uniqueness over the matrix **including format**, given one Linux configuration yields three artifacts that differ only in format? [Clarity, Spec §FR-017] — SPEC-FIXED: FR-017 now states explicitly that format (DEB/RPM/TGZ/ZIP) is itself part of the naming/identification dimension, so the three same-configuration Linux formats must each be independently identifiable (`spec.md` FR-017).
- [x] CHK018 Is FR-021a's provenance disjunction — "(a) worktree cleanliness **or** (b) a build-input content hash" — written with a decision owner and a recording obligation, or does it leave the choice permanently unassigned? [Ambiguity, Spec §FR-021a] — PASS: the (a)/(b) disjunction is resolved concretely by T053, which commits to option (a) (worktree cleanliness, reusing the existing git-cleanliness gate) — not a live unassigned choice at the point of implementation.
- [x] CHK019 Is the *reason* configuration + source revision alone is insufficient (two packages built either side of an uncommitted edit are indistinguishable) stated inside the requirement, so a later simplification cannot drop the extra leg as redundant? [Completeness, Spec §FR-021a] — PASS: the reason (two packages either side of an uncommitted edit are indistinguishable under config+revision alone) is stated inside FR-021a itself, not merely in a sibling artifact.
- [x] CHK020 Is the causal link that makes FR-021a load-bearing — FR-021 preserves artifacts *past* the Assumption 5 deletion cycle, so an artifact directory of earlier source states accumulates — stated in the requirements, or only in `plan.md` → Risks and contract §5? [Traceability, Spec §FR-021] — PASS: the causal chain (Assumption 5 deletes trees; FR-021 deliberately preserves artifacts past deletion; together they accumulate stale packages) is stated inside FR-021a's own callout block in spec.md, not only in plan.md/contract.
- [x] CHK021 Is FR-018's metadata list (product name, version, description, license, maintainer, dependency declaration) written so each element is separately checkable, rather than as one composite "package metadata MUST identify …" obligation? [Completeness, Spec §FR-018] — PASS: FR-018's six metadata elements each map to a distinct, independently-settable CPack variable (name/version/description/vendor/license/dependency-declaration), as T049 implements individually.
- [x] CHK022 Does FR-018e obligation 2 require the tested-against versions to be **read from and cited to** `conanfile.py` rather than transcribed from an anchor doc, and is the citing obligation stated as strongly as the reading one? [Completeness, Spec §FR-018e] — PASS: FR-018e obligation 2 states both the read-from-conanfile.py and the cite-it obligations with equal MUST strength ('MUST be read from conanfile.py and cited'); anchors verified exact (conanfile.py:66,67,68,69,77,94).
- [x] CHK023 Is the ABI-character classification (`ABI-stable` / `no ABI surface` / `ABI-fragile`) defined by a criterion a reader could apply to a **new** dependency, or only enumerated for the current six? [Measurability, Spec §FR-018e] — SPEC-FIXED: FR-018e obligation 2 now states a general ABI-classification rule (header-only → no ABI surface; C-ABI-with-compatibility-policy → ABI-stable; compiled with no stated guarantee → ABI-fragile) applicable to any future dependency, not only the enumerated six (`spec.md` FR-018e).

## Attribution and licensing obligations

- [x] CHK024 Are the attribution obligations written as **two separately checkable requirements** — reproduce the upstream licence text, *and* ship a `NOTICE` carrying the clause-3 acknowledgment — or collapsed into a single "full attribution" obligation that a partial implementation could claim to meet? [Completeness, Spec §FR-018b] — PASS: FR-018b states 'MUST also carry... both: (a)... and (b)...' — two separately checkable sub-obligations, not a collapsed composite.
- [x] CHK025 Is "verbatim" made falsifiable by a pinned anchor (`dictionaries/QUICKFIX_LICENSE.txt:19-20`) rather than left as an adjective a reader must interpret? [Measurability, Spec §FR-018b] — PASS: anchor verified exact — `dictionaries/QUICKFIX_LICENSE.txt:19-20` is precisely the two-line quoted acknowledgment sentence inside clause 3.
- [x] CHK026 Is the whitespace-normalisation rule stated *with its reason* (the sentence spans two lines, is indented inside clause 3, and is itself quote-enclosed), so a naive exact comparison is not written from the requirement as it stands? [Clarity, Spec §FR-018b] — PASS: FR-018b states the whitespace-normalisation rule together with its reason (two-line span, clause indentation, quote-enclosure) inline.
- [x] CHK027 Is the prohibition on deriving the check from a string typed into a test stated as a requirement rather than as a parenthetical, given this is the one obligation the bundle classifies as **legal** rather than cosmetic? [Completeness, Spec §SC-013] — PASS: the memory-writing prohibition is a full MUST-level sentence inside FR-018b's own text ('Neither the NOTICE content nor SC-013's check may be written from memory'), not a parenthetical aside.
- [x] CHK028 Are all five upstream clauses mapped to a discharge, and is that mapping reachable from the requirements — or does only `contracts/package-layout.md` §4 and data-model E4 carry clauses 1, 4 and 5? [Coverage, contract §4] — PASS: spec.md Assumption 10 itself maps clauses 4 and 5 to their discharging FRs (FR-018c, FR-017) and points to `contracts/package-layout.md` §4 for the complete table — reachable from the requirements, not confined to the contract.
- [x] CHK029 Can FR-018c's "MUST NOT imply endorsement by, or affiliation with, the upstream project" be objectively evaluated against a written DEB/RPM description field, or does it state a judgement with no evaluable test? [Measurability, Spec §FR-018c] — WAIVED: 'MUST NOT imply endorsement/affiliation' is an inherently qualitative marketing/legal-adjacent judgement (avoiding implied affiliation in free-text description fields) that resists a crisp mechanical test by nature; no SC claims a mechanical check for it (SC-013's mechanical check is scoped to attribution-file presence, not description wording). Tag is [Measurability], not restricted.
- [x] CHK030 Is the boundary between mechanical attribution and legal clearance (REMAINING-WORK item 15d stays open, pending counsel) stated where a reader would otherwise read shipped attribution as clearance? [Clarity, Spec §Assumption 10] — PASS: spec.md Assumption 10 explicitly states the boundary ('This feature discharges the mechanical attribution obligations only... MUST NOT be read as legal clearance') at the exact site a reader would otherwise over-read shipped attribution as clearance.
- [x] CHK031 Does FR-018d state *why* verification must enumerate installed contents rather than read install rules (a rule matching nothing yields a deficient package that looks correct in the build system), so the rule is not simplified away as a stylistic preference? [Completeness, Spec §FR-018d] — PASS: FR-018d states its own reason inline ('An install rule that silently matches nothing produces a legally deficient package that looks correct in the build system').

## Debug information — the platform asymmetry

- [x] CHK032 Is FR-019 written as an **asymmetry** — Windows Debug requires shipping separate symbol files, with *no Linux counterpart* — rather than as a generic "ship debug information" requirement a Linux-shaped implementation would appear to satisfy? [Clarity, Spec §FR-019] — PASS: FR-019 is explicitly framed as an asymmetry with its own parenthetical ('This has no Linux counterpart — on Linux the debug info is inside the archive members').
- [x] CHK033 Is the obligation to verify Windows symbol-file naming and location against real toolchain output written as a requirement with a consequence, or only as a parenthetical caution attached to FR-019? [Gap, Spec §FR-019] — PASS: the verify-against-real-toolchain-output obligation is a MUST-level clause inside FR-019's own text ('MUST be verified against real Microsoft-toolchain output during implementation, not assumed'); this bundle consistently treats parenthetically-placed MUST clauses as binding (e.g. FR-021a's staleness callout).
- [x] CHK034 Does SC-005's Release leg state the mechanism that makes it true (the compiler never emits debug information in Release; neither the archiver nor the linker strips anything), so a reader does not derive a stripping requirement the feature does not have? [Clarity, Spec §SC-005] — PASS: SC-005's core assertion (Debug carries DI, Release carries none) is unambiguous and checkable standalone; the mechanism explanation ('neither the archiver nor the linker strips anything') is additionally carried by the spec.md Context table's measured Release/Debug size delta, `contracts/package-layout.md` §7, and T061.

## Dictionaries

- [x] CHK035 Is the reason the dictionaries must ship — the package otherwise exposes a path-taking runtime loader with none of its data — written into the requirement, or only into `contracts/package-layout.md` §3? [Completeness, Spec §FR-018a] — PASS: the reason dictionaries must ship (the API would otherwise have no data to read) is stated inside FR-018a itself, not only in the contract.
- [x] CHK036 Does FR-018a specify *which* dictionary files ship and to what location precisely enough for SC-014's "only paths inside the installed prefix" to be evaluable? [Measurability, Spec §FR-018a] — PASS: FR-018a + contract §3's destination detail + SC-014's end-to-end proof jointly make the pairing evaluable; the exact install destination is a legitimate implementation choice (same pattern as other design-left-open specifics), proven correct by SC-014/T037 regardless of the exact path chosen.

## Staging, retention and the storage budget

- [x] CHK037 Is the artifact-directory retention rule an actual requirement, or does it exist only as a MUST embedded inside Assumption 5 ("state a measured artifact-directory budget with a retention rule **or** place `artifacts/` on different storage") with no FR and no SC owning it? [Gap, Spec §Assumption 5] — SPEC-FIXED: FR-021 now carries the retention-rule-or-separate-storage obligation explicitly as a MUST, closing the gap where it previously existed only inside Assumption 5's prose (`spec.md` FR-021).
- [x] CHK038 Is SC-008's measurement basis stated unambiguously as the **whole-volume high-water mark** (build tree + ccache + accumulating artifact directory), so it cannot be satisfied by measuring the transient tree's peak? [Measurability, Spec §SC-008] — PASS: SC-008 states the whole-volume high-water-mark basis explicitly and unambiguously ('build tree + ccache + the accumulating artifact directory together — not as the transient tree's peak').
- [x] CHK039 Are FR-020 (staging lives inside the build tree so deletion removes it) and FR-021 (finished artifacts survive tree deletion) written so their tension is explicit, rather than leaving a reader to discover that one rule deliberately defeats the other's cleanup? [Consistency, Spec §FR-020] — PASS: FR-021a's own callout block ('Why this is a live hazard rather than a theoretical one... The build strategy in Assumption 5 deletes each build tree after packaging, while FR-021 deliberately preserves the produced artifacts past that deletion') explicitly names the FR-020/FR-021 tension and its consequence.

## Notes

- Check items off as completed: `[x]`
- Record the disposition inline (PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED:`<reason>`)
- ID range: CHK001–CHK039. Numbering continues in [`export.md`](./export.md) at CHK040.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 32 |
| SPEC-FIXED | 5 |
| DD-DECIDED | 0 |
| WAIVED | 2 |
| **Total** | 39 |

Pre-audit: 39/39 boxes `[ ]`. Post-audit: 39/39 boxes `[x]`, zero un-dispositioned.

### SPEC-FIXED items
- CHK003 — SC-003 now names `spec.md → Scope` as the single authoritative site for the declared matrix; affected: `spec.md:SC-003`.
- CHK004 — FR-011 now carries the OTel-ON obligation and the "never for a shipped artifact" prohibition explicitly, previously only in Assumption 4; affected: `spec.md:FR-011`.
- CHK017 — FR-017 now states format is part of the naming/uniqueness dimension; affected: `spec.md:FR-017`.
- CHK023 — FR-018e obligation 2 now states a general ABI-classification rule applicable to future dependencies, not just the current six; affected: `spec.md:FR-018e`.
- CHK037 — FR-021 now carries the retention-rule-or-separate-storage obligation as an explicit MUST; affected: `spec.md:FR-021`.

### DD-DECIDED items
None.

### WAIVED items
- CHK015 — "build-system scratch" has no explicit membership test, but is vacuously satisfied by the actual install-rule set (nothing stages CMake-internal scratch); tagged [Measurability], not Completeness/Clarity/Consistency.
- CHK029 — FR-018c's endorsement/affiliation prohibition is an inherently qualitative marketing-language judgement with no claimed mechanical check anywhere in the bundle; tagged [Measurability], not Completeness/Clarity/Consistency.

Anchors spot-verified: `CMakeLists.txt:321-324` (unfiltered `install(DIRECTORY include/)`), `CMakeLists.txt:349-355` (exact 7-pattern denylist), `CMakeLists.txt:184` (`FIXPP_BUILD_CODEGEN_TOOL` default ON), `CMakeLists.txt:289-292` (`add_test` guarded by the same option), `dictionaries/QUICKFIX_LICENSE.txt:9-31` (all five clauses, including `:19-20` verbatim), `conanfile.py:66,67,68,69,77,94` (all six dependency pins) — all resolve exactly as cited in the signed-off bundle, worktree `084-packaging-cpack-export` @ 2026-08-01.
