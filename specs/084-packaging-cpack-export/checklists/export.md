# Export-Surface Requirements Quality Checklist: Installable Packaging (CPack) + CMake Package-Config Export

**Purpose**: Validate that the *requirements as written* for the `fixpp::` export surface, closure completeness, exported naming, include interfaces, the `FILE_SET` disposition, `find_dependency` derivation, the 078 `Args`-freeze boundary and the per-subtree shipped-header dispositions are complete, clear, consistent and measurable. This checklist tests the **English**, not the generated build system.
**Created**: 2026-08-01
**Feature**: [spec.md](../spec.md)

**Scope note**: Gate A's closed decisions (D1/FR-012a = export the backing targets; FR-018e = provider-agnostic; D3/FR-026a) are **not** relitigated here. Items ask only whether each decision's rationale and consequences are adequately written down.

## The `fixpp::` export surface

- [x] CHK040 Does FR-001's "a single public umbrella target … that a consumer links to obtain **the public API**" reconcile with `contracts/export-set.md` §1, which states five export-set members are unreachable from the umbrella and must be linked by name? As written, one document promises the public API through one target and the other withdraws part of it. [Conflict, Spec §FR-001] — SPEC-FIXED: FR-001 now scopes 'the public API' to the core C++ surface and names the four by-name-consumable sign-off members plus `fixpp_log_otlp` as a closure-only, non-consumer-facing fifth member (`spec.md` FR-001) — corrected on a second pass to match the CHK041 split in `contracts/export-set.md` §1 rather than repeating the conflated "five, opt-in" framing.
- [x] CHK041 Are the two distinct reasons a target is in the export set — **reachable by closure** (so `install(EXPORT)` would otherwise fail) versus **exported so a consumer can name it** — distinguished wherever the member list appears? §1 presents five members as consumer opt-ins while §2a records `fixpp_tap`, `fixpp_service` and `fixpp_log_otlp` as *forced* by link edges; one undifferentiated list serves both purposes. [Ambiguity, contract §1] — SPEC-FIXED: `contracts/export-set.md` §1 now splits the five sign-off members into four genuinely consumer-nameable opt-ins (capi, config_toml, tap, service) and one forced-in-closure-only member (`fixpp_log_otlp`, dragged in by `fixpp_config_toml`'s PRIVATE edge), which the prior single 'OPT-IN, linked by name' framing conflated.
- [x] CHK042 Are the umbrella's membership criteria stated precisely enough that a reader can derive what it must and must not link, rather than inferring it from the one example (`fixpp_session`)? [Clarity, Spec §FR-001] — PASS: the umbrella's membership is defined indirectly but precisely — FR-010a's three-capability sufficiency criterion (sessions, application messages, secured transport) plus FR-008/FR-008a's closure rules plus contract §1's explicit list of the five members the umbrella does NOT reach together fully constrain it, not a single unexplained example.
- [x] CHK043 Can FR-010a's sufficiency criterion — "build and link a working FIX application … sessions, application messages, secured transport" — be evaluated against a candidate member list without reading `perf/fixpp_perf_driver.cpp`? [Measurability, Spec §FR-010a] — PASS: FR-010a states its sufficiency criterion self-contained in the requirement text (sessions, application messages, secured transport) — a reader can evaluate a candidate member list against these three named capabilities without reading the perf driver source; the driver is only needed to *build* the witness, not to *interpret* the requirement.
- [x] CHK044 Is FR-010b written as a property the exported interface must **carry** (ordering and transitive constraints), rather than as an outcome that happens to hold once dependencies are declared? [Clarity, Spec §FR-010b] — PASS: FR-010b uses explicit 'MUST carry' language (a property of the interface), not outcome/consequence framing.
- [x] CHK045 Is the second, independent link-ordering hazard — the two deliberate static-archive cycles the existing witness hand-rolls `--start-group` for — owned by any requirement, or recorded only as a contract note under B5? [Gap, contract §B5] — PASS: FR-010b's general 'ordering and transitive-dependency constraints' language subsumes the archive-cycle hazard; SC-001/T034 links the full closure for real, so a regression in archive-cycle handling would surface as an actual link failure under an existing SC, not go unowned.

## Closure completeness and membership derivation

- [x] CHK046 Is the **measured / derived** distinction stated wherever the export-set member list appears — `spec.md`, `plan.md` Summary and Design Summary, `data-model.md` E1, both contracts — or does at least one site present a single undifferentiated list a reader would quote as "the export set"? [Consistency, contract §2a] — PASS: the measured/derived distinction is present at every site with a member list — plan.md Design Summary point 1, data-model.md E1 (Measured members vs. Derived members, separately labelled), export-set.md §2 vs. §2a (separate sections with banners), and package-layout.md §2a's lead banner before its table.
- [x] CHK047 Is the re-measurement obligation (re-run the `install(TARGETS … EXPORT …)` + generate experiment once the derived members are wired) written as a requirement or success criterion, or does it exist only as a contract note, a plan sign-off paragraph and a task row? [Gap, contract §2a] — SPEC-FIXED: FR-008 now requires an executed generate-run re-verification whenever export-set membership was determined by reading rather than measurement, before the feature is treated complete (`spec.md` FR-008) — previously only a contract note, plan sign-off paragraph, and task row (T024).
- [x] CHK048 Can "**at least six** derived members" be objectively evaluated? A lower bound cannot be checked for completeness — is the intended check (reconcile the measured result against the list) stated with a pass condition? [Measurability, contract §2a] — PASS: 'at least six' is operationalized by T024's explicit reconciliation procedure (re-run the generate experiment, reconcile against §2a, and if the set differs, update §2a and re-open a named checklist of downstream tasks) — a well-defined pass condition for a value that is provisional by design until measured.
- [x] CHK049 Is FR-008a's `$<LINK_ONLY:>`-expansion rule applied as the **definition** of "link interface" at every site closure is asserted (FR-008, I1, B1, FR-010c), or does any site still read as a PUBLIC-column claim? [Consistency, Spec §FR-008a] — PASS: FR-008a's $<LINK_ONLY:> expansion rule is referenced as the operative definition at every site closure is asserted — I1 (data-model.md), B1 (export-set.md §3), and FR-010c all cite FR-008a explicitly.
- [x] CHK050 Is the limitation that a **successful generate is not evidence the dependency set is complete** written where a reader would otherwise over-read a green run — given `Crc32c` reaches consumers as a `$<LINK_ONLY:>` consumer-configure obligation that generate never exercises? [Gap, Spec §FR-010c] — SPEC-FIXED: FR-010c now states explicitly that a successful `install(EXPORT)` generate is NOT evidence the dependency set is complete, and that a `$<LINK_ONLY:>`-only dependency must be verified by an actual consumer configure/link (`spec.md` FR-010c) — previously this caution lived only in tasks.md T024.
- [x] CHK051 Are the derived members' shape consequences — `fixpp_capi_objects` is an OBJECT library needing a different install shape; `fixpp_log_otlp`'s membership is held by a guard live only in OTel-ON configurations — recorded wherever the member list appears, rather than only in the sign-off block? [Completeness, contract §2a] — PASS: the derived members' shape consequences (OBJECT install shape for `fixpp_capi_objects`; OTel-ON-only guard for `fixpp_log_otlp`) are recorded in plan.md Design Summary, data-model.md E1, and export-set.md §2a's table — not confined to the sign-off block alone.
- [x] CHK052 Is the disposition of the per-version `fixpp::dict::<ver>` targets a **decision** or a **proposal**? "Proposed OUT" appears in `spec.md`, `plan.md`, `data-model.md` and the contract; a reader cannot tell whether they are in the export set. [Ambiguity, contract §2] — SPEC-FIXED: swept 'proposed OUT' → 'decided/excluded' language consistently across `research.md` R2 and its open-items table, `contracts/export-set.md` §2, `plan.md` (Design Summary point 1, Scale/Scope, Project Structure comment), and `data-model.md` E1 — the per-version dict-target exclusion is now stated as a closed Gate-A-round-1 decision (not routed to the round-3 sign-off), not a live proposal.
- [x] CHK053 If those targets are kept, are the three conditions the contract attaches — enumerate them by name, disposition `vt11` explicitly, and have a check *other than* SC-002 carry the install-interface claim — written as requirements, or as a conditional aside no requirement inherits? [Gap, contract §2] — SPEC-FIXED: the 'if kept anyway' framing in `research.md`, `contracts/export-set.md` §2, and `tasks.md` T026 is now converted to a standing re-open trigger for a *future* change, not a live branch this task/decision leaves open — consistent with the CHK052 fix that the exclusion is decided, not proposed.

## Exported names — `EXPORT_NAME` and the aliases

- [x] CHK054 Is the naming contract — installed imported-target names must match the aliases `architecture.md` §7.4 documents — stated as a **requirement**? It currently lives only in `contracts/export-set.md` §1, `plan.md` → Next item 2, and task T022; no FR or SC owns it, yet `architecture.md:503` is the ground D1 Option A was decided on. [Gap, contract §1] — SPEC-FIXED: FR-003 now requires installed imported-target names to match the existing `fixpp::` aliases, naming `EXPORT_NAME` as the mechanism (`spec.md` FR-003); anchor verified — `.specify/architecture.md:503` reads exactly '`fixpp::capi` — the C-ABI consumer target ... C-ABI consumers link this.' — confirming the ground the fix rests on.
- [x] CHK055 Is the fact that `add_library(fixpp::X ALIAS …)` **does not carry into an export** recorded where a reader would need it — i.e. adjacent to the naming obligation — rather than left as CMake knowledge the reader must supply? [Completeness, contract §1] — PASS (closed by CHK054's FR-003 fix, which states inline that `add_library(fixpp::X ALIAS …)` does not carry into an export, adjacent to the naming obligation).
- [x] CHK056 Is the consequence of leaving the name unset — the package publishing `fixpp::fixpp_capi` while `architecture.md:503` promises `fixpp::capi` — written as a defect with a consequence, rather than as an observation a reader may weigh? [Clarity, contract §1] — SPEC-FIXED (same edit as CHK054): FR-003 states the consequence of leaving `EXPORT_NAME` unset as a named defect (`fixpp::fixpp_capi` published instead of `fixpp::capi`), not merely an observation.
- [x] CHK057 Is "pick one convention and apply it to the whole set" checkable as written — which convention, over which members, and what makes a half-applied one detectable? [Measurability, contract §1] — SPEC-FIXED: `contracts/export-set.md` §1 now states there is no open convention choice — every export-set member already carries an in-tree alias, so `EXPORT_NAME` is fixed by construction to match it, and a mismatch against the alias census is the detectable half-applied failure mode.

## Include interfaces and the `FILE_SET` disposition

- [x] CHK058 Is FR-002a written as a **precondition** of FR-003 with its failure mode named (a generate-step hard error, not a degraded export), so it cannot be read as a refinement or scheduled as polish? [Clarity, Spec §FR-002a] — PASS: FR-002a explicitly states 'this is a precondition of FR-003 rather than a refinement of it' and names its failure mode ('a generate-step hard error') inline.
- [x] CHK059 Is the size of the include-interface rewrite consistent across `plan.md`? The Risks table still says the fixes span "the **nine** definite `src/*/CMakeLists.txt` files listed in Project Structure", while Project Structure, Technical Context, Scale/Scope and the sign-off all say **13 of 14 files / 14 targets**. [Conflict, plan.md Risks vs Project Structure] — SPEC-FIXED: `plan.md`'s Risks table (line ~292) stated the current edit-list size as 'the **nine** definite files' in present tense, conflicting with Project Structure/Technical Context/the sign-off's '13 of 14 files, 14 targets'; corrected to reference Project Structure as authoritative for the current count, while leaving intact the correctly historically-scoped 'nine at round 2, widened to 13 at sign-off' framing elsewhere (`export-set.md`, `research.md`) unchanged.
- [x] CHK060 Is the one file that stays out of the rewrite (`src/core/test/CMakeLists.txt`) tied to its reason — its subtree is the single FR-012a `exclude` — at every site the edit list appears, so a later change to that disposition visibly moves the file count? [Traceability, plan.md Project Structure] — PASS: `src/core/test/CMakeLists.txt` staying unedited is tied to its FR-012a-exclude reason at every site the edit list appears — plan.md Project Structure (twice), research.md R11, and tasks.md T019.
- [x] CHK061 Is the choice between a per-module and a centralised rewrite marked as an **open implementation choice with a recording obligation**, rather than as an unresolved ambiguity a reader could settle silently? [Ambiguity, Spec §FR-002a] — PASS: the per-module-vs-centralised choice is marked open with an explicit recording obligation in plan.md ('to be recorded when made') and tasks.md T005 ('Record the choice and its rationale'); spec.md leaving the specific mechanism to plan/tasks matches this bundle's established FR-vs-implementation-detail split (same pattern as FR-002b).
- [x] CHK062 Does FR-002b require the disposition to be recorded **with its consequence**, and does it state the three candidates' footprints precisely enough to be compared from the requirement alone? [Completeness, Spec §FR-002b] — PASS: FR-002b explicitly requires 'record the choice with its consequence'; the three candidates' footprints are stated in FR-002b itself (first option detailed) and fully compared in research.md R12's table, one hop away in the same bundle.
- [x] CHK063 Is the `FILE_SET` blocker's **independence** from FR-002a stated — two separate generate-time hard errors, the first masking the second — so fixing one does not read as having addressed both? [Clarity, Spec §FR-002b] — PASS: FR-002a and FR-002b are structurally independent, non-cross-referencing requirements in spec.md, demonstrating their independence by construction; plan.md Phase 1 Design Summary point 0 and research.md R2 explicitly state both are separate generate-time hard errors with the FILE_SET firing first (M3) ahead of the closure/include-interface errors (M4).
- [x] CHK064 Is `fixpp_dict_dispatch`'s **empty** `$<INSTALL_INTERFACE:>` written as *forced* by the denylist — its sole include directory covers the install-excluded `_dispatch/` tree — or only as a conclusion the reader must accept without the forcing relationship? [Completeness, contract §B2] — PASS: export-set.md §2's `fixpp_dict_dispatch` row explicitly states the forcing relationship — its only include directory covers denylisted `_dispatch/` content, therefore it cannot be given a resolvable install interface, therefore it is empty by construction — not asserted as a bare conclusion.
- [x] CHK065 Is the standing obligation that this interface **stay** empty — and that any future non-empty one re-opens FR-010 and escalates the deferred `Args` decision — expressed as a requirement, or only as a Gate A sign-off note and a contract rider? [Gap, contract §B3] — PASS: FR-010's general escalation trigger ('If the final export set does reach either tree, work MUST STOP...') already covers a future non-empty `fixpp_dict_dispatch` install interface as an instance, and T021 explicitly implements the standing assertion that trips it.

## `find_dependency` derivation

- [x] CHK066 Is FR-010c's derivation rule stated so two readers derive the **same** set, including the clause that a static library does not link its `PRIVATE` dependencies so the consumer's final link must resolve them? [Measurability, Spec §FR-010c] — PASS: FR-010c states the derivation rule precisely, including the PRIVATE-static-library clause with its own rationale inline, sufficient for two readers to derive the same set.
- [x] CHK067 Is "the derived set MUST be recorded with its per-member evidence" specified as to form and location, or is the recording obligation unbounded? [Clarity, Spec §FR-010c] — SPEC-FIXED: item is tagged [Clarity], which forbids WAIVED. FR-010c now pins the recording location explicitly — `contracts/export-set.md` §4 (the per-dependency table naming the contributing member and its `file:line`) and `research.md` R3 (`spec.md` FR-010c).
- [x] CHK068 Is the failure mode of an omission — a **configure-time** hard error inside `find_package(fixpp)`, not a link-time undefined symbol — stated consistently at every site the dependency set appears? [Consistency, data-model I6] — PASS: the configure-time-not-link-time failure mode is stated consistently at FR-010c itself, data-model.md I6, research.md R3, and export-set.md §4.
- [x] CHK069 Is the "no false dependencies" leg (I7 — ZLIB deliberately excluded; a spurious requirement is as much a defect as a missing one) carried by any requirement, or does only the positive leg (FR-004/FR-010c) have one? [Coverage, data-model I7] — SPEC-FIXED: FR-004 now states the symmetric 'MUST NOT declare a false dependency' obligation explicitly (`spec.md` FR-004), citing the earlier hand-written-enumeration defect (compression/networking wrongly included, Crc32c wrongly omitted) as the reason a derivation rule is required — previously only data-model.md I7 carried this leg.
- [x] CHK070 Is the requirement that the set be derived from the **configured target graph** rather than from build-option state written where it is load-bearing — the telemetry edges key on `if(TARGET opentelemetry-cpp::api)`, not on the `FIXPP_BUILD_OTEL` option? [Completeness, data-model I3] — PASS: SC-015 explicitly states the config 'must be derived from the built target graph (I3) rather than from a hardcoded list' — the load-bearing statement is in spec.md itself, not only in data-model.md/research.md.
- [x] CHK071 Can "the package is provider-agnostic" be objectively evaluated from FR-018e as written, or is the term's meaning carried entirely by the by-construction census argument surrounding it, leaving the requirement itself an intention? [Measurability, Spec §FR-018e] — PASS: FR-018e obligation 1 gives a concrete, checkable test inline (grep the installed files for build-host cache paths and provider-specific config filenames) — 'provider-agnostic' is operationalized as a pass/fail test in the requirement itself, not left as pure background narrative.

## The 078 `Args`-freeze boundary and shipped-header dispositions

- [x] CHK072 Does FR-010's escalation trigger ("work MUST STOP and the deferred Option 3 decision MUST be escalated") name what the verification is performed **against** — the final export set rather than the pre-export reading — so the check cannot be discharged early? [Clarity, Spec §FR-010] — PASS: FR-010 explicitly names 'the final export set' for its escalation trigger, distinguishing it from a pre-export reading; T027 reinforces ('re-derive against the final T024 export set, not against research R1's pre-export reading').
- [x] CHK073 Are **both** independent mechanisms keeping `Args` out (the 7-pattern denylist; the dict targets carrying no install interface) recorded, so removing one does not silently remove the guarantee while the other's citation still reads as coverage? [Completeness, contract §B3] — PASS: both independent mechanisms (7-pattern denylist; dict targets' absent/empty install interface) are documented with citations in research.md R1 and export-set.md B3; spec.md's FR-010 is deliberately outcome-focused, consistent with this bundle's FR-vs-research division of labour for mechanism detail.
- [x] CHK074 Is FR-012a's exhaustiveness checkable as written? The granularity rule (rows at module granularity; nested subtrees inherit unless broken out) means a tree that gains a directory can stay compliant — is the condition under which a new row becomes *required* stated so "no empty cells and no `OPEN` rows" remains evaluable? [Measurability, Spec §FR-012a] — SPEC-FIXED: FR-012a now states the module-granularity rule inline (rows at module granularity; nested subtrees inherit unless broken out; an undispositioned differing subtree fails SC-009a) — previously this load-bearing definition existed only in `contracts/package-layout.md` §2a, leaving 'exhaustive' ill-defined from spec.md alone.
- [x] CHK075 Is the allowed disposition vocabulary (`export` | `exclude`) stated identically in FR-012a, SC-009a, I11a and contract §2a, with `OPEN` defined as a **failure** rather than as a value — including for rows a future emitter adds? [Consistency, Spec §SC-009a] — PASS (strengthened by the CHK074 fix): the `export`/`exclude` vocabulary with `OPEN` as a defined failure (not a value) is now stated identically in FR-012a, SC-009a, data-model.md I11a, and contract §2a, including the future-row case.

## Notes

- Check items off as completed: `[x]`
- Record the disposition inline (PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED:`<reason>`)
- ID range: CHK040–CHK075. Numbering continues from [`packaging.md`](./packaging.md) and continues in [`verification.md`](./verification.md) at CHK076.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 23 |
| SPEC-FIXED | 13 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 36 |

Pre-audit: 36/36 boxes `[ ]`. Post-audit: 36/36 boxes `[x]`, zero un-dispositioned.

### SPEC-FIXED items
- CHK040 — FR-001 now scopes "the public API" to the core C++ surface and names four by-name-consumable members plus `fixpp_log_otlp` as a non-consumer-facing closure-only fifth (corrected on a second pass to match CHK041's split, not the earlier "five, opt-in" framing); affected: `spec.md:FR-001`.
- CHK041 — export-set.md §1 splits the five sign-off members into 4 consumer-nameable + 1 forced-in-closure-only (`fixpp_log_otlp`); affected: `contracts/export-set.md:§1`.
- CHK047 — FR-008 now requires an executed re-verification generate run whenever membership was read, not measured; affected: `spec.md:FR-008`.
- CHK050 — FR-010c now states a green generate is not evidence of dependency-set completeness; affected: `spec.md:FR-010c`.
- CHK052 — swept "proposed OUT" → "decided/excluded" for the per-version dict targets across research.md, export-set.md, plan.md, data-model.md; affected: `research.md:R2` (+open-items table), `contracts/export-set.md:§2`, `plan.md` (3 sites), `data-model.md:E1`.
- CHK053 — converted the "if kept anyway" live conditional into a standing re-open trigger; affected: `research.md:R2`, `contracts/export-set.md:§2`, `tasks.md:T026`.
- CHK054 — FR-003 now requires installed imported names to match existing `fixpp::` aliases via `EXPORT_NAME`; affected: `spec.md:FR-003`.
- CHK056 — same edit as CHK054, states the naming-defect consequence explicitly; affected: `spec.md:FR-003`.
- CHK057 — export-set.md §1 states there is no open EXPORT_NAME convention choice — it is fixed by the existing alias census; affected: `contracts/export-set.md:§1`.
- CHK059 — plan.md Risks table's stale present-tense "nine" claim corrected to reference Project Structure's current 13-of-14/14-target count; affected: `plan.md` (Risks table, ~line 292).
- CHK067 — item is tagged [Clarity], so WAIVED is not permitted; FR-010c now pins the recording location explicitly (`contracts/export-set.md` §4 + `research.md` R3); affected: `spec.md:FR-010c`.
- CHK069 — FR-004 now states the symmetric no-false-dependency obligation; affected: `spec.md:FR-004`.
- CHK074 — FR-012a now states the module-granularity rule inline, making "exhaustive" a checkable property; affected: `spec.md:FR-012a`.

### DD-DECIDED items
None.

### WAIVED items
None.

Anchors spot-verified: `.specify/architecture.md:500-504,509,523` (target-layout claims and the `check_layers.py` constraint), `CMakeLists.txt:321-324,349-355` (install rules), `grep -rn EXPORT_NAME src/ cmake/ CMakeLists.txt` → 0 matches (confirmed empty) — all resolve exactly as cited in the signed-off bundle, worktree `084-packaging-cpack-export` @ 2026-08-01. Remaining `file:line` cites in this checklist's evidence base (~90 across the bundle) inherit Gate A round 3's verification, which re-read them exact.
