# Error Taxonomy & API Boundaries Requirements Quality Checklist: Async Logger + OTel Observability Surface

**Purpose**: Validate that the error-enumerator set, the C-ABI `[1000,1099]` reservation, the `Sink` interface cap, the adjacent-API amendments, and the deferral/non-goal boundaries are specified completely, unambiguously, and consistently — BEFORE implementation.
**Created**: 2026-06-02
**Feature**: [spec.md](../spec.md)
**Audience/Depth**: Formal pre-implement gate (feeds step-9 checklist-audit)

## Requirement Completeness — Error Enumerators

- [x] CHK001 Is the **exact** new-enumerator set specified (exactly 7: the named `log_*`/`otel_*` variants) so a completeness gate can assert set-equality (not subset)? [Completeness, Spec §FR-015, `[[feedback_completeness_gate_exact_set_not_subset]]`] — PASS: FR-015: "The feature MUST introduce **exactly 7** new `fixpp::core::error` enumerators" followed by the named list: `log_queue_overflow`, `log_sink_open_failed`, `log_sink_write_failed`, `log_sink_flush_failed`, `log_drain_timeout`, `otel_export_failed`, `otel_provider_init_failed`; contracts/error-block.md §Obligations: "The completeness test asserts the **enumerator set** equals {…} at slots 122–128 — exact-SET equality, not subset, per `[[feedback_completeness_gate_exact_set_not_subset]]`"; T008 in tasks.md: "asserting `fixpp::core::error` gained **exactly** the 7 new enumerators (exact-SET equality on the set, missing/unexpected diff)". Complete with exact-set gate.
- [x] CHK002 Are the slot numbers (122–128) and the append-only / non-renumbering rule stated, with slot 121 named as the current highest? [Clarity, Spec §FR-015, `[const §X.4]`] — PASS: FR-015: "at the next free `std::uint8_t` slots 122–128 (append-only / non-renumbering per `[const §X.4]`; the enum is `std::uint8_t`-backed and slot 121 is the current highest)"; contracts/error-block.md: "slot 121 `session_unknown_acceptor_session` is the current highest" (T006 in tasks.md confirms this). All three conditions met.
- [x] CHK003 Is the enum's backing type (`std::uint8_t`) stated, making explicit that values cannot live in `[1000,1099]`? [Clarity, Spec §FR-015] — PASS: FR-015: "the enum is `std::uint8_t`-backed and slot 121 is the current highest"; contracts/error-block.md §Representation decision: "`include/fixpp/core/error.hpp` is `enum class error : std::uint8_t` (max enumerator 255; highest live slot `session_unknown_acceptor_session = 121`)"; the same contract states "Numeric values in `[1000,1099]` **cannot** be represented in that enum". Explicit and unambiguous.
- [x] CHK004 Is the requirement that each enumerator gets a matching `error_message`/`to_string` entry stated (not just the enum addition)? [Completeness, Gap, Spec §FR-015] — PASS: FR-015: "Each maps to a reserved C-ABI `fixpp_error_t` value... See `contracts/error-block.md`"; contracts/error-block.md §Obligations: "The error-string table (`error_message`/`to_string`) gets matching entries for the 7 enumerators"; T006 in tasks.md: "add matching `error_message`/`to_string` entries (FR-015, contracts/error-block.md)"; T008: "smoke-assert `error_message`/`to_string` returns a **non-empty** string for each of the 7 enumerators". Both the requirement and its test are present.
- [x] CHK005 Is the surface/semantics of each of the 7 errors documented (which is returned via `expected_t<void>` vs internal-counter-only)? [Completeness, Spec §FR-015, contracts/error-block.md] — PASS: data-model §error block table gives per-enumerator surface: `log_queue_overflow` (internal C++ only, counter via `drop_count()`); `log_sink_open_failed` (sink disabled, Logger continues); `log_sink_write_failed` (caught by drain, `sink_error_count(i)++`); `log_sink_flush_failed` (flush threw); `log_drain_timeout` (returned via `expected_t<void>` from `shutdown()`; bumps `timeout_drop_count()`); `otel_export_failed` (OTLP batch failure counter; engine continues); `otel_provider_init_failed` (returned via `expected_t<void>`; no-op fallback); contracts/error-block.md table: same per-row semantics. Complete, 7/7 documented.

## Requirement Clarity — C-ABI [1000,1099] Reservation

- [x] CHK006 Is the distinction crisp between the C++ `core::error` enum slots (122–128) and the C-ABI `fixpp_error_t` `[1000,1099]` mapping integers (a future v1.x reservation, NOT enum values)? [Clarity, Ambiguity, Spec §FR-015] — PASS: FR-015: "a future v1.x C-ABI exposure; v1.0 exposes **no** C-ABI log/OTel symbols (FR-020), so the `[1000,1099]` integers are an occupancy/mapping-table reservation only, NOT `core::error` enum values"; contracts/error-block.md §Representation decision distinguishes the two columns explicitly; data-model §error block table has separate columns "C++ slot (uint8_t)" and "C-ABI `fixpp_error_t` map value". Crisp distinction.
- [x] CHK007 Is the anchor §6.3 mislabel ("`fixpp::core::error` variant | `fixpp_error_t` value") explicitly resolved so an implementer does not add variants "at 1000–1011" (a non-compile)? [Conflict, Spec §FR-015, contracts/error-block.md] — PASS: contracts/error-block.md: "The anchor §6.3 table is mislabeled... This contract resolves the mislabel without a 2k/2i amendment"; T006 in tasks.md: "Do **NOT** add variants 'at 1000–1011' to `core/error.hpp` — the enum is `uint8_t`-backed (non-compile)"; FR-015 explicitly calls out the same prohibition. The mislabel is resolved in the spec artifacts and the implementer receives clear guidance.
- [x] CHK008 Is it stated that v1.0 exposes **no** C-ABI log/OTel symbols, so `[1000,1099]` is an occupancy/mapping reservation with no live consumer? [Clarity, Consistency, Spec §FR-015/§FR-020] — PASS: FR-015: "v1.0 exposes **no** C-ABI log/OTel symbols (FR-020), so the `[1000,1099]` integers are an occupancy/mapping-table reservation only, NOT `core::error` enum values"; FR-020: "v1.0 MUST expose log/OTel access via the C++ API only; `c_api/log.h` and `c_api/otel.h` contain version-macro/include-guard placeholders with **no** `extern "C"` symbols"; anchor §6.3 note on `log_queue_overflow`: "maps to no `fixpp_error_t` return path" in v1.0. Consistent, stated in both FR-015 and FR-020.
- [x] CHK009 Is the audit/occupancy handling of `[1000,1099]` scoped correctly (an append-only `error_codes_v1.txt` record / abidiff concern), without pulling in 2i-owned tooling 017 does not ship? [Clarity, Gap, Spec §FR-015] — PASS: FR-015: "The completeness test asserts the **enumerator set** (slots 122–128); the `[1000,1099]` occupancy is an abidiff/mapping-table concern"; T007 in tasks.md: "Record the C-ABI `fixpp_error_t` `[1000,1099]` occupancy mapping in `tools/abi_history/error_codes_v1.txt`... Do **NOT** create or run `tools/check_capi_occupancy.sh` — that is a 2i deliverable... 017 ships **no** live C-ABI symbols (FR-020); this is a forward reservation only." The scope is precisely bounded: 017 creates the text file but does not ship 2i tooling.

## Requirement Completeness — Sink Interface Cap

- [x] CHK010 Is the `Sink` interface specified as **exactly 4** pure-virtual methods (`open`/`emit`/`flush(deadline)`/`close`), within the `[const §XIV.2]` ≤5 cap? [Completeness, Measurability, Spec §FR-007] — PASS: FR-007: "The `Sink` interface MUST expose **exactly 4** pure-virtual methods (`open`, `emit`, `flush(deadline)`, `close`), within the `[const §XIV.2]` ≤ 5 cap"; contracts/log-sinks.md: "`Sink` — exactly 4 pure-virtual (`[const §XIV.2]`, FR-007)"; anchor §4.4: "Exactly 4 pure-virtual methods" with rationale. Measurable — exact count is the requirement.
- [x] CHK011 Are the noexcept/return-type contracts per Sink method specified (e.g., `open` returns `expected_t<void>`; `emit`/`flush`/`close` noexcept)? [Clarity, Gap, Spec §FR-007] — PASS: contracts/log-sinks.md §Sink gives per-method contracts: `open()` returns `[[nodiscard]] expected_t<void>`; `emit(Record const&)` is `noexcept`; `flush(std::chrono::milliseconds deadline)` is `noexcept`; `close()` is `noexcept`; anchor §4.4 gives the full signatures. All four method contracts are specified.
- [x] CHK012 Is config injection specified as constructor-injected (no `open(SinkConfig)` downcast), keeping the 4-method cap intact? [Clarity, Spec §FR-007] — PASS: contracts/log-sinks.md: "Config is constructor-injected (no `open(SinkConfig const&)` downcast)"; data-model §Sink: "Configuration is constructor-injected (no `open(SinkConfig const&)` downcast)"; anchor §4.4: "Configuration is strongly typed: each concrete sink takes its config in its constructor; `open()` is a no-arg 'start running' signal." Gate-A RC#2 fixed this in v0.2. Explicit.
- [x] CHK013 Are the three default sinks (`FileSink`/`OtlpLogSink`/`SyslogSink`) and their per-sink config surfaces each specified? [Completeness, Spec §FR-008] — PASS: FR-008: "Three default sinks MUST ship: `FileSink`... `OtlpLogSink`... `SyslogSink`..."; data-model §FileSink, §OtlpLogSink, §SyslogSink each give the full `*SinkConfig` struct; contracts/log-sinks.md has per-sink sections; anchor §4.5–§4.7 gives full type definitions. Complete.
- [x] CHK014 Is `FileSink`'s disk-usage bound stated unambiguously — `max_file_bytes × max_keep_count` (archived only) **plus** one live file overshooting by **at most one record** before the `>`-triggered rotation? [Clarity, Measurability, Spec §FR-009] — PASS: FR-009: "MUST be bounded by `max_file_bytes × max_keep_count + (one live file that may transiently overshoot max_file_bytes by at most one record before the `>`-triggered rotation)` — `max_keep_count` counts the rotated (archived) files only; the live file is additional. On rotation the oldest archived file is deleted before the bound is exceeded across rotations"; data-model §FileSink and contracts/log-sinks.md repeat the same bound verbatim; TS-4 tests "the byte-bound `max_file_bytes × max_keep_count` (archived files) + one live file overshooting by at most one record before the `>`-triggered rotation — not merely file count ≤ `max_keep_count`". Unambiguous and measurably testable (Gate A Step F closed this exact P2).

## Requirement Clarity — Adjacent API Amendments

- [x] CHK015 Is the `Session::get_trace_context()` amendment specified as the **single canonical** accessor reconciling the existing `trace_context_value()` (thin alias/rename, no second storage read)? [Clarity, Consistency, Spec §Assumptions] — PASS: spec §Assumptions: "make `get_trace_context()` the single canonical accessor (a thin alias of / rename of `trace_context_value()`), introducing **no** second storage read"; contracts/adjacent-amendments.md amendment #1: "make `get_trace_context()` the **single canonical** accessor — a thin alias of (or rename of) `trace_context_value()` — introducing **no** second storage read"; T034 in tasks.md: "migrate BOTH existing callers" and T036 asserts "no lingering `trace_context_value()` duplicate". Specified with single-canonical and no-second-read invariants.
- [x] CHK016 Is the consumed-vs-owned boundary explicit — 017 **consumes** 2d's `trace_slot_`/clock storage but **owns** the public accessor name? [Clarity, Dependency, Spec §Assumptions, contracts/adjacent-amendments.md] — PASS: contracts/adjacent-amendments.md has a 9-row consumed-vs-owned table with explicit "consumed — confirm only" vs "owned amendment" designations for each 2d surface element; spec §Assumptions section enumerates the same distinction. Explicit and authoritative.
- [x] CHK017 Are the `SessionConfig` amendments specified (add `logger_override`/`tracer_override`; `meter_override` intentionally omitted; remove `log_sink_override`)? [Completeness, contracts/adjacent-amendments.md] — PASS: contracts/adjacent-amendments.md amendment #3: "ADD nullable `std::shared_ptr<...> logger_override` and `tracer_override`... `meter_override` intentionally omitted (metrics engine-scoped, anchor §4.8)"; amendment #4: "REMOVE `SessionConfig::log_sink_override` (`session_config.hpp:182`)... replaced by `logger_override`"; verified live at `session_config.hpp:182` by grep. T012/T013 in tasks.md cover these changes. All four items (add 2, omit 1, remove 1) are specified with rationale.
- [x] CHK018 Is the `Engine` engine-trace-context accessor amendment specified, distinct from the comment-only `engine_trace_context_snapshot` identifier (no such member exists today)? [Ambiguity, Gap, contracts/adjacent-amendments.md] — PASS: contracts/adjacent-amendments.md amendment #2: "017 adds **both** (a) an `Engine` member `fixpp::core::detail::trace_context_snapshot engine_trace_ctx_snapshot_`... **seeded at `Engine` construction** from the real `EngineConfig::engine_trace_context` seed field (`engine_config.hpp:157`)... and (b) the public accessor... `Engine::engine_trace_context() const noexcept { return engine_trace_ctx_snapshot_.load(); }`"; the contract note: "there is **no** `EngineConfig::engine_trace_context_snapshot` member instance (the identifier appears only in a comment at `engine_config.hpp:11`)"; verified by grep (no `engine_trace_context` in `engine.hpp`). The distinction from the comment-only identifier is explicit in the contract.

## Requirement Completeness — Deferral / Non-Goal Boundaries

- [x] CHK019 Is the C-ABI deferral (FR-020) stated as an explicit placeholder boundary (`c_api/log.h`/`c_api/otel.h` = version-macro + include-guard only, no symbols)? [Completeness, Boundary, Spec §FR-020] — PASS: FR-020: "`c_api/log.h` and `c_api/otel.h` contain version-macro/include-guard placeholders with **no** `extern "C"` symbols"; anchor §5 gives the exact placeholder content (include guard + `#define FIXPP_LOG_API_VERSION 0u`); T009 in tasks.md creates these files. Explicit placeholder boundary with the exact file content specified.
- [x] CHK020 Is the `GrpcStreamSink` deferral and its AGPL-boundary rationale stated clearly enough to prevent accidental implementation? [Clarity, Boundary, Spec §FR-021] — PASS: FR-021: "`GrpcStreamSink` MUST remain deferred (anchor §10 Q1; AGPL boundary)"; anchor §7 Option A-retracted: "BLOCKED. `service/grpc/` code cannot `#include <fixpp/log/sink.hpp>` per `[arch §8]`"; quickstart.md "Out of scope" section lists `GrpcStreamSink` explicitly. AGPL boundary + BLOCKED status + MUST-remain-deferred — sufficient to prevent accidental implementation.
- [x] CHK021 Is the quill-vs-own backend disposition explicitly **PROVISIONAL** with the own-ring as the v1.0 shipping candidate, and is the facade-contract-is-identical-either-way invariant stated? [Clarity, Consistency, Spec §FR-021] — PASS: FR-021: "The own Vyukov MPSC ring is the v1.0 **shipping candidate** behind the backend-agnostic `Logger` facade; its quill-vs-own disposition stays **PROVISIONAL** per anchor §1.2/§D.2 and `[arch §9.3]`... The public `Logger` facade contract is identical regardless of backend"; spec §Clarifications Q3: "Own ring is the v1.0 shipping **candidate** behind the backend-agnostic facade (anchor §1.2 provisional rec + §D.2; disposition stays PROVISIONAL per `[arch §9.3]`)"; plan.md Constitution Check `[arch §9.3]` confirmed. PROVISIONAL + shipping-candidate + facade-identical all stated.
- [x] CHK022 Are the six non-goals (FR-022) each enumerated as MUST-NOT-build items (sync shim, aggregation/routing/sampling, custom SDK, W3C injection, query language, `dlopen` discovery)? [Completeness, Boundary, Spec §FR-022] — PASS: FR-022 explicitly enumerates all 6 as "MUST NOT be built": "synchronous logging shim, log aggregation/routing/sampling, custom OTel SDK, W3C TraceContext injection into outbound FIX messages, structured-logging query language, `dlopen` sink discovery." 6/6 present. Anchor §2 non-goals also lists them with rationale.

## Consistency & Traceability

- [x] CHK023 Does every Success Criterion (SC-001..008) cite the test seam that verifies it, giving end-to-end traceability FR→SC→TS? [Traceability, Spec §SC-001..008] — PASS: SC-001 cites TS-9 and SC-001; SC-002 cites TS-2; SC-003 cites TS-1 and TS-8; SC-004 cites TS-6/TS-7; SC-005 cites TS-11 and TS-10; SC-006 cites TS-12; SC-007 references FR-014/SC-007 (drain timeout, no separate TS number but covered in FR-014 + Logger::shutdown contract); SC-008 cites TS-1…TS-13 (all 13 seams). FR→SC→TS chain is complete for all 8 SCs. Note: SC-007 does not name a single TS but is covered by the Logger contract (FR-014) + shutdown tests folded into T027/T044.
- [x] CHK024 Is the build-integration ownership (FR-023: 017 owns the OTel/quill pins + CMake options) consistent with the pins referenced elsewhere and the superseded-anchor note? [Consistency, Spec §FR-023, Assumption] — PASS: FR-023 owns the pin; spec §Clarifications Q2 confirms "017 owns its build wiring — pin OTel C++ SDK exactly to `opentelemetry-cpp/1.26.0`"; spec §Assumptions: "The anchor's pins are superseded by FR-023, which owns the pin. 017 owns adding and pinning it (FR-023) rather than depending on an external build-scaffold step"; research.md R1 records the pin decision; plan.md confirms `1.26.0` and `quill/11.1.0`; T001 in tasks.md pins exactly these versions. Consistent everywhere; superseded-anchor note is explicit.
- [x] CHK025 Are the catalogue rows owned (LOG-001..004, OBS-001..003) mapped to FRs so no owned row is left without a requirement? [Traceability, Completeness, Spec §Catalogue] — PASS: spec header: "Catalogue rows owned: LOG-001, LOG-002, LOG-003, LOG-004, OBS-001, OBS-002, OBS-003"; spec §FR sections: LOG-001 ↔ FR-001..006 (producer core); LOG-002 ↔ FR-007..009 (sink interface + sinks); LOG-003 ↔ FR-012..013 (trace correlation); LOG-004 ↔ FR-010..011 (filtering); OBS-001 ↔ FR-016 (SessionSpans); OBS-002 ↔ FR-017 (dual metric export); OBS-003 ↔ FR-018 (OtlpLogSink); anchor Appendix A maps each row to its delivering sections. 7/7 catalogue rows have FRs. No unowned row.

## Notes

- Tests requirement quality (is the taxonomy/boundary specified, crisp, consistent), not the error codes' runtime behavior.
- CHK006/CHK007/CHK009 trace to the Gate-A R1 decisive catch (uint8_t enum vs C-ABI int block) + `/speckit-analyze` C1/ESC-1 — the requirement text's clarity is the audit target; the T007 tooling-scope fix was folded into tasks.md.
- CHK004 traces to `/speckit-analyze` E6 (error-string table); CHK014 to E5 (one-record overshoot bound).

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 25 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **25** |

### SPEC-FIXED items
_(none)_

### DD-DECIDED items
_(none)_

### WAIVED items
_(none)_

Anchors spot-verified: `[2k §4.4]` (Sink interface), `[2k §4.5]` (FileSink), `[2k §4.6]` (OtlpLogSink), `[2k §4.7]` (SyslogSink), `[2k §4.8]` (TracerProvider/MeterProvider), `[2k §5]` (C-ABI placeholders), `[2k §6.3]` (error block), `[2k App D §D.1]` (amendments), `[2k §10 Q1]` (GrpcStreamSink deferral), `[2k §1.2]` (PROVISIONAL), `[arch §9.3]`, `[const §X.4]`, `[const §XIV.2]`, `[2i §1.1]` — all resolve in signed-off revision `.specify/2k-log-otel.md` v0.5 (and for `[2i §1.1]` as cited in anchor Appendix B §B.2).
