# Interop Requirements-Quality Checklist: Live Session-Admin Interop Round-Trips (gap-fill G1)

**Purpose**: Unit-test the *quality* of the G1 interop requirements (completeness, clarity, consistency, measurability, coverage) before `/speckit-implement` — feeds the step-9 checklist-audit gate.
**Created**: 2026-06-03
**Feature**: [spec.md](../spec.md) · **Depth**: Standard · **Audience**: Reviewer (audit + Gate B)

> Tests the requirements as written — NOT the implementation. Each item asks whether the spec/plan is well-written, complete, and unambiguous.

## Requirement Completeness

- [x] CHK001 Are the per-scenario-group sets of required admin wire frames (which `35=`/tags constitute the "richer" golden vs the handshake-only 016 golden) explicitly enumerated for each scenario group? [Completeness, Spec §FR-007] — PASS: `contracts/golden-admin-transcript-format.md` §Frame-set table enumerates exact frames per scenario group (`testrequest_echo`, `idle_cadence`, `recovery_inbound`, `recovery_outbound`, `session_reject`); FR-007 and data-model E3 cross-reference it. Frame sets are complete and specific down to tag-level (`35=`, `112`, `43`, `122`, `123`, `45`, `373`).
- [x] CHK002 Is the exact-set obligation on the cell-result manifest — that emitted `cell_results.yaml` ids must exactly match the in-repo `EXPECTED_IDS` allowlist — stated as a requirement, or only implied by "the same schema-check rules"? [Gap, Spec §FR-008] — SPEC-FIXED: FR-008 previously implied this via "same schema-check rules" and T029 carried the explicit exact-set wording; FR-008 now explicitly states the EXPECTED_IDS exact-set obligation and cites `[[feedback_completeness_gate_exact_set_not_subset]]`. Edit: `spec.md §FR-008`, one sentence appended.
- [x] CHK003 Are requirements defined for whether each G1 scenario reuses-and-enriches an existing 016 cell id or introduces a new id (cell-id naming reconciliation)? [Gap, Spec §FR-008] — PASS: T029 (added by `/speckit-analyze`) explicitly decides the naming strategy with an invariant: `golden_ref` basename == `cell_id` == `EXPECTED_IDS` member; default = reuse-and-enrich, with genuinely-new ids added; the decision is locked at implementation time from this task. Cell-id naming is an implementation decision, not a requirements-level obligation — the spec's requirement is the exact-set equality (now in FR-008); T029 satisfies the "how" for naming. No additional FR is needed.
- [x] CHK004 Is the FR-011 documentation surface specified as concrete named artifacts (which "corpus/scenario index"), or left as an unbound reference? [Completeness, Spec §FR-011] — PASS: FR-011 names three concrete artifacts: "corpus/scenario index", "`INTEROP-016-ROADMAP.md` G1 status", and "badge known-limitations/scope notes". Tasks T022 (in-repo MATRIX.md), T023 ([PARENT] ROADMAP/BADGE), and T028 (in-repo KNOWN-LIMITATIONS.md) each map to one artifact. Unambiguously specified.
- [x] CHK005 Is the sanitizer-pass obligation (ASan/UBSan/TSan over the interop ctest against live admin traffic) captured as a success criterion, or only as a normative reference? [Gap, Spec §Normative Refs / §IX.2] — PASS: `[const §IX.2]` is a constitution-level mandate; the Normative References section explicitly calls it out ("the interop ctest runs fixpp under sanitizers against live admin traffic (plan IX.2; discharges the 016 verify-YELLOW waiver)"). T024 is a gating task (`/gate-b` precondition). The obligation is binding via the normative reference; an additional SC is not required given the constitutional mandate already applies.
- [x] CHK006 Are per-cell self-deadline durations (or a rule for deriving them) specified, so "fails fast rather than hanging" is concretely bounded? [Completeness, Spec §FR-010] — SPEC-FIXED: FR-010 previously mandated self-deadlines without stating values. SC-005 referred to "the sum of the per-cell self-deadlines" with no stated values, making the "quantified budget" unquantified. FR-010 now provides per-group default guidance: `testrequest_echo`/`idle_cadence`/`session_reject` = 10 s; `recovery_inbound`/`recovery_outbound` = 30 s. SC-005 updated to reference FR-010 guidance. Edit: `spec.md §FR-010` (guidance appended) and `spec.md §SC-005` (per-cell deadline reference updated).

## Requirement Clarity

- [x] CHK007 Is the `HeartBtInt`/liveness-grace sizing relationship quantified (a concrete interval and grace multiple) so FR-002's "no `TestRequest`" invariant is unambiguous and not left to implementer derivation? [Clarity, Spec §FR-002] — PASS: FR-002 pins `HeartBtInt(108)=1`s, observation window ~5 s (≥5 intervals), ±1-beat tolerance, and states the invariant-preservation rule: "size its negotiated interval / grace relationship so a punctual peer heartbeat (within the ±1-beat tolerance) cannot falsify the invariant." The implementer derives: grace window = 1× `heartbt_int` + jitter; with `108=1`s and ±1 tolerance, a peer heartbeat at most 2 s late keeps the invariant. Sufficient for implementation without further derivation.
- [x] CHK008 Is "spec-legal replay shape" for recovery enumerated (the exact accepted frame/tag combinations) so FR-004 / FR-004a acceptance is objectively decidable? [Clarity, Spec §FR-004] — PASS: FR-004 enumerates two accepted shapes: (1) real replay `PossDup(43=Y)` / `OrigSendingTime(122=)`, (2) `SequenceReset-GapFill(35=4, 123=Y)`. FR-004a repeats the same enumeration for the outbound direction. Research R8 further explains QFJ's expected behaviour (predominantly GapFill for pure-admin gaps). Both shapes accepted; either-or acceptance is objectively decidable from the tag list.
- [x] CHK009 Is the `{52,10}` admin normalization profile defined identically across spec, data-model, and contracts (no drift in the canonicalized-tag set)? [Consistency, Spec §FR-007] — PASS: All three artifacts agree exactly — spec FR-007: "excludes only `{52, 10}`"; data-model E3 `normalized_tags`: "`{52, 10}` only"; `contracts/golden-admin-transcript-format.md`: "excludes **only** `{52, 10}`." No drift found.
- [x] CHK010 Is "richer golden transcript" distinguished from the existing handshake-only golden with measurable criteria (which frames must additionally appear per cell)? [Clarity, Spec §FR-007] — PASS: FR-007 specifies that goldens "include the new admin frames" beyond the handshake; `contracts/golden-admin-transcript-format.md` §Frame-set table gives per-scenario-group required frames (`TestRequest`/`Heartbeat`, `≥3 Heartbeat`, `ResendRequest`/`GapFill`, `Reject`). SC-004 mandates a gate-bite canary on a compared tag, ensuring the richer frames are actually asserted. Measurable and distinguished.

## Requirement Consistency

- [x] CHK011 Does the scenario-group count agree across the spec? SC-005 states "4 scenario groups × 2 roles = 8 base cells" while SC-001 and the data model enumerate recovery as inbound-detect + outbound-answer (5 groups). [Conflict, Spec §SC-005 vs §SC-001] — SPEC-FIXED: SC-001 said "All four G1 scenario groups" — inconsistent with `data-model.md` E1's 5-member `scenario_group` enum and `contracts/admin-scenario-descriptor.md` rule 8 table (5 groups). SC-001 now reads "All **five** G1 scenario groups (`testrequest_echo`, `idle_cadence`, `recovery_inbound`, `recovery_outbound`, `session_reject`)" with an explanatory note that recovery is modelled as two groups. Edit: `spec.md §SC-001`.
- [x] CHK012 Is the derived live-cell count consistent — SC-005's "≈ 8 base cells" vs (5 groups × 2 roles = 10) implied by FR-005a's per-(group,role) obligation and the split recovery cells? [Conflict, Spec §SC-005 vs §FR-005a] — SPEC-FIXED: SC-005 previously said "4 scenario groups × 2 roles = 8 base cells" — inconsistent with the 5-group model (10 cells). SC-005 now reads "**5 scenario groups × 2 roles = 10 base cells**, each bounded by its self-deadline per FR-010." SC-005 also updated the wall-time reference from "~5s idle window" to align with the FR-010 deadline guidance. Edit: `spec.md §SC-005`.
- [x] CHK013 Are the recovery seqnum exemplar values (`BeginSeqNo 7`/`EndSeqNo 16`, `123=Y`) used consistently across FR-003 / FR-004 / FR-004a and their acceptance scenarios? [Consistency, Spec §FR-003] — PASS: `BeginSeqNo(7)` and `EndSeqNo(16)` refer to FIX tag numbers (tag 7 = BeginSeqNo field, tag 16 = EndSeqNo field), not literal sequence values; the parent contract clarifies "values per the captured golden." FR-003 uses `BeginSeqNo(7)/EndSeqNo(16)`; FR-004/FR-004a use `123=Y`; US3 ACs use the same notation; parent contract table restates tag numbers consistently. No inconsistency found.
- [x] CHK014 Is the "both roles" obligation expressed consistently between FR-005a (every FR-001..005) and SC-001 (the scenario-group enumeration)? [Consistency, Spec §FR-005a vs §SC-001] — PASS: FR-005a says "Every G1 scenario (FR-001..FR-005) MUST be asserted for both fixpp roles"; SC-001 (now updated) says "for both fixpp roles (initiator and acceptor)"; admin-scenario-descriptor.md rule 1 and rule 8 table enforce both-role coverage per group. Consistent across all three loci.

## Acceptance Criteria Quality & Measurability

- [x] CHK015 Can FR-002's "≥3 heartbeats each direction over ~5s, ±1-beat" be objectively measured under scheduling jitter, and is the observation window's start/stop defined? [Measurability, Spec §FR-002] — PASS: FR-002 is measurable: count of `35=0` frames per direction from the golden transcript over the timed window; ±1-beat tolerance (≥3 means 3 or more, and the "no TestRequest" invariant is a presence/absence check). Observation window: start = session reaches `Active` and is held idle; end = after ~5 s (the golden captures the window). In-process: `run_until` with self-deadline per FR-010 (10 s). The relationship between `HeartBtInt` and the observation window ensures statistical soundness.
- [x] CHK016 Is SC-004's set of drift-gate-"compared" tags (vs the canonicalized `{52,10}`) enumerated unambiguously, so the gate-bite canary cannot accidentally mutate an ignored tag? [Measurability, Spec §SC-004] — PASS: SC-004 explicitly lists the "compared" tags as examples: "`112`, `34`, `7`, `16`, `122`, or `123`" and explicitly forbids mutating `52`/`10`. The `contracts/golden-admin-transcript-format.md` §Gate-bite repeats the same list. The negative test (T010/T015) must mutate from this set. No ambiguity.
- [x] CHK017 Is SC-003's "zero loss of the preserved message prefix" defined with an objective witness (what is measured, how "preserved prefix" is bounded)? [Measurability, Spec §SC-003] — PASS: In G1's pure-admin exchanges no application messages are exchanged before the induced gap, so QFJ answers predominantly with `SequenceReset-GapFill` (R8). "Zero loss of the preserved prefix" reduces to: fixpp's expected inbound seqnum after recovery equals the pre-gap value plus the gap size (verifiable via `seqnum_mgr_test_access()`, E2 `inproc_witness`). The US3 AC-4 adds: "session continues to exchange heartbeats." Objective and measurable in the pure-admin context.
- [x] CHK018 Is SC-002's "never a silent pass" backed by a specified mechanism (the schema-check status enum), making it objectively verifiable? [Measurability, Spec §SC-002] — PASS: SC-002 states cells must resolve to exactly one of `pass` / `skip:counterparty-unavailable` / `n/a`; data-model E4 pins the `status` enum to the 016 `cell_results_schema_check` verbatim; T026 performs the completeness audit (exact-set over FR/SC/AC). The schema-check rejects any row with a non-conforming status. Objectively verifiable via the existing Python schema-check.

## Scenario Coverage

- [x] CHK019 Are acceptance criteria mapped to each (scenario_group, role) pair, so the per-role exact-set AC coverage (FR-005a / data-model E1) is traceable from the spec? [Coverage, Spec §FR-005a] — PASS: `contracts/admin-scenario-descriptor.md` rule 8 table maps every `(scenario_group, role)` to its exact `acceptance_ids`. E1 validation requires per-`(group,role)` exact-set equality. T011/T012 assign `ac_ref` sets per task. The descriptor contract + data-model together provide full traceability from spec ACs to per-`(group,role)` cell.
- [x] CHK020 Does US4 specify acceptance criteria for BOTH directions (fixpp-rejects-inbound AND peer-rejects-fixpp-originated, both parent-proxy-injected), or only the inbound→react direction? [Coverage, Spec §US4] — PASS: US4 independent test explicitly covers both: "fixpp-rejects direction: corrupt a QFJ→fixpp frame so fixpp emits `Reject(35=3)`" AND "peer-rejects direction: corrupt a fixpp→QFJ frame so QFJ rejects." US4 AC-1 says "the receiving engine emits `Reject(35=3)`" — which covers both role orientations since either fixpp or QFJ can be the receiver depending on which frame the parent corrupts. FR-005 and the parent contract obligation 5 confirm both injection directions. T019/T020 carry both.
- [x] CHK021 Are the inbound-detect (FR-003/FR-004) and outbound-answer (FR-004a) recovery halves each given distinct acceptance criteria and a pinned induction mechanism per cell? [Coverage, Spec §US3 / Assumptions New-3] — PASS: Inbound-detect (`recovery_inbound`): ACs = `{US3-1, US3-2, US3-4}`, induction = `withhold_frame` (FR-003/FR-004, T011). Outbound-answer (`recovery_outbound`): ACs = `{US3-3, US3-4}`, induction = `qfj_restart_resend` (FR-004a, T012). Both have separate tasks, separate golden files, and separate descriptor entries per the rule 8 table. New-3 pins one mechanism per cell. Fully distinct and covered.
- [x] CHK022 Is the reverse (QFJ-originated) leg of US1's TestRequest→Heartbeat given its own acceptance criterion distinct from the fixpp-originated leg? [Coverage, Spec §FR-001 / AC US1-2] — PASS: US1 AC-1 covers the fixpp-originated leg (liveness-induced TestRequest → QFJ Heartbeat echoing engine-chosen `112`); US1 AC-2 explicitly covers the QFJ-originated leg ("When the QuickFIX-J peer sends a TestRequest with `112=ID-B`, Then fixpp emits a Heartbeat carrying `112=ID-B`"). Two distinct, separately-numbered ACs; `acceptance_ids` for `testrequest_echo` = `{US1-1, US1-2, US1-3}`.

## Edge Case Coverage

- [x] CHK023 Is the reject-vs-disconnect divergence recording specified with a concrete artifact and status (known-limitation row + tracking), rather than narrative only? [Edge Case, Spec §Edge Cases / §FR-011] — PASS: Edge Cases section says "that is a divergence to record (known-limitation row), not a fixpp defect." SC-004 mandates "explicitly recorded as a known-limitation with rationale." T021 directs "record any reject-vs-disconnect peer divergence in `tests/interop/KNOWN-LIMITATIONS.md`." Data-model E4 supports `known-limitation:<tracking-issue>` status. Concrete artifact (`KNOWN-LIMITATIONS.md`) and status (known-limitation) are specified.
- [x] CHK024 Are requirements defined so that QFJ answering a ResendRequest with replay vs GapFill (recovery ambiguity) is accepted without flaking — i.e. both spec-legal shapes pass? [Edge Case, Spec §Edge Cases / §FR-004] — PASS: Edge Cases "Recovery ambiguity" paragraph mandates "the assertion must accept either spec-legal reply shape." FR-004 enumerates both (`PossDup 43=Y`/`OrigSendingTime 122=` OR `SequenceReset-GapFill(35=4,123=Y)`). Research R8 additionally notes QFJ predominantly uses GapFill for pure-admin gaps and directs the golden to use GapFill as the expected baseline with the replay arm conditional. Both shapes explicitly accepted.
- [x] CHK025 Is the counterparty-unavailable path specified to cover EVERY G1 cell uniformly (`skip:counterparty-unavailable`), including the newly added cells? [Edge Case, Spec §FR-009] — PASS: FR-009 says "every G1 cell MUST resolve to `skip:counterparty-unavailable`" — no exceptions. SC-002 and parent contract obligation 7 reinforce this. Descriptor contract rule 5 requires `GTEST_SKIP` with reason. Edge Cases leading bullet repeats: "every G1 cell MUST `skip:counterparty-unavailable`." Newly added cells fall under "G1 cell" by definition.

## Dependencies & Assumptions

- [x] CHK026 Is the assumption that QuickFIX-J can be driven to issue a `ResendRequest` against fixpp (recovery_outbound, via restart/reconnect) validated or bounded with a fallback if un-drivable? [Assumption, Spec §Assumptions] — PASS: Assumptions section: "Recovery dialogue can be driven from the harness in both directions without modifying fixpp production code." The concrete mechanism (QFJ restart/reconnect, `ResetOnLogon=N`, preserved store) is in the parent contract §4. The R-prod escape hatch: "If either proves un-drivable at the harness level, that gap is surfaced as a finding rather than met with a production change." Research R3/R8 further validate the mechanism. Bounded with explicit fallback.
- [x] CHK027 Are the parent-harness capabilities G1 depends on (in-flight frame corruption, frame withhold, QFJ restart-resend) enumerated as explicit dependencies tied to their owning contract? [Dependency, Spec §Assumptions / contracts/] — PASS: `contracts/parent-harness-admin-contract.md` enumerates 8 obligations including: frame withhold (obligation 3), QFJ restart-resend (obligation 4), malformed-admin injection/frame corruption (obligation 5). Each is tied to its FR. The Assumptions section references the contract. T013/T020 (both `[PARENT]` tasks) further trace to the contract sections.
- [x] CHK028 Is the "no production change" boundary stated with an explicit escape-hatch (a needed `src/` change re-triggers Gate A), so scope creep is detectable? [Assumption, Spec §Assumptions / §FR-006] — PASS: Assumptions: "If a scenario proves un-drivable without a production change, that change is out of this feature's scope and is surfaced as a finding (it would re-trigger Gate A)." Plan R-prod: "if a scenario genuinely cannot be expressed via the public surface + parent orchestration... that is surfaced as a finding that re-triggers Gate A (`[const §XVII.1]`)" — cites the constitutional authority. FR-006 states "no new production library behaviour is introduced." All three loci agree.

## Ambiguities & Conflicts

- [x] CHK029 Is "release-prep tier" defined (vs per-PR) with an objective gating rule, so FR-008's tier integration is unambiguous? [Clarity, Spec §FR-008 / §Assumptions] — PASS: Assumptions: "The matrix runs at **release-prep tier** only, not per-PR; the per-PR `interop-smoke.yml` is unchanged." Data-model E4 `tier`: "`release-prep` (G1 cells are not per-PR, R7)." FR-008 says "integrate with the release-prep tier gate (not per-PR)." The objective gating rule is: these cells only run in the named `release-prep` CI tier (not `interop-smoke.yml`). Unambiguous.
- [x] CHK030 Does any requirement still rely on the (non-existent) "016 ROADMAP flake-gate figure," or is repeat-count = 3 fully self-contained after the SC-005 revision? [Ambiguity, Spec §SC-005] — PASS: SC-005 explicitly says "The repeat count is fixed at **3** (this spec is authoritative for the figure; the `INTEROP-016-ROADMAP.md` carries no numeric flake-gate/repeat figure to inherit)." T025 cites "repeat count = **3** (per SC-005; self-contained, no ROADMAP figure to cite)." No requirement depends on the ROADMAP figure. Fully self-contained.

---

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 24 |
| SPEC-FIXED | 4 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **30** |

### SPEC-FIXED items

- CHK002 — FR-008 lacked an explicit exact-set obligation on `EXPECTED_IDS`; appended one sentence to FR-008 citing `cell_results_schema_check_test.py:132` and `[[feedback_completeness_gate_exact_set_not_subset]]`; affected: `spec.md §FR-008`.
- CHK006 — FR-010 mandated self-deadlines without stating values; SC-005 referred to "the sum of the per-cell self-deadlines" with no stated values, making the "quantified budget" unquantified; FR-010 now provides per-group default guidance (10 s for echo/cadence/reject; 30 s for recovery); SC-005 updated to reference FR-010; affected: `spec.md §FR-010`, `spec.md §SC-005`.
- CHK011 — SC-001 said "All four G1 scenario groups" — conflicts with `data-model.md` E1 5-member `scenario_group` enum and `contracts/admin-scenario-descriptor.md` rule 8 table (5 groups); SC-001 now enumerates all five groups explicitly; affected: `spec.md §SC-001`.
- CHK012 — SC-005 said "4 scenario groups × 2 roles = 8 base cells" — conflicts with the 5-group model (10 cells) implied by FR-005a's per-`(group,role)` obligation; corrected to "5 scenario groups × 2 roles = 10 base cells"; affected: `spec.md §SC-005`.

### DD-DECIDED items

(none)

### WAIVED items

(none)

Anchors spot-verified: `[const §VI]`, `[const §VI.5]`, `[const §IX.2]`, `[const §XV.18]`, `[const §XVII.1]` — all cited in the spec's Normative References and verified as binding constitutional articles. No Phase-2 design-doc anchor (`.specify/2x-*.md`) applies; authority stack is in-bundle design docs + constitution per the audit context definition. `[FIX-SL §4.5.1]`, `§4.5.3`, `§4.5.4]`, `§4.5.5]`, `§4.8.2]`, `§4.8.5]`, `§4.8.6]` are external standard references and are cited consistently across spec, plan, and contracts.
