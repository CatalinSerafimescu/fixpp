# Checklist: Resend-reply wire-conformance requirements quality

**Purpose**: Unit-tests-for-English over the 037 requirements — validate the GapFill/replay PossDup requirements are complete, unambiguous, consistent, and measurable before `/speckit-implement`.
**Created**: 2026-06-14
**Domain**: FIX wire conformance (resend-reply emitters)
**Audience/timing**: Gate B reviewer / pre-implementation
**Feature**: [spec.md](../spec.md)

## Requirement Completeness

- [x] CHK001 Are requirements defined for BOTH resend-reply frame kinds — the GapFill (`35=4`) and the replayed application frame? [Completeness, Spec §FR-001..005] — PASS: FR-001/002/003 cover the GapFill (35=4); FR-004/005 cover the replayed application frame; both US1 and US2 have independent test cells; spec §Key Entities defines both frame kinds. All frame kinds accounted for.
- [x] CHK002 Is the source of the GapFill's `OrigSendingTime(122)` value explicitly specified (the frame's own `52`, not a stored original)? [Completeness, Spec §FR-002, Assumptions] — PASS: spec §FR-002 "equal to that frame's own `SendingTime(52)` value"; Assumptions para 1 "verified against cloned reference engines… both set `122 = 52`"; data-model frame kind A row "= the `sending_time` param (== this frame's 52)"; research D-1 decision. Unambiguous, reference-engine verified.
- [x] CHK003 Is the source of the replayed app frame's `122` explicitly specified (the stored `52`, not a caller-supplied `122`)? [Completeness, Spec §FR-005] — PASS: spec §FR-005 "the replayed frame's `OrigSendingTime(122)` MUST be the stored frame's `SendingTime(52)` (preserving today's replay semantics — the engine sources `122` from the stored `52`, not from any caller-supplied `122`)"; data-model INV-2 "For a replayed app frame, `52` is the stored original `52` (NOT any caller-supplied `122`)"; research D-4 decision.
- [x] CHK004 Are the default-path vs non-default (`allow_pos_dup=true`) behaviors each separately specified? [Completeness, Spec §FR-006, US2] — PASS: spec §FR-006 specifies the default-path byte-identity guarantee explicitly; spec §US2 and FR-004/005 address the non-default `allow_pos_dup=true` retain case; data-model frame kind B table distinguishes "Default config" vs "Retain config" rows.
- [x] CHK005 Is the full set of GapFill fields that MUST remain unchanged enumerated (`8/35/34/49/52/56/36/123`)? [Completeness, Spec §FR-003] — PASS: spec §FR-003 enumerates all eight: "`BeginString(8)`, `MsgType(35)=4`, `MsgSeqNum(34)`, `SenderCompID(49)`, `SendingTime(52)`, `TargetCompID(56)`, `NewSeqNo(36)`, `GapFillFlag(123)=Y`"; data-model frame kind A table lists all eight with ✓ Unchanged; INV-6 cross-references FR-003.
- [x] CHK006 Are the verification surfaces (unit cell, golden re-bake, live re-run) each tied to a named success criterion? [Completeness, Spec §SC-001..004] — PASS: SC-001 → unit cell + golden (GapFill exact-count assert); SC-002 → unit cell with retain knob (replayed frame); SC-003 → golden byte-identity comparison; SC-004 → live QFJ in-process recovery-outbound cell (`hp_fix44_recovery_outbound_answer`, both roles). All four SC rows name a verifiable witness surface.

## Requirement Clarity

- [x] CHK007 Is "exactly one `43`/`122`" stated as an exact-count, not a presence check (so a duplicate-tag frame fails)? [Clarity, Spec §FR-004, Contract C-2] — PASS: spec §FR-004 "MUST carry exactly one `PossDupFlag(43)` and exactly one `OrigSendingTime(122)`"; spec §SC-001 "exactly one `43=Y` and exactly one `122`"; spec §SC-002 "exactly one `43` and exactly one `122`"; contract C-2 items 1/2 "Contains exactly one…"; tasks T004 "count_tag(43)==1 AND … count_tag(122)==1". Exact-count stated uniformly.
- [x] CHK008 Is the `122 == 52` relationship stated as byte-equality (not merely "a timestamp")? [Clarity, Spec §FR-002, data-model INV-2] — PASS: spec §FR-002 "whose value equals the frame's `SendingTime(52)` value"; spec §SC-001 "`122` whose value equals the frame's `52`"; data-model INV-2 "emitted `122` value byte-equals the frame's `52`" (explicit "byte-equals"); contract C-1 item 3 "byte-equals the frame's own `SendingTime(52)`"; quickstart Cell 1 assert 4 "value(122) bytes == value(52) bytes". Byte-equality is the stated form.
- [x] CHK009 Is "the unmarked GapFill" disambiguated so it cannot be read as the corrected (and false) `122`-reject rationale? [Ambiguity, Spec §US1 Why-this-priority + Assumptions] — PASS: spec §US1 "Why this priority" first states "A strict peer rejects the unmarked (missing `43=Y`) GapFill as a too-low sequence number" and immediately disambiguates in the same paragraph: "(The kill is the too-low-seqnum check, which `43=Y` suppresses — *not* the `122`-required check, from which a `SequenceReset` is exempt; see Assumptions)". spec §Assumptions para 2 further expounds: "`43=Y` is emitted together with `122` by emit-parity, NOT because a strict peer rejects a GapFill missing `122`" and explains the QFJ `validatePossDup` guard. No surviving ambiguity across any artifact (grep confirmed; see CHK012).
- [x] CHK010 Is the field-placement decision (append after `123`) stated with its interop justification and its limitation (header-after-body, not strict-order canonical)? [Clarity, research §D-3] — PASS: research D-3 decision fully stated with (a) primary proof = live-shipped precedent (`build_replay_frame` ships green against both QFcpp/QFJ with append-at-end); (b) secondary explanation = map-based parsing; (c) limitation = "header-after-body shape — interoperable with QuickFIX's map-based parsers and the live QFcpp/QFJ targets, but **not** strict-header-order canonical"; tasks T017 records the L-037-1 limitation B&L row for Polish.

## Requirement Consistency

- [x] CHK011 Does the `122 = own 52` (GapFill) vs `122 = stored 52` (replay) distinction read consistently across spec / data-model / contract / research? [Consistency, INV-2 / D-1 / D-4] — PASS: spec §FR-002 (GapFill: "own `SendingTime(52)`"), §FR-005 (replay: "stored frame's `SendingTime(52)`"), §Assumptions, §Edge Cases; data-model INV-2 ("For the GapFill, `52` is the `sending_time` param; for a replayed app frame, `52` is the stored original `52`"); contract C-1 item 3 (GapFill: "byte-equals the frame's own `SendingTime(52)`"), C-2 item 3 (replay: "the stored frame's original `SendingTime(52)` — NOT any caller-supplied `122`"); research D-1 (GapFill) and D-4 (replay). All four artifacts agree.
- [x] CHK012 Is the `122`-emit rationale (emit-parity + FIX grammar, NOT inbound-reject avoidance) stated consistently in ALL artifacts, with no surviving "strict peer rejects missing 122" claim? [Consistency, research §D-2] — PASS: comprehensive grep across all feature-dir files confirms no surviving inverted rationale. Every hit with "reject" + "122" context is either (a) the research D-2 "earlier claim is FALSE" correction, (b) spec §Assumptions para 2 explicit "NOT because a strict peer rejects a GapFill missing `122`", (c) contract C-1 rationale "so a strict peer does **not** reject a GapFill missing `122` — the `122` is for parity/well-formedness, not to dodge a reject", or (d) plan:125 "NOT a strict-peer reject". All artifacts state the correct rationale (emit-parity + FIX grammar) consistently.
- [x] CHK013 Are the unchanged-field enumerations identical across FR-003, AS-2, quickstart Cell 1, and T004 (incl. `52`)? [Consistency, Spec §FR-003 / quickstart / tasks] — PASS: FR-003 (spec:66) enumerates `8/35/34/49/52/56/36/123`; spec §US1 AS-2 (spec:34) lists "every previously-present field (`8/35/34/49/52/56/36/123`) unchanged"; quickstart Cell 1 assert 5 "tags `8/35/34/49/52/56/36/123` unchanged"; T004 (tasks:53) "tags `8/35/34/49/52/56/36/123` present/unchanged (the full FR-003 set incl. `52`)". All four enumerations are identical, including `52`.
- [x] CHK014 Do the catalogue citations (S-005/S-006/S-010/S-033, `[FIX-SL §4.8.x]`) match between the Normative References section and the plan? [Consistency, Spec §Normative References] — PASS: spec §Normative References lists `[FIX-SL §4.8.5]`→S-006, `[FIX-SL §4.8.4]`→S-010, `[FIX-SL §4.8.2]`→S-005, `[FIX-SL §4.8.4]`→S-033. Plan summary (plan:13) names S-006/S-010/S-005 for the two builder edits; plan §Phase 0 key decisions enumerates all; plan Constitution Check (plan:42) cites §4.8.5/4.8.4/4.8.2 and all four S-rows. All four S-rows confirmed to exist in `spec/feature-catalogue.md` (S-005/S-006/S-010/S-033 rows verified by grep). `[FIX-SL §4.8.x]` pairings consistent.

## Acceptance Criteria Quality (Measurability)

- [x] CHK015 Can each SC be objectively verified by a named witness (unit assert / golden diff / live cell), with no subjective term? [Measurability, Spec §SC-001..004] — PASS: SC-001 → "unit-level frame inspection and by a banked golden capture" (exact-count assert + golden diff); SC-002 → "unit-level frame inspection with the knob explicitly enabled"; SC-003 → "golden comparison" (byte-identical); SC-004 → "in-process recovery-outbound cell … (both roles)" named by file path. No subjective terms ("looks like", "similar") in any SC. All success criteria are machine-verifiable.
- [x] CHK016 Is the default-path byte-identity criterion (FR-006/SC-003) expressed as a byte-for-byte oracle comparison, not "looks the same"? [Measurability, Spec §SC-003, quickstart Cell 3] — PASS: spec §SC-003 "replayed application frames are byte-identical to prior behavior … verified by golden comparison"; quickstart Cell 3 assert "build_replay_frame output == the pre-037 expected bytes, byte-for-byte"; tasks T013 "assert `build_replay_frame` output == the T003 pre-037 oracle, byte-for-byte"; tasks T003 "capture the pre-037 default-path replay byte oracle". Oracle-based byte-exact comparison fully specified.
- [x] CHK017 Is the no-new-surface guarantee (FR-007) given a concrete, checkable acceptance form (e.g., header diff is doc-comment-only)? [Measurability, Spec §FR-007, tasks T022] — PASS: spec §FR-007 "MUST NOT introduce any new public configuration field, error code/slot, generated-code surface, or C-ABI surface"; tasks T022 "FR-007 surface gate: assert `git diff include/` shows ONLY doc-comment changes (no signature/type/error-slot/config change)"; research D-6 confirms `sending_time` already exists as a parameter; header confirmed by source read (admin_messages.hpp:168-171, signature unchanged). Mechanically checkable via `git diff`.

## Scenario & Edge-Case Coverage

- [x] CHK018 Is the "stored frame already carries `43`/`122`" case (the DEFECT-2 trigger) covered as a requirement, including the honesty obligation to first prove the stored frame had them? [Coverage, Spec §US2, Contract C-4] — PASS: spec §US2 describes the scenario ("operator has opted into retaining caller-supplied `43`/`122`… One such frame is stored verbatim. Later the peer requests a resend"); spec §US2 Independent Test "with the retain knob enabled, send an application frame that already carries `43=Y`/`122`"; FR-004/005 cover the requirement; contract C-4 "The retain-case witness MUST first prove the STORED frame contained `43`/`122` (else 'exactly one' is trivially satisfied… and proves nothing about dedup)"; tasks T012 "FIRST assert the STORED frame contains `43` AND `122` (honesty, C-4)".
- [x] CHK019 Is the "GapFill spans a range → no single original `52`" edge case addressed in requirements? [Edge Case, Spec §Edge Cases, research §D-1] — PASS: spec §Edge Cases para 1 "a GapFill represents a *range* of skipped messages, not a single original message, so there is no single stored `SendingTime` to copy. The `OrigSendingTime(122)` value is the GapFill's own `SendingTime(52)`"; research D-1 full decision with alternatives considered and rejected. Explicitly addressed.
- [x] CHK020 Is the retained-`122` ≠ stored-`52` case specified (the engine value wins)? [Edge Case, Spec §Edge Cases, §FR-005] — PASS: spec §Edge Cases para 2 "a retained frame whose own `122` differs from its `52` still replays with `122 = stored 52` (engine semantics), exactly once"; spec §FR-005 "the replayed frame's `OrigSendingTime(122)` MUST be the stored frame's `SendingTime(52)`… NOT from any caller-supplied `122`"; contract C-2 item 3 "the stored frame's original `SendingTime(52)` — NOT any caller-supplied `122`"; tasks T012 uses `t != stored 52` to discriminate.
- [x] CHK021 Are the cross-frame requirements (a resend reply emitting BOTH a GapFill and a replayed frame) captured? [Coverage, Contract C-3] — PASS: contract C-3 "A single resend reply may emit both kinds (replayed app frames interleaved with GapFills covering admin ranges). Every frame in the reply that carries `43=Y` MUST also carry exactly one `122 == that frame's 52`. No frame in a resend reply carries a duplicate `43` or `122`."; plan §Why one feature "share the witness harness (a resend reply that emits both a GapFill and a replayed app frame)".

## Dependencies, Assumptions & Boundaries

- [x] CHK022 Is the reference-engine ground truth (both engines stamp `122=52`; QFJ `35=4` exemption) recorded as a validated assumption with resolvable citations? [Assumption, research §D-1/D-2, Assumptions] — PASS: spec §Assumptions para 1 "Verified 2026-06-14 against the cloned reference engines: QuickFIX-cpp `Session::generateSequenceReset` (`insertOrigSendingTime(hdr, hdr.getField<SendingTime>())`) and QuickFIX-J `Session.generateSequenceReset` (`header.setUtcTimeStamp(OrigSendingTime, header.getUtcTimeStamp(SendingTime), prec)`) both set `122 = 52`"; spec §Assumptions para 2 cites QFJ `validatePossDup` (`Session.java:~2575`) with the guard condition; research §D-1/D-2 provide full resolvable file paths for the reference-engine sources.
- [x] CHK023 Is the SC-004 QFcpp-live limitation documented as an explicit waiver (L-021-3), not silently omitted or over-claimed? [Assumption/Boundary, Spec §SC-004, §FR-008] — PASS: spec §SC-004 explicitly: "The **QuickFIX-cpp arm is waived with rationale** (L-021-3 precedent: QFcpp's resend choreography cannot be induced into the fixpp-emitted-GapFill frame on command, and the in-process witness skips it). Unit-level frame inspection + the banked golden cover the byte-level GapFill conformance for both engines"; spec §FR-008 mirrors the waiver language; spec §US1 AS-3 states the waiver; tasks T011 records "QFcpp live arm is **waived per L-021-3**". L-021-3 confirmed to exist in `spec/behaviors-and-limitations.md:462`.
- [x] CHK024 Are the scope exclusions (C-103 chunked-resend deferred; inbound validation untouched; the GapFill builder is the sole SequenceReset emitter) explicitly bounded? [Boundary, research §D-5] — PASS: research D-5 enumerates all five non-expansions explicitly: (1) GapFill builder is the sole SequenceReset emitter (D-5.1, `123=Y` hardcoded); (2) single production caller `replay_outbound_range_` (D-5.2); (3) inbound validation untouched (D-5.3); (4) C-103 chunked-resend stays deferred (D-5.4); (5) store bytes untouched (D-5.5). spec §Assumptions last para mirrors items 3/4/5.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 24 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **24** |

### SPEC-FIXED items
None.

### DD-DECIDED items
None.

### WAIVED items
None.

Anchors spot-verified:
- `spec/feature-catalogue.md` S-005, S-006, S-010, S-033 — all rows confirmed present.
- `spec/behaviors-and-limitations.md` L-021-3 — confirmed at line 462.
- `[FIX-SL §4.8.2]`, `[FIX-SL §4.8.4]`, `[FIX-SL §4.8.5]` — cited via the catalogue S-rows (S-005/S-006/S-010/S-033) which are the authoritative catalogue rows for those FIX-SL sections.
- Reference-engine source paths (QFcpp `Session.cpp`, QFJ `Session.java`) — recorded in research.md D-1/D-2 with file paths and line numbers; verified 2026-06-14 per research.md header.
