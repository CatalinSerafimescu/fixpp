# Validation-Compat Toggles (CheckCompID & ValidateSequenceNumbers) — Requirements Quality Checklist

**Purpose**: Unit-tests-for-English over the 028 requirements — validate completeness, clarity, consistency, measurability, and coverage of spec/plan/contracts/data-model BEFORE implementation. These items test whether the requirements are WRITTEN correctly, NOT whether the implementation works.
**Created**: 2026-06-09
**Feature**: [spec.md](../spec.md) (session inbound-validation compat-knob domain; G3 slice; net-new S-040/S-041)

## Requirement Completeness

- [x] CHK001 Are both knobs' default AND relaxed behaviour fully specified (`check_comp_id` / `validate_sequence_numbers`, each default `true`=strict; relaxed = skip the per-message `49`/`56` equality / skip too-high+too-low gap checks)? [Completeness, Spec §FR-001/FR-003/FR-004/FR-006] — PASS: FR-001/FR-002/FR-003 (CompID) and FR-004/FR-005/FR-006 (seqnum) fully specify default (strict) and relaxed behaviour; data-model entities table + C1/C2 contracts corroborate. Both defaults `true` stated explicitly in data-model entities, research D-1, plan summary.
- [x] CHK002 Is the relaxed CompID path bounded by an explicit enumeration of what is NOT relaxed (field presence of `49`/`56`, `BeginString(8)`, Logon-time CompID match, 013 authorization allow-list)? [Completeness, Spec §FR-003, Edge Cases] — PASS: FR-003 explicitly carves out all four: `49`/`56` not made optional, `BeginString(8)` unchanged, Logon CompID stays strict (FR-012), 013 authz not bypassed. Edge Cases section repeats all four with the same precision. Contract C1.3 mirrors. I-VCT-1/I-VCT-2 in data-model corroborate.
- [x] CHK003 Is the relaxed sequence path's counter-advance rule stated for every ordering class (exact-match advances; too-high delivered-no-advance; too-low delivered-no-advance)? [Completeness, Spec §FR-006/SC-006] — PASS: FR-006 states the exact-match-only advance rule; I-VCT-4 in data-model closes all three ordering classes; SC-006 provides the counter-state observable. C2.3 in contract corroborates. The too-high (S2 guard removed + falls through) and too-low (S4 else) are both covered as deliver-without-advance.
- [x] CHK004 Are the relaxed-sequence carve-outs that REMAIN strict specified (PossDup(43)/021 handling retained; `seq==0` still fatal; too-low `Heartbeat(35=0)` still silently dropped, not delivered)? [Completeness, Spec §FR-006, Edge Cases] — PASS: FR-006 explicitly retains PossDup handling. Edge Cases sections specify the too-low-Heartbeat silent-drop carve-out (N3). I-VCT-5 (PossDup), I-VCT-10 (seq==0 fatal) in data-model; data-model S4 table row names all three carve-outs explicitly. C2.4/C2.5 in contract corroborate.
- [x] CHK005 Is the `SequenceReset(35=4)` interaction with `validate_sequence_numbers=false` fully specified for BOTH reset-mode (`GapFillFlag≠Y`) and gapfill-mode (`123=Y`)? [Completeness, Spec §FR-013/SC-008] — PASS: FR-013 covers both modes with per-mode/ordering counter-advance rules (reset-mode = unchanged; exact-match gapfill = +1 via ordinary path, NewSeqNo not applied). I-VCT-11 in data-model and C2.7 in contract mirror this. SC-008 requires split per-mode assertions. Gate A round 3 specifically propagated this carve-out to all normative sites.
- [x] CHK006 Is the admin-vs-app delivery fan-out specified for an out-of-order frame on the relaxed path (admin msgtype → `fromAdmin`, app msgtype → `fromApp`)? [Completeness, Spec §FR-006/FR-013] — PASS: FR-006 references the existing dispatch pattern. Data-model S4 explicitly states "discriminate via the existing `is_admin_msgtype` helper (`src/session/msgtype_classifier.hpp:43`): admin msgtype → `fromAdmin`, app msgtype → `fromApp`". C2.2 in contract corroborates. FR-013 specifies `35=4` is delivered to `fromAdmin` (admin msgtype).
- [x] CHK007 Is the byte-identical no-op requirement at default (both knobs `true`) stated as a zero-regression guarantee covering inbound AND outbound paths? [Completeness, Spec §FR-009/SC-003] — PASS: FR-009 states "byte-for-byte identical to current behaviour on every path." FR-008 and Assumption (Inbound-only) close the outbound half. SC-003 ties to an objective full-regression suite gate. I-VCT-7 + I-VCT-9 in data-model corroborate both axes.
- [x] CHK008 Is the inbound-only scope explicit — that neither knob is read on any outbound construction path (our `49`/`56`, our outgoing seqnums)? [Completeness, Spec §FR-008/SC-005, Edge Cases] — PASS: FR-008 ("Both knobs MUST govern inbound validation only; outbound message construction ... MUST be unchanged"). Edge Cases §Outbound-unaffected restates it. C3.2 in contract mirrors. I-VCT-9 in data-model confirms neither knob is read on any outbound construction path.
- [x] CHK009 Is the net-new feature-catalogue obligation specified (each knob surfaces its own row — S-040/S-041 — with normative refs)? [Completeness, Spec §FR-011] — PASS: FR-011 requires two net-new catalogue rows. Assumption §Net-new-catalogue-rows + plan §VI delta name S-040/S-041 explicitly with normative refs (QuickFIX + FIX-SL §4.2.2/§4.8). Task T015 is the implementation task. D-7 in research.md specifies the exact catalogue delta.
- [x] CHK010 Is the ABI/wire/codegen non-impact specified (no new error slot, no new wire field, no codegen-emitter change, no C-ABI surface — two additive C++-only `SessionConfig` value fields)? [Completeness, Spec §FR-010] — PASS: FR-010 enumerates all four non-impacts explicitly. Assumption §No-new-wire-field corroborates. C3.4 in contract states "additive C++-only value-type fields ⇒ struct-layout change requiring a normal source rebuild; no C-ABI surface, no error slot." Plan Article X row confirms.

## Requirement Clarity

- [x] CHK011 Is the inverted default polarity stated unambiguously (strict IS the default ⇒ bool defaults `true`, `false` is the opt-in relaxation — the OPPOSITE of the 021/022/024/026/027 false-default knobs)? [Clarity, Spec §Assumptions] — PASS: Assumption §Default-strict explicitly states "Note the polarity INVERTS versus the false-default additive knobs (021/022/024/026/027): here strict IS the current default, so the bool defaults `true` and `false` is the opt-in relaxation." Research D-1 has identical language. Plan summary "both default `true` (current strict behaviour)" is consistent. No residual false-default language found.
- [x] CHK012 Is "steady-state only" defined precisely against the Logon/establishment boundary (relaxation applies to post-Logon Active traffic; Logon-time CompID match + sequence/establishment + 013/024 `ResetSeqNumFlag(141)` stay strict)? [Clarity, Spec §FR-012, Edge Cases] — PASS: FR-012 is the dedicated requirement for this boundary; it explicitly names the Logon/establishment path, 013/024 ResetSeqNumFlag(141) reset interaction, and characterises this as a deliberate QFJ divergence. Edge Cases §CompID-relaxation-scope and §Sequence-validation-off-vs-Logon repeat the same with concrete references to D-4. Research D-4 provides the authoritative rationale.
- [x] CHK013 Is the distinction between the QuickFIX per-message CompID **equality match** and the 013 CompID **authorization allow-list** drawn sharply enough that "relax CheckCompID" cannot be read as "bypass authz"? [Clarity, Spec §FR-003/SC-004, Edge Cases] — PASS: FR-003 names "013 `compid_authorization_policy`" explicitly as a control that MUST NOT be bypassed. Edge Cases §CompID-relaxation-does-NOT-bypass-security-allow-list explains both mechanisms separately. Assumption §CompID-check-≠-CompID-authorization provides a dedicated paragraph. SC-004 requires a paired negative test.
- [x] CHK014 Is the `SequenceReset(35=4)` "+1" counter outcome for an exact-match gapfill explained as a fixpp ordering artifact (intercept sits after `check_inbound`/S5) and explicitly disclaimed as NOT a QFJ-parity claim? [Clarity, Spec §FR-013/SC-008, Clarifications Gate-A-round-1] — PASS: FR-013 contains "(The +1 is a fixpp ordering artifact — the gapfill intercept sits after `check_inbound` — NOT a QFJ-parity claim; QFJ never `+1`s a reset.)". The Clarifications (Gate A round 1) session has the same disclaimer verbatim. SC-008 includes the per-mode/ordering split so the test separates this case. C2.7 in contract repeats the disclaimer.
- [x] CHK015 Is "NewSeqNo not applied" defined as gating only WHETHER `apply_inbound_sequence_reset` is INVOKED — the shared helper itself unchanged for the strict 013/024/027 paths? [Clarity, Spec §FR-013, Clarifications] — PASS: FR-013 states "The shared sequence-reset application MUST be unchanged for the strict default and the Logon-establishment paths (013/024/027); only whether it is invoked is gated on the knob." Clarifications (Gate A round 1) session: "the shared `apply_inbound_sequence_reset` itself is unchanged ... only whether it is CALLED is gated." I-VCT-11 and data-model S6/S7 rows corroborate.
- [x] CHK016 Is the relaxed too-high behaviour stated as "no `ResendRequest` AND no AwaitingResend entry" (not merely "no disconnect"), so the frame falls through to deliver-without-advance? [Clarity, Spec §FR-006/SC-002] — PASS: FR-006 "a too-high MsgSeqNum MUST NOT trigger a `ResendRequest` / gap-fill recovery." SC-002 "zero `ResendRequest` messages on the wire AND no too-low disconnect." Data-model S2 row: "guarded `&& validate_sequence_numbers`; off ⇒ skip (fall through, no ResendRequest)" — AwaitingResend is named in the plan Complexity Tracking "no AwaitingResend" hazard. C2.2 contract: "MUST NOT emit a ResendRequest or enter AwaitingResend." The AwaitingResend-entry gate is explicit in contracts.
- [x] CHK017 Are the QFJ reference anchors (`Session.java:1543`/`:1550`; `isCorrectCompID`; `if (getExpectedTargetNum()==msgSeqNum) incr…`) cited concretely rather than gestured at, so the parity claim is checkable? [Clarity, Spec §Clarifications/Normative References] — PASS: Spec Clarifications cites `Session.java:1543`/`:1550` and `Session.java:1800`; FR-013 and C2.7 repeat `:1543`/`:1550`. Research D-0 cites `Session.java:1800`, `:1812`, `:1832`/`:1835`, `:1826`, `:2208`, and the exact counter-advance pattern with line numbers. Anchors verified: Session.java:1543 = `isGapFill = sequenceReset.getBoolean(GapFillFlag.FIELD) && validateSequenceNumbers`; :1550 = `if (validateSequenceNumbers && sequenceReset.isSetField(NewSeqNo.FIELD))`. Both resolve in the live QFJ 3.0.1 source.

## Requirement Consistency

- [x] CHK018 Is the default-polarity description consistent across spec/plan/data-model/research (no residual "default false" or "opt-in to enable checking" phrasing that would invert the semantics)? [Consistency, Spec §FR-001/FR-004/Assumptions] — PASS: Swept all four artifacts. FR-001/FR-004 say "defaulting to strict (checking enabled)." Plan summary "both default `true` (current strict behaviour)." Data-model entity table "Default: `true`." Research D-1 "Default `true` = strict = byte-identical no-op." No residual "default false" or "opt-in to enable" phrasing found in any artifact. Polarity inversion is called out consistently as a NOTE in the Assumptions section.
- [x] CHK019 Is "steady-state only / Logon untouched" stated consistently everywhere (no artifact claiming either knob relaxes the Logon-establishment CompID or sequence path)? [Consistency, Spec §FR-012, Edge Cases] — PASS: FR-012 + Edge Cases (2 entries) + Clarifications (Q3 answer) + research D-4 + data-model I-VCT-6 + data-model §Untouched section + plan Summary/Technical Context/Complexity-Tracking + contract C2.6 all consistently state steady-state-only. No artifact claims either knob relaxes Logon establishment. The Logon-path sites (NotConnected :1524, LogonSent :2688) are named as explicitly untouched in data-model and plan.
- [x] CHK020 Is the exact-match-only advance rule consistent between the ordinary-seqnum requirement (FR-006) and the `SequenceReset` requirement (FR-013), including why S6/S7 must ALSO be gated (else `35=4` advances outside the seqnum gate)? [Consistency, Spec §FR-006/FR-013] — PASS: FR-006 states the ordinary-frame exact-match advance. FR-013 closes the `35=4` gap by gating S6/S7. I-VCT-4 explains the bridging: "exact-match advance holds for ALL inbound paths only because S6/S7 are also gated (I-VCT-11)." Research D-3 provides the same explanation. No internal inconsistency between FR-006 and FR-013 found. Tasks notes section repeats this invariant.
- [x] CHK021 Is the knob-independence claim consistent with the four-combination matrix (both-default / CompID-only-off / seqnum-only-off / both-off), with no combination left unspecified? [Consistency, Spec §FR-007/SC-005, Edge Cases] — PASS: FR-007 declares independence. Edge Cases §Knob-independence states "any combination (both default, one relaxed, both relaxed) is valid." SC-005 requires a four-cell combination matrix test. C3.1 in contract mirrors. Research D-5 provides independent guard rationale. No combination is left unspecified; the combinations map to FR-002/003/005/006 on each axis.
- [x] CHK022 Is the MaxLatency exclusion stated consistently as OUT OF SCOPE (already covered by the existing `sending_time_threshold`), with no FR/SC/edge-case implying a latency knob? [Consistency, Spec §Assumptions] — PASS: Input section and Assumption §MaxLatency-is-OUT-OF-SCOPE both explicitly cite `sending_time_threshold`. No FR, SC, or Edge Case mentions latency or MaxLatency as in-scope. Neither contract entry nor research mentions a latency knob. MaxLatency does not appear in the feature's FR/SC/Edge-Cases sections.
- [x] CHK023 Is the "knob relaxes only the equality match, fields still parsed" claim consistent with the BeginString-stays-strict claim (both live in the same combined CompID/BeginString gate)? [Consistency, Spec §FR-003, Edge Cases] — PASS: FR-003 says "MUST NOT make `49`/`56` optional, MUST NOT relax `BeginString(8)` validation." Edge Cases §CompID-relaxation-is-the-match-only states "it does NOT make the header fields optional." The implementation site D-2 (research) and data-model S1 both show the split: BeginString mismatch always disconnects; `49`/`56` clauses gated behind `check_comp_id`. These claims are mutually consistent — the gate is split, not removed. Code verification: `session.cpp:1869-1871` contains the combined gate structure confirmed by surrounding code.

## Acceptance Criteria Quality & Measurability

- [x] CHK024 Is SC-002 measurable against concrete observable signals (a wire-message count asserting ZERO `ResendRequest` AND a session-liveness/Active assertion), not a subjective "tolerates out-of-order"? [Measurability, Spec §SC-002] — PASS: SC-002 explicitly states "zero `ResendRequest` messages on the wire AND no too-low disconnect (verified by an automated test that counts wire messages and asserts session liveness)." Both signals are concrete and automatable: wire-message count is countable by a test harness; session-liveness is an Active-state assertion. The paired strict test is also specified.
- [x] CHK025 Is SC-003 (default byte-identity) tied to an objective gate (the full existing session/establishment/recovery/CompID/sequence suite green + byte-identical wire), not "behaves the same"? [Measurability, Spec §SC-003/FR-009] — PASS: SC-003 says "100% of existing session/establishment/recovery/CompID regression witnesses remain green and behaviour is byte-identical to the pre-feature baseline." "100% of … witnesses remain green" is measurable. FR-009 says "byte-for-byte identical." Task T013 implements the full-regression gate. US3 AS1 specifies the same in Given/When/Then form.
- [x] CHK026 Is SC-006 (exact-match-only advance) stated as an objectively checkable counter-state assertion after an out-of-order-then-in-order frame sequence? [Measurability, Spec §SC-006] — PASS: SC-006 says "the next-expected inbound counter advances only on an exact-expected match (an out-of-order frame is delivered but does not move the counter), verified by an automated test asserting counter state after an out-of-order then in-order frame." The test fixture must assert a specific counter value — objectively checkable. T006 `Seq_KnobOff_ExactMatchOnlyAdvance` is the named test.
- [x] CHK027 Is SC-008's counter-state assertion split per mode/ordering (reset-mode + any out-of-order ⇒ unchanged; exact-match gapfill ⇒ +1), so the test cannot pass with a single blanket assertion? [Measurability, Spec §SC-008] — PASS: SC-008 explicitly splits: "reset-mode and any out-of-order `35=4` ⇒ counter unchanged; an exact-match gapfill `35=4` ⇒ counter advances by one." Three distinct orderings are named, requiring separate counter assertions. T007 in tasks lists three separate test cases (`SeqReset_KnobOff_ResetMode_*`, `SeqReset_KnobOff_GapfillOutOfOrder_*`, `SeqReset_KnobOff_GapfillExactMatch_AdvancesByOne_*`). A single blanket "unchanged" assertion would fail the exact-match-gapfill case.
- [x] CHK028 Is SC-004 (authz allow-list still enforced) expressed as a negative test with an observable outcome (a non-allow-listed principal's Logon still refused with the CompID knob relaxed)? [Measurability, Spec §SC-004] — PASS: SC-004 says "a Logon from a principal not on the allow-list is still refused even with the CompID-check knob relaxed (verified by a negative test)." The observable outcome is a refused/rejected Logon — a binary observable (connection established vs refused/disconnected). T004 `CompID_KnobOff_AuthzAllowListStillEnforced` is the named test.
- [x] CHK029 Does each US acceptance scenario (US1 AC1-3, US2 AC1-3, US3 AC1-2) state a Given/When/Then evaluable from observable wire / delivery / persisted-counter output? [Acceptance Criteria, Spec §US1/US2/US3] — PASS: All eight acceptance scenarios use the Given/When/Then structure. US1 AC1 (relaxed → mismatching message delivered), US1 AC2 (default → rejected), US1 AC3 (matching path unchanged) — observable delivery/disconnect. US2 AC1 (too-high → no ResendRequest), US2 AC2 (too-low → not disconnected), US2 AC3 (default → existing behaviour). US3 AC1 (regression suite green), US3 AC2 (default fields read as strict). All outcomes are observable from wire, delivery callback, or counter/field state.

## Scenario & Edge Case Coverage

- [x] CHK030 Are requirements defined for the relaxed-CompID matching-path (CompIDs DO match with the knob off) so the relaxation provably never changes the matching path? [Coverage, Spec §US1 AC3/FR-003] — PASS: US1 AC3 states "Given the CompID-check knob relaxed, When CompIDs DO match, Then processing is unchanged." T004 `CompID_KnobOff_MatchingPathUnchanged` covers it. The requirement that the relaxation only skips the equality check (not the processing path when they match) is implicit in FR-003 ("skipped for ... messages ... so mismatching-CompID messages are accepted") and made explicit by US1 AC3.
- [x] CHK031 Are the too-low-Heartbeat silent-drop and `seq==0`-fatal carve-outs covered as requirements that survive the relaxed path (i.e. the relaxation does NOT reach them)? [Coverage, Edge Case, Spec §FR-006] — PASS: I-VCT-10 (seq==0 fatal, data-model) and data-model S4 carve-out (too-low Heartbeat silently dropped before the Arm-B else) are both stated. Edge Cases does not list these explicitly in spec.md, but FR-006 retains the PossDup handling and the data-model table names the too-low Heartbeat carve-out as pre-existing and unchanged. C2.4 (PossDup) and C2.5 (seq==0) in contracts cover the requirements. T006 in tasks lists both as named test cases. Sufficient coverage.
- [x] CHK032 Is the Logon-path negative scenario covered for BOTH knobs (mismatching-CompID Logon still refused; too-high Logon still strict) — SC-007's establishment guard? [Coverage, Spec §SC-007/FR-012] — PASS: SC-007 requires both: "a Logon with a mismatching CompID is still refused and Logon-time sequence/reset handling is byte-identical to the strict baseline, verified by a Logon-path negative test." T004 `CompID_KnobOff_LogonTimeMismatchStillRefused` and T006 `Seq_KnobOff_LogonTimeTooHighStillRefused` are both named. FR-012 backs both axes. Both knobs are covered by the establishment guard.
- [x] CHK033 Is the no-new-allocation / no-new-suspension expectation on the relaxed deliver-without-advance path stated as a requirement (zero-alloc hot-path discipline)? [Coverage, Spec §FR-009, plan §IX] — DD-DECIDED §plan-VIII.5/D-6: The no-new-allocation constraint on the relaxed path is settled in the signed-off plan (Article VIII.5: "relaxed paths reuse the existing no-heap `parse_and_dispatch_` / builders; no new allocation. No-heap witness on the relaxed delivery path" — planned and witnessed) and research D-6 ("no new alloc/suspension"). FR-009 closes the default-path zero-regression. The obligation flows from [const §VIII.5] which is a constitution constraint, not a spec FR; it is plan-level design decision and witnessed by T012 `NoHeap_RelaxedDeliverPath`. No spec.md FR change required.
- [x] CHK034 Is the cross-engine live-interop scenario (fixpp vs a QFJ/QFcpp peer configured `CheckCompID=N`/`ValidateSequenceNumbers=N`, both roles) identified as in-scope coverage rather than unit-only? [Coverage, Spec §SC-001/SC-002, Gap] — PASS: Plan [const §VII.6] row explicitly lists the live both-role interop cell. SC-001/SC-002 reference "verified by an automated test" (the unit tests), and the plan adds the live cell separately. T002 (interop test skeleton) and T014 (live interop cell) are defined tasks. Research D-8 specifies the interop cell design. The feature is confirmed as both-role, cross-engine, skip-without-counterparty.

## Dependencies & Assumptions

- [x] CHK035 Is the dependence on the 013 `compid_authorization_policy` as an independent, still-active control documented as an explicit assumption (CompID-check ≠ CompID-authorization)? [Assumption, Spec §Assumptions/Key Entities] — PASS: Assumption §CompID-check-≠-CompID-authorization is a dedicated paragraph. Key Entities section lists "CompID authorization allow-list (013)" as a separate entity with the text "the CompID-check knob MUST NOT bypass." FR-003 repeats the prohibition with the specific symbol name. Research D-2 explains the separate sites (`:1678` vs `:1869`). The assumption is explicit, named, and cross-referenced.
- [x] CHK036 Is the QuickFIX-compat parity assumption documented WITH its two deliberate QFJ divergences (steady-state-only scope; authz allow-list not bypassed), so "QFJ-parity" is not over-claimed? [Assumption, Spec §Assumptions] — PASS: Assumption §QuickFIX-compat-semantics explicitly states "with two deliberate divergences from QFJ: (a) scope is steady-state only (Logon establishment stays strict — FR-012), and (b) the CompID-check knob does NOT bypass fixpp's 013 authorization allow-list (QFJ has no analogue)." Research D-4 provides the rationale for divergence (a). Research D-0 establishes the reference sweep basis. "QFJ-parity" is used selectively in FR-013 for the NewSeqNo-not-applied claim and in FR-006 for the counter advance, each scoped precisely.
- [x] CHK037 Is the dependence on the 021 PossDup disposition (`redeliver_poss_dup`) as a retained, untouched path documented so a future PossDup change cannot silently regress the relaxed path? [Dependency, Spec §FR-006, Edge Cases] — PASS: FR-006 states "The PossDup(43)-flagged duplicate handling (021 `redeliver_poss_dup`) MUST still run — only the too-high/too-low gap checks are skipped." Edge Cases §Sequence-validation-off-vs-PossDup names the 021 feature by ID and specifies the boundary. Clarifications Q4 answer records the explicit decision. I-VCT-5 in data-model designates S3 (PossDup Stage-1/Stage-2) as "unchanged." The dependency on 021 is documented as a retention requirement; a future removal of Stage-1/Stage-2 would conflict with this FR text and be detectable. Note: the item does not request a full forward-compatibility guard (e.g. a task to audit 021 changes); the requirement text + the UNCHANGED designation in data-model provides the necessary traceability.

## Notes

- Check items off as completed during the step-9 checklist audit: `[x]` with an inline `— PASS:`/`— SPEC-FIXED:`/`— DD-DECIDED §X:`/`— WAIVED:<reason>` disposition citing the design-doc anchor.
- This checklist tests REQUIREMENT QUALITY (English), not implementation behaviour — paired witnesses live in tasks.md (T004/T006/T007/T012/T014).
- ≥80% of items carry a `[Spec §…]` / `[Gap]` / `[Assumption]` / `[Dependency]` traceability reference.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 36 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 1 |
| WAIVED | 0 |
| **Total** | **37** |

### SPEC-FIXED items

None.

### DD-DECIDED items

- CHK033 — anchor `plan §VIII.5 + research D-6`; rationale: the no-new-allocation constraint on the relaxed deliver-without-advance path is a constitution [const §VIII.5] design decision settled in the signed-off plan (plan Article VIII.5 row explicitly planned and witnessed by T012 `NoHeap_RelaxedDeliverPath`) and research D-6; it does not require a normative FR in spec.md — the obligation flows from [const §VIII.5] and is plan-level.

### WAIVED items

None.

Anchors spot-verified:
- `Session.java:1543` (QFJ 3.0.1) — resolves to `isGapFill = sequenceReset.getBoolean(GapFillFlag.FIELD) && validateSequenceNumbers` — VERIFIED.
- `Session.java:1550` (QFJ 3.0.1) — resolves to `if (validateSequenceNumbers && sequenceReset.isSetField(NewSeqNo.FIELD))` — VERIFIED.
- `session.cpp:1869-1871` (fixpp) — resolves to the combined `begin_string`/`sender_comp_id`/`target_comp_id` gate (S1) — VERIFIED by surrounding code.
- `session.cpp:1966` (fixpp) — resolves to the reset-mode `SequenceReset(35=4)` intercept region (S6) — VERIFIED by surrounding comment "Inbound SequenceReset(35=4) — Reset mode".
- `session.cpp:2017` (fixpp) — resolves to `if (seq == 0) { record_state_transition_(fsm_state::Disconnected); co_return ... }` (I-VCT-10 seq==0 fatal) — VERIFIED.
- `session.cpp:2232` (fixpp) — resolves to `auto chk = co_await seqnum_mgr_.check_inbound(seq);` (S5 exact-match advance) — VERIFIED.
- `session.cpp:2234` (fixpp) — resolves to `if (hdr.msg_type == "0") { ... co_return ... }` inside the `!chk` arm (too-low Heartbeat silent drop) — VERIFIED.
- `session.cpp:2295` (fixpp) — resolves to gapfill-mode `SequenceReset(35=4)` intercept `apply_inbound_sequence_reset` call (S7) — VERIFIED by surrounding comment.
- `src/session/msgtype_classifier.hpp:43` — resolves to `[[nodiscard]] constexpr bool is_admin_msgtype(std::string_view msg_type) noexcept` — VERIFIED.
- `SessionConfig` copy-constructible `static_assert` — resolves at `include/fixpp/session/session_config.hpp:400` — VERIFIED.

All anchors resolve in the signed-off Gate A round 3 artifacts (plan.md, research.md, data-model.md, spec.md, contracts/validation-compat-toggles.md) as of 2026-06-07.
