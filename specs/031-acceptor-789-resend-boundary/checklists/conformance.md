# Requirements-Quality Checklist: Conformance — 031 acceptor 789 resend-range boundary

**Purpose**: Unit-test the *requirements writing* of the acceptor-789 resend-range boundary fix before `/speckit-implement` — completeness, clarity, consistency, measurability, coverage of the FRs/SCs (NOT implementation verification).
**Created**: 2026-06-11
**Focus**: conformance-correctness (acceptor 789-honor pre-reply comparison). Depth: standard. Audience: reviewer (pre-implement gate).
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md)

## Requirement Completeness

- [x] CHK001 Is the corrected comparison reference (pre-reply next-outbound `N_pre`) specified for **all three** honor comparisons (too-high / behind / in-sync), not just the confirmed in-sync case? [Completeness, Spec §FR-001/§Clarifications] — PASS: FR-001 explicitly states "MUST evaluate **all** of its honor comparisons (too-high, behind, in-sync) against the **pre-reply** next-outbound sequence number `N_pre`"; the §Clarifications (2026-06-10) records the user decision as "Comprehensive — capture `N_pre` once … and use it for **all three** comparisons"; contracts/honor-next-expected.md C-031 applies `next_outbound_ref` to all four pre-conditions; research.md R6 para 1 enumerates the same.
- [x] CHK002 Is the in-sync case (`X == N_pre`) explicitly required to emit **no** `SequenceReset` and **no** `ResendRequest` after the reply Logon? [Completeness, Spec §FR-002] — PASS: FR-002 states "MUST treat the session as in-sync and emit **no** `SequenceReset` and **no** `ResendRequest` after the reply Logon"; SC-001 quantifies "zero `SequenceReset` and zero `ResendRequest` frames are emitted by fixpp after the reply Logon"; SC-003 adds "fixpp emits no frame after the reply Logon"; US1 acceptance scenario 1 restates the same.
- [x] CHK003 Is the genuine-gap (`X < N_pre`) resend range specified as **unchanged from 027** (range endpoint deferred to research.md, only the decision-to-resend boundary corrected)? [Completeness, Spec §FR-003] — PASS: FR-003 states "the resend range begins at `X`; its upper endpoint … is NOT changed from the already-shipped `027` genuine-gap range (only the *decision to resend* and its lower-bound guard are corrected)"; research.md R4 resolves the endpoint as `[X, N_pre]` and confirms "The genuine-gap RANGE is already correct"; contracts INV-NEX-RANGE pins `[X, peek_outbound()-1]` = `[X, N_pre]` as unchanged; tasks.md T007 Gate-A directive #3 enforces range stays `peek_outbound()-1`.
- [x] CHK004 Is the too-high arm (`X > N_pre`) specified to preserve the existing 027 Logout+disconnect, with the boundary now at `N_pre` (so `X == N_pre+1` in the initial Logon is too-high, not in-sync)? [Completeness, Spec §FR-005/§Edge Cases] — PASS: FR-005 states "MUST emit a `Logout` … preserving the existing `027` sequence-integrity arm, with the too-high boundary evaluated against `N_pre` (so a peer advertising exactly `N_pre + 1` … is Logout-and-disconnect, not silently treated as in-sync)"; §Edge Cases repeats this explicitly; research.md R7 item 2 / Gate-A directive confirms the QFcpp `:230`/QFJ `:2255` oracle for this boundary.
- [x] CHK005 Is the invalid-789 (`X == 0`) Logout arm explicitly stated to be unchanged? [Completeness, Spec §FR-006] — PASS: FR-006 states "MUST continue to emit a `Logout` ('NextExpectedMsgSeqNum invalid') … preserving the existing `027` invalid-789 arm; an invalid value MUST NOT be treated as a genuine 'behind' value"; data-model.md row `X == 0` annotated "unchanged".
- [x] CHK006 Is the initiator-role honor path explicitly required to be byte-identical (a non-regression constraint, not a change target), and is the *reason* (peer reply `789 = target+1` already matches post-own-Logon outbound) documented? [Completeness, Spec §FR-008/§Edge Cases] — PASS: FR-008 states "MUST NOT change the observable behavior of fixpp acting as an **initiator**"; §Edge Cases para "Initiator role" explains the reason ("peer reply `789 = target+1` already accounts for fixpp's Logon → `X == peek_outbound()` ⇒ in-sync"); research.md R5 provides the full role-asymmetry rationale; contracts INV-NEX-INIT pins "byte-identical to 027".
- [x] CHK007 Is the blast-radius pin set enumerated (the existing `XeqN_NoResend` acceptor pin that flips to Logout, and the absence of any dedicated 789-`X>N` acceptor pin)? [Completeness, research.md R7, Tasks T002/T004] — PASS: research.md R7 item 1 identifies `TEST(Honor, XeqN_NoResend)` acceptor arm at `:776-817` as flipping to Logout; R7 item 2 confirms "no dedicated acceptor 789-`X>N` (too-high) pin today"; tasks T002 encodes the split directive; tasks T004 creates the first dedicated acceptor too-high pin; plan.md Gate-A confirms R7 as reviewed.
- [x] CHK008 Are the §VI catalogue / coverage-index / B&L deltas specified (which row to amend, which new B- entry) rather than left "TBD at polish"? [Completeness, Plan §Exact §VI delta, Tasks T013-T015] — PASS: plan.md §"Exact §VI delta" itemizes exactly: (1) amend S-031 row in `spec/feature-catalogue.md`; (2) map branches in `spec/coverage-index.md` under existing 027 entry; (3) add B-031-1 in `spec/behaviors-and-limitations.md`; tasks T013/T014/T015 encode each as separate Polish tasks; no TBD text found in spec bundle.

## Requirement Clarity

- [x] CHK009 Is the FR-004/SC-003 invariant stated precisely as "no **newly originated** frame re-uses a consumed seqnum" (in-sync emits no post-reply frame), and is universal strict-monotonicity explicitly **disclaimed** (genuine-gap PossDup/GapFill legitimately carries historical seqnums)? [Clarity, Spec §FR-004/§SC-003] — PASS: FR-004 explicitly scopes to "newly originated frame … whose sequence number equals a sequence number already consumed" and explicitly exempts "Legitimate retransmissions on the genuine-gap path … carry their historical sequence numbers as PossDup / GapFill by FIX resend semantics and are expressly **exempt** from any 'strictly increasing' reading"; SC-003 repeats the same scoping; US2 acceptance scenario 2 also adds "retransmitted frames legitimately carry their historical sequence numbers … is required FIX resend behavior and is not a monotonicity violation"; the Gate-A R1 P1 fix in plan.md records this was a confirmed Gate-A correction.
- [x] CHK010 Is the comparison reference vs the resend range distinguished unambiguously — comparisons use `N_pre` (`next_outbound_ref`), but the range keeps live `peek_outbound()-1` (NOT `next_outbound_ref-1`)? [Clarity, Plan Technical Context, contracts/honor-next-expected.md INV-NEX-RANGE] — PASS: contracts/honor-next-expected.md §Semantics explicitly states "The three comparisons use `next_outbound_ref`; the resend range still reads the live `seqnum_mgr_.peek_outbound()`"; INV-NEX-RANGE states "genuine-gap resend range is `[X, peek_outbound()-1]` = `[X, N_pre]` … unchanged"; tasks T007 Gate-A directive #3 explicitly forbids `next_outbound_ref-1` ("MUST read the **live** `seqnum_mgr_.peek_outbound()-1`"); live code at `:4591` currently uses `n789 - 1U` (equivalent to `peek_outbound()-1` pre-fix; T007 instructs the change to explicit `peek_outbound()-1` post-fix). Plan Technical Context also draws the same distinction. No ambiguity remains.
- [x] CHK011 Is the comprehensive scope (per /clarify) stated clearly enough that an implementer corrects the too-high boundary too, not only the in-sync guard? [Clarity, Spec §Clarifications/§FR-005] — PASS: §Clarifications opens with "Comprehensive — capture `N_pre` once … and use it for **all three** comparisons, matching QuickFIX" and explicitly states "This also corrects the too-high boundary (a peer advertising exactly `N_pre + 1` … is genuinely too-high — it claims a message fixpp has not sent — so it must Logout, not be treated as in-sync)"; FR-005 quantifies the boundary; §Edge Cases repeats it; tasks T004 creates W3 (the too-high pin). An implementer fixing only in-sync would fail T004.
- [x] CHK012 Is the acceptor capture-placement requirement (`n_pre` captured BEFORE the reply Logon `store_then_emit` at `:2015`, passed at the honor call `:2027`) stated clearly enough that an implementer cannot satisfy a counter while leaving the comparison reading the post-reply value? [Clarity, Plan Technical Context, contracts §C-031-CS, Tasks T008] — PASS: contracts §C-031-CS "Caller obligations" table states for the acceptor: "MUST pass `next_outbound_ref` = the **pre-reply** outbound — capture `const seqnum_t n_pre = seqnum_mgr_.peek_outbound();` **before** the reply Logon `store_then_emit` (`:2015`)"; plan.md Gate-A directive #4 restates: "capture `const seqnum_t n_pre = seqnum_mgr_.peek_outbound();` **lexically ABOVE** the reply Logon `store_then_emit` (`:2015`)"; tasks T008 is dedicated entirely to this requirement with the same directive. Live code confirms `:2015` is `store_then_emit` and `:2027` is the honor call — capture must be between them.

## Requirement Consistency

- [x] CHK013 Do spec / plan / research / contract agree on the fix shape (comparison threshold corrected to pre-reply `N_pre`; range and initiator byte-identical) with no surviving "range changes" or "initiator changes" wording? [Consistency, Spec §FR-003/§FR-008, research.md R4/R5] — PASS: All four artifacts agree on the fix shape. Grep across spec/plan/research/contracts found no "range changes" framing — every occurrence of "range" in requirements context states "unchanged" or "already correct". FR-003 explicitly says range is "NOT changed from the already-shipped 027"; FR-008 says initiator is "byte-identical"; research.md R4 concludes "The genuine-gap RANGE is already correct"; contracts INV-NEX-RANGE and INV-NEX-INIT both pin unchanged semantics. The "post-reply" references in the bundle fall into three categories, none implying a range or initiator change: (a) the defect description ("today fixpp compares against the post-reply value" — explains the bug); (b) T016's grep-sweep targets (implement-time cleanup of stale code comments); (c) correct range semantics — FR-003 ("the upper endpoint … MUST match the reference-engine semantics for the post-reply outbound counter") and contracts §INV-NEX-RANGE ("post-reply = `[X, N_pre]`") correctly describe the live `peek_outbound()-1` counter after the reply, which equals `N_pre` — these are accurate range-semantics statements, not comparison-threshold framing.
- [x] CHK014 Is terminology consistent (`N_pre`, `N_post`, `next_outbound_ref`, "pre-reply / post-reply outbound") across all artifacts? [Consistency] — PASS: Terminology is used consistently. `N_pre` = pre-reply next-outbound, `N_post = N_pre + 1` = post-reply, `next_outbound_ref` = the new parameter name — all four terms appear with the same meaning in spec.md (Overview, FR-001), plan.md (Summary, Technical Context), research.md (R1, R5, R6), data-model.md (Values table), and contracts/honor-next-expected.md (Signature, C-031-CS). No cross-artifact terminology conflict found.
- [x] CHK015 Does the post-Gate-A spec (FR-004/SC-003 scoped to newly-originated frames) agree with tasks.md (T003 asserts no post-reply `34==N_pre`, not universal monotonicity) and plan.md (W1 wording)? [Consistency, Spec §FR-004 / Tasks T003 / Plan Testing] — PASS: All three are consistent. FR-004 scopes to "newly originated frame" re-using a consumed seqnum (not universal strict-monotonicity). Tasks T003 W1 asserts: "(b) inspecting only frames AFTER the reply, **zero** `35=4`, **zero** `35=2`, **zero** `43=Y`, and **no** further frame at `34==N_pre`" — this matches the "newly originated duplicate" framing exactly and does not assert universal strict-monotonicity. Plan.md Gate-A W1 wording matches: "reply Logon WAS emitted (`34==N_pre`) AND zero post-reply `35=4`/`35=2`/`43=Y` AND no further `34==N_pre`". The Gate-A sign-off in plan.md notes the P1 fix was scoping FR-004/SC-003/US2 to newly-originated frames — all downstream artifacts reflect this correction.

## Acceptance-Criteria Quality (Measurability)

- [x] CHK016 Is the W1 in-sync witness a discriminating triple (reply Logon emitted at `34==N_pre` AND zero post-reply `35=4`/`35=2`/`43=Y` AND no further `34==N_pre`) such that no weaker `drive_to_active` proxy and no mis-placed-capture no-op passes? [Measurability, research.md R8 W1, Tasks T003] — PASS: Tasks T003 specifies all three discriminating legs: "(a) the reply Logon WAS emitted at `34==N_pre`; (b) inspecting only frames AFTER the reply, **zero** `35=4`, **zero** `35=2`, **zero** `43=Y`, and **no** further frame at `34==N_pre`; (c) state Active"; research.md R8 W1 adds the same; plan.md Gate-A directive #2 mandates RED-verification on unmodified `main` before the fix (catches the no-op capture-placement). A mis-placed capture (post-reply) would still let the spurious GapFill through, failing (b). A `drive_to_active` proxy without frame inspection would miss (b). The triple is genuinely discriminating.
- [x] CHK017 Is the W2 genuine-gap witness specified to pin the resend **endpoint** so the directive-#3 off-by-one (`next_outbound_ref-1`) is caught (RED) if introduced? [Measurability, research.md R8 W2, Tasks T010] — SPEC-FIXED (orchestrator, 2026-06-11): the auditor's structural finding is **verified at source** — `session.cpp:4419-4420` forces `eff_end = our_last` unconditionally when `end_is_through_current=true` (the 789-honor call at `:4591` always passes it), and the trailing GapFill reads `peek_outbound()` directly (`:4430`), so `requested_end` (`next_outbound_ref-1` vs `peek_outbound()-1`) is **behaviorally inert** and no unit test can discriminate it. This also means Gate-A New-P2 over-stated the risk (the through-current resend never includes `N_pre` regardless). **Resolution:** amended **T010** (removed the false "RED's here" discriminator claim; reframed W2 as non-regression of the actual resend output `[X, our_last]` + GapFill-through-current) and **T007** + **research.md R8 W2** (directive #3 reframed as contract-fidelity/clarity — write `peek_outbound()-1U` to match INV-NEX-RANGE and stay correct if `end_is_through_current` ever flips — enforced by code review of the one-line change + the live cell T017, NOT a unit RED). No FR/SC text changed, so the coverage table in /speckit-analyze is unaffected (T010 still covers FR-003/SC-002). The endpoint correctness is genuinely enforced by T017 live interop (the peer counts what it received) + T007 code review.
- [x] CHK018 Is the W3 too-high witness objectively stated (`789 = N_pre+1` ⇒ Logout with "expecting N_pre … received X" + Disconnected)? [Measurability, research.md R8 W3, Tasks T004] — PASS: Tasks T004 states "acceptor, seed `N_pre=4`, feed peer initial Logon `789=5` (`==N_pre+1==N_post`). Assert a `Logout` (35=5) with text 'expecting 4 ... received 5' then Disconnected (NOT Active)"; research.md R8 W3 states "peer `789 = N_pre + 1` (initial Logon) ⇒ Logout + disconnect"; the logout text is mechanically verifiable (exact expected/received values). The test both confirms the Logout is emitted and rejects the erroneous Active outcome.
- [x] CHK019 Is the live close-out (SC-004) measurable (session reaches Active, zero fixpp SequenceReset/ResendRequest, peer does NOT Logout-reject, vs BOTH QFcpp and QFJ; witness hardened past `drive_to_active`)? [Measurability, Spec §SC-004, Tasks T017] — PASS: SC-004 specifies "the live `027` SC-005 acceptor interop cell (`NE-*-acc`) against a running QuickFIX-cpp / QuickFIX-J counterparty … establishes and the counterparty does **not** reject with a 'MsgSeqNum too low' `Logout` — verified by the live interop harness wire transcript"; tasks T017 adds "Harden the witness past `drive_to_active` (stay-Active / `recent_events` discriminator, mirroring 030 RC#2)" and "Skip-guard for CI without a counterparty". Both engines named, both conditions stated, witness hardening explicitly required.

## Scenario & Edge-Case Coverage

- [x] CHK020 Is the knob-disabled (default) path explicitly required to be byte-identical to the pre-feature baseline? [Coverage, Spec §FR-008/§Edge Cases] — PASS: FR-008 explicitly states "MUST NOT change the observable behavior … nor the default knob-off path (byte-identical to the pre-feature baseline)"; §Edge Cases para "Knob disabled (default)" confirms "behavior is byte-identical to the pre-feature baseline — unchanged by this fix"; SC-005 quantifies "default (knob-off) path … byte-identical to the pre-fix baseline — verified by the existing test suite plus a byte-identity check"; tasks T012 covers regression confirmation.
- [x] CHK021 Is the behind-side tolerance (027 "formulation A") path required to still compute its resend range against the correct pre-reply outbound (not silently broken by the boundary shift)? [Coverage, Spec §Edge Cases] — PASS: §Edge Cases para "Behind-side tolerance path" states "the honor of the peer's `789` must still compute its resend range against the correct pre-reply next-outbound"; FR-003 together with the range-unchanged constraint (research.md R4 / contracts INV-NEX-RANGE) ensures this — the fix only changes the comparison threshold; the range computation (`peek_outbound()-1`) reads the live counter which is the same value regardless of whether formulation-A was triggered.
- [x] CHK022 Is the initiator non-regression covered by both a unit witness and the live `NE-*-init` cells? [Coverage, research.md R8 W4, Tasks T011] — PASS: research.md R8 W4 specifies "`NE-*-init` unit + live ⇒ unchanged (in-sync, no resend)"; tasks T011 explicitly covers "Confirm `TEST(Honor, Initiator_XltN_ResendsExactRange_NoResendRequest)` (`:716`) + the initiator arm of `XeqN` (kept from T002) stay GREEN (W4 initiator non-regression)" and cross-references FR-008. Live code confirms the initiator call site at `:3322` uses the same 2-param call currently; post-fix it will pass `peek_outbound()` unchanged. The `NE-*-init` live cells are referenced in plan.md §VII.6 interop row.

## Dependencies & Assumptions

- [x] CHK023 Is the conformance oracle (QFcpp initial-Logon `789=getExpectedTargetNum()` no +1; reply `+1`; decision/range pre-reply at `getExpectedSenderNum()`; QFJ equivalent) grounded in source line cites, not asserted? [Assumption, Spec §Normative References, research.md R2/R3] — PASS: research.md R2 gives QFcpp cites `:228-229` (behind check pre-reply), `:230-238` (too-high pre-reply), `:244` (generateLogon reply), `:687` (initial Logon no `+1`), `:709-710` (reply `+1`), `:273-292` (range post-reply = `[X, N_pre]`); research.md R3 gives QFJ cites `:2244-2263` (too-high check pre-reply), `:2250` (actualNextNum pre-reply), `:2278` (generateLogon reply), `:2308-2336` (resend decision/range), `:2312-2313` (endSeqNo), `:2334` (resendMessages); contracts/honor-next-expected.md §Conformance oracle repeats the key cites. All cites verified against live reference-engine source (`/home/catalin/Work/Programming/Antreprenoriat/reference-engines/`): QFcpp `Session.cpp:228-230` confirmed pre-reply comparison, `:687` confirmed no-`+1` initial, `:709-710` confirmed `+1` reply, `:277` confirmed `endSeqNo = getExpectedSenderNum()-1` post-reply = `N_pre`; QFJ `Session.java:2250` confirmed pre-reply snapshot, `:2312-2313` confirmed `nextSenderMsgNumAtLogonReceived`, `:2334` confirmed `resendMessages`. Oracle is source-grounded.
- [x] CHK024 Is it stated that NO new wire field / error slot / codegen / C-ABI / config knob / store interface is introduced (the fix stays within the existing 027 surface)? [Assumption, Spec §FR-009, Plan Constitution Check] — PASS: FR-009 explicitly states "MUST introduce **no** new wire field, error code/slot, generated-code change, or C-ABI surface; it is a pure correction of an existing internal boundary computation"; plan.md Constitution Check row "ABI" states "no C-ABI/error-slot/wire change; internal honor signature gains a param (source rebuild only)"; row "No-implicit-default" confirms no new config flag; row "Testing/TDD" confirms no new codegen; research.md R7 states "No new wire field, error slot, codegen, or C-ABI (FR-009)"; tasks T018 includes "FR-009 explicit check: confirm no new C-ABI symbol / error slot / codegen output delta (abidiff clean)".

## Ambiguities & Conflicts

- [x] CHK025 Does any requirement or doc still frame the acceptor honor as comparing against the **post-reply** outbound, or imply the in-sync case resends (the root-cause framing the fix removes)? [Conflict, Spec §Overview/§FR-001, Tasks T016 grep-sweep] — PASS: No surviving requirements-level post-reply framing found for the **comparison**. "Post-reply" occurrences in the bundle fall into three categories: (a) the defect description ("today fixpp compares against the post-reply value" — explains the bug); (b) T016's grep-sweep target list (implement-time cleanup of source comments, not requirements framing); (c) correct range semantics — FR-003 ("the upper endpoint … MUST match the reference-engine semantics for the post-reply outbound counter") and contracts §INV-NEX-RANGE ("post-reply = `[X, N_pre]`") accurately describe the resend range endpoint using the live `peek_outbound()-1` counter (which IS post-reply but equals `N_pre`). Category (c) is correct-and-intended (range reads live counter; only the comparison threshold must be pre-reply). FR-001 states the comparison in the corrected form ("pre-reply next-outbound"); no FR/SC/US/Edge Case implies the in-sync case resends. The root-cause framing is cleanly separated from the requirement. Tasks T016 captures the exhaustive grep-sweep to clean up code comments post-implement.
- [x] CHK026 Are there any remaining placeholders, "TBD", or stale source/test line cites (`:2015`/`:2027`/`:3322`/`:4509`/`:4512`/`:4587`/`:4591`/`:776`/`:643`/`:716`) in the requirements? [Ambiguity, Spec/Plan/Tasks] — PASS: No TBD/TODO/placeholder text found anywhere in the spec bundle (grep confirmed). All cited line numbers verified against live files: `:4509` (honor fn start — confirmed), `:4512` (n789 = peek_outbound — confirmed), `:4513` (x789==0 guard — confirmed), `:4544` (x789>n789 — confirmed), `:4555` (n789 in text format — confirmed), `:4587` (x789<n789 — confirmed), `:4591` (replay_outbound_range_ — confirmed, uses `n789-1U` pre-fix), `:4597` (in-sync fall-through — confirmed), `:2015` (store_then_emit reply Logon — confirmed), `:2027` (honor call — confirmed), `:3322` (initiator honor call — confirmed), `:776` (TEST XeqN_NoResend — confirmed), `:643` (TEST Acceptor_XltN_ResendsExactRange — confirmed), `:716` (TEST Initiator_XltN — confirmed). All cites resolve correctly in the pre-implement codebase.

## Notes

- This checklist tests the *requirements*, not the code; it is the `/speckit-checklist` artifact that `/speckit-checklist-audit` (mandatory, pipeline step 9) dispositions before `/speckit-implement` is unblocked.
- The companion `requirements.md` is the `/specify` spec-quality checklist; this `conformance.md` adds the domain-specific (acceptor-789-conformance) dimension.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 25 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **ESCALATED** | **0** |
| **Total** | **26** |

**Verdict: GREEN** (after orchestrator resolution of the one escalation — CHK017 SPEC-FIXED 2026-06-11). All 26 items dispositioned; zero un-dispositioned boxes; no open unwaivable gap. Pipeline step 10 (/speckit-implement) unblocked. The SPEC-FIXED edit touched only test-strength descriptors in `tasks.md` (T010/T007) + `research.md` R8 W2 — no FR/SC text changed, so `/speckit-analyze` need NOT be re-run (the requirement↔task coverage table is unchanged).

### SPEC-FIXED items

- **CHK017** (orchestrator, 2026-06-11) — the auditor's escalation (W2 endpoint-pin structurally undetectable) was source-verified (`session.cpp:4419-4420`: `end_is_through_current=true` forces `eff_end=our_last`, the `requested_end` arg is inert). Resolved by amending T010 (removed the false unit-RED discriminator claim; reframed W2 as non-regression of the actual resend output), T007 + research.md R8 W2 (directive #3 reframed as contract-fidelity, enforced by code review + live cell T017, not a unit RED). Chose option (i) — no redesign; the live cell T017 + the one-line T007 code review are the correct enforcement for a behaviorally-inert endpoint arg.

### DD-DECIDED items

None.

### WAIVED items

None.

### ESCALATED items

- CHK017 — RESOLVED by the orchestrator as **SPEC-FIXED** (see SPEC-FIXED items above + the CHK017 inline disposition). The escalation was correct and source-verified; the chosen path is option (i) — the directive-#3 endpoint arg is behaviorally inert (`end_is_through_current=true` ⇒ `eff_end=our_last`), so it needs no unit discriminator; correctness is enforced by T017 live interop + the one-line T007 code review. T010/T007/research-R8-W2 amended to remove the false claim. No open escalation remains.

Anchors spot-verified: `spec.md` (Gate-A-converged 2026-06-11) — all §FR/§SC/§Clarifications/§Normative References sections resolve; `research.md R2/R3` QFcpp/QFJ source cites verified against live reference-engine source at `/home/catalin/Work/Programming/Antreprenoriat/reference-engines/` (QFcpp `Session.cpp:228/230/244/687/709-710/277`, QFJ `Session.java:2250/2278/2312-2313/2334`) — all resolve in their respective engines. Source/test line cites (`:4509/:4512/:4513/:4544/:4555/:4587/:4591/:4597/:2015/:2027/:3322` in `session.cpp`; `:776/:643/:716` in `test_next_expected_msgseqnum.cpp`) verified against live library code — all resolve in the pre-implement codebase. No dangling refs found.
