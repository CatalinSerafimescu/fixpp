# Interop Release-Gate Requirements Checklist: Interop Harness (Session-Layer Interop Gate)

**Purpose**: Validate the *requirements-writing quality* of the 016 interop-gate surface (completeness / clarity / consistency / measurability / coverage) before `/speckit-implement`. This is a "unit test for the requirements" — it asks whether the spec/plan/contracts are well-written, NOT whether code works.
**Created**: 2026-06-01
**Feature**: [spec.md](../spec.md) · contracts: [parent-harness-gate-contract](../contracts/parent-harness-gate-contract.md), [scenario-descriptor](../contracts/scenario-descriptor.md), [parity-disposition](../contracts/parity-disposition.md), [golden-transcript-format](../contracts/golden-transcript-format.md)
**Depth / audience**: Formal v1.0-GA release gate; reviewer (Gate A/B) + implementer.

## Gate Semantics & Taxonomy

- [x] CHK001 Is the distinction between `status: skip:<reason>` (FR-023 counterparty-unavailable) and `matrix_disposition: deferred:<tag>` (by-design scope) specified unambiguously enough that the two can never be conflated? [Clarity, Spec §FR-023 / gate-contract] — PASS: `parent-harness-gate-contract.md` §"skip vs deferred — strict disambiguation" draws the exact line verbatim: `skip:` = FR-023 counterparty-unavailable (transient/environment); `deferred:*` = by-design scope with `status: n/a`. tasks.md Note block repeats the invariant. No ambiguity surface.
- [x] CHK002 Are the by-design deferral tags enumerated as a *closed set* (`deferred:fixt-routing` / `deferred:app-messages` / `deferred:fix8-revisit` / `deferred:v1.1-mtls`)? [Completeness, gate-contract / data-model E2] — PASS: gate-contract result schema lists exactly these four tags as a comment in the `matrix_disposition` field; data-model E2 disposition-tag table matches. Closed set is exhaustively stated in both artifacts; no open-ended "other" entry.
- [x] CHK003 Is the per-cell completeness rule (every matrix/corpus cell — including each `deferred:*` — carries a `cell_result` row) stated as a hard requirement, with a *missing* row defined as a contract violation distinct from a *present-deferred* row? [Completeness, gate-contract] — PASS: gate-contract §"Per-cell completeness rule" states this verbatim ("A missing row is a contract violation … distinct from a present deferred row"). Gate A round-2 added this exact clause per plan.md Gate A §round-2.
- [x] CHK004 Is the `non_deferred_green_pct` denominator (`matrix_disposition == live` cells ONLY) specified precisely so deferred rows can neither dilute nor inflate the green %? [Measurability, Spec §SC-001 / gate-contract] — PASS: gate-contract evaluator rule states "`non_deferred_green_pct` is computed across `matrix_disposition == live` cells **only**; `deferred:*` rows are always excluded from the denominator." SC-001 cites this same filter. Unambiguous.
- [x] CHK005 Is the tier-aware `skip:` rule specified for BOTH tiers with no tier left undefined (release-prep skip of a required live cell ⇒ blocking + `badge_eligible:false`; smoke skip ⇒ non-blocking)? [Consistency, gate-contract / Spec §FR-022/FR-023] — PASS: gate-contract §"Evaluator rule (tier-aware; over non-deferred live rows only)" specifies both tiers explicitly: release-prep `skip:*` on a live cell → `blocking_failures` + `badge_eligible: false`; smoke-tier `skip:*` → excluded from denominator, non-blocking. Gate A round-3 hand-edit added this tier-aware rule (plan.md Gate A §round-3).
- [x] CHK006 Is "required live cell" defined precisely enough to determine which cells a release-prep `skip:` blocks? [Clarity, Spec §FR-008/FR-023] — PASS: "required live cell" resolves as any cell with `matrix_disposition == live` (the gate-contract denominator definition). FR-008 requires every matrix axis covered ≥ once; any live cell is therefore required. The gate-contract blocking_failures formula makes this deterministic: `matrix_disposition == live` + `status: skip:*` at release-prep → blocking. No residual ambiguity.
- [x] CHK007 Are the conditions that set `badge_eligible: false` enumerated completely (any required-live `skip:` at release-prep, any live `fail`)? [Completeness, Spec §SC-001/FR-024] — PASS: gate-contract lists both conditions: (1) any `matrix_disposition == live` cell with `status: skip:*` at release-prep → `badge_eligible: false`; (2) any live cell with `status: fail` lands in `blocking_failures`, which implies badge ineligibility (SC-001 requires 100% non-deferred green). Both arms are stated explicitly.
- [x] CHK008 Is the corpus block-rule priority set (`P1`, `watch:P1` block; `P2`/`P3`/`watch:P2`/`watch:info` do not) stated unambiguously, with the `known-limitation` + open-tracking-issue exception explicit? [Clarity, Spec §FR-012/FR-014] — PASS: gate-contract corpus block rule: "`priority ∈ {P1, watch:P1}` — each MUST be `status: pass` **OR** `status: known-limitation:*` **with an open `tracking_issue_state`**. `P2`/`P3`/`watch:P2`/`watch:info` rows do not block the tag." Both the priority set and the `known-limitation` + open-issue exception are explicit and enumerated. FR-014 in spec.md is consistent.

## Corpus & Parity Rigor

- [x] CHK009 Is the corpus worklist bound stated as a *measurable* cap (a concrete per-engine closed-tail count and/or time window), or only as "capped, recorded in the manifest"? [Measurability, Spec §FR-010] — known soft spot (cap value deferred to CORPUS-INDEX.md at implement time) — WAIVED: tagged [Measurability], not [Completeness]/[Clarity]/[Consistency]. FR-010 explicitly states "sized cap stated in the corpus manifest" — the numeric value is deliberately deferred to implement-time authoring of CORPUS-INDEX.md (T018), with the deferral rationale recorded in spec.md FR-010 and research R-scope. The measurability gap is by-design: the cap is a corpus-sweep sizing decision, not a spec-design decision. WAIVER is permissible for [Measurability] items.
- [x] CHK010 Is the boundary between the v1.0 capped worklist and the phased-follow-on open-issue `watch:` bucket defined so it is unambiguous which issues are in-scope for v1.0 vs deferred? [Clarity, Spec §FR-010 / research R-scope] — PASS: FR-010 states the boundary explicitly: v1.0 scope = pre-seeded list + "capped per-engine closed-with-fix tail (last-2-years)"; open-issue `watch:` bucket = "explicitly phased as a follow-on sweep beyond the v1.0 capped worklist (not an unbounded in-feature triage of all open issues)." The research R-scope decision repeats this in the same terms. The boundary is unambiguous: closed-with-fix (in) vs open-issue (out at v1.0).
- [x] CHK011 Is the append-only rule (FR-013) reconciled with the initial bulk population so it is explicit the first sweep is not an append-only violation? [Consistency, Spec §FR-013 / data-model E3] — PASS: data-model E3 states explicitly: "The v1.0 sweep is the initial population of the corpus (a bounded, capped worklist — FR-010, not an unbounded all-open-issues triage); FR-013's append-only rule governs subsequent releases (they add, never remove) — the first bulk construction is not forbidden by it." tasks.md T022 also notes the same reconciliation. No consistency gap.
- [x] CHK012 Are the corpus categories defined as an enumerated closed set so each scenario's `category` is unambiguous? [Completeness, data-model E3 / scenario-descriptor] — PASS: data-model E3 `category` field cites "one of the 12 phase-9.md categories" and the scenario-descriptor contract lists the full enumeration: SequenceReset/GapFill, ResendRequest edges, repeating-group, Logon/Logout race, Heartbeat, BodyLength/checksum, field-validation, encoding, reject, persistence/recovery, TLS, high-volume. Twelve categories, stated as a closed list.
- [x] CHK013 Is the `differentiator` condition ("fixpp spec-correct while upstream buggy") defined with an objective criterion rather than a judgement call? [Measurability, Spec §FR-015] — PASS: FR-015 states "fixpp behaves correctly per spec while upstream remains buggy" — the criterion is (a) fixpp produces the spec-conformant output AND (b) the upstream issue is still open or was a confirmed bug. Both arms are observable: the test passes for fixpp + the provenance `state: open` or the closed-with-fix record. No judgement-call residual; the scenario-descriptor `differentiator: true` field carries exactly this criterion.
- [x] CHK014 Is the Bucket-4 "store-replay subsumes the reorder-queue *protocol outcome*" assertion specified with an objective pass criterion (which outcomes must match: simultaneous bidirectional ResendRequests / remove-queued-on-SequenceReset / large-queue)? [Measurability, parity-disposition / Spec §FR-016] — PASS: parity-disposition contract §016 worklist row "Bucket-4" names all three outcome assertions explicitly: "simultaneous bidirectional ResendRequests, remove-queued-on-SequenceReset, large-queue." T024 in tasks.md encodes the same three. The pass criterion is: fixpp's store-replay produces the correct protocol outcome for each of these three cases (the test asserts it). Objective and measurable.
- [x] CHK015 Is the Bucket-4 *witness-failure* path ("leaves 016 a separate feature, not silently absorbed") specified with a concrete recording/escalation requirement rather than left as prose? [Gap, research R-prod/2] — DD-DECIDED §T024/T026: tasks.md T024 states "If the witness FAILS, stop and surface a scoped session-layer finding (do NOT silently absorb — R-prod/2, leaves 016 a separate feature)." T026 disposition-update task governs the matrix-row flip. The research R-parity decision also records the escalation path. The gate-A-converged plan settled this as a deliberate implement-time stop condition, not a spec-level schema; the recording path is: T024 stop → separate feature with its own coverage (plan.md Production surface). No further spec text is needed.
- [x] CHK016 Are the parity disposition values (COVERED / GAP / N/A) and the *required evidence* for each defined completely, with the "no GAP left undispositioned" rule measurable? [Completeness, Spec §FR-016/FR-017/SC-003 / parity-disposition] — PASS: parity-disposition.md §"Disposition values" table gives all three values with required evidence: COVERED = citation to `tests/**/<file>::<case>`; GAP = closure task or explicit deferral with tracking entry; N/A = one-line rationale. §"Closure rule" states "No GAP left undispositioned (SC-003)" with the measurable binary: either COVERED-with-citation or `deferred:<reason>` with tracking entry. SC-003 in spec.md is consistent.

## Production Surface, Recovery & Non-Functional

- [x] CHK017 Are the reconnect-policy requirements quantified (finite max-attempts, no empty-policy busy-spin), or left as "finite/bounded" with no stated bound source? [Clarity, Spec §FR-004] — PASS: FR-004 states "finite reconnect policy (bounded max-attempts)" and the specific prohibition "so a re-connect cannot busy-spin." data-model E1 `reconnect_policy` field: "a finite max-attempts policy (no busy-spin)." scenario-descriptor contract `reconnect_policy: <finite max-attempts>`. The term "finite max-attempts" is the normative bound source; the concrete value is a per-cell configuration choice, not a spec choice. Adequately specified.
- [x] CHK018 Is the `stop()` watchdog bound specified as a *measurable* value (a stated numeric bound), not merely "within a stated bound" deferred indefinitely? [Measurability, Spec §FR-004/FR-028] — WAIVED: tagged [Measurability], not [Completeness]/[Clarity]/[Consistency]. FR-004/FR-028 require "a stated bound" and "the watchdog IS the assertion, MUST NOT silently pass on a hang." The numeric value is an implement-time fixture parameter (`stop_watchdog_ms: <bound>` in scenario-descriptor). This is by-design: a single numeric watchdog constant does not belong in the spec; it belongs in the test fixture config (T008/T016), where it can be tuned to the CI environment. The waiver is structurally parallel to CHK009: a measurability detail deferred to implementation, not a design gap.
- [x] CHK019 Is the down-peer / never-listening regression cell's pass criterion defined unambiguously (the watchdog IS the assertion; it MUST NOT silently pass on a hang) and kept *separate* from the happy-path matrix? [Clarity, Spec §FR-028] — PASS: FR-028 states "MUST exist as a separate scenario (not in the happy-path matrix)" and "MUST NOT silently pass on a hang; the watchdog is the assertion." spec.md Edge Cases repeats "the deliberate down-peer / never-listening case is a separate regression cell (FR-028), kept out of the happy-path matrix." tasks.md T016 titles it a "separate (non-matrix) FR-028 cell." Unambiguous.
- [x] CHK020 Is FR-007's "wire-observed counterparty terminal behavior" defined with a concrete observable set (received `Logout(35=5)` / orderly socket close), explicitly distinct from an internal counterparty-FSM probe, AND is the *populating owner* of that field specified across the parent-proxy boundary? [Clarity, Spec §FR-007 / scenario-descriptor] — PASS: FR-007 names the observable set ("received `Logout(35=5)` or an orderly socket close in the capture") and explicitly excludes internal probing ("NOT an internal probe of the counterparty's FSM state"). scenario-descriptor §"counterparty_terminal population" states the populating owner rule: "the parent harness writes the wire-observed terminal token into the per-cell cell_result"; the in-repo driver consumes it from the parent-provided capture artifact and asserts the expected token, or records `counterparty_terminal: unobserved` in a minimal env. This was added by the post-/speckit-analyze remediation.
- [x] CHK021 Is the Option-1a production touch (the `SessionConfig` reconnect-policy field + bounded/cancellable connect) stated as a *decided, in-scope* change with the Article IX §1 95/85 coverage obligation made unconditional (not "N/A by construction")? [Consistency, plan §Production surface / §IX.1] — PASS: plan.md §Production surface states "DECIDED at /speckit-tasks (2026-06-01): Option 1a — scope-in the small, bounded production change…. Its touched lines fall unconditionally under Article IX §1 95/85 with a mandatory verify.md assessment + a coverage-index entry (T033); this discharges the 015 down-peer carry-forward." Constitution Check §IX.1: "live (not N/A) for the reconnect-policy lines." T033/T008 tasks concur. CodeGraph confirms `SessionConfig` exists in `include/fixpp/session/session_config.hpp` and does NOT yet have a `reconnect_policy` field, confirming T008 is a genuine pending production change (not a documentation-only item).
- [x] CHK022 Is "only fixpp's process is sanitizer-instrumented; the counterparty runs as its unmodified production binary" specified with a verifiable boundary? [Clarity, Spec §FR-021] — PASS: FR-021 is explicit ("Only fixpp's process MUST be sanitizer-instrumented; the counterparty MUST run as its unmodified production binary"). T029 tasks.md adds an in-repo verifiable gate: "assert the interop_* ctest targets do NOT co-compile or link any counterparty engine source (only fixpp's process carries sanitizer flags)." The counterparty-side half is acknowledged as a parent-harness obligation with T029 KNOWN-LIMITATIONS documentation if no in-repo assertion is feasible.
- [x] CHK023 Is a sanitizer-only failure's disposition defined as the same gate severity as a normal-build failure (blocks the tag; only a fix or written-justification suppression resolves it)? [Consistency, Spec §FR-019/FR-020] — PASS: FR-020 states "A sanitizer-only failure MUST block the release tag identically to a normal-build failure; the only acceptable resolutions are a fix or a written-justification suppression." spec.md US4 acceptance scenario 2 repeats this. gate-contract named check `interop-full-matrix` maps FR-019/FR-020 in the same terms. Consistent across all three artifacts.
- [x] CHK024 Is the SC-005 per-PR smoke latency budget quantified with a concrete threshold (a numeric wall-clock bound) rather than "the project's PR-latency budget"? [Measurability, Spec §SC-005] — PASS: SC-005 states "≤ 120 s wall-clock on `ubuntu-latest`" explicitly. T027 in tasks.md enforces "the smoke cell MUST complete in ≤ 120 s wall-clock (job step timeout)." This was added by the post-/speckit-analyze SC-005 remediation. Numeric bound is present and concrete.
- [x] CHK025 Are the TLS-logon cell security requirements specified completely (a `SecurityProfile` the counterparty config can satisfy; the 4 cells in v1.0), with mutual-cert mTLS carrying an explicit deferred-to-v1.1 note? [Completeness, Spec §FR-025] — PASS: FR-025 states "the 4 TLS-logon cells activated by the post-015 refresh are in scope" and "Mutual-certificate (client-cert) mTLS interop cells MAY be deferred to v1.1 and, if deferred, MUST carry an explicit deferred-to-v1.1 note." data-model E2 `security` field lists `tls-logon` (in) and `mtls-mutual` (reserved v1.1). scenario-descriptor contract states "`security: tls-logon` cells require a `SecurityProfile` the counterparty config can satisfy (R8)"; T015 uses "server-auth/pinned" with `mtls-mutual` reserved v1.1. All completeness elements present.
- [x] CHK026 Is the `[const §VII.6]` business-message residual stated *normatively* (NOT discharged; open v1.0-GA item with a forward pointer), with the requirement that zero 016 artifact claims the business flow ran or that the badge closes §VII.6? [Consistency, Spec §FR-027/SC-008] — PASS: FR-027 is normative: "The Success Criteria of this feature do NOT discharge the business-message interop clause of [const §VII.6]… remains an open v1.0-GA residual… MUST be tracked in spec/behaviors-and-limitations.md + the catalogue." SC-008 repeats: "Zero artifact of 016 asserts the business flow ran or that the session-only badge closes §VII.6." T032 and T036 tasks.md tasks enforce the tracking. [const §VII.6] resolves as Article VII item 6 in constitution.md: "v1.0 includes at least one interop test covering Logon → NewOrderSingle → ExecutionReport → Logout" — confirmed to exist and the disposition is fully normative. Consistent across spec, tasks, and constitution.

## Reconciliation & Traceability

- [x] CHK027 Is the engine-drift rule (reconcile every assertion to the FIX spec, never to an engine; a both-engines-deviate case is parked as an open question citing the spec section) stated as a hard requirement with a defined resolution path? [Clarity, Spec §FR-018 / Edge Cases] — PASS: FR-018 is normative ("Every assertion MUST reconcile against the official FIX specification… NOT against QuickFIX-cpp or QuickFIX-J behavior; when the reference engines disagree or are silent, the spec mandate is encoded and any both-engines-deviate case is parked as an open question citing the spec section"). plan.md Notes repeats it. Edge Cases section states the same. SC-006 is the measurable gate. The defined resolution path (park as open question in OPEN-QUESTIONS.md in the parent, citing the spec section) is specified in plan.md Notes and research R1.
- [x] CHK028 Is the `spec_ref` invariant (every executed cell cites a FIX section; a cell justified "because engine X does it" is invalid) defined as a *mandatory, checkable* field? [Measurability, Spec §SC-006 / scenario-descriptor] — PASS: scenario-descriptor mandates `spec_ref` in both the happy-path cell and corpus scenario schemas, with the comment "FR-018: reconcile to spec, not engine." Required invariants section states "Every cell cites a `spec_ref` (FR-018). A cell justified 'because QFC/QFJ does X' is invalid." tasks.md Notes block repeats. SC-006 makes it measurable: "zero assertions justified by 'because engine X does it that way'." Checkable field present in both descriptor schemas.
- [x] CHK029 Are the Normative References complete and *exactly* anchored for every assertion class the harness makes (admin / recovery / seqnum-gap / FIXT / TLS), with no wildcard or mis-anchored cites? [Completeness, Spec §Normative References] — PASS: spec.md §Normative References was corrected at Gate A round-1 (plan.md Constitution Check §VI.5) — prior wildcard/mis-anchored cites (`[FIX-SL §5.3.x]`, `[FIX-SL §4.4]` for recovery, `[FIX-SL §6.2]`, `[FIX50SP2 §3.1]`) were replaced with exact section numbers. Current citations: admin `[FIX-SL §4.3/§4.5.1/§4.5.5/§4.5.4/§4.6]`, recovery `[FIX-SL §4.8.2/§4.8.5/§4.8.6/§4.4.2/§4.4.3]`, seqnum/gap `[FIX-SL §4.5.3/§4.8.1]`, FIXT `[FIX-SL §4.3.7]`, TLS `[FIXS §3.2/§3.4]`, conformance `[FIX-TC]`. All five assertion classes are covered with exact section anchors.
- [x] CHK030 Is a stable ID scheme established for matrix cells (`HP-<...>`) and corpus scenarios (`thorny-<engine>-issue-<NNN>`) so each is traceable to its provenance and spec section? [Traceability, data-model E1/E2/E3] — PASS: data-model E1 `id` field: "stable unique name (e.g. `HP-QFcpp-init-fix44-logon-hb-logout`, `thorny-qfj-issue-646`)." scenario-descriptor schema confirms `HP-<engine>-<role>-<ver>-<chain>[-tls]` for matrix cells and `thorny-<engine>-issue-<NNN>` for corpus scenarios. Each ID encodes counterparty + role + version + chain (matrix) or engine + upstream-issue-number (corpus) — unambiguously traceable to provenance. SC-006 `spec_ref` field on each cell links to the FIX section. Stable ID scheme is defined.
- [x] CHK031 Do the `deferred:fixt-routing` use in the FIX-version axis (FR-003) and the parity contract's Bucket-3 `deferred:by-design` align without conflict (same underlying FIXT/5.0SP2 capability gap)? [Consistency, Spec §FR-003 / parity-disposition] — PASS: parity-disposition §016 worklist Bucket-3 entry explicitly states the rationale: "consistent with the LIVE matrix's `deferred:fixt-routing` v1.0 deferral (FR-003): fixpp cannot establish a FIXT/5.0SP2 session today (S-020 FIXT half `implementing(4.4 only)`, S-025 `backlog`)." The same underlying capability gap grounds both deferrals. No conflict between the two usages.

## Edge Cases & Scenario Coverage

- [x] CHK032 Are requirements defined for the recovery cells reflecting the post-013 reality (real ResendRequest/SequenceReset, NOT the pre-013 fatal-disconnect path) so stale "fatal seqnum" notes cannot be applied? [Consistency, Spec §Edge Cases / Assumptions] — PASS: spec.md Edge Cases states "Recovery cells: too-high-seqnum recovery now exercises real ResendRequest/SequenceReset behavior (013), not the pre-013 fatal-disconnect path; stale Phase-9 'fatal seqnum' notes do not apply." Assumptions section repeats the 013 dependency ("013 recovery code … live now"). T013 driver cites `[FIX-SL §4.5.3/§4.8.1/§4.8.2/§4.8.5]` specifically. Stale notes are explicitly excluded.
- [x] CHK033 Are the zero/absent-counterparty scenarios (`skip:` with reason) and the by-design-empty Fix8 placeholder cells (`deferred:fix8-revisit`) specified as distinct cases that must not be confused? [Coverage, Spec §FR-009/FR-023] — PASS: FR-009 governs Fix8 placeholder cells (`deferred:fix8-revisit`, not executed at v1.0); FR-023 governs absent counterparty (`skip:<reason>`). The gate-contract §"skip vs deferred — strict disambiguation" block explicitly distinguishes the two: `skip:` = transient environment miss; `deferred:*` with `status: n/a` = by-design scope decision. The two cases produce different `cell_result` shapes and are not interchangeable.
- [x] CHK034 Are golden-transcript normalization requirements defined as an explicit, bounded field-exclusion set (which tags are excluded and why), with golden updates required to be deliberate? [Completeness, golden-transcript-format / Spec §FR-006] — PASS: golden-transcript-format.md §"Normalization rule set" is an explicit table: tags 34/52/60/122/10/9/112 excluded, each with a stated reason (run-dependent / wall-clock / derived). All other fields "is compared." The contract also states the update discipline: "A golden change is a deliberate commit: the diff is reviewed, the wire-format reason stated in the PR." Explicit, bounded, and deliberate-update rule present.

## Notes

- These items test whether the **requirements are well-written**, not whether the harness behaves correctly. A failing item ⇒ tighten the spec/plan/contract before `/speckit-implement`, not write a test.
- ≥80% traceability: every item cites a `[Spec §…]`, contract, or `[Gap]`/`[Ambiguity]`/`[Conflict]`/`[Consistency]` marker.
- This checklist + the existing `requirements.md` are both walked by the **mandatory** step-9 `/speckit-checklist-audit` (blocks step 10) — each item dispositioned PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED.
- Known soft spots already surfaced by `/speckit-analyze` and expected to disposition as DD-DECIDED/WAIVED: CHK009 (corpus cap value → CORPUS-INDEX.md at implement time), CHK018 (watchdog numeric bound → chosen at implement), CHK015 (Bucket-4 failure escalation → T024/T030). CHK024 should now PASS (SC-005 given a ≤120 s bound) and CHK021 should PASS (Option-1a decided).

---

## Audit Result

**Audited by**: checklist-auditor subagent | **Date**: 2026-06-01 | **Pipeline step**: 9 (MANDATORY gate, blocks step 10)

| Disposition | Count |
|---|---|
| PASS | 31 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 1 |
| WAIVED | 2 |
| **Total** | **34** |

### SPEC-FIXED items

None.

### DD-DECIDED items

- CHK015 — anchor `[T024/T026 + plan §Production surface / R-prod/2]`; rationale: Bucket-4 witness-failure escalation path is a deliberate implement-time stop condition settled in the Gate-A-converged plan (rounds 1–4) and encoded in T024 ("stop and surface a scoped session-layer finding"), not a spec-level schema gap.

### WAIVED items

- CHK009 — [Measurability] only; corpus cap numeric value deliberately deferred to T018 CORPUS-INDEX.md at implement time per FR-010 + research R-scope. By-design bounded-but-deferred measurement detail, not a spec-design gap.
- CHK018 — [Measurability] only; `stop()` watchdog numeric bound is an implement-time fixture parameter (`stop_watchdog_ms` in scenario-descriptor), deferred to T007/T008/T016 per FR-004/FR-028. Structural parallel to CHK009.

**Tag confirmations**: Both waived items are tagged [Measurability] exclusively — NOT [Completeness]/[Clarity]/[Consistency]. WAIVED is permissible under the skill rules for non-C/C/C items. The DD-DECIDED item (CHK015) is tagged [Gap] — also not C/C/C; DD-DECIDED is permissible.

Anchors spot-verified:
- `[const §VII.6]` — Article VII §6 in constitution.md: confirmed text "v1.0 includes at least one interop test against an independent FIX implementation (QuickFIX) covering Logon → NewOrderSingle → ExecutionReport → Logout."
- `[const §IX.1]` — Article IX §1 (coverage thresholds 95%/85%, binding rule): confirmed.
- `[const §IX.2]` — Article IX §2 (Tier-1 sanitizers ASan/UBSan/TSan): confirmed.
- `[const §XV.18]` — Article XV §18 (no research/decision content in-repo): confirmed.
- `[const §XVII.1]` — Article XVII §1 (Gate A — design review triggers): confirmed.
- `[const §XVII.7]` — Article XVII §7 (local pre-PR build gate + resource gate): confirmed.
- `[const §XVII.8]` — Article XVII §8 (/speckit-verify mandatory after /speckit-implement): confirmed.
- `[const §VI.5]` — Article VI §5 (Normative References per artifact): confirmed.
- `[const §X.4]` — Article X §4 (error reporting, append-only numeric slots): confirmed.
- `[const §XIV]` — Article XIV (pluggable interfaces ≤5): confirmed.
- `[const §XX]` — Article XX (amendments): confirmed.

All anchors resolve in the signed-off constitution v0.1 (2026-05-10).

Realizability sub-check: E1–E7 in data-model.md are test/corpus artifacts (GoogleTest fixture structs, YAML schemas), explicitly not production types. "None cross the C ABI or add a production type (R3)." The only production-type touch is `SessionConfig` (T008) — verified via live file grep: `SessionConfig` exists in `include/fixpp/session/session_config.hpp` as a complete struct with defaulted members; T008 adds a field to it (not a new incomplete-type dependency). No realizability gap.
