# Grouping Requirements Quality Checklist: Test-Binary Grouping

**Purpose**: Validate that the grouping taxonomy, gate-preservation, and measurement requirements are complete, unambiguous, consistent, and measurable before implementation (release-gate depth; audience: PR reviewer).
**Created**: 2026-07-10
**Feature**: [spec.md](../spec.md)

## Requirement Completeness

- [x] CHK001 Are the criteria that make a test "isolation-safe" (groupable) versus "isolation-sensitive" (standalone) each enumerated exhaustively rather than by example? [Completeness, Spec §FR-001/FR-002] — SPEC-FIXED: the "genuinely-concurrent / global-singleton-freshness" category (FR-002/research D3) had no detection criterion at all; added a concrete (if manual-review) signal to `tasks.md` T003 and `quickstart.md` §1. Remaining categories were already exhaustively enumerated, not by example.
- [x] CHK002 Is every isolation-sensitive category paired with the concrete signal that detects it (grep pattern / CMake property), so a maintainer can classify a test deterministically? [Completeness, Spec §FR-002, Tasks §T003] — SPEC-FIXED: same edit as CHK001 — `tasks.md` T003 and `quickstart.md` §1 now pair every standalone category with a signal (grep pattern, CMake property, or explicit manual-review flag for the concurrency/singleton category).
- [x] CHK003 Are requirements defined for tests that a preset does *not* build (TSan-only / release-only targets), so grouping cannot silently add/drop a test from a preset? [Coverage, Spec §FR-007] — PASS: FR-007 ("Grouping MUST NOT change which tests any preset builds or runs... identical before and after") plus the matching Edge Cases bullet directly cover this.
- [x] CHK004 Is the handling of a module with **zero** groupable tests (e.g. `alloc_guard`) specified so it is still fully dispositioned rather than skipped? [Completeness, Spec §FR-011, Tasks §T022] — PASS: FR-011 (100% accounting) + Tasks T022 explicitly require `alloc_guard`'s expected near-empty grouping to be fully recorded, not skipped.
- [x] CHK005 Are requirements stated for per-target properties beyond LABELS — `TIMEOUT`, `ENVIRONMENT`, compile-definitions, extra link deps — including what forces a test to standalone? [Completeness, Spec §FR-005] — PASS: FR-005 enumerates TIMEOUT/ENVIRONMENT/link-deps preservation and the "cannot be represented → standalone" rule; data-model.md's Standalone-test entity lists the same attribute set.
- [x] CHK006 Is the disposition-record content (per `.cpp`: decision + reason + any ODR action) specified precisely enough to prove FR-011's 100% accounting? [Completeness, Data-model §Disposition record] — PASS: data-model.md's Disposition-record entity (`cpp`; `decision ∈ {grouped:<bucket> | standalone:<reason>}`; optional `odr_action`) plus Tasks T002's ledger column skeleton give an unambiguous, per-`.cpp` record shape.

## Requirement Clarity

- [x] CHK007 Is "label-homogeneous bucket" defined unambiguously — i.e. is the LABEL-set-equality rule for bucket membership explicit? [Clarity, Spec §FR-003] — PASS: FR-003 + Research §D2 define it as exact LABEL-set equality across a bucket's members, re-applied via `gtest_discover_tests PROPERTIES LABELS`.
- [x] CHK008 Is the bucket key "(shared link-deps ∩ label-homogeneous)" defined precisely enough that two maintainers would form the same buckets for a given module? [Clarity, Spec §FR-013, Research §D4] — PASS: Research §D4 gives the canonical partition key verbatim — "(sorted set of link libraries, sorted set of intended labels)" — a deterministic, reproducible partition function.
- [x] CHK009 Is "several-fold" disk reduction (SC-001) either quantified with a threshold or explicitly stated as a recorded-observation-only target (no pass/fail bar)? [Ambiguity, Spec §SC-001] — SPEC-FIXED: `spec.md` SC-001 amended to state explicitly this is a recorded-observation outcome, not a numeric pass/fail bar; the enforced gates are FR-008 (delta captured) and SC-006 (100% dispositioned). `plan.md`'s Performance-Goals echo updated to match.
- [x] CHK010 Is the ≤10% ctest wall-time bound unambiguous as to its baseline (pre-grouping same-preset wall-time) and measurement scope (whole preset vs module subset)? [Clarity, Spec §SC-005, Research §D6] — SPEC-FIXED: `spec.md` SC-005 previously said "CI unfiltered ctest wall-time per preset" while Research §D6/Tasks T009 actually measure the module's `-L` subset — a real scope mismatch. Amended SC-005 to state the module-subset measurement is the launch-overhead proxy for the preset-wide bound, not a full unfiltered-preset timing; propagated to `plan.md` Performance Goals.
- [x] CHK011 Is "no new sanitizer findings / no loss of isolation" defined in a way that is checkable, or does it rely on the undefined notion of "each sanitizer still detects what it detected"? [Clarity, Spec §FR-006] — PASS: backed by FR-007's stronger, checkable guarantee — the executed gtest-case set is identical before/after — which is what actually proves no finding is masked/reordered by pooling, not merely "stays green."
- [x] CHK012 Does the ODR policy make explicit which symbols may be renamed (test-local helpers/globals) versus which may never be (`Suite.Name`), and why? [Clarity, Spec §FR-012, Research §D5] — PASS: FR-012 + Research §D5 explicit — test-local helper/global renameable (behavior-neutral); `Suite.Name` never renameable (coverage-index/completeness-audit key).

## Requirement Consistency

- [x] CHK013 Do the spec's FR-002 standalone categories agree with research.md D3's *narrowed* taxonomy (fork-based death tests group; homogeneous ENVIRONMENT groups)? [Consistency, Spec §FR-002 vs Research §D3] — SPEC-FIXED: found a real contradiction — FR-002 said "any test carrying a per-target `ENVIRONMENT`" is standalone (unconditional), while Research §D3 says ENVIRONMENT is groupable when homogeneous across the bucket (unconditional only when heterogeneous). The death-test clause was already correctly narrowed to top-level-terminating only. Amended FR-002's ENVIRONMENT clause in `spec.md` to match D3.
- [x] CHK014 Is the module count consistent across spec, plan, data-model, tasks, and baseline (23), with `tests/abi/` explicitly excluded? [Consistency, Spec §Key Entities / §SC-006] — SPEC-FIXED: verified against the live repo (`find tests -maxdepth 2 -name CMakeLists.txt` → 23; `ls tests/` → 25 subdirectories). The stated exclusion list named only `tests/abi/`, but `tests/support/` is equally fixtures-only (headers only, no `.cpp`/`CMakeLists.txt`) and was silently the second exclusion needed to reach 23 — the count math (25 − 1 named exclusion ≠ 23) didn't reduce as written. Added `tests/support/` to the exclusion note in `spec.md` (Key Entities) and `data-model.md` (Test-module entity).
- [x] CHK015 Do the baseline-path references resolve identically (parent-repo `../research/…`) across tasks, quickstart, research, and plan? [Consistency, Tasks §T001/T030] — PASS: verified all six artifacts (spec/plan/research/data-model/tasks/quickstart) use the qualified `../research/test-grouping-baseline/…` path uniformly, following the prior `/speckit-analyze` F1 remediation (commit `3b46f3f0`). No stale bare `research/…` reference remains.
- [x] CHK016 Is the rollout order in tasks (T011–T025) consistent with the descending-disk-impact order asserted in the spec/plan and the baseline table? [Consistency, Spec §US2, Tasks §Phase 4] — PASS: cross-checked T011–T025's module sequence against `../research/test-grouping-baseline/BASELINE.md`'s per-module ASAN-size ranking — exact match once `dictionary` (the pilot, done separately in Phase 3) is excluded from the ranked list: session, interop, capi, config, sync, transport, core, otel, wire, tls, log, alloc_guard, codegen, perf, then the T025 sweep (conformance/integration/tap/service, all ≤27 MB).
- [x] CHK017 Are the "no production/library code change" constraint (plan) and the "test source edited only on ODR collision" allowance (FR-012) mutually consistent and jointly bounded? [Consistency, Plan §Constraints vs Spec §FR-012] — PASS: plan.md's Constraints line scopes "no production/library code change" to `src/`/`include/`/C-ABI/Python/runtime, and separately bounds the FR-012 test-source-edit allowance strictly to ODR-collision renames — non-overlapping, jointly consistent scopes.

## Acceptance Criteria Quality (Measurability)

- [x] CHK018 Can each of SC-001..SC-006 be objectively evaluated from a recorded artifact (CSV delta, ctest log, audit output) rather than subjective judgment? [Measurability, Spec §Success Criteria] — PASS (post CHK009/CHK010 fixes): SC-001 (inventory.sh CSV diff), SC-002 (ctest logs), SC-003 (audit output), SC-004 (T028's enumerated command run), SC-005 (timing log against the now-clarified module-subset scope), SC-006 (disposition ledger) — each has a concrete recorded artifact.
- [x] CHK019 Is the "before" side of every disk delta pinned to the committed 2026-07-10 baseline (not a re-measured pre-state that could drift)? [Measurability, Data-model §Baseline inventory, Tasks §T001] — PASS: data-model.md's Baseline-inventory invariant states this explicitly; T001 only verifies no drift against the committed CSV, it does not substitute a freshly re-measured value.
- [x] CHK020 Are the three pilot metrics (disk, wall-time, incremental relink) each given a defined measurement method and a recorded location? [Measurability, Spec §FR-009, Research §D6] — PASS: Research §D6 defines the exact command for each of the 3 metrics; Tasks T009 records all three in `measurements.md`.
- [x] CHK021 Is SC-004 (selectability preserved) measurable via an enumerable set of documented `ctest -L`/`-R` commands rather than an open-ended claim? [Measurability, Spec §SC-004, Tasks §T028] — SPEC-FIXED: T028's enumeration procedure cited a dangling path, `.claude/commands/speckit-verify.md`, which does not exist anywhere in the parent or submodule tree (empirically verified). The real file is `../../../.claude/skills/speckit-verify/SKILL.md` (3 levels above the `library/` submodule cwd). As written, T028's enumeration was not executable for that source — corrected the path in `tasks.md` §T028 so SC-004 is measurable via an enumerable, executable command set, not an open-ended/broken claim.

## Scenario & Edge-Case Coverage

- [x] CHK022 Are requirements defined for an ODR/`Suite.Name` collision that cannot be resolved by an in-test rename (fallback to standalone)? [Edge Case, Spec §FR-012, §Edge Cases] — PASS: FR-012 explicit fallback-to-standalone "when the collision is a gtest `Suite.Name`... or is otherwise unresolvable."
- [x] CHK023 Are requirements defined for a bucket that would span two feature labels (split to stay label-homogeneous)? [Edge Case, Spec §Edge Cases, §FR-003] — PASS: the matching Edge Cases bullet + FR-003 both require the bucket to split (or the odd test to stay standalone) to preserve label-homogeneity.
- [x] CHK024 Is the failure mode where `gtest_discover_tests` POST_BUILD discovery cannot enumerate an instrumented binary addressed as a requirement (not just noted)? [Coverage, Spec §Edge Cases, Research §D1] — SPEC-FIXED: the Edge Cases bullet previously only noted the failure ("caught by the pilot before rollout") with no stated remedy. Amended `spec.md` to specify the remedy: carve the offending `.cpp` out to standalone using the same policy as an unresolvable ODR collision (FR-012), recorded in the disposition ledger.
- [x] CHK025 Are requirements defined for ENVIRONMENT that is homogeneous across a bucket vs heterogeneous per-test, with the decision rule stated? [Coverage, Research §D3, Spec §FR-005] — PASS (post CHK013 fix): Research §D3 is now the single, non-contradicted decision rule (homogeneous → groupable, heterogeneous → standalone), directly referenced from the corrected FR-002.
- [x] CHK026 Is the recovery/rollback expectation defined for a module that fails a preset mid-rollout (does the sequential gate FR-010 block the next module)? [Recovery, Spec §FR-010] — WAIVED: no formal rollback procedure is specified beyond FR-010's sequential gate (next module cannot start until the current one is green). Acceptable because every edit is a git-tracked, revertible `tests/**/CMakeLists.txt` change with no runtime/data-migration risk; the implicit remedy (adjust bucket/disposition, re-run quickstart §§2–4) is low-risk fix-forward for a test-infra-only feature. Not tagged Completeness/Clarity/Consistency, so WAIVED is permitted here.

## Dependencies & Assumptions

- [x] CHK027 Is the assumption that coverage-index + completeness audits key only on `.cpp` stem + `Suite.Name` (and are thus grouping-invariant) stated as a validated precondition, not an unbacked claim? [Assumption, Spec §FR-004] — PASS: FR-004 states this as a given, and Research §D2 grounds it explicitly in an actual Explore census dated 2026-07-10 — a validated precondition, not an unbacked assumption.
- [x] CHK028 Is the dependency on `tests/core` as the reference precedent documented, and its already-grouped state reconciled rather than re-grouped? [Dependency, Tasks §T017] — PASS: plan.md's Project Structure names `tests/core/CMakeLists.txt` as the reference precedent; Tasks T017 explicitly reconciles (groups only the remaining standalone-but-groupable tests) rather than re-grouping the already-grouped `fixpp_core_tests`/`fixpp_capi_tests`.
- [x] CHK029 Is the local-build resource gate (AskUserQuestion before heavy builds) captured as a binding constraint on every build/ctest task? [Assumption/Constraint, Plan §Article XVII §7] — PASS: tasks.md's header states the gate as a blanket rule binding every task; quickstart.md §4 (the shared per-module build procedure every module task in T004–T025 invokes) additionally carries the explicit `AskUserQuestion` annotation inline.

## Traceability & Close-out

- [x] CHK030 Does every FR-001..FR-013 and SC-001..SC-006 map to at least one task, and is that mapping recorded (analyze coverage table)? [Traceability] — PASS: manually re-verified every FR/SC maps to ≥1 task in this audit (e.g. FR-012→T006, FR-013→T005/T010, SC-005→T009/T011/T030); consistent with the `/speckit-analyze` verdict recorded in commit `3b46f3f0` ("100% FR/SC→task coverage, proceed"). This repo does not persist a separate analyze-coverage decision file for any feature (checked `.specify/decisions/`, none exists) — the commit-message record is this repo's established evidence form for analyze verdicts.
- [x] CHK031 Are the two mandatory Gate-B close-out obligations (catalogue disposition + completeness audit) present as explicit, non-skippable requirements with a recorded verdict location? [Completeness, Tasks §T033/T034] — PASS: T033 (catalogue close-out, N/A-with-reason) and T034 (final completeness audit) are explicit tasks with a stated recorded-verdict location (`.specify/decisions/068-test-binary-grouping-verify.md` `## Completeness`).

## Audit Result

Pipeline step 9 (CHECKLIST AUDIT) — no signed-off design-doc anchor for this feature (Gate A waived, no trigger path; `.specify/decisions/068-test-binary-grouping-gatea.md`). Authoritative artifacts: `spec.md` + `research.md` (D1–D7). All 31 items dispositioned; zero un-dispositioned boxes.

| Disposition | Count |
|---|---|
| PASS | 22 |
| SPEC-FIXED | 8 |
| DD-DECIDED | 0 |
| WAIVED | 1 |
| **Total** | **31** |

### SPEC-FIXED items

- CHK001/CHK002 — added the missing "genuinely-concurrent / global-singleton-freshness" census signal (no reliable grep; manual-review flag for `std::thread`/`std::jthread`/`std::async` spawn or function-local-`static`/process-global mutation shared across `TEST`s); affected: `tasks.md` §T003, `quickstart.md` §1.
- CHK009 — clarified SC-001's "several-fold" as a recorded-observation outcome, not a numeric pass/fail bar (enforced gates are FR-008/SC-006); affected: `spec.md` §Success Criteria (SC-001), `plan.md` §Performance Goals.
- CHK010 — clarified SC-005's measurement scope: the module's `-L <module>` subset wall-time (launch-overhead proxy), not a full unfiltered-preset timing, resolving a real scope mismatch against Research §D6/Tasks T009's actual measurement command. This is a criterion-meaning change (a module-subset ≤10% bound is materially stricter/different than a whole-preset ≤10% bound for a small module), applied because Research §D6 — the settled authority for this feature (no design-doc anchor) — already operationalizes SC-005 as `time ctest --preset <p> -L <module> ... assert ≤10%/preset`; the spec text was aligned to the pre-existing, settled D6 operationalization, not a new/invented stricter bar; affected: `spec.md` §Success Criteria (SC-005), `plan.md` §Performance Goals.
- CHK013 — fixed a real contradiction: FR-002 said "any test carrying a per-target `ENVIRONMENT`" is unconditionally standalone; Research §D3 (the settled /clarify-session census correction) says ENVIRONMENT is groupable when homogeneous across the bucket. Narrowed FR-002's ENVIRONMENT clause to match D3; affected: `spec.md` §FR-002.
- CHK014 — the module-count exclusion list named only `tests/abi/`, but the stated count of 23 requires excluding `tests/support/` too (also fixtures-only — headers only, no `.cpp`/`CMakeLists.txt` — verified via `find tests -maxdepth 2 -name CMakeLists.txt` = 23 against `ls tests/` = 25 subdirectories). Added `tests/support/` to the exclusion note; affected: `spec.md` §Key Entities, `data-model.md` §Test module.
- CHK024 — the `gtest_discover_tests` POST_BUILD-discovery-crash edge case was only noted, with no stated remedy. Added the remedy: carve the offending `.cpp` to standalone per the same policy as an unresolvable ODR collision (FR-012), recorded in the disposition ledger; affected: `spec.md` §Edge Cases.
- CHK021 — `.claude/commands/speckit-verify.md` (cited by Tasks §T028) does not exist (empirically verified: no such path anywhere in the parent or submodule tree); the real file is `../../../.claude/skills/speckit-verify/SKILL.md` (3 levels above the `library/` submodule cwd, parent-repo Spec-Kit skill). Corrected the path; affected: `tasks.md` §T028.

### DD-DECIDED items

None — no signed-off design-doc anchor applies to this feature (Gate A waived; no trigger path). Settled decisions cited below are `research.md` D1–D7 (the /clarify + Phase-0-census authority), referenced inline in the relevant PASS/SPEC-FIXED dispositions above (not a separate DD-DECIDED tag, per the task brief for this audit).

### WAIVED items

- CHK026 — rationale: no formal rollback procedure beyond FR-010's sequential per-module gate; every edit is a git-tracked, revertible `tests/**/CMakeLists.txt` change with no runtime/data-migration risk, so fix-forward (adjust bucket/disposition, re-run quickstart §§2–4) is an acceptable implicit remedy for a test-infra-only feature. Tagged `[Recovery]` — not Completeness/Clarity/Consistency — so WAIVED is permitted.

Anchors spot-verified: N/A (no design-doc anchor cited by this feature's spec — Gate A waived). Research §D1–D7 cross-checked against `spec.md`/`tasks.md`/`quickstart.md` text and found one contradiction (CHK013, fixed above); all other D1–D7 references resolve correctly in `research.md`.

**`/speckit-analyze` must be re-run** — 7 `SPEC-FIXED` edits were applied to `spec.md`, `plan.md`, `tasks.md`, `quickstart.md`, and `data-model.md` after the prior analyze pass (commit `3b46f3f0`), invalidating its drift-check baseline.
