# Correctness-Surface Checklist: Typed builder tier via group-Args deduplication

**Purpose**: Requirements-quality gate ("unit tests for English") over the feature's *correctness surface* — dedup-key soundness, v44 byte-invariance, and per-version completeness. Validates that these requirements are complete, unambiguous, consistent, and measurable BEFORE `/speckit-implement`. Spec-*quality* (placeholders, mandatory sections) is covered separately by [requirements.md](./requirements.md); this checklist does not re-test that.
**Created**: 2026-07-16
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [data-model.md](../data-model.md) · [contracts/generated-builder-dedup.md](../contracts/generated-builder-dedup.md) · [contracts/builder-completeness.md](../contracts/builder-completeness.md)

**Note**: Each item tests whether a REQUIREMENT is well-written, not whether code passes. `[Gap]`/`[Ambiguity]`/`[Conflict]`/`[Assumption]` mark the quality problem; `§` refs cite the anchor.

## 1. Dedup-Key Soundness (the load-bearing design decision)

- [x] CHK001 Is the dedup key defined with a single, unambiguous formula — `(no_tag, recursive structural signature)` — everywhere it appears, with no residual `no_tag`-alone framing? [Consistency, Spec §FR-001 / plan §22-28 / data-model Entity 1 / research R3] — PASS: identical formula in spec.md FR-001/Key Entities, plan.md Summary lines 22-28, data-model Entity 1, research R3; the earlier `no_tag`-alone framing is explicitly marked REFUTED in spec.md §Assumptions, not left as residual.
- [x] CHK002 Is the "recursive structural signature" itself fully specified as to its constituent fields (delimiter + ordered members + per-member required-ness + child signatures) and their ordering (declaration order, NOT tag-sorted)? [Completeness, data-model Entity 1] — PASS: data-model Entity 1 table enumerates `no_tag`, `delimiter_tag`, ordered `members` ("declaration order... NOT tag-sorted"), per-member required-ness source, and recursive child plans, plus an explicit signature formula.
- [x] CHK003 Is it explicitly stated WHY `no_tag` must be part of the key in addition to the signature (the signature omits the count tag; closes the cross-`no_tag` collision the census did not measure)? [Clarity, Spec §Key Entities / data-model Entity 1] — PASS: spec.md Key Entities + data-model Entity 1 both state the signature alone excludes the group's own count tag and that including `no_tag` keeps two distinct count tags from colliding.
- [x] CHK004 Is the required-ness source of truth named (`MessageIR.fields[tag].ref.rule`) and is it stated that required-ness is serialization-critical yet invisible in the Args struct body (lives only in `writer_traits`)? [Completeness, data-model Entity 1] — PASS: data-model Entity 1 table row states exactly this ("serialization-critical, invisible in the Args struct body... lives only in `writer_traits`").
- [x] CHK005 Is the claim "`no_tag` alone is unsound" backed by a stated, reproducible measurement (per-version distinct-plan census, max-plans-per-`no_tag`) rather than assertion? [Measurability, research R2 / Spec §Assumptions] — PASS: research.md R2 gives a per-version census table (distinct `no_tag`s, `no_tag`s with >1 plan, max plans/`no_tag`, e.g. `NoLegs`/555 = 8); spec.md §Assumptions cites the same figures as REFUTED-by-census.
- [x] CHK006 Is the reason the read-tier `no_tag`-union key does NOT transfer to builders spelled out (order- and required-ness-sensitivity; read compiling proves nothing about builder-key soundness)? [Clarity, research R2] — PASS: research.md R2 states this explicitly, including the "read tier compiling proves nothing about this" line, verbatim-consistent with spec.md §Assumptions.

## 2. Naming Rule G1a (deterministic, collision-free)

- [x] CHK007 Is the bare-vs-ordinaled naming rule stated as an exact decision procedure: exactly-one-signature ⇒ `G_<no_tag>Args`; ≥2 signatures ⇒ **all** ordinaled `G_<no_tag>_1..kArgs` with **no bare name**? [Clarity, Spec §FR-001 / contracts §G1a] — PASS: FR-001 and contracts G1a state the exact procedure with no bare name in the ≥2 case.
- [x] CHK008 Is the ordinal-assignment order defined deterministically (first-encounter over the bytewise-sorted `ir.messages` × declaration-order `group_order`)? [Measurability, contracts §G1a / research R3] — PASS: contracts G1a and data-model Entity 2 both state "first-encounter over the bytewise-sorted message list × declaration-order `group_order`", a concrete deterministic algorithm.
- [x] CHK009 Is the G1a rule stated identically across all loci it appears (Spec FR-001, data-model Entity 2, contracts G1a, tasks T007/T008) with no drift in the ≥2 wording? [Consistency, Spec §FR-001] — PASS: cross-checked all 5 loci (spec FR-001, data-model Entity 2, contracts G1a, tasks T007, T008) — identical "no bare name, all ordinaled" wording, no drift.
- [x] CHK010 Is it specified that two distinct `no_tag`s with byte-identical bodies stay SEPARATE plans (never contend for a name)? [Coverage, Spec §FR-001 / contracts §G1a] — PASS: FR-001 last sentence and contracts G1a both state this explicitly ("the key includes `no_tag`").
- [x] CHK011 Is the accepted source-breaking rename surface (v44 nested `<Msg>…Args` → `groups::G_…Args`, no aliases) quantified with a measured count rather than an unverified estimate? [Measurability, plan §Decision / tasks §T011 re-count] — PASS: plan.md §Decision defers the count to `/tasks`; tasks.md T011 gives a per-type-name breakdown (16+15+13+13+12+9+9+8+4×7+3×2+1 = 130, verified by arithmetic) across the 7 files, all 7 confirmed to exist on disk (`ls` + grep for `Args` in each).

## 3. Dependency Ordering & Cycle Bound

- [x] CHK012 Is the emission-ordering requirement stated (children fully defined before any referencing parent; post-order)? [Completeness, Spec §FR-011 / contracts §G1c] — PASS: FR-011 and contracts G1c both state post-order (children before parents).
- [x] CHK013 Is the cyclic/over-deep-reuse bound specified with a concrete discipline (read-tier `kMaxGroupDepth`-style) AND the invariant that a bounded-out edge never leaves a referenced-but-undefined Args type? [Completeness, Spec §Edge Cases / §FR-011] — PASS: spec.md Edge Cases + FR-011 both state the bound and the never-undefined-reference invariant; `kMaxGroupDepth` confirmed as a real, precedented read-tier construct (`tools/codegen/fixpp-codegen/emit_messages.cpp:137`, value 16), so the cited discipline is not aspirational.
- [x] CHK014 Is the "same group under multiple parents / multiple depths within one message references the single shared Args" edge case addressed as a requirement (not just an example)? [Coverage, Spec §Edge Cases] — PASS: stated in Edge Cases AND elevated to a formal MUST in FR-002 ("Each per-message `build_<Msg>` MUST reference the shared structural-plan Args structures"), not merely illustrative.

## 4. v44 Byte-Invariance (behavior unchanged after dedup)

- [x] CHK015 Is the v44 no-regression pin defined against a **frozen pre-077 external** corpus (QuickFIX `.fix` + 061 exemplars), and is it stated WHY a regenerated golden is insufficient (self-consistent-but-changed emitter would pass)? [Clarity, Spec §FR-008 / §US3 Independent Test] — PASS: FR-008 and US3 Independent Test both name the frozen external corpus and explicitly state a regenerated golden "would only prove the new emitter self-consistent."
- [x] CHK016 Is the differential's message coverage quantified — every distinct structural plan / every multi-plan `no_tag` (NoLegs/555, NoOrders/73, NoRelatedSym/146), or ≥ all 83 — rather than left at the 5 current exemplars? [Completeness, Spec §FR-008 / §SC-004] — PASS: FR-008/SC-004/tasks T020 all state the extension past 5 exemplars to every distinct plan/multi-plan `no_tag`, with an explicit "or at minimum all 83" floor — a bounded alternative, not an open-ended escape hatch.
- [x] CHK017 Is it explicitly stated that round-trip (build→read-back) is NOT a sufficient differential (reads scan by tag; a reordered/dropped member of a collapsed variant round-trips clean)? [Ambiguity, Spec §US3 Independent Test] — PASS: US3 Independent Test states this verbatim; repeated in FR-008 and tasks T020.
- [x] CHK018 Is the scope of what MAY change (struct identities/layout) vs what MUST NOT (serialized bytes, validation outcomes) drawn as an unambiguous line? [Clarity, Spec §FR-008 / §Assumptions] — PASS: FR-008 and §Assumptions state the line identically ("only the generated struct identities/layout change").
- [x] CHK019 Are both v44 goldens' provenance and paths specified — `official` regenerated in place (069 path), `all` newly created (no prior `all` golden exists) — and both required to pin deduped output? [Completeness, Spec §FR-007a / data-model Entity 5] — PASS: FR-007a states both paths exactly; data-model Entity 5 gives a full 5-row table with the same paths.
- [x] CHK020 Is the `FIXPP_CODEGEN_V44_FAMILIES` retention (both modes flow through the single deduped path) stated as a requirement? [Consistency, Spec §FR-007a / clarify 2026-07-16] — PASS: FR-007a MUST-retain, Clarifications Q2 "Keep both", contracts G4c all agree.

## 5. Determinism & Read-Tier Isolation

- [x] CHK021 Is byte-determinism (generate-twice-byte-compare, across runs/machines/compilers) required for **every** builder-bearing version, not just v44? [Coverage, Spec §FR-007 / §SC-004] — PASS: SC-004 states "determinism holds for every builder-bearing version"; contracts G4a/G4b apply the determinism gate across v42/v44/v50sp2/vlatest uniformly (not v44-scoped).
- [x] CHK022 Is the read-tier no-regression requirement (FR-009) defined with an explicit measurement basis, and is the narrow-vs-full choice named (full 4-artifact recursive byte-diff vs Messages-golden-only + recorded residual)? [Measurability, Spec §FR-009 / §SC-005] — PASS: FR-009 states both options explicitly and requires the residual to be recorded if narrowed; tasks T006 assigns the decision + T022 the implementation.
- [x] CHK023 Does FR-009 state WHY `codegen_determinism_test` alone AND the Messages golden alone are each insufficient (determinism ≠ pre/post identity; Fields/Validator/Reify ungoldened)? [Clarity, Spec §FR-009] — PASS: FR-009 states both reasons verbatim.
- [x] CHK024 Is the "change confined to `emit_builders.cpp` (ir.hpp/ir.cpp no-change) so read tiers identical by construction" claim required to be GATED rather than trusted? [Assumption, Spec §FR-009 / plan §Source Code] — PASS: FR-009 explicitly requires the confinement claim to be "gated, not trusted"; tasks T004 verifies the no-change claim and T022 implements the gate.

## 6. Per-Version Completeness (non-circular, exact-set, red-provable)

- [x] CHK025 Is `expected(V)` required to be derived from a raw-XML/Orchestra walk **independent of `emit_builders`** (msgtype + msgcat/category read directly from source), and is it stated WHY reading `ir(V).messages` would be circular? [Clarity, Spec §FR-010 / contracts §C1] — PASS: FR-010 and contracts C1 both state the raw-XML/Orchestra independence requirement and cite the 076 V-1/blind-corpus rationale; both cited anchors are internally unambiguous on their own (the adjacent data-model Entity 4 ambiguity found under CHK030 is a separate defect in an artifact this item does not cite).
- [x] CHK026 Is `actual(V)` defined as a compile-time address-of ODR-use of every `build_<Msg>`/`validate_<Msg>` (existence proof), with the `builder_registry` text-parse explicitly demoted to a SECONDARY check? [Completeness, contracts §C2] — PASS: contracts C2 and data-model Entity 6 both state this exactly, including the ODR-use/compile-failure mechanism and the "secondary consistency check" demotion.
- [x] CHK027 Is the assertion strength specified as **exact-set equality** (not subset/superset), and is the two-way split stated (address-of pins expected⊆actual; registry parse pins actual⊆expected)? [Measurability, Spec §FR-010 / contracts §C3a] — PASS: contracts C3a states exact-set equality plus the two-way split verbatim.
- [x] CHK028 Is the red-proof required to be a **committed test-only mutation seam** (`FIXPP_CODEGEN_DROP_BUILDER_MSGTYPE`, census target only) that is actually RUN and observed RED — not "documented in prose"? [Completeness, Spec §FR-010 / contracts §C3b] — PASS: FR-010 and contracts C3b name the exact env-var seam and the `builder_completeness_mutation_witness` test; tasks T025 requires it be run and the failure captured ("Prove it RED").
- [x] CHK029 Is the `actual ⊆ expected` leg's dependence on the unproven **co-emission invariant** (build/registry/validate emitted from one loop) recorded, per the Gate A round-3 residual P3? [Gap, plan §Gate A round 3 / tasks §T023] — DD-DECIDED §Gate A round 3 (`.specify/decisions/077-builder-args-dedup-gatea.md`): the residual is a frozen Gate-A-adjudicated decision (Codex P2 downgraded to P3 on a decisive source read that `emit_builders.cpp:715/717/718` co-emit `build_<Msg>`/registry/`validate_<Msg>` from one untouched loop, making the failure mode unreachable — verified these line numbers resolve to the registry-push/build-fn/official_msg_ids statements in the current source). It is recorded, not re-spec'd, per tasks.md T023's explicit carry-forward note (optional header-symbol-census closure named, not mandated). Not a blocker.
- [x] CHK030 Is the per-version in-scope predicate fully specified (v44 `all` = exclude {BE,BF,BW,BX,BY}; v44 `official` = frozen 33; v42/v50sp2/vlatest = full is_application; vt11 = ∅)? [Completeness, contracts §C1 / data-model Entity 4] — SPEC-FIXED: the exclusion-set table itself was correct and verified (`tools/codegen/fixpp-codegen/emit_builders.cpp:71` literally defines `kN002N003Excluded = {"BE","BF","BW","BX","BY"}`, consistent with data-model Entity 4 and research.md R4's clarification that BW/BX/BY are simply absent from FIX44 so excluding them is arithmetically a no-op — no defect there). The real defect: BOTH `data-model.md` Entity 4's closing note ("the independent predicate FR-010 derives its expected set from" directly after naming `MessageIR::is_application`, ir.hpp:99) AND `research.md` R4's closing sentence ("that the FR-010 completeness census derives its expected set from, non-circularly") carried the same literal claim — that the census reads the emitter's own IR field — which is exactly the circular derivation FR-010/contracts §C1 forbid (a `grep -n is_application` across spec/research/data-model confirmed these two as the only two occurrences of the pattern; spec.md FR-010/Key Entities were already clean). Edited BOTH `data-model.md` Entity 4's closing note and `research.md` R4's closing sentence to state explicitly that the census must NOT read `MessageIR::is_application` and instead independently re-implements the same rule on contracts §C1's standalone `raw_walk(V)`.

## 7. Per-Version Scope & Gating

- [x] CHK031 Are the per-version counts (app msgs 39/83/156/173; distinct plans 29/89/558/578) stated consistently across spec/plan/data-model/research, and is the v44 census 58-vs-read-tier-59 discrepancy assigned a reconciliation owner? [Consistency, research R2 note / plan §Scale / tasks §T005] — PASS: same counts confirmed byte-identical across plan.md §Scale/Scope, data-model Entity 2/4, research.md R3 table. The 58-vs-59 discrepancy is explicitly flagged "not yet verified against source" in research.md R2 and assigned to tasks T005 ("v44 count reconciliation... closes the 'not yet verified against source' caveat before any golden expectation is frozen").
- [x] CHK032 Is the "per-version set decided per version, NOT inherited from v44's exclusion" rule stated, with BW/BX/BY called out as genuine v50sp2 application builders? [Clarity, Spec §FR-006 / clarify 2026-07-16] — PASS: FR-006 and Clarifications Session 2026-07-16 Q3 both state this rule with the BW/BX/BY example named explicitly.
- [x] CHK033 Is vt11's no-output requirement specified as a first-class expected outcome (empty-skip, no file) AND explicitly "not an error"? [Coverage, Spec §FR-006 / contracts §G4a / §C3c] — PASS: FR-006, US2 Acceptance Scenario 2, contracts G4a and C3c all state the empty-skip/no-file/not-an-error outcome consistently.
- [x] CHK034 Is the vlatest `FIXPP_CODEGEN_FIX_LATEST` gating requirement complete for BOTH directions — emit when ON, AND a conditional OFF-clean that removes a previously-generated header (replacing 076's unconditional delete, not simply dropping it)? [Completeness, Spec §FR-012] — PASS: FR-012 states both directions explicitly; tasks T014 cites the exact CMake lines to replace, verified against source (`cmake/Codegen.cmake:330` `if(NOT FIXPP_CODEGEN_FIX_LATEST)` … line 341 `file(REMOVE ".../vlatest/Builders.hpp")` inside the ON-branch `else()` — the cited ~330-342 block is real and matches the described unconditional-delete pattern 076 left behind).
- [x] CHK035 Is the compile-resource acceptance stated as a measurable threshold (peak RSS low-single-digit GB, down from >21 GB; ~10 MB source order) with a named measurement mechanism (003 compile-bench convention, NOT Article VIII)? [Measurability, Spec §SC-001/§SC-002 / plan §Constraints] — PASS: SC-001/SC-002 give the exact thresholds; plan.md §Constraints and the Constitution Check table (Article VIII row) both name the 003 compile-bench/decision-record convention and explicitly disclaim Article VIII (runtime-only).

## 8. Cross-Cutting Consistency

- [x] CHK036 Is SC-003's "100% ... zero skips" backed by a requirement that actually exercises the FULL in-scope set per version (not one representative message), and is that coverage assigned? [Coverage, Spec §SC-003 / tasks §T016] — PASS: tasks T016 explicitly covers the FULL in-scope set for v42(39)/v50sp2(156)/vlatest(173) ("NOT one representative per version"); v44's full-83 coverage is via T020's frozen-corpus differential (stronger than round-trip); vlatest's US1 MVP smoke (T012) is explicitly labelled partial, deferring exhaustive coverage to T016 — no gap.
- [x] CHK037 Are the two constitution amendments (Article I §1 FIX-Latest builder carve-out removal; Article XVIII §7 v42/v50sp2 reclassification) documented with the exact articles/loci they touch, so the amendment can be drafted unambiguously despite no copy-ready payload? [Completeness, Spec §Dependencies / plan §Constitution Check] — PASS: spec.md §Dependencies and plan.md Constitution Check table both name Article I §1 and Article XVIII §7 (plus XVIII §2 annotation-narrowing); tasks T003 gives a 3-part (a)/(b)/(c) edit instruction. All three articles/sections verified to exist in `.specify/constitution.md` (Article I §1 at line 67-77, Article XVIII §2/§7 at line 346-362, Article XX §1 amend-then-proceed at line 376-378) at the current ratified v0.8.
- [x] CHK038 Is the "no runtime / C-ABI / Python surface change (C-ABI frozen 1.5.0)" boundary stated as an invariant the feature must preserve? [Consistency, Spec §Assumptions / plan §Constraints] — PASS: spec.md §Assumptions and plan.md §Constraints state this identically; plan.md Constitution Check Article X row confirms PASS on the same basis.

## Notes

- Check items off as validated against the spec/plan/contracts wording: `[x]`.
- A failing item = a requirement that is missing, vague, inconsistent, or unmeasurable — resolve by editing the spec/plan/contract (SPEC-FIXED), then re-run `/speckit-analyze` (checklist-audit does this automatically).
- This is the checklist `/speckit-checklist-audit` (pipeline step 9) will discipline before it unblocks `/speckit-implement`.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 36 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 1 |
| WAIVED | 0 |
| **Total** | 38 |

### SPEC-FIXED items

- CHK030 — both `data-model.md` Entity 4's closing note and `research.md` R4's closing sentence read as claiming the FR-010 completeness census derives `expected(V)` by reading the emitter's own `MessageIR::is_application` IR field — exactly the circular derivation FR-010 and contracts §C1 forbid (research.md's use of "non-circularly" as a bare adjective next to that claim made the contradiction sharper, not weaker). Edited both to state the census must NOT read `MessageIR::is_application` and instead independently re-implements the same `msgcat`/`category` rule on contracts §C1's standalone `raw_walk(V)`. Affected: `specs/077-builder-args-dedup/data-model.md` (Entity 4, closing paragraph) AND `specs/077-builder-args-dedup/research.md` (R4, closing paragraph).

### DD-DECIDED items

- CHK029 — anchor `.specify/decisions/077-builder-args-dedup-gatea.md` (Gate A round 3); rationale: the `actual(V) ⊆ expected(V)` leg's dependence on the co-emission invariant (`emit_builders.cpp:715/717/718`, one untouched loop) is a Gate-A-adjudicated residual (Codex P2 → P3 on a decisive source read that the failure mode is unreachable), recorded verbatim in tasks.md T023 with an optional (not mandated) closure path — frozen authority, not re-spec'd.

### WAIVED items

- None.

Anchors spot-verified: constitution `.specify/constitution.md` Article I §1 (line 67-77), Article XVIII §2/§7 (line 346-362), Article XX §1 (line 376-378), Appendix A (line 390-404) — all resolve at the current ratified revision v0.8. Gate A canonical record `.specify/decisions/077-builder-args-dedup-gatea.md` — resolves, round 3 converged 2026-07-16. `ir.hpp:99` (`bool is_application = false;`), `emit_builders.cpp:415-416` (`child_prefix`/`child_type`), `emit_builders.cpp:658` (`if (ir.ns != "v44") return {};`), `emit_builders.cpp:71` (`kN002N003Excluded` literal array), `cmake/Codegen.cmake:330-341` (vlatest unconditional-delete block) — all resolve as cited.
