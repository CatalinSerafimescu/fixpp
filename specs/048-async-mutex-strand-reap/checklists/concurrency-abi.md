# Checklist: Concurrency + ABI requirements quality — 048 async_mutex strand-local reap

**Purpose**: "Unit tests for the requirements" — validate that the 048 spec/plan/research/data-model/contract
state the strand-local-drain, OOM, contract-narrowing, ABI, and test-discipline requirements completely,
clearly, consistently, and measurably. Audience: Gate B reviewers. **Created**: 2026-06-22.
**Feature**: [spec.md](../spec.md) · design [research.md](../research.md) · [contracts/async_mutex-contract.md](../contracts/async_mutex-contract.md)

## Strand-local drain correctness (requirements)

- [x] CHK001 Is the drain TERMINAL condition specified completely and identically across spec/research/data-model/contract — i.e. `holders==0 ∧ in_flight_resumers_==0 ∧ both lists empty in one pass`? [Consistency, Spec §FR-001 / contract INV-D] — PASS: all four bundle artifacts carry the identical three-predicate conjunction: spec FR-001 "unified quiescence loop (break iff active_holders_count_==0 ∧ in_flight_resumers_==0 ∧ both lists empty)"; research D-2 quiescence loop body; data-model §state-diagram terminal block; contract INV-D. Consistency confirmed.
- [x] CHK002 Is it explicitly stated WHY `active_holders_count_==0` alone is insufficient (the immediate-destroy UAF), so an implementer cannot "simplify" back to a holder-only wait? [Clarity, research D-2 / P1-1] — PASS: research D-2 step 4 + P1-1 provide the UAF argument verbatim ("posted resumers still hold a raw pointer to the mutex; destruction before they run is a UAF"); quickstart "Immediate-destroy-after-reap" witness states "Reverting the unified quiescence loop to a holder-only wait must RED this." Clarity: argument present in two bundle artifacts.
- [x] CHK003 Are the reentrant-drain completion semantics defined unambiguously — a second drain AWAITS `draining_complete_` and returns the terminal result, NOT eager-ok? [Completeness, contract §cancel_and_drain.1 / FR-001] — PASS: contract §cancel_and_drain item 1 "a concurrent drain call on the same strand awaits draining_complete_ (acquire) and returns the terminal result"; quickstart "Reentrant-during-active-drain" witness "awaits the first's draining_complete_, returns the terminal result; no false-success, no early-destroy." Unambiguous in contract + quickstart.
- [x] CHK004 Is the `in_flight_resumers_` ownership rule single-sourced — `schedule_record_resume()` is the SOLE incrementer on EVERY path (reap/grant/drained-bypass/on_cancel), runner decrements after `release_ref`? [Consistency, research W-3c] — PASS: research W-3c states "sole incrementer is schedule_record_resume(); every reap/grant/drained-bypass/on_cancel path routes through it; runner decrements after release_ref." Single-sourced in research; cross-referenced in data-model member table and task T005. No inconsistency across bundle artifacts.
- [x] CHK005 Is the finalize ordering specified (`state_=not_locked` BEFORE `draining_complete_=true`, release, no co_await between) so a reentrant drainer observing completion also observes not_locked? [Clarity, data-model state diagram] — PASS: research D-2 step 5 "finalize: state_=not_locked (then) draining_complete_=true (release), no co_await between"; data-model state diagram terminal block "SET state_=not_locked → SET draining_complete_=true(release)." Ordering explicit in two bundle artifacts; reentrant-observer consequence stated.
- [x] CHK006 Is the single-winner cancel-vs-reap CAS argued (not asserted "by construction") — both on the owning strand, serialized, loser no-ops, winner schedules exactly one counted resume? [Measurability, research W-1 / FR-004] — PASS: research W-1 provides the strand-serialization argument: "cancel handler and reap both run on the owning strand → interleaving is impossible; per-waiter phase CAS (queued→{granted,cancelled}) is the single-winner gate; loser sees CAS fail, no-ops; winner calls schedule_record_resume() exactly once (counted)." Argued, not asserted. Contract §cancel_and_drain item 4 replicates the claim.
- [x] CHK007 Is the always-post (E-3) resumption discipline preserved and stated as a requirement (no inline/synchronous resume)? [Completeness, Spec §FR-005] — DD-DECIDED E-3: always-post discipline is settled in signed-off erratum E-3 at 2f:1214 ("always post resume to the executor; never inline"). Spec FR-005 and contract §cancel_and_drain item 5 cite E-3 and confirm preservation. E-3 verified to resolve at 2f:1214.
- [x] CHK008 Is the removal of `active_acquirers_count_` justified with the synchronous-initiation interleaving argument (so it is not silently reopening the 006/047 lost-wake window)? [Coverage, research W-3b] — PASS: research W-3b gives the synchronous-initiation argument: "cancel fires on the strand; acquire-path CAS (not_locked→locked) is synchronous on the strand's thread; if acquire wins the CAS before cancel fires, cancel's reap loop sees it as a holder and awaits unlock; no window to lose a wake." Argued in research W-3b; removal is not silent.
- [x] CHK009 Is the holder-yield boundedness honestly bounded by a DRAIN PRECONDITION (holders release promptly), with the two callers' satisfaction verified per-callsite — NOT claimed as an O(holder-count) bound? [Clarity, research §DRAIN PRECONDITION / FR-001] — PASS: research D-2 "DRAIN PRECONDITION: drain caller guarantees all holders release promptly … NOT an O(holder-count) convergence bound"; research W-4 per-callsite verification for both drain callers (write_gate at session.cpp:1524, seqnum at session.cpp:1542); spec FR-001 "holder-yield bounded by DRAIN PRECONDITION not O(holder-count)." Source-lines verified.

## OOM fail-closed (requirements)

- [x] CHK010 Is the `inherited_slot.assign` fail-closed recovery specified as a NORMATIVE posted ref-balance (not prose), distinct from the `store_executor` exit, going through the awaiter/stored-handler? [Clarity, research D-3 / FR-003] — PASS: research D-3 contains a normative code block (pseudocode with exact branch) showing the catch path posting via awaiter/stored-handler; explicitly flagged as "not prose." Contract §async_lock item 2 specifies the same path. Distinct from store_executor exit (research D-3 table row 1 vs row 2). Clarity: normative code + contract item present.
- [x] CHK011 Is the exact ref-balance arithmetic stated (creator + attached + scheduled-resumer → 0, freed once) so a reviewer can check it? [Measurability, research D-3] — PASS: research D-3 states "ref-balance: creator (+1) + attached (+1) + scheduled-resumer (+1) → all decremented before destroy → freed exactly once." Three summands named, arithmetic explicit; reviewer can check each site independently. Measurable.
- [x] CHK012 Is the `reaper_slot.assign` elimination stated as a consequence of removing the reaper park (not a separate fix)? [Completeness, research D-3] — PASS: research D-3 table row "reaper_slot.assign → ELIMINATED (free win: reaper park removed; no reaper coroutine to assign a slot to)." Stated as a consequence of the reaper park removal, not an independent fix. Requirement present.
- [x] CHK013 Is the DEFERRED OOM residual (L-048) honest — resume-post pre-existing; holder-yield post NEW but replacing the shipped channel `async_wait` (same class) — and NOT claimed fail-closed? [Consistency, Spec §US2 / contract INV-C] — PASS: spec US2 Limitation "L-048: resume/drain-yield post OOM-terminate is a pre-existing residual (same class as 047 L-047-2); holder-yield post is NEW but replaces the shipped channel async_wait (same terminate class), not net-new." Contract INV-C "048 does NOT claim a fully terminate-free lock path." Not claimed fail-closed. Consistent across spec + contract.
- [x] CHK014 Is it specified that 048 introduces NO new error variant (`sync_lock_alloc_failed=44` already exists) and reaps with the SHIPPED `sync_lock_aborted` (not `sync_lock_drained`)? [Consistency, data-model §error / FR-007] — PASS: data-model §error "NO CHANGE — reaped waiters keep sync_lock_aborted (shipped behavior, async_mutex.hpp:1129-1130) — NOT sync_lock_drained." Spec FR-007 "no new error variant; reaps with shipped sync_lock_aborted." Source-verified: error.hpp:83 sync_lock_aborted=43; :87 sync_lock_alloc_failed=44; :97 sync_lock_drained=46 — all pre-existing. Consistent.

## Contract narrowing (requirements)

- [x] CHK015 Does the contract narrow ONLY `cancel_and_drain()` overlap, with ordinary cross-thread `async_lock`/`unlock` explicitly PRESERVED (2f §1.1 contention seam)? [Clarity, contract §cancel_and_drain / FR-006] — PASS: contract §cancel_and_drain "NARROWED: drain-overlap with another thread's acquire/cancel/unlock is UNDEFINED BEHAVIOR"; contract §unlock "ordinary cross-thread async_lock/unlock with no drain in flight remains SUPPORTED." Spec FR-006 mirrors this. Plan Constitution Check confirms. Clarity: both narrowed and preserved scopes stated in contract.
- [x] CHK016 Is the reachability proof stated PER INSTANCE PER OPERATION — exactly 2 drain consumers (strand-confined) vs 4 lock consumers (stores never drain)? [Completeness, research D-1 / FR-007] — PASS: research D-1 provides the per-instance per-operation reachability table: write_gate (session.cpp:1524, session strand) + seqnum_mgr (session.cpp:1542, session strand) = 2 drain consumers both strand-confined; MemoryStore/FileStore = 2 lock consumers (stores never drain). Engine.cpp:1251-1283 marshals cancel to session strand. Source-lines verified directly: engine.cpp:1269-1283, session.cpp:1524/1542, seqnum_manager.hpp:146.
- [x] CHK017 Is the unsupported (INV-2 direct_executor) path documented as UNDEFINED, and is the absence of a production assertion seam acknowledged (test-only instrumented executor)? [Coverage, contract §Unsupported / FR-006] — PASS: contract §Unsupported "drain overlap with another thread's acquire/cancel/unlock (INV-2: UB — no production assertion, test-only instrumented executor)"; research D-5 "documentation-primary misuse detection; no production assertion seam (P2-4)"; quickstart "NOT barriered-to-green." All three artifacts consistent.
- [x] CHK018 Is the "drain is uninterruptible" contract change (abort path removed; no `sync_lock_aborted` return from drain) stated, and confirmed no supported caller depends on cancelling a drain? [Completeness, research D-2.2 / contract §6] — PASS: research D-2 step 2 "drain is UNINTERRUPTIBLE (close() disables cancellation at session.cpp:1334 before invoking the drain chain)"; research W-5 "no supported caller depends on cancellable drain"; contract §cancel_and_drain item 7 "drain is uninterruptible: the awaitable does not respond to cancellation_type::total." Source-verified: session.cpp:1334 reset_cancellation_state(disable_cancellation) precedes drain chain. Completeness confirmed.

## ABI / layout (requirements)

- [x] CHK019 Is the ABI claim precise — C ABI FROZEN (abidiff-clean, no enum change) but C++ `sizeof(async_mutex)` CHANGES — with NO surviving "no ABI change" wording? [Consistency, plan Constitution Check / FR-008] — PASS: plan Constitution Check "C ABI frozen; C++ layout changes → header-recompile; layout golden re-baselined — NOT 'no ABI change'"; data-model §ABI "Do NOT claim 'no ABI change'"; quickstart "expect no C-ABI change … BUT sizeof(async_mutex) CHANGES"; contract line 3 "C ABI is unchanged (no new/renumbered error code)" — accurate and qualified (no enum change only). No audited artifact carries an unqualified "no ABI change" claim. Consistent.
- [x] CHK020 Is the layout-change impact specified (embedded by value in seqnum_manager/memory_store/file_store → header-recompile) and the sizeof/alignof layout golden mandated as a witness? [Completeness, data-model §ABI / FR-008] — PASS: data-model §ABI "async_mutex embedded by value in seqnum_manager, memory_store, file_store → header-recompile-required, NOT a runtime .so break; compile-time sizeof/alignof layout golden added as witness and re-baselined"; quickstart ABI section mandates the same; task T020 mandates layout golden. Three embedders named, witness mandated.
- [x] CHK021 Is the FR-009 4→3 `atomic<shared_ptr>` consumer claim backed by the four named anchors, and flagged as a cross-feature (046) re-confirm? [Traceability, Spec §FR-009 / research New P2-N1] — PASS: spec FR-009 names four sites: engine reader_snapshot_, transport cert_source_slot_, pinset pin_snapshot, async_mutex drain_latch_ptr_. Research W-6 "removing drain_latch_ptr_ reduces 046's consumer set 4→3; re-confirmed via T025." Source-verified: engine.hpp:466, transport_factory.hpp:210, pinset.hpp:135, async_mutex.hpp:249 — all four present in current code; 048 removes drain_latch_ptr_ → 3 remain. Task T025 mandates 046 rebase re-confirm. Traceability complete.

## Test discipline (requirements)

- [x] CHK022 Is the no-enshrine rule explicit — the cross-thread witness is REPURPOSED to assert unsupported use is rejected, NOT barriered-to-green to pass? [Coverage, quickstart / `[[feedback_coverage_push_enshrines_bugs]]`] — PASS: quickstart "Repurposed cross-thread misuse witness: NOT barriered-to-green"; research D-5 cites `[[feedback_coverage_push_enshrines_bugs]]` explicitly; task T018 "cross-thread misuse witness (NOT barriered-to-green)." No-enshrine rule explicit in three bundle artifacts.
- [x] CHK023 Is the member-removal census specified as grep-exhaustive (every `active_acquirers_count_`/`drain_latch_ptr_`/`drain_latch_state` occurrence), not a fixed list? [Completeness, tasks T003 / census discipline] — PASS: task T003 "census-complete removal: grep every occurrence of active_acquirers_count_, drain_latch_ptr_, drain_latch_state_ across all translation units (not a fixed list); cite [[feedback_census_all_handrolled_scanners_before_scoping_parse_fix]]." Grep-exhaustive requirement present.
- [x] CHK024 Is the discrimination basis stated as STRUCTURAL for removed machinery + BEHAVIORAL for the new contract (not "reverting the reap fails a supported-topology behavioral test")? [Clarity, Spec Independent Test / P2-6] — PASS: spec "Independent Test" section "discrimination is STRUCTURAL for removed machinery (latch/channel/Dekker gone) and BEHAVIORAL for the new contract (in_flight_resumers_ terminal condition)"; quickstart "Discrimination is structural for the removed machinery … NOT a claim that reverting the reap fails a supported-topology behavioral witness (P2-6)." Stated precisely in two artifacts.
- [x] CHK025 Are the three NEW RED witnesses named with their discriminating condition (immediate-destroy-after-reap, reentrant-during-active-drain, on-strand-cancel-during-reap)? [Coverage, quickstart / tasks T010-T012] — PASS: quickstart names all three with discriminating conditions: "Immediate-destroy-after-reap (NEW, P1-1)" + reverting condition; "Reentrant-during-active-drain (NEW, P1-2)" + condition; "On-strand-cancel-during-reap (NEW, P1-N2)" + condition. Tasks T010/T011/T012 assign each. All three named with their discriminating hypothesis.
- [x] CHK026 Is the SC-001 stress threshold measurable (≥200 rounds × ≥25 reps, both deadlines, zero hangs)? [Measurability, Spec §SC-001] — PASS: spec SC-001 "≥200 rounds × ≥25 repetitions, both 5-second and 30-second deadlines, zero hangs." Three numeric thresholds + two deadline points + binary outcome. Fully measurable.
- [x] CHK027 Is the SC-002 OOM witness scope measurable (injected failure at inherited_slot.assign; 0 process terminations; unrelated sessions operational)? [Measurability, Spec §SC-002] — PASS: spec SC-002 "injected allocation failure at inherited_slot.assign → caller gets sync_lock_alloc_failed; 0 process terminations; unrelated sessions remain operational." Three binary predicates; quickstart "RED on the pre-fix terminate." Fully measurable.
- [x] CHK028 Is the SC-003 perf gate quantified with a baseline (vs committed main, ±5% per §VIII.2) and assigned a task? [Measurability, Spec §SC-003 / tasks T026] — PASS: spec SC-003 "≤5% regression vs committed main baseline per §VIII.2"; task T026 "bench_async_mutex contended path vs committed main baseline; ±5% threshold; record baseline file." Quantified threshold + named baseline + assigned task.

## Dependencies & assumptions

- [x] CHK029 Is the assumption "all production drain consumers + cancel-emit are strand-confined" source-anchored (engine.cpp:1255-1281; session strand) rather than asserted? [Assumption, research D-1] — PASS: research D-1 source-anchors the claim with explicit line-numbers: engine.cpp:1255-1281 (cancel marshaling), session.cpp:1524 (write_gate drain), session.cpp:1542 (seqnum drain), seqnum_manager.hpp:146 (drain → cancel_and_drain). Source-lines verified directly: engine.cpp:1269-1283 strand-marshaled emit (co_spawn *entry.session_strand, awaited); session.cpp:1524/1542 drains within close() (strand-confined per engine.cpp:1197-1202 co_spawn(session_strand)). Anchored, not asserted.
- [x] CHK030 Is the design-doc amendment matrix (E-5 + the real §-IDs §1.1/§3.1/§4.1/§4.5/§4.7.3/§4.7.4) complete, with no reference to the phantom 047 E-5/I-33? [Traceability, research D-6 / tasks T006] — PASS: research D-6 "merged doc has E-1..E-4 only (no 047 E-5/I-33); 048 adds E-5 via T006." Direct grep on 2f-async-mutex.md confirmed E-1@774, E-2@810, E-3@1214, E-4@1032; no E-5, no I-33. All six §-IDs verified to resolve: §1.1@66, §3.1@261, §4.1@302, §4.5@1119, §4.7.3@1521, §4.7.4@1544. Amendment matrix complete; T006 mandates adding E-5.
- [x] CHK031 Is the supersede-047 + 046-rebase dependency documented (047/PR #143 closed; 046 consumer set 4→3)? [Dependency, Spec Assumptions] — PASS: spec Assumptions "supersedes feature 047 (PR #143, to be closed); branched off main after 047 PR closed." Research W-6 "removing drain_latch_ptr_ reduces 046's atomic<shared_ptr> fallback consumer set 4→3; re-confirm at 046 rebase (task T025)." Quickstart "046 follow-on … drops drain_latch_ptr_." Both dependencies documented in spec + research + quickstart.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 30 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 1 |
| WAIVED | 0 |
| **Total** | **31** |

### SPEC-FIXED items

None.

### DD-DECIDED items

- CHK007 — anchor `[2f E-3 @line 1214]`; rationale: always-post resumption discipline is settled in signed-off erratum E-3 (2f:1214, TSan-diagnosed, user-authorized); spec FR-005 and contract item 5 cite E-3 explicitly as the basis; this is genuinely a frozen signed-off authority decision, not merely a bundle restatement.

### WAIVED items

None.

Anchors spot-verified: §1.1@line 66, §3.1@line 261, §4.1@line 302, §4.5@line 1119, §4.7.3@line 1521, §4.7.4@line 1544, E-1@line 774, E-2@line 810, E-3@line 1214, E-4@line 1032 — all resolve in signed-off revision `.specify/2f-async-mutex.md` v1.5/v1.6. No E-5, no I-33 present.
