# Session Send-Path AllowPosDup Strip + Inbound PossResend(97) — Requirements Quality Checklist

**Purpose**: Unit-tests-for-English over the 022 requirements — validate completeness, clarity, consistency, measurability, and coverage of spec/plan/contracts/data-model BEFORE implementation. These test whether the requirements are written correctly, NOT whether the implementation works.
**Created**: 2026-06-05
**Feature**: [spec.md](../spec.md) (session send-path + interop domain; G3 slice 2, completes S-010)

## Requirement Completeness

- [x] CHK001 Is the `AllowPosDup` knob's default AND both settings' behavior fully specified (default `false`=strip caller `43`/`122`; `true`=retain verbatim)? [Completeness, Spec §FR-006/FR-009, data-model §1] — PASS: spec.md FR-006 states default `false`=strip both tags, `true`=retain verbatim; data-model §1 repeats it in the field table; contract C1 confirms the intentional-default wire-behavior change. Both paths are measurably distinct (SC-002). No ambiguity.
- [x] CHK002 Are requirements defined for which fields the strip removes and how (the COMPLETE `43=…`<SOH> and `122=…`<SOH> fields, leaving all other fields intact)? [Completeness, Spec §FR-008/§US2 AS1, contract §C2.1] — PASS: FR-008 says "removes only complete `43=…`<SOH> and `122=…`<SOH> fields … leaving all other fields intact"; US2 AS1 repeats "removes the complete `43=…`<SOH> and `122=…`<SOH> fields"; contract C2.1 and data-model §2 transform table both confirm span-bounded complete-field excision.
- [x] CHK003 Is the auto-resend independence requirement stated explicitly (the resend path always re-adds `43=Y`+`122` regardless of the knob)? [Completeness, Spec §FR-007, contract §C3] — PASS: FR-007 is an explicit numbered requirement stating "regardless of the `AllowPosDup` knob"; contract C3 adds the structural explanation (`build_replay_frame` does not call `send_impl`); research D3 grounds it in QFcpp `Session.cpp:649-650` + QFJ `Session.java:1448-1449`. Also C3 mandates the anti-trivial-pass witness must carry `43`/`122` in the original send.
- [x] CHK004 Is the fail-closed disposition for an unvalidatable plain-`send` payload specified (which error, and that NO bytes are excised)? [Completeness, Spec §FR-008/§US2 AS4, contract §C2.4] — PASS: FR-008 specifies "fails the send closed … slot 131 `app_payload_malformed`"; US2 AS4 names "no byte excised by an unanchored match"; contract C2.4 explicitly states "return `app_payload_malformed=131` BEFORE seqnum peek/assign, SendingTime stamp, excision, or transmit" and enumerates the malformed cases; data-model §2 transform table row 4 captures "fail closed 131 — no excision". Signal and no-excision/no-seqnum invariants are both present.
- [x] CHK005 Are requirements defined for every inbound `PossResend(97)` disposition (in-seq app deliver, no-app byte-identity, `43`+`97` combo, `97` without `122`, too-high, admin)? [Completeness, Spec §FR-001..FR-005/§Edge Cases, data-model §3] — PASS: FR-001 (in-seq deliver), FR-002 (no session reject), FR-003 (no 122-required), FR-004 (43+97 combo), FR-005 (no-app byte-identity), spec Edge Cases (too-high, too-low without 43=Y, admin). data-model §3 disposition table covers all six rows with explicit "Witness assertion" column. Tasks T003 cross-checks the same set with edge-case delegation notes for too-high/admin.
- [x] CHK006 Does the spec state the observable surface (or intentional silence) for `97=Y` — i.e. no `Reject`/`Logout`/disconnect, and delivery to `fromApp` with tag 97 readable? [Completeness, Spec §FR-001/FR-002, contract §C4.1] — PASS: FR-001 says "MUST NOT reject, drop, or disconnect"; FR-002 says "MUST NOT, by itself, trigger any session-level `Reject`, `Logout`, or disconnect"; contract C4.1 adds "No `Reject`/`Logout`/disconnect results from `97`" and "tag 97 readable" in the `MessageView`; spec Key Entities and Clarifications Q2 confirm the app reads tag 97 from the full delivered `MessageView` (no new callback surface).
- [x] CHK007 Is the no-heap requirement on the strip path stated as a binding requirement with a named gate (mallocnesia LD_PRELOAD over the strip witness binary, NOT a pre-existing alloc-guard binary)? [Completeness, Spec §plan VIII.5/INV-4, contract §C2.6] — PASS: plan Constitution Check VIII.5 explicitly names "a new `send_impl`-strip no-heap cell in `test_send_allow_pos_dup_strip.cpp`" (NOT `test_session_alloc_guard`), with "binding gate = mallocnesia LD_PRELOAD"; contract C2.6 repeats "Binding gate: mallocnesia LD_PRELOAD"; research D6 explains why `counting_resource` alone is a false-pass (citing `[[feedback_tracking_pmr_resource_false_pass]]`); tasks T006/T010 specify the exact gate. The gate is named, the binary is named, and the reason the prior guard binary is insufficient is documented.

## Requirement Clarity

- [x] CHK008 Is "boundary-anchored" defined objectively (a token at start-of-payload or immediately after `\x01`) rather than left vague? [Clarity, Spec §FR-008, contract §C2.3] — PASS: contract C2.3 states "only a complete, scanner-validated `43=<value>\x01` / `122=<value>\x01` field at a field boundary (start-of-payload or immediately after `\x01`) is excised"; data-model §2 repeats "a literal `43=`/`122=` inside a value (not at a boundary) is never matched"; research D2 gives the formal definition. Objectively testable: two distinct test cases in T005/quickstart (embedded-in-value vs true-SOH-boundary).
- [x] CHK009 Is the embedded-vs-boundary distinction defined unambiguously (literal `43=`/`122=` inside another field's value with NO preceding SOH is preserved; a genuine SOH-boundary field is excised)? [Clarity, Spec §Edge Cases, contract §C2.3a/b] — PASS: spec Edge Cases states "a literal `43=`/`122=` appearing inside another field's value (no preceding `<SOH>`) is preserved — it is not a field boundary and must not be excised. A genuine SOH-boundary `43=…`<SOH> / `122=…`<SOH> field is excised under default-strip." Contract C2.3 sub-cases (a) and (b) match exactly. INV-2 codifies the injection-safe invariant. T005 item 3 requires a RED-prove hostile witness.
- [x] CHK010 Is the set of "malformed interior field" cases that must fail closed enumerated concretely (missing `=`, empty tag, non-digit tag, empty/zero-length field) rather than described generically? [Clarity, Spec §Edge Cases, data-model §2, research §D2] — PASS: spec Edge Cases enumerates all four: "missing `=` (`11BROKEN`), an empty tag (`=bad`), a non-digit tag (`4a=x`), or an empty (zero-length) field"; data-model §2 floor section repeats all four with concrete byte examples; research D2 gives the same list; contract C2.4 names "missing `=`, non-digit/empty tag, empty field"; quickstart item 5 lists the four exact test payloads. Concrete and enumerated.
- [x] CHK011 Is the relationship between the 020 floor and the 022 scanner stated precisely (the 020 floor is a six-check DENYLIST that guarantees a trailing SOH but does NOT prove per-field `<tag>=<value>` grammar; the 022 scanner owns that proof)? [Clarity, research §D2, data-model §2] — PASS: research D2 ("Decision: … a six-check denylist … there is no loop asserting every interior field is `<tag>=<value><SOH>`"); data-model §2 opens with a "Floor (the 020 validation that ran first — a six-check DENYLIST, NOT a per-field grammar)" section; plan Summary, Complexity Tracking, and Constitution Check VIII.5 all repeat the exact distinction. Contract C2.4 sources the "six-check denylist" description. Precisely stated across all artifacts.
- [x] CHK012 Is the strip ordering within `send_impl` specified (scanner/excision runs AFTER the 020 validation and BEFORE seqnum peek / SendingTime stamp / frame build; on malformed → 131 with no seqnum/stamp/excise/transmit)? [Clarity, plan §Project Structure, contract §C2.4] — PASS: plan Project Structure comment block names the insertion point "AFTER the 020 opaque-payload validation (~:2932-2990) and BEFORE seqnum peek / SendingTime stamp / frame build (~:3034)"; contract C2.4 states "return `app_payload_malformed=131` BEFORE seqnum peek/assign, SendingTime stamp, excision, or transmit"; tasks T008 specifies "inserted after the 020 validation / before seqnum/stamp/frame".
- [x] CHK013 Is "delivered to the application" for `PossResend(97)` defined via an observable (the full `MessageView` reaches `fromApp`, app reads tag 97 itself) rather than a new callback parameter? [Ambiguity, Spec §FR-001/Key Entities, research §D4] — PASS: spec Key Entities PossResend disposition entry: "the app reads tag 97 from the full delivered `MessageView`"; Clarifications Q2 → "fixpp's `fromApp` already delivers the full `MessageView`"; spec FR-001 says "MUST be delivered to the registered application"; spec Assumptions "no new callback surface"; research D4 grounds in QFcpp/QFJ ("the application … performs business-level duplicate detection on its own keys"). Observable is the `fromApp` call with the full frame, no new surface.
- [x] CHK014 Is the default-strip stated as an INTENTIONAL default wire-behavior change (not "no behavior change"), with the QFcpp/QFJ default-strip grounding? [Clarity, Spec §FR-006, contract §C1, research §D1] — PASS: contract C1 (post Gate-A round-1 fix) states "Intentional default wire-behavior change: default-strip is an intentional change for any existing caller currently passing `43`/`122` into a plain `send`… It is NOT 'no behavior change.'"; plan Gate A round-1 records the "no-behavior-change overclaim" was fixed in the round-1 rewrite; research D1 grounds QFcpp (`Session.cpp:533-537` unconditional strip) and QFJ (`Session.java:2788-2796` strip unless `AllowPosDup=true`). CHK036 provides the negative-confirmation check.

## Requirement Consistency

- [x] CHK015 Is the knob name/spelling (`AllowPosDup` config key / `allow_pos_dup` field, one `s` in `Pos`) consistent across spec, plan, data-model, and contract? [Consistency, Spec §FR-006, data-model §1, contract §C1] — PASS: spec FR-006 "knob named `AllowPosDup`"; spec Clarifications Q1 answer "`AllowPosDup` (QFJ's literal `SETTING_ALLOW_POS_DUP_MESSAGES` spelling, one `s` in `Pos`)"; data-model §1 field name `allow_pos_dup`; contract C1 "Field: `bool allow_pos_dup` … name mirrors QuickFIX-J `AllowPosDup`"; plan Summary "`+1` additive `SessionConfig` field `allow_pos_dup`". Consistent single-`s` spelling throughout. CodeGraph lookup confirmed `redeliver_poss_dup` exists at `session_config.hpp:293`; `allow_pos_dup` is not yet added (pre-implementation), which is expected.
- [x] CHK016 Is the error-slot reuse consistent everywhere (malformed framing → existing `app_payload_malformed=131`; NO new `error::core` slot claimed in any artifact)? [Consistency, research §D5, plan Constitution §X] — PASS: research D5 Decision "Reuse `app_payload_malformed = 131` (020) … add no new enum slot, no `fixpp_error_t` change"; plan Summary "no new `error::core` enum slot (`app_payload_malformed=131` already covers fail-closed framing)"; plan Constitution §X "no C-ABI surface or error-slot change; adding the public `SessionConfig` member … `app_payload_malformed=131` reused for fail-closed framing"; contract C2.4 and data-model §2 both say "return `app_payload_malformed=131`". CodeGraph confirmed `app_payload_malformed` at `session.cpp:2940,2945,2952,2962,2976,2988,3026` — the existing slot.
- [x] CHK017 Is the `97`-not-subject-to-`OrigSendingTime(122)`-rule consistent with the 021 rule that the 122-requirement keys on `43=Y` only? [Consistency, Spec §FR-003/FR-004, data-model §3] — PASS: FR-003 "MUST NOT trigger the `OrigSendingTime(122)`-required validation — that requirement (021 Arms C/D) is keyed on `PossDupFlag(43)=Y` only"; FR-004 "evaluated on `43` independently of `97`"; data-model §3 row "97=Y, no 122, no 43=Y → NOT rejected for missing 122"; contract C4.2 "not subject to the `OrigSendingTime(122)`-required rule (that keys on `43=Y` only)". Consistent with 021's Arms C/D implementation which fires on `43=Y` presence only.
- [x] CHK018 Is the no-heap claim consistent across artifacts about WHERE the stripped payload lives (ONE bounded strip stack scratch distinct from the framer's `body_buf`; in-place compaction NOT realizable against the `const std::span` input)? [Consistency, research §D6, data-model INV-4, contract §C2.6] — SPEC-FIXED: quickstart.md item 8 attributed the binding no-heap gate to `test_session_alloc_guard` — contradicting plan Constitution §VIII.5, data-model INV-4, research D6, contract C2.6, and tasks T006/T010, which ALL specify the binding gate is a new cell inside `test_send_allow_pos_dup_strip.cpp`. Edited quickstart.md §US2 item 8 to name `test_send_allow_pos_dup_strip.cpp` as the binding gate and document why `test_session_alloc_guard` is insufficient (covers only 005-era seqnum+admin paths, never exercises `send_impl`). After this fix all artifacts are consistent: the strip scratch is ONE named bounded stack array distinct from `body_buf`; in-place compaction is not realizable; binding gate = mallocnesia LD_PRELOAD in `test_send_allow_pos_dup_strip.cpp`.
- [x] CHK019 Is the oversize disposition consistent (an oversized strip scratch returns the EXISTING `wire_frame_too_large`, not a new 022 path)? [Consistency, plan §Technical Context, contract §C2.6] — PASS: plan Technical Context "an oversized one returns `wire_frame_too_large` (the EXISTING send-path oversize disposition the framer's `body_buf` overflow already owns at `session.cpp:3034` — NOT a new path added by 022; the strip scratch is sized identically)"; contract C2.6 "An oversized payload returns `wire_frame_too_large` — the EXISTING send-path oversize disposition the framer's `body_buf` overflow already owns (`session.cpp:3034`); the strip scratch is sized identically, so 022 adds no new oversize path". CodeGraph confirmed `wire_frame_too_large` at `session.cpp:3055,3063`. Consistent — existing path, no new slot.

## Acceptance Criteria Quality & Measurability

- [x] CHK020 Are the success criteria measurable without implementation detail (e.g., "100% of plain sends emit outbound bytes free of tags 43/122 at default")? [Measurability, Spec §SC-002] — PASS: SC-001 "100% of cases … processes it … delivers it … stays established"; SC-002 "100% of plain `send` calls whose payload contains `43`/`122` emit outbound application bytes free of tags 43 and 122" vs "both tags are passed through unchanged"; SC-003 "100% of replayed messages … emits … `PossDupFlag(43)=Y` + `OrigSendingTime(122)`"; SC-004 "no byte excised by an unanchored match"; SC-005 "interops cleanly … live interop cells green". All are observable outcomes (byte assertions, seqnum state, callback invocation count) with no internal implementation detail.
- [x] CHK021 Is SC-003 (resend re-adds 43/122 regardless of knob) tied to an objectively checkable artifact (the replayed `build_replay_frame` bytes)? [Measurability, Spec §SC-003, contract §C3] — PASS: SC-003 "zero regression in retransmission flags"; contract C3 explicitly names the artifact "`build_replay_frame`, `session.cpp:1186-1239`" and explains structural independence (it does not call `send_impl`); quickstart item 7 and tasks T006 item 7 both require the test to capture the replayed frame bytes and assert `43=Y`+`122` present. Objectively checkable from the replay frame bytes.
- [x] CHK022 Is SC-004 (fail-closed on malformed payload) measurable against a concrete signal (slot 131 returned, zero bytes excised, no seqnum consumed)? [Measurability, Spec §SC-004, contract §C2.4] — PASS: SC-004 "no byte excised by an unanchored match, matching the 020 opaque-payload validation floor"; contract C2.4 names the three invariants (131 returned, no seqnum consumed, nothing transmitted, NO excision); tasks T006 item 6 parameterizes over four exact input cases and asserts all three properties; quickstart item 5 names "assert `app_payload_malformed=131`, the outbound seqnum is NOT consumed, nothing is transmitted, and NO excision occurred". Concrete and triple-checked.
- [x] CHK023 Does each US acceptance scenario state a Given/When/Then that can be objectively evaluated from observable wire/state output? [Acceptance Criteria, Spec §US1/US2] — PASS: all US1 scenarios (AS1–AS3) follow the explicit Given/When/Then format in the spec; US2 scenarios (AS1–AS4) do likewise. Each "Then" clause names observable output: seqnum N→N+1, `fromApp` called, no `Reject`/`Logout`/disconnect, framed bytes free of tags 43/122, etc. No acceptance scenario is stated only in terms of internal implementation.
- [x] CHK024 Is the FR-007 witness's anti-trivial-pass requirement stated (the original send MUST have carried `43`/`122` so the strip provably ran)? [Measurability, contract §C3, Spec §US2 AS3] — PASS: contract C3 explicitly mandates the anti-trivial shape: "the FR-007 witness MUST (a) set `allow_pos_dup=false`, (b) send a payload that contained `43`/`122` (so the strip provably ran and removed them from the stored frame), then (c) drive the resend"; quickstart item 7 specifies "send a payload that contained `43`/`122` (strip provably removed them)"; tasks T006 item 7 repeats "the original send MUST carry 43/122 or the witness passes trivially".

## Scenario & Edge-Case Coverage

- [x] CHK025 Are requirements defined for the no-op-when-absent case (a payload with no `43`/`122` emitted byte-identically under either knob setting)? [Coverage, Spec §Edge Cases, contract §C2.5] — PASS: spec Edge Cases "Strip when the field is absent: a plain `send` with no `43`/`122` in the payload is emitted unchanged under either knob setting (no-op strip)"; contract C2.5 "a payload with no `43`/`122` is emitted unchanged under either knob setting"; data-model §2 transform table row 2 (false, neither present → no-op); quickstart item 6 and tasks T005 item 5 both cover this case.
- [x] CHK026 Are the embedded-`43=`-in-a-value hostile case AND the true SOH-boundary case both covered as distinct requirements? [Coverage, Spec §Edge Cases, contract §C2.3] — PASS: spec Edge Cases states both explicitly as distinct cases; contract C2.3 sub-cases (a) and (b) name them individually with byte examples; data-model §2 transform table rows 3 (embedded) and 1 (SOH-boundary) are separate entries; quickstart items 3 and 4 are distinct tests; tasks T005 items 3 and 4 are separate witnesses with RED-prove requirement for the hostile case.
- [x] CHK027 Are the `97=Y` out-of-band edge cases (too-high seqnum, too-low without `43=Y`, on an admin message) addressed with explicit dispositions deferring to existing paths? [Coverage, Spec §Edge Cases, data-model §3] — PASS: spec Edge Cases three bullets: too-high ("handled by the existing too-high/resend-request path; PossResend does not alter the too-high disposition"), too-low-without-43=Y ("follows the existing too-low disposition … the 021 Arm-B fatal too-low path — `97` alone does not grant too-low tolerance"), admin ("processed on the existing admin path and `97` has no effect"); data-model §3 disposition table rows 5 and 6 match exactly; tasks T003 explicitly requires edge-case coverage or documented delegation note for too-high/admin.
- [x] CHK028 Is role-symmetry (initiator AND acceptor) stated as a requirement for BOTH behaviors, not assumed? [Coverage, Spec §FR-010/§Edge Cases, contract §C5] — PASS: FR-010 "Both the inbound `PossResend(97)` disposition and the `AllowPosDup` send-path knob MUST interop cleanly … in both initiator and acceptor roles"; spec Edge Cases "Both roles: both behaviors must hold whether fixpp is the initiator or the acceptor"; contract C5 "both behaviors … in initiator and acceptor roles"; tasks T011 and T012 both specify "both roles, skip-without-counterparty".
- [x] CHK029 Is the `43=Y`+`97=Y` combined-message disposition specified (021 PossDup arms evaluate on `43` only; `97` adds no further session-level reject)? [Coverage, Spec §FR-004/§US1 AS3, contract §C4.3] — PASS: FR-004 "021 session-level PossDup disposition (Arms A–E) MUST be evaluated on `43` independently of `97`; `97` MUST add no additional session-level reject"; US1 AS3 repeats as Given/When/Then; data-model §3 row "43=Y and 97=Y → 021 PossDup arms (A–E) evaluated on `43` only; `97` adds nothing"; contract C4.3 exact match; tasks T003 item 3 is the witness.

## Interop Requirements Quality

- [x] CHK030 Do the interop requirements specify both counterparty engines (QFcpp v1.16.0, QFJ 3.0.1) AND both roles, with the skip-without-counterparty contract? [Completeness, Spec §FR-010/SC-005, quickstart §Live cells] — PASS: FR-010 "QuickFIX-cpp v1.16.0 and QuickFIX-J 3.0.1 … in both initiator and acceptor roles"; SC-005 repeats both engine names; quickstart Live cells section names both cells with "skip-without-counterparty"; tasks T011/T012 explicitly state "both roles, skip-without-counterparty" and reference "same skip mechanism as the 018/020 cells".
- [x] CHK031 Is the wire-capture mechanism for the AllowPosDup interop cell specified (engine-log-seam, 016 P4 discipline) so "outbound bytes free of 43/122" is objectively assertable? [Clarity, quickstart §Live cells, contract §C5] — PASS: quickstart Live cells "capture outbound bytes via the engine-log-seam (016 P4)"; tasks T011 "capture outbound bytes via the engine-log-seam (016 P4 discipline), assert no boundary `43`/`122` in the app portion"; contract C5 "engine-log-seam capture, 016 P4 discipline". The capture mechanism is named, the 016 precedent is cited, and the assertion is "no boundary `43`/`122` in the application portion" (byte-level check, not a QF-acknowledgement proxy).

## Dependencies & Assumptions

- [x] CHK032 Is the US1 witness-only assumption (zero production code for `PossResend(97)`) stated AND grounded against both reference engines (neither references tag 97 in its session layer)? [Assumption, Spec §Assumptions, research §D4] — PASS: spec Assumptions "No explicit `PossResend(97)` handling exists today (verified — zero `src`/`include` reference to tag 97), and none is needed … neither QuickFIX-cpp nor QuickFIX-J handles `PossResend` in its session layer (the field is defined but never read in `Session.cpp`/`Session.java`)"; research D4 is fully grounded ("QuickFIX-cpp defines `PossResend` (`FixFieldNumbers.h:37`) but never references it in `Session.cpp`"; "QuickFIX-J has no `PossResend` reference anywhere in `quickfixj-core` session code"); tasks T004 explicitly says "if any witness FAILS, STOP: D4 is falsified — escalate". Both engines named, both grounded by source line.
- [x] CHK033 Is the "no new persistent store / no codegen / no new error slot / reuse existing machinery" boundary stated and bounded? [Assumption, Spec §Assumptions, plan Constitution §X] — PASS: spec Assumptions "No new persistent store, codegen, or error slot is anticipated"; plan Summary "No new infrastructure, no new module, no codegen, no new `error::core` enum slot … no new public C-ABI surface"; plan Constitution §X "no C-ABI surface or error-slot change; adding the public `SessionConfig` member … `app_payload_malformed=131` reused". All four negative constraints named; the single additive `allow_pos_dup` field is the only addition.
- [x] CHK034 Is the reuse of the 020 floor + `has_boundary_token` (as the boundary-rule PRECEDENT only, NOT the excision primitive — it is bool-only) documented so the scanner ownership is not misattributed? [Assumption, plan §Summary, data-model §4] — PASS: plan Summary "has_boundary_token (injection-safe by construction; the 020 RC#1 hazard)" — cited as precedent; plan Project Structure source comment "`has_boundary_token` (:2917) is the boundary-rule PRECEDENT only, not the strip primitive (it is bool-only)"; data-model §4 "has_boundary_token … bool-only boundary-token check … cited as the boundary-rule precedent only — it cannot produce field spans, so it is NOT the excision primitive; the 022 scanner is new"; research D2 "(bool-only) … it is retained only as the boundary-rule precedent, not the excision primitive"; contract C2.4 cites `has_boundary_token` only as precedent. Ownership is unambiguous across all artifacts.
- [x] CHK035 Is S-010 completion documented to enumerate BOTH halves (`43`/`122` inbound via 021; `PossResend(97)` + `AllowPosDup` knob via 022) so the row cannot be mis-closed as a 97-only close? [Dependency, Spec §FR-011, plan §VI delta] — PASS: FR-011 "This feature MUST complete catalogue row S-010, flipping it `backlog → done` once `PossResend(97)` inbound semantics and the `AllowPosDup` send-path knob ship"; plan §VI delta states the completion note must enumerate "BOTH halves (43/122 inbound via 021; PossResend(97) inbound witness-confirmed + the AllowPosDup send-path knob FR-008 via 022)"; tasks T013 and T015 both require the "BOTH halves" language; plan §VI delta also confirms "S-033 (021) is already `done` and is NOT re-touched, and that S-010's done-note enumerates BOTH halves … so it cannot be misread as a 97-only close".

## Ambiguities & Conflicts

- [x] CHK036 Does any artifact still carry a "no behavior change" overclaim that conflicts with the intentional default-strip wire change (a Gate-A round-1 fix)? [Conflict, contract §C1, plan Gate A] — PASS: Confirmed no "no behavior change" overclaim survives. Contract C1 explicitly states "It is NOT 'no behavior change.'"; plan Gate A round-1 records the overclaim was fixed in the rewrite; plan Summary "default-strip is an intentional default wire-behavior change for plain sends containing caller-supplied 43/122"; data-model §1 doc-comment "default-strip is an intentional default wire-behavior change for plain sends containing caller-supplied 43/122 (matching QF defaults)". All artifacts are consistent on the intentional-change framing.
- [x] CHK037 Does any artifact still carry the unreachable missing-SOH / over-excision framing that Gate A rounds 2–3 deleted (the 020 floor guarantees a trailing SOH at `session.cpp:2951`, so no run-off-the-end case exists)? [Conflict, research §D2, plan Gate A r2/r3] — PASS: Confirmed no stale missing-SOH / over-excision framing remains. Plan Gate A r2 records deletion of this framing "from all sites incl. normative contract C2.4"; plan Gate A r3 records "spec.md Edge Case stale missing-SOH framing deleted (sole surviving site)". Reading the current contract C2.4: it says "the scanner's job is to reject interior fields malformed in ways the 020 denylist admits … BEFORE excision, so excision runs only over a proven-well-formed sequence — not to prevent run-off-the-end, which cannot occur behind the trailing-SOH floor" — this is the correct framing. No artifact scanned for the session retains the removed framing.
- [x] CHK038 Are all `[FIX-SL §X]` / `[const §X]` references exact (not vague), per `[const §VI.5]`? [Traceability, Spec §Normative References] — PASS: Spot-verified all cited anchors (see Audit Result). `[FIX-SL §4.8.4]` resolves to coverage-index.md line 80 "§4.8.4 Possible duplicates (PossDupFlag semantics)" and is the S-010 anchor at `feature-catalogue.md:30`. `[FIX-SL §4.5.4]` resolves to coverage-index.md (Rejecting invalid messages). `[const §VI.5]` resolves to constitution.md Article VI §5 ("Normative References section listing the exact `[DocAbbrev §X.Y.Z] Title` entries"). All plan/contract `[const §X.Y]` refs resolved to actual constitution article+subarticle numbers (VIII §5 = Article VIII §5 allocator policy; X = Article X ABI policy; XV.9 = Article XV banned patterns §9 std::mutex; XV.15 = Article XV §15 message drops; XVI.3/XVI.4 = Article XVI §3/§4 /clarify and /analyze; XVII.1 = Article XVII §1 Gate A). No vague references found.

---

## Audit Result

*Dispositions filled by `/speckit-checklist-audit` — pipeline step 9. Audited 2026-06-05.*

| Disposition | Count |
|---|---|
| PASS | 37 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **38** |

### SPEC-FIXED items
- CHK018 — quickstart.md §US2 item 8 attributed the binding no-heap gate to `test_session_alloc_guard` instead of the correct `test_send_allow_pos_dup_strip.cpp`; edited `specs/022-possresend-allowpossdup-send/quickstart.md` item 8 to name the correct binary and document why the old attribution is wrong (005-era guard never exercises `send_impl`). Affected: `specs/022-possresend-allowpossdup-send/quickstart.md:16`.

### DD-DECIDED items
*(none)*

### WAIVED items
*(none)*

Anchors spot-verified:
- `[FIX-SL §4.8.4]` — resolves in `spec/coverage-index.md` line 80 "§4.8.4 Possible duplicates (PossDupFlag semantics)"; `feature-catalogue.md:30` S-010 row.
- `[FIX-SL §4.5.4]` — resolves in `spec/coverage-index.md` (Rejecting invalid messages section; referenced only as confirmation that `97=Y` produces no Reject).
- `[const §VI.5]` — resolves at constitution.md Article VI §5 ("Normative References section listing the exact `[DocAbbrev §X.Y.Z] Title` entries").
- `[const §VIII.5]` — resolves at constitution.md Article VIII §5 (allocator policy: "zero `new`/`delete` between parse and `fromApp` callback").
- `[const §X]` — resolves at constitution.md Article X (ABI Policy).
- `[const §XI.4]` — resolves at constitution.md Article XI §4 (application threading default: per-session strand).
- `[const §XIV.2]` — resolves at constitution.md Article XIV §2 (pluggable interfaces: ≤5 pure-virtual).
- `[const §XV.9]` — resolves at constitution.md Article XV §9 ("std::mutex in coroutine context" banned pattern).
- `[const §XV.15]` — resolves at constitution.md Article XV §15 (application-layer message drops banned).
- `[const §XVI.3]` — resolves at constitution.md Article XVI §3 (`/clarify` mandatory before `/plan` for the named trigger set).
- `[const §XVI.4]` — resolves at constitution.md Article XVI §4 (`/analyze` mandatory).
- `[const §XVII.1]` — resolves at constitution.md Article XVII §1 (Gate A before `/tasks`).
All anchors resolve in the signed-off constitution.md v0.1.
