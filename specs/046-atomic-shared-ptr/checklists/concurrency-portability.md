# Checklist: Concurrency & Portability Requirements Quality — 046-atomic-shared-ptr

**Purpose**: Unit-tests-for-English — validate that the REQUIREMENTS (spec FR/SC, contracts CT-1..CT-4, data-model E-1..E-7) for the libc++ fallback integration are complete, clear, consistent, measurable, and cover the concurrency/portability edge cases. NOT implementation tests.
**Created**: 2026-06-21
**Feature**: [spec.md](../spec.md) · derived from FR-001..015, SC-001..006, contracts, data-model E-4a

## 1. Concurrency correctness of the type-erased fallback

- [x] CHK001 Is the publish/acquire guarantee (a reader `load(acquire)` observes either the prior or the new fully-constructed `shared_ptr`, never torn/null) stated as a testable requirement for all four consumers? [Completeness, Spec §FR-008 / §INV-3 / data-model E-1] — PASS: FR-008 states the release-store/acquire-load pair for all four consumers; INV-3 (data-model E-1) states "A `load` never returns a torn/partial value"; contract C-2 binds it as a testable behavioral contract with T-API-6 + row 6-consumers tests covering all four. refs: spec.md §FR-008, data-model E-1 INV-3, contract C-2, plan row 6-consumers.
- [x] CHK002 Is the CAS-equivalence rule defined precisely enough to test (success iff stored-ptr equality AND owner equality; aliasing-same-raw-diff-ctrl-block ⇒ unequal; null-null ⇒ equal; `expected` updated on failure)? [Clarity, Spec §Edge Cases / research D-7 / contract atomic-shared-ptr-api] — PASS: spec.md §Edge Cases enumerates all four discriminators verbatim; contract C-3 repeats them with the four-case breakdown; T-API-4 / plan row 5 tests the three-discriminator CAS equivalence (distinct-object FAIL / aliasing-same-raw-diff-ctrl FAIL / shared-ownership SUCCEED / null-null SUCCEED / expected-update on fail). refs: spec.md §Edge Cases, contract C-3, plan row 5.
- [x] CHK003 Is the memory-order obligation distinguished between consumers — async-mutex-specific I-23/I-13/I-24 vs the plain release/acquire pair for the other three — rather than over-generalized? [Consistency, Spec §FR-008] — PASS: FR-008 explicitly distinguishes: "The I-23/I-13/I-24 labels are async-mutex-specific invariants [006 data-model I-23/I-24] (the drain_latch_ptr_ drain path only); the other three consumers … carry no I-23/I-13 — they each rely on the plain release/acquire pair stated here." Cross-feature anchor verified: 006 data-model I-23 (line 273) and I-24 (line 274) exist and describe exactly the drain_latch_ptr_ release/acquire path. refs: spec.md §FR-008, 006 data-model.md lines 273–274.
- [x] CHK004 Are refcount-integrity requirements specified for the fallback (weak_ptr snapshot → `store(nullptr)` → expired; no leaked strong ref / UAF)? [Coverage, plan Integrated test inventory row 3] — PASS: contract T-API-5 names "refcount integrity / no-UAF under contention (weak_ptr snapshot → store(nullptr) → expired()), ASan-clean"; plan row 3 (`atomic_shared_ptr_refcount_integrity`, forced-fallback, ASan) is the concrete test. refs: contract T-API-5, plan row 3.
- [x] CHK005 Is the no-nested-lock / no-deadlock property documented where two operands may hash to the same shard (e.g. `pinset` move/copy `store(load())`)? [Edge Case, research D-5 edge note] — PASS: research D-5 documents the nested-lock edge case and confirms it is safe because the shard-guard is non-movable and the fallback's `load` + `store` are separate ops each acquiring only one shard lock sequentially (no nested acquisition). The edge is documented as an acknowledged limitation, not a latent defect. refs: research.md D-5.
- [x] CHK006 Is the `noexcept` contract of the shard-guard ctor (terminate-on-`lock()`-throw, matching `std::atomic`'s no-throw contract) stated as a requirement, not left implicit? [Clarity, data-model E-3 / Clarifications 2026-06-21] — PASS: spec.md Clarifications 2026-06-21 (session Gate A round 1) states: "Keep noexcept; a throw from lock() std::terminates, by design." data-model E-3 elaborates: "The noexcept ctor std::terminates if std::mutex::lock() throws — deliberate std::atomic no-throw match." Contract C-5 records it as "terminate-on-lock-failure … recorded decision … not an oversight." refs: spec.md §Clarifications 2026-06-21, data-model E-3, contract C-5.
- [x] CHK007 Are the omitted P0718 surfaces (`wait`/`notify[_one|_all]`) explicitly declared out of scope WITH a stated basis (no consumer uses them)? [Completeness, Spec §FR-001 / data-model E-1 / D-5] — PASS: spec.md §FR-001 names the API as "P0718 minus wait/notify[_one|_all]"; data-model E-1 states "Omitted (vs P0718): wait / notify_one / notify_all — documented gap; no consumer uses them (D-5)"; contract surface section states "NOT provided: wait, notify_one, notify_all (documented gap; no consumer depends on them)." The basis (no consumer uses them) is stated. refs: spec.md §FR-001, data-model E-1, contract surface, research D-5.

## 2. Awaitable-header mutex-cleanliness / no-amendment thesis

- [x] CHK008 Is "the header carries no `std::mutex` token on either standard library" stated as an objectively checkable requirement (vs an aspiration)? [Measurability, Spec §FR-012 / contract CT-2] — PASS: FR-012 (as amended by this audit for CHK011) states the header MUST NOT introduce any of the six banned types; contract CT-2 pins a `static_assert`/grep confirmation as the acceptance criterion; the §XV.9 corpus gate (CT-1 + CT-1b/c) provides the automated falsifiability. refs: spec.md §FR-012, contract CT-2.
- [x] CHK009 Is the corpus-gate falsifiability requirement specified — that the libc++ leg must actually run under libc++ (positive `_LIBCPP_VERSION` probe) and RED on empty/misconfigured output, not silently pass? [Clarity, contract CT-1c / plan Project Structure] — PASS: contract CT-1c explicitly requires: "asserts each supplied header emitted the asio/awaitable.hpp marker (so a libc++ misconfig → empty output → RED, never silent-pass)" AND "a positive _LIBCPP_VERSION-active probe: preprocess `#include <version>` with `-stdlib=libc++ <preset-clang>` + `-dM`, then grep the macro dump for `_LIBCPP_VERSION`; RED-fail if it is absent." plan Project Structure repeats the requirement. refs: contract CT-1c, plan §Project Structure libc++-leg ctest comment.
- [x] CHK010 Is the mutation-discrimination requirement (a project-local transitive include of a banned mutex type MUST make the gate fire, on BOTH stdlib legs) defined? [Coverage, contract CT-1d / New-P3-1] — PASS: contract CT-1d states "a fixture in which a project-local transitive include introduces a banned mutex type into an awaitable header → the gate MUST fire … The fixture MUST run on BOTH the libstdc++ leg … AND … the libc++ leg." tasks T022 implements CT-1d as part of the corpus-gate hardening. refs: contract CT-1d, tasks T022.
- [x] CHK011 Are the FR-014 "six banned mutex types" enumerated so the header-cleanliness check is unambiguous about what it forbids? [Clarity, Spec §FR-012/FR-014] — SPEC-FIXED: Three artifacts carried the ambiguous "FR-014 six banned mutex types" label, where 046's own FR-014 defines catalogue recording obligations (not the six types). All three fixed: (1) spec.md §FR-012 — enumerated all six inline with references to the corpus gate script and Constitution; (2) contracts/type-erased-lock-and-awaitable-cleanliness.md §Goal — redirected to "FR-012"; (3) tasks.md T002 — redirected to "FR-012". refs: spec.md §FR-012, contracts/type-erased-lock-and-awaitable-cleanliness.md §Goal, tasks.md T002 (all edited).
- [x] CHK012 Is the no-amendment claim internally consistent across all artifacts (no doc still implies a constitutional amendment or exemption)? [Consistency, plan §Constitution Check / research D-2 / data-model E-5] — PASS: plan §Constitution Check XI §3 row: "PASS (via FR-012 type-erasure) … No exemption, no amendment." research D-2 documents the amendment alternative as "considered and rejected." data-model E-5: "Article XI §3 / XV §9 stay satisfied as-is — no amendment, no exemption, no constitution version bump." contract type-erased-lock heading: "Replaces the (rejected) Article XI §3 amendment." All consistent. refs: plan §Constitution Check, research D-2, data-model E-5, contract type-erased-lock-and-awaitable-cleanliness.md.

## 3. libc++ build / portability + OTel toggle realizability

- [x] CHK013 Is "full functional suite passes at least once under libc++" given a precise, measurable definition of "full" under both the OTel-ENABLED primary and OTel-OFF fallback configurations? [Measurability, Spec §FR-007/FR-011/SC-002] — PASS: SC-002 states: "Primary config = OTel ENABLED under libc++ (the full suite incl. OTel targets); if opentelemetry-cpp proves unbuildable under libc++ this cycle, 'full suite' means the full OTel-OFF-configuration suite (the paired with_otel/FIXPP_BUILD_OTEL options, FR-011)." FR-011 defines "full" as "full functional suite once" under the chosen config with "no new failures attributable to the migration." Each definition is precise and measurable. refs: spec.md §SC-002, §FR-011.
- [x] CHK014 Is the OTel toggle specified as a realizable paired Conan+CMake option that fails configuration on disagreement (not a CMake-only flag that cannot drop an unconditional Conan requires)? [Clarity, Spec §FR-011 / research D-4] — PASS: FR-011 specifies "a paired Conan option (fixpp:with_otel, default ON — moves the opentelemetry-cpp member into the conditional def requirements(self)) and a matching FIXPP_BUILD_OTEL CMake option (default ON, preserving every existing build) that fail configuration if they disagree." The Conan requires conditionality is explicitly modeled. tasks T014 implements both halves. refs: spec.md §FR-011, tasks T014.
- [x] CHK015 Is the public-API-invariance requirement under OTel-OFF stated (only `engine.cpp` gated; `engine.hpp`/`engine_config.hpp` unchanged) with its dependency-clean basis? [Consistency, Spec §FR-011/FR-015 / research D-4] — PASS: FR-011 source-verifies: "engine.cpp:33/1424/1427 — this is the only OTel coupling that breaks under OFF; it is .cpp-only, so the public engine.hpp is unchanged and FR-015 holds. Verified exhaustively: EngineConfig is held by value in engine.hpp … engine_config.hpp pulls no opentelemetry/* header — so neither public header drags the OTel dependency." FR-015 states "no new public API type." tasks T014 bounds the guard to engine.cpp only. refs: spec.md §FR-011, §FR-015, tasks T014.
- [x] CHK016 Is the dependency set to rebuild under libc++ censused from the ACTUAL active Conan options (not a stale list), and is the per-dep scope-out path defined for any that won't port? [Completeness, research D-3/D-4] — PASS: research D-3 provides the libc++ profile dependency census (asio header-only = free; opentelemetry-cpp is the build watch-item) derived from the active Conan options. research D-4 (re-censused 2026-06-21) defines the scope-out path: if OTel proves unbuildable under libc++ this cycle, the non-OTel configuration is used and OTel-under-libc++ is recorded as a tracked follow-up. refs: research.md D-3, D-4.
- [x] CHK017 Is the CI-lane tier (Tier-2 / opt-in, label-triggered) and ongoing scope (concurrency-relevant subset under sanitizers) specified with the subset enumerated? [Clarity, Spec §FR-011 / tasks T021] — PASS: FR-011 states "Tier-2 / opt-in CI lane (label-triggered, like the existing windows-msvc lane; not blocking on every PR). The lane MUST build the library + run the concurrency-relevant test subset under the available sanitizers." tasks T021 enumerates the subset explicitly: "the CTest sync label scope: all tests/sync/ targets + the four per-consumer publish/acquire witnesses from T008 + the forced-fallback link tests from T019 (label each accordingly)." refs: spec.md §FR-011, tasks T021.
- [x] CHK018 Is the macOS lane (FR-011a) clearly scoped OUT of this feature's acceptance with an explicit sequencing condition (after the Linux lane is green)? [Coverage, Spec §FR-011a / Assumptions] — PASS: spec.md §FR-011a: "a committed follow-up, NOT in this feature's core delivery … gated on the Linux linux-clang-libc++ lane (FR-011) being green first." §Assumptions: "macOS macos-14+ runner is a committed follow-up sequenced AFTER the Linux lane is green." SC-001 explicitly states "macOS lane compile is the FR-011a deferred follow-up, not part of this feature's acceptance." refs: spec.md §FR-011a, §Assumptions, §SC-001.

## 4. Consumer-migration completeness + census-regrowth

- [x] CHK019 Is the consumer census exact (all four members named with file+line) and is exact-set completeness — not subset — the stated acceptance? [Completeness, Spec §FR-004/SC-004 / data-model E-4] — PASS: data-model E-4 table names all four with exact file:line — `drain_latch_ptr_` (async_mutex.hpp:249), `snapshot_` (pinset.hpp:135), `cert_source_slot_` (transport_factory.hpp:210), `reader_snapshot_` (engine.hpp:466) — all source-verified. spec.md §FR-004 states "ALL four production consumers … MUST be migrated … After migration, no production header or source file may contain a raw std::atomic<std::shared_ptr<…>> outside the primitive's own header" (exact-set completeness). SC-004 states "100% of the production raw occurrences (the four census'd consumers) are migrated." refs: data-model E-4, spec.md §FR-004, §SC-004.
- [x] CHK020 Is the census-regrowth guard's scope unambiguous (what it scans, what it excludes — the primitive header, tests, third-party)? [Clarity, Spec §FR-005 / data-model E-7] — PASS: data-model E-7 and contract census-regrowth-guard.md §Behavior both state: "Scan scope: project-owned include/** + src/**. Exclude: the primitive header include/fixpp/core/sync/detail/atomic_shared_ptr.hpp (the legitimate sole site), tests/** fixtures … third-party/Conan paths." The scope is explicitly bounded on both inclusion and exclusion sides. refs: data-model E-7, contract census-regrowth-guard.md.
- [x] CHK021 Is the guard's discrimination requirement defined (it MUST fail on an injected raw `std::atomic<std::shared_ptr>` re-introduction — mutation-verified)? [Measurability, Spec §SC-004 / research D-6] — PASS: SC-004 states "the census-regrowth guard demonstrably fails on an injected raw re-introduction (exact-set completeness, mutation-verified)." contract census-regrowth-guard.md §Acceptance: "Inject a raw std::atomic<std::shared_ptr<int>> x; into a project header → guard exits non-zero naming that file:line (RED-proves the guard discriminates). Revert." tasks T024 includes the mutation test. refs: spec.md §SC-004, contract census-regrowth-guard.md §Acceptance, tasks T024.
- [x] CHK022 Is the reduced-surface sufficiency claim backed by a documented call-site census (only `load`/`store` used; no `exchange`/CAS/`wait`/`notify`)? [Assumption, research D-5] — PASS: research D-5 provides the documented call-site census confirming all four consumers use load/store only. contract C-3-note states: "none of the four migrated consumers uses compare_exchange or exchange — they use load/store only (D-5)." plan "Integrated test inventory" row 6-consumers confirms: "consumers witness publish/acquire (load/store) only." refs: research.md D-5, contract C-3-note, plan row 6-consumers.

## 5. Zero Tier-1 regression + always-ship-guard linkage

- [x] CHK023 Is "zero Tier-1 regression" given a measurable definition (every existing gate's disposition unchanged; no new waivers attributable to the migration)? [Measurability, Spec §SC-005] — PASS: SC-005: "Every existing Tier-1 gate (sanitizers, coverage thresholds, the §XV.9 awaitable-mutex corpus gate, ABI hygiene) retains an unchanged disposition on the default libstdc++ build after the migration — zero regression, no new waivers attributable to this feature." tasks T023 makes this the explicit US3 acceptance gate. refs: spec.md §SC-005, tasks T023.
- [x] CHK024 Is the native zero-overhead requirement stated AND reconciled with the always-compiled guard TU (symbols present-but-never-called; alias never constructs a guard)? [Consistency, Spec §FR-002/SC-005 / research D-9 / data-model E-3] — PASS: spec.md Clarifications 2026-06-21 (Gate A round 2): "Always compile and ship the guard … Native pays zero overhead (symbols present-but-never-called; lazy-init never fires)." data-model E-3: "on native the shard_guard symbols it defines are present-but-never-called (the alias path never constructs a guard → the function-local-static lazy-init never fires), so native pays zero runtime overhead and SC-005 holds." FR-002 pins the alias as zero overhead. The reconciliation is explicit. refs: spec.md §FR-002, §Clarifications 2026-06-21, data-model E-3, research D-9.
- [x] CHK025 Is the always-ship-guard force-mode contract pinned (one target-level decision; guard symbols always link) rather than deferred to implementation? [Clarity, research D-9 / contract CT-4] — PASS: contract CT-4 is explicitly tagged "PINNED: always-ship-the-guard, Codex #3/#4" and states "Decision (pinned at Gate A round 2 — no longer deferred to /implement or tasks): src/core/sync/atomic_shared_ptr.cpp … is always compiled into fixpp_sync on every mode." plan §Summary and tasks T005 both reflect the pinned decision. refs: contract CT-4 "PINNED" annotation, spec.md §Clarifications 2026-06-21 round 2, tasks T005.
- [x] CHK026 Is the forced-fallback link-resolution requirement (one link test per owning target: tls/transport/session) defined? [Coverage, plan inventory row +B / contract CT-4] — PASS: plan row +B: "Forced-mode link test per owning target (guard symbols resolve under -DFIXPP_FORCE_ATOMIC_SHARED_PTR_FALLBACK)" — three named tests: `tls_forced_fallback_link`, `transport_forced_fallback_link`, `session_forced_fallback_link`. contract CT-4 requires them. tasks T019 implements them. refs: plan row +B, contract CT-4, tasks T019.
- [x] CHK027 Is SC-006's toolchain scope (libstdc++ this cycle; MSVC forced-fallback an explicit deferred follow-up) stated consistently with the spec's narrowing? [Consistency, Spec §SC-006] — PASS: SC-006 states the scope explicitly: "the forced-fallback path is exercised green on the native toolchain(s) this feature validates — libstdc++ (Linux clang/gcc) — not only on a libc++ host … MSVC-STL forced-fallback is an explicit deferred follow-up." spec.md §Clarifications 2026-06-21 (round 1) narrows SC-006 to libstdc++. plan §Constitution Check and tasks T020 are consistent with this scope. refs: spec.md §SC-006, §Clarifications 2026-06-21 round 1.

## 6. 023 CHK046 reversal + stale-documentation census

- [x] CHK028 Is the 023 CHK046 reversal framed as "the objection is removed (mutex-free header)" rather than an override, with the cross-referenced 023 artifacts enumerated? [Clarity, Spec §FR-013 / research D-8] — PASS: spec.md §FR-013: "FR-012 type-erasure … removes CHK046's underlying objection (no header-resident std::mutex) rather than overriding it … not a silent swap." plan §Constitution Check 023 CHK046 row: "FR-012 type-erasure removes that objection (mutex-free header), so the reversal is grounded in the objection no longer applying — not an override." Cross-feature anchor verified: 023 concurrency.md CHK046 confirmed present, states "avoid implying the snapshot uses the project's unshipped atomic_shared_ptr (whose std::mutex fallback is barred from the awaitable header)." The framing is consistent and the 023 artifact is named. refs: spec.md §FR-013, plan §Constitution Check row, 023 checklists/concurrency.md CHK046.
- [x] CHK029 Is the mutation-check requirement defined (the CHK046 prohibition must be ABSENT from active 023 artifacts post-reversal, not merely contradicted)? [Measurability, research D-8 / data-model E-4a] — PASS: data-model E-4a §Mutation-check: "confirm the obsolete 023 CHK046 prohibition ('avoid the unshipped atomic_shared_ptr; pin the standard primitive') is ABSENT from active 023 artifacts after the FR-013 reversal (not just superseded in prose) — see research D-8." tasks T026: "Mutation-check: grep the active 023 artifacts and confirm the prohibition … is ABSENT, not merely contradicted." The requirement is defined and measurable. refs: data-model E-4a, research D-8, tasks T026.
- [x] CHK030 Is the stale-documentation census exhaustive (each false-on-fallback comment + the two pinset benches enumerated with corrected wording)? [Completeness, data-model E-4a] — PASS: data-model E-4a table enumerates 7 named sites with corrected wording: `src/tls/pinset.cpp:8`, `include/fixpp/tls/pinset.hpp:118-120`, `bench/tls/bench_pinset_snapshot_acquire.cpp`, `bench/tls/bench_pinset_find.cpp`, `include/fixpp/session/engine.hpp:460-462`, `include/fixpp/session/engine.hpp:119`, `include/fixpp/core/sync/async_mutex.hpp:21`, `include/fixpp/transport/transport_factory.hpp:86-87`. The two pinset benches are enumerated with specific wording corrections. tasks T025 implements all sites. refs: data-model E-4a table, tasks T025.
- [x] CHK031 Is the bench-detector correctness requirement stated (branch on `FIXPP_ATOMIC_SHARED_PTR_NATIVE_ACTIVE`, not raw `__cpp_lib_atomic_shared_ptr`; no lock-free ceiling on the fallback lane)? [Clarity, data-model E-4a / New-P3-2] — PASS: data-model E-4a bench sites: "the detector MUST branch on the fixpp selector FIXPP_ATOMIC_SHARED_PTR_NATIVE_ACTIVE, NOT the raw __cpp_lib_atomic_shared_ptr feature-test macro — otherwise a forced-fallback build under libstdc++ (where __cpp_lib_atomic_shared_ptr is still defined) reports a false-green 30 ns lock-free ceiling." tasks T025 implements New-P3-2 for both bench files. refs: data-model E-4a (bench_pinset_snapshot_acquire.cpp and bench_pinset_find.cpp entries), tasks T025.
- [x] CHK032 Is there a requirement to confirm NO consumer test or perf gate asserts lock-freedom (which the forced-fallback lane would violate)? [Coverage, data-model E-4a / New-D] — PASS: data-model E-4a last row (consumer tests + V-6 perf gate): "grep the four consumers' tests + V-6; confirm NONE asserts lock-freedom (the forced-fallback lane would violate it) — New-D." tasks T025 includes this grep as part of the stale-doc census (E-4a last row). refs: data-model E-4a New-D row, tasks T025.

## 7. Recording / completeness obligations

- [x] CHK033 Is the FR-014 catalogue-row obligation specified to rewrite the STALE description (four-consumer reality + type-erasure mechanism + FR-008 correction), not merely flip the status field? [Completeness, Spec §FR-014] — PASS: spec.md §FR-014: "FR-014 MUST update the description to the four-consumer reality (drain_latch_ptr_, pinset.snapshot_, transport_factory.cert_source_slot_, engine.reader_snapshot_) + the type-erasure mechanism (lock in src/core/sync/atomic_shared_ptr.cpp, header mutex-free, no constitution amendment) + the FR-008 ordering correction (I-23/I-13 = async-mutex-only) — not merely flip the status field. (New-E.)" tasks T027 implements this specific rewrite. refs: spec.md §FR-014, tasks T027.
- [x] CHK034 Are the forward-tracking follow-ups (FR-011a macOS; OTel-under-libc++ if the OFF fallback was taken) required to be recorded so they are not silently forgotten? [Coverage, tasks T027 / analyze E1] — PASS: tasks T027 explicitly requires: "Also record two forward-tracking follow-ups (analyze E1 + escalation-2): (a) FR-011a macOS Tier-2 lane (deferred, sequenced after the Linux lane is green); (b) IF the OTel-OFF fallback was taken in T015, the OTel-under-libc++ port as a tracked follow-up — point both at REMAINING-WORK.md / a future feature." refs: tasks T027.
- [x] CHK035 Is a feature-completeness audit (every FR/SC ↔ task ↔ catalogue, 100% or explicitly waived) defined as a Gate-B precondition? [Traceability, tasks T029 / const §XVII.8] — PASS: tasks T029: "Feature-completeness audit (the /gate-b precondition): map every task ↔ FR-001..015 / SC-001..006 ↔ catalogue NFR-017; assert 100% covered or each gap explicitly waived. [const §XVII.8, feedback_feature_completeness_gate]." This matches the constitution's pipeline gate requirement. refs: tasks T029.

## Notes

- Items are requirement-quality questions (is X specified/clear/consistent/measurable?), not implementation tests.
- Traceability: ≥80% reference a Spec §/contract/data-model anchor or a `[Gap]`/`[Assumption]` marker.
- This checklist is the input to `/checklist-audit` (pipeline step 9) — each item gets a PASS / SPEC-FIXED / DD-DECIDED / WAIVED disposition before `/speckit-implement` is unblocked.

## Audit Result

**Audited**: 2026-06-21 | **Auditor**: checklist-auditor (pipeline.md step 9)

| Disposition | Count |
|---|---|
| PASS | 34 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **35** |

### SPEC-FIXED items

- CHK011 — The "FR-014 six banned mutex types" label in multiple 046 artifacts was an intra-spec cross-reference that does not resolve to the types (046's own FR-014 defines catalogue recording obligations, not the six mutex types). Applied three edits to remove the ambiguity: (1) spec.md §FR-012 — replaced "any FR-014-six banned mutex type" with inline enumeration of all six types (`std::mutex`, `std::recursive_mutex`, `std::timed_mutex`, `std::recursive_timed_mutex`, `std::shared_mutex`, `std::shared_timed_mutex`) plus cross-reference to `tools/check_no_std_mutex_in_awaitable_headers.sh` and Constitution Article XI §3 / XV §9; (2) contracts/type-erased-lock-and-awaitable-cleanliness.md §Goal — replaced "the FR-014 six banned mutex types" with "the five other banned std:: mutex types enumerated in FR-012"; (3) tasks.md T002 — replaced "the FR-014 six banned mutex types" with "the six banned mutex types (FR-012)". Affected: `specs/046-atomic-shared-ptr/spec.md §FR-012`, `specs/046-atomic-shared-ptr/contracts/type-erased-lock-and-awaitable-cleanliness.md §Goal`, `specs/046-atomic-shared-ptr/tasks.md T002`.

### DD-DECIDED items

*(none)*

### WAIVED items

*(none)*

Anchors spot-verified:
- `006 data-model I-23/I-24` — confirmed lines 273–274 of `specs/006-async-mutex/data-model.md`; both describe `drain_latch_ptr_` release/acquire store/load exactly as FR-008 claims.
- `023 checklists/concurrency.md CHK046` — confirmed present; text states "avoid implying the snapshot uses the project's unshipped `atomic_shared_ptr` (whose `std::mutex` fallback is barred from the awaitable header)"; reversal framing in FR-013/research D-8 accurately characterizes CHK046's objection.
- `tools/check_no_std_mutex_in_awaitable_headers.sh` lines 8–16 — six banned mutex types confirmed enumerated: `std::mutex`, `std::recursive_mutex`, `std::timed_mutex`, `std::recursive_timed_mutex`, `std::shared_mutex`, `std::shared_timed_mutex`.
- `src/core/sync/CMakeLists.txt` — `fixpp_sync` is `add_library(fixpp_sync INTERFACE)` with comment "promote to STATIC iff a .cpp is added"; T005 is realizable.
- CT-1a script defects at `tools/check_no_std_mutex_in_awaitable_headers.sh` L171 (`|| true`) and flag parser L67–78 (no `-stdlib=*` branch) — both confirmed source-verified; addressed by T022/contract CT-1a.
