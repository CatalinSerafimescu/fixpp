# Feature Specification: Fold the Flat Per-Instance Cap Loop into the Nesting-Aware Traversal

**Feature Branch**: `085-fold-flat-cap-loop`

**Created**: 2026-08-03

**Status**: Draft

**Input**: User description: "Fold the redundant flat per-instance cap loop in `OffsetTable::group()` (`src/wire/offset_table.cpp:584-594`) into the nesting-aware `consume_group_extent` traversal. Resolves GitHub issue #214 — leg 2 of the deferred fix recorded in `spec/behaviors-and-limitations.md` L-063-4."

**Closes**: fixpp#214.

---

## Context — what the two loops are and why one of them is redundant

A repeating group's **delimiter** is the tag that opens every instance. `OffsetTable::group(no_tag)` answers two questions about such a group: *where does its byte extent end* and *does any single instance exceed `max_group_entries_per_instance`* (a defence-in-depth DoS cap). Today it answers them with **two separate walks over the same entries**.

**Walk 1 — `consume_group_extent` (`src/wire/offset_table.cpp:442-528`), nesting-aware.** It consumes **up to** `declared` delimiter-opened instances — its `while` at `:477` is a three-way conjunction, `k < entries_.size() && inst < declared && entries_[k].tag == delim`, and the inner scan breaks on a non-member at `:503-504`, so a truncated, malformed or membership-terminated frame yields fewer *(corrected at Gate A round 1; the earlier "exactly `declared`" contradicted this section's own lying-count edge case and `research.md` R-1)* — descending into any member tag that itself heads a group in the child membership context (`:491-499` at the instance-opening delimiter, `:510-518` for later members), and it **already applies the per-instance cap** at `:521-524`, setting `overflow` on breach. `group()` calls it at `:575` and converts `overflow` into `err_group_too_large` at `:576-578`.

**Walk 2 — the flat cap loop (`:584-595`).** It re-walks the same `[first, group_end]` range with a **flat** boundary test — `entries_[k].tag == delim` at `:586`, no notion of nesting depth — and applies the same cap at `:591`. This is the one remaining flat, wire-derived instance-boundary rule inside `group()`.

`group()` is not a cold path: `group_slices_status` calls it at `:667` for every group materialization, so Walk 2 runs on every group access that reaches the dictionary path.

### Why Walk 2 cannot fire when Walk 1 ran — the redundancy argument

This feature's central claim is that on the dictionary path Walk 2 is **provably unreachable as an error return**, not merely "usually right". The argument has four steps, each checkable at source:

1. **Both walks use the same `delim`.** `group():551` computes `delim = entries_[first].tag` with `first = count_idx + 1U` (`:545`). `consume_group_extent():458` computes `delim = entries_[first].tag` with the same `first` (`:450`). Identical value, same frame.
2. **Every boundary Walk 1 recognises, Walk 2 also recognises.** Walk 1 opens an instance only at an index whose tag equals `delim` (the `while` condition at `:477`). Walk 2 fires a boundary at *every* `k > first` with `entries_[k].tag == delim`, plus at `k == group_end`. So Walk 2's boundary set is a **superset** of Walk 1's on `[first, group_end]`.
3. **Therefore every Walk 2 segment is a subdivision of some Walk 1 instance**, and `inst_count` at `:590` is always **≤** the `(k - inst_start)` that `:521` measured for the enclosing instance.
4. **Walk 1 runs first and returns early on breach.** If any Walk 2 segment exceeded the cap, the Walk 1 instance containing it exceeded it too, so `group()` already returned `err_group_too_large` at `:577` and `:584` was never reached.

The subdivision in step 3 is not hypothetical: on the **485 contexts** where the outer delimiter is itself a nested group's count tag (083 leg (c) — FIX50SP2 240 + Orchestra 245), Walk 2 fires boundaries *inside* the nested extent that Walk 1 correctly treats as interior. That is exactly the direction that makes Walk 2's segments smaller, never larger.

Every degenerate exit of Walk 1 is also safe. `declared == 0` returns `first` (`:465-467`), so `group_end == first` and Walk 2's single iteration measures `0`. The depth cap sets `overflow` (`:446-449`) and exits at `:577`. Walk 1's three remaining early returns — `first >= entries_.size()` (`:451-453`), dict-free (`:454-456`), and delimiter-not-a-member (`:461-462`) — are each unreachable from `group()`, which guards the same conditions first at `:546`, `:553` and `:566`.

### Why Walk 2 nevertheless cannot simply be deleted

`group()` has a **dict-free fallback branch** at `:579-582`, taken when `opaque_dict_ == nullptr || group_member_fn_ == nullptr`. On that branch `consume_group_extent` is never called and `group_end` is set to `entries_.size()`. Walk 2 is therefore the **only** per-instance cap enforcement non-dictionary callers get. Deleting it outright would be a silent fail-open regression on that path.

Nesting-awareness is unachievable without a dictionary — membership is what tells the walk a tag opens a nested group — so the dict-free branch keeps a flat cap check. That residual flatness is a **limitation to record**, not a defect this feature repairs.

---

## Clarifications

### Session 2026-08-03

- Q: What should the issue's verb *"fold the redundant flat cap loop into the same traversal"* deliver concretely — a literal merge into `consume_group_extent`, or removal from the dictionary path with the flat check retained where it is still load-bearing? → A: **The latter.** The flat loop is **relocated without semantic change** into the dict-free `else` branch and deleted from the dictionary path — moved, not rewritten, against FR-001a's nine-item preservation checklist (`contracts/group_cap_accounting.md` C-3), which permits mechanical re-indentation and nothing else; `consume_group_extent`'s signature, contract and body are untouched. The rejected alternative was removing `consume_group_extent`'s dict-free bail (`:454-456`) so one traversal served both paths: that reads more literally as "folding", but the function is documented and named as a *membership-driven, nesting-aware* walk, and with no dictionary it can be neither — so it would embed a flat mode inside a function whose whole contract is that it is not flat, and would put FR-008 in play for no behavioural gain. Moving the loop unchanged also makes the dict-free path inspection-checkable rather than argued-equivalent, which turns FR-003 from a claim needing a proof into one discharged by inspecting the diff. Consequence for honest wording: this feature delivers *"the dictionary path now performs one traversal"*, **not** *"the two loops became one"* — and the spec, the code comment and the L-063-4 update must all say the former. **Wording provenance, Gate A rounds 1–2:** as originally recorded, this answer read "the flat loop moves **verbatim**… byte-identical". Those words are unsatisfiable — the block moves from 4-space to 8-space indentation and `clang-format` (`[const §IX.4]`) forces the re-indent — so round 1 replaced the *rule* with C-3's checklist while leaving the *disposition* unchanged (move it, do not rewrite it). Round 2 additionally restated the **answer itself** in its corrected form above, because leading with the superseded word and correcting it a paragraph later meant a skim read the withdrawn rule as operative.
- Q: The dict-free branch sets `group_end = entries_.size()`, so the flat cap's **final segment runs to the end of the entry table** and counts trailing non-group fields toward one instance. On a frame with a small group followed by many top-level fields that is a `err_group_too_large` **false positive** on a group nowhere near the cap. Preserve it, or repair it here? → A: **Preserve it unchanged (see Q1's Gate A round 1 wording correction — "byte-identically" as originally answered here is superseded by FR-001a's C-3 checklist), record it as a named limitation, and file a separate tracking issue.** Repairing it would be a behaviour change on the one path this feature was told not to touch: it would break SC-002's *"zero group requests change outcome"*, require its own witness frames, and widen a redundancy-removal into a correctness fix. Recording it without a tracking issue was also rejected — a residual left in prose with no issue is precisely how L-063-4 leg 2 became orphaned when `#180` closed, which is the situation this feature exists to clean up. See FR-003a and SC-010. **Severity correction, 2026-08-03:** the answer above was given, and an earlier FR-003a revision written, before the config bounds were checked. They are equal by default (`max_offset_entries` = `max_group_entries_per_instance` = 4096), so the breach is **unreachable under default configuration** and needs a caller-supplied `Config` that tightens the per-instance cap below the table cap. The disposition is unchanged — still preserved, recorded and tracked — but the record must not present it as a default-path defect. FR-003a and SC-010 carry the corrected framing.
- Q: FR-005 requires the cap pin to be *"proven RED under mutation"* but does not say what evidence discharges that. Which form? → A: **Both** a manual mutation transcript and a permanent bracketing pair. **(i)** During `/speckit-implement`, the dict-free cap check is deleted, the new pin is run, its RED output is captured, and the check is restored; the mutation applied and the failure output are recorded in the feature's `/speckit-verify` evidence file and cited from the test's comment block — a reviewer checks the transcript rather than rebuilding. **(ii)** A permanent companion test constructs the same over-cap frame with the cap raised above it and asserts success, bracketing the threshold from both sides. Neither alone suffices: (ii) cannot detect enforcement being deleted outright (both halves stay green), and (i) is a one-time observation that cannot catch a later threshold regression. See FR-005a and SC-004a.
- Q: A-004 commits an Article VIII §3 benchmark to this PR but does not fix its shape. New case, or existing? → A: **Reuse the three existing cases.** `bench/wire/typed_read_group_bench.cpp` already covers this path with `BM_TypedReadGroup_{Flat2,ModeC2,ModeC8}` — 083 built them for the group-split path this feature touches. They are run before and after and the delta is carried in the PR. No new bench source and no new baseline entry. A large-extent case sized to make the removed `O(extent)` walk's saving visible above noise was rejected: the obligation is *no regression*, A-004 already disclaims any win claim, and a case tuned to flatter a deletion is a microbench, not evidence. See SC-006.
- Q: Every line number this feature writes into L-063-4 goes stale on merge, and the row already carries three generations of numbering (072-era `:548-558`/`:596-599`, 083-era `:584-594`/`:586`/`:591`). How should the update anchor its references? → A: **Function and role first, line numbers appended and explicitly stamped "as of \<merge sha\>".** Each surviving site is named by what it *is* — "the dict-free cap check in `OffsetTable::group()`", "the instance splitter in `group_slices()`" — so the reference survives any later layout change, with the line number as a convenience that carries its own expiry. The historical brackets are **left untouched**: they are already era-marked, and rewriting them would destroy the record 083 deliberately preserved (its own convention for this row was to mark superseded text, not edit it in place). Bare re-pointed line numbers were rejected because they are precise on merge day and indistinguishable from the stale 072-era numbers the day after; symbol-only was rejected because pointing at `:586` unambiguously is what made `#214` filable at all. See FR-007b.

### Session 2026-08-03 (Gate A round 1)

Two questions the `/clarify` session did not raise, both surfaced by the round-1 reviews. Recorded here because each changed a requirement, not just its wording.

- Q: Contract C-1 was written as a **standing** invariant — "`group()` and `consume_group_extent` MUST resolve `delim` from the same entry" — and FR-002 mandated writing its consequence ("otherwise `group()` silently under-enforces the cap on the dictionary path") permanently into `src/wire/offset_table.cpp`. Is that property still true after the flat loop is removed, and is it the right thing to freeze? → A: **No, and no — the premise and the standing property are two different things and must be recorded separately.** The consequence is **empirically false against the tree**: `group_slices_status` already resolves its splitter delimiter from the per-context dictionary store (`offset_table.cpp:704-711`, via `group_delim_fn_`) while `group():551` reads the wire, over the *same* interval `[first, group_end]` with the *same* boundary predicate — divergent sources, one extent, shipped since 083 — and the cap does **not** under-enforce, because dictionary-path enforcement lives inside `consume_group_extent` (`:521-524`), not in either flat walk. After the removal, `group()`'s local `delim` partitions nothing; its only remaining dictionary-path use is the group-recognition gate at `:566`, so re-pointing it changes *whether a group is recognised*, never the cap. The delimiter-source equality is therefore demoted to **C-1a**, a **delivery-time proof premise** of the one-time removal, discharged by inspection and carrying nothing forward. The **standing** property is restated as **C-1**: `consume_group_extent` must enforce the per-instance cap over the same nesting-aware instances whose extent it returns — which this feature makes *more* load-bearing, since it removes the second walk that used to mask a divergence there. FR-002's mandated comment is re-pointed at C-1, SC-005a verifies its content, and FR-005b/SC-003 give C-1 a mutation-proven test guard. Also settled: a divergent-delimiter-*source* fixture **is** constructible (`:566` and `:461` check membership only, never delimiter identity), so the old "no test can guard it" was false as an absolute — but such a fixture would guard the recognition gate, not the cap, so it is not required by this contract.
- Q: `plan.md` and `research.md` R-6 both recorded Article VII §3 (TDD, red-green-refactor) as **PASS (planned)** on the sequence "the dict-free cap pin is written and observed RED before the relocation". Is that sequence achievable? → A: **No — it is arithmetically unavailable, and the verdict is corrected rather than re-planned.** On baseline the flat cap loop sits *after* the `if/else` (`:584-595`, following `:553-582`), so it runs on **both** paths including dict-free. A dict-free + tight-`Config` cap pin is therefore **green on baseline**; there is nothing for it to fail against. What FR-005a(i) actually describes — delete the delivered check, observe RED, restore — is **mutation testing**: legitimate, load-bearing, and now required on both sites (FR-005b), but not "failing test first, then implementation". **The round-1 answer stopped here and recorded VII §3 as "not cleanly applicable". That disposition is WITHDRAWN at Gate A round 2 — see Session 2026-08-03 (Gate A round 2) Q1 below, which supersedes this answer's verdict.** The *cap pin* half above stands: a **behavioural** dict-free cap pin cannot be red-first here. What round 1 got wrong is the leap from "no behavioural red-first artifact" to "no red-first artifact at all"; a **structural** one is red-first and is now required by FR-001b.

### Session 2026-08-03 (Gate A round 2)

Two questions raised by the round-2 reviews. Each changed a requirement, not merely its wording.

- Q: Round 1 recorded Article VII §3 as **"NOT CLEANLY APPLICABLE"**, justified by (i) "no precedent in this repo" for a structural red-first artifact and (ii) "it would require a novel structural/AST gate". Is that disposition available to a feature spec at all, and are its two justifying clauses true? → A: **No to all three.** *(a) The disposition is not available.* `.specify/constitution.md:86` — *"Conflicts are resolved by amending the constitution first (Article XX) — never by silently violating an article"* — and Article XX §1 (`:402`) — *"The constitution is amendable but **not silently violatable**"* — between them leave exactly three routes for a conflict with a mandatory article: comply, amend, or take a rationale-bearing waiver. "Not cleanly applicable" is a fourth that the bundle invented for itself, and it has **no precedent across ten sibling bundles** (023, 060, 064, 065, 069, 070, 071, 072, 074, 083 all record VII §3 as PASS/PLANNED against a named red-first artifact — 065 manufactured one from a pre-authored `GTEST_SKIP`'d witness). *(b) Clause (i) is factually false.* `tests/dictionary/load_any_test.cpp:143-171`, `LoadAny.FR004_SingleSharedDispatchSourceInspection`, is a permanent, behaviour-blind **structural** pin on an *absence*, and its own header comment (`:134-142`) states it exists precisely because *"Behavior Scenarios 1/3 would still pass if each site sniffed independently; this gate is what pins 'defined once, not duplicated'"* — materially the same evidence shape 085 needs, for the same reason. *(c) Clause (ii) is false on mechanism.* That precedent uses plain `std::string::find` over a slurped TU (`FIXPP_SRC_DIR`, `tests/dictionary/CMakeLists.txt:183`); **no AST is involved anywhere.** **Disposition: comply.** VII §3 becomes `PASS (planned)` against a named red-first **structural** pin — FR-001b, verified by SC-005b — authored at `/speckit-implement` before the relocation, observed RED on baseline and GREEN after. The FR-005a(i)/FR-005b mutation transcripts remain exactly what they are: real, load-bearing, **supplementary**. The escalation route (an Article XX amendment or an Article XVII §1/§6 waiver for pure-removal features) was rejected because compliance is available, cheap and — see FR-001b — a net asset: C-2 and SC-005 were discharged by *manual* source inspection only, and the pin converts the feature's headline criterion into an executable regression gate.
- Q: FR-005b/SC-003's mutation was described as "the test guard for contract **C-1**". Does it guard all of C-1 — that the cap stays coupled to the same nesting-aware partition whose extent is returned? → A: **No; the claim is narrowed rather than the evidence widened.** The mutation deletes `consume_group_extent`'s comparison **entirely** and observes the pre-existing pin go RED, against `WireOffsetTable.DoSCapPerInstanceRejectsOversizedSingleInstance` (`tests/wire/offset_table_test.cpp:199-235`), whose frame carries **one unnested instance** — so its flat and nesting-aware partitions are identical and the mutation cannot distinguish them. What it proves is that **the cap check is load-bearing on the dictionary path**: enforcement exists and deleting it is observable. C-1's *partition coupling* rests on **source inspection** (the three sites `:477-478` / `:521-524` / `:527` are one walk over one partition, checked at delivery) **plus** the FR-002 source comment that tells the next maintainer to re-verify it. A nesting-sensitive tight-cap fixture that could mutation-test the coupling was **considered and rejected**: it is new behavioural test scope, aimed at a gap in a *standing* contract rather than at anything this change introduces, on a feature whose deliverable is a removal. The honest record is the narrower claim, not a wider fixture.

---

## User Scenarios & Testing *(mandatory)*

### User Story 1 - One traversal owns instance-boundary and cap accounting (Priority: P1)

An application reads a repeating group from a parsed FIX message through `MessageView::group<>()` or the C-ABI top-level group getter. The engine determines the group's extent and enforces the per-instance entry cap from a **single nesting-aware traversal**, not from one nesting-aware traversal followed by a second flat re-walk of the same entries.

**Why this priority**: This is the issue's stated acceptance and the whole reason the feature exists. It removes the last flat instance-boundary rule from `group()` and eliminates a redundant O(extent) walk from a path taken on every group materialization. Nothing else in the feature is meaningful without it.

**Independent Test**: Fully testable on its own — run the existing wire, C-ABI and dictionary suites plus the census pin against a build with the second walk removed from the dictionary path, and confirm every **asserted** slice, extent, `group_index`, reserve bound and error disposition is unchanged, with no fixture or expected-value edit. *(The stronger claim — that every returned value, including those no test asserts, is identical across all ten shipped dictionaries — is established by `research.md` R-1's source argument, not by this run; see SC-002.)*

**Acceptance Scenarios**:

1. **Given** a message carrying a repeating group on any shipped dictionary, **When** the application requests that group's slices, **Then** the returned slices, instance count and extent are identical to those the pre-change engine returned.
2. **Given** a message whose outer group delimiter is itself a nested group's count tag (one of the 485 such contexts), **When** the application requests the group, **Then** the outer group still spans all of its declared instances and the nested entries are still interior to their instance — unchanged from the post-083 behaviour.
3. **Given** a message with a repeating group whose instance entry count exceeds `max_group_entries_per_instance`, **When** the application requests that group, **Then** the request fails with the group-too-large disposition, as before.
4. **Given** any message accepted by the pre-change engine, **When** the same message is read after the change, **Then** no group request that previously succeeded now fails, and no group request that previously failed now succeeds.

---

### User Story 2 - Non-dictionary callers keep their cap (Priority: P2)

A caller uses an `OffsetTable` built without a dictionary (no membership callback available). Such a caller still gets per-instance cap enforcement against a hostile or malformed frame — the DoS defence is not silently removed as a side effect of tidying the dictionary path.

**Why this priority**: Fail-open on a security cap is a worse outcome than the redundancy this feature removes. It is P2 rather than P1 only because it is a **preservation** requirement: the behaviour already exists and the feature must not lose it.

**Independent Test**: Fully testable on its own — drive a dict-free `OffsetTable` with a frame whose flat instance segmentation exceeds the cap and assert the group-too-large disposition, then mutate the delivered code to drop the check and confirm the same test goes RED.

**Acceptance Scenarios**:

1. **Given** a dict-free `OffsetTable` and a frame whose flat segmentation puts more than `max_group_entries_per_instance` entries between two delimiter occurrences, **When** the group is requested, **Then** it fails with the group-too-large disposition.
2. **Given** a dict-free `OffsetTable` and a frame within the cap, **When** the group is requested, **Then** it succeeds and returns the same extent the pre-change engine returned.
3. **Given** the delivered dict-free cap check is removed by deliberate mutation, **When** scenario 1's test runs, **Then** it fails — proving the pin is load-bearing rather than vacuous.

---

### User Story 3 - The remaining flatness is recorded honestly (Priority: P3)

An operator or maintainer reading `spec/behaviors-and-limitations.md` sees that L-063-4's leg 2 is delivered, that leg 1 remains descoped with its evidence intact, and that two flat instance-boundary rules **survive on purpose** — the dict-free cap check in `group()`, and the instance splitter in `group_slices_status()` — each with the reason it was not changed.

**Why this priority**: Documentation, not behaviour. It is nonetheless required: the issue names the L-063-4 update as acceptance, and leaving a reader to infer that "leg 2 delivered" means "no flat rules remain" would be a false claim in the operator-facing artifact.

**Independent Test**: Testable by inspection of the updated L-063-4 row against the delivered source.

**Acceptance Scenarios**:

1. **Given** the updated L-063-4 row, **When** a reader looks for leg 2's disposition, **Then** it is recorded as DELIVERED by this feature with the redundancy argument's conclusion stated.
2. **Given** the updated L-063-4 row, **When** a reader looks for leg 1's disposition, **Then** the descope-with-evidence text from 083 is intact and not weakened.
3. **Given** the updated L-063-4 row, **When** a reader asks which flat instance-boundary rules remain, **Then** both surviving sites are named with their justification, by function and role, and with the fact that they resolve their delimiter from **different sources** stated rather than left as a shared "flat".
4. **Given** the recorded dict-free trailing-field false positive, **When** a reader asks what is being done about it, **Then** the row cites fixpp#220 — not a promise in prose, which is the failure mode that orphaned this row's own leg 2.
5. **Given** the same record, **When** a reader asks how severe it is, **Then** it states that the breach cannot occur under default configuration and names the `max_group_entries_per_instance < max_offset_entries - 1` precondition — so the row neither overstates nor understates it.

---

### Edge Cases

- **A group declaring zero instances.** The nesting-aware traversal consumes no extent; the removed walk measured a zero-length segment. The delivered path must return the same empty group, not a cap breach.
- **A group whose count field is the last entry in the table.** Handled before either walk is reached; the delivered path must not change that disposition.
- **Nesting deeper than the depth cap.** The nesting-aware traversal already reports overflow and `group()` already converts it; removing the second walk must not change which error is returned or the order in which it is decided.
- **A group whose delimiter is also a nested group's count tag.** The removed walk fired boundaries *inside* the nested extent. Removing it must not change the extent, the slices, or the cap outcome for any of the 485 shipped contexts of this shape.
- **A declared count that lies (declares more instances than the frame carries).** The traversal is hard-bounded by `entries_.size()`, so a lying count can never run the scan off the end — that bound is what `offset_table.cpp:474-476` calls *fail-closed*, and this feature must not change it. To be precise about what the requirement is and is not: the walk **stops and returns the available extent**; it does **not** set `overflow`, so `group()` still returns a successful `group_index`. Declared-vs-actual **mismatch rejection is the validator's job**, not `group()`'s, exactly as the source comment says. Making `group()` reject a count mismatch would be a behaviour change requiring its own requirement and tests, and is **out of scope**. *(Disambiguated at Gate A round 1 — "fail-closed" was read by one reviewer as "rejects the mismatch"; the repo uses the term for the bound.)*
- **A dict-free table whose group extent runs to the end of the entry table.** The surviving flat check measures its last segment to `entries_.size()`, so trailing non-group fields count toward that segment. This is **pre-existing looseness** and must be preserved unchanged, not tightened, by this feature.

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: On the dictionary path, `OffsetTable::group()` MUST derive its per-instance cap accounting from the single nesting-aware extent traversal, and MUST NOT perform a second, flat re-walk of the group's extent.
- **FR-001a**: The flat cap loop MUST be relocated into the dict-free `else` branch **without semantic change** rather than rewritten, parameterised, merged, re-ordered, or extracted into a helper. **Mechanical re-indentation is permitted and expected** — the block sits at 4-space function-body indentation, the `else` body is 8-space, and `clang-format` is a Tier-1 gate (`[const §IX.4]`), so an absolute no-reindent rule would be unsatisfiable. What must be preserved is the nine-item checklist in `contracts/group_cap_accounting.md` C-3: the separate `inst_start` declaration, the loop bounds, the boundary predicate and its disjunct order, the `continue`, the segment measure, the **strict** `>`, the immediate `err_group_too_large` return, the post-check re-anchoring, and the unchanged function return. `consume_group_extent`'s signature, contract and body MUST NOT be modified, and its dict-free early return (`:454-456`) MUST remain. *(Clarified 2026-08-03; "verbatim / byte-identical" replaced by the explicit checklist at Gate A round 1 — the literal rule contradicted itself, and an unsatisfiable rule silently reverts FR-003 to the equivalence argument this bundle claimed to avoid.)*
- **FR-001b**: The relocation MUST be preceded by a **red-first structural pin** — a permanent, behaviour-blind source-inspection test asserting that the flat cap block lives **inside** `group()`'s dict-free `else` branch and **not** in `group()`'s function body after the `if/else`. This is Article VII §3's failing-test-first artifact for this feature (`[const §VII.3]`, `.specify/constitution.md:173`), and it discharges the gate by **compliance**, not by substitution.

  - *Name and home*: `WireOffsetTable.FR001_SingleTraversalSourceInspection`, in `tests/wire/offset_table_test.cpp` — the existing `wire_pure_tests` bucket per `[const §VII.8]`, **no new executable**. Its comment block MUST state, as the precedent's does, that it is a source-inspection assertion rather than a behaviour test and **why** behaviour cannot distinguish the two states here (a dictionary-path flat re-walk is unreachable-as-an-error either way — Context §"Why Walk 2 cannot fire" — so every behavioural test stays green across the change).
  - *Build wiring*: `tests/wire/CMakeLists.txt` MUST gain `target_compile_definitions(wire_pure_tests PRIVATE "FIXPP_SRC_DIR=\"${CMAKE_SOURCE_DIR}/src\"")`, mirroring `tests/dictionary/CMakeLists.txt:178-183` verbatim in mechanism. The bucket does not define it today (verified 2026-08-03 at `main` = `c1564dd2`); that one line is the only build change this feature makes.
  - *Precedent, and the mechanism it fixes*: `tests/dictionary/load_any_test.cpp:143-171` (`LoadAny.FR004_SingleSharedDispatchSourceInspection`) is the shipped instance of exactly this construction — a permanent structural pin on an *absence*, slurping production source via `FIXPP_SRC_DIR` and asserting token presence/absence with plain `std::string::find`. **No AST, no new tooling, no novel gate.**
  - *Discriminant*: delivery picks it; a **workable one is verified to discriminate**, so this requirement is not a hope. Keying on indentation — the flat block's own statement `std::size_t inst_start = first;` sits at 4-space function-body indentation on baseline and at 8-space `else`-body indentation after relocation — the pin asserts the TU contains **no** `"\n    std::size_t inst_start = first;\n"` and **exactly one** `"\n        std::size_t inst_start = first;\n"`. Measured against `src/wire/offset_table.cpp` at `c1564dd2`: 4-space occurrences **1**, 8-space occurrences **0** ⇒ **RED on baseline**, GREEN after the relocation. `group_slices_status`'s own `inst_start` (`:712`) sits at 16-space indentation and collides with neither key (measured: 16-space occurrences **1**). Indentation is a stable signal in this tree because `clang-format` is a Tier-1 gate (`[const §IX.4]`); brace-region membership within `group()` is an equally available key if delivery prefers it.

  - *Second discriminant — **REQUIRED**, and it guards a different failure than the first* (added post-audit, 2026-08-03). The `inst_start` key above proves the block **moved**; it does **not** prove the block moved **unchanged**. An `else`-inverted relocation — `if (boundary) { … }` in place of `if (!boundary) continue;` — leaves `inst_start` at 8-space, so the first key alone goes **GREEN** on a diff that violates C-3 #4 and FR-001a. The pin MUST therefore *also* assert the TU contains **no** `"\n        if (!boundary) {\n"` (8-space, baseline) and **exactly one** `"\n            if (!boundary) {\n"` (12-space, post-relocation). Measured at `c1564dd2`: 8-space **1**, 12-space **0**, 4-space **0**, 16-space **0** ⇒ RED on baseline on this key too. Collision-free: `boundary` occurs as an identifier at exactly two sites in the file — this block (`:586-587`) and `group_slices_status`'s walk (`:714-716`), the latter in the **positive** form at 20-space, matching neither key at any indentation. Reflow-free: the key line is 24 chars, 28 after the shift, against `ColumnLimit: 100`. **Why this key and not another:** the sibling walk's positive form makes "match the sibling" a natural, well-intentioned edit during re-indentation, and C-3 #4 is the one preservation item no behavioural test, sanitizer, or corpus can observe — `research.md` R-1 makes the dictionary-path flat re-walk unreachable-as-an-error, so semantics are identical either way. This converts the bundle's single acknowledged blind spot from a manual diff walk into a standing gate at zero runtime cost.
  - *Sequence*: pin authored and observed **RED on the unmodified tree** → relocation lands → pin GREEN. The RED output MUST be captured in `.specify/decisions/085-fold-flat-cap-loop-verify.md` alongside the two mutation transcripts. See SC-005b.
  - *Why this is an asset rather than gate-compliance overhead*: **C-2** ("MUST NOT re-walk `[first, group_end]` after `consume_group_extent` returns") and **SC-005** ("exactly one traversal") are this feature's headline obligations and were discharged by **manual source inspection alone**. The pin converts both into an executable regression gate, at zero runtime cost — it reads a file at test time and never touches the hot path.
- **FR-002**: The removal in FR-001 MUST be behaviour-preserving on the dictionary path for every input: no frame may change its returned slices, instance count, extent, `group_index`, reserve bound, or error disposition as a result of it. The delivered source MUST carry a comment **on the dictionary branch** stating **two** things, both of which are true after the removal:
  1. **Why the second walk went** — the redundancy argument in Context §"Why Walk 2 cannot fire": the cap is already applied inside the nesting-aware extent walk, over boundaries the flat partition merely refined, and that walk returns first on breach.
  2. **What now stands in its place** — contract **C-1**: this branch's per-instance DoS defence is now *solely* `consume_group_extent`'s per-instance cap applied over the same nesting-aware instances whose extent it returns. A change to that walk must re-verify the cap still measures that partition, or re-introduce an independent cap here.

  The comment MUST NOT assert that `group()`'s local `delim` and `consume_group_extent`'s must stay equal *as a standing rule*. After the removal that equality no longer partitions anything for cap purposes — `group()`'s `delim` has one remaining dictionary-path use, the group-recognition gate — so writing it into the source as a standing invariant would enshrine a false consequence for exactly the maintainer most likely to touch these lines. The equality is a **delivery-time proof premise** (contract **C-1a**), recorded in this spec and in `research.md` R-1, not in the source. Per FR-007b the comment names each site by **function and role first**, with any line number appended and stamped as-of the merge commit. *(Worded as the branch rather than "the removal site" because once the loop is gone there is no removal site left to anchor a comment to. Content re-scoped at Gate A round 1.)*
- **FR-003**: Per-instance cap enforcement on the **dict-free** fallback path MUST be preserved with its current semantics — same delimiter source (wire-derived), same segmentation, same extent bound (`entries_.size()`), same error disposition. This feature MUST NOT tighten, loosen, or re-scope it. Per FR-001a the preservation is discharged by walking C-3's nine-item checklist over the diff — a bounded inspection, not an equivalence argument — with the ~16 existing dict-free tests (SC-004) as the behavioural cross-check.
- **FR-003a**: The dict-free cap's **trailing-field false positive** — its final segment extending to `entries_.size()`, so top-level fields after the group count toward one instance and can trip `err_group_too_large` on a group far below the cap — MUST be recorded as a named limitation in `spec/behaviors-and-limitations.md`, and MUST be tracked by its own GitHub issue — **filed 2026-08-03 as fixpp#220**, which the limitation row MUST cite. It MUST NOT be repaired here. The filed issue MUST state the reachability precisely, because it is **narrower than "a live defect on a public API"**: it is **unreachable under default configuration** and reachable only under a caller-supplied `Config` in which `max_group_entries_per_instance < max_offset_entries - 1`.

  - *Why unreachable by default:* `default_max_offset_entries` and `default_max_group_entries_per_instance` are **both 4096** (`include/fixpp/wire/offset_table.hpp:27-28`), and `build()` clamps at `entries_.size() >= cfg_.max_offset_entries` (`src/wire/offset_table.cpp:326`). The dict-free segment is bounded by `entries_.size() - first ≤ 4095 < 4096`, so it can never exceed the cap. The codebase already records this reasoning for the same call site: `tests/wire/offset_table_error_path_test.cpp:13-14` waives `group()`'s `err_group_too_large` as *"provably unreachable: max table entries = 4096, so avail ≤ 4095 < default_max_group_entries_per_instance"*.
  - *Why it is still worth filing:* both bounds are public `Config` members, and tightening the **per-instance** cap while leaving the **table** cap at its default is a natural hardening choice — precisely the configuration that turns the trailing-field over-count into a false rejection. It is a config-dependent defect, not a latent-shape curiosity and not a default-path bug.
  - *Reachability of the dict-free path itself is not in question:* it is a public, documented constructor (`OffsetTable(frame_view const&, memory_resource*)`, `offset_table.hpp:125`, labelled "Dict-FREE construction" at `:161`) and `group()` is already driven through it (`tests/wire/offset_table_test.cpp:150-157`). Only the **cap breach** needs the non-default `Config`.

  *(Clarified 2026-08-03. Reachability re-verified 2026-08-03 and this bullet **corrected**: an earlier revision claimed the shape was "reachable, not latent" without qualification, which overstated it by ignoring that the two default bounds are equal.)*
- **FR-004**: The cap MUST remain reachable and MUST still fire on **both** paths after the change. A frame exceeding `max_group_entries_per_instance` MUST return the group-too-large disposition on the dictionary path and on the dict-free path.
- **FR-005**: The regression pin for FR-004 MUST be proven load-bearing by mutation **on each of the two enforcement sites** — with that site's delivered check removed, its pin MUST go RED. A cap pin that has never been observed RED does not discharge FR-004. The two sites are the **dict-free** cap check relocated into `group()`'s `else` branch (FR-005a) and, on the **dictionary** path, `consume_group_extent`'s per-instance comparison (FR-005b). *(Second site added at Gate A round 1: after this feature the dictionary path has no other cap, so an unproven pin there is the same false-green risk the dict-free pin was scoped to prevent.)*
- **FR-005a**: FR-005's **dict-free** leg is discharged by **two** artifacts, not one. **(i)** A recorded mutation transcript: the delivered dict-free cap check is deleted, the pin is run, its RED output is captured verbatim, and the check restored — the mutation and the failure output recorded in the feature's `/speckit-verify` evidence file and cited from the test's comment block. **(ii)** A permanent companion test that builds the same over-cap frame with `max_group_entries_per_instance` raised above it and asserts success, bracketing the threshold from both sides. Neither alone discharges FR-005: (ii) stays green if enforcement is deleted outright, and (i) cannot catch a later threshold regression. *(Clarified 2026-08-03.)*
- **FR-005b**: FR-005's **dictionary** leg is discharged by a recorded mutation transcript on the pre-existing pin. Scope of what it proves, stated precisely: it guards **C-1's cap-existence half** — that a per-instance cap is enforced on the dictionary path and that the check is load-bearing on an *unnested* instance. It does **not** test C-1's nesting-aware **partition coupling**, which remains discharged by source inspection and the FR-002 comment; the existing fixture is a single unnested instance whose flat and nesting-aware partitions coincide. The mutation is: the per-instance comparison inside `consume_group_extent` (the `(k - inst_start) > cfg_.max_group_entries_per_instance` check, `:521-524` as-of `main` = `c1564dd2`) is deleted, `WireOffsetTable.DoSCapPerInstanceRejectsOversizedSingleInstance` is run and its RED output captured, then the check is restored and the pin re-run green. No new test is required — the pin exists and stays green in the delivered tree; what is missing today is the evidence that it is load-bearing. This is also the test guard for contract **C-1**.

  **This transcript MUST be taken AFTER the FR-001 relocation has landed, and is invalid if taken on baseline.** Measured 2026-08-03 at `main` = `c1564dd2`: with the flat loop still present on the dictionary branch, deleting `:521-524` leaves the pin **GREEN** — the flat loop catches the same breach one branch later and returns the same `wire_group_too_large` from `:592`. That is correct pre-removal behaviour, and it is the reason this discharge did not exist before: the RED is only available once the loop is gone. A transcript recorded on baseline records a false negative and does **not** discharge FR-005b or SC-003. *(The baseline green is itself worth recording as a second witness for the redundancy argument — it demonstrates the flat loop independently catching what the nesting-aware cap catches on that fixture.)*

  *(Added at Gate A round 1 — SC-003 already demanded this proof and no artifact performed it. The ordering precondition was found by running the mutation rather than reasoning about it.)*
- **FR-006**: `spec/behaviors-and-limitations.md` L-063-4 MUST be updated to record leg 2 as DELIVERED by this feature, MUST preserve leg 1's descope-with-evidence disposition from 083 unweakened, and MUST NOT reopen fixpp#180.
- **FR-007**: The updated L-063-4 row MUST name the flat instance-boundary rules that **survive** this feature — the dict-free cap check in `group()` and the instance splitter in `group_slices_status()` (`src/wire/offset_table.cpp:712-733`; `group_slices()` at `:639-641` is only the delegating wrapper) — each with the reason it was not changed, so "leg 2 delivered" cannot be read as "no flat rules remain".
- **FR-007a**: That same record MUST state that the two surviving sites do not merely share a flat *shape* — they resolve their delimiter from **different sources**. `group()` and `consume_group_extent` derive `delim` from the wire (`:551`, `:458`); the `group_slices_status()` splitter derives it from the dictionary's per-context store (`:704-711`), which is 083's change and moved 330 contexts' delimiters. The asymmetry is pre-existing and outside this feature's scope, but "flat" alone understates it and MUST NOT be the only word used.
- **FR-007b**: Every source reference this feature adds to `spec/behaviors-and-limitations.md` **and to the FR-002 source comment** MUST name the site by **function and role first**, with any line number appended and explicitly stamped as-of the merge commit. The row's existing historical brackets MUST be left untouched — they are already era-marked, and rewriting them destroys the record 083 preserved deliberately. *(Clarified 2026-08-03. Extended to the source comment at Gate A round 1: the original rule exempted the one artifact where stale numbering is most corrosive — a comment living inside the very file whose line numbers this change shifts, so it would be born stale. L-063-4 accumulated three generations of numbering precisely this way.)*
- **FR-008**: No public or exported surface may change: no header signature, no exported symbol, no error enum value, no C-ABI version. `consume_group_extent`'s signature and contract MUST be unchanged.
- **FR-009**: The change MUST NOT introduce any allocation, and MUST NOT increase the work performed per `group()` call on either path. The two clauses are discharged differently and the asymmetry is deliberate: the **per-call-work** clause is measured by SC-006's benchmark; the **allocation** clause is discharged **by construction** and carries no success criterion — this feature deletes a loop and moves it, introducing no expression capable of allocating, in a `noexcept` member with no container operation on either branch. *(The absence of an SC for the allocation leg was flagged at Gate A round 1. It is recorded here and in `contracts/group_cap_accounting.md`'s Verification matrix rather than closed with a synthetic alloc-guard test, which would be a new gate for a removal.)*

### Key Entities

- **Group extent**: the half-open entry range a repeating group occupies, computed by the nesting-aware traversal and returned as `group_index`. Unchanged by this feature.
- **Per-instance entry cap**: `max_group_entries_per_instance`, a configured defence-in-depth bound on how many entries one group instance may contain. Its *enforcement site* is what this feature consolidates; its *value and meaning* are unchanged.
- **Instance boundary rule**: the predicate deciding where one group instance ends and the next begins. Two exist today — nesting-aware (dictionary-driven) and flat (wire-derived). This feature removes the flat one from the dictionary path only.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: Across all ten shipped dictionaries, the delimiter census pin `DelimiterCensus.RedCountsReconcileWithSpecBaseline` (`tests/dictionary/delimiter_census_test.cpp:476`) and all seven `TypedReadSplitAgreement.*` tests (`tests/wire/typed_read_split_agreement_test.cpp`: six `ExtentWalkDescendsAtNestedGroupDelimiter_*` legs plus `OutOfScopeWireProbesUnchanged`) pass unchanged — same counts, same baseline, no fixture edits made to accommodate the change. *(Both names verified present at `main` = `c1564dd2`, 2026-08-03, rather than inherited from the issue body.)*
- **SC-002**: **Zero group requests change outcome, as measured by the corpora: every test in `ctest -L wire` (4/4), `ctest -L capi` (22/22) and `ctest -L dictionary` (17/17) that asserts a group's slices, instance count, extent, `group_index`, reserve bound or error disposition remains green, with no fixture or expected-value edit.** The **universal** claim — that *no* frame, including frames no test carries, changes outcome — is proven by `research.md` **R-1**'s source argument plus `quickstart.md` §1c's semantic-move inspection, **not** by the corpus run. *(Narrowed at Gate A round 1. The earlier wording claimed the returned values were "identical before and after" for every frame in the corpora, which the procedure does not measure: an identical **pass set** is not an identical **result set** — `offset_table_test.cpp:156` asserts `EXPECT_GT(g->entry_count(), 0U)`, which stays green across a wide range of differing extents. No differential runner or before/after result oracle is planned, and specifying one for a change whose source proof is already complete would be a new harness for no added confidence. The source proof is the strong evidence here; the corpus is the cross-check.)*
- **SC-003**: The per-instance cap fires on the dictionary path — a frame exceeding the cap returns the group-too-large disposition — **and the pin asserting it is proven RED by the FR-005b mutation** (delete `consume_group_extent`'s per-instance comparison, `:521-524` as-of `main` = `c1564dd2`; observe RED; restore; re-run green) — **taken after the FR-001 relocation has landed; a transcript taken on baseline is invalid, because the flat loop still pre-empts and the pin stays green (measured, see FR-005b)** — with the transcript recorded in the `/speckit-verify` evidence file. The existing pin `WireOffsetTable.DoSCapPerInstanceRejectsOversizedSingleInstance` (`tests/wire/offset_table_test.cpp:199-235`) already covers the positive direction and MUST stay green; its companion `WireOffsetTable.DoSCapPerInstanceAllowsAggregateOverCap` (`:164-197`) MUST stay green too, pinning that an aggregate over the cap is still accepted when no single instance breaches. Procedure: `quickstart.md` §2a and §2a-mut. **What this mutation does and does not prove, stated precisely (narrowed at Gate A round 2):** it proves the dictionary path's **cap check is load-bearing** — enforcement exists and deleting it is observable. It does **not** prove contract **C-1**'s partition coupling. The fixture it drives (`tests/wire/offset_table_test.cpp:199-235`) builds `453=1` with a single **unnested** instance (`448,447,452,802`, 4 entries, cap 3), so its flat and nesting-aware partitions are identical and no whole-check deletion can distinguish them; a future change that re-anchored `inst_start` *after a nested descent* (`:493` / `:512`) would under-count only for nested instances and stay green here. C-1's coupling is discharged by source inspection plus the FR-002 comment — see C-1's own row in `contracts/group_cap_accounting.md`'s Verification matrix, which was correctly scoped from the start. *(The mutation clause was present before Gate A round 1 with **no** artifact performing it — quickstart §2c mutated the dict-free site only, and the contract asked merely that this pin "stay green". It is now discharged, at the scope stated above.)*
- **SC-004**: The per-instance cap fires on the **dict-free** path under the same two conditions as SC-003 (fires when exceeded; pin proven RED under mutation). No dict-free **cap** pin exists today — the two named in SC-003 both drive a `table_view` and therefore exercise the dictionary path only — so this criterion is discharged by **two new** cases in the `wire_pure_tests` bucket, named `WireOffsetTable.DictFreeDoSCapPerInstanceRejectsOversizedInstance` and `WireOffsetTable.DictFreeDoSCapPerInstanceAllowsWhenCapRaised` so that a `--gtest_filter='WireOffsetTable.DictFreeDoSCapPerInstance*'` selection is deterministic and its expected count is exactly **2**. The dict-free *path* is nonetheless already well covered (roughly sixteen direct `OffsetTable{frame, mr}` constructions across `offset_table_test.cpp`, `offset_table_overflow_test.cpp`, `offset_table_error_path_test.cpp` and `hostile_input_hardening_test.cpp`, including a dict-free `group(453)` at `offset_table_test.cpp:150-157`), and every one of those MUST stay green — which is what makes FR-003's relocation checklist checkable rather than merely asserted. *(Case names fixed at Gate A round 1: the previous artifacts selected them with `ctest -R 'DictFree.*Cap'`, which matches no registered CTest test.)*
- **SC-004a**: The dict-free cap pin is bracketed on both sides — the over-cap frame fails, and the same frame with the cap raised above it succeeds — and the RED-under-mutation transcript for that pin is present in the `/speckit-verify` evidence file, quoting the mutation applied and the resulting failure output. A missing transcript fails this criterion regardless of how many tests are green. The transcript MUST also record the **number of cases the selector actually ran**; a zero-match run exits 0 and is otherwise indistinguishable from a pass (see `quickstart.md` Build constraints).
- **SC-005**: On the dictionary path, `OffsetTable::group()` contains exactly **one** traversal of a group's entries. Discharged by **two** artifacts: source inspection at delivery (`quickstart.md` §1c — the second walk is absent from the dictionary branch) **and**, from Gate A round 2, the executable structural pin `WireOffsetTable.FR001_SingleTraversalSourceInspection` (FR-001b, SC-005b), which makes it a standing regression gate rather than a one-time human read. The performance consequence is carried by SC-006's benchmark. *(Deliberately not stated as an entries-visited count: no such counter exists, and adding one to a hot path to satisfy a success criterion would be scope creep.)*
- **SC-005b**: The FR-001b red-first structural pin `WireOffsetTable.FR001_SingleTraversalSourceInspection` **exists, was observed RED on the unmodified tree before the relocation, and is green in the delivered tree** — with the RED output captured in `.specify/decisions/085-fold-flat-cap-loop-verify.md`. A pin authored *after* the relocation, or one whose RED was never observed, **fails** this criterion: it would be a green-from-birth structural assertion, which is Article VII §3's failure mode, not its discharge. The `--gtest_filter='WireOffsetTable.FR001_SingleTraversalSourceInspection'` selection MUST report count **1**; a zero-match run exits 0 and is otherwise indistinguishable from a pass (`quickstart.md` Build constraints). Procedure: `quickstart.md` §0. *(Added at Gate A round 2 — this is the artifact that moves the VII §3 row from an invented disposition to a compliance PASS.)*
- **SC-005a**: The FR-002 comment **exists on the dictionary branch of the delivered `OffsetTable::group()` and states both required parts** — why the second walk went (the redundancy argument's conclusion) **and** contract C-1's standing property (the cap is now enforced solely by `consume_group_extent` over the instances whose extent it returns) — with every site anchored by function and role first per FR-007b. A comment that instead states the delimiter-source equality as a *standing* invariant **fails** this criterion: after the removal that equality no longer governs cap accounting, and enshrining it would misdirect the next maintainer of these lines. Verified by inspection (`quickstart.md` §1d). *(Added at Gate A round 1. The comment is the sole carrier of the plan's only High-impact-risk mitigation and, before this criterion, nothing in the bundle checked that it landed at all, let alone what it said.)*
- **SC-006** *(primary authority, added at the fresh Gate A round: the disposition below rests first on the **baseline's own provenance**, not on locally measured host noise. `bench/baselines/wire/typed_read_group_bench.json`'s `Flat2` row states verbatim that its ±5% is the A/B tolerance vs the pre-083 run, **"not a recorded-baseline regression budget"**; `ModeC2` and `ModeC8` carry `pre083_median_ns: null` with no budget note. 083 — the feature that wrote the file — recorded three times that these are **seed rows** for which Article VIII §2's regression budget has no prior referent. Leg 2 is therefore the applicable recorded A/B tolerance. No constitutional waiver and no baseline reseed are requested or required; the host-noise measurements below are corroborating, not load-bearing.)*: The three existing cases `BM_TypedReadGroup_{Flat2,ModeC2,ModeC8}` (`bench/wire/typed_read_group_bench.cpp`) show no regression, discharged by **two** measurements, both carried as numbers in the PR body rather than asserted in prose. No new bench source is added and **`bench/baselines/` is not updated** — this is not an intentional perf change.
  1. **The constitutional check (`[const §VIII.2]`, `.specify/constitution.md:185` — ±5% vs `bench/baselines/`).** Against the checked-in baseline `bench/baselines/wire/typed_read_group_bench.json`, at that file's own recorded parameters: `build_type: release`, `repetitions: 9`, `min_time_s: 0.5`, same host. Each case's `_median` aggregate **`cpu_time`** from `--benchmark_format=json` is compared against that case's `seed_median_ns` in the baseline (`386` / `661` / `1643` ns). Procedure and the exact commands: `quickstart.md` §3a.

     **Pass condition, stated so this leg is decidable — and so it cannot be satisfied by run selection.** The run count is fixed **in advance at three**, and **all three are reported**; leg 1's observation is the **median of the three per-case medians**. Leg 1 **PASSES** when that aggregate puts all three cases within ±5%. Otherwise leg 1 is recorded as **`WITHIN-NOISE`**. *(Corrected at the fresh Gate A round: the prior wording — "a single recorded run … if repeated runs cannot achieve that" — let an implementer repeat until one run landed inside ±5% and record only that one. The file's own drift table shows a ~9-point spread on `Flat2`, which is exactly the variance that makes the distinction matter.)* — an explicit, non-blocking disposition — and it requires *both*: (a) the same block run against **unmodified `main`** in the same session, with those control numbers recorded alongside, and (b) leg 2's A/B within ±5%. `WITHIN-NOISE` without the `main` control attached is **not** a disposition and fails this criterion. **Leg 1 alone never blocks and never clears**; a >±5% **leg 2** delta is the blocking signal. *(This is the procedure, not a hedge: see leg 2 for the measurements that forced it.)*
  2. **Same-session `main`-vs-branch A/B — the discriminating measurement for *this change*, and the one that governs the disposition.** Both runs on the same machine in the same session. This is not a substitute for (1); it is what isolates the delta attributable to the relocation from host drift. Two independent reasons leg 1 cannot carry a verdict on its own, both **measured** on 2026-08-03, same host as the baseline's `context`:
     - *Run-to-run drift exceeds the budget on unmodified code.* Three back-to-back baseline comparisons against **unmodified `main`** returned `+7.1% / +1.8% / +5.8%`, then `+4.0% / +4.9% / +2.2%`, then `−2.4% / +2.1% / +2.7%` — a ~9-point spread on `Flat2`, with **two of nine readings outside ±5% on code identical to the baseline's**.
     - *The baseline does not record which clock its `seed_median_ns` came from*, and the choice is not immaterial here: on the same run, `real_time` medians sit **3.7–4.6% below** `cpu_time` medians (`359.4`/`648.7`/`1623.8` vs `376.8`/`675.1`/`1686.6` ns). Reading `Flat2` as `real_time` gives −6.9%; as `cpu_time`, −2.4%. This spec fixes the comparison on **`cpu_time`**, because that is the field the repo's own comparator reads (`tools/bench_compare.py::load_benchmarks`) and therefore the house convention — but the ±4% clock ambiguity is a second reason a lone leg-1 row is not a finding.

     *(Both tables and the underlying runs in `quickstart.md` §3a.)*

  *(Basis corrected at Gate A round 2. The round-1 SC-006 specified only the A/B and the plan's VIII §2 row cited it as the ±5%-vs-`bench/baselines/` discharge — a status row naming an artifact that does not perform the named comparison. A null result remains a **pass** on both legs: the bench exists to prove no regression, not to claim a win (A-004).)*
- **SC-007**: Zero change to exported symbols, public headers, error enum values and the C-ABI version, verified by the existing ABI-hygiene gates.
- **SC-008**: The full sanitizer matrix (ASan/UBSan/TSan) and the static-analysis gate pass with no new findings attributable to this change.
- **SC-009**: L-063-4 states leg 2 DELIVERED, retains leg 1's descope evidence, names both surviving flat rules (with their differing delimiter sources per FR-007a), and contains no dangling reference. Every reference the feature adds names its site by function and role and stamps any line number as-of the merge commit (FR-007b); the historical brackets are byte-unchanged. Verified by inspection against the merged tree.
- **SC-010**: The dict-free trailing-field false positive (FR-003a) is recorded as a named limitation row **and** cites its tracking issue **fixpp#220** from that row, before close-out. A row without the issue number does not discharge this criterion. Both the row and the issue MUST state the default-config unreachability and the `max_group_entries_per_instance < max_offset_entries - 1` precondition; a record that presents it as a default-path defect **fails** this criterion for overstating severity, exactly as one that omits it fails for understating.

## Assumptions

- **A-001**: The redundancy argument in Context §"Why Walk 2 cannot fire" was derived by the orchestrator from `src/wire/offset_table.cpp` at `main` = `c1564dd2` on 2026-08-03. `/speckit-plan` MUST re-verify each of its four steps against the tree it plans on, rather than inherit the conclusion. If any step fails, FR-002's behaviour-preservation claim is void and the feature must be re-scoped, not patched.
- **A-002**: Leg 1 of L-063-4 stays descoped. 083 measured its target population as empty across all ten dictionaries and showed a literal implementation would break the 485-context shape that *is* reachable. This feature does not re-open it, and the `group_slices_status()` splitter at `:712-733` therefore stays flat.
- **A-003**: fixpp#180 is closed and stays closed. 072-nested-group-hardening delivered what it asked for; the splitter residual was never its deliverable.
- **A-004**: `group()` is on the group-read hot path (reached from `group_slices_status:667` on every materialization), so Article VIII §3 applies and a benchmark ships **in this PR**. It is planned in rather than waited for at Gate B. The expected direction is a small improvement, since the change removes work; the bench exists to prove no regression, not to claim a win. Discharged by the three existing `BM_TypedReadGroup_*` cases per SC-006 — no new bench source.
- **A-005**: The dict-free path's existing looseness — its last segment extending to `entries_.size()` and thus counting trailing non-group fields — is inherited behaviour, not a defect introduced or repaired here. It is preserved without semantic change (FR-001a's C-3 checklist), recorded as a limitation, and tracked by its own issue (FR-003a, SC-010). It is a **false positive** on a defence-in-depth cap, never wrong returned data, which is why deferring it is proportionate.
- **A-006**: No configuration surface, no compatibility mode, and no runtime switch is introduced. The consolidation is unconditional on the dictionary path.
- **A-006a**: The cap is fixture-reachable. `max_group_entries_per_instance` defaults to `4096` (`include/fixpp/wire/offset_table.hpp:28`) but is a per-parse `OffsetTable::Config` member (`:95`), and three existing tests already drive it down to `3` via `tight_cfg`. FR-004/FR-005/SC-004 are therefore constructible without an oversized frame and without touching the default. *(Verified 2026-08-03 — checked because a cap that were global-and-fixed would have made SC-004 undischargeable, and that is a `/plan`-time discovery, not an `/implement`-time one.)*
- **A-007**: `/speckit-verify` runs clang-only locally; the gcc-release and MSVC legs are CI-only. Given the change is a code removal with no platform-specific construct, no platform-divergent behaviour is expected — but the CI legs remain the authority, not the local run.

## Normative References

*(Added at Gate A round 1. Its absence was a direct `[const §VI.5]` violation and made `checklists/requirements.md`'s "All mandatory sections completed" tick false — see that file's Note 6.)*

Per `[const §VI.5]` (`.specify/constitution.md:164`), the exact coverage-index, design-doc and behaviour-record entries that inform this spec. **No new OFFICIAL catalogue rows are introduced** — this feature removes a provably-unreachable code path from an existing row's implementation and changes no observable behaviour, so traceability is via existing rows plus a B&L L-row update (`[const §VI.4]` is therefore not engaged; `[const §VI.6]`'s closure conditions are satisfied by the FR-006/FR-007 L-063-4 update and the FR-003a limitation row). Every anchor below was resolved against the tree at `main` = `c1564dd2` on 2026-08-03 before it was written.

**Coverage-index entries**

- **`[FIX50SP2 §3.2] Repeating groups (NoXxx delimiter, ordered field list, nested groups)`** — `spec/coverage-index.md:184` (catalogue rows **W-006 / W-007 / D-010**). The row this feature lives under, and the only one it touches. The *NoXxx delimiter* clause is the instance-boundary rule FR-001 consolidates; the *nested groups* clause is why the nesting-aware walk exists at all and why its partition is coarser than the flat one (Context §"Why Walk 2 cannot fire", steps 2–3). The row already records 063's nesting-aware extent and 083's per-context delimiter resolution; 085 adds no behaviour to it.
- **`[FIX50SP2 §3] Tag=Value encoding — field format, SOH delimiter, ASCII`** — `spec/coverage-index.md:181` (catalogue row **W-001**). The substrate both walks traverse: `entries_[k].tag` is a decoded tag=value field, and the flat boundary rule is a §3-encoding-level predicate applied to a §3.2 question. Cited because the surviving dict-free check remains exactly that — a wire-derived rule with no dictionary above it.

**Design-doc entries** *(anchored to `.specify/2b-wire.md`, which `research/G19-fix-fpml-iso20022/decisions/2b-wire.md` records as the signed-off v0.2 doc; these are design sections, not coverage-index rows)*

- **`[2b §4.4] OffsetTable — (tag, offset, length) index`** — `.specify/2b-wire.md:309`. Owns `OffsetTable`'s shape and its entry-count budget, including the **4096 default cap** whose equality with `default_max_group_entries_per_instance` is what makes the dict-free breach unreachable under default `Config` (FR-003a, `research.md` R-2, `data-model.md` E-2). FR-003a's severity framing rests directly on this section.
- **`[2b §4.7] Repeating-group view`** — `.specify/2b-wire.md:507`. Owns the group sub-index that `group()` feeds and the "group-instance boundaries by the dictionary's first-field-of-group rule" contract. The user-visible surface FR-002 must leave unchanged (`group_view<GroupT>::size()` / `operator[]`) is specified here.

**Constitutional obligations this feature leans on**

- **`[const §VII.3]`** (`.specify/constitution.md:173`) — TDD, red-green-refactor. **Complied with**, not dispositioned around: the red-first artifact is FR-001b's structural pin `WireOffsetTable.FR001_SingleTraversalSourceInspection`, RED on baseline and GREEN after the relocation, verified by SC-005b. The FR-005a(i)/FR-005b mutation transcripts are supplementary. Read together with `.specify/constitution.md:86` and `[const §XX.1]` (`:402`), which permit only compliance, amendment or a rationale-bearing waiver for a conflict with a mandatory article. Reasoning in Clarifications §Session 2026-08-03 (Gate A round 2) and `plan.md`'s Constitution Check.
- **`[const §VII.8]`** (`:178`) — isolation-safe tests join an existing grouped bucket and are selected by `ctest -L <label>`, never `-R <exe-name>`. **All three** new cases join `wire_pure_tests` — SC-004's two dict-free cap pins and FR-001b's structural pin — with **no new executable**; the one build change is `FIXPP_SRC_DIR` added to that same existing bucket in `tests/wire/CMakeLists.txt`, which is why `ctest -L wire` stays at 4. `quickstart.md` selects by label plus `--gtest_filter` on the bucket binary.
- **`[const §VIII.2]`** (`:185`) — ±5% vs `bench/baselines/`, no baseline update (this is not an intentional perf change). SC-006 leg 1, against `bench/baselines/wire/typed_read_group_bench.json` at that file's own recorded parameters; SC-006 leg 2's same-session A/B is supplemental noise control, not the constitutional check.
- **`[const §VIII.3]`** (`:186`) — no perf change merged without a benchmark in the same PR. A-004 commits the three existing `BM_TypedReadGroup_*` cases to this PR rather than deferring to Gate B.
- **`[const §VIII.5]`** (`:191`) — zero hot-path allocation. FR-009's allocation leg, discharged by construction.
- **`[const §IX.1]`** (`:198`) — ≥95% line / ≥85% branch on touched modules, and the binding no-silent-uncovered-error-path rule. Engaged twice: the relocated cap return becomes covered for the first time (`research.md` R-3), and the now-false waiver at `tests/wire/offset_table_error_path_test.cpp:10-14` must be repaired. Measured at `/speckit-verify` and recorded in `.specify/decisions/085-fold-flat-cap-loop-verify.md`, which is the artifact this clause names.
- **`[const §IX.4]`** (`:206`) — `clang-format` is a Tier-1 gate. This is why FR-001a permits mechanical re-indentation rather than demanding byte-identity.
- **`[const §X.1]`** (`:220`) — the C ABI is a versioned contract, GA-frozen at 1.5.0. Cited as **not engaged**: FR-008/SC-007 hold the surface unchanged and no `capi/` file is touched, so §X's four mandatory controls are not triggered. Named explicitly so "ABI untouched" is a checked claim rather than an omission.

**Behaviour records**

- **`spec/behaviors-and-limitations.md` L-063-4** (`spec/behaviors-and-limitations.md:1701`) — the row this feature closes out. Its **leg 2** (*"fold the redundant flat cap loop into the same traversal"*) is the deliverable; **leg 1** stays descoped with 083's measured evidence (A-002); the third disposition 083 added (the `consume_group_extent` nesting correctness claim, repaired by 083) is untouched. FR-006/FR-007/FR-007a/FR-007b and SC-009 govern the update.
- **`spec/coverage-index.md:995`** — the 072-nested-group-hardening section that records `#180` as closed and leg 2 as orphaned into **fixpp#214**, the issue this feature closes. It also names the residual site precisely (`src/wire/offset_table.cpp:586`, cap at `:591`), which is what made `#214` filable; those numbers are 083-era and this feature moves them, hence FR-007b's anchoring rule.
