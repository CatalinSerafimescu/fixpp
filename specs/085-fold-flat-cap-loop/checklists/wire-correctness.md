# Checklist: Wire correctness & semantic preservation

**Feature**: `085-fold-flat-cap-loop` · **Created**: 2026-08-03 · **Audience**: Gate B
**Domain**: the `src/wire/offset_table.cpp` change itself — does the relocation preserve what it must, and is the removal actually safe?

> **Why this domain exists.** The feature's entire risk is that a change advertised as a no-op is not one. The redundancy argument (`research.md` R-1) is a *proof*, and a proof's premises can rot. These items check the premises, the preservation, and the one thing no test can see.

## Premises of the removal

- [x] CHK001 Is R-1 **step 1** (both walks read `entries_[count_idx + 1].tag` — `group():545,551` vs `consume_group_extent():450,458`, same `count_idx` passed at `:575`) re-verified against the tree being implemented on, not inherited from `/plan`? [A-001, C-1a, T001] — PASS: `research.md` R-1 step 1 re-derives this against `c1564dd2` and A-001 obliges `/plan` to re-verify rather than inherit; audit-time re-check confirmed all five anchors (`:450,458,545,551,575`) resolve exactly as cited, byte-for-byte, in `src/wire/offset_table.cpp` at current `main` (`c1564dd2`, `HEAD` = `a9997077` one commit ahead with an empty diff on this file).
- [x] CHK002 Is R-1 **step 2** (`consume_group_extent` opens an instance only where `entries_[k].tag == delim`, `:477`, so the flat cut-set is a **superset**) still true of the delivered `consume_group_extent`? [R-1, T001] — PASS: `research.md` R-1 step 2; confirmed `:477` reads `while (k < entries_.size() && inst < declared && entries_[k].tag == delim)` verbatim in source.
- [x] CHK003 Is R-1 **step 4** (the nesting-aware walk runs first and returns `err_group_too_large` at `:576-578` on breach, **before** `:584`) still true — i.e. is `:576-578` still unconditional? [R-1, T001] — PASS: confirmed source `:576-578` is `if (overflow) { return err_group_too_large<group_index>(); }`, unconditional, and precedes `:584`; `research.md` R-1 step 4 plus the empirical corroboration (deleting `:521-524` on baseline leaves the dictionary pin green — the redundancy observed, not just argued).
- [x] CHK004 Are all five of `consume_group_extent`'s exceptional exits still accounted for — including that the depth guard returns `count_idx` (not `first`) but sets `overflow` first, so the underflowing `group_end - first` state stays unreachable? [R-1, T001] — PASS: `research.md` R-1 "Degenerate exits" table enumerates all five with reachability from `group()`; confirmed source `:446-449` (`overflow = true; return count_idx;`) matches exactly. Also independently re-derived by both Gate A reviewers per the Gate A record's Convergence story.
- [x] CHK005 If any premise fails, is the feature **re-scoped rather than patched**, per T001's stop-gate? [A-001, T001] — PASS: T001 states verbatim "If any step fails, **STOP** — FR-002's no-op claim is void and the feature is re-scoped, not patched," and A-001 states the same disposition.

## Semantic preservation of the relocated block

- [x] CHK006 Was **C-3's checklist walked item-for-item against the contract table**, not against a summary? (An abbreviated restatement is what `/speckit-analyze` F1 caught; the dropped items were the load-bearing ones.) [FR-001a, C-3, T007] — PASS: T007 explicitly forbids re-summarising the checklist and directs "read it there ... an abbreviated restatement drops items and the dropped ones are the trap," directly responding to the cited `/speckit-analyze` finding. This is a forward obligation on `/speckit-implement` and is re-walked at Gate B against the delivered diff.
- [x] CHK007 C-3 #1 — is `inst_start` still declared on its **own statement** before the loop, not merged into the for-init? [C-3, T007] — PASS: `contracts/group_cap_accounting.md` C-3 table row #1, cross-referenced against R-4's corrected quoted block (the round-1 draft had merged it into a for-init and was corrected precisely because that would have violated this item).
- [x] CHK008 C-3 #3 — is the boundary predicate unchanged in **both disjuncts and their order**? [C-3, T007] — PASS: C-3 table row #3, quoting the exact predicate; confirmed verbatim against source `:586`.
- [x] CHK009 C-3 #4 — is the **early `continue` on non-boundary** retained, with **no `else`-inversion**? *(The single most likely unintended edit: semantically identical, invisible to T006's indentation-keyed pin, and green under every behavioural test.)* [C-3, FR-001a, T007] — PASS: the obligation is complete and unambiguous, not merely aspirational — C-3 table row #4 names it, FR-001a's prohibition list ("MUST NOT be ... re-ordered, or restructured") covers it, T007 names the exact failure mode by name and states it is "caught by **nothing**" automated, assigning the item-for-item diff walk as the explicit mitigation, and `quickstart.md` §1c states "an inverted condition ... violates FR-001a even if every test is green." This checklist item is itself part of the mitigation (Audience: Gate B — CHK009 is re-walked against the delivered diff, not only at `/speckit-implement`). Audit-time verification: the file's only other flat-boundary walk (`group_slices_status:714-716`) already uses the **positive** form `if (boundary) { ... }`, confirming the inversion is a real, plausible failure mode (a "helpful" stylistic match to the sibling) — which is exactly why T007 names it as the item to watch, not evidence the bundle is silent about it. **Re-dispositioned 2026-08-03, post-audit, on the orchestrator's decision: the hardening the audit escalated was ACCEPTED and applied.** FR-001b now carries a **second discriminant** — no 8-space `if (!boundary) {`, exactly one at 12-space — so an `else`-inverted relocation makes the red-first pin **fail** instead of passing. This item is no longer guarded by a manual diff walk alone; it is a standing gate. The audit's three reasons for not applying it unilaterally were: no mandatory article forces it; the sweep risked the RC-B pattern; and it appeared to reverse a signed-off Gate A decision. The third was the decisive one and it was **wrong on inspection** — Gate A round-2 disagreement #1 rejected a *nesting-sensitive tight-cap **behavioural** fixture* aimed at **C-1's partition coupling**, a standing-contract concern this removal does not introduce. This is two `std::string::find` assertions inside a test T006 already creates, aimed at **C-3 #4 / FR-001a** — the relocation's own fidelity, which this change absolutely does introduce. Different risk, different mechanism, different cost class. The sweep was scoped to the **six operative loci** and left the nine historical/narrative ones byte-unchanged, per this bundle's mark-don't-edit convention.
- [x] CHK010 C-3 #6 — is the comparison still **strict `>`**, not `>=`? [C-3, T007] — PASS: C-3 table row #6; confirmed source `:591` reads `if (inst_count > cfg_.max_group_entries_per_instance)`.
- [x] CHK011 C-3 #7/#8 — does a breach return **immediately**, and is `inst_start = k` re-anchored **after** the check? [C-3, T007] — PASS: C-3 table rows #7/#8; confirmed source `:592` (`return err_group_too_large<group_index>();`) and `:594` (`inst_start = k;`) in that order.
- [x] CHK012 C-3 #9 + FR-009's ordering obligation — is the shared `return group_index{...}` reached with the same values from outside the `if`/`else`, and is `group_end = entries_.size()` still assigned **before** the loop? [C-3, FR-009, T007] — PASS: C-3 table row #9 + the separate Ordering obligation; confirmed source `:581` (`group_end = entries_.size();`, inside the `else`, before any loop) and `:596` (the shared `return`).
- [x] CHK013 Does the diff for the `else` branch read as a **move plus a uniform indent shift** — nothing parameterised, extracted, merged, re-ordered or restructured? [FR-001a, C-3, T007] — PASS: C-3 states this framing verbatim ("The diff for this branch MUST read as a move plus a uniform indent shift") and FR-001a's prohibition list matches; `research.md` R-4 quotes the real baseline block so the delivered `else` body has a byte-exact comparison target.

## The dictionary path after removal

- [x] CHK014 Does `group()`'s dictionary branch contain **exactly one** traversal of the group's entries — no re-walk of `[first, group_end]` after `consume_group_extent` returns? [FR-001, C-2, SC-005] — PASS: FR-001/C-2/SC-005, discharged doubly by source inspection (`quickstart.md` §1c) **and** the FR-001b red-first structural pin (SC-005b) added specifically because Article VII §3 is a mandatory constitutional article and manual inspection alone was judged insufficient for it at Gate A round 2 (RC-A).
- [x] CHK015 Is the FR-002 comment present on the dictionary branch, and does it state **both** required parts — why the second walk went, **and** C-1 as the standing property? [FR-002, SC-005a, T008] — PASS: FR-002 and C-2's "Justification obligation" state the two required parts verbatim; SC-005a is a dedicated success criterion for the comment's *content*, not merely its presence; T008 assigns it; `quickstart.md` §1d gives a check-table for both parts and a documented FAIL condition.
- [x] CHK016 Does that comment **avoid** stating the C-1a delimiter-source equality as a standing invariant? *(It is a discharged delivery-time premise. The round-1 contract asserted the opposite and was false against the tree — `group_slices_status:704-715` already diverges benignly.)* [C-1, C-1a, FR-002] — PASS: FR-002 explicitly forbids this ("MUST NOT assert that `group()`'s local `delim` and `consume_group_extent`'s must stay equal *as a standing rule*"), C-1a restates why, and `quickstart.md` §1d states the FAIL condition explicitly, with the `:704-715` divergent-source precedent verified in source (confirmed `group_delim_fn_` at `:705` and the wire-derived `:551` are indeed two independent sources over the same extent, exactly as claimed).
- [x] CHK017 Is the returned `group_index` — `no_tag`, `first_entry()`, `entry_count()` — unchanged in value on every frame? [E-1, FR-002, SC-002] — PASS: `data-model.md` E-1 states the invariant this relies on ("the removed loop never wrote `group_end`; it only read it") and verifies it by construction (`:584-595` contains no assignment to `group_end`, confirmed in source: the block only reads `group_end`, `delim`, `first`, `k`); SC-002 is the regression-corpus discharge.
- [x] CHK018 Is the error **disposition, origin and ordering** unchanged: `err_group_too_large` still reaching callers only via `:577`? [C-2, SC-002] — PASS: C-2's post-conditions table states this; confirmed source `:576-578` is the sole surviving dictionary-path origin post-removal (the `:592` return only exists on the dict-free branch after relocation).

## The dict-free path

- [x] CHK019 Is the cap still enforced on the dict-free path — i.e. was the loop **relocated**, not deleted? *(Deleting it outright is a silent fail-open; that is the whole reason this feature is not a one-line removal.)* [FR-003, C-3] — PASS: FR-003 and C-3's Statement both require this explicitly, and `spec.md` Context §"Why Walk 2 nevertheless cannot simply be deleted" states the fail-open rationale directly.
- [x] CHK020 Are the dict-free path's delimiter source (wire, `:551`), segmentation, extent bound (`entries_.size()`) and error value all unchanged? [FR-003, C-3] — PASS: C-3's post-conditions table enumerates exactly these four properties with their current values; confirmed against source (`:551` wire-derived `delim`, `:581` `entries_.size()`, `:586` boundary rule, `:592` error value) — all four already read exactly as the contract states pre-relocation.
- [x] CHK021 Do the ~16 existing dict-free `OffsetTable{frame, mr}` tests stay green — including the dict-free `group(453)` at `offset_table_test.cpp:150-157`? [FR-003, R-5, T011] — PASS: `research.md` R-5 enumerates the ~16 sites by file:line across four test files; confirmed the cited dict-free `OffsetTable t{*fv, &arena}` construction followed immediately by `t.group(453)` exists in `tests/wire/offset_table_test.cpp` in that region; T011/SC-002 assign the regression run.
- [x] CHK022 Is the dict-free trailing-field over-count **preserved, not tightened** — with its default-config unreachability stated? [FR-003a, A-005, SC-010] — PASS: FR-003a states the preservation-not-repair disposition and the unreachability precondition explicitly; verified the arithmetic independently against source — `include/fixpp/wire/offset_table.hpp:27-28` both defaults are `4096`, `src/wire/offset_table.cpp:326` clamps the table at `cfg_.max_offset_entries` — confirming R-2's claim that the breach is unreachable under default `Config`.

## What no test can see

- [x] CHK023 Is **C-1** (the nesting-aware walk caps the same instances whose extent it returns — `:477-478` opens, `:521-524` caps, `:527` returns) recorded as **standing**, and **C-1a** as **discharged**? [C-1, C-1a] — PASS: `contracts/group_cap_accounting.md` C-1 is headed "STANDING" and C-1a "NOT standing," each with an explicit "Status" line stating this distinction; confirmed all three C-1 anchors resolve exactly in source.
- [x] CHK024 Is the scope of what FR-005b's mutation proves stated honestly — **C-1's cap-existence half only**, on an *unnested* instance, with the partition coupling left to source inspection plus the FR-002 comment? [FR-005b, SC-003] — PASS: FR-005b, SC-003 and the Verification matrix all state this narrowed scope explicitly and identically (added/corrected at Gate A round 2), and `quickstart.md` §2a-mut repeats the same narrowing in its "What this transcript proves, and what it does not" section.
- [x] CHK025 Is FR-008 satisfied — `include/fixpp/wire/offset_table.hpp` untouched, no signature, `Config` member, default, exported symbol, error enum value or C-ABI version changed? [FR-008, SC-007, T021] — PASS: FR-008/SC-007 state this exactly; T021 assigns the verification command (`git diff main -- include/ src/capi/` empty); `data-model.md` records the header as the confirmation artifact. Realizability note: `group_index` (the one value-typed entity this feature reasons over) is a fully-defined nested class in `offset_table.hpp:169` today, not forward-declared, with no pointer/reference to any incomplete or deferred-spec-owned type — this feature adds no new type, so the realizability sub-check is vacuous here (confirmed by direct inspection).
- [x] CHK026 Is FR-009's allocation leg true by construction — no `new`/`delete`/container growth introduced on either path? [FR-009, `[const §VIII.5]`] — PASS: FR-009 and the Verification matrix's dedicated "no SC" row both state this is discharged by construction rather than by a test artifact, with the reasoning made explicit (a loop deletion + move introduces no new expression capable of allocating, in a `noexcept` member with no container operation on either branch) rather than left implied — this is itself the discharge the Gate A round-1 finding asked for.

## Notes

- Items are dispositioned at `/speckit-checklist-audit` (pipeline step 9) as PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED:`<reason>`. Completeness/Clarity/Consistency gaps may **not** be WAIVED.
- CHK009 is the item to spend real attention on. It is the only one whose failure mode is invisible to every automated gate this feature ships. Dispositioned PASS (see above) — the bundle specifies the obligation completely and assigns it an explicit human-walked guard at two points (`/speckit-implement` T007, Gate B re-walk of this checklist); an optional zero-runtime-cost hardening (extending FR-001b's structural pin to also key on the `if (!boundary) {` token) was evaluated and is recorded as an escalation, not applied.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 26 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 26 |

### SPEC-FIXED items

None.

### DD-DECIDED items

None.

### WAIVED items

None.

Anchors spot-verified against `src/wire/offset_table.cpp`, `include/fixpp/wire/offset_table.hpp`, `tests/wire/offset_table_test.cpp` at `main` = `c1564dd2a1c021b0f68c30cb1983a0ad6d2626f5` (current `HEAD` = `a9997077`, one commit ahead, empty diff on `src/wire/offset_table.cpp`): `:442,446-449,450,451,454,458,461,465,474-476,477,478,491,503,504,510,521-524,527,528,536,545,546,551,553,566,567,575-578,579,581,582,584-596,599,634,639,641,667,704-716,733` — all resolve exactly as cited in `contracts/group_cap_accounting.md`, `research.md`, and `data-model.md`. `include/fixpp/wire/offset_table.hpp:27-28,169` and `src/wire/offset_table.cpp:326` also resolve exactly. No dangling reference found.
