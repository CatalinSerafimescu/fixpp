# Checklist: Verification evidence & false-green resistance

**Feature**: `085-fold-flat-cap-loop` · **Created**: 2026-08-03 · **Audience**: Gate B
**Domain**: does the evidence this feature ships actually prove what it claims — and can any of it pass while measuring nothing?

> **Why this domain exists.** This bundle's validation procedure shipped a **false-green mechanism in four consecutive Gate A rounds**, twice introduced by the fix for the previous one: zero-match `ctest -R` selectors that exit 0 → count guards using `return 1` at top level → a stale `/tmp` JSON parsed after a failed benchmark run → a per-name comparator whose early failure was overwritten by a shell loop. Assume a fifth exists until checked.

## Article VII §3 — the red-first artifact

- [x] CHK101 Does `WireOffsetTable.FR001_SingleTraversalSourceInspection` **exist**, in `tests/wire/offset_table_test.cpp`, in the `wire_pure_tests` bucket, with **no new executable**? [FR-001b, `[const §VII.8]`, T006] — PASS: FR-001b names the exact test name and home; T006 assigns authoring it in the existing bucket "no new executable"; confirmed `wire_pure_tests` is CTest test #68 (registered bucket, `ctest -L wire` = 4/4 verified against the live build directory) and `offset_table_test.cpp` is already compiled into it (`tests/wire/CMakeLists.txt:43`).
- [x] CHK102 Was it **authored and observed RED on the unmodified tree, before** the relocation — with the RED output captured verbatim in `.specify/decisions/085-fold-flat-cap-loop-verify.md`? [FR-001b, SC-005b, T006] — PASS: `quickstart.md` §0 is explicitly the only step that runs on the unmodified tree, states "Running it after the relocation does not discharge SC-005b," and T006 mandates capturing the RED transcript into the named verify file. This is a forward `/speckit-implement` obligation, stated unambiguously and sequenced (T006 before T007, tasks.md's "one ordering constraint" section).
- [x] CHK103 Is it GREEN after the relocation, with that transcript recorded beside the RED one? [SC-005b, T009] — PASS: T009 assigns exactly this re-run and recording.
- [x] CHK104 Does `tests/wire/CMakeLists.txt` define `FIXPP_SRC_DIR` for the bucket, mirroring `tests/dictionary/CMakeLists.txt:178-183`? Is that the **only** build change the feature makes? [FR-001b, T002] — PASS: T002 states the exact `target_compile_definitions` line and mirrors the precedent "verbatim in mechanism"; audit-time verification confirms **both halves** empirically: (a) `tests/dictionary/CMakeLists.txt:178-183` does carry exactly this `FIXPP_SRC_DIR` line for `dictionary_load_any_tests`, confirming the precedent is real and the mechanism copies correctly; (b) `tests/wire/CMakeLists.txt`'s current `target_compile_definitions(wire_pure_tests ...)` calls (line 70 `FIXPP_TEST_HOOKS`, lines 74-75 `FIXPP_DICT_DATA_DIR`) do **not** yet include `FIXPP_SRC_DIR` — confirming the bucket does not define it today, exactly as FR-001b claims, and that this one line is genuinely the only build-file change.
- [x] CHK105 Does the pin's comment block state that it is a **source-inspection assertion, not a behaviour test**, and **why** behaviour cannot distinguish the two states? [FR-001b, T006] — PASS: FR-001b's *Name and home* bullet states this requirement verbatim, T006 restates it, and both point to the shipped precedent `tests/dictionary/load_any_test.cpp:143-171` — confirmed by direct read: its comment block (`:134-142`) states almost the identical rationale ("Behavior Scenarios 1/3 would still pass if each site sniffed independently; this gate is what pins 'defined once, not duplicated'"), giving FR-001b's pin a verified, structurally identical template to mirror.
- [x] CHK106 Does its discriminant actually discriminate on the delivered tree — is the 4-space / 8-space / 16-space key set still mutually exclusive after `clang-format` has run? [FR-001b] — PASS, verified independently rather than taken on the bundle's word. `.clang-format` (repo root) sets `BasedOnStyle: Google`, `IndentWidth: 4`, no `NamespaceIndentation`, `AllowShort*OnASingleLine`, or brace-style override that would collapse or re-nest a multi-statement `else` block — so indentation here is a deterministic function of brace-nesting depth (namespace 0 → function body 4 → `else`-block body 8), not of line content. The current tree already exhibits exactly this pattern under the live clang-format gate: `group_end = entries_.size();` sits at 8-space `else`-body indentation (`:581`) and `std::size_t inst_start = first;` sits at 4-space function-body indentation (`:584`) today — i.e. the discriminant's two key depths are not hypothetical, they are the tree's current clang-format-compliant state. Re-indenting the moved block one level (+4 spaces) does not risk a reflow: measured the longest candidate line (`bool const boundary = ...`) at 90 chars today, 94 after the shift, both under `ColumnLimit: 100`. Checked the collision case too: `consume_group_extent`'s own `std::size_t const inst_start = k;` (`:478`) is different token text (`const`, `= k` not `= first`) so it cannot collide with either key regardless of indentation, and `group_slices_status`'s `std::size_t inst_start = first;` (`:712`) sits at 16-space, confirmed by direct measurement to match neither the 4-space nor 8-space key.
- [x] CHK107 Is `plan.md`'s Article VII §3 row **`PASS (planned)` against this pin** — not the withdrawn "NOT CLEANLY APPLICABLE", and not resting on mutation testing as a substitute? [`[const §VII.3]`, `[const §XX.1]`] — PASS: confirmed `plan.md`'s Constitution Check table row reads exactly `PASS (planned)` with the withdrawal of "NOT CLEANLY APPLICABLE" stated inline and cross-referenced to `## Gate A`'s round-2 RC-A closure, which is corroborated by the Gate A decision record (`.specify/decisions/085-fold-flat-cap-loop-gatea.md`), confirmed to exist and to record this exact resolution.

## Mutation evidence — both sites

- [x] CHK108 Is the **dict-free** mutation transcript (FR-005a(i)) recorded: check deleted → pin RED → restored → green? [FR-005a, SC-004a, T015] — PASS: FR-005a(i) states the sequence exactly; T015 assigns it with an explicit stop-condition ("If the pin stays GREEN with the check deleted, STOP"); `quickstart.md` §2c mirrors both.
- [x] CHK109 Is the **dictionary** mutation transcript (FR-005b) recorded, and does every statement of it carry the **post-relocation only** qualifier? [FR-005b, SC-003, T012] — PASS: FR-005b states the qualifier and measured evidence for why it is required (baseline green, measured); T012 repeats it ("post-relocation only... If this is run before T007 it stays GREEN — that is a false alarm, not a finding"); the Gate A round-2 sweep (RC-B) specifically swept this qualifier into five previously-omitting sibling loci, and audit-time inspection confirms it is present in all cited locations (contracts C-1 Verification-matrix row, `research.md` R-1 step 4/R-5/R-7, `data-model.md` Validation rules, `quickstart.md` §2a-mut and Failure triage).
- [x] CHK110 Is the **baseline control** recorded — that deleting `consume_group_extent:521-524` on the *unmodified* tree leaves the dictionary pin **GREEN**, because the flat loop still catches the breach? [T005] — PASS: T005 assigns exactly this measurement and its rationale, cross-referenced from `research.md` R-1 step 4 and `quickstart.md` §2a-mut's precondition box, both of which quote the actual measured GoogleTest output (`[ PASSED ] 1 test.`).
- [x] CHK111 Is that control written to the verify doc? *(It is obtainable only pre-relocation; unrecorded, it cannot be reconstructed, and T012's RED loses its contrast.)* [T005, `/speckit-analyze` F2] — PASS: T005 states verbatim "Record it in `.specify/decisions/085-fold-flat-cap-loop-verify.md`, alongside where T006/T012/T015 write theirs — this control is obtainable *only* pre-relocation... so if it is not written down now it cannot be reconstructed."
- [x] CHK112 Is the dict-free pin **bracketed on both sides** — over-cap frame fails, same frame with the cap raised succeeds? [FR-005a(ii), SC-004a, T014] — PASS: FR-005a(ii) and T014 both specify the bracketing companion test by name (`DictFreeDoSCapPerInstanceAllowsWhenCapRaised`) on the same frame.
- [x] CHK113 Do both dict-free cases use dict-free construction **and** a tightened `Config`? *(Under default `Config` the branch is arithmetically unreachable — both bounds 4096, table clamped at `:326`. A pin using defaults tests nothing.)* [SC-004, R-2, T013] — PASS: T013 states both requirements explicitly and cites R-2's reachability argument; independently re-verified the arithmetic against `include/fixpp/wire/offset_table.hpp:27-28` (both defaults 4096) and `src/wire/offset_table.cpp:326` (the clamp) — confirms a default-`Config` pin would indeed test nothing, as claimed.

## False-green resistance in the procedure itself

- [x] CHK114 Does **every** test selection in `quickstart.md` assert a count, inside a subshell, chained so a wrong count **halts before** the command it guards? [`/gate-a` r2, T023] — PASS, verified by direct read of the delivered `quickstart.md` rather than taken on the changelog's word: every code block in §0, §1a/b, §2a/2a-mut/2b/2c and §3a/3b is a `( ... )` subshell with `expect ... || exit 1` chains preceding the guarded command, exactly as the file's own "How the guards halt" convention states.
- [x] CHK115 Is every asserted count the **real** count? *(A wrong number fails a correct run — the mirror-image defect.)* [T023] — PASS, independently re-measured rather than trusted: `ctest --test-dir build/linux-clang-debug -N -L {wire,capi,dictionary}` against the live build directory returned **4**, **22**, **17** respectively — matching `quickstart.md`'s asserted counts exactly. The two counts legitimately asserting **0**-today (`DictFreeDoSCapPerInstance*`, `FR001_SingleTraversalSourceInspection`) are correctly annotated in the "Expected counts" table as "0 today — the pin(s) do not exist yet," which is the correct handling of this item's own trap, not an oversight.
- [x] CHK116 Does the benchmark step carry an **executable** count assertion (`--benchmark_list_tests=true`), not a prose expectation? *(A zero-match `--benchmark_filter` exits 0.)* [`/gate-a` r2, T020] — PASS: `quickstart.md` §3a's leg-1 block contains `expect "$(bm_count $BENCH "$FILTER")" 3 || exit 1` using `--benchmark_list_tests=true` per the `bm_count` helper definition in Prerequisites.
- [x] CHK117 Is the benchmark output path **removed before** the run and **asserted non-empty after** it, with the invocation chained `|| exit 1`? *(Google Benchmark `std::exit(1)`s on an unopenable `--benchmark_out`; the parse step then reads the previous run's file.)* [`/gate-a` r3, T020] — PASS: confirmed in `quickstart.md` §3a: `rm -f "$OUT"` precedes the run, `[ -s "$OUT" ] || { echo "FRESHNESS FAIL..."; exit 1; }` follows it, and the benchmark invocation itself is chained `|| exit 1`.
- [x] CHK118 Do the two A/B legs write to **distinct** paths (`TAG=main` / `TAG=branch`)? *(One shared path lets a failed branch run compare `main` against itself for a ≈0% delta and **pass the blocking gate on identical data**.)* [`/gate-a` r3, T004, T020] — PASS: confirmed `OUT="/tmp/085-bench-$TAG.json"` with an explicit warning block in §3a naming the exact failure mode this guards against, and T004/T020 use the distinct filenames (`/tmp/085-bench-main.json`, `/tmp/085-bench-branch.json`) consistently.
- [x] CHK119 Does the A/B comparator decide over **all three cases in one process before printing any row**, so an early missing case cannot be overwritten by a later success? *(The per-name shell loop was order-dependent: a missing last case failed, a missing first case did not.)* [`/gate-a` r4, T020] — PASS: confirmed the single Python invocation in §3a computes `missing = [...]` over all three names before printing any row and `sys.exit`s on the aggregate, with an explicit callout of the prior order-dependent shell-loop defect and its reproduction under both bash and zsh.
- [x] CHK120 Is the release **build** inside the guarded subshell and chained? *(§3a is the only step that builds release; a release-only compile failure is invisible everywhere else.)* [`/gate-a` r3, T020] — PASS: confirmed `cmake -S . -B build/linux-clang-release || exit 1` and the subsequent `--build ... || exit 1` sit inside §3a's leg-1 subshell, with an explicit comment stating why ("§3a is the ONLY step that builds release; nothing else would catch it").
- [x] CHK121 Were the failure paths **executed**, not asserted — each guard shown to halt? [T023] — PASS, weighing the recorded execution evidence rather than the forward task alone: the Gate A record documents each guard mechanism was actually run — round 2's fix was "executed... in `bash script.sh`, pasted `bash -i`, `zsh script.sh` and pasted `zsh -i`" with both true and deliberately-wrong counts, and the round-4 hand-edit closed the §3a benchmark false-green "across all three vectors... **executed** after the edit: stale-file-plus-failure → status 1..., missing-case A/B → comparator raises..., failing build → block halts." T023 is the corresponding standing `/speckit-implement` obligation to re-walk the same procedure end to end on the delivered tree, not the sole evidence for this item.

## Regression corpora

- [x] CHK122 SC-001 — `DelimiterCensus.RedCountsReconcileWithSpecBaseline` and all **seven** `TypedReadSplitAgreement.*` green, with **no fixture or baseline edit** made to achieve it? *(An edit to turn these green is a failure of SC-001, not a fix.)* [SC-001, T010] — PASS: independently re-counted rather than trusted — `grep -n '^TEST(TypedReadSplitAgreement' tests/wire/typed_read_split_agreement_test.cpp` returns exactly **seven** matches at `:194,296,337,383,452,633,820`, matching `research.md` R-5's citation exactly; `DelimiterCensus.RedCountsReconcileWithSpecBaseline` confirmed to exist at `tests/dictionary/delimiter_census_test.cpp:476`.
- [x] CHK123 SC-002 — `ctest -L wire` 4/4, `-L capi` 22/22, `-L dictionary` 17/17, identical pass set to `main`? [SC-002, T011] — PASS, independently measured against the live build directory (not merely cited): `ctest --test-dir build/linux-clang-debug -N -L wire/capi/dictionary` returns **4**, **22**, **17** respectively, matching the bundle's asserted counts exactly.
- [x] CHK124 Are the two pre-existing dictionary cap pins still green — `DoSCapPerInstanceRejectsOversizedSingleInstance` (`:199-235`) and `DoSCapPerInstanceAllowsAggregateOverCap` (`:164-197`)? [SC-003, T010] — PASS: confirmed both `TEST(WireOffsetTable, ...)` bodies exist in `tests/wire/offset_table_test.cpp` at the cited region, with the exact `tight_cfg{.max_offset_entries = 4096, .max_group_entries_per_instance = 3}` shape SC-004/R-2 describe.

## Performance (Article VIII)

- [x] CHK125 Is the leg-1 observation the **median of three per-case medians**, with the run count fixed in advance and **all three runs reported**? *(A "single run within budget" rule permits repeating until one lands.)* [SC-006, T020] — PASS: SC-006 states this exactly ("the run count is fixed **in advance at three**, and **all three are reported**") and corrects the earlier round-1 wording that would have permitted run-selection; T020 restates the same procedure.
- [x] CHK126 Does SC-006 rest first on the **baseline's own seed-row provenance** — `Flat2`'s "not a recorded-baseline regression budget", `pre083_median_ns: null` on the other two — rather than on locally measured host noise? [SC-006] — PASS, verified directly against `bench/baselines/wire/typed_read_group_bench.json` rather than trusted from the spec's quotation: the file's `Flat2` row carries `"budget_pct_note": "no [2b 6.6] ceiling for the typed-read splitter; +/-5% here is the A/B tolerance vs the pre-083 run, not a recorded-baseline regression budget"` verbatim, and `ModeC2`/`ModeC8` both carry `"pre083_median_ns": null`; `seed_median_ns` values `386`/`661`/`1643` and the `context` block (`repetitions: 9`, `min_time_s: 0.5`, `build_type: release`) all match the spec's citations exactly. Also confirmed `tools/bench_compare.py::load_benchmarks` reads `bm.get("cpu_time")` — a key this baseline file does not carry at all — corroborating the bundle's claim that the repo's own comparator would print `N/A` for all three rows if pointed at this file, which is why SC-006 does not use it.
- [x] CHK127 Are both legs' numbers **in the PR body**, and is `bench/baselines/` **not** updated? *(This is not an intentional perf change.)* [`[const §VIII.2/3]`, A-004, T020] — PASS: SC-006, A-004 and T020 all state "Put both sets of numbers in the PR body" and "do not update `bench/baselines/`" explicitly; this is a forward `/speckit-implement`/PR obligation, correctly stated as such.

## Coverage, sanitizers, hygiene

- [x] CHK128 Is the stale waiver at `tests/wire/offset_table_error_path_test.cpp:10-14` **repaired, not merely re-pointed**? *(It is wrong twice: pre-063 line numbers, **and** a "provably unreachable" claim that is false for `:577` and, after this feature, for the relocated branch.)* [R-3, T019] — PASS: independently read the cited waiver block — it does list stale line numbers (`125-127`, `157-158`, `183-184`) against a file whose corresponding real content today is at different lines, and its `183-184` entry does state "provably unreachable" for `group()`'s `err_group_too_large`, exactly the claim R-3 identifies as now false. `research.md` R-3 and T019 both require a content repair, not a re-point, matching the actual defect found.
- [x] CHK129 Are the Article IX §1 percentages **measured** at `/speckit-verify` with fresh per-binary profraw and recorded — not asserted from the "coverage improves" argument? [`[const §IX.1]`, T022] — PASS: `plan.md`'s IX §1 row states "the percentages are unmeasured pre-implementation" and names `/speckit-verify` plus the verify-doc as the artifact that measures and records them; T022 assigns it identically. This is a correctly-deferred forward obligation, not a premature claim of improvement.
- [x] CHK130 Does the sanitizer matrix (ASan/UBSan/TSan) and static analysis pass with no new findings? *(No new constructs are introduced, so any finding is a signal the relocation was not semantic-preserving.)* [SC-008, T022] — PASS: SC-008/T022 assign this to `/speckit-verify` Step 2 + Tier-1 CI, correctly, since `quickstart.md` §3b documents (and this audit confirms by directory listing) that no sanitizer build directory exists in this tree locally — writing an unrunnable local command here would itself be the false-green defect this bundle was rewritten to avoid.

## Close-out

- [x] CHK131 Does the L-063-4 update record leg 2 **DELIVERED**, leg 1's descope evidence **intact**, and both surviving flat rules named with their **differing delimiter sources**? [FR-006, FR-007, FR-007a, SC-009] — PASS: FR-006/FR-007/FR-007a and T016/T017 state all three requirements as explicit forward obligations on the same L-063-4 row, whose current (pre-085) text was directly read and confirmed to already carry the leg-1 descope evidence and leg-2 OUTSTANDING/tracked-by-#214 disposition this feature must flip.
- [x] CHK132 Is the splitter named for its real home — **`group_slices_status()`**, not the delegating wrapper `group_slices()` (`:639-641`)? [FR-007, `/speckit-analyze`] — PASS: FR-007 and T017 name `group_slices_status()` explicitly and call out `group_slices()` as "only the delegating wrapper"; confirmed against source — `group_slices()` at `:639-641` is a one-line `return group_slices_status(no_tag).slices;` delegation, and the actual splitter logic lives in `group_slices_status()` (`:712-733`), exactly as claimed.
- [x] CHK133 Are references anchored **function-and-role first**, line numbers stamped as-of the merge commit, historical brackets **byte-unchanged**? [FR-007b, SC-009] — PASS: FR-007b states this rule explicitly and extends it (at Gate A round 1) to the FR-002 source comment as well, precisely because a comment living inside the file whose own line numbers this change shifts would otherwise be born stale — the same failure mode that caused L-063-4 to accumulate three generations of numbering, confirmed by direct read of the current L-063-4 row's own historical-bracket structure.
- [x] CHK134 Does the fixpp#220 row cite the issue **and** state the default-config unreachability with its `max_group_entries_per_instance < max_offset_entries - 1` precondition? *(Overstating fails SC-010 exactly as omitting does.)* [FR-003a, SC-010, T018] — PASS: FR-003a/SC-010/T018 state this precise, symmetric failure condition (overstating fails exactly as omitting does); the precondition arithmetic was independently re-verified against `include/fixpp/wire/offset_table.hpp:27-28` and `src/wire/offset_table.cpp:326` (see CHK022 in `wire-correctness.md`).
- [x] CHK135 Are the two mandatory close-out tasks done — catalogue/coverage-index flip, and the completeness audit recorded in the verify doc? *(`/gate-b` pre-flight 4d **hard-blocks** without the audit record.)* [`[const §XVII.8]`, T024, T025] — PASS: T024/T025 are tasks.md's "Mandatory close-out tasks (ALWAYS emit)" section, explicitly labelled as such per Article XVII §8, with T025 naming the exact hard-block precondition and the `## Completeness` record location.
- [x] CHK136 Does the close-out compute the **post-merge** open-issue count rather than copying a mid-feature figure? [T024, `/speckit-analyze` F3] — PASS: T024 explicitly instructs computing the count at close-out rather than copying it, citing the exact enumeration (`196, 209, 211, 213, 214, 215, 217, 218, 220` → 9, closing #214 leaves 8) and naming the reason ("the count in `CLAUDE.md` has already gone stale once"). Observation for the record, not a defect in this item: the submodule `CLAUDE.md` at repo root currently states "Open issues: 6" — a figure T024 itself is designed to supersede rather than inherit, and this audit did not independently verify #217/#218's existence; that reconciliation is exactly what T024 assigns to close-out.

## Notes

- Dispositioned at `/speckit-checklist-audit` as PASS / SPEC-FIXED / DD-DECIDED §X / WAIVED:`<reason>`. Completeness/Clarity/Consistency gaps may **not** be WAIVED.
- CHK114–CHK121 exist because this exact section false-greened four times. Treat a green run of the procedure as evidence only after the guards themselves have been shown to fire. All eight dispositioned PASS above on **verified** (not merely cited) evidence — direct read of the delivered `quickstart.md` plus the Gate A record's documented execution history.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 36 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 36 |

### SPEC-FIXED items

None.

### DD-DECIDED items

None.

### WAIVED items

None.

Anchors spot-verified: `tests/wire/CMakeLists.txt` (confirmed `FIXPP_SRC_DIR` absent from `wire_pure_tests` today), `tests/dictionary/CMakeLists.txt:178-183` (confirmed the precedent line), `tests/dictionary/load_any_test.cpp:134-171`, `.clang-format`, `tests/wire/typed_read_split_agreement_test.cpp` (7 `TypedReadSplitAgreement.*` cases confirmed by direct count), `tests/dictionary/delimiter_census_test.cpp:476`, `tests/wire/offset_table_test.cpp:150-235` (dict-free `group(453)` + both cap pins), `bench/baselines/wire/typed_read_group_bench.json` (full contents read), `tools/bench_compare.py::load_benchmarks`, `bench/wire/CMakeLists.txt:148-150`, `tests/wire/offset_table_error_path_test.cpp:1-20`, `include/fixpp/wire/offset_table.hpp:27-28`, `src/wire/offset_table.cpp:326`, `spec/behaviors-and-limitations.md:1701-1709` (L-063-4), `.specify/constitution.md:86,173,178,185,186,198,206,220,402`, `.specify/decisions/085-fold-flat-cap-loop-gatea.md`, and live `ctest -N -L {wire,capi,dictionary}` counts against `build/linux-clang-debug` (4/22/17) — all resolve exactly in signed-off revision `main` = `c1564dd2a1c021b0f68c30cb1983a0ad6d2626f5`. No dangling reference found.
