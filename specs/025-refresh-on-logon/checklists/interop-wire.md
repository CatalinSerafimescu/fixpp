# Interop / Wire-Invariance Requirements Checklist: RefreshOnLogon

**Purpose**: Validate that the requirements governing **wire and ABI invariance** (no new wire field / error slot / codegen / C-ABI), **reference-engine parity** (QuickFIX-cpp / QuickFIX/J / Fix8), the cross-engine **`141=Y` ⟹ `MsgSeqNum=1`** floor, and the **acceptor received-141 reset precedence** over a forced re-hydrate are complete, clear, consistent, and measurable. This is the surface Gate B and the live interop matrix exercise.
**Created**: 2026-06-09
**Feature**: [spec.md](../spec.md) · [contracts/refresh-knob.md](../contracts/refresh-knob.md) · [data-model.md](../data-model.md)

## Wire & ABI invariance

- [x] CHK001 Is it stated that the knob governs **only seqnum-counter hydration at logon** — it MUST NOT introduce a new wire field, MUST NOT change outbound message construction beyond sampling the (possibly refreshed) counters, and MUST NOT alter inbound validation rules? [Completeness, FR-007] — PASS: FR-007 states all three MUST-NOT constraints verbatim; Edge Cases "Outbound construction otherwise unaffected" restates the scope; FR-011 confirms no new wire field, no error slot, no codegen, no C-ABI change.
- [x] CHK002 Is the no-ABI-break boundary specified precisely — one additive `SessionConfig` value-type bool (a struct-layout change requiring a normal source rebuild), no new error slot, no codegen-emitter change, no C-ABI surface change? [Clarity, FR-011 / Assumptions] — PASS: FR-011 states all four exclusions ("no new error slot, no new wire field, no codegen-emitter change, no C-ABI surface change") and the one addition ("one additive `SessionConfig` field (C++-only value type; struct-layout change requiring a normal source rebuild)"); Assumptions repeats "No new wire field, no new error slot, no codegen change, no C-ABI change"; plan Constitution Check article X confirms "source rebuild only, additive."
- [x] CHK003 Is the negative/MUST-NOT nature of FR-007/FR-011 backed by a verification path (the byte-identity regression W3 + the verify gate), so "no new wire field" is checkable and not merely asserted? [Measurability, FR-007 / FR-011 / SC-003 / W3] — PASS: SC-003 and W3 mandate the full existing regression suite remains green (a byte-identity check that catches any new wire output); plan Constitution Check article XV.9 says "confirm at verify"; tasks T044 mandates the 6-preset sanitizer matrix including the byte-identical regression. The verification path is specified, not merely asserted.

## Reference-engine parity (QFcpp / QFJ / Fix8)

- [x] CHK004 Is the store-wins / unconditional / default-off contract grounded in named reference-engine authorities (QFcpp `RefreshOnLogon` `Session::refresh()` at setResponder/nextLogon/generateLogon; QFJ `refreshOnLogon`; Fix8 `recover_seqnums()`), so FR-001/002/003 are traceable to parity rather than invented? [Traceability, Normative References / Assumptions] — PASS: Normative References names all three engines with exact function/method names and default-off parity; research reference-engine sweep table documents reload semantics for all three; Assumptions section "QuickFIX-faithful store-wins" cites QFcpp and QFJ with the exact call sites; FR-001/002/003 cite QFcpp/QFJ in-line.
- [x] CHK005 Is the divergence that *necessitates* the suppression made explicit — that `bilateral_strict`'s unconditional `141=Y` is a fixpp-024 construct with **no analogue** in any of the three engines (all of which `{1,1}`-guard or reset-couple the `141=Y`) — rather than left as an unexplained fixpp-specific restriction? [Clarity, Clarifications / FR-008 / Normative References] — PASS: Clarifications opens with "fixpp's `bilateral_strict` policy — which emits `141=Y` UNCONDITIONALLY regardless of counter value (a 024 invention with no analogue in any of the three engines) — is the sole reason a store-wins refresh could produce a malformed Logon"; FR-008 repeats "a fixpp-024 construct with no analogue in QFcpp/QFJ/Fix8, all of which `{1,1}`-guard or reset-couple the `141=Y`"; research D-RoL-3 documents the three-engine cross-check.
- [x] CHK006 Is the documented use case (hot-standby / backup topology) carried as the requirement's rationale and the operator guidance (enable on standby only), consistent with the QuickFIX NEWS framing? [Consistency, US1 / Edge Cases / Assumptions / FR-012] — PASS: US1 "Why this priority" says "QuickFIX 'RefreshOnLogon … useful for creating backup systems'"; Edge Cases "Active-session reconnect regression is an operator responsibility" states "enable `refresh_on_logon` only on backup/standby topologies"; Assumptions "QuickFIX-faithful store-wins" cites QFcpp NEWS framing; FR-012 mandates the B&L entry documenting the standby-only guidance (L-025-1).

## Cross-engine `141=Y` ⟹ `MsgSeqNum=1` floor

- [x] CHK007 Is the only normative protocol constraint on this interaction stated correctly — a Logon carrying `141=Y` MUST have `MsgSeqNum=1`, and RefreshOnLogon itself is an engine config the FIX/FIXT spec does **not** define — so the requirement floor is the `141`-rule, not an invented protocol feature? [Clarity, Normative References / Clarifications] — PASS: Clarifications states "The official FIX session spec / FIXT does NOT define RefreshOnLogon (it is an engine config, not a protocol feature); the only normative constraint is the `141=Y` ⟹ `MsgSeqNum=1` rule"; Normative References section names "FIX session-layer `ResetSeqNumFlag(141)` rule" as the floor; research confirms the same cross-engine invariant.
- [x] CHK008 Is the requirement that the 025 re-hydrate **MUST NEVER** produce a `141=Y`+non-1 Logon stated as an absolute (enforced by the strict suppression), distinct from the deferred inherited cold-open gap that the policy can still hit independent of the knob? [Consistency, FR-008 / INV-RoL-3 / L-029-3] — PASS: FR-008 says "the 025 re-hydrate MUST NEVER produce a `141=Y`+non-1 Logon" and frames it as absolute; INV-RoL-3 says the invariant "covers the **re-hydrate**, not the cold-open seed: the strict + non-1 cold-open path is the inherited L-029-3 gap, NOT a 025 guarantee"; SC-005 clause (b) explicitly limits to "no NEW malformed Logon attributable to the knob." The absolute / deferred distinction is maintained throughout all artifacts.

## Acceptor received-141 reset precedence (RC-1)

- [x] CHK009 Is it stated that a forced re-hydrate on an acceptor receiving a peer **reset** Logon (`34=1, 141=Y`) MUST still honour the reset — the inbound seed produced by the refresh MUST be withheld/overridden on the peer-reset arm so the peer's `34=1` is accepted (not rejected as too-low)? [Completeness, FR-009 / INV-RoL-5 / US3 AS2] — PASS: FR-009 says "a received peer reset Logon (`34=1, 141=Y`) MUST still be honoured — the inbound seed produced by the refresh MUST be withheld/overridden on the peer-reset arm so the existing 013/024 received-141 reset establishes `{1,1}` and the peer's `34=1` is accepted (not rejected as too-low)"; US3 AS2 specifies the same outcome; INV-RoL-5 is a dedicated invariant for this.
- [x] CHK010 Is the withhold condition specified precisely (`apply_inbound_seed = !(peer_sent_reset || reset_on_logon)`) and identified as the **same** reset-Logon inbound-withhold the 029 cold path already performs (RC-1), so the requirement reuses an established, tested arm rather than inventing a new one? [Clarity, INV-RoL-5 / C2.4 / C5.3 / FR-009] — PASS: C2.4 states "`force` does **not** alter the `apply_inbound_seed` withhold (`:601`): a forced re-hydrate on a reset Logon still withholds the inbound seed (INV-RoL-5)"; C3.2 gives the exact expression "`withhold_inbound` stays `peer_sent_reset || cfg_.reset_on_logon` (029)"; FR-009 says "the same reset-Logon inbound-withhold the 029 cold path already performs — RC-1"; confirmed in live session.cpp `:1738` unchanged `withhold_inbound = peer_sent_reset || cfg_.reset_on_logon`.
- [x] CHK011 Is the non-regression constraint stated as a hard MUST-NOT — the refresh MUST NOT precede, mask, reorder, or suppress the existing 013/024 received-141 reset arm — so a forced re-hydrate cannot leave a non-1 store value that would pre-empt the peer reset? [Consistency, FR-009 / C5.3 / Edge Cases] — PASS: FR-009 says "The refresh MUST NOT precede / mask the received-141 reset arm"; Edge Cases "Refresh vs received-141 reset" says "the inbound seed from the refresh MUST be withheld/overridden on a peer reset Logon so the peer's `34=1` is accepted"; C5.3 confirms "the `:1925` reset still owns the post-state"; the ordering is enforced by the call sequence in live session.cpp (hydrate at `:1739` withholding inbound seed, received-141 reset at `:1925`).
- [x] CHK012 Is the acceptor reset post-state given a definite expected value for the witness (the received-141 reset establishes `{1,1}`, session reaches Active, peer `34=1` accepted), rather than left undefined? [Measurability, SC-006 / W6] — PASS: SC-006 says "the session reaches Active and the peer's `34=1` is accepted (not rejected as too-low)"; data-model W6 says "inbound seed withheld; peer `34=1` accepted; reaches Active"; tasks T032 spells out the exact postcondition assertions (`:1925` received-141 reset establishes `{1,1}`, session reaches Active, peer `34=1` in-sequence).

## Doc-surface completeness (FR-012)

- [x] CHK013 Is the doc-surface delta enumerated and bounded (feature-catalogue S-018 flip, coverage-index rows, B&L L-024-1 retire / L-025-1 add) and its verification routed to the mandatory `/speckit-checklist-audit` gate (the 027/028 precedent) rather than a synthetic byte-witness over the catalogue files? [Completeness, FR-012 / data-model W-matrix note] — PASS: plan §VI delta enumerates all four doc-surface changes exhaustively (feature-catalogue S-018 flip, coverage-index §4.3.12+§4.4 rows, B&L L-024-1 retire + L-025-1 add + L-029-3 add); data-model W-matrix note states "FR-012 has no synthetic runtime witness by design: it is a doc-surface §VI delta discharged at Polish and verified by the mandatory `/speckit-checklist-audit` gate (the 027/028 precedent)"; tasks T042/T043 are the dedicated Polish tasks.
- [x] CHK014 Is the active-session-regression **operator responsibility** (L-025-1: a reconnect refresh can pull a counter below the live value past the INV-H1 lag window; enable on standby topologies only) documented as a behaviors-and-limitations entry, distinct from an INV-H1 store-side violation? [Clarity, FR-012 / Edge Cases / INV-RoL-6 carried-invariants note] — PASS: Edge Cases explicitly distinguishes: "Active-session reconnect regression is an operator responsibility (known limitation): on an active session...the operator opts into this by enabling the knob; the documented guidance is to enable `refresh_on_logon` only on backup/standby topologies"; data-model Invariants "Carried 029 invariants" note says "a store-wins refresh on an ACTIVE session can transiently violate the *manager's monotonicity*...is the operator-accepted regression L-025-1, NOT an INV-H1 store-side violation"; FR-012 mandates the B&L entry; tasks T043 specifies the L-025-1 text.

## Notes

- This checklist tests the **wire/ABI invariance, parity, and reset-precedence** requirements; the re-hydrate mechanics live in `refresh-semantics.md` and the no-op/compat paths in `compat-noop.md`.
- The pivotal items are **CHK008** (the absolute "never emit `141=Y`+non-1 via the knob", distinct from the deferred policy gap) and **CHK009/CHK010/CHK011** (the RC-1 received-141 precedence the forced re-hydrate must not break). These are the correctness interactions with the established 024/013 logon FSM.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 14 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **14** |

### SPEC-FIXED items
None.

### DD-DECIDED items
None.

### WAIVED items
None.

Anchors spot-verified: FR-007, FR-008, FR-009, FR-011, FR-012, INV-RoL-3, INV-RoL-5, C2.4, C3.2, C5.3, SC-006, W6, US1, US3 AS2, Edge Cases (received-141 reset, active-session-regression), data-model Invariants (carried 029 invariants note), Normative References (QFcpp/QFJ/Fix8/FIX-SL §4.3.12/§4.4), research D-RoL-3 — all resolve in Gate-A-converged spec/plan/data-model/contracts/research (submodule head `fb33a65`, session.cpp unchanged from 029 merge `0b9c8b8`). Live source anchors `:1738`/`:1739` (acceptor ensure_hydrated_ call with peer_sent_reset withhold), `:1925` (received-141 reset arm) confirmed.
