# Checklist: Guard Seam, Typed Depth-3 Push & C-ABI Freeze (Requirements Quality)

**Purpose**: Validate that the requirements around the load-time delimiter guard, the strictly-stronger census, the typed depth-3 pushed context (reconciled UP to the C-ABI), the FR-010 validator rewrite + split-trigger, and the GA-frozen C-ABI are complete, unambiguous, measurable, and consistent — BEFORE implementation. (Unit tests for the requirements, not the code.)
**Created**: 2026-07-12
**Feature**: [spec.md](../spec.md) · **Focus**: load-guard seam, census strength, depth-3 push direction, FR-010 severability, C-ABI zero-delta

## Guard seam correctness — Completeness & Clarity

- [x] CHK001 - Is the guard's enforcement seam stated unambiguously as `XmlLoader::load_*` (`LoaderState::finalize()`), with `as_table_view()` explicitly required to stay allocation-only / non-throwing (the parent-chain walk moved INTO the loader)? [Clarity, Spec §FR-003/FR-005, Clarification 2026-07-12, Contract load-guard §Behavior] — PASS: FR-003 names the seam explicitly ("Detecting the collision requires a parent-chain walk in the loader... `as_table_view()` stays allocation-only and non-throwing"), matching the Session-2026-07-12 clarification answer and contract load-guard.md §Behavior; `LoaderState::finalize()` and `GroupDef{parent_group_no_tag,first_field_tag}` verified on-disk (`src/dictionary/xml_loader.cpp:207-213,633`).
- [x] CHK002 - Is the reject condition specified precisely — a nested group whose `first_field_tag` equals its immediate parent's `first_field_tag`, detected via a parent-chain walk over `groups_` (`parent_group_no_tag != 0` → compare via `group_index_by_no_tag_`) — with an objective outcome (throw + NO `table_view` produced)? [Measurability, Spec §FR-003/SC-002, Contract load-guard §Behavior] — PASS: FR-003 states the exact predicate and objective outcome (throw before any `table_view` built, fail whole load); contract load-guard.md §Behavior gives the literal algorithm (`g.first_field_tag == parent.first_field_tag`). `GroupDef` fields verified to exist exactly as named (`xml_loader.cpp:207-213`).
- [x] CHK003 - Is the new error type's contract fully specified — `dict::group_delimiter_collision_error` derives from `dict::xml_parse_error`, reuses the inherited `code()` (`dict_xml_parse_failed`), appends NO `fixpp::core::error` variant — AND is the discrimination mechanism (by catch type, because `code()` is non-virtual) stated, not left implicit? [Completeness, Spec §FR-003, Research §D-A1, Data-model §New] — PASS: FR-003 + data-model.md §New state derivation, inherited `code()`, no-append, and catch-type discrimination explicitly. Realizability checked: the new type derives from a fully-defined base (`xml_parse_error : public std::runtime_error`, itself fully defined, non-virtual `code()` verified at `include/fixpp/dict/error.hpp:33,39`); it is never held by value as a member of another struct, so the D-15/T010a forward-declared-owned-dependency hazard does not apply — clean.
- [x] CHK004 - Is the "no enum append" constraint traced to its blast-radius rationale (the no-`default` `error_message()` `-Wswitch`/`-Werror` build + `test_020` slot-132 pin), so an implementer cannot "helpfully" append a variant? [Clarity, Research §D-A1, Plan §Constitution-Check X] — PASS: FR-003 states the rationale inline ("would trip the no-`default` `error_message()` `-Wswitch`/`-Werror` build and the `test_020_error_completeness.cpp` slot-132 pin"); research D-A1 elaborates. Verified on-disk: `core/error.hpp:760` highest slot `app_payload_malformed=131`; `test_020_error_completeness.cpp:28-44` pins the exact-set `{131}` + slot-132-unknown boundary — an appended slot-132 variant would flip this test RED, confirming the traced blast radius is real.
- [x] CHK005 - Is the guard required to fail the WHOLE load (not silently skip the offending group), before any `table_view` is built, and to not crash / invoke UB / silently mis-split? [Coverage, Spec §FR-003/SC-002] — PASS: FR-003 states this explicitly ("Rejection MUST happen at load, before any `table_view` is built, and MUST fail the whole load (not silently skip the offending group)... MUST NOT crash, invoke UB, or silently mis-split"); SC-002 restates as a measurable outcome (0 silent mis-splits, no crash/UB).

## Guard scope asymmetry — Consistency & Boundary

- [x] CHK006 - Is the delimiter-only scope of the load guard specified as a DELIBERATE asymmetry — scalar-member disjointness (L-062-3) stays census-only (FR-002), NOT load-enforced — with the completeness gate warned not to read "guard the conventions" as "enforce both at load"? [Consistency, Spec §FR-004/Edge-Cases] — PASS: FR-004 states the asymmetry as MUST/MUST NOT with the exact warning ("The asymmetry is intentional; the completeness gate must not read 'guard the conventions' as 'enforce both at load'" — Edge Cases, restated in FR-004).
- [x] CHK007 - Are the two guard residuals (FR-005a: a hand-built / non-`load_*` `table_view`/`Dictionary` is unguarded; FR-005b: `groups_`'s global-first-seen dedup misses a collision only in a non-first-seen context of a reused `no_tag`) recorded as explicit caller-responsibility residuals, not implied covered? [Completeness, Spec §FR-005, Contract load-guard §Explicit non-coverage] — PASS: FR-005(a)/(b) name both residuals explicitly as "remaining caller responsibility (catalogue note), not silently implied as covered"; contract load-guard.md §Explicit non-coverage restates both. `expand_field_list`'s `if (!group_index_by_no_tag_.contains(no_tag))` first-seen-dedup guard verified on-disk (`xml_loader.cpp` ~:486).

## Census strictly-stronger-than-guard — Clarity & Measurability

- [x] CHK008 - Is the FR-001 census specified as a DIFFERENT walk from the guard's `groups_` seam (raw per-`<group>` walk, parent-delimiter-threaded, `<component>`-expanded, delimiter resolved POST-component-expansion), with the rationale that sourcing from the first-seen-deduped `groups_` would reintroduce a first-seen blind spot and could not pin ALL contexts? [Clarity, Spec §FR-001, Research §D-A3] — PASS: FR-001 states the three-way extension (parent-delimiter threading, `<component>` expansion, post-expansion delimiter resolution) and the explicit non-reuse rationale verbatim ("This raw all-contexts walk is deliberately NOT the FR-003 guard's seam... The census must be strictly stronger than the guard"). `walk_groups`/`delimiter_scan_for`/`saw_fix44_295_collision` scaffold verified present in `tests/dictionary/reused_tag_census_test.cpp:78-119,281`.
- [x] CHK009 - Is non-vacuous coverage a stated, measurable requirement (assert > 0 group-declaration sites per dict claimed covered — including FIX40/41/42, whose `INT` counts register zero `NumInGroup` groups), so the census cannot pass false-green? [Measurability, Spec §FR-001/FR-002/SC-001, Research §D-A4] — PASS: FR-001/FR-002/SC-001 all state the `> 0` non-vacuous assertion as a hard requirement. D-A4's claim verified directly on-disk: `src/dictionary/dictionary.cpp:335,368` gate on `fr.type == field_data_type::NumInGroup`, and `dictionaries/FIX40.xml:701,823,848` declare `NoOrders`/`NoExecs`/`NoMiscFees` count fields as `type='INT'` (not `NUMINGROUP`) — confirming the zero-registration risk FR-001 guards against is real, not hypothetical.

## Typed depth-3 push — reconcile UP, exactly once

- [x] CHK010 - Is the push site specified as EXACTLY ONE (emitter view-mint `emit_messages.cpp:270-271`), with the three invariants that MUST stay unchanged stated explicitly (the `nested_group_slices` call-arg stays `ctx_.group_ctx`; `group_view::operator[]` stays a verbatim `base_ctx_` copy — the double-push hazard; the `parser.hpp:309` depth-1 seed)? [Clarity, Spec §FR-007, Contract typed-context §Change] — PASS: FR-007 states "The push happens exactly once per descent, at mint" plus all three invariants verbatim. Anchors verified exactly on-disk: `emit_messages.cpp:270-271` (`w.raw(group_cls(c)); w.raw(">{nested, ctx_}; }")`), `group_view.hpp:143-150` (`operator[]` copies `base_ctx_` verbatim, no push), `parser.hpp:309` (`ctx.group_ctx = root_ctx.pushed(NoTag);`).
- [x] CHK011 - Is the reconciliation DIRECTION unambiguous — push the typed path UP to match the already-correct C-ABI cursor (`message_read.cpp:506`), and NEVER un-push / alter / regress the C-ABI — with post-fix typed≡C-ABI depth-3 agreement required? [Consistency, Spec §FR-008/SC-004, Research §D-B3] — PASS: FR-008 states the direction and prohibition verbatim ("push the typed path *up*... MUST NOT un-push, alter, or regress the C-ABI cursor. Post-fix, the typed and C-ABI paths MUST agree at depth-3"). `message_read.cpp:506` verified exactly (`nested_grp->group_ctx = parent_grp->group_ctx.pushed(nested_tag);`) and `:490` confirms the call-arg passed to `nested_group_slices` is the parent's unpushed `group_ctx` — the same pattern FR-007 requires of the typed path.
- [x] CHK012 - Is the lookup-key vs stored-child-context distinction specified sharply enough that a witness cannot conflate them (555's own members resolve under the KEY `(msg_type="i", parent_path=[296,295], 555)`; the triple `[296,295,555]` is the STORED context minted for a FURTHER descent, NOT 555's own members' key)? [Clarity, Spec §FR-011/SC-003, Data-model §group_context] — PASS: FR-011/SC-003 state the distinction explicitly with a parenthetical guard against the exact conflation ("`[296,295,555]` is the stored child context, not the lookup key for 555's own members"); data-model.md §`group_context` restates it and explicitly warns "A witness that registers/queries `555` under `parent_path=[296,295,555]` gets a permanent miss." `table_view::add_group_member_ctx` signature verified at `include/fixpp/dict/table_view.hpp:359` (`msg_type, parent_path, no_tag, member_tag` as four separate params, matching the KEY/stored-context split).

## FR-010 validator rewrite + split-trigger — Completeness

- [x] CHK013 - Is FR-010 specified as a FROM-SCRATCH recursive query-before-push rewrite (not a push tweak) with a concrete algorithm — thread a path stack; FIRST query `group_first_field`/`group_member_tags(msg_type, current_parent_path excluding G's own no_tag, G)`; THEN push `G` only to recurse into its children; per-level counts from the slicer-produced extent, NOT the flat `seen_in_instance` heuristic? [Completeness, Spec §FR-010a, Research §D-B4, Contract typed-context §Validator] — PASS: FR-010(a) states the full query-before-push algorithm verbatim, matching contract typed-context.md §Validator. Anchors verified exactly: `validator.hpp:204` (`std::span<std::uint16_t const> const root_path{};`), `:185` in-code comment ("has no notion of nesting depth"), `seen_in_instance` heuristic at `:258-267`; `consume_group_extent` (`offset_table.cpp:457,466`) confirmed to query-under-`ctx` before forming `child = ctx.pushed(group_no_tag)` — the exact mirror pattern FR-010(a) requires. Realizability N/A (algorithm spec, no new value-typed entity).
- [x] CHK014 - Is the SPLIT-TRIGGER specified as an `/implement`-time escape with a recorded default (rides 072, user-signed-off Gate A round 3) AND the consequence if it fires (072 ships Part A + FR-007/008/011; L-063-3 stays "open/tracked"; FR-013's L-063-3 "fixed" promotion withheld), so the bundle is cleanly severable? [Clarity, Spec §FR-010c, Plan §Gate-A] — PASS: FR-010(c) states the default, escape condition, and consequence verbatim, cross-referencing `plan.md` `## Gate A`. Verified `plan.md` lines 101-116 `## Gate A` section contains the round-3 recorded decision ("SPLIT-TRIGGER decision (round 3, user-signed-off 2026-07-12): FR-010 RIDES 072...") — the cited anchor resolves; tasks.md Phase 5 header restates the same severability contract, so the plan/spec/tasks triad agree.

## C-ABI freeze — Measurability

- [x] CHK015 - Is the zero-C-ABI-delta requirement stated with an objective witness (ABI hygiene gate reports no exported-symbol / public-header / error-enum / version-constant delta vs `1.5.0`) rather than prose alone? [Measurability, Spec §FR-012/SC-006] — PASS: FR-012/SC-006 state the objective, mechanically-checkable witness ("ABI hygiene gate reports no exported-symbol / header / enum / version delta"). The "ABI hygiene gate" is an established recurring CI-gate concept referenced by multiple prior merged features (e.g. specs/014, 046, 065, 012, 013), so this is a real, known verification mechanism, not an unverifiable novel claim.
- [x] CHK016 - Is it explicit that this feature touches NEITHER `include/fixpp/core/error.hpp` NOR `tests/core/test_020_error_completeness.cpp`, so the "no enum append" claim is verifiable by absence-of-diff? [Consistency, Research §D-A1/Cross-cutting, Plan §Source-Code-touched] — PASS: `plan.md` §Source-Code-touched lists both files explicitly under "# NOT touched: include/fixpp/core/error.hpp, tests/core/test_020_error_completeness.cpp (no enum append — NEW-1)"; FR-003 restates it; research D-A1/Cross-cutting reiterate. Consistent across spec/plan/research — no drift.

## Notes

- Requirements-quality checklist (probes the SPEC's obligations, not the implementation). Dispositioned by the pipeline step-9 checklist audit before `/speckit-implement`.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 16 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 16 |

### SPEC-FIXED items
None.

### DD-DECIDED items
None. Every research.md (D-A1..A5, D-B1..B6) / Gate-A finding folded into spec.md itself (research.md's own "Corrected in spec §…" notes confirm this), so items are dispositioned PASS against spec.md as written, not deferred to the design doc.

### WAIVED items
None.

Anchors spot-verified (all resolve against on-disk source at branch `072-nested-group-hardening`): `include/fixpp/dict/error.hpp:33,39,47,64`; `src/dictionary/xml_loader.cpp:207-213,486,633`; `include/fixpp/core/error.hpp:760`; `tests/core/test_020_error_completeness.cpp:28-44`; `tools/codegen/fixpp-codegen/emit_messages.cpp:270-271`; `include/fixpp/wire/group_view.hpp:66-67,100-101,143-150`; `include/fixpp/wire/parser.hpp:309`; `src/capi/message_read.cpp:490,506`; `include/fixpp/wire/validator.hpp:185,204,258-267`; `src/wire/offset_table.cpp:457,466,505-571,611,646-668`; `include/fixpp/dict/table_view.hpp:316,359`; `src/dictionary/dictionary.cpp:335,368`; `dictionaries/FIX40.xml:701,823,848`; `dictionaries/FIX44.xml:943`; `tests/dictionary/reused_tag_census_test.cpp:78-119,281`; `tests/codegen/nested_group_read_test.cpp:632`; `plan.md` `## Gate A` (round-3 SPLIT-TRIGGER decision). No design-doc anchor of the `.specify/2x-*.md` form exists for this feature — the authoritative anchors are research.md's D-A/D-B decisions and plan.md's `## Gate A`, both spot-verified above.
