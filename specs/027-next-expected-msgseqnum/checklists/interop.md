# Interop-&-Default-Off-Parity Checklist: NextExpectedMsgSeqNum(789) fast session resume

**Purpose**: Requirements-quality gate ("unit tests for English") for the default-off byte-identity guarantee, both-roles symmetry, reference-engine parity claims, and FIX-version applicability scoping. Tests whether the *requirements* are complete, clear, consistent, and measurable — NOT whether the implementation works.
**Created**: 2026-06-07
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contracts/next-expected-msgseqnum.md](../contracts/next-expected-msgseqnum.md)

## Requirement Completeness

- [x] CHK001 Is the default-off requirement complete on BOTH halves — no 789 emitted outbound AND inbound 789 ignored with existing recovery used? [Completeness, Spec §FR-006, data-model I-NEX-7] — PASS: FR-006 states "When the knob is **disabled** (default), the outbound Logon MUST be byte-for-byte identical to current behaviour (no `789` field), and an inbound `789` MUST be ignored with recovery proceeding via the existing `ResendRequest` mechanism"; data-model I-NEX-7 repeats both halves ("outbound Logon contains no 789 field and is byte-identical to the pre-feature baseline; an inbound 789 is ignored and standard recovery applies"); US2 Acceptance Scenarios 1 and 2 address each half separately. Both halves are present.
- [x] CHK002 Is the both-roles symmetry requirement explicit (initiator and acceptor each emit AND honor 789)? [Completeness, Spec §FR-007, contracts C8] — PASS: FR-007 states "The behaviour MUST be symmetric across **both roles** — an initiator and an acceptor each emit and honour `789` identically when the knob is enabled"; contracts C8 states "C2–C7 apply to both inbound-Logon handlers (acceptor + initiator), at each handler's correct sites"; SC-005 requires "Both roles (initiator and acceptor) emit and honour `789`, verified by both-role unit tests and a live interop cell." Explicit symmetry requirement covering both emit and honor.
- [x] CHK003 Is the no-surface-change requirement complete (no new error slot, no C-ABI change, no codegen, one additive config field)? [Completeness, Spec §FR-010, plan §X] — PASS: FR-010 states "The feature MUST NOT introduce a new error slot or a C-ABI surface change; it adds one additive `SessionConfig` field and uses the standard `789` tag in the Logon message"; plan Constitution Check §X states "no C-ABI/error-slot change. `build_logon` (internal `admin_messages.hpp` builder, NOT C-ABI) signature gains the optional 789 — source-level change to its 2 callers. +1 `SessionConfig` bool"; spec Assumptions states "No new wire field beyond the standard 789 tag, no new error slot, no codegen-emitter change, no C-ABI change". All four no-change axes are named.
- [x] CHK004 Is a live-interop acceptance requirement present (both roles, against a counterparty configured with the equivalent knob)? [Completeness, Spec §SC-005, contracts C10] — PASS: SC-005 states "Both roles (initiator and acceptor) emit and honour `789`, verified by both-role unit tests and a live interop cell against a counterparty configured with `NextExpectedMsgSeqNum` enabled"; contracts C10 names the cell: "`tests/interop/happy/hp_fix44_next_expected_test.cpp` (skip-without-counterparty), both roles: fixpp + a `EnableNextExpectedMsgSeqNum` peer (QFcpp/QFJ) recover an at-logon gap with **zero `ResendRequest` on the wire**"; tasks T022 is a Polish task covering it. Requirement is present with a named test file and acceptance criterion.
- [x] CHK005 Is the FIX-version applicability boundary documented (implement for 4.4 now; FIXT.1.1/5.0SP2 gating deferred), so S-031 is scoped as partial rather than blanket-done? [Completeness, Spec §Edge Cases "Version applicability", plan §VI] — PASS: spec Edge Cases "Version applicability" states "Catalogue row S-031 (versioned '5.0–5.0SP2, FIXT.1.1') is therefore marked **implementation-parity for FIX 4.4**, NOT blanket `done` — the listed official versions stay outstanding until G4"; plan §VI explicitly states "mark S-031 … `backlog → implementation-parity-4.4` (NOT `done` — the row is versioned '5.0–5.0SP2, FIXT.1.1' and this slice delivers only FIX 4.4; the FIXT/5.0SP2 versions stay outstanding to G4)"; spec Assumptions confirms "implemented now on fixpp's current FIX 4.4 sessions … FIXT.1.1/5.0SP2 per-version gating deferred to G4." Boundary is documented with the specific version designation and the deferral target.

## Requirement Clarity

- [x] CHK006 Is "byte-identical to the pre-feature baseline" stated as an objective, capturable property rather than a vague "no change"? [Clarity, Spec §FR-006/§SC-002] — PASS: FR-006 states "byte-for-byte identical to current behaviour (no `789` field)"; SC-002 states "100% of existing session/recovery/logon regression witnesses remain green and the outbound Logon is byte-identical to the pre-feature baseline"; US2 independent test states "capture an outbound Logon and assert it is byte-identical to the pre-feature baseline"; RED witness `DefaultOff_ByteIdenticalLogon_InboundIgnored` operationalises it. "Byte-for-byte identical" is capturable (compare raw bytes).
- [x] CHK007 Are the reference-engine parity claims (QFcpp `m_sendNextExpectedMsgSeqNum` / QFJ `EnableNextExpectedMsgSeqNum`) named precisely enough to be traceable? [Clarity, Traceability, Spec §Clarifications, research D-2/D-6] — PASS: spec Clarifications Q2 names "QFcpp `m_sendNextExpectedMsgSeqNum`" and "QFJ `EnableNextExpectedMsgSeqNum`"; research D-2 cites "QFcpp `m_sendNextExpectedMsgSeqNum` (`Session.cpp:71` default false) and QFJ `enableNextExpectedMsgSeqNum` (`Session.java:443` default false)"; research D-6 cites "QFcpp `generateLogout(stream)+disconnect()` (`Session.cpp:230-237`), QFJ `generateLogout(...)` (`Session.java:2257-2260`)". Named with version (QFcpp v1.16.0 / QFJ 3.0.1 per plan) and file:line. Traceable.
- [x] CHK008 Is the knob shape unambiguous — a single bool controlling BOTH emit and honor, default false? [Clarity, Spec §Clarifications Q2, data-model E1] — PASS: spec Clarifications Q2 states "**A single additive `SessionConfig` bool** (default false) controlling BOTH emit and honour"; data-model E1 states "Type: `bool`, default `false` (explicit, [const §XII.5]). Semantics: a single knob controlling BOTH emitting our 789 in Logon AND honoring a peer's inbound 789"; contracts C1 states "Single knob controlling both emit (C2) and honor (C3–C6). Default false ⇒ byte-identical no-op (C8)." Unambiguous on all three axes (single/bool/default).

## Requirement Consistency

- [x] CHK009 Is the default-off no-op claim consistent with FR-004's unconditional at-logon ResendRequest suppression (i.e. suppression is gated strictly on the knob, so off ⇒ 013 path fully intact)? [Consistency, Spec §FR-004/§FR-006] — PASS: FR-004 states the suppression; FR-006 states "When the knob is **disabled** (default) … an inbound `789` MUST be ignored with recovery proceeding via the existing `ResendRequest` mechanism — zero regression for existing sessions"; data-model I-NEX-7 states "with the knob off, the outbound Logon contains no 789 field and is byte-identical to the pre-feature baseline; an inbound 789 is ignored and standard recovery applies"; contracts C9 and C5's second paragraph both state "All suppression/tolerance is gated strictly on the knob; the default path is byte-identical (013 recovery intact)." FR-004's suppression is consistently gated on the knob throughout; the 013 path is explicitly called out as intact when off.
- [x] CHK010 Are the S-031 catalogue status and the version-applicability scope consistent across spec, plan §VI delta, and the planned coverage-index entry (4.4-parity, FIXT/5.0 outstanding)? [Consistency, plan §VI, Spec §Edge Cases] — PASS: spec Edge Cases uses "implementation-parity for FIX 4.4, NOT blanket done"; plan §VI delta specifies exactly `backlog → implementation-parity-4.4` with "FIXT/5.0SP2 stay outstanding to G4"; plan Constitution Check §VI lists coverage-index as part of the §VI delta ("add the §4.4.1 coverage entry … with the same '4.4-parity, FIXT/5.0 outstanding' qualifier"); tasks T023/T024 implement those exact deltas. All three surfaces are consistent on the status label and the version-gap qualifier.
- [x] CHK011 Do both-roles requirements stay consistent given the two handlers are structurally asymmetric (acceptor reply vs initiator already-sent Logon)? [Consistency, Spec §FR-007, research D-0] — PASS: FR-007 states symmetric behaviour; research D-0 derives the structural asymmetry (acceptor reply vs initiator own-Logon-already-sent); contracts C4 handles the ordering asymmetry explicitly ("Acceptor: the resend runs AFTER the reply Logon's `store_then_emit` succeeds (`:1766`)" vs "Initiator: the resend runs after processing the Logon-ack (its own Logon was already sent @ `:601`)"); tasks T014/T015 implement each handler separately. The symmetry requirement (same observable outcomes) is consistent despite the structural asymmetry (different sequencing of reply vs own-Logon-already-sent); the contract makes the asymmetric ordering explicit so the requirement remains consistent and realizable.

## Acceptance Criteria Quality

- [x] CHK012 Can the default-off guarantee be objectively measured (captured Logon byte-identical + full existing regression suite green)? [Measurability, Spec §SC-002] — PASS: SC-002 states "100% of existing session/recovery/logon regression witnesses remain green and the outbound Logon is byte-identical to the pre-feature baseline"; RED witness `DefaultOff_ByteIdenticalLogon_InboundIgnored` names the measurement; tasks T018/T019 specify "T018 (`DefaultOff_ByteIdenticalLogon_InboundIgnored`) is green AND the full existing session/recovery/logon regression suite (013/021) passes unchanged" as the explicit completion criterion. Two objective, capturable criteria.
- [x] CHK013 Is the interop success criterion objectively measurable (zero ResendRequest on the wire + all missed messages delivered in order, both roles)? [Measurability, Spec §SC-001/§SC-005] — PASS: SC-001 specifies "reconnect delivers all K missed messages in order using only the Logon exchange — zero `ResendRequest` messages on the wire — verified by an automated test that counts wire messages"; SC-005 specifies "both-role unit tests and a live interop cell"; contracts C10 names the test file and "zero ResendRequest on the wire" as the criterion; US1 Acceptance Scenario 1 enumerates three independent criteria. All measurable with named wire-level assertions.

## Dependencies & Assumptions

- [x] CHK014 Is the dictionary-support assumption (tag 789 already permitted in the 4.4 Logon; no codegen change) stated and validated? [Assumption, Spec §Assumptions, research D-1] — PASS: spec Assumptions states "Dictionary support for tag 789 in Logon (emit + parse) is assumed available or an additive, no-new-error-slot extension"; research D-1 validates the assumption by source inspection: "`dictionaries/FIX44.xml:6128` defines `<field number='789' name='NextExpectedMsgSeqNum' type='SEQNUM'/>`" and "`Logon` message block (`:278`) lists `<field name='NextExpectedMsgSeqNum' required='N'/>` at `:284`"; plan states "The field already exists in the dictionary; no codegen." The assumption is not only stated but also directly validated against the source artifact.
- [x] CHK015 Is the cross-repo dependency for the live cell (counterparty configured with the equivalent knob in the parent harness) documented? [Dependency, contracts C10, tasks T022] — PASS: contracts C10 states "fixpp + a `EnableNextExpectedMsgSeqNum` peer (QFcpp/QFJ) recover an at-logon gap"; tasks T022 has a sub-deliverable: "configure the QFcpp/QFJ counterparty in the parent `phase-9-harness/` with `EnableNextExpectedMsgSeqNum=Y` — a harness-side config delta (cross-repo), NOT a fixpp source change"; plan Project Structure names `phase-9-harness/` in the parent. The cross-repo dependency is documented with the exact config key and identified as a harness-side delta.

## Ambiguities & Conflicts

- [x] CHK016 Is there any residual claim that 789 is "done" for all S-031 versions that would conflict with the 4.4-only scope? [Conflict, plan §VI, Spec §Edge Cases "Version applicability"] — PASS: no residual blanket-done claim found. Every artifact consistently uses "implementation-parity for FIX 4.4" or "backlog → implementation-parity-4.4" (never "done") for S-031; spec, plan, and tasks all explicitly qualify "FIX 4.4 parity; FIXT/5.0 deferred to G4." No conflict exists; the scoping is consistently partial.
- [x] CHK017 Is the asymmetric-peer behaviour (knob on, peer doesn't support 789) free of ambiguity — clearly a documented limitation (L-027-1), not an implied auto-fallback? [Ambiguity, Spec §FR-009/§Edge Cases] — PASS: FR-009 is unambiguous: "there is NO automatic `ResendRequest` fallback when the knob is enabled … operators MUST enable the knob on both ends. This both-sides-required constraint is a documented limitation (matches QFcpp/QFJ)"; spec Edge Cases "Asymmetric support" states "This is a documented limitation (matches QFcpp/QFJ); operators MUST enable 789 on both ends (FR-009). [Candidate B&L L-027 entry.]"; plan §VI delta names L-027-1; data-model I-NEX-6 formalises it; tasks T025 adds it to behaviors-and-limitations.md. No ambiguity; no auto-fallback is implied anywhere.

## Notes

- Requirements-quality only. Default-off byte-identity is RED-witnessed by T018; both-roles symmetry by T007–T009/T013–T016; the live cell by T022; the §VI catalogue/coverage scoping by T023/T024.
- Feeds the mandatory `/speckit-checklist-audit` gate (pipeline step 9).

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 17 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **17** |

### SPEC-FIXED items
(none)

### DD-DECIDED items
(none)

### WAIVED items
(none)

Anchors spot-verified: research D-1 (dictionary validation), D-2 (knob shape + QF parity refs), D-6 (X>N disposition); spec FR-006/FR-007/FR-010/SC-001/SC-002/SC-005/Edge Cases "Version applicability"/"Asymmetric support"; plan §VI delta; contracts C8/C9/C10 — all resolve in the Gate-A-converged bundle (plan.md `## Gate A` round 2 applied 2026-06-07).
