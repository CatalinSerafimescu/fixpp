// SPDX-License-Identifier: AGPL-3.0-or-later
// GENERATED by fixpp-codegen (078-precompiled-builder-libs). DO NOT EDIT.
// fixpp/vlatest/messages/UserResponse.builder.cpp -- external-linkage build_ definition (data-model.md Entity 4), compiled only into fixpp_builders_<ver>; no validator symbol (SC-003).
#include "UserResponse.hpp"
#include <fixpp/wire/body_builder.hpp>

namespace fixpp::vlatest {

::fixpp::core::expected_t<::std::span<::std::byte>> build_UserResponse(::std::span<::std::byte> out, UserResponseArgs const& args) noexcept {
    ::fixpp::wire::body_builder bb{"BF"};
    if (args.username) {
        auto r = bb.field(553, *args.username);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.user_request_id) {
        auto r = bb.field(923, *args.user_request_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.user_status) {
        auto r = bb.field(926, *args.user_status);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.user_status_text) {
        auto r = bb.field(927, *args.user_status_text);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.throttles) {
        auto gh0 = bb.group_begin(1610, 1611);
        if (!gh0) return ::std::unexpected(gh0.error());
        for (auto const& item0 : *args.throttles) {
            auto en0 = gh0->add_entry();
            if (!en0) return ::std::unexpected(en0.error());
            auto& eh0 = *en0;
    if (item0.throttle_action) {
        auto r = eh0.set_int(1611, *item0.throttle_action);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.throttle_type) {
        auto r = eh0.set_int(1612, *item0.throttle_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.throttle_no_msgs) {
        auto r = eh0.set_int(1613, *item0.throttle_no_msgs);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.throttle_time_interval) {
        auto r = eh0.set_int(1614, *item0.throttle_time_interval);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.throttle_time_unit) {
        auto r = eh0.set_int(1615, *item0.throttle_time_unit);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.throttle_msg_type) {
        auto gh1 = eh0.group_begin(1618, 1619);
        if (!gh1) return ::std::unexpected(gh1.error());
        for (auto const& item1 : *item0.throttle_msg_type) {
            auto en1 = gh1->add_entry();
            if (!en1) return ::std::unexpected(en1.error());
            auto& eh1 = *en1;
    if (item1.throttle_msg_type) {
        auto r = eh1.set_string(1619, *item1.throttle_msg_type);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge1 = bb.group_end(*gh1);
        if (!ge1) return ::std::unexpected(ge1.error());
    }
        }
        auto ge0 = bb.group_end(*gh0);
        if (!ge0) return ::std::unexpected(ge0.error());
    }
    return bb.commit(out);
}

}  // namespace fixpp::vlatest
