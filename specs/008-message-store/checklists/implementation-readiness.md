# Implementation-Readiness Checklist: 008-message-store

**Purpose**: Second-pass requirements-quality audit (`unit tests for English`) across the four highest-risk dimensions before `/speckit-implement`: durability & crash-safety, concurrency & cancellation, allocator & PMR discipline, API surface & error taxonomy. Complements the broad Gate-A `requirements.md` (content-quality and completeness already checked there) by probing for ambiguities, measurability gaps, and inter-FR consistency that the broad pass cannot deep-test.
**Created**: 2026-05-20
**Audited**: 2026-05-20 (pipeline step 9 — `/speckit-checklist-audit`; design-doc anchor `.specify/2e-msgstore.md` v0.4 cross-checked; every CHK dispositioned)
**Adversarial re-audit 2026-05-20** (user direction post-audit): pressure-tested the 5 WAIVED items; found **3 of 5 waiver rationales contained fabricated scope-pinning** the spec/design-doc did not carry — CHK004 (FIX-SL alphanumeric claim — false; FIX-SL §4.3 admits ASCII printables; directory-traversal risk via `cfg_loader`-provisioned CompIDs), CHK008 (local-NVMe-only scope-pin — `grep -nE "local|NFS|SMB"` across spec/design-doc/research returned zero hits; silent-corruption risk on NFS v3 flock-no-op), CHK030 (store-object-via-`new` claim — spec is silent; PMR-allocating user factory + default deleter = UAF). CHK006 and CHK018 waiver rationales held structurally but were mis-classified (anchors exist; should be DD-DECIDED). Verdict provisionally retracted; routed through single dual-pass on `.specify/2e-msgstore.md` v0.4 (Codex targeted review + Opus adversarial; per memory `feedback_gate_a_codex_dual_pass`).

**Closure 2026-05-20** — both passes CONFIRM all 3 confirmed gaps:
- Codex (`research/reviews/codex_2e_targeted_msgstore_review.md`): Gap 1 CONFIRM P1 / Gap 2 CONFIRM P1 / Gap 3 CONFIRM P2.
- Opus (`research/reviews/opus_2e_targeted_msgstore_adversarial_review.md`): CONFIRM all 3 at Codex's severities + 5 NEW findings (N-1..N-5 all fold into the §D.4/§D.5/§D.6 application surface; no §D.7+ needed); combined post-judging tally `P1=2, P2=3, P3=4`; closing recommendation "v0.5 can ship with §D.4/§D.5/§D.6 amendments after a single convergence pass; no Gate A reset needed; no structural reframe".

Resolution: `.specify/2e-msgstore.md` v0.4 → **v0.5** (post-sign-off targeted gap-closure pass per Appendix C v0.4→v0.5 entry; Appendix D §D.4 + §D.5 + §D.6 added; body cross-references at §4.3 / §4.4 / §6.1.1 / §6.3.5 / §6.7 / §8 / §9 seam #2). 008 bundle inherits: spec.md FR-008/FR-013/FR-025 tightened; data-model.md E5/I-16 cross-reference §D.4/§D.5/§D.6; contracts file_store.hpp / file_store_factory.hpp / message_store_factory.hpp / cfg_loader.hpp carry the docstring cross-references.

**Verdict restored: 🟢 GREEN.** `/speckit-analyze` re-run **REQUIRED** per skill step 7 before `/speckit-implement` (3 SPEC-FIXED items mean the prior `/speckit-analyze` drift check is invalidated).
**Feature**: [spec.md](../spec.md)
**Scope note**: Each item tests whether the **requirement is well-written** (complete, unambiguous, measurable, consistent, edge-covered) — not whether the implementation works. Anchors: `[Spec §FR-XXX]`, `[Spec §SC-XXX]`, `[Spec §EC]` (Edge Cases), `[Spec §Clar]` (Clarifications), or `[Gap]` / `[Ambiguity]` / `[Conflict]` markers when checking for a missing or conflicting requirement.

## Durability & Crash-Safety Requirements

- [x] CHK001 - Is the on-disk byte ordering (little-endian vs big-endian) of the 16-byte record header fields (`record_kind`, `dir`, `reserved`, `seq`, `len`, `crc32`) explicitly specified? [Gap, Spec §FR-009] — **DD-DECIDED data-model.md E4 + FR-009**: exact byte layout explicitly delegated to T-impl ("exact byte layout T-impl, must align to 8-byte boundary"); v1.0 Tier-1 (Linux/Clang/x86_64) + Tier-2 (Windows/MSVC on x86_64/ARM64) per `[const §II.3]` are all little-endian, so host-order is the implicit T-impl choice. No cross-arch portability claimed in v1.0; future BE port would bump the sentinel `version` field per the §6.3.1 mechanism.
- [x] CHK002 - Is the algorithm computing `session_triple_hash` in the sentinel record defined (input string format, hash function, truncation width)? [Gap, Spec §FR-009] — **DD-DECIDED §10 Q7 + data-model.md E4**: design doc `[2e §10 Q7]` names only the field purpose ("re-open verifies, returns `store_factory_failed` on mismatch") + the sentinel byte composition; hash algorithm + width are explicit T-impl latitude (same "exact byte layout T-impl" delegation as CHK001). The only normative contract is mismatch → `store_factory_failed`.
- [x] CHK003 - Is the data-loss window for `commit_batched(N)` and `commit_interval(ms)` quantified as a normative requirement (not just "documented in the per-policy block-comment")? [Measurability, Spec §FR-011 / §SC-002] — **DD-DECIDED data-model.md E10 + Spec §FR-011 + Spec §SC-002**: the window IS normatively quantified at the data-model behavioural-oracle layer (E10 lines 279–281: `commit_per_message` = 0% loss; `commit_batched(N)` = up to N-1 records; `commit_interval(ms)` = ms-bounded). Spec §SC-002 binds the contract ("loss MUST be bounded by the per-policy documented window — no surprise loss outside the window") and §FR-011 defers the verbatim numbers to the per-policy block-comment, where they ARE pinned (contracts/file_store.hpp lines 37–40). The split is the Phase-1 oracle convention: spec.md = abstract requirement; data-model.md = concrete normative numbers.
- [x] CHK004 - Are escaping/validation rules specified for `sender_comp_id` and `target_comp_id` strings that compose the `<sender>__<target>.log` filename (path separators, non-ASCII, length limits)? [Gap, Spec §FR-008 / §FR-009] — **SPEC-FIXED `[2e §D.4]`** (was WAIVED-with-fabricated-rationale; "FIX CompIDs are alphanumeric per FIX-SL convention" was false — FIX-SL §4.3 / Tag 49 / Tag 56 admits ASCII printables including `/`, `\`, `.`, `..`; adversarial re-audit confirmed real directory-traversal hazard, Codex CONFIRM P1 / Opus CONFIRM P1). Closure: design-doc v0.4 → v0.5 adds Appendix D §D.4 — `FileStoreFactory::make()` MUST validate CompIDs *before* composing `<directory>/<sender>__<target>.log` and *before* opening any file or taking the advisory lock, rejecting empty, path separators (`/` on Linux; `/` or `\\` on Windows), NUL, `.` / `..` segments, control chars `[0x00, 0x1F]` / `0x7F`, and excess of `NAME_MAX`. Mirrored at `cfg_loader::cfg_to_file_store_factory()` as defense in depth. `noexcept`-safe primitives (`string_view::find_first_of` / `find`; no `std::filesystem::path` until validated). 008 inherits: spec.md FR-008 tightened; data-model.md I-16 cross-references §D.4; contracts/file_store.hpp + file_store_factory.hpp + cfg_loader.hpp docstrings updated. Seam #2 sub-cases extended per N-3 (FR-033 21-seam count preserved).
- [x] CHK005 - Is the recovery contract specified for a torn **sentinel** record (first record corrupted, not just a tail record)? [Edge Case, Gap, Spec §FR-009 / §FR-012] — **DD-DECIDED §6.3.2 step 3**: "Verify the sentinel record; if invalid → return `store_factory_failed` (the directory holds a foreign file or a corrupted earlier session; recovery is operator's choice)". A torn sentinel is "invalid sentinel" and surfaces identically to sentinel-mismatch / foreign-file. Recovery is operator-driven (manual investigation), not auto-recovered — by design.
- [x] CHK006 - Is the contract for a partial `pwrite` return (short write to disk-full or signal-interrupted I/O) specified — retry, fail with `store_io_failure`, or treat as torn write? [Gap, Spec §FR-011] — **DD-DECIDED §6.1.4 Idempotency-under-cancellation footnote + §6.3.2 restart algorithm** (was WAIVED; adversarial re-audit found this waiver actually holds STRUCTURALLY but was mis-classified — the design doc DOES anchor this case): §6.1.4 footnote at `2e-msgstore.md:1005` says "FileStore treats partial writes at the tail as torn writes and truncates on next open per §6.3.2". §6.3.2 step 4 restart algorithm: "For each record: check `len` against `max_frame_bytes`; check `crc32`; on the first failure (CRC mismatch, short read, or `len > max_frame_bytes`) → truncate the log to that offset". Partial pwrite handling is T-impl (POSIX/Win32 short-return retry loop) within these contract anchors; persistent failure surfaces as `store_io_failure` per §6.1.2 trap_throw routing. Anchor exists at the design-doc level — proper disposition is DD-DECIDED, not WAIVED.
- [x] CHK007 - Is the durability scope of `MOVEFILE_WRITE_THROUGH` explicitly tied to power-loss (not just SIGKILL) survival, matching what Linux parent-dir `fsync` guarantees? [Consistency, Spec §FR-010 / §SC-002 / §SC-003] — **DD-DECIDED §6.3 + §6.3.5 + round-3 C-R3-P1-2**: design-doc §6.3 lines 1063 names the contract explicitly — "Durability of the rename — i.e., 'after a crash, will the rename still be visible?' — is **not** automatic on either platform: on Linux, the parent-directory `fsync` after the rename is **mandatory** ... on Windows, the `MOVEFILE_WRITE_THROUGH` flag ... is **mandatory** per Win32 semantics (NTFS metadata journaling commits the rename to the journal but the journal itself is buffered)". Both primitives target host-crash including power-loss; SIGKILL is the easier sub-case used in SC-002/SC-003 as the testable representative. Consistency holds.
- [x] CHK008 - Are requirements specified for `flock` / `LockFileEx` behaviour on network filesystems (NFS, SMB) where advisory locking is documented-unreliable — supported, rejected, or undefined? [Gap, Spec §FR-013] — **SPEC-FIXED `[2e §D.5]`** (was WAIVED-with-fabricated-rationale; "v1.0 deployment target is local NVMe per `[2e §1.2]`" was false — `grep -nE "local|NFS|SMB|network.?filesystem|local.?only"` across spec/design-doc/research returned zero hits; scope-pin invented. Adversarial re-audit confirmed real silent-corruption risk on NFS v3 where flock is documented silent no-op per `man 2 flock` BUGS section, Codex CONFIRM P1 / Opus CONFIRM P1). Closure: design-doc v0.4 → v0.5 adds Appendix D §D.5 — `FileStore` is supported only on filesystems where the platform advisory-lock primitive provides effective cross-process exclusive-lock semantics for every host that may open the live log path; behaviour on NFS / SMB / CIFS / FUSE / cluster FS (GPFS, Lustre, GFS2, OCFS2) is **unsupported**; `make()` does NOT detect or warn (probe-is-worse-than-nothing per Opus reasoning); operators MUST attest cross-host lock semantics out of band as a deployment precondition. A future v1.x feature may add `EngineConfig::store_filesystem_attested = true` (post-v1.0 per `[const §X.4]`). 008 inherits: spec.md FR-013 + §6.3.5 "Scope & trust" subsection cross-references §D.5; data-model.md I-16 + contracts/file_store_factory.hpp updated. §6.7 `store_io_failure` row tightened with N-2 operator-doc remediation note (shared-FS CRC mismatch likely caused by §D.5 scope violation, not hardware fault). FR-021 10-variant freeze preserved (no new variant).
- [x] CHK009 - Is the forward-compatibility invariant prohibiting cross-filesystem `.reset.tmp` written as a normative FR (not just an Edge Case discussion), so future implementations cannot regress it? [Gap, Spec §EC / §FR-010] — **DD-DECIDED Spec §EC (Cross-filesystem `.reset.tmp`)**: spec.md §EC carries the constraint as a normative `MUST` — "a future implementation that admits an external temp directory MUST reject cross-filesystem paths instead of falling back to copy". Edge Cases section is normative per the Spec-Kit template convention (`*(mandatory)*` enclosing block at spec.md §User Scenarios & Testing). Same invariant mirrored at design-doc §6.3 Cross-filesystem note (line 998).
- [x] CHK010 - Are the side-channel distinctions between sentinel-mismatch failure modes (foreign file vs. version drift vs. corrupted sentinel CRC) specified so the operator can choose recovery, given §EC says "recovery is the operator's choice"? [Clarity, Spec §EC / §FR-009] — **DD-DECIDED §6.3.2 step 3 + Spec §Observability (owed to 2k)**: by-design a single `store_factory_failed` variant covers all sentinel-invalidation modes; v1.0 does not claim auto-recovery and the operator's recovery path is uniform (manual investigation). Distinguishing failure-mode side-channels (foreign file vs. version drift vs. CRC-corrupted sentinel) is a 2k structured-log concern per design-doc §7.5 line 1163 ("All observability is implemented through 2k's Logger / Sink / TracerProvider interfaces"), explicitly deferred per Spec §Observability — owed to 2k. The single-variant + 2k-deferred-observability shape is the v1.0 contract.
- [x] CHK011 - Is `reset()`'s linearisation-vs-durability ordering unambiguous when SIGKILL occurs **between the `rename` returning success and the parent-dir `fsync` returning success** — does FR-020's "after linearisation must be durable" align with FR-010's "cannot return success until durability primitive returns"? [Consistency, Spec §FR-010 / §FR-020] — **DD-DECIDED §6.3.4 step 5 + §6.1.4 Linearisation-as-durability footnote (round-3 N3-P3-1)**: the footnote at design-doc:963 pins explicitly — "Linearisation point for FileStore methods means 'the point at which the on-disk state per §6.3 transitions to the new value AND the durability primitive ... has returned success'". §6.3.4 step 5 + Property paragraph at lines 1040–1047 confirm: SIGKILL between `rename` and parent-dir `fsync` (Linux) → durability primitive did not complete → linearisation point not reached → `reset()` did NOT return success → FSM retries on next open. FR-010 + FR-020 are consistent under this footnote.

## Concurrency, Cancellation & Strand Requirements

- [x] CHK012 - Is the invariant "concurrent calls on the same instance from the same session strand cannot occur" stated explicitly as the v1.0 single-serialisation-domain assumption that FR-016's FIFO-fairness defence-in-depth backstops? [Clarity, Spec §FR-015 / §FR-016] — **DD-DECIDED Spec §FR-024 + §FR-016**: FR-024 binds the single-strand invariant explicitly ("Every MessageStore async method MUST be invoked on the session strand via `session_executor` per `[2d §4.8]`"); FR-016's FIFO-fair `async_mutex` is the defense-in-depth backstop for the off-spec case where concurrent callers arrive from outside the session serialisation domain. The two-layer split is the v1.0 contract per `[2d §4.8]` single-session-serialisation-domain discipline + `[2f §4.3.2]` fast-path CAS-acquire.
- [x] CHK013 - Is the state of `store_arena` (post-FR-019 deep-copy) under "cancelled before linearisation point" defined — does the arena retain the copy (memory waste), or is it required to roll back? [Ambiguity, Spec §FR-019 / §FR-020] — **DD-DECIDED §6.1.4 Idempotency-under-cancellation footnote (design-doc:967)**: "no state change" in the cancellation table refers to *observable* state; pre-linearisation mutations to internal slab/arena are by-construction unobservable (`MemoryStore` does not insert into its index until the slab memcpy and entry write are complete; `FileStore` treats partial writes as torn writes truncated on next open). The deep copy in store_arena prior to linearisation is therefore not a "state change" for the cancellation-result contract; subsequent calls may reuse the slot (T-impl detail — bounded by `max_frame_bytes` per slot).
- [x] CHK014 - For `retrieve` cancelled between two `on_frame` invocations, is the contract for already-yielded frames specified (delivered? rollback semantics? side-effects observable to the visitor)? [Gap, Spec §FR-017 / §FR-020] — **DD-DECIDED §6.1.4 retrieve row + Spec §FR-020 retrieve clause**: design-doc cancellation table row for `retrieve` explicitly defines both branches — "before any visitor invocation → `expected_t::unexpected{store_cancelled}`"; "Visitor invocation in progress: cancellation propagates through the visitor's awaitable per root cause #2; the next visitor frame is not invoked; `retrieve()` completes with `store_cancelled`". Already-yielded frames are delivered (the visitor saw them); no rollback semantics (a read path has no state to roll back); side-effects of completed `on_frame` calls are the visitor's own concern, not the store's.
- [x] CHK015 - For mid-traversal mutation appending sequential seqnums in-range (e.g., snapshot ended at 42, store advances to 43 before the next `on_frame`), is the visitor's visibility of seqnum 43 defined — "already-visited frames are not re-visited" covers 42 but not 43? [Ambiguity, Spec §FR-017] — **DD-DECIDED Spec §FR-017 (already pinned)**: spec.md FR-017 explicitly says "iteration stops at the original `end`". The snapshot taken under the writer-mutex bounds the iteration; seqnum 43 appended after the snapshot is NOT visited on this retrieve call (it falls outside the original `[begin, end]`). Same applies to `end == 0` (snapshot resolves "to current tail" at snapshot time, not dynamically). This was a misread on my part — the spec is unambiguous.
- [x] CHK016 - Is the contract for `Session::close(terminal)` racing with an in-flight `FileStore::store` whose work is mid-flight on `file_io_executor` specified — does the rebind-to-strand surface a defined error, or is the awaitable abandoned? [Gap, Spec §FR-024 / §FR-028] — **DD-DECIDED §6.2.1 step 2 + data-model.md I-22**: design-doc §6.2.1 step 2 specifies "cancel → drain → destroy `unique_ptr<MessageStore>` → release `session_arena` → `~Session`". I-22 binds the test contract: "100 in-flight outstanding calls all complete (with `store_cancelled` for those whose linearisation point was not reached); no UAF on `session_arena`; `~MessageStore` runs before `session_arena` release". `cancellable_dispatch` per `[2d §6.5]` handles the `file_io_executor → session-strand` rebind under cancellation; the awaitable surfaces `store_cancelled`, not abandoned. Seam 18 verifies under TSan + ASan.
- [x] CHK017 - Are linearisation-point requirements stated symmetrically for **all four methods** (FR-020 lists store/next_seqnum/reset/retrieve) — is `retrieve`'s "per-frame, each visitor invocation has its own linearisation boundary" measurable in the same way the others are? [Measurability, Spec §FR-020] — **DD-DECIDED §6.1.4 linearisation-point list line 961**: `retrieve`'s per-frame linearisation boundary is precisely defined as "the visitor's awaitable resumes" — measurable per `on_frame` invocation by cancellation-vs-resumption timing. The four methods are symmetric in shape (each has a defined linearisation point that the cancellation table queries); `retrieve`'s split into per-frame boundaries reflects the per-frame visitor awaitable design (root cause #2), not a measurability defect. Seam 6 verifies per-method cancellation-result contract including `retrieve`'s per-frame variant.
- [x] CHK018 - Are latency requirements specified for `next_seqnum(_, false)` **under writer-mutex contention** (SC-008's ≤75 ns is "warm cache, uncontended")? [Coverage, Spec §SC-008 / §FR-015] — **DD-DECIDED Spec §SC-008 (uncontended qualifier) + Spec §FR-016 / §SC-005** (was WAIVED with framing "single-strand discipline makes contention off-design" — that framing was WRONG: FR-016 + SC-005 explicitly admit and bind the concurrent-store-from-outside-strand case as normative behavior, so concurrent-store IS in v1.0 contract. The correct anchor is SC-008's deliberate qualifier "warm cache, uncontended" — v1.0 deliberately does not budget latency under contention; concurrent-store *is* in contract but contended-path latency is by deliberate scope choice unbudgeted in v1.0; a future revision may add a contended-path SLO. The anchor exists; proper disposition is DD-DECIDED, not WAIVED).
- [x] CHK019 - Is the cancellation contract for `flush_for_session_close()` defined — FR-028 says it MUST NOT surface `store_cancelled` under graceful close, but is the behaviour under a root-total cancellation propagated from the engine specified? [Gap, Spec §FR-028] — **DD-DECIDED §6.2.1 step 1 (round-3 C-R3-P1-3)**: "The cancellation case is forced shutdown (`Session::close(terminal)` per step 2 / `cancellation_type::total` per `[2d §6.5]`), under which the hook is short-circuited and may leave the on-disk log in an unflushed state — recovery on next session open truncates the last incomplete record per the §6.3.4 startup contract; the user accepts that data-loss window." Under root-total cancellation the hook is short-circuited (not invoked at all); the §6.3.4 restart contract handles the unflushed tail.
- [x] CHK020 - Is the FIFO-fairness ordering specified as observable to the caller (i.e., is it just an internal property, or must `expected_t<void>{}` completions appear to the caller in arrival order)? [Clarity, Spec §FR-016 / §SC-005] — **DD-DECIDED Spec §SC-005 + design-doc §6.4**: SC-005 binds the observable contract explicitly — "Two coroutines concurrently invoking `store()` on the same instance from an `asio::thread_pool` MUST both complete with `expected_t<void>{}` in FIFO-arrival order". Under mutex serialisation, completion order = arrival order, observable by the calling coroutines.

## Allocator & PMR Discipline Requirements

- [x] CHK021 - Is the failure contract specified when `store_arena`'s PMR throws during FR-019's deep-copy (post-mutex-acquire, pre-pwrite) — `store_io_failure`, `store_capacity_exhausted`, or `store_visitor_aborted`? [Gap, Spec §FR-019 / §EC] — **DD-DECIDED §6.1.2 (round-2 Opus N2-P2-1)**: PMR allocation throw paths route through `fixpp::core::detail::trap_throw` per `[2a §4.2]` → `expected_t::unexpected{store_io_failure}` on FileStore disk-side; `expected_t::unexpected{store_capacity_exhausted}` on MemoryStore under `bounded` policy (unreachable under FR-007's zero-allocator-calls discipline); `expected_t::unexpected{store_io_failure}` on MemoryStore under `unbounded` policy where the throw is the only failure path. The mutex is released cleanly via RAII in all branches.
- [x] CHK022 - Is FR-007's "zero allocator calls under `bounded` after construction" reconcilable with FR-027's HALO-fallback to `session_arena` — does a HALO-elision-miss for the awaitable promise frame count as an allocator call that violates FR-007? [Conflict, Spec §FR-007 / §FR-027] — **DD-DECIDED §6.1.1 + Spec §SC-007**: scopes are disjoint. FR-007 + SC-007's "zero allocator calls" measures the `store_arena` tracking-PMR (MemoryStore's own per-call hot path); FR-027's HALO fallback is on `session_arena` (engine PMR), targeting the awaitable's coroutine promise frame on HALO-elision miss. Two different PMR resources, two different allocation domains — no conflict. Design-doc §6.1.1 confirms: "**`MemoryStore::store` performs zero allocator calls** ... `store()` is memcpy + index increment" (scope: MemoryStore-side). Coroutine promise allocation on `session_arena` is the engine's HALO fallback, not the store's.
- [x] CHK023 - Is the upstream `memory_resource` of the engine-provided default `monotonic_buffer_resource` (FR-026 fallback when `MemoryStore::Config::store_resource == nullptr`) constrained so a buffer grow cannot reach global `new` and violate FR-027? [Gap, Spec §FR-026 / §FR-027] — **DD-DECIDED §6.1.1 + data-model.md I-10 + FR-014 / I-11**: I-10 binds "one PMR allocation at construction for slot+slab combined", sized by the FR-014 / I-11 storage-DoS guard (overflow-safe checked arithmetic against `EngineConfig::max_store_memory_per_session`). Under bounded sizing the slab is allocated once at construction and never grows; subsequent `store()` calls are memcpy + index increment with zero allocator calls (FR-007). If the construction-time allocation exceeds the cap, `MemoryStoreFactory::make()` rejects with `store_factory_failed` before the upstream is touched. Upstream growth is unreachable by construction.
- [x] CHK024 - Does SC-007 extend the zero-global-allocation contract to **`FileStore::store`** under all three policies, or only to `MemoryStore::store`? [Coverage, Spec §SC-007 / §FR-027] — **DD-DECIDED Spec §FR-027 + §SC-007**: FR-027 applies to "`store(...)`" — i.e., all `MessageStore::store` implementations, not just MemoryStore. SC-007's `tools/check_alloc.py` post-link symbol scan + 10⁴-message session under `mallocnesia` covers the entire session run (both stores). The narrower zero-allocator-calls property (tracking-PMR counter unchanged) is MemoryStore-specific per FR-007 because FileStore necessarily uses `store_arena` for `pwrite` buffer scratch (FR-019 deep-copy lands there); FileStore's contract is no-global-new-but-may-use-store_arena. Both stores satisfy FR-027 collectively.
- [x] CHK025 - Is the lifetime of the `mr` parameter passed to `MessageStoreFactory::make()` (and its relationship to `store_resource`) specified — caller-owned, engine-owned, ownership-transferred? [Gap, Spec §FR-005 / §FR-026] — **DD-DECIDED §6.2.1 step 2 + I-22 + design-doc §4.4**: `mr` is the per-session PMR resource (typically pointing at `session_arena` per `[2d §4.5]`); lifetime is bound by the Session's ownership chain — I-22 binds "`~MessageStore` runs before `session_arena` release", so `mr`'s pointee outlives the store. Standard PMR convention (caller-owned, non-owning pointer); the engine holds the `session_arena` and threads its address into `make()`. No ownership transfer.
- [x] CHK026 - Is PMR-poison routing through `trap_throw` defined for both the visitor's caller-supplied resource (EC) AND the store-side `store_arena` (during deep-copy)? [Coverage, Spec §EC / §FR-019] — **DD-DECIDED §6.1.2 + data-model.md I-21**: routing is defined for both sides but to **different** variants. Visitor's caller-supplied resource throwing on the recovery path → `trap_throw` → `expected_t::unexpected{store_visitor_aborted}` (I-21 / Spec §EC PMR-poison-on-recovery-path). Store-side `store_arena` throwing during FR-019 deep-copy → `trap_throw` → `store_io_failure` (FileStore) or `store_capacity_exhausted` (MemoryStore bounded — unreachable) / `store_io_failure` (MemoryStore unbounded) per §6.1.2. Coverage is complete; the variant-side asymmetry reflects which surface owns the failure.
- [x] CHK027 - Is the `MemoryStore::Config::store_resource` lifetime contract specified — must outlive the store; what is the engine's obligation when it provides the default resource and the store is destroyed? [Gap, Spec §FR-026] — **DD-DECIDED Spec §FR-026**: engine-provided case is explicit — "the engine MUST provide a dedicated `std::pmr::monotonic_buffer_resource` whose lifetime matches the store instance" (so the engine owns it and destroys it after `~MemoryStore`). Caller-supplied case is PMR convention (`memory_resource*` is a non-owning pointer; caller must keep the resource alive for the store's lifetime), consistent with `mr` lifetime in CHK025. The two surfaces compose correctly.

## API Surface, ABI & Error-Taxonomy Requirements

- [x] CHK028 - Are the C-ABI slot numbers for the 10 `store_*` error variants (56–65 per CLAUDE.md) carried in **spec.md**, not only in `data-model.md`, so the spec is self-contained traceability for FR-021 / FR-023? [Traceability, Spec §FR-021] — **DD-DECIDED data-model.md §"Error mapping" + contracts/store_errors.hpp**: slot allocation (56–65) is carried in the Phase-1 behavioural oracle (`data-model.md` lines 354–364) AND the type-level oracle (`contracts/store_errors.hpp` lines 45–82) per the Phase-1 split convention established by 001/002/003/004/006/007. Spec.md FR-021 lists the 10 variant names + the prefix-group mapping (FR-023); concrete slot numbers belong in the data-model layer (consistent precedent — 007's `clock_*`/`dispatch_*` slots 47–55 live in `007-threading-clock/data-model.md`, not spec.md). Traceability is satisfied via the chain spec.md → data-model.md → store_errors.hpp.
- [x] CHK029 - Is the `noexcept` requirement on awaitable-returning methods (FR-001) consistent with the coroutine-frame `std::bad_alloc` path under HALO-elision miss + PMR fallback strategy? [Consistency, Spec §FR-001 / §FR-027] — **DD-DECIDED §6.1.2 (round-2 Opus N2-P2-1)**: `trap_throw` pattern at `[2a §4.2]` is the no-terminate-on-PMR-throw mechanism; PMR allocation throws inside `noexcept` coroutines are caught by `trap_throw` and converted to `expected_t::unexpected{store_io_failure}`. The `noexcept` contract is preserved at the function boundary; the throw inside the body is captured. Consistent.
- [x] CHK030 - Is the `unique_ptr<MessageStore>` deleter shape specified — default `delete`, custom deleter for PMR-allocated impls, or impl-defined? [Gap, Spec §FR-005 / §FR-025] — **SPEC-FIXED `[2e §D.6]`** (was WAIVED-with-fabricated-rationale; "store object is heap-allocated by the factory via `new`" was false — spec/design-doc/research are silent on store-object allocation; PMR-allocating user factory + bare `std::unique_ptr<MessageStore>` default-deleter = UAF on destruction; adversarial re-audit confirmed real latent UB, Codex CONFIRM P2 / Opus CONFIRM P2 with forward-compat reservation per N-4). Closure: design-doc v0.4 → v0.5 adds Appendix D §D.6 — `MessageStoreFactory::make()`'s return type commits the v1.0 contract to `std::default_delete<MessageStore>` destruction; the concrete store object MUST be destructible via `delete static_cast<MessageStore*>(p)`; factory impls wishing to PMR-allocate the store object MUST wrap deallocation into a `std::default_delete`-compatible path (typical: static `operator delete` overload + `std::pmr::polymorphic_allocator::new_object`). `std::unique_ptr<MessageStore, CustomDeleter>` return type is **NOT** supported in v1.0; reserved for post-v1.0 per `[const §X.4]` (N-4 forward-compat reservation). The PMR resources discussed in §6.1.1 / §8 / FR-026 / FR-027 govern only the store's *internal* storage (slab, ring, framing scratch, index), not the deleter shape of the store object itself. Cross-references added in §6.1.1 + §8 separating "object allocation" from "internal storage allocation". 008 inherits: spec.md FR-025 tightened; data-model.md E5 cross-references §D.6; contracts/message_store_factory.hpp docstring updated.
- [x] CHK031 - Is `retrieve_visitor::abort_error()`'s invocation contract specified for **`visit_result::stop`** (vs. `abort`) — is it called on `stop`, never called, or impl-defined? [Ambiguity, Spec §FR-004] — **DD-DECIDED contracts/retrieve_visitor.hpp lines 25–29**: the contract is unambiguous in the contracts/ type-level oracle — `cont` continues iteration; `stop` causes `retrieve()` to return `expected_t<void>{}` (success, no `abort_error()` invocation); `abort` causes `retrieve()` to return `visitor.abort_error()` (default `store_visitor_aborted`). `abort_error()` is invoked ONLY on `visit_result::abort`. Mirrored at data-model.md I-20.
- [x] CHK032 - Is the on-disk version-skew policy specified for the `dir(1)` field if `direction_t` ever gains a v-value reserved per `[const §X.4]` — does the FR-009 record format carry its own version field, or does the sentinel `version` govern? [Gap, Spec §FR-002 / §FR-009] — **DD-DECIDED §6.3.1 + Spec §FR-009**: the sentinel record carries `magic | version | session_triple_hash | crc32` (FR-009). A future direction_t v-value (e.g., admin = 2) per `[const §X.4]` would bump the sentinel `version` field; readers verify the sentinel `version` matches their expected schema and either accept or reject (mismatch → `store_factory_failed`). The sentinel `version` is the on-disk format-version owner; per-record format evolution is governed by the sentinel mechanism. No separate per-record version field needed.
- [x] CHK033 - Is the `static_assert(!std::is_constructible_v<…, FakeQuickFixStore*>)` constraint required only in the test (FR-032's framing) or also as a production-header guarantee that catches any user-defined sync-shaped class? [Clarity, Spec §FR-032] — **DD-DECIDED Spec §FR-032 + research D-15**: FR-032's `static_assert` is a test-side defense-in-depth check (seam 11; `FakeQuickFixStore` is test-only). The production-header constraint is implicit but stronger — `MessageStore` has deleted copy/move + virtual destructor + ONLY the default constructor (data-model.md E1 lines 20–25 / contracts/message_store.hpp lines 31–35); there is no constructor taking any pointer argument, so `is_constructible_v<MessageStore, FakeQuickFixStore*>` is false by type-system construction. The `static_assert` test catches regressions if someone later adds a misguided constructor; it is not a production-header concept.
- [x] CHK034 - Is the executor-injection contract for `cfg_loader`-produced factories complete — does the spec require the produced factory to fail-fast at `make()` if the engine threads in an empty `file_io_executor` and the Config's was also empty? [Coverage, Spec §FR-030 / §FR-014] — **DD-DECIDED Spec §FR-030 + §FR-014 + data-model.md I-11**: cfg_loader returns a `FileStoreFactory` whose `FileStore::Config::file_io_executor` is default-constructed (empty) per FR-030. FR-014's last clause binds: "`FileStoreFactory::make()` MUST also reject with `store_factory_failed` if both the factory's stored `Config.file_io_executor` AND the engine-threaded `file_io_executor` (make()'s 5th parameter per FR-005) are empty". I-11 verifies via seam 4. Composition is fail-fast and complete.
- [x] CHK035 - Are the prefix-group mappings in FR-023 symmetric and exhaustive — every variant in FR-021's 10-item list mapped to exactly one group, and the group totals (3+4+1+1+1 = 10) match? [Consistency, Spec §FR-021 / §FR-023] — **PASS**: verified element-wise. STORE_RUNTIME = {`store_io_failure`, `store_capacity_exhausted`, `store_seqnum_overflow`} (3); STORE_CONSISTENCY = {`store_seqnum_gap`, `store_seqnum_out_of_order`, `store_seqnum_invalid`, `store_invalid_range`} (4); STORE_CONFIG = {`store_factory_failed`} (1); STORE_VISITOR = {`store_visitor_aborted`} (1); CANCELLED (reused) = {`store_cancelled`} (1). Total = 10, matches FR-021's count; sets are disjoint and exhaustive; mirrored at data-model.md error table + contracts/store_errors.hpp lines 21–29.
- [x] CHK036 - Is the session-fatal property of `store_seqnum_overflow` (FR-022) reflected in `[Spec §FR-021]`'s variant list itself (e.g., a normative annotation), or is it carried only in FR-022? [Traceability, Spec §FR-021 / §FR-022] — **DD-DECIDED Spec §FR-022 + data-model.md I-18**: the session-fatal property is normatively bound by FR-022 prose ("session-fatal: once observed, the session cannot send further outbound messages until `reset()` is called") + data-model.md I-18 ("session-fatal: once observed, the session cannot send further outbound messages until `reset()` is called. The store does NOT autonomously reset"). FR-021 is the variant-enumeration list; per-variant semantics live in FR-022 (and FR-023 for ABI mapping). Same split convention as other variants — there is no normative requirement to inline per-variant semantics into FR-021's enumeration.
- [x] CHK037 - Does spec.md specify the `MessageStoreFactory::make()` parameter **order** (`sender_comp_id, target_comp_id, mr, max_store_memory_bytes, file_io_executor`) and the binding (positional vs. named-via-contracts header) to a degree that pins the ABI? [Clarity, Spec §FR-005] — **PASS**: spec.md FR-005 explicitly lists the parameter order in prose; contracts/message_store_factory.hpp lines 76–81 + data-model.md E5 pin the positional signature as the type-level oracle (per the contracts/ block-comment at lines 5–14: "SHAPE ORACLE — declaration-only contract ... Frozen at /plan time; /tasks generates implementation against this header"). The signature is pinned at /plan time and is the ABI; spec.md prose + contracts/ header agree on parameter order.

---

## Audit Result

**Feature**: 008-message-store
**Checklist**: `implementation-readiness.md` (37 items, CHK001–CHK037)
**Other domain checklists**: none (`requirements.md` is the auto-`/specify` spec-quality checklist, exempt per skill rule)
**Design-doc anchor verified**: `.specify/2e-msgstore.md` v0.4 (Gate-A-converged, round 3, post-cap line-edit pass)

### Per-domain tally (post-adversarial-re-audit, 2026-05-20)

| Domain | PASS | SPEC-FIXED | DD-DECIDED | WAIVED | Total |
|--------|------|------------|------------|--------|-------|
| Durability & Crash-Safety (CHK001–011) | 0 | 2 (CHK004 §D.4, CHK008 §D.5) | 9 | 0 | 11 |
| Concurrency, Cancellation & Strand (CHK012–020) | 0 | 0 | 9 | 0 | 9 |
| Allocator & PMR Discipline (CHK021–027) | 0 | 0 | 7 | 0 | 7 |
| API Surface, ABI & Error Taxonomy (CHK028–037) | 2 (CHK035, CHK037) | 1 (CHK030 §D.6) | 7 | 0 | 10 |
| **Total** | **2** | **3** | **32** | **0** | **37** |

**Initial audit tally (2026-05-20, before adversarial re-audit, for historical record):** 2 PASS / 0 SPEC-FIXED / 30 DD-DECIDED / 5 WAIVED. The 5 WAIVED items were CHK004, CHK006, CHK008, CHK018, CHK030. Adversarial re-audit reclassified all 5: CHK004/CHK008/CHK030 → SPEC-FIXED via design-doc Appendix D §D.4/§D.5/§D.6 amendments; CHK006/CHK018 → DD-DECIDED with anchors that the initial waiver-framing missed.

### Findings + resolutions (post-adversarial-re-audit)

All 37 items dispositioned. Notable:

- **3 SPEC-FIXED items (CHK004 / CHK008 / CHK030)** triggered by adversarial re-audit which surfaced fabricated scope-pinning in the initial waiver rationales. Resolution: design-doc `.specify/2e-msgstore.md` v0.4 → v0.5 via single dual-pass (Codex targeted review + Opus adversarial) per memory `feedback_gate_a_codex_dual_pass`; **not** a full `/gate-a-ph2` Gate A reset (the skill is shaped for fresh design-doc drafting, not for targeted post-sign-off amendments — surfaced this mismatch to user before sinking expensive agent calls). The dual-pass confirmed all 3 gaps at Codex's severities (Codex Gap 1 P1 / Gap 2 P1 / Gap 3 P2; Opus CONFIRM all 3 + 5 NEW findings N-1..N-5 folding into the §D.4/§D.5/§D.6 application surface; combined post-judging `P1=2, P2=3, P3=4`; closing recommendation "v0.5 can ship after a single convergence pass"). v0.4 → v0.5 closes the gaps via Appendix D §D.4 (CompID filesystem-safety at `FileStoreFactory::make()`), §D.5 (FS-type scope-pin in §6.3.5 for `flock` / `LockFileEx` semantics), §D.6 (`std::default_delete<MessageStore>` pin in §4.4 + post-v1.0 custom-deleter reservation per `[const §X.4]`). Body cross-references at §4.3 / §4.4 / §6.1.1 / §6.3.5 / §6.7 / §8 / §9 seam #2. 008 bundle propagation: spec.md FR-008 / FR-013 / FR-025 tightened; data-model.md E5 / I-16 cross-reference §D.4/§D.5/§D.6; contracts/file_store.hpp + file_store_factory.hpp + message_store_factory.hpp + cfg_loader.hpp docstrings updated. FR-021 10-variant freeze preserved (no new error variant — N-2's observability surface goes into §6.7 `store_io_failure` row remediation as operator-doc note).
- **2 reclassified DD-DECIDED items (CHK006 / CHK018)** — initial waiver rationales held structurally but were mis-classified; anchors exist (CHK006 → §6.1.4 idempotency-under-cancellation footnote + §6.3.2 restart algorithm; CHK018 → SC-008's explicit "warm cache, uncontended" qualifier). Reclassified from WAIVED to DD-DECIDED with corrected anchors.
- **Zero remaining WAIVED items.** All 5 initial WAIVED items reclassified.
- **Lessons learned (for v1.0 release-gate sweep per memory `project_v1_release_gate_recheck_checklists`):** waiver rationales generated optimistically (without adversarial pressure-test) can fabricate scope-pinning that the spec/design-doc do not actually carry. The adversarial re-audit is what caught it; the original step-9 audit alone did not. v1.0-release-gate sweep MUST pressure-test every WAIVED rationale across all features (001–008+) — `grep -nE "<claimed-scope-pin-term>"` against the cited anchor source is the cheapest screening filter.
- **Realizability sub-check (skill step 4)**: PASSED. Every value-typed entity in `data-model.md` / `contracts/` is realizable as a complete type from its contract alone. The placeholder `seqnum_t` is authored fresh in this feature (FR-003 / research D-1 / contracts/seqnum.hpp), so there is no "by-value member with forward-declared type owned by a deferred spec" hazard. `MessageStore::retrieve(retrieve_visitor& visitor)` takes a reference, so the forward-declaration of `retrieve_visitor` in `message_store.hpp` is C++-legal.
- **Anchor spot-verification (skill step 5)**: PASSED. Every design-doc anchor cited (including the new §D.4 / §D.5 / §D.6) was verified to exist in the post-amendment `.specify/2e-msgstore.md` v0.5 and the matching spec / data-model / contracts files. Zero dangling refs. The 2 reviews (`research/reviews/codex_2e_targeted_msgstore_review.md` + `research/reviews/opus_2e_targeted_msgstore_adversarial_review.md`) are recorded on disk.

### Pipeline disposition

- **`/speckit-analyze` re-run required?** **YES.** 3 items resulted in `SPEC-FIXED` (CHK004 / CHK008 / CHK030); the spec body and design-doc were edited; the prior `/speckit-analyze` drift check is invalidated. Re-run `/speckit-analyze` before `/speckit-implement` per skill step 7.
- **Verdict**: 🟢 **GREEN (restored after adversarial re-audit + v0.4 → v0.5 amendments + /speckit-analyze re-run pending)**. Pipeline step 9 (CHECKLIST AUDIT) is **satisfied**. `/speckit-implement` (pipeline step 10) is unblocked **subject to /speckit-analyze re-run**.
