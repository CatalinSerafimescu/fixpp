# Specification Quality Checklist: `MessageStore` Async API + Default Impls

**Purpose**: Validate specification completeness and quality before proceeding to planning
**Created**: 2026-05-20
**Re-derived**: 2026-05-20 (Gate A round 1 rewrite + fresh-loop round 1 — every prior `[x]` was reset and re-checked against the corrected bundle; manually editing `[x]` marks defeats the gate per `feedback_checklist_audit_realizability`)
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] No implementation details (languages, frameworks, APIs) — *Authority anchor: design doc wins. Per `007-threading-clock` precedent, naming concrete C++ types, headers, syscalls (`fdatasync` / `MoveFileExW` / `flock`), and `[FIX-SL]` / `[const]` / `[arch]` / `[2d]` / `[2f]` / `[2b]` anchor citations is mandatory exact-coverage per `[const §VI.5]` — not a leak of implementation choice. Evidence: spec.md §Normative References + §Authority anchor blockquote.*
- [x] Focused on user value and business needs — *Four user stories framed around the four consumer classes (application developer, deferred 005 FSM, C-ABI / SWIG / Python, QuickFIX migrator). Evidence: spec.md User Story 1–4 (lines 46–112).*
- [x] Written for non-technical stakeholders — *Bounded; like 007's spec, this is a constitutionally-grounded engineering spec for the FIX engine. Evidence: Authority anchor + User Scenarios are stakeholder-readable.*
- [x] All mandatory sections completed — *Authority anchor, Normative References, Clarifications (Session 2026-05-20), User Scenarios (4 stories + Edge Cases), Functional Requirements (FR-001..FR-039 minus FR-035/FR-036 which were deferred to 2k per Root cause #3 — observability owed to 2k), Key Entities, Success Criteria (12), Assumptions, Dependencies. Evidence: spec.md headers.*

## Requirement Completeness

- [x] No [NEEDS CLARIFICATION] markers remain — *Design doc is signed-off Gate-A-converged v0.4; the five Session 2026-05-20 entries cover codebase-reality scoping only. Evidence: `grep -n "NEEDS CLARIFICATION" spec.md` returns nothing.*
- [x] Requirements are testable and unambiguous — *Each FR cites the design-doc section / root-cause / Codex-or-Opus finding it traces to and is paired with a §9 seam under FR-033 + a measurable SC-* outcome. FR-021 uses the correct type name `fixpp::core::error` (Gate A round 1 R#2 fix). FR-030 (cfg_loader path-only) + FR-005 (5-param make) + FR-024a (`EngineConfig::file_io_executor` field) now compose end-to-end per fresh-loop root cause #1 — a `cfg_loader(path) → FileStoreFactory → make()` chain has a legal executor injection path via the engine-threaded 5th parameter (FR-014 / I-11). Evidence: FR-005 + FR-024a + FR-030 + FR-014 + data-model.md I-11.*
- [x] Success criteria are measurable — *SC-001..SC-012 specify N, byte-equality, % loss, ns/µs/ms ceilings (SC-008 now ≤ 75 ns for `next_seqnum(false)` after Gate A round 1 N3 widen with arithmetic breakdown), % regression thresholds, named tool outputs (`tools/check_alloc.py`, `mallocnesia`), 0-race / 0-UAF assertions. Evidence: SC-001..SC-012.*
- [x] Success criteria are technology-agnostic (no implementation details) — *Bounded by the same constraint as Content Quality #1: SC-* names latency budgets and platform durability primitives (the named promise of the feature), not framework choices. Evidence: SC-008 / SC-002 / SC-003.*
- [x] All acceptance scenarios are defined — *4 user stories × 3–5 acceptance scenarios each = 17 scenarios; Edge Cases section enumerates 11 boundary / error / failure-mode cases. US3 graceful-close scenario reworded to "32 < N-1 = 63, within the up-to-N-1 documented window" per Gate A round 1 R#3 wording fix. Evidence: spec.md User Story 1–4 + Edge Cases.*
- [x] Edge cases are identified — *Sequence overflow, `begin=0` rejection, `end<begin` rejection, never-persisted gap, visitor abort, mid-traversal mutation, cross-filesystem `.reset.tmp`, sentinel mismatch, directory contention, stale `.reset.tmp`, PMR poison. Evidence: spec.md §Edge Cases.*
- [x] Scope is clearly bounded — *Authority-anchor block states "store-side contract only" for S-014 (the FSM is `005`'s); §1.1 boundaries from the design doc are mirrored in the FR groupings; FR-030 / FR-039 close `[arch §11]` Q3; Assumptions enumerate what is in v1.0 vs post-v1.0 (COM-009). The cfg_loader is path-only after Gate A round 1 R#1 (no `EngineConfig&` back-channel); FileStore Config is the design-doc 7-field shape. Evidence: FR-030 + FR-008 + Assumptions.*
- [x] Dependencies and assumptions identified — *Dependencies section enumerates upstream merged features (001/002/003/004/006/007) with the specific surfaces each contributes, plus downstream consumers (005 deferred, 2g–2m). Assumptions section names the 2e v0.4 authority, the pre-applied Appendix D amendments, the deferred-005 scripted test-double pattern, the COM-009 forward-compat scope, the engine-level `shared_ptr` vs session-level `unique_ptr` factory asymmetry (Gate A round 1 N2 truth-in-advertising). Evidence: spec.md §Dependencies + §Assumptions + Clarifications Session 2026-05-20 Q5.*

## Feature Readiness

- [x] All functional requirements have clear acceptance criteria — *Each remaining FR maps to a §9 seam (FR-033) and a SC-* outcome; the Independent Test paragraph on each user story names the specific seams that validate the story's FRs. FR-035 / FR-036 were deferred to 2k (Root cause #3 — observability owed to 2k) and are no longer in the Requirements list, so the seam-mapping over-claim is corrected. Evidence: spec.md §Requirements + the deferred-to-2k Observability section at line 207.*
- [x] User scenarios cover primary flows — *P1: crash-survival + capacity (the two load-bearing invariants); P2: concurrent-writer + cancellation + reset atomicity (the safety story); P3: QuickFIX migration (the OSS-002 discharge). Evidence: spec.md User Stories 1–4.*
- [x] Feature meets measurable outcomes defined in Success Criteria — *SC-001 (round-trip) ↔ Story 1; SC-002/003 (crash + reset) ↔ Story 1 + 3; SC-004 (bounded) ↔ Story 2; SC-005 (FIFO-fair) ↔ Story 3; SC-006 (cancellation) ↔ Story 3; SC-007/008 (alloc + latency, with SC-008 next_seqnum ceiling widened to 75 ns per Gate A N3) ↔ all stories; SC-009 (migration) ↔ Story 4; SC-010 (conformance) ↔ Story 1; SC-011 (seams) ↔ FR-033/034; SC-012 (catalogue) ↔ FR-037/038/039 with merger-applied step-19 promotion at Gate-B merge per `feedback_pipeline_mark_done_step`. Evidence: SC-001..SC-012.*
- [x] No implementation details leak into specification — *Bounded by Content Quality #1 + #4: technical specificity here is the constitutional exact-coverage citation discipline, not framework choice. Evidence: same as Content Quality #1.*

## Notes

- Validation re-derived 2026-05-20 (Gate A round 1; refreshed at fresh-loop round 1 after the 3-round-exhausted hand-edit over-reach surfaced an executor-injection composition gap — root cause #1 — that the in-bundle rewrite then closed) — every `[x]` was reset to `[ ]` and re-checked against the corrected bundle with a one-line evidence note per row, per `feedback_checklist_audit_realizability`.
- The original 2026-05-20 first-pass checklist self-certified `[x]` on 7 of ~11 quality rows where Gate A round 1 surfaced defects: the FR-021 wrong `error_code` type name, FR-035/036 unmapped to §9 seams, the half-batch durability-bound wording, the split FileStore Config / cfg_loader surface, the pipeline-order contradiction at the Gate-A position. All those defects were closed in the rewrite, so the re-derived checklist's `[x]` marks are warranted against the corrected bundle (each carries a one-line evidence cite above).
- The Session 2026-05-20 clarifications resolve codebase-reality scoping only (not design-doc decisions), following the `007-threading-clock` Session 2026-05-19 precedent. **Five Q/A pairs** total: (1) Phase-4 FSM consumer via scripted test-double; (2) `seqnum_t` placeholder header authored fresh; (3) `[2d]` cross-doc amendments pre-applied, `[arch]` + `coverage-index` amendments owed by this feature; (4) Hybrid catalogue update ownership (structural edits in this feature's merge; Status flip merger-applied at Gate-B per `pipeline.md` step 19 / 007 / `[2c App D]` precedent); (5) `EngineConfig::max_store_memory_per_session` added by this feature (1 GiB default, engine-wide, no session override; threaded into `make()` as the 4th parameter so the factory CTOR stays Config-only per design-doc §4.4 frozen surface).
- The spec deliberately mirrors `007-threading-clock`'s "Authority anchor: design doc wins" framing because that pattern carried `007` through Gate-A, Gate-B, and merge with no design-doc-vs-spec drift.
