# Tasks: Initiator reset_on_logon Outbound Seqnum Restore on Peer 141=Y Echo

**Feature**: `032-initiator-reset-outbound-advance` · **Branch**: `032-initiator-reset-outbound-advance`
**Input**: spec.md (US1/US2, FR-001..009, SC-001..006), plan.md, research.md (R1–R5),
data-model.md, contracts/initiator-reset-echo.md, quickstart.md.
**Gate A**: CONVERGED 2026-06-11 (gate-a-done). The matured design (latch the emit-time fact;
Mechanism A only) is encoded inline below.

## Format: `[ID] [P?] [Story] Description`

## Path Conventions

- Source: `src/session/session.cpp`, `include/fixpp/session/session.hpp`, `include/fixpp/session/seqnum_manager.hpp` (+ `.cpp` if split)
- Unit tests: `tests/session/test_persistent_seqnum_hydrate.cpp`, `tests/session/test_reset_seqnum_policy_matrix.cpp`, `tests/session/test_reset_on_lifecycle.cpp`, `tests/session/test_refresh_on_logon.cpp`
- Interop cell: `tests/interop/happy/hp_fix44_reset_on_logon_test.cpp` (confirm exact filename at T002)
- Catalogue / docs: `spec/feature-catalogue.md`, `spec/coverage-index.md`, `spec/behaviors-and-limitations.md`

## Phase 1: Setup

- [X] T001 Confirm a green baseline + the true RED before any edit: build the debug preset (`-j2`, WSL2 OOM cap) and run `persistent_seqnum_hydrate`, `reset_seqnum_policy_matrix`, `reset_on_lifecycle`, `refresh_on_logon`. Record pass counts. **Guard against stale build artifacts** (a prior-branch `session.cpp.o` masked pins in 030 — clean-build to be sure): if the baseline looks off, `touch src/session/session.cpp` + rebuild. **TDD gate:** W1 (T002) and W2 (T003) MUST be confirmed RED on the unmodified `main` body before the fix lands (T006–T010).

## Phase 2: User Story 1 — reset_on_logon initiator establishes and stays up (Priority: P1) 🎯 MVP

**Goal** (spec US1 / FR-001/FR-002/FR-003/FR-004/FR-006): after a reset-before-send initiator's `Logon(141=Y,34=1)` and the peer's `141=Y` echo, fixpp keeps next-outbound at `2` (no duplicate `34=1`), reaches Active, preserves the 030 inbound restore (next-inbound==2), and labels `by_peer_request=false`.

**Independent test**: drive a `reset_on_logon=true` initiator; feed a peer `141=Y` Logon-ack at seq 1; assert Active, `peek_outbound()==2`, a post-Active originated frame carries wire `34=2` (no second `34=1`), and the reset event is `by_peer_request=false`.

### RED tests (write first; W1 + W2 must be RED on current `main`)

- [X] T002 [US1] **W1 (the bug; extend the committed harm test — P2#4)** in `tests/session/test_persistent_seqnum_hydrate.cpp`: un-`DISABLED_` and EXTEND `ResetOnLogon_Initiator_PeerAck141_OutboundStaysTwo` (`:2158`). Keep the existing asserts (Active, `peek_outbound()==2`, inbound==2 — counters are a SUPPORTING check). **Add the wire witness (SC-002):** after the peer `141=Y` ack, originate a post-Active outbound frame through the unit harness, capture the emitted bytes, and assert it carries `34=2` with **no** second frame at `34=1`. RED on current `main` (outbound rebased to 1 → next send duplicates `34=1`). [FR-001/FR-003, SC-001/SC-002, W1]
- [X] T003 [P] [US1] **W2 (event label)** in `tests/session/test_reset_seqnum_policy_matrix.cpp` (the `recent_events()` reset-event idiom already lives there — e.g. `:653` — so this is the idiomatic home; B1-resolved, no longer split with `test_persistent_seqnum_hydrate.cpp`): same `reset_on_logon=true` + non-strict-policy fixture ⇒ `recent_events()` carries `sequence_numbers_reset{by_peer_request=false}`. RED on `main` (today emits `true` because `we_initiated` is `bilateral_strict`-only at `:3224`). [FR-006, SC-006, W2]
- [ ] T004 [P] [US1] **W8 (the latch counterexample — RC1)** new test: hydrated initiator seeded `{next_inbound=37, next_outbound=1}`, `reset_on_logout=true`, `reset_on_logon=false`, non-strict policy → emits its Logon with **no** `141=Y` (because `seqnums_at_one` is false: inbound≠1 at emit); feed a peer spontaneous in-sequence `141=Y` ack ⇒ outbound stays at its baseline (NOT restored to 2) and `by_peer_request=true`. **Note:** GREEN-on-main (main has no restore at all) — discriminating ONLY against the rejected reconstruction gate; pair with the T005 mutation. [FR-005/FR-006, W8, research R3 hydrated row]
- [ ] T005 [P] [US1] **Latch-lifecycle cross-reconnect mutation witness** new test (research R5): a `Session` reused across reconnect where connection 1 is a reset-logon (latch true) and connection 2's Logon is NOT a reset (latch must read false); feed connection 2 a peer-spontaneous `141=Y` ⇒ outbound is NOT restored. Goes RED under a conditional-set-plus-clear latch mutation (stale `true` survives), GREEN under the unconditional-assign-every-emit design (T007). [W-latch-lifecycle]

### Implementation (make T002/T003 GREEN; keep T004/T005 GREEN)

- [X] T006 [US1] In `include/fixpp/session/session.hpp`, add a private strand-confined member `bool own_logon_sent_reset_flag_ = false;` (the latched emit-time `141=Y`-emit fact). [contract Inputs, data-model]
- [X] T007 [US1] In `src/session/session.cpp` `emit_initiator_logon_` (at `:721`, where `initr_reset_seqnum` is computed for the Logon being built): **unconditionally assign** `own_logon_sent_reset_flag_ = initr_reset_seqnum;` (overwrites to `false` when fixpp does not emit `141=Y`) — NOT a conditional set-only-when-true. This is the shared open()/`drive_reconnect()` emit point, so reconnect emits cannot inherit stale state. [RC1, contract C4, research R3]
- [X] T008 [US1] Add the outbound-restore primitive (no existing twin — Codex P2#3): in `include/fixpp/session/seqnum_manager.hpp` (+ `.cpp` if split) add a private/internal `set_next_outbound(seqnum_t)` mirroring the existing `set_next_inbound`. [contract Mechanism A, plan Technical Context]
- [X] T009 [US1] Add `Session::persist_outbound_advance_()` (no existing outbound-persist path — `assign_outbound` advances in-memory only): in `src/session/session.cpp` (+ decl in `session.hpp`) mirror `029`'s `persist_inbound_advance_()`, with the `030` **fatal-when-persistent** disposition. [contract C3, plan Technical Context, FR-007]
- [X] T010 [US1] In `src/session/session.cpp` the initiator `peer_ack_sent_reset_flag` arm (`:3185`): (a) **snapshot + clear the latch before the first `co_await`**: `const bool own_logon_sent_reset_flag = own_logon_sent_reset_flag_; own_logon_sent_reset_flag_ = false;` — all logic below uses the local (one-shot, independent of reset/persist success — Codex-r2 P2#1); (b) capture `const seqnum_t n_pre_outbound = seqnum_mgr_.peek_outbound();` BEFORE `reset_seqnums_to_one_durable` (`:3195`); (c) after the existing reset + the 030 inbound restore (`:3210`), add: `if (own_logon_sent_reset_flag && n_pre_outbound == seqnum_min + 1) { set_next_outbound(seqnum_min + 1); persist_outbound_advance_(); }` (manager-first, store-second; fatal-when-persistent); (d) at `:3224` replace `const bool we_initiated = (policy == bilateral_strict);` with `const bool we_initiated = own_logon_sent_reset_flag;` so `by_peer_request = !we_initiated` keys on the latch alone. **The two gates are DISTINCT** — restore needs `latch && reset_before_send`; the label needs the latch alone (they diverge on `bilateral_strict`-at-N). [RC1/RC2, FR-001/FR-005/FR-006, contract C1/C4]
- [ ] T010a [US1] **Mutation-confirmation of the latch gate (E1 — W7/W8 are GREEN-on-main, so they only discriminate via mutation; required by plan §VII + research R5, [[feedback_witness_asserts_named_postcondition_not_proxy]]).** After T010 makes W1/W2 green, run two temporary source mutations and confirm each goes RED, then REVERT: (a) drop the `own_logon_sent_reset_flag &&` conjunct from the restore gate (restore on `reset_before_send` alone) → **W7** (fresh-no-knob peer-spontaneous-at-seq-1, T012) MUST go RED (spurious restore to 2); (b) substitute the rejected reconstruction `we_initiated := bilateral_strict || (any_reset_knob && reset_before_send)` for the latch read → **W8** (hydrated `{37,1}`+reset_on_logout, T004) MUST go RED (misclassified fixpp-initiated); (c) **(label-gate distinctness — advisor-found)** collapse the C4 label gate into the C1 restore gate: substitute `we_initiated := own_logon_sent_reset_flag && (n_pre_outbound == seqnum_min + 1)` (i.e. `latch && reset_before_send`) for the latch-alone read at `:3224` → **W4b** (bilateral_strict-at-N label, T013) MUST go RED (`by_peer_request` flips to `true` because `reset_before_send` is false on that row). This is the ONLY proof that C4 (label = latch alone) and C1 (restore = latch && reset_before_send) are implemented as DISTINCT gates, not one collapsed predicate. All three reverted, all green under the shipped latch design. This is the only proof the RC1 latch is not inert AND that the two gates do not collapse. [RC1, W7/W8/W4b, FR-005/FR-006]

## Phase 3: User Story 2 — Peer-spontaneous reset is unaffected (Priority: P1)

**Goal** (spec US2 / FR-005/FR-008): a peer-initiated reset where fixpp did NOT emit `141=Y`, and `bilateral_strict`-at-N, and the acceptor/knob-off paths, are byte-identical to baseline; the latch-false and `reset_before_send`-false rows do NOT restore.

- [ ] T011 [US2] **W3 (peer-spontaneous at N>1, non-regression)** in `tests/session/test_reset_seqnum_policy_matrix.cpp`: `reset_on_logon=false`, fixpp Logon pre-seeded at seq `N>1`, fixpp sends no `141=Y`, peer sends `141=Y` under a tolerant policy ⇒ outbound UNCHANGED from baseline + `by_peer_request=true`. [FR-005, SC-004, W3]
- [ ] T012 [US2] **W7 (fresh, no knob, peer-spontaneous at seq 1)** in `tests/session/test_reset_seqnum_policy_matrix.cpp`: fresh initiator, NO reset knob, `bilateral_lenient`, default `{1,1}`, fixpp `Logon(34=1)` with no `141=Y`; peer spontaneously sends `Logon(34=1, 141=Y)` ⇒ outbound stays **`1`** + `by_peer_request=true`. RED under a `reset_before_send`-alone gate (would restore to 2 → too-high); GREEN under `latch && reset_before_send` (latch false). [FR-005, W7 — the load-bearing latch discriminator]
- [ ] T013 [P] [US2] **W4 (bilateral_strict-at-N, must NOT flip)** confirm `BilateralStrict_Initiator_CountersResetToOne` (`tests/session/test_reset_seqnum_policy_matrix.cpp:574`) stays GREEN UNCHANGED on the COUNTER side: Logon pre-seeded at seq 10 → `reset_before_send` false → no restore → outbound stays `1`. The discriminating proof the restore gate keys on the seq-1 conjunct, not the latch alone. **W4b (the LABEL-side of the C4/C1 divergence — the headline design property; advisor-found gap):** EXTEND this test (or add a sibling in the same file) to also assert `recent_events()` carries `sequence_numbers_reset{by_peer_request=false}` on this exact row. This is the UNIQUE row where `latch=true` AND `reset_before_send=false`, so it is the ONLY positive witness that C4 keys on the latch ALONE (not `latch && reset_before_send`). Without it, an implementer who collapses `we_initiated := latch && reset_before_send` passes every other witness (W2/W3/W7/W8 don't discriminate — latch and `latch&&reset_before_send` agree on all of them). Pre-fix `we_initiated=(policy==bilateral_strict)` already yields `false` here, so this assertion is GREEN-on-main on the label too; it discriminates ONLY via the T010a(c) mutation. [SC-005, SC-006, FR-006, W4/W4b, contract C4]
- [ ] T014 [P] [US2] **W5/W6 (persistent fatal + INV-H1)** in `tests/session/test_persistent_seqnum_hydrate.cpp`: W5 — persistent-store reset-failure on this arm ⇒ fatal-when-persistent (the `030` disposition) unchanged; W6 — persistent-store success ⇒ after the restore `store_outbound == manager_outbound` and never `store_outbound > manager_outbound`. **Seed the store to a sentinel `N≫asserted`** so the failure/INV assertions can falsify (not a trivial-seed tautology — [[feedback_witness_asserts_named_postcondition_not_proxy]]). [FR-007, W5/W6, INV-H1]
- [ ] T015 [US2] **030/029 initiator-arm non-regression sweep** (blast radius — Codex P2#6 + Opus New-P2): confirm GREEN UNCHANGED under a CLEAN-build FULL ctest — `tests/session/test_reset_on_lifecycle.cpp` `Initiator_Received141Ack_NextInboundTwo_NoResend` + `Initiator_Received141Ack_GuardSkipsWhenNoConsumedReset`, `tests/session/test_persistent_seqnum_hydrate.cpp` `INV_H1_Initiator_PeerAck141_NoOverPersist`, and `tests/session/test_refresh_on_logon.cpp` W6 (the store-interaction-count pin). [SC-005, blast radius]

## Phase 4: Polish & Cross-Cutting

- [ ] T016 Run the full `persistent_seqnum_hydrate` + `reset_seqnum_policy_matrix` + `reset_on_lifecycle` + `refresh_on_logon` + session/recovery/024/029/030 regression suites on debug; confirm SC-005 (100% existing witnesses green; default knob-off + acceptor + peer-spontaneous byte-identical). [SC-005]
- [ ] T017 [P] §VI delta — `spec/feature-catalogue.md`: amend the **S-017** row Notes (initiator reset-echo: outbound restored to 2 because the reset Logon consumed seq 1; `by_peer_request` reflects fixpp-initiated; acceptor/knob-off/peer-spontaneous/bilateral_strict-at-N unchanged) AND the **S-032** row (ResetSeqNumFlag(141) ack outbound semantics — Codex P2#5). No new S-row.
- [ ] T018 [P] §VI delta — `spec/coverage-index.md`: map the guarded outbound-restore branch + the widened latch-based `we_initiated`/`by_peer_request` ↔ the W1/W2/W7/W8 + persistent (W5/W6) witnesses under the existing 024 entry; update the **§4.4.2** coverage row (Codex P2#5). [SC-006]
- [ ] T019 [P] §VI delta — `spec/behaviors-and-limitations.md`: flip **L-024-2** to RESOLVED and add **B-032-1** (initiator reset-echo: a peer's `141=Y` echo of fixpp's own deliberate reset restores outbound to `2`, no duplicate-seq frame; reference-engine-conformant; covers all reset knobs via the emit-time latch).
- [ ] T020 **Obsolete-prose grep-sweep** (one exhaustive pass — [[feedback_verify_caught_design_pivot_stale_doc_bundle_drift]]): grep for any `024`/`030` comment/doc framing the initiator reset arm as resetting outbound to `1`, or the `we_initiated = (policy == bilateral_strict)` comment (`session.cpp:3220-3225`), or the `030` "no reply Logon on this arm" region (`:3202-3209`) that omits the outbound twin. Needles: `next_outbound`/`outbound` near the initiator arm, `peer_ack_sent_reset_flag`, `we_initiated`, the `BilateralStrict_Initiator_CountersResetToOne` rationale comment, L-024-2 prose. Amend each to the latch + outbound-restore semantics.
- [ ] T021 Live interop close-out (SC-003) — re-run the `RL-{QFcpp,QFj}-init-fix44-reset-on-logon` cells in `tests/interop/happy/hp_fix44_reset_on_logon_test.cpp` (filename CONFIRMED; the `RL-*-init` cell is at `:152`) vs **both** QuickFIX-cpp v1.16.0 and QuickFIX-J 3.0.1: a `reset_on_logon` initiator establishes, carries one post-logon frame at `34=2`, and the peer does **not** Logout-reject ("MsgSeqNum too low"). **Harden the witness past `drive_to_active`** (stay-Active / `recent_events` discriminator, per CP8). The `deferred:initiator-141echo-outbound-rebase` → pass status flip is **parent-side** (the `phase-9-harness` cell manifest, NOT the submodule — do not search the submodule for the tag). Skip-guard for CI without a counterparty. [SC-003]
- [ ] T022 `/speckit-verify` 6-preset matrix (debug + ASan/UBSan/TSan + coverage + gcc-release, ONE AT A TIME per the WSL2 cap, `--clean-first` each): ≥95/85 branch coverage on the new guarded restore branch — the `latch && reset_before_send` TRUE arm AND both FALSE sub-arms (`reset_before_send` false = bilateral_strict-at-N; latch false = fresh/hydrated peer-spontaneous) — + the latch-based `by_peer_request`. Confirm XV.9 (no new awaitable-header include) via an UNFILTERED Tier-1. **FR-009 check:** no new public C-ABI symbol / error slot / codegen delta (abidiff clean; the new methods are private/internal source-rebuild only). [SC, IX.1/IX.2/XV.9, FR-009]
- [ ] T023 **Feature-completeness audit** (/gate-b precondition — [[feedback_feature_completeness_gate]]): assert against the merged tree (i) every tasks.md row `[X]` or waived; (ii) every FR-001..009 + SC-001..006 ↦ a landed test AND landed impl; (iii) S-017 + S-032 rows amended with matching coverage-index entries; L-024-2 RESOLVED. Record 100% (or waivers) in the verify decision record.

## Dependencies & Execution Order

- **Phase 1 (Setup/RED-verify)** blocks everything (W1 + W2 RED-on-main gate the fix's validity).
- **Phase 2 (US1)**: RED tests T002–T005 → implementation T006–T010. Order within impl: T006 (member) → T007 (latch set at emit) and T008 (set_next_outbound) and T009 (persist_outbound_advance_) → T010 (the arm wires them). US1 is the MVP (the live-found defect).
- **Phase 3 (US2)** is non-regression + the two latch discriminators (T012 W7 = latch-false-at-seq-1; T013 W4 = reset_before_send-false). T011–T015 must be GREEN after the fix.
- **Phase 4 (Polish)**: T016 (regression) → T017–T019 (docs, parallel) → T020 (grep-sweep) → T021 (live) → T022 (verify matrix) → T023 (completeness). T022+T023 gate Gate B.

## Parallel Opportunities

- T003/T004/T005 (separate new TESTs — sequence file writes, logically parallel).
- T013/T014 (separate non-regression confirmations).
- T017/T018/T019 (separate doc files) run in parallel.

## Implementation Strategy

MVP = Phase 2 (US1): the latch + the guarded outbound restore + the new private primitives (~15–20 effective LoC: one member, one unconditional assign at emit, one `set_next_outbound`, one `persist_outbound_advance_`, one guarded restore + the `we_initiated` swap). The bulk of the work is the discriminating witnesses (W7 latch-false-at-seq-1, W8 hydrated counterexample, T005 cross-reconnect mutation) and the doc/grep-sweep. Mechanism A only (skip-reset B dropped as unsound for fresh bilateral_strict).
