# Requirements-Quality Checklist: Conformance — 032 initiator reset-echo outbound restore

**Purpose**: Unit-test the *requirements writing* of the initiator reset_on_logon outbound-restore fix (L-024-2) before `/speckit-implement` — completeness, clarity, consistency, measurability, coverage of the FRs/SCs (NOT implementation verification).
**Created**: 2026-06-11
**Focus**: conformance-correctness (initiator `peer_ack_sent_reset_flag` arm; latch the emit-time fact; two distinct gates). Depth: standard. Audience: reviewer (pre-implement gate).
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md)

## Requirement Completeness

- [x] CHK001 Is the outbound-restore trigger specified as the **conjunction** (`restore_outbound := latch && reset_before_send`) rather than either conjunct alone? [Completeness, Spec §FR-001/§FR-005, research R3] — PASS: FR-001 specifies "BOTH conjuncts" (a)+(b); FR-005 repeats the conjunction explicitly; plan Technical Approach states `restore_outbound := own_logon_sent_reset_flag_ && reset_before_send`; data-model.md, contracts C1, and research R3 all state the same conjunction. No artifact states either conjunct alone as the live gate.
- [x] CHK002 Is the latch defined as the **emit-time fact** (`own_logon_sent_reset_flag_ := initr_reset_seqnum`, carrying the `seqnums_at_one` inbound-at-1 conjunct), and is reconstruction-from-config+counter explicitly rejected? [Completeness, research R3 / Plan RC1] — PASS: FR-006 defines the latch as "the literal emit-time fact … NOT reconstructed from config flags plus a counter sample (which drops the inbound-at-1 conjunct)"; research R3, plan RC1, data-model.md, and contract Inputs all name `bilateral_strict || (any_reset_knob && reset_before_send)` as the rejected alternative. Line anchor `:721` verified in source: `initr_reset_seqnum = (bilateral_strict) || (any_reset_knob && seqnums_at_one)` where `seqnums_at_one` requires BOTH counters at 1 (lines 719-720). Reconstruction appears only as a labeled-rejected alternative across all artifacts.
- [x] CHK003 Is the `by_peer_request` event-label gate specified as **latch-alone** and explicitly DISTINCT from the restore gate (they diverge on `bilateral_strict`-at-N)? [Completeness, Spec §FR-006, contracts C4] — PASS: FR-006 states the label gates on the emit-time fact (latch) alone while the counter correction additionally requires the seq-1 conjunct; spec Clarifications Q2 states "two DISTINCT gates that diverge on the `bilateral_strict`-at-N row"; contract C4 states "C4 (latch only) and C1 (`latch && reset_before_send`) are DISTINCT gates"; data-model.md and research R3 carry the same divergence note. Distinct-gate invariant is consistently stated across all five artifacts.
- [x] CHK004 Is the covered set of reset paths stated (all of `reset_on_logon`/`reset_on_logout`/`reset_on_disconnect` via the latch), with `bilateral_strict`-at-`N>1` explicitly EXCLUDED? [Completeness, Spec §Clarifications Q1, research R3] — PASS: Clarifications Q1 answer states "All reset-before-send paths … covers `reset_on_logon`, `reset_on_logout`, and `reset_on_disconnect`"; FR-001 lists all three knobs explicitly; FR-005 states `bilateral_strict` is excluded because "its Logon may go at seq N>1" making conjunct (b) false; research R3 partition table row 4 shows `bilateral_strict`-at-N with `restore_outbound=false`; data-model.md transition table row 4 confirms. Exclusion of `bilateral_strict`-at-N>1 is explicit in spec, research, and data-model.
- [x] CHK005 Are the two new primitives (`SeqnumManager::set_next_outbound`, `Session::persist_outbound_advance_`) specified WITH their failure disposition (fatal-when-persistent, the 030 discipline)? [Completeness, Spec §FR-007, contracts C3] — PASS: FR-007 states "The fault disposition … MUST follow the existing `030` fatal-when-persistent contract"; contract C3 states the reset-failure disposition is "`030`'s `store_is_persistent_ ? fatal : logged` — unchanged"; plan RC2/Technical Context and research R4 both specify fatal-when-persistent for the new primitives. Note: `set_next_outbound` = **public** method on the internal `SeqnumManager` (mirrors public `set_next_inbound`); `persist_outbound_advance_` = private Session method; FR-009 (C-ABI) holds. Realizability sub-check: new entities are methods, not value-typed structs — the incomplete-type/defaulted-dtor hazard does not apply. Mirror symbols confirmed present (`set_next_inbound` at seqnum_manager.hpp:132; `persist_inbound_advance_` at session.hpp:652); new symbols confirmed absent (`set_next_outbound`, `persist_outbound_advance_`, `own_logon_sent_reset_flag_` — grep clean). Tasked by T008/T009. Clean.
- [x] CHK006 Is the 030 **inbound** restore on this same arm stated to be PRESERVED (next-inbound==2), not altered? [Completeness, Spec §FR-004] — PASS: FR-004 states "MUST preserve the `030` inbound restore on this same arm — next-inbound MUST be `2`"; contract C2 states "UNCHANGED — `030` already restores it to `2` on this arm"; data-model.md step table shows the 030 inbound restore step preceding the new 032 outbound restore step, unchanged; spec Assumptions para 2 states this feature "adds the outbound twin without altering them." The 030 inbound restore preservation is unambiguous across all artifacts.
- [x] CHK007 Is the §VI delta enumerated (amend **S-017** AND **S-032**, coverage-index **§4.4.2**, flip **L-024-2**→RESOLVED, add **B-032-1**) rather than left "TBD at polish"? [Completeness, Plan §VI delta, Tasks T017–T019] — DD-DECIDED §VI (Polish timing per 030/031 precedent): The §VI delta is fully enumerated in plan.md "Exact §VI delta (applied at Polish)" with all five items named (S-017 row amendment, S-032 row amendment, coverage-index §4.4.2 update, L-024-2 flip to RESOLVED, B-032-1 addition) with explicit amendment instructions. Tasked by T017 (S-017+S-032), T018 (coverage-index §4.4.2), T019 (L-024-2+B-032-1). Polish-timing deferral follows the 030/031 precedent per plan §VI and constitution §VI. The delta is enumerated (not "TBD") — the CHK criterion is satisfied; timing follows the frozen-authority §VI rule.
- [x] CHK008 Is the blast-radius pin set enumerated (the committed `DISABLED_` harm test that flips to enabled; the 030 `Initiator_Received141Ack_*` pins in `test_reset_on_lifecycle.cpp`; the 029 `INV_H1_Initiator_PeerAck141_NoOverPersist`; the `refresh_on_logon` W6) for non-regression? [Completeness, research R4, Tasks T015] — PASS: research R4 "Blast-radius hypothesis" enumerates all four test locations with specific names and line refs (`DISABLED_` harm test at `:2158`, `INV_H1_Initiator_PeerAck141_NoOverPersist` at `:2096`, `Initiator_Received141Ack_NextInboundTwo_NoResend` at `:1906`, `…_PersistentStore_StoreEqualsManagerTwo` at `:1952`, `…_ResetFailure_Disconnects` at `:1985`, `…_GuardSkipsWhenNoConsumedReset` at `:2036`, W6 at test_refresh_on_logon.cpp `:1196`); plan Gate A bullet 3 repeats the same enumeration; tasks T015 references all four files by name. The set is enumerated, not vague.

## Requirement Clarity

- [x] CHK009 Is "fixpp initiated the reset" defined unambiguously as the **latched** emit-time `141=Y`-emit fact, such that an implementer cannot satisfy it by reconstructing `bilateral_strict || (any_reset_knob && reset_before_send)` at the ack arm? [Clarity, Plan RC1, research R3] — PASS: FR-006 explicitly states "the literal emit-time fact, not an enumeration of which knob/policy was set" and "rather than reconstruct it on the ack arm from config flags plus a counter sample (a reconstruction that drops the inbound-at-1 conjunct …)"; plan RC1 section explains the mislabeling failure mode of the reconstruction with a specific hydrated counterexample; research R3 repeats the counterexample with the full partition table. The reconstruction appears only as a labeled-rejected alternative in spec, plan, research, and data-model — never as the live gate. An implementer reading any artifact cannot mistake the reconstruction for the live design.
- [x] CHK010 Is the latch **lifecycle** specified precisely enough to prevent a stale value — unconditionally ASSIGNED on every initiator-Logon emit (not conditionally set-only-when-true) AND snapshot-to-local + cleared BEFORE the first `co_await` on the ack arm? [Clarity, contracts Inputs, Tasks T007/T010] — PASS: contract Inputs section states "`own_logon_sent_reset_flag_` … **Unconditionally assigned** on every initiator-Logon emit … which OVERWRITES it to `false` when fixpp does not emit `141=Y` — NOT conditionally set-only-when-true … This unconditional assign on every emit is what makes a stale latch structurally impossible across reconnects"; tasks T007 tasks the unconditional assign explicitly ("overwrites to `false` when fixpp does not emit `141=Y`"); tasks T010 tasks "snapshot + clear the latch before the first `co_await`" with the exact local-variable capture pattern; research R5 latch-lifecycle section specifies the multi-connection mutation requirement (T005). Lifecycle is specified precisely at two points (emit + arm entry) with explicit stale-prevention rationale.
- [x] CHK011 Is **Mechanism A** (restore-after-reset) stated as the single chosen mechanism with skip-reset Mechanism B explicitly DROPPED (not an open Gate-A axis)? [Clarity, Plan Summary, research R4] — PASS: plan Gate A "Resolved at Gate A (round 1)" item 1 states "Mechanism — RESOLVED to A (restore-after-reset); B DROPPED" with the specific unsoundness proof (fresh `bilateral_strict`-at-`{1,1}`); research R4 "Why Mechanism B is dropped" section gives the same proof; spec Assumptions last para states "The mechanism is resolved at Gate A to restore-outbound-after-reset (Mechanism A … ); the skip-the-redundant-reset alternative (Mechanism B) is **dropped**"; contract "Mechanism" section states "A (the design)" and "B (DROPPED)". Mechanism B is not left open anywhere.
- [x] CHK012 Is the `reset_before_send` conjunct's load-bearing role clear — i.e. that the latch ALONE would wrongly restore `bilateral_strict`-at-N? [Clarity, Spec §FR-005, research R3] — PASS: FR-005 states "The fix MUST suppress the outbound rebase **only** when BOTH conjuncts … hold" and explicitly names `bilateral_strict` as a path where conjunct (b) is false — "its Logon was emitted at a pre-reset sequence N>1, so conjunct (b) is false (post-reset next-outbound of `1` is correct there)"; plan RC1 section states "The latch being **false** excludes both fresh peer-spontaneous-at-seq-1 (W7) and hydrated inbound≠1 (W8). The label diverges from the restore exactly on `bilateral_strict`-at-N"; research R3 makes the same argument with the partition table; data-model.md states "`reset_before_send` is load-bearing in `restore_outbound` (necessary but not sufficient): the latch alone is true for `bilateral_strict`-at-N … whose Logon went at seq 10 — restoring there regresses `BilateralStrict_Initiator_CountersResetToOne` (W4)." The load-bearing role of `reset_before_send` is clear and named in all artifacts.
- [x] CHK013 Is FR-009 ("no new wire/error/codegen/C-ABI/config/PUBLIC surface") reconciled with the new primitives (header change + source rebuild, not a public-ABI change)? [Clarity, Spec §FR-009, Plan Constitution X] — PASS: FR-009 states "no new wire field, error code/slot, generated-code change, configuration surface, or **C-ABI** surface; it is a pure correction of an existing internal sequence-counter computation"; plan Constitution Check article X row (corrected) states "no C-ABI/error-slot/wire/public change. Adds `SeqnumManager::set_next_outbound` (**public** method on the internal `SeqnumManager`, mirroring public `set_next_inbound`) + `Session::persist_outbound_advance_` (private Session method) + one private `Session` member — a header change → source rebuild; no new C-ABI surface (FR-009 holds: `SeqnumManager` is awaitable-returning C++, not C-ABI-exportable; no external caller)"; research R4 (corrected) states the same split. Accurate characterization: `set_next_outbound` = **public** method on the internal `SeqnumManager` (mirrors public `set_next_inbound`); `persist_outbound_advance_` = private Session method; FR-009 (C-ABI) holds. The reconciliation between FR-009 and the header additions is explicit and consistent. Realizability: same as CHK005 — methods, not value-typed struct members, so no incomplete-type hazard.

## Requirement Consistency

- [x] CHK014 Do spec / plan / research / data-model / contract agree on the conjunction gate, with the rejected reconstruction appearing ONLY as a labeled alternative (never as the live gate)? [Consistency, Conflict-check] — PASS: All five artifacts state the conjunction `own_logon_sent_reset_flag_ && reset_before_send` as the live gate. Verified per-artifact: spec FR-001/FR-005/FR-006; plan Technical Approach + RC1; research R3 partition table heading; data-model.md "The gates" section; contract C1 + Inputs. The rejected reconstruction `bilateral_strict || (any_reset_knob && reset_before_send)` appears in plan RC1 ("Why latch, not reconstruct"), research R3, data-model.md "Why latch, not reconstruct" — always labeled "rejected" / "dropped". No artifact states the reconstruction as the live gate. Cross-artifact consistency confirmed.
- [x] CHK015 Is terminology consistent across all artifacts (`own_logon_sent_reset_flag_`, `reset_before_send`, `restore_outbound`, `we_initiated`, `n_pre_outbound`, `seqnums_at_one`)? [Consistency] — PASS: all six named terms appear consistently across artifacts — spec, plan, research R3, data-model.md, and contract Inputs all use exactly these identifiers for the same concepts. `we_initiated` consistently refers to the per-arm local read of the latch (post-fix meaning), while the current-code equivalent is distinguished as the `(policy == bilateral_strict)` reconstruction being replaced. `seqnums_at_one` is always defined as the BOTH-counters-at-1 predicate at `:719-720`. No alias conflicts found across the bundle.
- [x] CHK016 Does spec `## Clarifications` Q2 agree with the resolved two-distinct-gates design (post the sign-off P3 fix), rather than still claiming "one coherent predicate"? [Consistency, Spec §Clarifications] — PASS: spec.md Clarifications Q2 answer (lines ~22-23) states "The event label (`by_peer_request`) keys off the **latched emit-time fact** `own_logon_sent_reset_flag_` ALONE, while the outbound restore additionally requires the seq-1 observable (`reset_before_send`) — i.e. two DISTINCT gates that diverge on the `bilateral_strict`-at-N row (latch true → `by_peer_request=false`, but the Logon went at seq N>1 so restore does NOT fire)." Q2 states the two-distinct-gates design explicitly; the phrase "one coherent predicate" does not appear in Q2. The sign-off P3 fix was applied — Q2 is consistent with the resolved design.
- [x] CHK017 Do the research R3 partition table (7 rows), the data-model transition tables, and contract C1/C4 agree on the disposition of every row — especially the two latch-false rows (hydrated `{37,1}` and fresh-no-knob)? [Consistency] — PASS: research R3 table has 7 rows (reset_on_logon; reset_on_logout/disconnect at seq-1; fresh bilateral_strict {1,1}; bilateral_strict pre-seeded N=10; peer-spontaneous at N>1; hydrated {in=37,out=1}+reset_on_logout non-strict; fresh no-knob lenient); data-model.md transition table has the same 7 rows with identical column values; contract C1 names the four "else" sub-cases explicitly (bilateral_strict-at-N, peer-spontaneous-at-N, fresh peer-spontaneous-at-seq-1, hydrated reset_on_logout-with-inbound≠1-at-emit); contract C4 carries the same latch-alone condition. The two latch-false rows (hydrated `{37,1}` and fresh-no-knob) both show `latch=false`, `restore_outbound=false`, `by_peer_request=true` in research R3, data-model, and contract C1 "Else" clause. Disposition is consistent across all three sources.

## Acceptance Criteria Quality & Measurability

- [x] CHK018 Is SC-002 measurable — does W1 require originating a post-Active frame and asserting **wire `34=2`** (no second `34=1`), rather than only the in-memory counter? [Measurability, Spec §SC-002, Tasks T002] — PASS: SC-002 explicitly states "verified by an automated test that, after the established transition, **originates a post-Active outbound frame through the unit harness, captures the emitted bytes, and asserts wire `34=2` with no second frame at `34=1`**" and further states "The committed counter-only harm witness … is a supporting check, not the wire proxy" with a citation to [[feedback_witness_asserts_named_postcondition_not_proxy]]; tasks T002 repeats "Add the wire witness (SC-002): … originate a post-Active outbound frame through the unit harness, capture the emitted bytes, and assert it carries `34=2` with **no** second frame at `34=1`." SC-002 is measurable at the wire level, not a counter proxy.
- [x] CHK019 Are the latch discriminators (W7 fresh-at-seq-1, W8 hydrated) paired with a **mutation-confirmation** requirement, given both are GREEN-on-main and otherwise non-discriminating? [Measurability, Tasks T004/T010a/T012, research R5] — PASS: tasks T010a explicitly tasks the mutation-confirmation step ("After T010 makes W1/W2 green, run two temporary source mutations … then REVERT: (a) drop the `own_logon_sent_reset_flag &&` conjunct … → W7 MUST go RED; (b) substitute the rejected reconstruction … → W8 MUST go RED"); plan §VII states "W7/W8 are GREEN-on-main AND green-post-fix → discriminating power confirmed by a MUTATION step at implement"; research R5 states the same and calls T010a the "only proof the RC1 latch is not inert". T010a is present in tasks.md and is explicitly required — the gap the audit brief warned about is absent.
- [x] CHK020 Is the persistent INV-H1 witness (W6) discriminating — not a trivial-seed tautology? [Measurability, Spec §FR-007, Tasks T014] — PASS: W6 seeds `{in=1, out=1}`; the arm's `store.reset()` drives `durable_outbound` to the post-reset base 1, then `persist_outbound_advance_()` advances it to 2 — the 1-vs-2 gap is the falsifier (if persist does not fire, durable stays 1 ≠ asserted 2 → FAIL). An external sentinel would be wiped by `store.reset()`; the post-reset base IS the discriminating seed. Not a trivial-seed tautology. The implemented W6 (`test_persistent_seqnum_hydrate.cpp:2600`) uses this 1-vs-2 post-reset-base discriminator, not an external sentinel.
- [x] CHK021 Are the in-sync vs the non-regression outcomes each stated as objectively checkable counter/frame/event values (outbound==2 + `by_peer_request=false` vs outbound==baseline + `by_peer_request=true`)? [Measurability, Spec §SC-001/§SC-004/§SC-006] — PASS: SC-001 states "next-outbound is `2`" + "reaches the established state" (Active + `peek_outbound()==2` + inbound==2 assertions); SC-002 states "wire `34=2` with no second frame at `34=1`"; SC-004 states "outbound counter is unchanged from the shipped baseline … verified by an automated test asserting the reference-pinned value"; SC-006 states `by_peer_request=false` on fixpp-initiated paths and `true` on peer-initiated paths, "verified by an automated test." The two outcomes are each stated as concrete counter/frame/event values, not vague qualitative descriptions.

## Scenario & Edge-Case Coverage

- [x] CHK022 Is the hydrated `{inbound≠1, outbound=1}` + `reset_on_logout` peer-spontaneous-`141=Y` case (latch false ⇒ no restore, `by_peer_request=true`) covered as a requirement? [Coverage, research R3, Tasks T004] — PASS: FR-005 names this case explicitly: "the path where fixpp requested a reset (`bilateral_strict`) but its Logon was emitted at a pre-reset sequence N>1" and, more specifically for the hydrated case via the reconstruction rejection in FR-006 ("a reconstruction that drops the inbound-at-1 conjunct of the real emit predicate and would mislabel a hydrated initiator"); spec §Edge Cases does not name the hydrated row explicitly, but FR-005(i) and FR-006 both require the hydrated non-restore disposition; tasks T004 explicitly tasks W8 for this exact case ("hydrated initiator seeded `{next_inbound=37, next_outbound=1}`, `reset_on_logout=true` … ⇒ outbound stays at its baseline"); research R3 has a named row for it. The case is covered as a requirement via FR-005/FR-006 + T004/W8.
- [x] CHK023 Is the fresh-no-knob `bilateral_lenient` peer-spontaneous-`141=Y`-at-seq-1 case (latch false ⇒ outbound stays 1) covered? [Coverage, Edge Case, Tasks T012/W7] — PASS: FR-005(i) covers "the peer-spontaneously-initiates path (fixpp sent no `141=Y`, so conjunct (a) is false — **including a fresh non-resetting initiator whose own `Logon(34=1)` predates a peer-declared reset epoch, where outbound MUST stay `1`**)"; tasks T012 tasks W7 explicitly ("fresh initiator, NO reset knob, `bilateral_lenient`, default `{1,1}`, fixpp `Logon(34=1)` with no `141=Y`; peer spontaneously sends `Logon(34=1, 141=Y)` ⇒ outbound stays **`1`**"); contract "Negative" section names "Fresh, no-knob, lenient/unilateral initiator … outbound MUST stay `1` (latch false) … (W7)." The case is explicitly required as a non-regression scenario.
- [x] CHK024 Is the cross-reconnect stale-latch case (reused Session: conn-1 reset-logon, conn-2 non-reset) covered to guard the latch lifecycle? [Coverage, Tasks T005] — PASS: tasks T005 tasks a "Latch-lifecycle cross-reconnect mutation witness" — "a `Session` reused across reconnect where connection 1 is a reset-logon (latch true) and connection 2's Logon is NOT a reset (latch must read false); feed connection 2 a peer-spontaneous `141=Y` ⇒ outbound is NOT restored. Goes RED under a conditional-set-plus-clear latch mutation, GREEN under the unconditional-assign-every-emit design (T007)"; research R5 "Latch-lifecycle mutation confirmation" section requires the same multi-connection mutation check. The stale-latch case is covered as a tasked test requirement.
- [x] CHK025 Are the out-of-scope paths explicitly stated as byte-identical (acceptor role, knob-off default, peer-spontaneous, `bilateral_strict`-at-N)? [Coverage, Spec §FR-008/§Edge Cases] — PASS: FR-008 states "The fix MUST NOT change the observable behavior of the **acceptor** role, the **knob-off** default path, or any path that does not enter the initiator `peer_ack_sent_reset_flag` arm — these MUST be byte-identical to the pre-fix baseline"; spec §Edge Cases names "Acceptor role: unchanged"; spec Assumptions names "acceptor role, knob-off default, and `bilateral_strict`-at-N paths **byte-identical**"; contract C6 states "Acceptor role, knob-off default, and any path not entering this arm: **byte-identical** to baseline"; SC-005 requires "the default (knob-off) path and the acceptor-role paths are byte-identical to the pre-fix baseline." All four named out-of-scope paths are explicitly stated as byte-identical in spec, contract, and success criteria.
- [x] CHK026 Is the live interop close-out (SC-003: `RL-*-init` cells vs QFcpp/QFJ, witness hardened past `drive_to_active`) specified with a concrete establish + no-Logout-reject criterion? [Coverage, Spec §SC-003, Tasks T021] — PASS: SC-003 states "The live `RL-*-init` interop cells establish and the counterparty does **not** reject with a 'MsgSeqNum too low' Logout — verified by the live interop harness wire transcript"; tasks T021 states "a `reset_on_logon` initiator establishes, carries one post-logon frame at `34=2`, and the peer does **not** Logout-reject ('MsgSeqNum too low'). **Harden the witness past `drive_to_active`** (stay-Active / `recent_events` discriminator, per CP8)"; quickstart.md names both cell IDs (`RL-QFcpp-init-fix44-reset-on-logon` and `RL-QFj-init-fix44-reset-on-logon`) and the concrete rejection text to watch for. The close-out criterion is concrete (establish + carry one post-logon frame at 34=2 + no Logout-reject).

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 25 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 1 |
| WAIVED | 0 |
| **Total** | **26** |

### SPEC-FIXED items

None.

### DD-DECIDED items

- CHK007 — anchor `[§VI / Plan "Exact §VI delta (applied at Polish)" / Tasks T017–T019]`; rationale: §VI delta is fully enumerated (not "TBD") with explicit amendment instructions; Polish-timing deferral follows the frozen-authority 030/031 precedent per plan §VI and constitution §VI.

### WAIVED items

None.

Anchors spot-verified: `session.cpp:721` (`initr_reset_seqnum`), `session.cpp:719-720` (`seqnums_at_one`), `session.cpp:3185` (`peer_ack_sent_reset_flag` arm), `session.cpp:3224` (`we_initiated`), `seqnum_manager.hpp:132` (`set_next_inbound` — mirror exists), `session.hpp:652` (`persist_inbound_advance_` — mirror exists) — all resolve in the signed-off working tree. No Phase-2 `2X` design-doc anchor (Phase-4 conformance fix; frozen-authority is the Gate A log in plan.md, verified as Round 1 applied 2026-06-11).
