# Tasks: async_mutex hardening (Cluster-4)

**Feature**: `058-async-mutex-hardening` | **Design**: [plan.md](./plan.md) · [research.md](./research.md) · [data-model.md](./data-model.md) · [contracts/](./contracts/)
**Single production file**: `include/fixpp/core/sync/async_mutex.hpp`. Tests under `tests/sync/`.
**TDD**: witnesses are the deliverable (FR-008) — each behavioral fix has a RED-first, mutation-tested witness.

> **GATE before Phase 3 implementation — coverage-design gate (Fable session #2), runs after `/tasks`,
> before `/implement`** (parked plan): Fable adversarially reviews the test matrix + enumerates the
> `async_mutex.hpp` branch inventory and hammering scenarios, aiming 100% of *reachable* branches
> (SC-004). It may ADD witness tasks to the US phases below. Codex is the independent second reviewer of
> the matrix. Do NOT start `/implement` until this gate runs. Re-asserted at `/speckit-verify` + Gate B.

---

## Phase 1: Setup

- [X] T001 [P] Add a **standalone** `test_async_mutex_aba_interleave` CMake target with `-DFIXPP_ASYNC_MUTEX_TEST_SEAM` in `tests/sync/CMakeLists.txt` — it MUST link no fixpp object/library that instantiates `async_mutex` bodies (ODR: header-only inline bodies differ under the macro; research.md D-7). Evidence: registered directly (not via `add_sync_test`); link line has ZERO fixpp objects/libraries (`libgtest_main.a`/`libgtest.a`/`-lm`/`-lpthread` only, confirmed via `build.ninja`); compile command confirmed carrying `-DFIXPP_ASYNC_MUTEX_TEST_SEAM`; stub `TEST(AsyncMutexAbaInterleave, Placeholder)` builds+passes on `linux-clang-debug`.
- [X] T002 [P] Wire the pre-fix mutation-RED seam runs onto the **non-TSan** lane (or document the expected TSan report), since the pre-fix RED execution exercises the very races being fixed (research.md D-7). Evidence: took the **"document the expected TSan report"** branch, not the exclusion branch — the target is registered on EVERY preset (TSan included), since D-7 part 2 (`pop_pre_link_load`, T008) is TSan-RED-only (its sole discriminating signal); a blanket TSan exclusion (the `tests/oracle` idiom, first attempt, reverted per advisor review) would leave part 2 non-discriminating everywhere. A block comment above the registration documents that the pre-fix mutation-RED run (T012) is expected to produce a real TSan report on TSan presets (part 2) and an assertion failure with no TSan report elsewhere (part 1, TSan-invisible per D-7). Verified stub builds+passes clean on both `linux-clang-debug` and `linux-clang-tsan` (`ctest -N -R aba` → 1 test on each).

## Phase 2: Foundational — shared pool-layout scaffolding (BLOCKS US1 + US3)

- [X] T003 Restructure `waiter_pool_slot` → `struct { alignas(std::max_align_t) std::byte storage[248]; std::atomic<std::uint32_t> free_link; }` + `static_assert(sizeof(waiter_pool_slot) == 256)` in `include/fixpp/core/sync/async_mutex.hpp` (data-model.md; keeps the 131120 golden). Evidence: `free_link` added as PERSISTENT unused-until-T010 slot metadata; `sizeof(waiter_pool_slot)==256` static_assert compiles; golden GREEN (below).
- [X] T004 Retype `waiter_pool_free_` → `std::atomic<std::uint64_t>`; add `{gen:54, slot_index:10}` pack/unpack helpers + the `0x3FF` empty-list sentinel in `async_mutex.hpp` (research.md D-1). Evidence: `waiter_pool_free_` retyped, initialised to `pack_free_list_head(0, free_list_empty_index)`; `detail::pack_free_list_head`/`unpack_free_list_head`/`free_list_head`/`free_list_empty_index` added as shared `constexpr` helpers used identically by both the pop and the push (T009/T010) so the encoding cannot drift between call sites; `sizeof(std::atomic<uint64_t>)==8` equals the old pointer — layout golden held (`sizeof(async_mutex)==131120`, `alignof==16`, confirmed by `sync_async_mutex_layout_golden` GREEN below).
- [X] T005 Tighten the `waiter_record` size `static_assert` `<= 256` → `<= 248` (`async_mutex.hpp:619`); build `test_async_mutex_layout_golden` to confirm `sizeof(async_mutex)==131120`, `alignof==16` UNCHANGED. Evidence: assert compiles (actual `sizeof(waiter_record)==144`, well under 248); golden test GREEN, `sizeof(async_mutex)=131120 (expected: 131120)`, `alignof(async_mutex)=16 (expected: 16)`.
- [X] T006 Add the compile-gated two-phase `FIXPP_ASYNC_MUTEX_TEST_SEAM` hook (`detail::async_mutex_test_seam(phase)` with phases `pop_pre_link_load`, `pop_pre_cas`) — no-op / absent when the macro is undefined (research.md D-7). Evidence: `enum class async_mutex_seam_phase` + `inline void (*async_mutex_test_seam)(async_mutex_seam_phase) noexcept = nullptr;` (nullable/settable pointer, matching D-7's `if (detail::async_mutex_test_seam)` guard idiom — a plain function name would always be truthy) guarded by `#ifdef FIXPP_ASYNC_MUTEX_TEST_SEAM`, no call sites wired in; `test_async_mutex_aba_interleave` stub still builds+passes; standalone macro-defined TU compile + assign-and-invoke smoke check confirms the guarded block itself actually compiles and is usable (not just that the ambient no-macro build ignores it).

## Phase 3: User Story 1 (P1) — cross-thread waiter-pool integrity / AM-P1

**Goal**: the free-list is ABA-safe and reuse-race-free under supported cross-thread contention.
**Independent test**: `test_async_mutex_aba_interleave` RED against pre-fix, GREEN after; mutation-verified.

- [X] T007 [US1] RED witness — `tests/sync/test_async_mutex_aba_interleave.cpp` **part 1** (`pop_pre_cas`): pin T1 between free-link load and CAS while T2 pop-pop-pushes the same head; assert no slot returned twice / no parked record clobbered / exact completion count. Confirm RED vs current tagless code. Evidence: `PopPreCasAbaCorruptionDetected` — T1 blocks (binary_semaphore) at `pop_pre_cas` with stale `(free_head=P, next=Q)` cached; T2 (single-threaded, driven only by explicit `ioc.poll()` pumps — no background thread, so nothing races the release) pops P, pops Q, cancels the P-waiter, drains (reclaims P with an EMPTY next since Q is still outstanding). T1 released; a probe posted onto `ioc_a` after release gives a hard (non-timing) confirmation T1's synchronous CAS+park has executed, exploiting asio's non-preemptive single-threaded FIFO handler execution. Oracle: 3 post-corruption contended pops under a fresh holder must all resolve (bounded `wait_for`, no hang); pre-fix, the corrupted free list forms a cycle and at most 2/3 resolve. 100/100 debug runs RED at the same `EXPECT_EQ(ready_count, 3)` assertion (`ready_count` 1 or 2, never 3); 10/10 TSan-lane runs likewise RED at the same assertion with NO additional TSan report (part 1's CAS window is TSan-invisible per D-7, confirmed). No hangs/crashes across 100+30 combined runs.
- [X] T008 [US1] RED witness — same file **part 2** (`pop_pre_link_load`): T2 reuses the slot while T1 is pinned before the link load; TSan-RED against the pre-fix plain-`next_` read (run per T002). Evidence: `PopPreLinkLoadReuseRaceIsDataRace` — T1 blocks (binary_semaphore) at `pop_pre_link_load` before reading `free_head->next_`; T2's OWN entry into `pop_pre_link_load` (any non-T1 thread, identified by tid) is the release trigger — never T2's write-completion — so T1's subsequent read and T2's placement-new write (`::new (record) waiter_record{}`, `async_mutex.hpp:878`) race with no happens-before edge. 20/20 TSan-lane runs produced the exact same TSan report: `Read of size 8 ... async_mutex.hpp:847:45` (T1's `next_` read) racing `Previous write of size 8 ... async_mutex.hpp:878:41` (T2's placement-new). 30/30 debug-lane runs (both tests together) clean/expected.
- [X] T009 [US1] Implement the generation-tagged **pop** (load head → `waiter_pool_storage_[idx].free_link.load(acquire)` → `CAS(head, pack(gen+1, next_idx))`), replacing `async_mutex.hpp:808-816` (research.md D-1). Evidence: Treiber loop re-unpacks `(gen, idx)` on every CAS-retry; `idx==free_list_empty_index` breaks to the unchanged bump allocator; both seam call-sites (`pop_pre_link_load` before the `free_link.load`, `pop_pre_cas` before the CAS) kept at the structurally-analogous points inside the loop; CAS carries `gen+1` (bump — the ABA defeat) with `next_idx` from the persistent slot `free_link`; success order `acq_rel`, failure `acquire`; returns `std::launder(reinterpret_cast<waiter_record*>(waiter_pool_storage_[idx].storage))` (same pattern as the bump-allocator return).
- [X] T010 [US1] Implement the **push** in `release_ref` (`:666-671`): compute `this_idx` from `(record - storage.data())/sizeof(waiter_pool_slot)`; `free_link.store(head_idx, relaxed)`; `CAS(release/acquire)` carrying the loaded generation. Uses the slot metadata — NOT the destroyed record. Evidence: `this_idx` computed via `reinterpret_cast<waiter_pool_slot*>(raw) - mutex->waiter_pool_storage_.data()` (friend-qualified nested-type name); Treiber loop re-stores `free_link` with the CURRENT head's index on every retry (not hoisted outside the loop); CAS carries the generation UNCHANGED (only the pop bumps it — load-bearing for T012a's discrimination, per D-1 "carries the loaded generation"); success order `release`, failure `acquire`; writes the PERSISTENT slot `free_link`, never the destroyed record — closes the Gate-A-1 lifetime blocker (record is `~waiter_record()`-d at `:713` before this push runs).
- [X] T011 [US1] Verify T007 + T008 GREEN after T009/T010; confirm no plain conflicting access remains on the reuse path (TSan clean). Evidence: `test_async_mutex_aba_interleave` both tests GREEN on `linux-clang-debug`; T007 25/25 GREEN (debug) + 20/20 GREEN (TSan, no report — part 1 is TSan-invisible per D-7, confirmed); T008 20/20 GREEN/clean on TSan; `sync_async_mutex_layout_golden` GREEN (`sizeof(async_mutex)=131120`, `alignof=16`, unchanged); full `linux-clang-debug` rebuild 852/852 objects, `ctest -R '^sync_|^session_|^store_|^alloc_guard_'` → 141/141 passed (0 failed); full unfiltered `ctest` → 524/526 passed, the 2 failures (`interop_cell_results_schema_check`, `decimal_compare_oracle`) are pre-existing Python-environment failures (`No module named pytest`) fully unrelated to `async_mutex.hpp`/this diff.
- [X] T012 [US1] Mutation-check: revert the generation bump → T007 RED; revert the slot-`free_link` (to a member) → T008 RED. Record. Evidence: (a) gen-bump reverted (`pack_free_list_head(unpacked.generation + 1, ...)` → `pack_free_list_head(unpacked.generation, ...)`) at the pop CAS → 10/10 debug runs RED at the SAME assertion as the pre-fix baseline (`test_async_mutex_aba_interleave.cpp:482`, `ready_count==1` vs expected `3`). (b) slot-`free_link` reverted to a plain `waiter_record` member (pop: `free_link.load(acquire)` → plain read of `reinterpret_cast<waiter_record*>(...)->next_`; push: `free_link.store(...)` → plain write to `reinterpret_cast<waiter_record*>(raw)->next_`) → 10/10 TSan runs RED with a real TSan data-race report at `async_mutex.hpp:916:31` (the mutated read) racing the placement-new write, matching T008's pre-fix report shape. Both mutations reverted immediately after capture; header diffed clean (`grep -n "T012a\|T012b MUTATION"` → no output); post-restore re-verify: both witnesses GREEN again on debug + TSan, `ctest -R '^sync_|^session_|^store_|^alloc_guard_'` → 141/141 passed.

## Phase 4: User Story 2 (P1) — safe teardown / AM-P2-1 + AM-P2-2

**Goal**: no write-after-free on drain/destroy; the destructor guard loudly rejects the in-flight-resumer violation shapes.
**Independent test**: the two death tests + the MT drain-then-destroy witness.

- [x] T013 [US2] RED death test — extend `tests/sync/test_destructor_release_death.cpp` with the **cancel-delivered-then-destroy** shape (`in_flight_resumers_ != 0`, runner tail pending); RED vs the current guard. DONE: `SeamDestructorReleaseDeath.CancelDeliveredThenDestroyTerminates` — constructed single-threaded/deterministic (no seam, no sleep): cancel a queued waiter via `cancel_sig.emit()` (synchronously schedules the abort-delivery resume — `in_flight_resumers_ == 1`, posted, never polled), then a direct `mtx->unlock()` unlinks the cancelled node (`state_ -> not_locked`, `next_drain_head_` stays empty). RED confirmed 3/3 runs: child exits cleanly (`Exited with exit status 0`) because neither current OR-term fires — not an unrelated crash the matcher happens to accept.
- [x] T014 [US2] RED death test — the **grant-shaped sibling** (owner unlocks-and-destroys inside the runner, no cancellation; Gate-A: Fable) in the same file. DONE: `SeamDestructorReleaseDeath.GrantDeliveredThenDestroyTerminates` — no cancellation; a queued waiter is granted (`mtx->unlock()` #1: `in_flight_resumers_ == 1`, posted, unrun), then a second direct `mtx->unlock()` simulates the granted waiter's own immediate release (state_ -> not_locked) WITHOUT ever running the posted delivery runner (avoids an in-test UAF vs. a literal nested `delete` inside the real runner, which would leave that runner's tail dereferencing freed memory nondeterministically on this non-ASan lane). RED confirmed 3/3 runs, same clean-exit signature as T013.
- [x] T015 [US2] MT teardown witness — `tests/sync/test_drain_destroy_inflight_mt.cpp` (register via the standard `tests/sync/CMakeLists.txt` sync-test helper): drain-then-destroy with a cross-executor in-flight resumer; assert the teardown observes all outstanding pool writes. DONE: `DrainDestroyInflightMt.CrossExecutorReapThenDestroyIsSafe` + `.RepeatedCrossExecutorReapThenDestroyIsClean` (20 reps). Two genuinely separate `asio::io_context`s, each serviced by its OWN dedicated `std::thread` (`thread_a`=drain-owning strand, `thread_b`=the parked waiter's own executor). Precondition state (holder acquired; waiter genuinely parked on `state_`'s LIFO) is reached via bounded single-threaded `poll_one()` loops BEFORE either background thread starts — deterministic, no sleep, no race in reaching the interesting state (same idiom as `test_destructor_release_death.cpp`). Holder/drain relative ordering on the single drain-owning io_context is governed by well-separated `yield_n()` counts (N*20 vs N*2 — the same "canonical §4.7.4 sequencing" ratio proven by `ProperlyDrainedMutexDoesNotTerminate`), so it stays deterministic even though a background thread (not the calling thread) now services it. The drain reaps the cross-executor waiter and posts its abort-delivery runner onto `thread_b`; that runner pushes the freed slot into the free-list (`release_ref`) and decrements `in_flight_resumers_` (RELEASE, T016) as its LAST statement; the drain's terminal loop observes `in_flight_resumers_==0` (ACQUIRE, T016) and `delete mtx` runs immediately on `thread_a`. Discriminator (advisor review, added post-initial-pass): a `resume_thread_id` atomic captured inside the waiter's post-lock continuation is asserted `== thread_b.get_id()` and `!= thread_a.get_id()` — proves the cross-executor path genuinely engaged (not silently collapsed to same-thread), so the TSan-clean result is non-hollow evidence. 10/10 TSan runs clean (`linux-clang-tsan`), 1/1 debug clean. FRAMING (explicit, not overclaimed): this is a POSITIVE HB-observation witness, not a RED-first mutation test — on this x86_64 (TSO) host the property would hold trivially even under a hypothetical relaxed-ordering regression, so this test is not expected to discriminate a reverted T016 here; the D-2 release/acquire pairing is a TSan-MODELED discriminator whose dedicated RED-on-mutation proof is T045 (deferred, later in this feature) — recorded in the file header comment.
- [x] T016 [US2] Implement release/acquire ordering: `in_flight_resumers_.fetch_sub(1, release)` (`:583`), `load(acquire)` at the drain terminal (`:1195-1196`) and the destructor (research.md D-2). DONE (landed by the prior agent, orchestrator-verified before this round; re-confirmed here): `waiter_record::store_executor`'s `resume_fn_`'s runner does `m->in_flight_resumers_.fetch_sub(1, std::memory_order_release)` (`async_mutex.hpp:672`, LAST statement, after `release_ref`); the drain's quiescence-loop terminal condition reads `in_flight_resumers_.load(std::memory_order_acquire)` (`async_mutex.hpp:1399`); the destructor reads it the same way (`async_mutex.hpp:781`). The sole incrementer (`schedule_resume`, `async_mutex.hpp:848`) stays `relaxed` — correct, since it publishes no other writes the acquire side needs to observe (only the DECREMENT side carries the payload-visibility obligation).
- [x] T017 [US2] Widen the destructor guard (`:641-646`) to `std::terminate()` also on `in_flight_resumers_.load(acquire) != 0` — NOT gated on `active_holders_count_` (research.md D-3). Record the redundancy argument in the commit/doc (Fable coverage-gate H3): a holders term would be redundant with the existing `state_ != not_locked` term — `active_holders_count_ > 0 ⟹ state_ != not_locked` (unlock decrements holders before restoring `state_`; grants increment holders while locked) — so dropping it loses no detection, it is not merely "not a barrier." DONE (landed by the prior agent, orchestrator-verified before this round; re-confirmed here): `~async_mutex` (`async_mutex.hpp:778-784`) is a THREE-term OR (`s != not_locked || next_drain_head_ != nullptr || in_flight_resumers_ != 0`), no `active_holders_count_` term; the redundancy argument is recorded verbatim in the destructor's preceding comment block (`:758-777`, citing Fable coverage-gate H3). T013/T014 (which exercise exactly this term) confirmed GREEN; the mandatory mutation-check (T019 below) confirms this term is surgically load-bearing for exactly T013/T014.
- [x] T018 [US2] Update the now-false strand-local justification comments (`:263-266`, `:566-569`) to the release/acquire pairing; fold the drain/destructor contract-delta (incl. the cross-executor granted-holder EXCLUSION) into the header docs (contracts/async_mutex-contract-delta.md). DONE (landed by the prior agent, orchestrator-verified before this round; verified complete here, no gaps found — this round's only edits to `async_mutex.hpp` were comment-only, confirmed by `git diff` showing zero non-comment delta beyond the already-verified T016/T017 lines): the `cancel_and_drain()` declaration doc (`:222-242`) states the release/acquire HB + the explicit cross-executor granted-holder EXCLUSION verbatim; the `store_executor`/`resume_fn_` runner doc (`:640-648`, `:660-672`) and the drain-terminal doc (`:1389-1395`) both cite T016/D-2 and the contract-delta; the destructor doc (`:181-186`, `:745-777`) cites T017/D-3 + the Fable H3 redundancy argument. `contracts/async_mutex-contract-delta.md` (pre-existing from the prior agent's pass) states the "New guarantee (scoped)" + "Explicit EXCLUSION" verbatim, matching the header comments word-for-word in substance.
- [x] T019 [US2] Verify T013/T014 GREEN (guard terminates both shapes) + T015 shows the HB; verify T003's legit drained-then-destroyed path does NOT terminate; mutation-check. DONE — full evidence:
  1. `sync_destructor_release_death` rebuilt + re-run: 6/6 GREEN (`DestroyWithLiveWaiterTerminates`, `DestroyWhileHeldTerminates`, `CancelDeliveredThenDestroyTerminates` [T013], `GrantDeliveredThenDestroyTerminates` [T014], `GrantWithTailThenDestroyTerminates` [T041], `ProperlyDrainedMutexDoesNotTerminate` [T003's legit path]).
  2. MANDATORY mutation (transient, reverted immediately after capture): commented out ONLY the T017-added `in_flight_resumers_.load(acquire) != 0` OR-term in `~async_mutex` (`async_mutex.hpp:778-784`), rebuilt, re-ran: **exactly** `CancelDeliveredThenDestroyTerminates` (T013) and `GrantDeliveredThenDestroyTerminates` (T014) went RED (`Exited with exit status 0` — clean exit, the guard did not fire) while the OTHER FOUR tests stayed GREEN (they trip on the `state_`/`next_drain_head_` terms, confirming the term is surgically load-bearing for exactly the two shapes it targets, not a redundant/entangled guard). Restored the term verbatim; rebuilt; re-ran: 6/6 GREEN again. `git diff include/fixpp/core/sync/async_mutex.hpp | grep "T019 MUTATION"` → empty (clean restore, zero residue).
  3. `sync_drain_destroy_inflight_mt` (T015) run 10/10 clean under `linux-clang-tsan` + 1/1 clean under `linux-clang-debug`.
  4. US1 regression check: `test_async_mutex_aba_interleave` (standalone, `FIXPP_ASYNC_MUTEX_TEST_SEAM`-gated target) — T007 (`PopPreCasAbaCorruptionDetected`) 2/2 GREEN on `linux-clang-debug`; T008 (`PopPreLinkLoadReuseRaceIsDataRace`) GREEN + TSan-clean on `linux-clang-tsan`. No regression from the T016/T017/T018 header edits.
  5. Full `linux-clang-debug` rebuild (852+ objects, 2 transient `clang-scan-deps` `.ddi.tmp` failures on unrelated TUs [`tc_seqnum_test.cpp`, `test_in_flight_acquirer_coverage.cpp`] resolved by an immediate single-target retry — an environment/scan-deps flake, not code-related) → exit 0. `ctest --test-dir build/linux-clang-debug -R '^sync_'` → **40/40 passed**. `ctest --test-dir build/linux-clang-debug -R 'check_no_std_mutex_corpus$'` (unfiltered corpus gate, run explicitly per `feedback_awaitable_header_mutex_include_edge` — a name-regex `sync_` filter would MISS this test) → PASSED, confirming the doc-only header edits added no new `#include` edge that drags a banned `std::mutex` type into the corpus. `sync_async_mutex_layout_golden` → `sizeof(async_mutex)==131120`, `alignof==16` (both compile-time `static_assert` and runtime `EXPECT_EQ`), unchanged. Full unfiltered `ctest --test-dir build/linux-clang-debug` → see report body for the final pass count (pre-existing unrelated failures, if any, are called out explicitly, not silently absorbed).

## Phase 5: User Story 3 (P2) — bounded exhaustion / AM-P2-3

- [X] T020 [US3] RED witness — `tests/sync/test_pool_exhaustion_reuse.cpp` (standalone target, registered in `tests/sync/CMakeLists.txt` — NOT via `add_sync_test`: the FIXPP_ASYNC_MUTEX_TEST_SEAM-gated `test_seam_preset_waiter_pool_next`/`test_seam_waiter_pool_next`/`test_seam_slot_attached_awaiter` accessors alter the `async_mutex` class definition, so this target must carry zero fixpp_sync linkage per the same D-7 ODR discipline as `test_async_mutex_aba_interleave` — advisor-confirmed deviation from the brief's literal "via add_sync_test"). Three tests: `ExhaustionAt513thWaiterFailsClosed` (FR-009 baseline: 513th real waiter fails closed, non-discriminating), `FreedSlotReusedViaFreeListAfterExhaustion` (T022 composition), `BoundedCounterPreventsWrapAndReissue` (primary witness — presets `waiter_pool_next_` to `0xFFFFFFFF`, drives two more contended attempts C/D, asserts pool slot 0's `attached_awaiter_` IDENTITY is unchanged — the direct "still-live-slot reissue" proof, not the non-discriminating "alloc_failed" proxy). Evidence: confirmed RED against the pre-fix `fetch_add` (10/10 deterministic runs, clean `ASSERT_FALSE(corruption_detected)` failure at the exact line, no hang/crash — see T022 mutation below for the full RED capture).
- [X] T021 [US3] Implemented the bounded CAS counter replacing `waiter_pool_next_.fetch_add` (`async_mutex.hpp`, bump-allocator fallback in `async_lock`'s pool-alloc lambda): `cur = load(acquire)`; loop — if `cur >= waiter_pool_capacity_` return `nullptr` (fail-closed, no incrementing) else `CAS(cur, cur+1, acq_rel/acquire)`, retry on CAS failure (research.md D-4). No unconditional increment; counter cannot advance past capacity, so cannot wrap.
- [X] T022 [US3] Verify: T020's 3 tests GREEN post-fix (10/10 deterministic debug runs; TSan-clean; ASan-clean). Free-list reuse composition (`FreedSlotReusedViaFreeListAfterExhaustion`): saturates the 512-slot pool, cancels waiter 0, releases the holder (triggering `unlock()`'s reversed-FIFO walk which unlinks+recycles waiter 0's slot to the free list AND grants waiter 1 as new holder), then confirms a further contended attempt reuses the freed slot via the free-list POP (T009/T010) — not the already-capacity-bound bump allocator — proving the two mechanisms compose. MUTATION (mandatory, transient — captured then reverted): reverted T021's bounded CAS to the original unconditional `fetch_add`; `BoundedCounterPreventsWrapAndReissue` went RED 10/10 (`ASSERT_FALSE(corruption_detected)` fails — `attached_awaiter_` at slot 0 changed identity, proving the wrapped counter (`fetch_add` returns `0xFFFFFFFF`→wraps to 0, next attempt returns 0 and reissues) handed live waiter B's memory to unrelated attempt D); the OTHER two tests (exhaustion boundary, free-list composition) stayed GREEN — confirming the mutation is surgically load-bearing for exactly the wrap/reissue witness. Restored the bounded CAS verbatim; `git diff` shows zero `MUTATION` residue; re-verified GREEN 10/10 after restore. Full `linux-clang-debug` rebuild (922 targets) + unfiltered `ctest` → 525/528 passed; the 3 failures are pre-existing/environmental, unrelated to this change: `interop_cell_results_schema_check` + `decimal_compare_oracle` (pytest not installed, precedented in T011's evidence) and `fixpp::dict::codegen-build-graph-check` (asserts a clean `git status --porcelain`; the working tree is mid-round WIP across US1/US2/US3, not yet committed — resolves once this round's changes land). US1/US2 regression witnesses unaffected: `sync_destructor_release_death` 6/6, `sync_drain_destroy_inflight_mt` 2/2, `sync_async_mutex_aba_interleave` 2/2, `sync_async_mutex_layout_golden` (131120/16) all GREEN; `check_no_std_mutex_corpus` GREEN (no new `#include` added to the header).

## Phase 6: User Story 4 (P3) — impossible-state hardening / AM-P3-1/2/3

- [x] T023 [US4] Implement the chain-walk **terminate-trap** in the `granted`-record `else` arms (`:1001-1003`, `:1056-1058`): `std::terminate()` (release-safe) + debug `assert(false && ...)` (research.md D-5, spec FR-005). DONE — both arms trapped in `include/fixpp/core/sync/async_mutex.hpp` `unlock()` (residual-list walk and fresh LIFO->FIFO walk). Mutation-tested: reverting either arm turns its dedicated T026 witness RED (clean `exit(0)`) while the other two witnesses stay GREEN.
- [x] T024 [US4] Implement the null-awaiter arm (`:575-580`) `assert(attached_awaiter_ != nullptr)` + terminate; any defensive `result_` disarm MUST neutralize via `guard.release()` WITHOUT running the engaged guard dtor (leaked-lock direction, never a phantom unlock — research.md D-6). DONE — trap added in the resume runner (`store_executor`'s `resume_fn_` lambda). No defensive `result_` disarm was added (D-6 "Primary" decision): the process terminates before `record->result_`'s engaged `async_lock_guard` is ever destroyed, so the guard.release() disarm guidance is documented as inapplicable-to-the-primary-fix rather than implemented (nothing to disarm — terminate precedes any destructor run). Mutation-tested (see T026).
- [x] T025 [US4] Document the accepted terminate-on-OOM at the resume `asio::post` (`:596`) in the header + contract-delta (research.md D-8); retire the "asymmetry" framing. DONE — header comment added directly above the `asio::post(*exec, std::move(runner));` call in `store_executor`; `specs/058-async-mutex-hardening/contracts/async_mutex-contract-delta.md` "OOM disposition on the resume path" section reworded from defending against "the review's asymmetry claim" to stating the settled, final disposition (AM-P3-3 closed).
- [x] T026 [US4] Fault-injection/mutation witnesses for the T023/T024 traps, OR a written source-level unreachability proof logged as a coverage waiver (per SC-004). DONE via the PREFERRED (stronger) discharge — real fault-injection witnesses, not waivers. New `tests/sync/test_am_p3_impossible_state_traps.cpp` (registered `sync_am_p3_impossible_state_traps`, standalone `FIXPP_ASYNC_MUTEX_TEST_SEAM` target per the D-7 ODR discipline) adds a new test-only mutable pool-slot accessor (`async_mutex::test_seam_mutable_slot`) that drives each mutex through a genuine `async_lock()`/`unlock()` sequence to place a REAL record in the exact list position each trap arm inspects, then corrupts EXACTLY the one field the trap's precondition depends on (`phase_` for T023's two arms, `attached_awaiter_` for T024), and drives the unmodified `unlock()`/resume-runner code into the trap. Three `EXPECT_DEATH` cases (`ChainWalkResidualGrantedTraps`, `ChainWalkFreshFifoGrantedTraps`, `ResumeRunnerNullAwaiterTraps`), each independently mutation-tested by hand-reverting its target trap: the targeted witness goes RED (clean `exit(0)`, EXPECT_DEATH fails) while the other two stay GREEN, confirming per-arm discrimination (not a shared/non-discriminating crash). W-2/W-3 (tasks.md Phase-7.5 waiver block) are therefore NOT invoked — superseded by the landed fault-injection witnesses.

## Phase 7: User Story 5 (P2) — trustworthy MT coverage / T-1..T-7

- [x] T027 [P] [US5] Convert `tests/sync/test_race_cancel_during_resume.cpp` to genuinely multi-threaded (real threads, contended `phase_` arbitration). DONE — holder runs on its own `io_context`/dedicated `std::thread` (thread_a), waiter(s) + their cancellation slot(s) on a SEPARATE `io_context`/thread (thread_b). Setup (holder acquires; waiter parks) is driven deterministically via bounded single-threaded `poll_one()` loops BEFORE either background thread starts (matches `test_drain_destroy_inflight_mt.cpp`'s idiom); only the cancel-vs-grant race itself runs genuinely concurrently. Per advisor review: the cancellation `emit()` is POSTED onto the waiter's OWN executor (`asio::post(ioc_b.get_executor(), ...)`) rather than fired from a third (main-test) thread — `asio::cancellation_signal`/`slot` are not thread-safe and the completion path clears the slot on the waiter's own executor, so an unsynchronized third-thread `emit()` would race asio's own bookkeeping (a harness bug, not a finding about `phase_` arbitration). This still creates a genuine, unsynchronized cross-thread race on the shared `phase_` atomic: the grant CAS (thread_a, inside `unlock()`'s drain) vs. the cancel CAS (thread_b, inside the posted `emit()`'s `on_cancel`) are never causally ordered by the test's own code. Discriminating assertions preserved verbatim (`waiter_kept_lock`, `lock_granted+lock_aborted==ROUNDS=32`, `total==N=16` + clean mutex destruction). `sync_race_cancel_during_resume`: 25/25 clean on `linux-clang-debug` + 25/25 clean on `linux-clang-tsan` (0 `WARNING: ThreadSanitizer` reports), 10/10 clean on `linux-clang-asan`.
- [x] T028 [P] [US5] Convert `tests/sync/test_race_multi_cancel.cpp` to genuinely multi-threaded. DONE — same two-`io_context`/two-thread + posted-`emit()`-on-waiter-executor design as T027, applied to all N cancellation signals (N=8, N=32, N=4 across the file's 3 tests). Discriminating assertions preserved verbatim (`total_completed==N`, `granted+aborted==N`, fresh-acquire-succeeds-after-all-cancelled). `sync_race_multi_cancel`: 25/25 clean on `linux-clang-debug` + 25/25 clean on `linux-clang-tsan` (0 TSan reports), 10/10 clean on `linux-clang-asan`.
- [x] T029 [P] [US5] Convert `tests/sync/test_race_cancel_pre_drain.cpp` to genuinely multi-threaded. DONE — same two-`io_context`/two-thread + posted-`emit()`-on-waiter-executor design as T027/T028, applied to the single-targeted-waiter (index 1 of 3) and all-N-simultaneous (N=8, N=4) scenarios. Discriminating assertions preserved verbatim (`granted+aborted==N`, `granted>=1`, clean mutex destruction after mixed grant/cancel). `sync_race_cancel_pre_drain`: 25/25 clean on `linux-clang-debug` + 25/25 clean on `linux-clang-tsan` (0 TSan reports), 10/10 clean on `linux-clang-asan`.
- [x] T030 [P] [US5] Convert `tests/sync/test_result_write_race.cpp` to genuinely multi-threaded (retire the single-threaded over-claim comment). DONE — `EveryAcquireReceivesWellFormedResult` (the file's primary contention case, the one the "even under high-concurrency" header claim was actually about) converted to the `test_arm64_weak_memory.cpp::MultiThreadedContentionMutualExclusion` idiom: T = `hardware_concurrency()` worker threads all call `ioc.run()` concurrently on ONE `io_context`, so N genuinely-concurrent coroutines contend for the mutex across real cores. N kept at the original 256 (comfortably under the 512-slot bounded waiter pool, AM-P2-3) — an initial N=2000 attempt legitimately tripped pool exhaustion (`bad_results` non-zero), confirmed as a different, already-covered seam (test_pool_exhaustion_reuse.cpp) rather than this seam's CAS-then-publish arbitration, and reverted. `SequentialResultsAreConsistent`/`ResultSlotClearedAfterResume` left single-threaded with an explanatory comment (uncontended-by-construction, no race to convert — feedback_prefer_fix_over_no_production_calls_waiver-adjacent reasoning: adding threads would add no discriminating power). 25/25 clean debug, 25/25 clean TSan (0 reports), 10/10 clean ASan.
- [x] T031 [P] [US5] Convert `tests/sync/test_cancellation_mid_wait.cpp` to genuinely multi-threaded. DONE — all 5 tests converted to the T027-T029 two-`io_context`/two-thread idiom (holder on thread_a, waiter(s)+cancellation slot(s) on thread_b, cancel `emit()` posted cross-thread onto the waiter's own executor). Discovered mid-conversion (documented in the file header): unlike T027-T029's races (Seam #17/#15/#16, "exactly one of {granted,cancelled}" is acceptable), Seam #4 is a DETERMINISTIC single-required-outcome contract (a cancel fired while genuinely parked, with no contending drain in flight, MUST abort) — an unbarriered first attempt raced the cross-thread cancel delivery against the holder's own unlock and made the fixed `EXPECT_FALSE(...has_value())` assertions flakily wrong (observed: waiter legitimately granted before the mutation was even discussed). Fixed by adding a cross-thread-safe atomic barrier (`wait_flag`, a `co_await asio::post`-repost busy-poll — no std::mutex/cv, FR-012) so each holder waits for the waiter's cancellation to be fully delivered (on the genuinely separate thread_b) before its own unlock — pins the required ordering while still exercising real cross-thread post + cancellation-slot delivery under TSan. 25/25 clean debug, 25/25 clean TSan (0 reports), 10/10 clean ASan.
- [x] T032 [US5] Add `tests/sync/test_fifo_across_cycles.cpp` (register in `tests/sync/CMakeLists.txt`): arrivals between unlocks, ≥2 acquire/release cycles, acquisition ORDER asserted (T-7). DONE — new file, registered via `add_sync_test(sync_fifo_across_cycles ...)`. Closes the cross-cycle half of the FIFO contract that `test_fifo_fairness.cpp::DrainCycleReversesLIFO` (within-cycle only) does not cover: constructs H holding, W1/W2/W3 parking (cycle 1 grants W1, residual=[W2,W3] via `next_drain_head_`), W4 arriving strictly after W1 becomes holder (parks on the fresh `state_` LIFO, chronologically after the residual already exists), then 3 more release cycles. Asserts the full granted SEQUENCE `[1,2,3,4]` (not a completion tally) — the discriminating step is `granted_order[1]==2` (cycle 2 must grant the cycle-1 residual W2, not the later-arriving-but-already-parked W4). Deliberate scoped deviation from the T027-T031 genuinely-MT idiom (advisor-reviewed, documented in the file header): this is a determinism contract, not a race — a "known arrival order" is the whole point, so the driver is ONE `io_context` pumped only via test-controlled `poll_one()` + explicit release-gate flags (no yield-count guessing, no std::mutex/cv). Mutation-tested (transient, hand-applied then reverted, `git diff` confirmed byte-identical to the pre-existing baseline after revert): neutering the residual-list check in `unlock()` (`async_mutex.hpp:1328`, `if (head_residual != nullptr)` → `if (false && head_residual != nullptr)`) turns the core assertion RED exactly as predicted (`granted_order[1]` becomes `4` instead of `2`, plus a bounded cycle-3 timeout since the orphaned W2/W3 residual is never reaped) — confirming the witness discriminates the exact class of regression T-7 names. 25/25 clean debug, 25/25 clean TSan (0 reports), 10/10 clean ASan (deterministic by construction — reps are a harness-robustness check, not a stress requirement).
- [x] T033 [US5] Regression: `test_async_mutex_layout_golden` and the `no-std-mutex` CI gate stay green (FR-011/FR-012). DONE (this round, post-T045): `sync_async_mutex_layout_golden` — `AlignmentIs16` + `SizeIsExact048Value` both GREEN on `linux-clang-debug` and `linux-clang-tsan` (`sizeof(async_mutex)==131120`, `alignof==16`, UNCHANGED — T045 added no new members). `check_no_std_mutex_corpus` (the unfiltered corpus gate, run explicitly by name per `feedback_awaitable_header_mutex_include_edge` — a `-R '^sync_'` name-regex filter would still catch it here since it's registered under `tests/sync/CMakeLists.txt`, but confirmed by exact name too) — PASSED (`ctest --test-dir build/linux-clang-debug -R 'check_no_std_mutex_corpus$'`), confirming T045's new `#include <asio/bind_cancellation_slot.hpp>`/`<asio/cancellation_signal.hpp>`/`<array>`/`<chrono>`/`<future>` edges in `test_arm64_weak_memory.cpp` (a TEST file, not corpus-scanned) add no banned `std::mutex` into the corpus headers (the corpus list itself, echoed in the ctest command line, is unchanged — `async_mutex.hpp` + the session/dict headers).
- [x] T034 [US5] Mutation-test EVERY added/converted witness (revert its target fix → RED); record the mutation matrix (SC-007, anti-enshrinement). DONE — see `## Mutation matrix` below.

## Mutation matrix (T034, SC-007 anti-enshrinement)

Every discriminating witness added/converted in 058, its target fix, and the mutation verdict when that fix is transiently reverted. Entries 1–7 transcribe mutation evidence already captured (and reverted) in their own task's Evidence block above; entry 8 (T045) is new evidence captured in this round. Entries 9 are converted MT tests that are regression NETS, not single-fix discriminators — labeled honestly per the brief rather than a fabricated mutation claim.

| # | Witness (file / test) | Target fix reverted | Reverted-state observation | Verdict |
|---|---|---|---|---|
| 1 | T012a — `test_async_mutex_aba_interleave.cpp::PopPreCasAbaCorruptionDetected` | T009 generation bump on the pop CAS (`pack_free_list_head(gen+1,...)` → `pack_free_list_head(gen,...)`) | 10/10 debug runs RED at the same assertion as the pre-fix baseline (`ready_count==1` vs expected `3`) | **RED confirmed** |
| 2 | T012b — `test_async_mutex_aba_interleave.cpp::PopPreLinkLoadReuseRaceIsDataRace` | T010 slot-`free_link` (persistent slot metadata) reverted to a plain `waiter_record` member | 10/10 TSan runs RED with a real TSan data-race report at the mutated read racing the placement-new write, matching the pre-fix report shape | **RED confirmed** |
| 3 | T019 — `test_destructor_release_death.cpp::{CancelDeliveredThenDestroyTerminates, GrantDeliveredThenDestroyTerminates}` | T017 destructor guard's `in_flight_resumers_ != 0` OR-term commented out | Exactly the two targeted tests went RED (clean `exit(0)`, guard did not fire); the other 4 tests in the file stayed GREEN (they trip on the `state_`/`next_drain_head_` terms) — confirms the term is surgically load-bearing for exactly its two shapes | **RED confirmed (surgical, non-entangled)** |
| 4 | T022 — `test_pool_exhaustion_reuse.cpp::BoundedCounterPreventsWrapAndReissue` | T021 bounded CAS counter reverted to the original unconditional `fetch_add` | 10/10 runs RED (`ASSERT_FALSE(corruption_detected)` fails — slot 0's `attached_awaiter_` identity changed, proving the wrapped counter handed live waiter B's memory to unrelated attempt D); the other 2 tests in the file stayed GREEN | **RED confirmed (surgical)** |
| 5 | T026 — `test_am_p3_impossible_state_traps.cpp::{ChainWalkResidualGrantedTraps, ChainWalkFreshFifoGrantedTraps, ResumeRunnerNullAwaiterTraps}` | Each trap (T023 chain-walk `else` arms, T024 null-awaiter arm) hand-reverted independently, one at a time | Targeted witness goes RED (clean `exit(0)`, `EXPECT_DEATH` fails) while the other two stay GREEN — per-arm discrimination confirmed, not a shared/non-discriminating crash | **RED confirmed (3× surgical)** |
| 6 | T032 — `test_fifo_across_cycles.cpp` (full-sequence assertion) | `unlock()`'s residual-list check neutered (`if (head_residual != nullptr)` → `if (false && ...)`) | `granted_order[1]` becomes `4` instead of `2` (cycle 2 wrongly grants the later arrival instead of the cycle-1 residual), plus a bounded cycle-3 timeout | **RED confirmed** |
| 7 | T042(a) — `test_pmr_fallback.cpp::AlwaysThrowingPmrReturnsAllocFailedAndLeavesMutexHealthy` | The always-throwing PMR's `throw` neutered | RED (no `sync_lock_alloc_failed`) | **RED confirmed** |
| 7b | T046 — `test_async_mutex_terminal_cas_recursive_unlock.cpp::{F4FastPathTerminalCasFailGrantsWaiter, F6FifoExhaustedTerminalCasFailGrantsWaiter}` | Each site's recursive `unlock();` call commented out, one at a time (F4 fast-path site, F6 post-FIFO-walk site) | Targeted test RED at `ASSERT_EQ(status, std::future_status::ready)` ("T2 was never granted"), process then aborts via the (expected) T017 destructor guard on the still-wedged mutex; cross-leg isolation via `--gtest_filter` confirmed the OTHER site's test stayed GREEN when only one site was mutated | **RED confirmed (2× surgical, cross-leg isolated)** |
| 8 | **T045 (this round)** — `test_arm64_weak_memory.cpp::{D2CancellationEpochCrossThreadDestroyIsSafe, D2DrainThenDestroyEpochIsSafe, D2FreeListChurnEpochIsSafe}` | D-2 release/acquire pairing relaxed: `in_flight_resumers_.fetch_sub(1, release)` (`:767`) → `relaxed`, AND both paired acquire loads — the destructor guard (`:909`) and the drain terminal condition (`:1613`) — → `relaxed` | **All 3 epochs** produced a genuine TSan data-race report on **10/10 TSan runs** (`ctest` exit code 66, test marked FAILED) — `Write of size 8 ... operator delete ...` (the destroying thread) racing `Previous read of size 8 ... waiter_record::release_ref (async_mutex.hpp:945) ... store_executor(...)::runner (async_mutex.hpp:754)` (the resumer thread) — precisely the pool-write-vs-destroy race D-2 exists to prevent. 0/10 TSan runs clean under the mutation; 10/10 clean after restore (`git diff` confirmed byte-identical restore, no `T045 MUTATION` residue) | **RED confirmed (3× genuine TSan report, ctest-visible FAILED)** — the dedicated D-2 discriminator T015 deferred |
| 9a | T027–T031 (`test_race_cancel_during_resume.cpp`, `test_race_multi_cancel.cpp`, `test_race_cancel_pre_drain.cpp`, `test_result_write_race.cpp`, `test_cancellation_mid_wait.cpp`) | — no single-line revert | These are **genuine-MT regression nets**, not single-fix discriminators: each asserts an invariant (exactly-once completion, mutual exclusion, no lost/double wakeup) that has held since before 058 and is not gated behind any ONE line this feature touched. Their value is running the SAME invariants under real cross-thread contention (TSan/ASan) where the pre-058 single-threaded-in-disguise driver could not have caught a regression — see `feedback_single_threaded_harness_masks_strand_races`. No mutation matrix row is claimed for them beyond that framing. | **Regression net (TSan/ASan), not a mutation-discriminator** |
| 9b | T015 (`test_drain_destroy_inflight_mt.cpp`) | D-2 (same pairing as row 8) | Explicitly NOT mutation-tested by design (file header, T015 evidence) — a positive HB-observation witness only; its dedicated RED-on-mutation proof is T045 (row 8) | **Positive witness only — discriminator deferred to (and now landed at) T045** |
| 9c | T040 (`test_async_mutex_mt_hammer.cpp`) | — no single-line revert | A volume/coverage hammer for CAS-retry arms (A4/A5/A11/C3, F4 organically) — its own Evidence block frames it as a coverage-measurement instrument (fresh `llvm-cov` line hits), not a RED-first discriminator; F4/F6's dedicated discriminators are T046 (row 7b) | **Coverage/volume net, not a mutation-discriminator** |

Anti-enshrinement note (SC-007): every row above whose verdict is a mutation-confirmed RED was captured with the mutation transient (hand-applied, observed, then reverted before the next task began) and `git diff`-confirmed byte-identical to the pre-mutation header afterward — no mutation residue ships. Rows 9a–9c are labeled as nets/instruments rather than forced into a false single-fix-discriminator claim, per the orchestrator brief's explicit instruction not to invent a discriminator where none exists.

## Phase 7.5: Coverage-design gate additions (Fable #2 + Codex, 2026-07-02)

Both reviewers converged: the unlock recursion arms and the CAS-retry arms are **cross-thread-only with
ZERO planned coverage**. These additions close the gaps toward 100% reachable branches; genuinely
unreachable branches are waived with written proofs (below), discharged at T036/verify. Execute within
the relevant US phases but BEFORE the close-out tasks T038/T039.

- [x] T040 [US5] Add the **H-A MT hammer** — `tests/sync/test_async_mutex_mt_hammer.cpp` (register in `tests/sync/CMakeLists.txt`): N=2×hw threads on `thread_pool(4)`, `mr==nullptr`, randomized 0–2µs pre-lock/pre-unlock jitter, ≥25 reps; oracles = exact completion count + mutual-exclusion counter + no-double-grant. Originally scoped by the Phase-7.5 coverage-design gate as "the ONLY coverage of: pop CAS-retry (A4), bump-counter CAS-retry (A5), unlock re-lock/push-retry (A11), the unlock terminal-CAS-fail → recursive-unlock arms (F4/F6), and push_residual CAS-retry (F7)" — **empirical measurement (fresh llvm-cov, `linux-clang-coverage`, per `feedback_coverage_profraw_staleness`) CORRECTS this claim; see the ESCALATION below.** DONE with the correction: `sync_async_mutex_mt_hammer` (`add_sync_test`, ordinary `fixpp_sync` linkage, no seam macro — runs on the seam-OFF coverage lane). `SeamAsyncMutexMtHammer.RealContentionNoLostWakeupNoDoubleGrant`, DUAL CONFIG (added after the correctness-lane-only design was measured insufficient for F4 — see ESCALATION): correctness lanes (debug/TSan/ASan, brief-literal, unchanged) use N=`max(4, 2*hw)`, 12 cycles/worker, 25 reps, randomized 0–2µs jitter; the coverage lane ONLY (`FIXPP_ENABLE_COVERAGE` → `FIXPP_ASYNC_MUTEX_MT_HAMMER_COVERAGE_BURST` compile-definition, wired in `tests/sync/CMakeLists.txt`, ctest `TIMEOUT` raised 120→300) uses N=`max(32, 8*hw)`, 40 cycles/worker, 100 reps, jitter DISABLED (always 0µs) — measured necessary to organically hit F4's few-instruction race window (see below). Both configs: fresh `async_mutex` + fresh `asio::thread_pool(4)` per rep, N coroutines co_spawned on ONE long-lived `pool.get_executor()` (never rebound per co_spawn, per `feedback_strand_in_any_executor_refcount_race`), `mtx.async_lock()` default `mr==nullptr` (inline pool free-list/bump-allocator path). `pool.join()` is the drain barrier (not `fut.get()`, per `feedback_use_future_get_not_a_pool_drain_barrier`) — futures collected only afterward to propagate exceptions. Three discriminating oracles, all direct (not proxies, per `feedback_witness_asserts_named_postcondition_not_proxy`): (1) `completed` atomic == N*cycles exactly (lost/double wakeup in either direction); (2) `nonatomic_holder_count` — a deliberately non-atomic `int` incremented/decremented inside the CS (a genuine double-grant races this plain int — real UB, TSan-reportable), its peak observed via a separate race-free atomic (`peak_holders`), asserted `<=1`; (3) `holder_ticket` identity CAS — a 100%-reliable, non-timing-dependent double-grant detector distinct in mechanism from (2). Evidence (correctness lanes, unchanged light config): 10/10 clean on `linux-clang-debug`; 25/25 clean on `linux-clang-tsan` (0 `WARNING: ThreadSanitizer` reports); 10/10 clean on `linux-clang-asan` (0 `ERROR:`/`SUMMARY: AddressSanitizer` reports). Neither oracle (2) nor (3) tripped in any run across any lane/config, including the coverage-lane burst (100 reps × up to 256 workers × 40 cycles, several trial runs) — no double-grant ever observed despite the deliberately maximized raw contention. `async_mutex.hpp` untouched (git diff pre-existing from T001-T045, zero delta from this task). Full `linux-clang-debug` rebuild → exit 0 (one `clang-scan-deps` `.ddi.tmp` transient flake on an unrelated TU, resolved by immediate single-target retry per T019 precedent); `ctest -R '^sync_'` → 44/44 passed; `check_no_std_mutex_corpus` → PASSED; `sync_async_mutex_layout_golden` → `sizeof(async_mutex)=131120`, `alignof=16`, both unchanged.
  **ESCALATION (ordered by branch, orchestrator decision needed — this revises the Phase-7.5 gate's stated task allocation, not something a single test file should silently redefine):**
  - **A4/A5/C3/A11 — CONFIRMED, no escalation needed.** Fresh `llvm-cov show` on `linux-clang-coverage` (regenerated profraw, not trusted-stale) shows hundreds of real CAS retries per run on all four sites even under the brief-literal jittered light config (e.g. one measured run: free-list pop CAS retried ~30×, bump-counter CAS retried ~6×, `release_ref` push CAS retried ~20×, `async_lock` slow-path push CAS retried ~165×). Solidly covered.
  - **F4 (`unlock()` first terminal-CAS-fail recursive-unlock arm, `async_mutex.hpp:1382`) — CONFIRMED reachable, but ONLY via the coverage-lane burst, not the brief-literal jittered design.** Measured 0 hits across 150+ opportunities under the light (jittered) config in every trial; measured 6 total hits across 6 independent trial runs of a jitter-free, high-volume burst (the `asio::steady_timer` suspension smears the few-instruction race window between `unlock()`'s `state_.exchange(locked_no_waiters)` and its immediately-following CAS-back-to-`not_locked`). The burst's hit rate is low/noisy (~0.3–1.5% of opportunities, e.g. 2 hits in 615 opportunities in the shipped ctest run) — this is a probabilistic, not-deterministically-guaranteed-every-run organic coverage claim. RECOMMENDATION: accept as landed (confirmed reachable, non-zero in every trial including the shipped ctest invocation) — if a future `/speckit-verify` run happens to show 0 hits on F4, that is NOT evidence of a regression, it is the known noise floor; do not chase a "guaranteed every run" bar for this specific race.
  - **F6 (`unlock()` SECOND terminal-CAS-fail recursive-unlock arm, `async_mutex.hpp:1442`, reached only when a FIFO walk exhausts with EVERY queued waiter already cancelled AND a fresh push lands in the immediately-following terminal CAS window) — MEASURED UNREACHABLE by every organic combination tried.** 0 hits under: the light jittered config; the high-volume jitter-free burst (multiple trials, thousands of unlock() calls); and a merged coverage run combining the burst WITH the already-landed T027-T031 cross-thread cancellation race tests (which DO organically drive 5 genuine all-cancelled walk-exhaustion events on their own — confirmed via the `ph == waiter_phase::cancelled` skip-arm lighting up 39× and the terminal-CAS-check being reached 5× — but the terminal CAS itself never failed in any of those 5 events). F6 requires TWO independent narrow races to coincide (full-cancellation AND a concurrently-timed push), and the two preconditions are mutually anti-correlated (raising push volume to hit the second race tends to add fresh `queued` records that break the first race's all-cancelled precondition). RECOMMENDATION: waive F6 at T036 as reachable-in-principle-but-not-organically-hittable within bounded test time (W-9 class — mirrors the existing `assign()` `catch(...)` waiver's "uninjectable with the tools available to this feature" framing) unless the orchestrator commissions a dedicated hybrid cancel+volume follow-up hammer (assessed high-effort/low-confidence-of-success).
  - **`push_residual` CAS-retry ("F7") — MEASURED STRUCTURALLY UNREACHABLE under the supported (non-drain-overlap) contract, recommend waiver not further hammering.** `push_residual` is called only from within `unlock()`, and `unlock()` is holder-serialized (only the current holder's thread ever executes it, one at a time, per the existing header commentary at `:1363-1364`/`:1428-1429`) — so `next_drain_head_` has a single writer at any instant and its CAS can structurally never observe a concurrent modification outside of drain-overlap (explicitly out-of-contract per `contracts/async_mutex-contract-delta.md`). Confirmed empirically across every trial including the highest-volume burst: `push_residual` call count == `do`-`while` loop-body execution count exactly (e.g. 78.9k == 78.9k in one run) — zero retries, zero variance. RECOMMENDATION: waive as a single-writer structural proof (W-4/W-5 class), not a T040 coverage gap — no further hammer investment is likely to change this outcome since it follows from the holder-serialization invariant, not from insufficient volume.
  - Full rationale + the fresh-coverage evidence trail is recorded in the file header comment of `tests/sync/test_async_mutex_mt_hammer.cpp` (CORRECTION section) for the next agent/reviewer to verify independently.
- [x] T041 [US2] Extend the death tests (`test_destructor_release_death.cpp`) with the **residual-chain destructor shape** (H2): grant-with-tail (residual spliced to `next_drain_head_`) then destroy-without-drain → exercises the destructor `next_drain_head_ != nullptr` term independently (else it is masked by the held/granted `state_` term). DONE: `SeamDestructorReleaseDeath.GrantWithTailThenDestroyTerminates` — GREEN today (coverage witness, not RED-first; the current guard already checks `next_drain_head_`). Two waiters queue behind the holder; `unlock()` grants the first and splices the second onto `next_drain_head_`, then `delete mtx` without draining. CONFIRMED masked (not independent): a mutation-pair on `~async_mutex` (disable the `next_drain_head_` term / disable the `state_` term, one at a time, rebuild+rerun, then revert) shows BOTH terms independently trip the guard in this shape — proven structurally (every `unlock()` path that CASes `state_ -> not_locked` runs only after `next_drain_head_.exchange(nullptr)` on the no-grant fall-through; the grant-with-tail path `return`s right after `push_residual`, before the `state_` CAS — so `next_drain_head_ != nullptr` can never coexist with `state_ == not_locked`) and empirically (both single-term builds pass). `next_drain_head_` term confirmed LIVE (not dead), masked by `state_` in this natural shape — matches research.md D-3's framing verbatim.
- [x] T042 [US1] Decide + execute the **cancellation-slot-assign-throws** branch (now `:1219-1226`, reachable under OOM) AND the **PMR `bad_alloc` → `sync_lock_alloc_failed`** arm (now `:1075-1081`, `mr->allocate` inside the `pmr_waiter_block` path): EITHER a fault-injection witness on a **non-sanitizer lane** (the `tools/mallocnesia` interceptor is available; must avoid the operator-new/sanitizer conflict) OR a written reachable-but-uninjectable waiver in the verify doc. Do NOT silently skip (SC-004). DONE (split decision, see W-9 for details/rationale): (a) PMR `bad_alloc` arm — landed a deterministic witness, `SyncPmrFallback.AlwaysThrowingPmrReturnsAllocFailedAndLeavesMutexHealthy` (`tests/sync/test_pmr_fallback.cpp`), an unconditionally-throwing `std::pmr::memory_resource` (layout-independent, unlike the existing `TinyPmrExhaustionReturnsAllocFailed`'s 32-byte-buffer magic number) — asserts `sync_lock_alloc_failed` AND mutex health (a follow-up acquire on the same mutex succeeds, clean destroy). Mutation-tested (neutered the throw → RED, confirmed, reverted). (b) cancellation-slot-assign-throws arm — **WAIVED as reachable-but-uninjectable**, see **W-9**: `tools/mallocnesia` is a pure allocation-call *counter* (never fails an allocation) and cannot inject; the allocation itself is `asio::detail::thread_info_base::allocate` → `aligned_new` → `std::aligned_alloc` (traced in asio 1.36's `cancellation_signal.ipp`/`thread_info_base.hpp`), which is neither global `operator new` nor `malloc`/`calloc`/`realloc`, so neither the repo's existing operator-new-throw idiom nor mallocnesia can reach it without a bespoke `aligned_alloc`-specific LD_PRELOAD interceptor (out of scope: fragile, asio-internals-coupled, and — per the T036 lane caveat — a seam wouldn't register on the seam-OFF coverage lane regardless). The `assign()` *success* path is already covered by `tests/sync/test_cancellation_mid_wait.cpp` (real bound `cancellation_signal`/slot), isolating the waiver to the `catch (...)` arm alone.
- [x] T043 [US4] Resolve the dead `destroy_handler()` (`:545-551`, zero call sites, grep-verified): prefer **REMOVAL** (it is in `async_mutex.hpp`, in-scope) — or waive as a dead defensive API. Removal avoids a forced 0%-lcov waiver. DONE: removed (`grep -rn destroy_handler` tree-wide → single hit, the definition itself; zero callers). Discharges **W-8 by removal**, not by waiver.
- [x] T044 [US1] Add `static_assert(sizeof(asio::any_io_executor) <= sizeof(exec_storage_))` beside the `store_executor` size check, documenting the dead `store_executor`-fail arm (A7, now `:866-872`) + its `destroy_executor` null-fn sibling as structurally unreachable (only `any_io_executor` is ever instantiated; the check is `if constexpr`). DONE: `static_assert` added inside `waiter_record` (`async_mutex.hpp`, beside `exec_storage_`) with the discharge rationale for both A7 and the `destroy_executor` null-fn sibling in a comment. Discharges **W-6 by construction**.
- [x] T045 [US5] Extend `test_arm64_weak_memory` (H-G) to include cancellation + drain-then-destroy epochs + free-list churn under TSan — the only lane that can witness a relaxed-where-release regression on the D-2 pairing (TSan models the C++ memory model regardless of x86 host). Closes gap T-5. DONE — three new `TEST(SeamArm64WeakMemory, ...)` cases added (`namespace weak_memory_d2`), each a rep-loop (20/20/15 reps) over a helper function: `D2CancellationEpochCrossThreadDestroyIsSafe` (waiter resolved via a cross-thread cancellation signal — mutex held throughout the wait so a grant is structurally impossible, cancellation wins by construction not by timing), `D2DrainThenDestroyEpochIsSafe` (single waiter genuinely QUEUED, reaped by `cancel_and_drain()`'s `reap_chain`, T015-style topology reproduced locally), `D2FreeListChurnEpochIsSafe` (M=6 waiters queued behind one holder, ALL reaped in one drain pass — `in_flight_resumers_` accumulates 6 increments and 6 independent resumer runners each perform their own pool-slot push + RELEASE decrement, amplifying the same D-2 pairing under multi-writer churn). Topology (load-bearing, advisor-reviewed): every epoch uses TWO separate SINGLE-THREADED `io_context`s, one dedicated OS thread each — never a shared multi-worker `thread_pool` driving one `io_context`, because a shared io_context's internal scheduler lock is itself a real synchronizes-with edge that would transitively mask a relaxed-ordering mutation regardless of the test's own atomics (this is why T015's cross-executor two-io_context design, not the T040 shared-pool hammer design, was reused/extended here). Channel audit (verified by reading the production code, not assumed): neither the destructor (`:906-912`) nor the drain terminal loop (`:1610-1617`) reads/CASes `waiter_pool_free_` — both touch only `state_`/`next_drain_head_`/`active_holders_count_`/`in_flight_resumers_` — confirming `in_flight_resumers_` is the SOLE channel from the resumer thread's pool writes to the destroyer thread's safety check, so no alternate HB edge can rescue a D-2 mutation. Each epoch's SEQUENCING gate (e.g. epoch 1's `waiter_resolved` poll before the holder's own unlock) is verified to observe state set INSIDE `invoke_handler`, strictly BEFORE the risky `release_ref`/`fetch_sub` writes in program order — so it cannot itself supply the HB edge under test. MUTATION (mandatory, transient, captured then reverted — `git diff`/line-diff confirmed byte-identical restore, zero `T045 MUTATION` residue): reverted `in_flight_resumers_.fetch_sub(1, release)` (`:767`) → `relaxed`, AND both paired acquire loads (destructor `:909`, drain terminal `:1613`) → `relaxed`; rebuilt `linux-clang-tsan`; ran `--gtest_filter='*D2*'` **10/10 times, ALL 10 produced a genuine TSan data-race report** (`ctest` exit code 66, test marked FAILED — not just a printed warning) — identical shape across all 3 epochs: `Write of size 8 ... operator delete(...)` on the destroying thread racing `Previous read of size 8 ... fixpp::sync::detail::waiter_record::release_ref(...) (async_mutex.hpp:945) ... store_executor(...)::runner (async_mutex.hpp:754)` on the resumer thread — precisely the pool-write-vs-destroy race D-2 exists to prevent, at the exact production call sites. Restored all three orderings verbatim (confirmed by direct line comparison against the pre-mutation `Read` capture — byte-identical); rebuilt; re-ran 10/10 clean (0 reports, exit 0) on TSan, 5/5 clean on debug, 3/3 clean on ASan. Full `ctest --test-dir build/linux-clang-{debug,tsan} -R '^sync_'` → 45/45 passed on both presets post-restore (incl. `sync_async_mutex_layout_golden`, `sync_no_std_mutex_ci_gate`/`check_no_std_mutex_corpus`). `async_mutex.hpp` untouched outside the transient mutation window (T045 adds zero production code). This is the dedicated RED-on-mutation proof T015 (test_drain_destroy_inflight_mt.cpp) explicitly deferred — see the T034 mutation matrix row 8.
- [x] T046 [US5] F4/F6 terminal-CAS recursive-unlock seam witnesses (coverage-design gate EMPIRICAL CORRECTION, `.specify/decisions/058-async-mutex-hardening-coverage-design.md`): T040's fresh llvm-cov measurement found F4 (`unlock()` fast-path terminal-CAS-fail, `:1379`) flaky (2/615) and F6 (post-FIFO-walk terminal-CAS-fail, `:1439`) 0-hit on the seam-OFF coverage lane, though both are REACHABLE IN-CONTRACT — not waivable as unreachable. DONE: two new `FIXPP_ASYNC_MUTEX_TEST_SEAM`-gated phases `unlock_pre_terminal_cas_fast` / `unlock_pre_terminal_cas_fifo` added to `detail::async_mutex_seam_phase`, with macro-gated `if (detail::async_mutex_test_seam) detail::async_mutex_test_seam(...)` call-sites inserted immediately before the `:1379` (F4) and `:1439` (F6) terminal CASes — identical `#ifdef`/no-op-when-off idiom as the existing `pop_pre_link_load`/`pop_pre_cas` phases (T006); zero production footprint when the macro is undefined. New standalone target `tests/sync/test_async_mutex_terminal_cas_recursive_unlock.cpp` (registered `sync_async_mutex_terminal_cas_recursive_unlock`, zero `fixpp_sync` linkage, same D-7 ODR discipline as `test_async_mutex_aba_interleave`): `F4FastPathTerminalCasFailGrantsWaiter` and `F6FifoExhaustedTerminalCasFailGrantsWaiter`, each genuinely 2-thread — T1 acquires the mutex (and, for F6, additionally queues+cancels a waiter W so the FIFO walk exhausts all-cancelled before reaching the terminal CAS), then calls `unlock()` directly from a dedicated thread and pins at the target phase (right after `state_.exchange(locked_no_waiters)`, right before the terminal CAS); T2 arrives on a separate `io_context`/thread and queues via the ordinary push CAS while T1 is pinned (queuing committed-and-confirmed via a hard `asio::post`-probe, the same non-timing idiom as T007's `t1_committed` probe, before T1 is released); T1 is then released, its terminal CAS fails against T2's freshly-queued record, and the discriminating oracle is T2's OWN `async_lock()` actually resolving (`r.has_value()==true`) within a bounded `wait_for` — not merely "no crash". Coordination via `std::atomic`/`std::binary_semaphore`/`std::promise`-`std::future` only (FR-012, no `std::mutex`/`condition_variable`). Evidence: 10/10 clean on `linux-clang-debug` (both tests, ~0-4ms each) + 20/20 clean on `linux-clang-tsan` (0 `WARNING: ThreadSanitizer` reports). MUTATION (mandatory, transient, captured then reverted — `git diff`/`grep "T046 MUTATION"` confirmed zero residue after each restore): commenting out the recursive `unlock();` call at the F4 site alone turned `F4FastPathTerminalCasFailGrantsWaiter` RED at the exact predicted assertion (`ASSERT_EQ(status, std::future_status::ready)` — "T2 was never granted"), with the process then aborting via the (expected, correct) T017 destructor guard on the still-wedged mutex (`state_ != not_locked` at `~async_mutex`) — the assertion failure itself is the discriminating RED signal; same shape confirmed independently for the F6 site's recursive call (its own dedicated `ASSERT_EQ` line). CROSS-LEG independence (advisor-flagged: the sibling-unaffected claim must be RUN, not just reasoned) genuinely executed via `--gtest_filter` isolation (the mutated test's own T017 SIGABRT would otherwise prevent gtest from ever reaching a second in-binary test): F4-mutation-applied + `--gtest_filter='*F6*'` → GREEN (F6 untouched); F6-mutation-applied + `--gtest_filter='*F4*'` → GREEN (F4 untouched) — confirming no cross-contamination between the two structurally distinct call sites. Restored both; re-verified 5/5 debug + 5/5 TSan clean (0 reports) post-restore. Full clean `linux-clang-debug` rebuild (1089/1089 ninja objects, exit 0) + unfiltered `ctest` → **529/532 passed**; the 3 failures are pre-existing/environmental, unrelated to this change, matching the exact precedent already recorded at T011/T022: `fixpp::dict::codegen-build-graph-check` (asserts a clean `git status --porcelain`; the working tree carries this round's uncommitted changes) and `interop_cell_results_schema_check` + `decimal_compare_oracle` (`No module named pytest`, pre-existing Python-environment gap). `ctest -R '^sync_'` → **45/45 passed** (incl. the new `sync_async_mutex_terminal_cas_recursive_unlock`); `check_no_std_mutex_corpus` → PASSED; `sync_async_mutex_layout_golden` → `sizeof(async_mutex)==131120`, `alignof==16`, both unchanged. Production (macro-off) build unaffected — both call-sites compile to nothing without `FIXPP_ASYNC_MUTEX_TEST_SEAM`.

## Phase 7.6: Gate-B fixer round (2026-07-03) — MAJOR-1 witness + MAJOR-2 fix

The Codex Gate-B review of PR #162 (commit `6788945`) returned BLOCK with two real MAJOR findings.
Both landed against the already-merged coverage-design ledger above; the ledger is updated in place
(W-7→W-12 relabel) rather than re-litigated.

- [x] T047 [Gate-B MAJOR-1] Witness the chain-walk CAS-loss arms (residual walk `:1357`-area, fresh
  FIFO walk `:1438`-area, `ph = expected_ph` after a failed `queued -> granted` CAS) instead of
  waiving them. The coverage-design/verify doc had waived this as "W-7, hard to force" — an INVALID
  §IX.1 waiver (the arm is reachable under supported cancel-vs-grant contention, the same
  "reachable ≠ unreachable" class as the T046 F4/F6 empirical correction) AND the ID collided with
  the coverage-design doc's own distinct W-7 (`on_cancel record_==nullptr`). DONE — no code change to
  `async_mutex.hpp` beyond two macro-gated seam call-sites (`detail::async_mutex_seam_phase::
  unlock_pre_grant_cas_residual` / `unlock_pre_grant_cas_fifo`, inserted immediately after each
  walk's `phase_.load()` and BEFORE the grant CAS — same `#ifdef FIXPP_ASYNC_MUTEX_TEST_SEAM`/
  no-op-when-off idiom as T046). New standalone target `tests/sync/test_async_mutex_chain_walk_cas_loss.cpp`
  (registered `sync_async_mutex_chain_walk_cas_loss`, zero `fixpp_sync` linkage, D-7 ODR discipline):
  `FifoWalkCancelWinsGrantCasLoss` (single fresh waiter, pool index 0) and
  `ResidualWalkCancelWinsGrantCasLoss` (two waiters — W1 legitimately granted first via an unpinned
  `unlock()` #1, spliced W2 becomes the residual, pool index 1). Both: a dedicated thread calls
  `unlock()` directly and pins at the new phase; the target waiter's cancellation is driven to
  completion on its OWN executor and its future's `sync_lock_aborted` resolution is awaited BEFORE
  releasing the pin — proving `on_cancel()`'s `queued -> cancelled` CAS has already committed, so the
  pinned `unlock()`'s CAS genuinely loses (not simulated). ORACLE (discriminating, per the brief): (a)
  the cancelled waiter's future resolves `sync_lock_aborted`; (b) the cancelled waiter's pool-slot
  `refcount_` (read via the existing T026 `test_seam_mutable_slot` accessor) reads EXACTLY 0 — proves
  list membership was released exactly once (a "skip the release" mutation leaks the ref and leaves
  it >0; a "double release" mutation underflows it to a huge value; both are caught by the SAME plain
  equality assertion, no ASan/sanitizer dependency needed — verified empirically, see MUTATION below);
  (c) `unlock()` returns without hanging and `thread_a.join()` completes cleanly (a stray double-resume
  would trip the existing T023/T024 impossible-state traps via `std::terminate()`, which did NOT fire).
  Coordination via `std::atomic`/`std::binary_semaphore`/`std::promise`-`std::future` only (FR-012).
  MUTATION (mandatory, transient, captured then reverted — `git diff` confirmed byte-identical restore):
  the `release_ref(cur)`/`release_ref(fifo_cur)` call in each arm's `cancelled` branch removed
  independently, one at a time. FIFO arm mutated: `FifoWalkCancelWinsGrantCasLoss` RED at the ref-balance
  assertion (`refcount_==1`, expected 0); `ResidualWalkCancelWinsGrantCasLoss` stayed GREEN (cross-leg
  isolation confirmed — the two arms are structurally independent code). Residual arm mutated (FIFO arm
  restored first): `ResidualWalkCancelWinsGrantCasLoss` RED at the ref-balance assertion (`refcount_==1`,
  expected 0); `FifoWalkCancelWinsGrantCasLoss` stayed GREEN. Both restored; re-verified GREEN debug + TSan
  (0 reports) post-restore. Relabels the colliding waiver **W-7 → W-12** in
  `.specify/decisions/058-async-mutex-hardening-verify.md` and marks it DISCHARGED (witnessed, not
  waived) — see that doc's Step 4 / Waivers sections.

- [x] T048 [Gate-B MAJOR-2] Fix + witness a pre-existing acquisition livelock at the contended
  acquire loop (`async_lock()`'s `while (true)` loop, `if (old_state == not_locked)` branch, pre-fix
  `:1250-1266`-area): the branch CASed with a FRESH `uintptr_t exp2 = not_locked;` (NOT `old_state`),
  and on CAS failure did `continue;` WITHOUT refreshing `old_state`. A waiter that observed
  `not_locked` and lost the CAS to another acquirer kept `old_state == not_locked` FOREVER —
  busy-spinning retrying the doomed `not_locked -> locked_no_waiters` CAS instead of falling through
  to the queueing branch (livelock; deadlock on a shared/oversubscribed single-thread executor). DONE
  — **FIX** (surgical, one CAS's expected-argument changed, no other logic touched): the CAS now
  operates directly on `old_state` (`state_.compare_exchange_weak(old_state, locked_no_waiters, ...)`)
  instead of a discarded local `exp2`, so a failed CAS refreshes `old_state` with the actual current
  value via the standard `compare_exchange_weak(expected&, ...)` contract — the next iteration
  correctly re-observes reality and falls through to the queueing branch. **WITNESS**: new standalone
  target `tests/sync/test_async_mutex_acquire_livelock.cpp` (registered `sync_async_mutex_acquire_livelock`,
  zero `fixpp_sync` linkage, D-7 ODR discipline), two new cooperating macro-gated seam phases
  (`acq_pre_state_reload` immediately before the loop's initial `old_state` load; `acq_pre_notlocked_cas`
  immediately after observing `old_state==not_locked`, before the CAS — matches the brief's named
  seam) pin the SAME acquirer thread ("A") twice: pin 1 lets the test deterministically release a
  temporary holder T0 (zero waiters queued) so the reload observes `not_locked` without a race; pin 2
  lets a second thread B win the fast-path CAS first, so A's own CAS genuinely loses. ORACLE
  (discriminating, bounded — advisor-refined from a re-entry-counter design to a simpler probe): after
  releasing pin 2 (B still holding), a probe is posted onto A's OWN `io_context` and awaited with a
  2-second bound — post-fix A's failed CAS re-queues and SUSPENDS almost immediately, freeing its
  `io_context` to service the probe (GREEN); pre-fix A never suspends (busy-spins inside the same
  synchronous initiation-lambda call), so the probe cannot run within the bound (RED). Test-process
  hygiene: B is released regardless of the probe's verdict, letting even a pre-fix spinning A
  eventually observe the free state and complete (via the buggy unfair bypass), so `thread_a.join()`
  never hangs the test binary even when the assertion has already gone RED. Coordination via
  `std::atomic`/`std::binary_semaphore`/`std::promise`-`std::future`/`shared_ptr`-owned-promise only
  (FR-012) — the probe helper deliberately heap-allocates its promise behind a `shared_ptr` captured
  BY VALUE in the posted lambda (not by reference, unlike every other `confirm_committed` helper in
  this suite): a timed-out probe's posted work item legitimately survives past the function's return
  in the pre-fix case (A's stuck `io_context` cannot drain it until B is later released), and a
  stack-local promise captured by reference there is a genuine dangling-reference UAF once that
  survivor eventually fires — caught empirically (SIGSEGV under gdb, `std::promise::set_value` on a
  freed stack frame) during this task's own mutation-verification pass, before it could ship.
  MUTATION (mandatory, transient, captured then reverted — `git diff` confirmed byte-identical
  restore): the CAS's expected argument reverted to a fresh local `mutation_exp2 = not_locked;`.
  5/5 runs RED (`probe_ok` false, clean assertion failure, no hang, no crash — process exits cleanly
  every time via the B-release self-heal). Restored; re-verified 5/5 GREEN debug + GREEN TSan (0
  reports) post-restore. `async_mutex.hpp` diff for this task is exactly: the one CAS's expected
  argument + two new seam call-sites (both `#ifdef`-gated, zero production footprint) + the new
  `acq_pre_state_reload`/`acq_pre_notlocked_cas` enum values.

  **Verification (both T047/T048), full suite**: `linux-clang-debug` full rebuild exit 0; `ctest -R
  '^sync_'` → 47/47 passed (incl. the 2 new targets, `sync_async_mutex_layout_golden` 131120/16
  unchanged, `check_no_std_mutex_corpus` PASS). `linux-clang-tsan` incremental rebuild exit 0; `ctest
  -R '^sync_'` → 47/47 passed, 0 TSan race reports (one transient `sync_consumer_contract_compile`
  "permission denied" on the FIRST run was a filesystem race from a concurrent rebuild moments
  earlier — confirmed a flake by re-running standalone AND the full suite again, both 100% clean; not
  reproducible, unrelated to this change).

### Coverage waivers (written source-level proofs → `.specify/decisions/058-async-mutex-hardening-verify.md` `## Coverage`, per SC-004 — no silent skips)

- **W-1** `async_lock :877` second `draining_`-check T-arm — synchronous initiation (no suspension `:773`→`:877`); cross-thread drain/acquire overlap is the `:189` UNDEFINED case.
- **W-2** runner null-awaiter trap (`:575-580`, post-T024) — `attached_awaiter_` is nulled only in the tail (inside `invoke_handler`, after this check) under the single-schedule invariant. **SUPERSEDED at T026**: discharged via a real fault-injection `EXPECT_DEATH` witness (`tests/sync/test_am_p3_impossible_state_traps.cpp::ResumeRunnerNullAwaiterTraps`) instead of this waiver — the witness is the stronger, landed discharge; this proof is kept for context only.
- **W-3** chain-walk `granted` TRAP arms (`:1001-1003`, `:1056-1058`, post-T023) — `granted` is set only by the walk that simultaneously unchains the record. **SUPERSEDED at T026**: discharged via real fault-injection `EXPECT_DEATH` witnesses (`ChainWalkResidualGrantedTraps` / `ChainWalkFreshFifoGrantedTraps` in the same file) instead of this waiver — the witnesses are the stronger, landed discharge; this proof is kept for context only.
- **W-4** `cancel_and_drain` `drain_in_progress_` defensive arm — no suspension between the `draining_` check and its store; cross-thread drain is out-of-contract.
- **W-5** `cancel_and_drain` finalize-CAS fail/assert arm (`:1209-1218`) — loop-break invariant (both lists empty, drain owns `locked_no_waiters`).
- **W-6** `store_executor` fail arm (now `:866-872`) + `destroy_executor` null-fn arm — **DISCHARGED BY CONSTRUCTION at T044**: `static_assert(sizeof(asio::any_io_executor) <= sizeof(exec_storage_))` inside `waiter_record` proves the size-fail arm is dead for the only production `Executor` type (`if constexpr` preprocesses it out for that instantiation); the `destroy_executor` null-fn sibling is dead by the same reasoning (no record ever reaches it with a null `destroy_exec_fn_` once `store_executor` is infallible — see the in-code comment for the full chain). No longer a live waiver — the guarantee is compile-time, not test-time.
- **W-7** `on_cancel` `record_==nullptr` (`:722`) — `record_` set (`:836`) before slot assign (`:863`); slot cleared (`:577`) before `record_` re-nulled (`:935`); a disconnected slot cannot fire.
- **W-8** dead `destroy_handler()` — **DISCHARGED BY REMOVAL at T043** (zero call sites tree-wide, grep-verified). Not waived — removed.
- **W-9** (new, T042(b)) cancellation-slot `inherited_slot.assign(...)`'s `catch (...)` arm (`async_mutex.hpp:1219-1226`) — reachable only under genuine process-wide OOM inside `asio::detail::thread_info_base::allocate` → `aligned_new` → `std::aligned_alloc` (asio 1.36 `impl/cancellation_signal.ipp` + `detail/thread_info_base.hpp`, traced source-level). Uninjectable with the tools available to this feature: `tools/mallocnesia` only *counts* `malloc`/`calloc`/`realloc` calls (`atomic_fetch_add` then `return real_malloc(size)`) and never fails an allocation, and the target call is `std::aligned_alloc`, not any of the three functions it intercepts, nor global `operator new` (so the repo's existing operator-new-throw witness idiom, `tests/session/test_business_messages_build.cpp`, doesn't reach it either). The user-supplied `mr` (PMR) parameter does not control this allocation (Erratum E-4: `slot_allocator` is explicitly NOT bound to the cancellation slot). A bespoke `aligned_alloc`-intercepting LD_PRELOAD tool would be required, coupling a test to an asio-version-specific internal call graph (`aligned_alloc` vs `posix_memalign` vs a future asio release's own allocator) for one arm; per the T036 lane caveat the coverage lane also builds seam-OFF, so even a seam-gated hook would not register. The `assign()` **success** path (the paired non-throwing arm) is landed coverage via `tests/sync/test_cancellation_mid_wait.cpp` (real bound `cancellation_signal`/slot), so this waiver is scoped to the `catch (...)` arm alone, not the whole `assign()` call.
- **F4, F6** (T046 disposition, `.specify/decisions/058-async-mutex-hardening-coverage-design.md` "EMPIRICAL CORRECTION") — NOT waived-as-unreachable. Both `unlock()` terminal-CAS-fail → recursive-unlock arms (fast-path `:1379`, post-FIFO-walk `:1439`) are reachable in-contract; WITNESSED by the T046 seam tests (`tests/sync/test_async_mutex_terminal_cas_recursive_unlock.cpp`, forced interleaving). The production lcov LINE on the seam-OFF coverage lane is waived per T036 lane-caveat (a) — the same bucket as the AM-P1 seam arms (T007/T008's `pop_pre_link_load`/`pop_pre_cas`) — correctness is carried by the T046 seam witness (RED-on-neuter mutation-tested), not the coverage lane. This is a lane-coverage waiver, not a reachability waiver.
- **W-11** (`push_residual` weak-CAS back-edge, "F7") — structurally unhittable: single-writer (both `push_residual` callers are inside holder-serialized `unlock()`; the only competing `next_drain_head_` writer is the drain exchange `:1552`, which overlaps unlock ONLY in the out-of-contract drain-overlap case `:189`) PLUS x86 `lock cmpxchg` has no spurious weak-CAS failures (weak==strong on x86) → the CAS back-edge cannot be taken on the coverage lane. Empirically: call-count==loop-iteration-count, zero variance (T040 measurement, 78.9k==78.9k in the highest-volume trial).

## Phase 8: Polish & Cross-Cutting

- [x] T035 `clang-tidy` / `clang-format` / `cppcheck` / `include-what-you-use` clean on `async_mutex.hpp` (Article IX §4). DONE: all four tools run against the ONE production file. **clang-tidy** (`-p build/linux-clang-debug`, project `.clang-tidy`, `HeaderFilterRegex` restricted to `include/fixpp/.*|include/fix/.*`, confirmed no `#` mid-`Checks:>`-block truncation): found 9 findings pre-fix — 6 genuinely 058-introduced (T004's `pack_free_list_head`/`unpack_free_list_head`/`free_list_empty_index`: 3× `hicpp/readability-uppercase-literal-suffix` `0x3FFu`→`0x3FFU`, 2× `readability-redundant-inline-specifier` on the two `constexpr` functions, 1× `modernize-use-designated-initializers`) — all fixed; 3× `clang-analyzer-core.StackAddressEscape` at the `async_lock` initiator lambda's three `handler`-return sites are CONFIRMED PRE-EXISTING (empirically verified via `git stash` swap to the pre-058 base commit `a6a1302` — the identical 3 findings fire on the byte-identical unmodified base lambda) — out of scope, noted not fixed. **clang-format** (`--style=file`, project `.clang-format`): found 12 pre-fix violations in 058-touched lines (confirmed 058-new by content-grep against the base file) — fixed via targeted `-lines=` ranges (never a whole-file `-i`, to avoid reflowing the 2 pre-existing violations at the unrelated `cancel_and_drain` finalize-CAS block, confirmed present byte-identical on the base commit too). Post-fix: 0 violations in 058-touched code, 2 pre-existing violations remain untouched (noted, not fixed, per surgical scope). **cppcheck** (`--enable=warning,style,performance,portability --std=c++23`, asio isystem, `--suppress=missingIncludeSystem`): 1 finding in `async_mutex.hpp` itself (`constVariablePointer` on `release_ref`'s `auto* end`) — CONFIRMED PRE-EXISTING (identical line present in the base commit) — out of scope, noted not fixed; all other cppcheck output was asio-internal noise (HeaderFilterRegex-equivalent scoping applied manually by grepping `^include/fixpp/core/sync/async_mutex.hpp`). **include-what-you-use** (`iwyu_tool.py -p build/linux-clang-debug` with `-Xiwyu --check_also="*/async_mutex.hpp"` since IWYU only analyzes the main TU by default): 1 genuine actionable finding — `<exception>` (for `std::terminate`, used 7× in the file post-058, only 3× pre-058, still never included) — FIXED (added between `<cstdint>`/`<expected>`, alphabetical). The 3 other "add" suggestions (`asio/detail/bind_handler.hpp`, `asio/impl/{any_io_executor,cancellation_signal}.ipp`) are a known IWYU-vs-asio false-positive class (asio detail/`.ipp` headers are implementation-only, never included directly anywhere else in `include/fixpp/`, confirmed by tree-wide grep) — not applied, would violate house style. The 3 "remove" suggestions (`asio/as_tuple.hpp`, `asio/dispatch.hpp`, `asio/error.hpp`, 2 forward-decls) are all CONFIRMED PRE-EXISTING (present byte-identical in the base commit) — out of scope, noted not fixed. Post-fix verification: full `linux-clang-debug` rebuild of all `tests/sync/*` targets (147/147 objects, exit 0, only pre-existing unrelated `-Wunused-result` warnings in `test_slot_allocator_storage.cpp`) + `ctest -R '^sync_'` → **45/45 passed** (incl. `sync_async_mutex_mt_hammer`, the 4 standalone `FIXPP_ASYNC_MUTEX_TEST_SEAM` targets) + `sync_async_mutex_layout_golden` re-confirmed `sizeof(async_mutex)=131120 (expected: 131120)`, `alignof(async_mutex)=16 (expected: 16)`, both unchanged. **Explicit corpus-gate re-check** (per `feedback_awaitable_header_mutex_include_edge` — the `-R '^sync_'` name-regex does NOT catch this test, it carries the `sync` *label*, not a `sync_` name-prefix; the added `#include <exception>` is the one include-edge edit this round made to a corpus header): `ctest --test-dir build/linux-clang-debug -R 'check_no_std_mutex_corpus$'` → **PASSED** (4.18 sec), confirming `<exception>` (already in the file's pre-058 transitive closure — `std::terminate` compiled fine with no explicit include pre-fix — pulling no `std::mutex`/`std::shared_mutex`) adds no banned-mutex edge to the corpus (`async_mutex.hpp` + the session/store/dict headers it also scans). Net diff to `async_mutex.hpp`: 6 clang-tidy fixes + 12-line-range clang-format realignment + 1 added `#include <exception>` — no behavioral change (confirmed by the 45/45 GREEN sync suite + unchanged layout golden + GREEN corpus gate).
- [ ] T036 `/speckit-verify 058-async-mutex-hardening`: serial sanitizer matrix (ASan/UBSan/TSan) + 100%-of-reachable-branch coverage of `async_mutex.hpp` (lcov BRDA/DA), every unreachable branch (W-1..W-8) waived with a written proof in the verify doc `## Coverage`. **Lane caveats (Fable coverage-gate §5, MUST record):** (a) the coverage lane builds **seam-OFF**, so T007/T008's deterministic branch hits do NOT count toward lcov — the pop/push/counter CAS-retry + recursion arms (A4/A5/A11/C3/F4/F6/F7) must be landed *organically* by the T040 H-A hammer on the coverage lane; the seam-gated lines are preprocessed out → no waiver needed for them. (b) AM-P1 is TSan-invisible (FR-013) → correctness carried by T007 (`pop_pre_cas`) + T008 (`pop_pre_link_load`) + T012 mutation + the D-1 generation-monotonicity reasoning — record all four. (c) the D-2 release/acquire ordering is only witnessable via TSan-modeling (T045 H-G), not any x86 coverage lane — cite reasoning + H-G, not coverage. Record → `.specify/decisions/058-async-mutex-hardening-verify.md`.
- [x] T037 Update `spec/behaviors-and-limitations.md` with any B-*/L-* deltas (e.g. an L-* note for the cross-executor granted-holder-vs-drain exclusion). DONE: appended a new `## 058-async-mutex-hardening` section (`spec/behaviors-and-limitations.md`, after 056), matching the file's existing `### Behaviors` / `### Limitations` row format (bold one-sentence claim + detail + **Status** + anchors). 3 Behaviors rows (B-058-1 ABA-safe free-list + bounded exhaustion counter/AM-P1+AM-P2-3; B-058-2 destructor in-flight-resumer guard + release/acquire HB/AM-P2-1+AM-P2-2; B-058-3 impossible-state chain-walk/null-awaiter traps + the settled OOM disposition/AM-P3-1..3) and 2 Limitations rows — **L-058-1 is the required cross-executor granted-holder-vs-drain EXCLUSION note**, transcribed from `specs/058-async-mutex-hardening/contracts/async_mutex-contract-delta.md` "Drain / teardown contract" (cites the pre-existing L-048-2 drain-overlap-UNDEFINED case this precisely scopes, not newly introduces); L-058-2 records the F4/F6/F7 lane-coverage-vs-correctness-witness disposition from T040/T046 (citing `.specify/decisions/058-async-mutex-hardening-coverage-design.md`; the T036 `## Coverage` record is noted as still-to-be-finalized, since T036 is out of this round's scope). No code change.
- [ ] T038 [close-out 1/2] Catalogue close-out — flip the feature-owned `spec/feature-catalogue.md` row(s) to `done` (evidence_pr/tests) + add the `spec/coverage-index.md` entry.
- [ ] T039 [close-out 2/2 — FINAL] Feature-completeness audit — verify tasks ↔ FR-001..013 / SC-001..007 ↔ catalogue all map to a landed test + impl; verdict 100%-or-waived recorded in `.specify/decisions/058-async-mutex-hardening-verify.md` (`## Completeness`). `/gate-b` 4d HARD-BLOCKS without this record.

---

## Dependencies & order

- Setup (T001-T002) → Foundational (T003-T006) → then user stories.
- **Foundational BLOCKS US1 + US3** (shared pool layout: slot restructure, head retype, packing, seam).
- US1 (T007-T012), US2 (T013-T019), US3 (T020-T022), US4 (T023-T026) are otherwise **independent** slices (all edit disjoint regions of the one header) — sequence by priority P1→P3.
- US5 (T027-T034) test conversions are mostly `[P]` (distinct files); T034 mutation-matrix runs last after all fixes land.
- Polish (T035-T039) after all stories; T039 is the FINAL task.
- **Coverage-design gate (Fable #2) runs BEFORE Phase 3** implementation and may inject witness tasks.

## MVP

US1 (AM-P1 free-list integrity) + US2 (safe teardown) — the two P1 slices — are the minimal
bullet-proofing increment; US3/US4/US5 harden and make the suite trustworthy.

## Parallel example

T027–T031 (five race-test conversions, distinct files) run in parallel; T007/T008 (one file) are
sequential; foundational T003–T006 are sequential (same header, layout-coupled).
