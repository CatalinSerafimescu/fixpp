# Pre-Implementation Requirements-Quality Checklist: 003-dictionary-codegen

**Purpose**: Formal pre-`/speckit-implement` reviewer gate — "unit tests for the requirements". Validates that the spec's requirements are complete, clear, consistent, measurable, and coverage-complete across all four high-risk domains (reify bridge & cross-strand; runtime dispatch & version resolution; determinism & build-graph hygiene; performance & allocation NFRs) **before** any code is written. Tests the *requirements*, not the implementation.
**Created**: 2026-05-15
**Worked**: 2026-05-15 (pre-`/speckit-implement` audit; 4 spec amendments applied — see "Findings summary")
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · companion: [requirements.md](requirements.md) (spec-quality gate)
**Scope**: All domains · Formal depth · Audience: pre-implement reviewer

## Findings summary

46/46 worked. **42 PASS** as written. **4 amend-to-pass** (spec.md edited this pass):

- **CHK007** [Gap] — NFR-003-7 now states the golden scope explicitly (Messages-only golden anchor; Reify/_dispatch byte-stable-but-not-golden, the accepted plan-local N-P3-2 residual).
- **CHK014** [Ambiguity] — AC-V4 round-1 "(in Validator.hpp or Fields.hpp, /plan-locked)" resolved → **Validator.hpp** (data-model Entity 3 + plan Project-Structure already locked it; spec text now matches). Mirrors `/speckit-analyze` A1.
- **CHK015** [Measurability] — NFR-003-6 "How verified" upgraded from vague "static inspection" to the concrete measurable artifact (the 4 `Messages.golden.hpp`; attribute drop fails `determinism_test.cpp`). Mirrors `/speckit-analyze` U1.
- **CHK040** — added **Assumption A6** codifying the 003-owned/002-enums-only ownership (also makes the CHK040 spec anchor resolvable). Substance already passed via §8; A6 hardens it.

CHK006 / CHK009 PASS with a recorded minor observation (anchored to the locked `[2c §7.6]` / Google-Benchmark project convention; non-blocking). Ready for `/speckit-implement`.

## Requirement Completeness

- [x] CHK001 - Are requirements defined for every artifact the codegen tool emits (Messages/Fields/Validator/Reify/NormativeReferences + the two `_dispatch/` headers), each with at least one binding AC? [Completeness, Spec §4.1–§4.4] — **PASS.** Messages→AC-G1..G12; Fields→AC-V1/V2/V6; Validator→AC-V3/V4; NormativeReferences→AC-V5; Reify.hpp→AC-R2; `_dispatch/`→AC-D1. Each artifact has ≥1 binding AC + a data-model entity (1–9).
- [x] CHK002 - Is the full set of `owning_<Msg>` move-semantics requirements documented (custom `noexcept` move, source+dest cache reset, lazy `view()` rebuild, copy-deleted), not just asserted in prose? [Completeness, Spec §4.3 AC-R2/AC-R4] — **PASS.** AC-R2 (move-only, copy-deleted, custom `noexcept` move resetting both caches) + AC-R4 (post-move rebuild; static-asserts: no ref members, `is_nothrow_move_constructible_v`, move not `=default`) + data-model Entity 4 I-9 + state-transitions. Concrete, not prose-only.
- [x] CHK003 - Are requirements specified for every error slot the feature introduces (the six `core::error` 23–28 variants) including which condition maps to which slot? [Completeness, Spec §4.8 AC-VP6, §4.4 AC-D7] — **PASS.** AC-VP6 locks slots 23–28; data-model "Error mapping" table binds each condition→variant→AC. Cross-feature note: `get<1128>()` field-absent stays 2b-owned (no 003 slot).
- [x] CHK004 - Is the ApplVerID(1128)→C++ `application_version` mapping documented exhaustively (every wire value, empty, and non-parsing input), not by example? [Completeness, Spec §4.8 AC-VP3] — **PASS.** AC-VP3 covers `"2".."9"`→`v40..v50sp2`, empty→`profile.default_appl`, `default_appl==Unknown`→`dict_unresolved_application_version`, `"0"`/`"1"`/other non-parsing→`dict_unknown_appl_ver_id`; full verbatim table in `contracts/version_profile.hpp` (`2c §4.3:486-501`); data-model Entity 10 resolution algorithm. Exhaustive, not by-example.
- [x] CHK005 - Are requirements for the curated CI conformance subset fully enumerated (what MUST be included) rather than left to sampling judgement? [Completeness, Spec §4.1 AC-G12] — **PASS.** AC-G12 enumerates the curated must-include set: every P1 headline msg, every group-bearing msg, all 7 FIXT admin MsgTypes, every AC-D4 worked-example msg, msgtype-boundary probes; checked-in Gate-A-reviewed manifest, not ad-hoc sampling.
- [x] CHK006 - Are the CMake target-graph requirements complete for all seven targets (`v42/v44/v50sp2/vt11/all_versions/runtime/dispatch`) including their dependency edges? [Completeness, Spec §4.6 AC-C1] — **PASS (minor observation).** AC-C1 names all seven + the load-bearing edges (`::dispatch`→`::all_versions`; `::runtime` = 002 surface + reify bridge decls; the four version targets independent `INTERFACE`). The `::all_versions`→{v42,v44,v50sp2,vt11} aggregation is stated as "umbrella" (implied, not edge-listed) — acceptable: `[2c §7.6]` locks the full graph and AC-C2 objectively tests the unused-version-isolation property. Non-blocking.
- [x] CHK007 - Does the spec define the determinism guarantee's full scope (which emitted files are golden-anchored vs. only byte-stable) rather than leaving `Reify.hpp`/`_dispatch` coverage implicit? [Gap, Spec §4.7 AC-T1, §6 NFR-003-7] — **AMENDED→PASS.** Was implicit (golden scope only in plan-local N-P3-2). NFR-003-7 now states: 4 goldens anchor `Messages.hpp` only; `Reify.hpp`/`_dispatch/*` byte-stable-but-not-golden (accepted plan-local N-P3-2 residual; R3/R4); AC-T1 still requires *all* emitted files byte-identical run-to-run.
- [x] CHK008 - Are requirements documented for the `version_registry` shape boundary (what ships vs. what is deferred to F3/2d) precisely enough to implement the shape without the ownership model? [Completeness, Spec §4.5 AC-X3] — **PASS.** AC-X1 (class + `get(application_version)→expected_t<Dictionary const*>` signature), AC-X2 (`dict_no_dictionary_for_application_version`), AC-X3 (shape-only; ownership/construction deferred to 2d/F3; in-test hand-built registry). data-model Entity 7. Implementable without the ownership model.

## Requirement Clarity & Measurability

- [x] CHK009 - Is every latency ceiling quantified with an unambiguous unit, percentile/condition, and regression budget (string/int/char ≤20 ns; decimal ≤75 ns; `field_value` ≤25 ns; reify bounds)? [Measurability, Spec §6 NFR-003-1/3] — **PASS (minor observation).** NFR-003-1/3 give unit (ns/µs), condition ("warm, 20-tag msg"; plan adds "warm cache, release `-O2`"), and budget (CI fails >5% vs baseline). Percentile is not stated numerically (no p50/p99) — it is the Google-Benchmark project convention (median) used uniformly by 001/002; not a blocking ambiguity.
- [x] CHK010 - Is "zero allocation on the read path" stated with precise boundaries (which accessors, which decimal trait) so it is objectively verifiable? [Clarity, Spec §6 NFR-003-4, §4.1 AC-G4a] — **PASS.** NFR-003-4: string/int/char unconditionally 0; decimal 0 for default `pod_decimal`; allocating substituted `FIXPP_DECIMAL_T` draws from caller `mr` (AC-G4a), never raw `new`/`delete`. Boundaries explicit; `mallocnesia`-verifiable.
- [x] CHK011 - Is the "≤ 4 PMR allocations per `reify_as`" budget defined with an itemised account of what each allocation is, so an implementer can check conformance? [Measurability, Spec §4.3 AC-R7, plan data-model PMR accounting] — **PASS.** data-model Entity 4 itemises: (1) `bytes_` vector; (2) `OffsetTable` object; (3) `OffsetTable` entry array; (4) hash overlay (may fuse with #3); none outside `mr`. AC-R7.
- [x] CHK012 - Is the single-version compile-time ceiling (≤ 3 s) distinguished as load-bearing vs. the ≤ 15 s all-versions "soft" ceiling with a defined measurement method? [Clarity, Spec §6 NFR-003-2] — **PASS.** NFR-003-2: ≤3 s "(load-bearing)" vs ≤15 s "soft" (configurable `FIXPP_BENCH_ALL_VERSIONS_CEILING`; not a default-supported build); method = compile-time bench seam #2 + per-header preprocessor-expansion size.
- [x] CHK013 - Is "byte-identical across runs and machines" defined with the exact invariant (sorted, locale-independent, bytewise) so determinism is testable rather than aspirational? [Measurability, Spec §6 NFR-003-7, Assumption A4] — **PASS.** NFR-003-7 "sorted-emission invariant, locale-independent bytewise compare"; Assumption A4 "sorted, locale-independent (bytewise) emission ordering … byte-stable without a post-emit sort". Testable invariant.
- [x] CHK014 - Is AC-V4's Length+Data pair-table location resolved to a single artifact, or does "(in Validator.hpp or Fields.hpp, /plan-locked)" leave an unresolved ambiguity in the spec text? [Ambiguity, Spec §4.2 AC-V4] — **AMENDED→PASS.** /plan had already locked it to `Validator.hpp` (data-model Entity 3; plan Project-Structure `Validator.hpp` row) but the spec text still carried the unresolved disjunction. AC-V4 now reads "/plan-locked to `Validator.hpp`". Mirrors `/speckit-analyze` A1 (LOW) — closed.
- [x] CHK015 - Is NFR-003-6 (`[[clang::lifetimebound]]`/`[[nodiscard]]` emitted unconditionally) tied to a measurable verification artifact, or only to "static inspection"? [Measurability, Spec §6 NFR-003-6] — **AMENDED→PASS.** "How verified" upgraded: the 4 `Messages.golden.hpp` capture the attributes verbatim — any drop fails `determinism_test.cpp` (seam #1/#2 byte diff); `Reify.hpp`/`_dispatch` attribute emission rides the N-P3-2 byte-stable residual + template review + `flyweight_shape_test.cpp`. Mirrors `/speckit-analyze` U1 (LOW) — closed.
- [x] CHK016 - Is the `owning_<Msg>` thread-safety claim stated unambiguously (single-strand-only; the only safe concurrent pattern is reify-A→move→consume-B) rather than as a blanket "thread-safe" term? [Clarity, Spec §4.7 AC-T3] — **PASS.** AC-T3 explicitly disclaims any blanket thread-safe-on-read claim; single-strand-only (unsynchronized lazy `view()` cache write; concurrent reads = UB, N-P1-2); the only safe concurrent pattern = reify-on-A → move → consume-on-B (AC-R5). data-model I-10.
- [x] CHK017 - Is "fail-loud default arm" for the application dispatch switch defined with the exact returned error and triggering condition? [Clarity, Spec §4.4 AC-D5/AC-D7] — **PASS.** AC-D5: default arm returns `dict_reify_unknown_msg_type` when the resolved version has no codegen owner (runtime-XML-only); AC-D7 enumerates the full mapping; data-model Entity 8 I-11 "default arm fail-loud, never misdispatches".

## Requirement Consistency

- [x] CHK018 - Are the decimal-accessor requirements consistent across AC-G4, AC-G4a, NFR-003-4, and data-model Entity 1 (all PMR-mandatory `decimal_t::parse(bytes, mr)`, `mr` a parameter not a member)? [Consistency, Spec §4.1, §6] — **PASS.** All four state the v1.4 PMR-mandatory `decimal_t::parse(fv->bytes(), mr)` with `mr` a caller-threaded accessor parameter (not a member); decimal excluded from `field_traits`. `contracts/generated_message.hpp` `price(mr)` matches. No divergence.
- [x] CHK019 - Is the flyweight `sizeof == one pointer` invariant stated consistently everywhere the decimal `mr` parameter is introduced (no contradiction implying an `mr` member)? [Consistency, Spec §4.1 AC-G7/AC-G4] — **PASS.** AC-G4, AC-G4a, Entity 1 I-1, and `generated_message.hpp` all explicitly state `sizeof == one pointer` is preserved because `mr` is a parameter, not a member. AC-G7 `static_assert` enforces it. No contradiction.
- [x] CHK020 - Are the `owning_message_traits<Msg>` / `owning_message_t<Msg>` resolvent requirements consistent between AC-G7a, `contracts/reify.hpp`, and `contracts/generated_message.hpp` (the canonical 2c v1.4 §4.8 external-trait form, no `Msg::owning_type` residue)? [Consistency, Spec §4.1 AC-G7a] — **PASS.** AC-G7a, `reify.hpp` (primary template + alias), and `generated_message.hpp` (per-message specialisation + `static_assert`) all use `typename owning_message_traits<Msg>::type` (2c v1.4 §4.8 L1456–1464). Bundle-wide grep for `owning_type` residue = clean (replan-loop round 3 verified the invented `Msg::owning_type` alias removed).
- [x] CHK021 - Is `resolved_message_version`'s authoritative definition single-sourced (declared in `version_profile.hpp` per AC-VP1, consumed-not-redeclared by `reify.hpp` per AC-R6) with no conflicting duplicate shape? [Consistency, Spec §4.8 AC-VP1, §4.3 AC-R6] — **PASS.** `contracts/reify.hpp` explicitly states the ONE authoritative definition lives in `version_profile.hpp` (no forward-declare/defer); AC-R6 consumes-not-redeclares; AC-VP1 declares it once with its `static_assert`s. Single-sourced.
- [x] CHK022 - Are the version-coverage requirements consistent between the "full message set emitted" rule and the "curated CI subset / exhaustive nightly" rule (no implied partial-header contradiction)? [Consistency, Spec §1, §4.1 AC-G12, Assumption A3] — **PASS.** §1 + A3: emission is *full* (mechanical; A-014..A-034 + FIX-Latest filtered at emit, not partially emitted); AC-G12: *conformance testing* is curated-CI + exhaustive-nightly. Emission ≠ testing; no partial-header contradiction.
- [x] CHK023 - Is the `get<1128>()` / `get<35>()` frozen-contract dependency stated consistently across AC-R8, AC-D2, AC-D3, and the R6 drift-guard seam (#18) with no divergent expectations? [Consistency, Spec §4.3/§4.4, §9 seam #18] — **PASS.** AC-R8 (`get<35>()`), AC-D2 (`get<35>()`), AC-D3 (`get<1128>()`) all pin frozen-contract well-formedness to the R6 drift-guard (N-P2-2) in `flyweight_shape_test.cpp`; plan N-P3-1 binds *both* `get<1128>()` and `get<35>()` `static_assert`s there. Consistent.
- [x] CHK024 - Are scope boundaries consistent between spec §5 (out of scope) and the ACs/seams (e.g., behavioral validation excluded but `Validator.hpp` shape-tested; overlay excluded but `field_value` forwarder shipped)? [Consistency, Spec §5, §4.2 AC-V3, §4.1 AC-G6] — **PASS.** §5 excludes behavioral validation while AC-V3 keeps emit+shape-test in scope; §5 excludes DialectOverlay merge while AC-G6 ships only the `field_value(uint16_t)` forwarder. Boundaries align with the ACs.

## Acceptance Criteria Quality

- [x] CHK025 - Does every AC family (G/V/R/D/X/C/T/VP/FT) have at least one acceptance criterion that is objectively pass/fail without interpreting implementation behavior? [Acceptance Criteria, Spec §4] — **PASS.** G→AC-G7 (`sizeof` `static_assert`); V→AC-V4 (exhaustive table); R→AC-R8 (specific error variant); D→AC-D4 (worked-example input→namespace pairs); X→AC-X1 (declared signature); C→AC-C3 (`static_assert` non-convertible); T→AC-T1 (byte-identical); VP→AC-VP1 (`static_assert`s); FT→AC-FT2 (negative static check). Every family has an objective AC.
- [x] CHK026 - Are the negative/exclusion ACs (AC-FT2 decimal NOT a field_traits specialisation; AC-VP4 C++ index NOT reused; AC-G9/G10 messages NOT emitted) framed as verifiable assertions rather than absence-of-evidence? [Acceptance Criteria, Spec §4.8 AC-FT2/AC-VP4, §4.1 AC-G9/G10] — **PASS.** AC-FT2 = a static trait check (`field_traits<decimal_t>` undefined); AC-VP4 = a positive `static_assert` (wire `"7"`→`v50`, not C++ index 7); AC-G9 = check warning emitted + class absent; AC-G10 = class absent + reachable only via runtime `get`. All framed as positive verifiable assertions.
- [x] CHK027 - Is AC-G7a's compile-time shape-oracle expressed as a checkable criterion (ill-formed alias on mis-wire) rather than a runtime expectation? [Measurability, Spec §4.1 AC-G7a] — **PASS.** AC-G7a: omission/mis-wire is "caught as a **compile-time** shape-oracle failure (the alias becomes ill-formed — the same compile-time mechanism as AC-G7's `sizeof` `static_assert`, not a runtime GoogleTest assertion)". `generated_message.hpp` pins the `static_assert`.
- [x] CHK028 - Are the reify error-path ACs (AC-R7 OOM→`dict_reify_oom`; AC-R8 msg-type mismatch; AC-R6 `as<Msg>()` nullptr on mismatch) each tied to a distinct, observable return value? [Acceptance Criteria, Spec §4.3] — **PASS.** AC-R7→`dict_reify_oom`; AC-R8→`dict_reify_msg_type_mismatch`; AC-R6→`nullptr` (no UB/throw). Three distinct observable returns; Error-mapping table confirms distinctness.
- [x] CHK029 - Is AC-C4 (configure-time / build-tree-only / `INTERFACE_INCLUDE_DIRECTORIES` / source-tree-clean) bound to a concrete acceptance mechanism (the named CTest target) rather than asserted narratively? [Acceptance Criteria, Spec §4.6 AC-C4] — **PASS.** AC-C4 binds to `fixpp::dict::codegen-build-graph-check` (`tests/codegen/codegen_build_graph_test.cmake`, a `cmake -P` script test) with four explicit assertions (a)–(d); plan seam→file map row "C4 (CMake-graph)". Concrete mechanism, not narrative.
- [x] CHK030 - Are the AC-D4 worked-example resolution outcomes (Logon→vt11, NOS=9→v50sp2, NOS=6→v44, OCR→v50sp2 default, Heartbeat→vt11) each individually assertable? [Acceptance Criteria, Spec §4.4 AC-D4] — **PASS.** AC-D4 lists five discrete (input → resolved namespace) pairs, each an individually assertable round-trip (seam #15a/#15b, `fixt_cross_vocabulary.cpp`).

## Scenario & Edge-Case Coverage

- [x] CHK031 - Are requirements defined for the FIXT `default_appl == Unknown` + no-ApplVerID edge (must yield `dict_unresolved_application_version`, NOT `dict_reify_unknown_msg_type`)? [Edge Case, Spec §3 Edge Cases, §4.4 AC-D6] — **PASS.** §3 Edge Cases bullet + AC-D6 + Entity 10 resolution algorithm (the v1.0 sentinel fall-through is explicitly *closed* per RC#1). seam #15c.
- [x] CHK032 - Are requirements defined for the runtime-XML-only resolved-version negative path via a hand-built synthetic `MessageView` (no `FIX43.xml` dependency)? [Coverage, Spec §3.6, §4.4 AC-D5] — **PASS.** Story 3.6 Independent Test + AC-D5 both mandate a hand-built synthetic `MessageView`, explicitly no `FIX43.xml` (a 002 F1 deferral). seam #10c.
- [x] CHK033 - Are requirements defined for `std::move` of an `owning_<Msg>` *after* its lazy `view()` cache was populated (both caches reset; moved-to rebuilds; concurrent reads UB)? [Edge Case, Spec §3 Edge Cases, §4.3 AC-R4] — **PASS.** §3 Edge Cases bullet + AC-R4 + Entity 4 I-9/I-10 + State-transitions. Covered incl. concurrent-reads-UB.
- [x] CHK034 - Are requirements defined for PMR allocation failure inside the `noexcept` reify path (trap_throw → `dict_reify_oom`, no terminate)? [Exception Flow, Spec §3 Edge Cases, §4.3 AC-R7, §9 seam #16] — **PASS.** §3 Edge Cases bullet + AC-R7 + seam #16 + Error-mapping ("trapped via `[2a §4.2]` `trap_throw`") + PMR-accounting OOM-injection row ("none terminate").
- [x] CHK035 - Are requirements defined for the allocating-trait decimal substitution (`cpp_dec_float`) edge — heap traffic confined to caller `mr`, never raw `new`/`delete`? [Edge Case, Spec §4.1 AC-G4a, §6 NFR-003-4] — **PASS.** AC-G4a + NFR-003-4 + Entity 1 decimal note + PMR-accounting all state allocating-trait heap traffic is confined to the caller-supplied `mr`, never raw `new`/`delete`.
- [x] CHK036 - Are requirements defined for source XML declaring out-of-locked-set messages (FIX-Latest A-035..A-065 → warning, not emitted; A-014..A-034 → not typed)? [Coverage, Spec §3 Edge Cases, §4.1 AC-G9/AC-G10] — **PASS.** Two §3 Edge Cases bullets + AC-G9 (warning, CI-downgradable, not emitted) + AC-G10 (not typed in v1.0) + Assumption A3 (filtered at emit, not partially emitted). `msgtype_boundary_test.cpp`.
- [x] CHK037 - Are requirements defined for capturing a typed flyweight past the originating view's lifetime (release-UB, debug-trap via generation counter; supported escape is `reify_as`)? [Edge Case, Spec §3 Edge Cases, §4.1 AC-G8] — **PASS.** §3 Edge Cases bullet + AC-G8 (release-UB; debug-trap via `[2b §6.4]` generation counter through the accessor path) + Entity 1 I-4; supported escape = `reify_as` (story 3.2).
- [x] CHK038 - Are the cross-strand handoff requirements coverage-complete (reify on A → move → A's arena reset → B reads stable values; original traps post-reset under TSan)? [Coverage, Spec §3.2, §4.3 AC-R5, §9 seam #12] — **PASS.** Story 3.2 Independent Test + AC-R5 + AC-T3 + Entity 4 I-10 + seam #12 (`reify_cross_strand_test.cpp`, TSan target). Original-traps-post-reset covered.
- [x] CHK039 - Is the recovery/no-owner scenario class addressed (resolved version has no codegen owner) without silently misdispatching? [Recovery, Spec §4.4 AC-D5, §3.6] — **PASS.** AC-D5 + story 3.6 + Entity 8 I-11 (fail-loud default arm returns `dict_reify_unknown_msg_type`, never misdispatches). R3 mitigation.

## Dependencies, Assumptions & Conflicts

- [x] CHK040 - Is the upstream-surface ownership unambiguous post-RC#1 (003 owns `version_profile`/`resolve_application_version`/`field_traits`/`decode_field`; 002 ships enums only) with no residual "002-shipped" claim? [Assumption, Spec §8 Upstream dependency audit, Assumption A6] — **AMENDED→PASS.** Substance already passed via §8 "Upstream dependency audit" + RESOLVED notes + data-model Entities 10–11 (no residual incorrect "002-shipped" claim for the RC#1 surface; the round-2 `core::error`/`expected_t` taxonomic-wording split was applied). The checklist's "Assumption A6" anchor previously did not resolve (spec had A1–A5 only); **added Assumption A6** codifying 003-owned vs 002-enums-only, locked slots 23–28, and the 2b-owned `get<1128>()` carve-out — hardening §8 and making the anchor valid.
- [x] CHK041 - Is the dict↔wire bridge dependency documented as a header-only dual-compile carve-out (no module link edge, no `wire↔dictionary` cycle) rather than an unstated layer violation? [Dependency, Spec §6 NFR-003-8, §11 R6] — **PASS.** NFR-003-8 (RESOLVED via `arch §2.4` v0.2→v0.3 dual-compile bridge-surface carve-out; literal `dictionary→wire` edge explicitly rejected as forming the forbidden `wire↔dictionary` cycle) + §11 R6 + plan "Re-/plan (RC#3)" + `check_layers.py` `BRIDGE_SOURCE_FILES`/`BRIDGE_EXEMPT_INCLUDES` exemption (lint clean, exit 0).
- [x] CHK042 - Is the vendored frozen `wire::MessageView<Index>` stub's contract scope and 2b-swap/drift-guard mechanism specified precisely enough to bound the R6 risk? [Assumption, Spec §11 R6, Assumption A2] — **PASS.** §11 R6 + Assumption A2 + data-model Entity 9 I-12 (`static_assert`s exact member signatures + the I-1 `sizeof` invariant; 2b later replaces the body against the same surface; drift fails at compile time) + plan R6 + N-P3-1 (`get<1128>()`/`get<35>()` well-formed assertions). Scope + swap/drift mechanism precise.
- [x] CHK043 - Are all deferred items (F1–F6) given a concrete, non-speculative trigger so scope exclusions are validated rather than open-ended? [Assumption, Spec §10] — **PASS.** F1 (closes at /plan — resolved to Candidate A); F2 (first dialect-overlay customer pull, w/ D-009); F3 (2d threading/EngineConfig design feature); F4 (post-Gate-A implementation spike); F5 (`[const §XVIII]` post-1.0 roadmap once Appendix D amendment lands + real consumer pull); F6 (first consumer needing typed component access, own /specify+/plan+Gate A). Each trigger concrete.
- [x] CHK044 - Is the "no new Conan dependency / no second XML parser" assumption (F1 Candidate A) stated as a verifiable constraint rather than a design aspiration? [Assumption, Spec §10 F1, plan §"Primary Dependencies"] — **PASS.** plan Constitution-Check `[const §III.2]` ("No new Conan row") + `[const §VII.7]` ("no new parser") + Primary Dependencies ("No new Conan row in this PR") + research D-1; verifiable (inspect conanfile + confirm tool links `fixpp::dict`, no second QuickFIX-XML parser). Constraint, not aspiration.
- [x] CHK045 - Are the conformance-corpus data-source assumptions (public QuickFIX/exchange samples only; no proprietary message data) documented as a validated constraint? [Assumption, Spec Assumption A5, §9 seam #1] — **PASS.** Assumption A5 (public QuickFIX `examples/*.dat`, public exchange specs, ICAP regression set per seam #1; no proprietary data checked in). Verifiable constraint.
- [x] CHK046 - Is the residual R3/R4 golden-coverage gap (no golden for `Reify.hpp`/`_dispatch`) explicitly recorded as an accepted scope decision rather than an unflagged blind spot? [Conflict, plan §"Round 1 — new contract-test / residual-risk items" N-P3-2] — **PASS.** plan N-P3-2 explicitly records it as a deliberate accepted v1.0 scoping gap (plan-local; residual R3/R4 named there; revisit trigger = a move-body/dispatch regression escaping review). Now also cross-referenced from spec NFR-003-7 (CHK007 amendment) so a spec reader sees the scope without inferring it.

## Notes

- Check items off as completed: `[x]`; record findings inline. An item "fails" if the requirement it interrogates is missing, ambiguous, inconsistent, or unmeasurable — the fix is to amend `spec.md`/`plan.md`, not the (not-yet-written) code.
- This is a **requirements-quality** gate, not a test plan. Items ask "is X specified well?", never "does X work?".
- Traceability: 46/46 items carry a `[Spec §…]` reference or a `[Gap]/[Ambiguity]/[Conflict]/[Assumption]` marker (100%, exceeds the ≥80% minimum). CHK040's `Assumption A6` anchor is now resolvable (A6 added this pass).
- Companion `requirements.md` covers the standard Spec-Kit spec-quality gate (content quality / completeness / readiness + Gate-A history); this file is the deeper per-domain pre-implement audit.
- Bundle state at creation: Gate A converged replan-loop round 3 (commit 3824bb5); `/speckit-analyze` clean (0 CRITICAL/HIGH; LOW items CHK014/CHK015 mirror analyze findings A1/U1). Ready for `/speckit-implement` once this gate is worked through.
- **Worked 2026-05-15:** all 46 audited; 4 amend-to-pass spec edits applied (CHK007 NFR-003-7 golden scope; CHK014 AC-V4 → Validator.hpp; CHK015 NFR-003-6 measurable artifact; CHK040 Assumption A6). 42 PASS as written. CHK014/CHK015 close the analyze A1/U1 LOW items. **Gate worked through — `/speckit-implement` may proceed.**
