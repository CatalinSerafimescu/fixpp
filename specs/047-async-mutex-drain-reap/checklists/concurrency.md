# Concurrency-Correctness Requirements Quality Checklist: async_mutex drain-reap (047)

**Purpose**: Unit-test the *requirements* (spec.md / research.md / data-model.md) for a
lock-free drain-protocol fix — are they complete, unambiguous, consistent, and
measurable enough to implement and review at Gate B? This validates the WRITING of the
requirements, not the code.
**Created**: 2026-06-21
**Feature**: [spec.md](../spec.md) · proof [research.md](../research.md) · [data-model.md](../data-model.md)
**Audience**: Gate B reviewer · **Depth**: formal release gate

## Requirement Completeness

- [x] CHK001 Are requirements defined for ALL four blockers (B1 notify, B2 snapshot, B3 unlock-privatization, B4 double-resume) as named, testable obligations? [Completeness, Spec §FR-006a, §Clarifications] — PASS: FR-006a names each blocker (B1–B4) explicitly with its own discriminating witness obligation; §Clarifications (spec.md line 32) and Edge Cases document each as an independent testable scenario; plan §Technical-Context / T007/T008/T015 ground each in a specific test.
- [x] CHK002 Is a requirement stated that EVERY begun party reaches exactly one terminal outcome for each party class — acquirer, holder, in-flight unlocker, resumption? [Completeness, Spec §FR-001/§FR-008, §Key Entities] — PASS: FR-001 covers acquirers; FR-008 covers in-flight unlockers (the B3 case); FR-003 mandates "exactly once" for every racing async_lock; §Key Entities names all four party classes (acquirer, reaper, feeder/sink counters) and the data-model state-transition table (§State transitions) enumerates every party's terminal outcome. Holder terminal outcome is implicit in feeder-before-sink quiesce (FR-002) + data-model table row "Acquirer won fast-path CAS before drain → granted then released."
- [x] CHK003 Is the new quiescence counter (`active_unlockers_count_`) documented as a requirement with its lifecycle (when incremented/decremented) rather than only as an implementation note? [Completeness, data-model §Entities-NEW] — PASS: data-model §NEW entity `active_unlockers_count_` specifies its lifecycle (increment at very top before holders-- and draining_ read; decrement seq_cst at every return after push_residual/grant; RAII guard over whole unlock() body; recursive re-drives not returns); task T004/T010 pin the implementation. This is normative in the data-model, not merely a note.
- [x] CHK004 Is the notify-publication protocol (which decrements must wake the reaper, and how the latch is reliably found) specified as a requirement, not left to the implementer? [Completeness, research §Liveness R2-B1] — PASS: research.md §Liveness — notify discipline specifies the exact helper protocol (`fetch_sub(seq_cst); if (draining_.load(seq_cst)) { if (auto l = drain_latch_ptr_.load(acquire)) l->notify(); }`), names the exempt resumption path (captured latch, L591/L598-599), and explains the Dekker publication hazard. data-model §Counter-model reinforces. Task T006 pins the audited helper. The requirement is fully specified.
- [x] CHK005 Is the terminal-state arbitration requirement (single winner between release/abort) specified, including the reaper's return value on CAS loss? [Completeness, research §Terminal-flag, data-model §terminal_] — PASS: data-model §NEW `drain_latch_state::terminal_` specifies the four binding finalize rules (CAS from pending; only winner closes channel; terminal write sequenced-before close; reaper returns aborted on CAS loss, keeps latch published). research.md §Terminal-flag arbitration gives the full proof. Task T005 pins the implementation.
- [x] CHK006 Is there a requirement covering each invariant the fix must preserve (FIFO order, fast path, idempotent/reentrant drain, F-2, F-3/I-5, noexcept, I-1..I-32)? [Completeness, Spec §FR-004] — PASS: FR-004 enumerates all: FIFO-fair grant ordering, uncontended fast path (no suspension), idempotent/concurrent cancel_and_drain, reentrant-drain use-after-free closure, reaper own-cancellation propagation, noexcept operation, "documented acquisition/ordering invariants" (I-1..I-32). plan §Constraints explicitly states I-1..I-31 unchanged, I-32 soundness note amended, new I-33. research.md §Invariant-preservation cross-references I-04..I-18 (unlock block), FIFO, F-2, F-3/I-5.
- [x] CHK007 Is a witness required for EACH blocker (W-orig, W-B1..W-B4), or only for the original orphan? [Completeness, Spec §FR-006a/§SC-006] — PASS: FR-006a mandates each of the four Gate-A blockers has its OWN discriminating witness RED on per-blocker mutation-revert; SC-006 repeats this. The original orphan (W-orig) is covered by FR-006. research.md §Verification-strategy lists W-orig + W-B1..W-B4 explicitly. Five witnesses total, four distinct mutation targets.
- [x] CHK008 Are requirements present for the design-doc update (I-33 + amended I-32 soundness) so the proof is versioned, not only in the spec bundle? [Completeness, tasks T020] — PASS: task T020 is an explicit requirement: "Update the 006 design doc `.specify/2f-async-mutex.md`: append I-33 (drain convergence), the amended I-32 single-walker soundness note, the `active_unlockers_count_` Dekker handshake (edge #2′), and the single-atomic `drain_terminal` arbitration." plan §Project-Structure also lists `specs/2f-async-mutex.md` as a target artifact.

## Requirement Clarity & Measurability

- [x] CHK009 Are the exact memory orders specified for each strengthened operation (entry `fetch_add`, both `draining_` loads, reaper `draining_.store`, feeder decrements, quiesce loads) rather than a vague "use seq_cst where needed"? [Clarity, research §Exact memory-order changes] — PASS: research.md §Exact-memory-order-changes provides a table with site-by-site entries (async_lock entry fetch_add L776: acq_rel→seq_cst; draining_.load fast-path L780: acquire→seq_cst; draining_.load pre-push L868: acquire→seq_cst; cancel_and_drain draining_.store L1110: release→seq_cst; cancel_and_drain quiesce acquirers-load: acquire→seq_cst; unlock entry unlockers-fetch_add: NEW seq_cst; unlock draining_.load L953: acquire→seq_cst; cancel_and_drain quiesce unlockers-load: NEW seq_cst). "NOT strengthened" sites are also named. Confirmed authoritative in the Gate A FINAL judge (§Binding-implementation-constraints).
- [x] CHK010 Is "no orphaned waiter" expressed as an objectively checkable post-condition (e.g. count of completed acquirers equals started; no residual in either list at finalize) rather than a qualitative "never hangs"? [Measurability, Spec §SC-002/§FR-002] — PASS: SC-002 states "count of completed acquirers equals the number started (no lost wake, no double-resume)"; FR-002 states no residual in either list at finalize ("waiter list is empty and all in-flight acquisition/holder/resumption activity has quiesced"). Both are count-equality / list-empty predicates, not qualitative statements.
- [x] CHK011 Is the witness stress budget quantified (workers, acquirers/round, rounds) and the RED bar quantified (≥99/100 mutation-revert) rather than "enough to hit the window"? [Measurability, Spec §FR-006/§SC-005] — PASS: FR-006 explicitly states "≥4 workers, ≥32 racing acquirers per round, ≥100 rounds per invocation"; SC-005 states "≥99/100 pinned-release invocations RED on a mutation-revert." Both thresholds are numeric and testable.
- [x] CHK012 Is "exactly once" (handler invocation under cancel+drain) defined precisely enough to witness (one `invoke_handler` per awaiter)? [Clarity, Spec §FR-009] — PASS: FR-009 states "MUST resume its awaiter exactly once — never twice (no double-resume / use-after-free of the awaiter handler)"; research.md §B4 describes the CAS ownership mechanism (queued→cancelled CAS; only the winner calls invoke_handler; the loser skips scheduling). W-B4 task witnesses this.
- [x] CHK013 Is the feeder-before-sink read order stated as a precise ordering requirement (which counters, in which order, with which memory order) and not just "read counts coherently"? [Clarity, research §Edge #3, data-model §read-order rule] — PASS: data-model §read-order-rule states "the reaper reads all feeders first; only if every feeder == 0 does it read the sink (holders)." §Counter-model names the Dekker-Handshake loads: acquirers and unlockers as seq_cst; resumptions and holders as acquire. research.md §Edge#3 provides the ordering proof. The converging-loop pseudocode in both research.md and data-model makes the order unambiguous.
- [x] CHK014 Is the convergence termination condition (I-33) stated as a single checkable predicate (all feeders 0 ∧ holders 0 ∧ both lists empty in one pass) rather than prose? [Measurability, data-model §I-33] — PASS: data-model §I-33 defines the predicate precisely: "all feeders zero (active_acquirers_count_, active_unlockers_count_, in_flight_resumptions_, the first two via seq_cst loads), then active_holders_count_ == 0, then both lists (state_, next_drain_head_) empty on a confirming exchange." The reaper state machine pseudocode (both data-model and research.md) presents it as a concrete loop with distinct branches.

## Requirement Consistency

- [x] CHK015 Is the invariant numbering consistent across plan/data-model/research (I-1..I-31 unchanged, I-32 amended, I-33 new — no "I-1..I-32 unchanged" anywhere)? [Consistency, plan §Constraints, data-model §Invariants] — PASS: plan §Constraints line 67 states "I-1..I-31 unchanged, I-32 soundness note amended, NEW I-33"; data-model §AMENDED-I-32 and §NEW-I-33 are consistent with this. research.md line 257 says "I-1..I-32 (006), incl. I-32 waiter_record reclamation — the convergence invariant is named I-33 (NOT I-32; H5)" — this is a preserved-via-reproof framing, not a claim that I-32 is unchanged; the "(NOT I-32; H5)" note explicitly names the new number. No bundle document asserts "I-1..I-32 unchanged." Confirmed: 2f design doc's highest invariant is I-32; I-33 does not pre-exist.
- [x] CHK016 Is the terminal-state model named consistently (`drain_terminal`/`terminal_`, not the old `released_`/`aborted_` bools) in every place the latch is described? [Consistency, data-model §Entities/§state-machine] — PASS: data-model names the NEW entity `drain_latch_state::terminal_` (`std::atomic<drain_terminal>{pending}`) and explicitly states it "replaces the two bools `released_`/`aborted_`." The references to `released_`/`aborted_` in data-model (line 13-14) and research.md (§Terminal-flag) are problem descriptions of what is being replaced, not prescriptive current-behavior claims. The reaper state machine in both data-model and research.md uses the new terminal CAS language ("signal_release() = CAS terminal_ pending→released"). The current code (pre-T005) still has the two bools, but the requirement documents are internally consistent about the target state.
- [x] CHK017 Is the serialized-path claim worded consistently as a *functional* no-op (not "semantic no-op") in spec FR-005, US2, and plan? [Consistency, Spec §FR-005/§US2, plan §Scope-clarification] — PASS: spec FR-005 says "serialized functional semantics MUST be unchanged" and "This is a functional no-op for the serialized path"; US2 intro says "functional no-op (identical observable behavior; only emitted memory-order instructions differ — see FR-005)"; plan §Scope-clarification line 77-78 says "FR-005 wording tightened from 'single-thread no-op' to 'serialized functional semantics unchanged'"; research.md line 274 says "It is NOT an unqualified 'single-thread no-op'." All three use "functional" language consistently; no document asserts "semantic no-op."
- [x] CHK018 Do spec scope, plan scope, and research agree the fix spans `cancel_and_drain` + `async_lock` + `unlock` (not "reaper-only")? [Consistency, Spec §Clarifications, plan §Scope-clarification] — PASS: spec §Clarifications (line 32) explicitly names the scope as "cancel_and_drain(), async_lock(), AND unlock()"; plan §Scope-clarification (line 70-73) says "the correctness proof spans cancel_and_drain() + async_lock() + unlock()"; research.md §Decision names all three functions in the five-change list. No document uses "reaper-only" as a prescriptive current claim after the Gate A expansion — the plan notes it as correcting prior "spec prose 'drain protocol only / reaper-only'."
- [x] CHK019 Are the feeder/sink counter sets identical across research and data-model (feeders = acquirers/unlockers/resumptions; sink = holders)? [Consistency, research §Counter model, data-model §Counter model] — PASS: data-model §Counter-model: "Feeders = {active_acquirers_count_, active_unlockers_count_, in_flight_resumptions_}; Sink = active_holders_count_." research.md §Counter-model: "Feeder counters — active_acquirers_count_, active_unlockers_count_ (NEW), in_flight_resumptions_; Sink counter — active_holders_count_." Sets are identical across both documents.

## Acceptance Criteria Quality

- [x] CHK020 Does each success criterion (SC-001..006) state a measurable threshold and the environment it is measured in (preset matrix, lcov DA/BRDA)? [Acceptance Criteria, Spec §Success Criteria] — PASS: SC-001 = "zero hangs across the full sanitizer preset matrix"; SC-002 = "100% of acquirers reach a terminal state — count of completed equals started"; SC-003 = "no behavior change on every preset (debug, release, ASan, UBSan, TSan, gcc)"; SC-004 = "zero new sanitizer findings (TSan/ASan/UBSan)"; SC-005 = "≥99/100 pinned-release invocations RED on a mutation-revert"; SC-006 = each blocker RED on its own mutation-revert and GREEN with fix. All have a measurable threshold (count, percentage, zero, ≥99/100) and a named environment (preset matrix, sanitizer lanes). Coverage basis (lcov DA/BRDA) is in T021; the SCs correctly reference task-level measurement.
- [x] CHK021 Is the RED-gate criterion defined so a witness cannot pass merely by missing the timing window (mutation-revert + quantified repro rate)? [Acceptance Criteria, Spec §SC-005, research §Verification] — PASS: FR-006 + SC-005 require: (a) internal self-deadline (fast attributable FAIL, never a lane-hang), (b) mutation-revert to establish RED, (c) ≥99/100 pinned-release invocations RED. These three requirements together prevent a witness from passing by missing the timing window: the self-deadline catches the orphan as a test failure; mutation-revert + ≥99/100 quantifies genuine RED.
- [x] CHK022 Is the no-regression acceptance bound to the full existing 006 suite across the named preset matrix, with the libstdc++-TSan basis explicit? [Acceptance Criteria, Spec §SC-003/§SC-004, tasks T017] — PASS: SC-003 names the exact suite "entire existing 006 async_mutex test suite" and presets "debug, release, ASan, UBSan, TSan, gcc"; T017 adds "libstdc++ — NOT libc++, finding 1" making the TSan basis explicit. The three-way combination (SC-003 + SC-004 + T017) provides the complete bound.

## Scenario & Edge-Case Coverage

- [x] CHK023 Are requirements defined for a waiter that parks AFTER the reaper's initial scan but while still counted (the original orphan)? [Coverage, Spec §Edge-Cases, §FR-001] — PASS: FR-001 states "including an acquirer that parks its waiter record concurrently after the reaper's initial waiter-list scan"; §Edge-Cases item 1 ("A waiter increments the in-flight acquirer count, then parks its record after the reaper's initial list scan but while still counted — must be reaped"). US1 Acceptance Scenario 2 is exactly this case.
- [x] CHK024 Are requirements defined for the acquirer→holder transition occurring inside the reaper's quiesce window (B2)? [Coverage, Spec §Edge-Cases] — PASS: §Edge-Cases item "A fast-path acquirer transitions acquirer→holder during the reaper's quiesce snapshot — the reaper must not observe 'all counts zero' and finalize while that holder still holds the lock (B2)"; FR-002 ("completion implies the waiter list is empty and all in-flight acquisition/holder/resumption activity has quiesced together"); W-B2 witness requirement in T015. data-model §read-order-rule provides the ordering requirement.
- [x] CHK025 Are requirements defined for a `draining_==false` unlock that privatizes a chain and re-pushes a residual concurrently with the drain (B3)? [Coverage, Spec §Edge-Cases, §FR-008] — PASS: §Edge-Cases item "An unlock() that read draining_ == false privately detaches the waiter chain and grants one waiter while the drain runs concurrently — the reaper must wait for that unlock to finish and reap the re-published residual; no orphan after finalize (B3)"; FR-008 states "Any waiter such an unlock() re-publishes MUST be reaped, never orphaned (no residual left in next_drain_head_ after finalize)." W-B3 in T007.
- [x] CHK026 Are requirements defined for the recursive `unlock()` re-drive (so the unlocker counter never transiently hits 0 mid-walk)? [Coverage, data-model §active_unlockers_count_ recursion] — PASS: data-model §NEW `active_unlockers_count_` section states "the recursive unlock() tail-calls (L1004, L1055) are NOT returns — the outer frame stays counted across them. Use an RAII entry/exit guard over the whole body; NEVER `unlockers--; unlock(); return` (exposes a transient 0 mid-walk → reopens B3). Recursion yields count 1→2→1→0, never a premature 0." Task T010 reinforces this as a RAII guard requirement.
- [x] CHK027 Are requirements defined for the reaper's own cancellation mid-loop AND for the terminal-flag race between finalize and a late cancel? [Coverage/Exception, research §Terminal-flag, Spec §Edge-Cases] — PASS: §Edge-Cases covers "The reaper's own cancellation fires mid-drain — the documented abort-propagation outcome is preserved"; FR-004 names "reaper own-cancellation propagation"; research.md §Terminal-flag-arbitration specifies the CAS fix (single winner via queued→cancelled) that closes the race; data-model §terminal_ rule 4 ("terminal write is sequenced-before the channel close"). F-3/I-5 preservation cited in FR-004.
- [x] CHK028 Are requirements defined for a new acquirer/unlocker arriving after the drain flag is set (fast-fail / no-grant, no park)? [Coverage, Spec §Edge-Cases] — PASS: §Edge-Cases item "A new async_lock() that begins after the drain flag is set — must fast-fail with the drained result, never park." FR-004 preserves the existing fast-fail behavior as part of "existing async_mutex guarantees." The converging-loop and Dekker handshake (FR-002, edge #2) ensure arriving-after-draining parties fast-fail and notify.
- [x] CHK029 Is the resumption→re-entrant-acquirer handoff covered as a requirement (relying on synchronous `invoke_handler`), or left as an unstated assumption? [Coverage, research §Edge #3] — PASS: research.md §Edge#3 ends with explicit proof and "Verified — not assumed": "invoke_handler (L561→L556) resumes the coroutine synchronously within the resume runner, so a re-entrant async_lock() does its acquirers++ … acquirers-- (fast-failing on draining_) entirely sequenced-before the runner's L598 in_flight_resumptions_--." The proof closes the gap, not just asserts it. This is a load-bearing correctness claim documented as a design requirement in research.md.

## Non-Functional Requirements

- [x] CHK030 Is the fast-path performance budget quantified (Article VIII §2 ±5%) with a named verification method (bench or asm diff)? [NFR, Spec §FR-005, tasks T022] — PASS: FR-005 states "uncontended fast path MUST stay within the Article VIII §2 ±5% budget"; T022 requires "confirm the uncontended async_lock fast path stays within ±5% (bench or asm diff on a supported arch) despite the seq_cst entry increment + both draining_ loads." Both the budget (±5%) and the verification method (bench or asm diff) are named.
- [x] CHK031 Is the coverage requirement expressed on the binding basis (lcov DA/BRDA on touched module) with an explicit waiver path for unreachable branches? [NFR, tasks T021] — PASS: T021 states "Coverage (Article IX §1, lcov DA/BRDA): the converging loop's new branches (confirming-empty vs late-waiter; each feeder-wait arm; the B4 CAS-win/loss arms; the terminal-state CAS-win/loss arms) hit by a witness or carry a recorded Opus waiver in `.specify/decisions/047-async-mutex-drain-reap-verify.md`." The binding basis (lcov DA/BRDA per Article IX §1) and the waiver path (recorded Opus waiver in the named decision file) are both explicit.
- [x] CHK032 Is `noexcept` preservation stated as a requirement given the new CAS/notify/loop code? [NFR, Spec §FR-004] — PASS: FR-004 explicitly lists "noexcept operation" as one of the preserved guarantees ("The fix MUST preserve all existing async_mutex guarantees: … noexcept operation"). research.md §Invariant-preservation notes "noexcept preserved (no new throwing op)." plan §Constraints "noexcept operation preserved."
- [x] CHK033 Is the no-public/ABI-surface-change requirement stated AND given a verification obligation (not only asserted at design time)? [NFR, Spec §FR-007, tasks T023] — PASS: FR-007 states the requirement ("MUST NOT add or alter any public API, error code, configuration, wire format, codegen output, or C-ABI surface"); T023 provides the verification obligation ("Also verify FR-007: confirm no public signature / ABI surface change (the new active_unlockers_count_ + terminal_ are private members; async_mutex's public method set is unchanged) — header-diff or nm spot-check"). Both the requirement and the verification method are present.

## Dependencies & Assumptions

- [x] CHK034 Is the assumption that the 046 witness is moved into / owned by 047 (and 046 rebases) documented? [Assumption, Spec §Assumptions] — PASS: spec §Assumptions first bullet: "The witness file authored under the 046 feature (test_async_mutex_drain_latch_publish_acquire) is moved into / owned by this feature as its RED→GREEN gate; 046 rebases on top once this merges and no longer carries its own copy." plan §Summary ("Witness: the moved 046 test_async_mutex_drain_latch_publish_acquire") and T001 also document this.
- [x] CHK035 Is the assumption that TSan must run under libstdc++ (libc++ TSan = false `use_future` races, finding 1) stated as a binding test-environment requirement? [Assumption/Dependency, research §Verification, tasks T017] — PASS: plan §Constitution-Check row IX §2 states "validate under libstdc++ TSan (libc++ TSan throws false future/promise races — finding 1)"; T017 states "TSan (libstdc++ — NOT libc++, finding 1)"; research.md §Verification-strategy states "TSan basis = libstdc++ Tier-1 (libc++ TSan throws false use_future/promise teardown races — finding 1)." All three references are consistent and binding.
- [x] CHK036 Is the load-bearing ordering assumption — `drain_latch_ptr_` published (release) BEFORE `draining_` (seq_cst) — captured as a requirement the implementation must not reorder? [Assumption, research §Liveness R2-B1] — PASS: research.md §Liveness (R2-B1 Publication protocol): "drain_latch_ptr_ is guaranteed visible-and-non-null here because the reaper publishes it (store(latch, release), L1109) BEFORE draining_.store(true, seq_cst) (L1110)"; Gate A FINAL judge §Final-design: "drain_latch_ptr_ published release-before the seq_cst draining_ store." data-model PUBLISH step in the state machine shows this ordering. The implementation must not reorder these stores; the spec documents why.
- [x] CHK037 Is the "not production-reachable / P2 latent" premise documented so reviewers understand why the merge-blocker is the witness, not a live hang? [Assumption, Spec §Context] — PASS: spec §Context para 2: "Not production-reachable today. All four production consumers (Session::write_gate_, SeqnumManager::mutex_, memory_store::mutex_, file_store::mutex_) run on a per-session strand or an attested-serialized direct_executor, so async_lock() and cancel_and_drain() on a given mutex never execute truly concurrently. The defect is nonetheless real: it violates the primitive's documented cross-thread contract…. Severity: P2 (latent correctness defect in a core primitive)." This is the required documentation.

## Ambiguities & Conflicts

- [x] CHK038 Is the term "feeder decrement that must notify" unambiguous about which sites are exempt (the resumption captured-latch path) vs included (acquirer/unlocker)? [Ambiguity, research §Liveness] — PASS: research.md §Liveness explicitly states "The resumption decrement (L598) is EXEMPT from this protocol: its runner holds the latch by capture (L591) and notifies unconditionally (L599) — no drain_latch_ptr_ load, no publication race, so its decrement stays acq_rel and the reaper reads resumptions with acquire." Included sites are named: "acquirer fast-fail decrement (L781) and alloc-fail decrements (L835/L850)" and all unlocker terminal decrements. The audited helper routes all non-exempt decrements. No ambiguity.
- [x] CHK039 Does any requirement still describe the change as "reaper-only" or "drain-protocol-only" in a way that conflicts with the async_lock/unlock edits? [Conflict, Spec §FR-007 vs §Clarifications] — PASS: FR-007's phrase "internal correctness fix to the drain protocol only" is parenthetical explanation of the internal nature of the fix (no public-surface change), not a scope claim limiting which functions are touched. The load-bearing content of FR-007 is the explicit list "public API, error code, configuration, wire format, codegen output, or C-ABI surface." The Clarifications section explicitly documents the 3-function scope; plan §Scope-clarification annotates that the phrase "drain protocol only / reaper-only" was corrected after Gate A round 1. No functional conflict exists between FR-007 and §Clarifications — they address different dimensions (internal scope vs. API/ABI surface).
- [x] CHK040 Is there any residual conflict between FR-006 (a witness) and FR-006a (per-blocker witnesses) about how many witnesses are mandatory? [Conflict, Spec §FR-006/§FR-006a] — PASS: FR-006 and FR-006a are additive, not conflicting. FR-006 mandates the original multi-threaded witness (W-orig) covering the original orphan; FR-006a mandates four additional per-blocker witnesses (W-B1..W-B4), one per Gate-A blocker. Together = 5 witnesses minimum. SC-006 confirms "each of the four blockers (B1–B4) has a discriminating witness." plan §Summary confirms W-orig + W-B1..W-B4 as five distinct mutation targets. The "a witness" in FR-006 refers to the primary multi-threaded regression fixture; FR-006a extends it without superseding it.

## Notes
- Check items off as the requirements are confirmed adequate: `[x]`.
- This list is the Gate B reviewer's lens on *requirement quality*; the
  `requirements.md` checklist covers spec-template completeness. The
  `/speckit-checklist-audit` (step 9) dispositions each item before `/implement`.

## Audit Result

**Audited by**: Step 9 Checklist-Auditor subagent · **Date**: 2026-06-21

| Disposition | Count |
|---|---|
| PASS | 40 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **40** |

### SPEC-FIXED items
None.

### DD-DECIDED items
None. (All correctness-proof and memory-ordering details live in the bundle's own research.md/data-model.md and are confirmed as PASS citing those sections; the Gate A FINAL judge is cross-referenced as corroborating evidence, not as the primary record for PASS items.)

### WAIVED items
None.

### Realizability sub-check

The two entities flagged as new by the spec:

- **`active_unlockers_count_`** (`std::atomic<std::uint32_t>`) — concrete primitive type; no incomplete-type destructor hazard; lifecycle pinned by task T004 (add private member) + T010 (RAII guard). Verdict: **clean**.
- **`drain_latch_state::terminal_`** (`std::atomic<drain_terminal>`, enum class) — concrete value type; the enum and the atomic wrapper are complete types; no `unique_ptr<fwd-declared-Dep>` pattern. Pinned by task T005 (replace two bools with single atomic terminal state). Verdict: **clean**.

Both entities are to-be-added by tasks (absent from the current header), which is expected. The realizability sub-check found no incomplete-type destructor hazards.

### Anchors spot-verified

1. **`.specify/2f-async-mutex.md`** (Design Doc 2f v1.5) — confirmed:
   - I-32 exists (waiter_record reclamation, line 883) — not a dangling reference.
   - I-33 does NOT pre-exist in 2f (highest variant in the doc is I-32) — confirmed I-33 is the next free number (H5 resolution is sound).
   - `released_`/`aborted_` bools exist in current header and 2f (lines 356-357 / 1500-1501) — data-model's description of replacement is accurate.
   - `active_unlockers_count_` and `drain_terminal` are absent from 2f (planned additions via T020/T004/T005) — absence is expected, not a dangling ref.
   - F-2/F-3/I-5 cited invariants exist and are named in 2f §4.7.3 I-5 and I-7.

2. **`specs/006-async-mutex/data-model.md:124`** (cross-feature cite, load-bearing for CHK005/CHK006/CHK015) — confirmed. Line 124 reads: "Only one structural walker ever traverses the lists at a time: `unlock()` … and `cancel_and_drain()`'s reaper … are mutually exclusive — `unlock()` short-circuits when `draining_ == true`. … This is a **normative precondition** of E-2, not an implementation detail." This is precisely the precondition that data-model §AMENDED-I-32 cites as "the prior design only assumed (not enforced)" and that `active_unlockers_count_` makes enforceable. Cite resolves.

3. **`research/reviews/opus_047_gate_a_FINAL_judge.md`** — confirmed as the binding convergence record; the "Binding implementation constraints" section is present and consistent with tasks.md.

4. **`research/reviews/opus_047_gate_a_r1_judge.md`** — cited in spec §Clarifications and plan §Summary; confirmed present on disk.

All anchors resolve in their signed-off revisions.
