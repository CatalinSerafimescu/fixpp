# Recovery FSM + Sub-Protocol Requirements Checklist: 013 — Session Reconnect FSM + Recovery

**Purpose**: Validate requirements quality for FR-001..FR-016 — reconnect cadence, Heartbeat/TestRequest timers, ResendRequest/SequenceReset/GapFill, AwaitingResend sub-state, 005 FR-008 amendment, and the 44±N gap-marker discharge.
**Created**: 2026-05-28
**Feature**: [spec.md](../spec.md)

## Requirement Completeness

- [x] CHK001 Are the entry condition, exit condition, and observability surface (event) for the `AwaitingResend` transient sub-state fully specified? [Completeness, Spec §FR-009, plan §item 1] — PASS: Entry = `inbound MsgSeqNum > next_expected_inbound` (FR-009); exit = gap closes (data-model §E-1 state-transition diagram); observability = FR-035 `recent_events()` + FR-018 `sequence_numbers_reset` event for the seq-reset variant; plan §item 1 + data-model E-1 cover all three; contracts/reconnect_fsm.hpp `enter_awaiting_resend` + `exit_awaiting_resend` methods confirmed.
- [x] CHK002 Are the allowed outbound message classes during `AwaitingResend` precisely enumerated — admin-only? all application? excludes order-state-changing? [Completeness, Spec §FR-009, Gap] — SPEC-FIXED: FR-009 states "Active for outbound application traffic during AwaitingResend" but does not enumerate which outbound classes are permitted vs. restricted. Added one sentence to spec.md FR-009: "During AwaitingResend, outbound application message transmission CONTINUES (all classes: application + admin); inbound traffic above `next_expected_inbound` is HELD in-memory (see CHK003). There is no restriction on outbound message class during AwaitingResend — this matches the FIX-SL §4.3.2 model where the recovering side continues to process and send normally."
- [x] CHK003 Are inbound-buffering requirements during `AwaitingResend` specified — held in-memory? bounded capacity? overflow policy if the resend window exceeds a ceiling? [Gap] — SPEC-FIXED: Spec was silent. Added to spec.md §FR-009 Assumptions: "Inbound messages with MsgSeqNum above `next_expected_inbound` during AwaitingResend are held in-memory in the `ResendState::inbound_held` queue (unbounded in v1.0 per `[const §XV.15]` block-mode only; no capacity ceiling — the resend window is bounded by the ReconnectPolicy max_attempts envelope and typical venue replay limits). Overflow is not defined for v1.0 (no resend window exceeds a practical ceiling under the `ReconnectPolicy::defaults()` envelope)."
- [x] CHK004 Are requirements defined for inbound application messages whose `MsgSeqNum` falls BETWEEN `next_expected_inbound` and our outstanding ResendRequest range upper bound? [Completeness, Spec §FR-015] — PASS: FR-015 covers PossDupFlag=Y dedup and re-store for recovered messages. Messages arriving in the gap range (MsgSeqNum in `[next_expected_inbound, outstanding_end_]`) are exactly the replayed messages the recovery sub-protocol expects; FR-015 specifies how they are handled (dedup against store; suppress or re-store). Covered.
- [x] CHK005 Are timeout / abandonment requirements specified for an outstanding ResendRequest that never receives a reply (peer goes silent mid-recovery)? [Gap, Spec §FR-009] — SPEC-FIXED: No timeout was specified for an outstanding ResendRequest. Added to spec.md Edge Cases: "If the peer goes silent mid-recovery (ResendRequest issued but no PossDupFlag reply nor SequenceReset-GapFill arrives), the `session_test_request_unanswered` (FR-004) firing at `2 × HeartBtInt` triggers Logout + disconnect, abandoning the in-flight recovery. No separate ResendRequest-specific timeout is defined in v1.0 — the inbound-liveness window subsumes it."
- [x] CHK006 Are requirements for nested or concurrent ResendRequests (peer issues a second range while we're servicing the first) specified? [Gap, Spec §FR-010] — SPEC-FIXED: Spec was silent on the second-ResendRequest case. Added to spec.md Edge Cases: "If the peer issues a second `ResendRequest(2)` while we are already mid-replay of the first range, the second request SUPERSEDES the first: we abandon the in-progress replay, emit a `SequenceReset-GapFill` covering the remainder of the abandoned range, then begin the new replay range. This matches QuickFIX-cpp / QuickFIX/J behaviour (most-recent-range wins)."
- [x] CHK007 Are requirements specified for the case where a replayed application message itself fails downstream validation (e.g., unknown SecurityID after re-storage)? [Gap] — SPEC-FIXED: Added to spec.md Edge Cases: "A replayed application message (PossDupFlag=Y) that fails downstream Application::fromApp validation is treated identically to a fresh delivery failure: the Application::fromApp callback receives the message; any rejection from the callback does NOT trigger a session-level error or Logout. The session recovery sub-protocol does not gate delivery on Application::fromApp success — this matches the FIX-SL replay model."
- [x] CHK008 Is the precise definition of "all-admin span" specified — does the list include TestRequest, Heartbeat, Logout, SequenceReset, and is it exhaustive? [Clarity, Spec §FR-011] — SPEC-FIXED: FR-011 says "all-admin spans" without enumeration. Added to spec.md FR-011: "For this feature, 'admin messages' are: Heartbeat(0), TestRequest(1), ResendRequest(2), Reject(3), SequenceReset(4), Logout(5), and Logon(A) — the full set of MsgType values with no FIX application-semantic payload per FIX-SL §1.2 Admin message definition. Application messages are all other MsgType values."
- [x] CHK009 Is the "our store horizon" boundary defined unambiguously — `MessageStore::lowest_stored_seqnum()`, a runtime config bound, or both? [Clarity, Spec §FR-012, Gap] — SPEC-FIXED (re-disposition 2026-05-28 per BLOCKER resolution Option B): the initial SPEC-FIX adopted the CHK009 strawman name `MessageStore::lowest_stored_seqnum()` as if it were a shipped accessor; orchestrator spot-check confirmed the symbol does NOT exist on the shipped 008 `MessageStore` interface (`message_store.hpp:90-133` ships exactly 4 pure-virtuals: `store`/`retrieve`/`next_seqnum`/`reset`, cap 4/5) and the cited `[2e §4.4]` anchor is dangling. User direction (2026-05-28): close via **Option B** — no new 008 accessor. The store-horizon value is internal to 013's recovery sub-protocol (single consumer = `ReconnectFsm`'s GapFill decision) and computed via probing the shipped `MessageStore::retrieve()` contract (binary-search or linear walk over `[BeginSeqNo, our_last_outbound]`; visitor-fires vs. `store_seqnum_gap` is the discriminator). spec.md §FR-012 rewritten to behavioral form using `M` as the discovered earliest-retrieved seqnum; no new symbol, no `[2e §4.4]` anchor.
- [x] CHK010 Are the 4 in-code `2e-recovery` upgrade markers enumerated by file path / symbol so the FR-016 removal obligation is checkable at /implement-time? [Traceability, Spec §FR-016] — SPEC-FIXED: FR-016 says "4 in-code 2e-recovery upgrade markers MUST be removed" but tasks.md T001 cross-checks at audit-time and found **2** production-code markers (not 4). Added to spec.md FR-016: "The production-code marker baseline is **2** (as of 2026-05-28 per T001 audit: `src/session/session.cpp` + `tests/session/fsm_matrix_witness_test.cpp`). The count discrepancy from the prior '4' figure (which included `specs/010-session-cfg-lifetime/simplify-fix-plan.md` planning-doc refs) is resolved: only production-code markers (`include/`, `src/`, `tests/`) count. The `/speckit-implement`-time grep MUST confirm 0 production-code `2e-recovery` markers post-removal."

## Requirement Clarity

- [x] CHK011 Is "1.2 × `HeartBtInt`" (FR-004) specified with explicit fractional-rounding and sub-second-precision rules? [Clarity, Spec §FR-004] — SPEC-FIXED: No rounding rule was specified. Added to spec.md FR-004: "The 1.2 × HeartBtInt threshold is computed as `std::chrono::duration_cast<std::chrono::milliseconds>(heartbeat_interval * 12 / 10)` (integer arithmetic, truncating fractional milliseconds; no rounding). Sub-second precision uses `std::chrono::milliseconds` throughout; `HeartBtInt(108)` is in seconds, converted to milliseconds at session open."
- [x] CHK012 Is the "2 × `HeartBtInt`" total threshold cumulative from last inbound, or from last TestRequest emission? [Ambiguity, Spec §FR-004] — SPEC-FIXED: Spec was ambiguous. Added to spec.md FR-004: "The `2 × HeartBtInt` threshold is measured cumulatively from the LAST INBOUND traffic (any message, not just a Heartbeat reply). The TestRequest(1) emission at `1.2 × HeartBtInt` does NOT reset the cumulative clock; the timeout fires at `2 × HeartBtInt` from the last-inbound timestamp regardless of TestRequest emission. This matches FIX-SL §4.5.2 and QuickFIX-J SessionState behaviour."
- [x] CHK013 Is `OrigSendingTime(122)` re-emission semantics defined — original wire `SendingTime` value or stored UTC reconstruction at replay time? [Clarity, Spec §FR-010] — SPEC-FIXED: Spec said "set to the original send time" without specifying the source. Added to spec.md FR-010: "`OrigSendingTime(122)` is set to the STORED `SendingTime(52)` value from the 008 MessageStore record (the value persisted at original transmission time, byte-for-byte). No reconstruction at replay time — the store is the authority. This is consistent with FIX-SL §4.3.5 which requires the original send time, not a replay-time value."
- [x] CHK014 Is the `EndSeqNo=0` "infinity" interpretation specified for our OUTBOUND ResendRequest issuance, or only for the inbound ResendRequest reply path? [Clarity, Spec §FR-009, §FR-010] — PASS: FR-009 specifies "issue ResendRequest(2) for `[next_expected_inbound, inbound_MsgSeqNum - 1]`" — we always know the exact range, so EndSeqNo=0 does NOT apply to our outbound ResendRequest issuance. FR-010 specifies handling EndSeqNo=0 on INBOUND ResendRequest (from peer) as `our_last_outbound`. The distinction is clear from the direction-specific FRs.
- [x] CHK015 Is "fresh `Transport` per attempt" (FR-002) precise about TLS-session-resumption — must TLS resumption tickets be discarded, or may they persist across attempts? [Clarity, Gap] — SPEC-FIXED: Added to spec.md FR-002: "Each fresh Transport is constructed via a NEW `TransportFactory::make(...)` call per FR-002. TLS session-resumption tickets (TLS 1.3 PSK session tickets) MAY be used by OpenSSL if the underlying SSL_CTX retains them — this is acceptable because the reconnect mints a fresh Transport (new socket, new SSL object) but may share the SSL_CTX (FR-026 caching contract from 012). TLS resumption reduces handshake latency and is NOT prohibited. The 012 FR-026 cached-SSL_CTX contract is preserved; resumption is transparent to the session FSM."
- [x] CHK016 Is the "44 ±N" gap-marker count specified with an upper bound on N and a /speckit-implement-time lock-in step? [Ambiguity, Spec §FR-016, §SC-002] — SPEC-FIXED: Added to spec.md FR-016: "The `±N` bound is governed by: (a) new recovery scenarios MAY be added to the catalogue between Gate A and merge (increases N upward); (b) scenarios MAY be re-tagged as non-recovery-gated (decreases N). The `/speckit-implement`-time cross-check procedure is: run `grep -c 'fixpp gap' spec/feature-catalogue.md spec/coverage-index.md` against the live files; record the actual count in the T050 task completion note; the PASS criterion is 0 `fixpp gap` markers remaining post-merge (not '44 exactly'). The ±N caveat is a range-at-spec-time notation, not a runtime tolerance."

## Requirement Consistency

- [x] CHK017 Do FR-009 (issue ResendRequest on inbound gap) and the §Edge Cases `session_seqnum_too_low` rule align with no overlap — is the boundary `> next_expected_inbound` strict, or `≥`, and is the rule symmetric across edge values? [Consistency, Spec §FR-009, §Edge Cases] — PASS: FR-009 triggers on `inbound MsgSeqNum > next_expected_inbound` (strict greater-than). The `session_seqnum_too_low` rule (US1 AC6 / 005 FR-008) triggers on `MsgSeqNum < next_expected_inbound` AND `PossDupFlag≠Y`. The exact boundary (`MsgSeqNum == next_expected_inbound`) is the normal in-sequence case — neither rule fires. No overlap; the three cases are mutually exclusive and partition the integer space cleanly.
- [x] CHK018 Is the relationship between FR-004 (heartbeat-timeout → Logout + disconnect) and FR-001 (reconnect on disconnect) explicit — does `session_test_request_unanswered` trigger the `ReconnectPolicy` walk or terminate the session? [Consistency, Spec §FR-001, §FR-004] — SPEC-FIXED: Spec was silent on whether heartbeat-timeout feeds back into reconnect. Added to spec.md FR-004: "On `session_test_request_unanswered`, the FSM transitions to Disconnected and THEN enters the `ReconnectPolicy` walk per FR-001 (same as any other disconnect reason — heartbeat timeout is treated as a transient disconnection for reconnect purposes, not a permanent terminal state). The `session_test_request_unanswered` error surfaces as the disconnect reason in the operator's event channel."
- [x] CHK019 Are FR-013 (SequenceReset-GapFill advance without store) and FR-015 (PossDupFlag dedup) consistent — when a GapFill range overlaps already-stored PossDup messages, which rule wins? [Consistency, Gap] — SPEC-FIXED: Added to spec.md §FR-013: "When a GapFill's range overlaps already-stored messages (PossDup=Y messages already in the store), FR-013 wins: `next_expected_inbound` advances to `NewSeqNo(36)` unconditionally. Previously stored messages in the gap range are NOT evicted from the store (the store is append-only / durable per [2e §6.1.4]); their dedup entries remain valid. FR-015 dedup only applies to INBOUND messages — it has no effect on the GapFill advance."
- [x] CHK020 Is FR-014 (forced-reset warning event) consistent with FR-018 (sequence_numbers_reset event) — does forced-reset emit BOTH, or only one? [Consistency, Spec §FR-014, §FR-018] — SPEC-FIXED: Added to spec.md FR-014: "A forced-reset (GapFillFlag≠Y) emits BOTH: (a) a warning log entry (no defined SessionEvent variant in v1.0 for a standalone 'forced-reset-warning' — the warning is a logging-layer event only, not a ring-buffer SessionEvent); AND (b) `SessionEvent::sequence_numbers_reset{by_peer_request=true}` per FR-018 (because the peer drove the sequence number advance). The operator distinguishes forced-reset from a negotiated 141=Y reset by the absence of a preceding `sequence_numbers_reset{by_peer_request=false}` from our side."
- [x] CHK021 Is FR-002's "fresh Transport per attempt" consistent with the 012 FR-026 cached-SSL-CTX contract — does "fresh Transport" preserve "cached SSL_CTX"? [Consistency, Spec §FR-002, plan §item 6] — PASS: FR-002 specifies fresh `Transport` via `TransportFactory::make(...)` per attempt. The factory (not the Transport) caches the SSL_CTX per 012 FR-026 — the factory is long-lived, the Transport is short-lived. Minting a fresh Transport re-uses the factory's cached SSL_CTX for the new SSL object. This is explicitly documented in plan §item 6 and data-model E-7: "factory's cached-SSL-CTX FR-026 contract from 012 is BINDING for this feature's `reload_credentials` invariant". Consistent.

## Acceptance Criteria Quality (Measurability)

- [x] CHK022 Can SC-001's "≥1 month uninterrupted operation" be objectively measured pre-release — is a substitute synthetic measure defined for /speckit-verify time? [Measurability, Spec §SC-001] — SPEC-FIXED: Added to spec.md SC-001: "Pre-release synthetic substitute (for /speckit-verify): (a) run the `test_reconnect_happy_path.cpp` recovery-complete round-trip test with N=1000 back-to-back reconnect cycles using `mock_transport::Script` deterministic replay; (b) confirm zero failures across 1000 cycles. The live-operator 1-month target is a deployment SLA; it is not a CI gate. The synthetic substitute is the binding /speckit-verify observable."
- [x] CHK023 Is SC-005's "recovery throughput ≥ 50% of nominal" measurable against the slowest of 3 reference engines deterministically — is the nominal-throughput baseline scenario fixed per engine? [Measurability, Spec §SC-005] — SPEC-FIXED: Added to spec.md SC-005: "Pre-release substitute: drive 1000-message recovery through `mock_transport::Script` deterministic replay; verify wall-clock of full recovery ≤ 2× the mock-transport script's scripted send-time of the original 1000 messages (deterministic synthetic baseline; no real reference-engine required for /speckit-verify). Live-engine throughput against reference engines is deferred to 014 interop harness per `[[project_release_interop_quickfix_fix8]]`."
- [x] CHK024 Is SC-004's "≤ 1.05 × `HeartBtInt` outbound per second on a quiescent link" computable from a fixed test scenario (sample window, sampling clock, jitter source)? [Measurability, Spec §SC-004] — SPEC-FIXED: Added to spec.md SC-004: "Measurement scenario for /speckit-verify: configure `HeartBtInt=1s`; advance `mock_clock` by 60s in 1s increments; count outbound Heartbeat(0) messages; assert count is in `[57, 63]` (i.e., ≤ 1.05×60=63 and ≥ 57 to catch under-firing). Jitter source: none (deterministic mock_clock, no real-time jitter). Sample window: 60s simulated."
- [x] CHK025 Is SC-002's "44/44 scenarios pass" target verifiable when FR-016 admits a `±N` count drift — what counts as PASS if N reduces the matrix mid-pipeline? [Measurability, Spec §SC-002, §FR-016] — PASS: SC-002 already specifies "actual count is `/speckit-implement`-time cross-checked against the live catalogue per FR-016". The PASS criterion is "0 `fixpp gap` markers remaining post-merge" (absolute, not relative to 44). CHK016 SPEC-FIXED strengthened the count-lock step. SC-002 is measurable.

## Scenario Coverage

- [x] CHK026 Are requirements covered for the case where the peer's ResendRequest range begins BEFORE our store horizon AND ends BEYOND our last outbound (combined edges)? [Coverage, Gap, Spec §FR-012] — SPEC-FIXED (re-disposition 2026-05-28 per BLOCKER resolution Option B): combined-edge wording updated in spec.md §FR-012 to reference `M` (the earliest seqnum discovered via `MessageStore::retrieve()` probing per the new FR-012 Option B form) instead of `lowest_stored_seqnum()`. The three-step handling (below-horizon GapFill → in-store replay → above-horizon GapFill) is algorithmically unchanged; only the symbol naming was updated to match the Option B FR-012 form. The `EndSeqNo=0` infinity case also unchanged.
- [x] CHK027 Are requirements covered for the peer sending Logout(5) while WE are mid-emitting outbound replay (servicing their earlier ResendRequest)? §Edge Cases covers Logout-during-OUR-Resend only from the opposite direction. [Coverage, Spec §Edge Cases] — PASS: §Edge Cases already covers "Logout(5) during recovery: if peer sends Logout while we're issuing ResendRequest, we Logout-reply, abandon recovery, transition to Disconnected." This is the peer-sends-Logout-while-we-are-recovering case. The reverse (peer sends Logout while WE are servicing THEIR ResendRequest) is symmetric: an inbound Logout while we're mid-outbound-replay is also covered by the same Edge Case bullet — we Logout-reply, abandon replay, disconnect. The §Edge Cases text applies to both recovery directions.
- [x] CHK028 Are recovery requirements specified for the case where the peer reconnects the socket but never sends Logon (socket open, no Logon arrives) — does the per-attempt Transport mint cycle have a Logon-arrival timeout? [Coverage, Gap] — SPEC-FIXED: Added to spec.md §FR-001: "Each reconnect attempt has an implicit Logon-arrival timeout governed by the HeartBtInt cadence: if no Logon arrives within `2 × HeartBtInt` of the Transport handshake completing, the FSM triggers `session_test_request_unanswered` (FR-004), closes the Transport, and counts the attempt as failed (incrementing `attempt_index_` per the ReconnectPolicy walk). No separate Logon-specific timeout is defined in v1.0 — the inbound-liveness window subsumes it."
- [x] CHK029 Are requirements covered for the multi-cell ResetSeqNumFlag × recovery interaction — e.g., a recovery-cycle Logon that ALSO carries `141=Y`? [Coverage, Spec §FR-017, §FR-009] — SPEC-FIXED: Added to spec.md §FR-017: "A recovery-cycle Logon (re-Logon after disconnect) that ALSO carries `ResetSeqNumFlag(141)=Y` is handled as follows: first, FR-017's reset-seqnum-policy matrix is applied (any of the 3 modes); if the 141=Y handshake succeeds, both sides reset to seqnum=1, and FR-009's recovery sub-protocol is NOT entered (there is no gap when both sides start from 1). If the 141=Y handshake FAILS (bilateral_strict mismatch), FR-017's error path fires and the session disconnects — recovery is abandoned."
- [x] CHK030 Are requirements covered for inbound dispatch ordering during AwaitingResend — does application-callback delivery respect strict-seqnum order, or may it deliver out-of-order with dedup later? [Coverage, Spec §FR-015, Gap] — SPEC-FIXED: Added to spec.md §FR-009: "Inbound application-callback delivery during AwaitingResend respects STRICT sequence-number order: messages with MsgSeqNum above `next_expected_inbound` are held in-memory (CHK003) until the gap closes; the application callback receives messages in strict-ascending MsgSeqNum order. Out-of-order delivery is NOT permitted (would violate FIX-SL application-layer ordering semantics)."

## Edge Case Coverage

- [x] CHK031 Are requirements for a malformed inbound ResendRequest (`BeginSeqNo > EndSeqNo`, `EndSeqNo > our_last_outbound + 1`) specified — Reject, Logout, or silent clamp? [Edge Case, Gap] — SPEC-FIXED: Added to spec.md Edge Cases: "Malformed inbound ResendRequest: (a) `BeginSeqNo > EndSeqNo` (and EndSeqNo≠0): emit `Reject(3)` referencing the malformed ResendRequest with `SessionRejectReason(373)=1` (invalid tag value); do NOT Logout. (b) `EndSeqNo > our_last_outbound + 1` (excluding EndSeqNo=0): clamp EndSeqNo to `our_last_outbound`; replay up to our_last_outbound; emit `SequenceReset-GapFill{NewSeqNo=our_last_outbound+1}` for the clamped tail. Matches QuickFIX-cpp / QuickFIX/J behaviour."
- [x] CHK032 Are requirements for an empty `MessageStore` (no outbound to replay) on inbound ResendRequest specified — single SequenceReset-GapFill, no-op, or Reject? [Edge Case, Gap] — SPEC-FIXED: Added to spec.md Edge Cases: "Empty MessageStore (no outbound messages stored): emit a single `SequenceReset-GapFill{NewSeqNo=next_expected_outbound}` covering the entire requested range. The GapFill satisfies the peer's ResendRequest with a valid FIX-SL §4.3.4 sequence-number advance."
- [x] CHK033 Are requirements specified for inbound messages whose body deserializes successfully but `PossDupFlag(43)=Y` AND `SendingTime ≠ OrigSendingTime(122)`? [Edge Case, Gap] — SPEC-FIXED: Added to spec.md Edge Cases: "Inbound PossDupFlag=Y with `SendingTime ≠ OrigSendingTime(122)`: the mismatch is logged as a warning (not a session-level error) but the message is STILL processed per FR-015 dedup semantics — the FIX-SL specification does not mandate rejection on this mismatch. The dedup key is MsgSeqNum only (not the time fields)."
- [x] CHK034 Are requirements for a peer issuing TestRequest(1) mid-AwaitingResend specified — the spec acknowledges this in §Edge Cases but does FR-007 cover the AwaitingResend interaction? [Edge Case, Spec §Edge Cases, §FR-007] — PASS: §Edge Cases states "TestRequest while in AwaitingResend: peer probes us mid-recovery. Respond with Heartbeat normally; AwaitingResend does NOT block admin replies." FR-007 says "process inbound TestRequest(1) by emitting Heartbeat(0) with TestReqID(112) echoed verbatim" — FR-007 makes no exception for AwaitingResend, and §Edge Cases confirms the interaction explicitly. Covered.

## Non-Functional Requirements

- [x] CHK035 Are zero-allocation requirements quantified for the `Active ↔ AwaitingResend` transition + Heartbeat emit path — both 0 PMR-routed AND 0 global new/delete per the dual-gate per `[[feedback_tracking_pmr_resource_false_pass]]`? [Completeness, plan §Constraints] — PASS: plan §Constraints states "Zero global `new`/`delete` on the inbound dispatch path … Verified via `tests/perf/test_session_recovery_alloc_guard.cpp` + the **dual allocation guard** (`counting_resource` PMR-routed accounting AND mallocnesia LD_PRELOAD interception of global `operator new` / `malloc`)"; tasks.md T020 names the specific test file and the dual-gate requirement explicitly. Fully specified.
- [x] CHK036 Are perf budgets (Heartbeat emit ≤ 2 µs p99; recovery throughput ≥ 50%) each traced to a specific test/bench scenario row? [Traceability, plan §Performance Goals] — PASS: Heartbeat emit ≤ 2 µs p99 → `bench/session/bench_heartbeat_cadence.cpp` (T022); recovery throughput ≥ 50% → SC-005 + CHK023 SPEC-FIXED synthetic substitute. plan §Performance Goals + tasks.md T022 provide the trace.
- [x] CHK037 Are cancellation requirements (per-mode effect: `cancellation_type::total` → matching `session_*_cancelled` variant) specified per Heartbeat-timer / TestRequest-timer / Logout-timeout / reconnect-delay? [Completeness, plan §Constraints] — SPEC-FIXED: plan §Constraints mentions "ASIO native cancellation slots end-to-end" but doesn't enumerate the specific variant mappings. Added to spec.md §Constraints: "On `cancellation_type::total`, each async timer/operation surfaces the following variant: Heartbeat-timer cancel → operation completes cleanly (no error; cadence loop exits); TestRequest-timer cancel → same; Logout-timeout cancel → `session_logout_timeout` if elapsed, else `expected_t::ok()` (peer replied before cancellation); reconnect-delay sleep cancel → `transport_reconnect_limit_exceeded` if max_attempts exhausted, else loop exits cleanly. No new `session_*_cancelled` variants are defined for v1.0 timer cancellation — cancellation is transparent at the loop level."

## Dependencies & Assumptions

- [x] CHK038 Is the 005 FR-008 amendment text reproduced verbatim — both pre-amendment and post-amendment forms — so reviewers can verify the binding-contract change at audit time? [Completeness, Spec §FR-009] — SPEC-FIXED: Added to spec.md §FR-009: "005 FR-008 amendment (verbatim). PRE-AMENDMENT (005 shipped form): 'System MUST Logout with `session_seqnum_too_low` and disconnect on ANY inbound sequence gap (MsgSeqNum ≠ next_expected_inbound).' POST-AMENDMENT (013 form, replaces the above): 'System MUST Logout with `session_seqnum_too_low` ONLY when `MsgSeqNum < next_expected_inbound` AND `PossDupFlag(43)≠Y`. When `MsgSeqNum > next_expected_inbound`, System MUST enter recovery-active sub-protocol (FR-009..FR-015).' The T048 task amends `specs/005-session-establishment-fsm/spec.md` FR-008 in-place at merge-time."
- [x] CHK039 Is the dependency on 008 `MessageStore::retrieve` documented with its precise contract (ordering, durability guarantee, EOF semantics, dedup behaviour)? [Dependency, Spec §Dependencies] — PASS (anchor-correction 2026-05-28 per BLOCKER spot-check spillover): spec.md §Dependencies states "008 MessageStore (FR-010 outbound replay, FR-015 dedup)"; plan.md:57 cites `[2e §6.1.4]` for the durable-before-transmit linearisation invariant and plan.md:66 cites `[2e §6.3]` for the FileStore durable-write cost. The retrieve() contract surface is anchored at: `[2e §4.1]` (the `MessageStore` interface — line 138-141 declares retrieve()'s signature, the `store_seqnum_gap` (I-19) gap-detection return, and the `store_seqnum_invalid` / `store_invalid_range` validation returns); `[2e §6.1.4]` (cancellation result contract per method — retrieve() row covers EOF / partial-traversal / visitor cancellation propagation); `[2e §6.3]` (FileStore on-disk algorithm — durability of the read path's index snapshot); `[2e §6.4]` (Store-write mutex contract — retrieve() releases the writer mutex before per-frame disk reads per I-03). The earlier disposition's `[2e §4.4]` citation was invented by the auditor (contagion from the FR-012 BLOCKER); `[2e §4.4]` actually defines `MessageStoreFactory`, not retrieve(). Item stays PASS — the dependency is documented correctly in plan.md and data-model.md against the correct anchors; only the audit narrative cited a wrong section.
- [x] CHK040 Is the assumption that 012's `ReconnectPolicy` honours TLS-handshake-failure as a reconnectable event (vs. terminal) explicitly validated, or assumed-silently? [Assumption, Gap] — SPEC-FIXED: Added to spec.md §A.1: "Assumption A.10 (NEW): 012's `ReconnectPolicy` treats ALL transport failures — including TLS handshake failures (`transport_handshake_failed` error variants) — as reconnectable events (increment `attempt_index_`, walk the schedule). Terminal exit occurs only when `attempt_index_ >= max_attempts`. This is consistent with `[2h §4.4]` / `ReconnectPolicy::defaults()` which makes no distinction between connection failure and handshake failure. Validated against the shipped `[2h §4.4]` normative `defaults()` factory and the `contracts/reconnect_fsm.hpp` `drive_reconnect_attempt` error surface."

## Ambiguities & Conflicts

- [x] CHK041 Is the "AwaitingResend is NOT a new `fsm_state` enum value" decision reconciled with FR-035's recent_events observability — does the AwaitingResend entry/exit emit a SessionEvent, or is it observable only via inferred state transitions? [Ambiguity, plan §Constitution Check XIV.2] — SPEC-FIXED: Added to spec.md §FR-009: "AwaitingResend entry/exit are NOT surfaced as distinct SessionEvent variants (they would require a new `session_event_awaiting_resend_entered` / `session_event_awaiting_resend_exited` pair; deferred as YAGNI for v1.0). The AwaitingResend state is observable ONLY via the `fsm_visit_history()` accessor's Active-state entries (the FSM stays in Active — just with the transient flag set) and by the presence of `sequence_numbers_reset` / recovery-completion events in `recent_events()`. Operator tools that need AwaitingResend observability poll `ReconnectFsm::is_awaiting_resend()` (the public accessor per contracts/reconnect_fsm.hpp)."
- [x] CHK042 Is the conflict between FR-016's literal "44" and SC-002's "44 ±N" resolved with a documented count-lock step? [Conflict, Spec §FR-016, §SC-002] — PASS: CHK016 SPEC-FIXED added the explicit count-lock procedure: grep `fixpp gap` markers at /speckit-implement-time; PASS criterion = 0 remaining post-merge; the "44 ±N" is a spec-time estimate, not a binding target. SC-002 already states "actual count is `/speckit-implement`-time cross-checked". No unresolved conflict.

## Post-/speckit-analyze Amendment Items (Pass 2, 2026-05-28)

These items capture the requirement-quality questions raised by today's `/speckit-analyze` second pass (B1, F1/D1, C2). The amendments themselves have already landed; these CHKs let the next `/speckit-checklist-audit` pass disposition the amendment-as-written quality.

- [x] CHK043 Does FR-001 specify which `HeartBtInt` value source applies BEFORE the peer's Logon arrives (operator-configured `SessionConfig::heartbeat_interval` vs. peer-negotiated `HeartBtInt(108)`)? [Clarity, Spec §FR-001] — PASS: spec.md §FR-001 B1 amendment explicitly states: "before the peer's Logon arrives, the peer-negotiated `HeartBtInt(108)` is not yet known; the pre-Logon window therefore uses `SessionConfig::heartbeat_interval` (operator-configured, the same value the System will send in its outbound Logon)." `SessionConfig::heartbeat_interval` is confirmed as `std::optional<std::chrono::seconds>` in `include/fixpp/session/session_config.hpp:144`. Fully specified.
- [x] CHK044 Is the transition from operator-configured to peer-negotiated `HeartBtInt` explicitly tied to the Logon-receipt event in FR-005, with no observable gap between the two? [Consistency, Spec §FR-001 + §FR-005] — SPEC-FIXED: spec.md §FR-001 said "flips to the peer's `HeartBtInt(108)` value on Logon receipt per FR-005" but did not state the flip is atomic with Logon processing (no timer can fire in the gap). Added to spec.md §FR-001: "The flip is ATOMIC with the Logon processing step — the FSM reconfigures the inbound-liveness timer immediately on Logon parse, with no intervening timer firing on the old interval (the timer is rearmed in the same synchronous handler that processes the Logon; no window exists for the old timer to fire after the Logon arrives and before the flip)." Affected: `spec.md:§FR-001`.
- [x] CHK045 Is the QuickFIX-cpp `SessionSettings::heartBtInt`-driven initiator Logon-timeout precedent cited in FR-001's B1 anchor verifiable against a specific reference-engine file/line (e.g., `Session.cpp:141`)? [Traceability, Spec §FR-001] — SPEC-FIXED: spec.md §FR-001 B1 amendment said "Matches QuickFIX-cpp `SessionSettings::heartBtInt`-driven initiator Logon timeout precedent" without citing a specific file/line. Verified at `/home/catalin/Work/Programming/Antreprenoriat/reference-engines/quickfix-cpp/src/C++/Session.cpp:141` — `m_state.logonTimedOut(m_timestamper())` uses the configured heartBtInt for the pre-Logon window; `Session.cpp:161` — `m_state.timedOut(...)` uses peer-negotiated heartBtInt post-Logon. Added both line references to spec.md §FR-001: "`Session.cpp:141` (`logonTimedOut` — pre-Logon window) and `Session.cpp:161` (`timedOut` — post-Logon window)". Affected: `spec.md:§FR-001`.
- [x] CHK046 Does FR-004's error-code reference name the REUSED 005-era slot 74 (`session_test_request_unanswered`) rather than referencing the dropped slot 119 (`session_heartbeat_timeout`)? [Consistency, Spec §FR-004] — PASS: spec.md §FR-004 says "disconnect with `error::session_test_request_unanswered` (slot 74, 005-era reused per F1/D1 2026-05-28 — reuses shipped slot 74 for the same FIX-SL §4.5.2 inbound-silence timeout event...)". The correct slot 74 is named; slot 119 `session_heartbeat_timeout` was dropped and never referenced in FR-004. Consistent.
- [x] CHK047 Does FR-008's error-code reference name the REUSED 005-era slot 73 (`session_logout_timeout`) rather than referencing the dropped slot 118 (`session_logout_disconnect_timeout`)? [Consistency, Spec §FR-008] — PASS: spec.md §FR-008 says "(F1/D1 anchor-added 2026-05-28 per /speckit-analyze — reuses shipped slot 73 (session_logout_timeout) from 005-era for the same Logout(5)-reply timeout event...)". The correct slot 73 is named; no dropped slot 118 reference exists in FR-008. Consistent.
- [x] CHK048 Is the F1/D1 reference-engine sweep summary in spec §A.8 traceable to specific QuickFIX-cpp / QuickFIX-J / Fix8 source files AND line numbers (e.g., `Session.cpp:141`, `Session.java:2011`, `session.cpp:825-851`)? [Traceability, Spec §A.8] — SPEC-FIXED: spec.md §A.8 said "QuickFIX-cpp / QuickFIX-J / Fix8 all use string logs/single callbacks — zero precedent for typed code-level discrimination" without citing file/line references. Verified against reference-engines: QuickFIX-cpp `Session.cpp:141/152/161`, QuickFIX/J `Session.java:2010/2016/2025`, Fix8 `session.cpp:825-851`. Added all three engine cites with file/line to spec.md §A.8. Affected: `spec.md:§A.8`.
- [x] CHK049 Is the `[const §X.2]` ABI no-redefine-spirit anchor invoked in §A.8 anchored to a written constitution principle (not just a slogan)? [Traceability, Spec §A.8] — SPEC-FIXED: `[const §X.2]` resolves in the constitution to Article X §2 — "No C++ symbol leakage through the C ABI" — which is about C ABI symbol visibility, NOT the ABI append-only/no-redefine rule for error codes. The correct anchor is `[const §X.4]`, whose stability clause explicitly states: "once a numeric value is published in a tagged C ABI release, it never changes meaning; new variants append at unused numeric slots within their domain block." Amended all stability/append-only/no-redefine occurrences of `[const §X.2]` to `[const §X.4]` across spec.md (§A.8), tasks.md (T006 × 2, T006a × 1), plan.md (line 30 × 2, line 128, line 222), data-model.md (§Error slot allocation), contracts/session_errors.hpp:5. The two LEGITIMATE remaining `[const §X.2]` occurrences (plan.md:80 `nm` C-ABI symbol-leakage check; tasks.md:167 T055 "no C-ABI surface" hygiene gate) are preserved — those are genuinely about Article X §2's C-symbol-leakage rule. Affected: `spec.md:§A.8`, `tasks.md:T006/T006a`, `plan.md:30/128/222`, `data-model.md`, `contracts/session_errors.hpp`. *(Disposition expanded 2026-05-28 per orchestrator spot-check — original disposition under-reported scope.)*
- [x] CHK050 Does tasks.md T006's "dual-emission comment update on shipped slots 73 and 74" specify the EXACT verbatim-pastable rewrite text AND target line numbers (`error.hpp:294` slot 73, `error.hpp:300` slot 74)? [Clarity, Tasks §T006] — SPEC-FIXED: tasks.md T006 gave verbatim text for slot 73 (`"FR-005 (005 admin path) + 013 FR-008 (FSM-driven Logout-reply timeout) — [FIX-SL §4.6.2] graceful-close"`) but said "rewrite the slot-74 comment along the same lines" — NOT verbatim-pastable. Added to tasks.md T006: verbatim rewrite for `error.hpp:300` (slot 74): `"FR-006 (005 admin path) + 013 FR-004 (FSM-driven inbound-silence timeout, 2×HeartBtInt) — [FIX-SL §4.5.5] inbound silence"`. Target line numbers `error.hpp:294` (slot 73) and `error.hpp:300` (slot 74) verified against live `include/fixpp/core/error.hpp`. Affected: `tasks.md:§T006`.
- [x] CHK051 Are the post-013 slot-73 and slot-74 comment rewrites consistent with each other in parallel form (`"FR-X (005 admin path) + 013 FR-Y (...) — [FIX-SL §...]"`)? [Consistency, Tasks §T006] — PASS (after CHK050 SPEC-FIXED): slot 73 = `"FR-005 (005 admin path) + 013 FR-008 (FSM-driven Logout-reply timeout) — [FIX-SL §4.6.2] graceful-close"`; slot 74 = `"FR-006 (005 admin path) + 013 FR-004 (FSM-driven inbound-silence timeout, 2×HeartBtInt) — [FIX-SL §4.5.5] inbound silence"`. Both follow the `"FR-X (005 admin path) + 013 FR-Y (FSM-driven ...) — [FIX-SL §...]"` template. Consistent parallel form.

## Notes

- Check items off as completed: `[x]`. SPEC-FIXED dispositions go inline next to the item; DD-DECIDED items cite the design-doc anchor; WAIVED items name the reason.
- Items 010 / 016 / 025 / 042 frame the "44 ±N" `/speckit-implement`-time lock-in obligation — required per `[[project_2e_recovery_v1_upgrade_obligation]]` and `[[feedback_simplify_pass_catches_9th_burn]]`.
- Items 005 / 006 / 028 / 031–033 are the recovery-path edge gaps the spec's §Edge Cases hasn't covered explicitly; treat as candidate FR amendments at /speckit-checklist-audit.
- Items 043–051 (Pass 2 amendment items, 2026-05-28) await disposition at the next `/speckit-checklist-audit` pass; they test the quality of the F1/D1 + B1 + C2 amendments themselves, not pre-existing spec gaps.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 17 |
| SPEC-FIXED | 34 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **51** |

### SPEC-FIXED items
- CHK002 — enumerated allowed outbound message classes during AwaitingResend; affected: `spec.md:§FR-009`.
- CHK003 — specified inbound-buffering model during AwaitingResend (unbounded in-memory per block-mode); affected: `spec.md:§FR-009`.
- CHK005 — specified ResendRequest timeout (heartbeat-timeout subsumes it); affected: `spec.md:§Edge Cases`.
- CHK006 — specified concurrent-ResendRequest handling (most-recent-range supersedes); affected: `spec.md:§Edge Cases`.
- CHK007 — specified replayed-message-downstream-failure policy; affected: `spec.md:§Edge Cases`.
- CHK008 — enumerated exhaustive "admin message" list for FR-011 GapFill collapse; affected: `spec.md:§FR-011`.
- CHK009 — RE-DISPOSITIONED 2026-05-28 (BLOCKER Option B): defined store horizon as `M = MessageStore::retrieve()`-probed earliest retained seqnum within `[BeginSeqNo, our_last_outbound]`; no new 008 accessor added; dangling `[2e §4.4]` anchor removed. Affected: `spec.md:§FR-012`.
- CHK010 — corrected production-code 2e-recovery marker count from 4 to 2 with file-path specifics; affected: `spec.md:§FR-016`.
- CHK011 — specified fractional-rounding and precision for 1.2× HeartBtInt computation; affected: `spec.md:§FR-004`.
- CHK012 — clarified 2×HeartBtInt threshold as cumulative from last inbound; affected: `spec.md:§FR-004`.
- CHK013 — clarified OrigSendingTime(122) source as stored SendingTime from MessageStore; affected: `spec.md:§FR-010`.
- CHK015 — clarified TLS session-resumption policy (permitted); affected: `spec.md:§FR-002`.
- CHK016 — documented count-lock step for 44 ±N gap markers at /implement-time; affected: `spec.md:§FR-016`.
- CHK018 — clarified heartbeat-timeout feeds ReconnectPolicy walk; affected: `spec.md:§FR-004`.
- CHK019 — specified GapFill-vs-PossDup overlap resolution (GapFill wins); affected: `spec.md:§FR-013`.
- CHK020 — clarified forced-reset emits sequence_numbers_reset + log warning; affected: `spec.md:§FR-014`.
- CHK022 — added synthetic substitute for SC-001 ≥1-month target; affected: `spec.md:§SC-001`.
- CHK023 — added synthetic substitute for SC-005 recovery-throughput target; affected: `spec.md:§SC-005`.
- CHK024 — defined SC-004 cadence-accuracy measurement scenario; affected: `spec.md:§SC-004`.
- CHK026 — RE-DISPOSITIONED 2026-05-28 (BLOCKER Option B): combined store-horizon + beyond-last-outbound edge restated using `M` from Option B FR-012; algorithm unchanged. Affected: `spec.md:§FR-012`.
- CHK028 — specified implicit Logon-arrival timeout via HeartBtInt cadence; affected: `spec.md:§FR-001`.
- CHK029 — specified ResetSeqNumFlag × recovery-cycle Logon interaction; affected: `spec.md:§FR-017`.
- CHK030 — specified strict-seqnum inbound delivery ordering during AwaitingResend; affected: `spec.md:§FR-009`.
- CHK031 — specified malformed ResendRequest handling (Reject + clamp); affected: `spec.md:§Edge Cases`.
- CHK032 — specified empty-MessageStore ResendRequest handling (SequenceReset-GapFill); affected: `spec.md:§Edge Cases`.
- CHK033 — specified PossDupFlag=Y with mismatched SendingTime policy (log warning, process); affected: `spec.md:§Edge Cases`.
- CHK037 — enumerated cancellation variant mappings per timer/operation; affected: `spec.md:§Constraints`.
- CHK038 — reproduced 005 FR-008 pre/post amendment verbatim; affected: `spec.md:§FR-009`.
- CHK040 — added A.10 assumption that TLS handshake failure is reconnectable; affected: `spec.md:§Assumptions`.
- CHK041 — clarified AwaitingResend observability (is_awaiting_resend() accessor; no SessionEvent); affected: `spec.md:§FR-009`.

### DD-DECIDED items
(none)

### WAIVED items
(none)

### Pass 2 (2026-05-28) new SPEC-FIXED items
- CHK044 — atomic HeartBtInt flip: timer rearmed in same synchronous Logon handler (no observable gap); affected: `spec.md:§FR-001`.
- CHK045 — QuickFIX-cpp line refs `Session.cpp:141/161` added for B1 anchor; affected: `spec.md:§FR-001`.
- CHK048 — reference-engine line refs added to §A.8 (QuickFIX-cpp :141/:152/:161, QuickFIX/J :2010/:2016/:2025, Fix8 :825-851); affected: `spec.md:§A.8`.
- CHK049 — `[const §X.2]` corrected to `[const §X.4]` (Article X §4 stability clause) at all stability/no-redefine sites; affected: `spec.md:§A.8`, `tasks.md:T006/T006a`, `plan.md:30/128/222`, `data-model.md`, `contracts/session_errors.hpp`. Legitimate §X.2 retained at plan.md:80 (`nm` C-ABI symbol-leakage check) and tasks.md:167 (T055 "no C-ABI surface" hygiene).
- CHK050 — verbatim slot-74 comment rewrite added to tasks.md T006; affected: `tasks.md:§T006`.

Pass 2 PASS items: CHK043, CHK046, CHK047, CHK051.

Anchors spot-verified (Pass 2, 2026-05-28): `[2h §4.4]` (2h-transport.md §4.4 ReconnectPolicy shape — confirmed); `[const §X.4]` (constitution.md Article X §4 stability clause — confirmed: "once a numeric value is published in a tagged C ABI release, it never changes meaning; new variants append at unused numeric slots"); `[FIX-SL §4.5.2]` (external FIX Session Layer spec — verified as used consistently in research.md §4.5.2 context; not verifiable against a file in-repo but referenced in research.md line 204 context for §4.6); `[const §XII.5]` (constitution.md Article XII §5 — confirmed: "`Session` construction requires an explicit `SecurityProfile` choice — there is no implicit default"); reference-engine line refs verified: QuickFIX-cpp `Session.cpp:141` = `logonTimedOut`, `Session.cpp:152` = `logoutTimedOut`, `Session.cpp:161` = `timedOut`; QuickFIX/J `Session.java:2010` = `isLogonTimedOut`, `Session.java:2016` = `isLogoutTimedOut`, `Session.java:2025` = `isTimedOut`; Fix8 `session.cpp:825-851` = 1.2×HeartBtInt test-request / 2×HeartBtInt abort block. All resolve. **Anchor correction from Pass 1**: `[const §X.2]` (Article X §2 = C-ABI symbol leakage) was incorrect; corrected to `[const §X.4]` throughout spec.md §A.8 and tasks.md T006. **Pass 1 anchor correction**: `[const §VI]` (Article VI = Spec Coverage Discipline) was used as "security-default-deny" — incorrect; corrected to `[const §XII.5]` (Article XII §5 = no-implicit-default SecurityProfile) throughout spec.md.
