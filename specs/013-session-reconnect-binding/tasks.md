# Tasks: 013 — Session Reconnect FSM + Recovery + CompID↔TLS-Identity Binding

**Input**: Design documents from `specs/013-session-reconnect-binding/`
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/, quickstart.md
**Design anchors**: NO Phase-2 design doc of its own — session-module surface is split across `architecture.md §4.4` + `005-session-establishment-fsm` (6-state `fsm_state` ground truth) + `010-session-cfg-lifetime` (`SessionConfig` value type + W-5 `static_assert(std::is_copy_constructible_v<SessionConfig>)` + F-04 `fsm_visit_history()`) + `.specify/2g-tls.md` v0.4 (`verify_peer` + 6-cell `error::tls_*` + `last_handshake_sub_reason`) + `.specify/2h-transport.md` v0.3 (`Transport` + `TransportFactory` + `ReconnectPolicy` + `handshake_result.peer_id` + Appendix D §D.2 `transport_factory_override` reservation) + `.specify/2j-controlplane.md` v0.2 (`[2j §3.12]` IN-PROCESS reload reservation) + `.specify/2d-threading.md` v0.4 + `.specify/2e-msgstore.md` v0.4 + `.specify/2a-decimal.md` v0.3 + `constitution.md` v0.2 + `architecture.md` v0.2. Phase-4 Gate A converged (`research/reviews/codex_013-session-reconnect-binding_gate_a_*.md` + `research/reviews/opus_013-session-reconnect-binding_gate_a_*_adversarial_review.md`; 3 rounds + 1 hand-edit pass per Step F option 2; record at `library/.specify/decisions/013-session-reconnect-binding-gatea.md` + parent `decisions/phase-4/013-session-reconnect-binding-gatea.md` per `[[project_gate_records_layout]]`).

**Tests**: TDD is mandatory per `[const §VII.1]` / `[const §VII.3]`. The test list is the **10 named tests + 1 libFuzzer harness + 1 perf alloc-guard + 2 benches** re-emitted by NAME from plan.md §Project Structure + Phase 1 Test Plan (NOT re-derived from FRs) per `[[feedback_speckit_pipeline_order_gate_a_before_tasks]]`. The `test_heartbeat_cadence_8cell.cpp` matrix (8 cells = 4 cadence axes × 2 executor modes per `[2d §4.8]`) + `test_reset_seqnum_policy_matrix.cpp` 3-mode × 2-role matrix + `test_tls_validation_failed_taxonomy.cpp` 6-master-enum + 3-sub_reason taxonomy cells + `test_session_invariant_counter_witness.cpp` (per `[[feedback_half_restructure_symmetric_api]]`) are pinned by their plan.md row.

**Organization**: 4 user stories from spec.md — US1 (P1, MVP: Recovery-active reconnect after transient disconnect), US2 (P1 co-equal: CompID↔TLS-identity authorization at Logon), US3 (P2: TLS validation outcome as session-level event), US4 (P3: In-process credential rotation without session restart). Foundational types `error::session_*` new slots 116..119 (plus reuse of 005-era slots 73/74 per F1/D1 2026-05-28) + `SessionEvent` variant + `bound_principal` + `CompIdAuthorizationPolicy` value-type skeleton + 4 new SessionConfig fields + `TransportFactory::reload_credentials` pure-virtual extension + `cert_source_snapshot()` accessor block all stories.

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Maps task to a user story (US1, US2, US3, US4)
- File paths are relative to the **library submodule root** (`research/G19-fix-fpml-iso20022/library/`)

## Path Conventions

`include/fixpp/session/`, `include/fixpp/transport/`, `include/fixpp/core/`, `src/session/`, `src/transport/`, `tests/session/`, `tests/perf/`, `tests/fuzz/`, `bench/session/`, `spec/`, `docs/src/`. EXTENDED module — `session/` is already registered with `tools/check_layers.py`; no new module directory. 4 new headers + 3 extended headers under `include/fixpp/session/`; 4 new `.cpp` + 2 extended under `src/session/`; 1 extended header + 1 extended `.cpp` under `include/fixpp/transport/` + `src/transport/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Verify pre-conditions (all inbound bindings merged), confirm directory tree exists, ensure CMake wiring slots are reachable. No production code authored in this phase.

- [X] T001 Verify all inbound preconditions per Assumption A.1: 005-session-establishment-fsm + 009-session-fsm-finalize (PR #82 squash `ba2222d`), 010-session-cfg-lifetime (PR #83 squash `25cf09d`), 011-tls-policy (PR #84 squash `67448c0`), 012-2h-transport (PR #85 squash `53e25b1`) all merged into `main`. Confirm branch base is post-PR-#85 `main` (`git merge-base 013-session-reconnect-binding main` resolves to `53e25b1` or descendant). Confirm submodule pointer at parent matches `8fdc12c` or descendant. Confirm production-code `2e-recovery` marker baseline via `grep -rn "2e-recovery" include/ src/ tests/` — live grep at 2026-05-28 (per `/speckit-analyze` F1 closure): **2 production-code markers** (`src/session/session.cpp` + `tests/session/fsm_matrix_witness_test.cpp`); the additional markers in `specs/010-session-cfg-lifetime/simplify-fix-plan.md` are historical planning-doc references, NOT production markers, and remain untouched. Memory entry `[[project_2e_recovery_v1_upgrade_obligation]]` says "4 in-code markers" because it counted both scopes; the binding production-code count is 2. Record this baseline for FR-016 closure tracking at MERGE-time.
- [X] T002 [P] Confirm `include/fixpp/session/` + `src/session/` + `tests/session/` + `tests/perf/` + `tests/fuzz/` + `bench/session/` directory trees exist (all already present from 005..012); verify `tools/check_layers.py` already registers `session` module; no new module directory needed. Run `python tools/check_layers.py` baseline PASS before any 013 edit (load-bearing — must remain PASS after every Phase 3+ commit).
- [X] T003 [P] Confirm CMake wiring slots reachable: `src/session/CMakeLists.txt`, `tests/session/CMakeLists.txt`, `tests/perf/CMakeLists.txt`, `tests/fuzz/CMakeLists.txt`, `bench/CMakeLists.txt` (parent that walks subdirs). The 4 new `src/session/*.cpp` files + new test/bench files will be appended in their respective Phase rows. No new top-level `add_subdirectory` calls required (all targets exist).
- [X] T004 [P] Confirm `Conan` baseline rows unchanged: `asio/1.36.0`, `openssl/3.6.2`, `gtest/1.17.0`, `benchmark/1.9.5`. No new Conan rows for 013 per plan.md §Primary Dependencies (`No new Conan rows.`). Verify `conanfile.py` head untouched by 013.
- [X] T005 Confirm cross-feature fixture reuse — 011's `tests/tls/fixtures/` PEM/DER set is reachable from `tests/session/` test binaries for the CompID-binding + TLS-validation-event + reload-credentials cells; no new fixtures authored at this phase (fixtures used at-site by Phase 5/6/7 implementation tasks).

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Ship the shared types every user story consumes — the 4 new `error::session_*` variants (T006; plus reuse of 005-era slots 73/74 per F1/D1 2026-05-28) + delete the orphaned slot 70 (T006a, Option D per orchestrator decision 2026-05-28) + the `SessionEvent` variant union (NEW public type) + the `bound_principal` value + the `CompIdAuthorizationPolicy` skeleton + the 4 new `SessionConfig` fields + the `TransportFactory::reload_credentials` pure-virtual + the `cert_source_snapshot()` accessor + the `SessionConfig` static_assert preservation + **the cross-story event-sink skeleton (T013a) — `Session::recent_events_` ring field + `Session::emit_event(...)` private method declaration + `ListenerEvents` pre-Session ring type (Option A pin per FR-028)**. Per data-model E-3..E-7 + §Error slot allocation.

**⚠️ CRITICAL**: No user-story work can begin until this phase completes. The `error::session_*` slots + `SessionEvent` shape + `CompIdAuthorizationPolicy` skeleton + `TransportFactory::reload_credentials` + `Session::emit_event` write path + `ListenerEvents` pre-Session sink are consumed by US1/US2/US3/US4 tests AND implementations.

- [X] T006 Append **4 new `error::session_*` variants** to `include/fixpp/core/error.hpp` at the contiguous block 116..119 immediately after 012's `transport_*` block (which occupies 94..115 per shipped post-PR-#85 header — `transport_accept_cancelled = 115` per `[2h §6.6]:1199`). Names per data-model §Error slot allocation: `session_seqnum_reset_mismatch = 116` (FR-017 / US1 AC7); `session_compid_unauthorized = 117` (FR-021 / US2 AC2); `session_testreqid_mismatch = 118` (FR-006); `session_invalid_argument = 119` (FR-033 — runtime nullptr rejection on `reload_credentials`; added 2026-05-28 per `/speckit-analyze` C3 resolution; renumbered from 121 to 119 per F1/D1 2026-05-28; no existing `session_invalid_*` slot fits — 66 FIX-Logon-shape, 76 config-time, 77 FSM-state). FR-008 and FR-004 REUSE shipped 005-era slots 73 (`session_logout_timeout`) and 74 (`session_test_request_unanswered`) per F1/D1 resolution 2026-05-28 — reference-engine sweep across QuickFIX-cpp / QuickFIX-J / Fix8 confirmed zero precedent for typed code-level discrimination of these timeout classes; adding new slots alongside shipped 73/74 for the same events would violate `[const §X.4]` ABI no-redefine spirit. **/implement-time reconciliation rule** (data-model.md §Error slot allocation): cross-check the actual `error.hpp` post-PR-#85 boundary remains at slot 115; if a 012 carry-forward waiver-close has shifted the boundary by ±1, slide the 013 block accordingly without re-running Gate A; NEVER renumber existing slots (post-v1.0 GA invariant per `[const §X.4]`). F1/D1 resolution 2026-05-28 per /speckit-analyze: slots 73 (session_logout_timeout) and 74 (session_test_request_unanswered) are REUSED from 005-era for FR-004 and FR-008 emissions (same FIX-SL §4.5 events); reference-engine sweep across QuickFIX-cpp / QuickFIX-J / Fix8 confirmed zero precedent for typed code-level discrimination; new-slot count is now 4 (slots 116..119) instead of 6. **Dual-emission comment update on shipped slots 73 and 74** (C2 anchor-added 2026-05-28 per `/speckit-analyze` second pass): update the comments on `error.hpp:294` (slot 73) and `error.hpp:300` (slot 74) to document the dual-emission semantic — slot 73 was originally `"FR-005, [FIX-SL §4.6.2] — graceful-close"` (005-era admin path); post-013 it is ALSO emitted by the FR-008 FSM-driven Logout-reply-timeout path; rewrite to `"FR-005 (005 admin path) + 013 FR-008 (FSM-driven Logout-reply timeout) — [FIX-SL §4.6.2] graceful-close"`. Slot 74 similarly: post-013 it is ALSO emitted by FR-004's `2 × HeartBtInt` cumulative-silence path; verbatim rewrite for `error.hpp:300` (slot 74): `"FR-006 (005 admin path) + 013 FR-004 (FSM-driven inbound-silence timeout, 2×HeartBtInt) — [FIX-SL §4.5.5] inbound silence"` (parallel form: `"FR-X (005 admin path) + 013 FR-Y (FSM-driven ...) — [FIX-SL §...]"` — same template as slot 73). *(CHK050 SPEC-FIXED 2026-05-28 — verbatim text added for slot 74; CHK051 confirmed consistent parallel form.)* Preserves `[const §X.4]` ABI comment-quality (Article X §4 stability rule — the comment documents every binding emission site; anchor corrected from §X.2 per CHK049 2026-05-28).
- [X] T006a **Delete pre-release `error::session_seqnum_gap_unrecoverable = 70` outright** (Option D per orchestrator decision 2026-05-28 closing `/speckit-analyze` F5). Pre-v1.0 GA — no consumer ABI contract exists; `[const §X.4]` append-only ABI discipline activates at v1.0 release, not before. The slot's reason for existing (`"Real ResendRequest-driven recovery is the deferred session-recovery feature"`) is discharged by 013 FR-009. Step 1: audit every reference via `grep -rn "session_seqnum_gap_unrecoverable" include/ src/ tests/` + `codegraph_callers` on the variant; record emit-sites + catch-sites. Step 2: migrate each emit-site to the FR-009 recovery-active path (which T023..T026 implement anyway — the old fatal-disconnect emit becomes `ReconnectFsm::enter_awaiting_resend(...)` + ResendRequest issue); update each catch-site to either match the new error variants (`session_seqnum_reset_mismatch` slot 116, `session_test_request_unanswered` slot 74 (reused 005-era), etc.) or delete if no longer reachable. Step 3: remove the variant from `error.hpp`. **Leave slot 70 as a numeric hole** — do NOT renumber slots 71..115 down (would ripple through 22 `transport_*` variants 94..115 + 6 `tls_*` variants 88..93 + 013-new slots 116..119 + every test + every C-ABI macro). **Do NOT fill the gap with a 013-new variant** — keep the 116..119 block exactly as plan.md / data-model.md §Error slot allocation / spec.md A.8 specify. Step 4: verify post-delete `grep -rn "session_seqnum_gap_unrecoverable" include/ src/ tests/` returns 0; CI GREEN under Clang Debug + ASan + UBSan + TSan + GCC Release per `[const §IX.2]`.
- [X] T007 [P] Create `include/fixpp/session/session_event.hpp` declaring the **NEW public variant union** introduced by this feature (NOT an extension of 010 F-04 per Assumption A.3 + RC#1 closure). Declare the 5 alternative struct types per data-model E-5 + contracts/session_event.hpp using the `session_event_` prefix per "Naming convention" lock (`session_event_peer_identity_bound`, `session_event_compid_authorization_failed`, `session_event_tls_validation_failed{code: core::error, sub_reason: std::string_view, peer_endpoint: std::string_view, reason_string: std::string_view}`, `session_event_credentials_rotated`, `session_event_sequence_numbers_reset{by_peer_request: bool}`). Declare `using SessionEvent = std::variant<...>` with the 5 prefixed names in the published order. Apply `[[clang::lifetimebound]]` on every `string_view` / `span` field per data-model E-5 lifetime rules. Contents re-emit verbatim from `contracts/session_event.hpp`.
- [X] T008 [P] Create `include/fixpp/session/compid_authorization_policy.hpp` declaring the `bound_principal` value type + `CompIdAuthorizationPolicy` class skeleton per data-model E-3 + contracts/compid_authorization_policy.hpp. Public API: default + PMR-aware constructors; **COPY-CONSTRUCTIBLE per 010 W-5** (`include/fixpp/session/session_config.hpp:176-180` `static_assert(std::is_copy_constructible_v<SessionConfig>)`) — declare copy/move ctor + assignment + destructor; `add_binding(std::string_view principal, std::string_view compid)` (throws at construction time per `[arch §5.3]` carve-out on empty strings); `authorize(fixpp::tls::peer_identity const& pid, std::string_view asserted_compid) const noexcept -> expected_t<bound_principal>` (`[[nodiscard]]`). Storage type per data-model E-3: `std::pmr::unordered_map<std::pmr::string, std::pmr::flat_set<std::pmr::string>>`. Contents re-emit verbatim from `contracts/compid_authorization_policy.hpp`.
- [X] T009 [P] Create `include/fixpp/session/resend_state.hpp` declaring the `ResendState` POD-like value type per data-model E-2 + contracts/resend_state.hpp: fields `outstanding_begin_`, `outstanding_end_`, `started_at_`, `inbound_filled_through_`, `outbound_replay_cursor_`. Validation rules: `outstanding_begin_ <= outstanding_end_ OR outstanding_end_ == 0`. No allocations (embedded in `ReconnectFsm`). Contents re-emit verbatim from `contracts/resend_state.hpp`.
- [X] T010 [P] Create `include/fixpp/session/reconnect_fsm.hpp` declaring the `ReconnectFsm` driver class per data-model E-1 + contracts/reconnect_fsm.hpp. Fields per E-1 table: `policy_` (by-value), `attempt_index_`, `factory_` (NON-OWNING `TransportFactory*`), `heartbeat_timer_`, `test_request_timer_`, `logout_timer_`, `awaiting_resend_` (transient bool flag NOT a new `fsm_state` value per D-1), `resend_state_` (E-2), `last_outbound_testreqid_`. NOTE in the file header: the `awaiting_resend_` flag preserves the 6-state `fsm_state` enum's ABI per `[arch §5.6]` frozen-at-open rule; ANY 7th `fsm_state` value would break 010 F-04 consumers. Contents re-emit verbatim from `contracts/reconnect_fsm.hpp`.
- [X] T011 Extend `include/fixpp/session/session_config.hpp` with the **4 new fields** per data-model E-4 + `[arch §5.6]` frozen-at-open carve-out + 2h Appendix D §D.2 reservation: (a) `reset_seqnum_policy: enum class reset_seqnum_policy : std::uint8_t { bilateral_strict, bilateral_lenient, unilateral } = bilateral_strict` per D-6 / FR-017 / Clarifications Q1=A; (b) `logout_disconnect_timeout_ms: std::uint32_t = 2000` per D-13 / FR-008 / Clarifications Q5=A; (c) `compid_authorization_policy: CompIdAuthorizationPolicy` (default-constructed = empty allow-list = default-deny per D-9 / FR-023); (d) `transport_factory_override: std::shared_ptr<fixpp::transport::TransportFactory> = nullptr` per 2h Appendix D §D.2 reservation. **Ownership reconciliation** (data-model E-4): storage is `std::shared_ptr<TransportFactory>` (per 010 FR-001a `store_factory` precedent — unique_ptr→shared_ptr for SessionConfig-copy semantics; 2h "no factory shared across Sessions" invariant preserved via Session::open-time hygiene assertion authored in T030). Verify `static_assert(std::is_copy_constructible_v<SessionConfig>)` at line 176-180 still PASSES post-extension (load-bearing — break = compile fail; W-5 invariant). Validation rules per E-4: `logout_disconnect_timeout_ms > 0` validated at SessionConfig-build time (`[arch §5.3]` permits construction-time throw); `compid_authorization_policy` MAY be empty; `transport_factory_override` MAY be nullptr.
- [X] T012 [P] Extend `include/fixpp/transport/transport_factory.hpp` to add the **new pure-virtual** `reload_credentials(std::shared_ptr<fixpp::tls::cert_source> new_source) noexcept -> core::expected_t<void>` on the abstract `TransportFactory` base per data-model E-7 + FR-030 / FR-033 / D-11. The factory IS the symmetric authority for initiator + acceptor halves per `[[feedback_half_restructure_symmetric_api]]`. `TransportFactory` pure-virtual count moves 1 → 2 (was just `make`; +1 for `reload_credentials`); still under `[const §XIV.2]` 5/5 cap. Add file-header comment documenting the atomic-swap contract (atomic STORE on factory-internal `std::atomic<std::shared_ptr<cert_source>> cert_source_slot_`; O(1) strand-free; rejects nullptr → `error::session_invalid_argument` (slot 119 per T006, renumbered from 121 per F1/D1 2026-05-28) per data-model E-6 validation rule). Update concrete `asio_tls_transport_factory` class declaration (in same header per 012 shipped layout) to declare the override + add the 013-introduced `cert_source_slot_: std::atomic<std::shared_ptr<fixpp::tls::cert_source>>` private field + the public `cert_source_snapshot() const noexcept -> std::shared_ptr<fixpp::tls::cert_source>` accessor (returns strong-ref by-value per `[[feedback_weak_ptr_cache_needs_owning_context]]` — NEVER raw pointer or weak_ptr). **Additionally update the stale Appendix D §D.2 reservation comment at `transport_factory.hpp:L156-164`** (closes `/speckit-analyze` F4 2026-05-28) — the shipped 012 comment declares `transport_factory_override` as `std::unique_ptr<TransportFactory>`; rewrite to `std::shared_ptr<TransportFactory>` reflecting the 013 ownership reconciliation per data-model E-4 "Ownership reconciliation" (010 FR-001a precedent: shared_ptr for SessionConfig-copy semantics; 2h "no factory shared across Sessions" invariant preserved via `Session::open`-time hygiene assertion in T044, NOT by storage type).
- [X] T013 [P] Wire foundational headers (`session_event.hpp`, `compid_authorization_policy.hpp`, `resend_state.hpp`, `reconnect_fsm.hpp`, extended `session_config.hpp`, extended `transport_factory.hpp`, NEW `listener_events.hpp` per T013a) into `include/fixpp/session/CMakeLists.txt` and `include/fixpp/transport/CMakeLists.txt` interface targets (header-only at this phase); run `python tools/check_layers.py` and confirm PASS (no module boundary violations — `session/` already registered; `transport/` extension stays within `transport/` module); run `cmake --build` and confirm clean compile under Clang Debug + GCC Release sanity.
- [X] T013a [P] **Author the cross-story event-sink skeleton** so US1 (T027 emit `sequence_numbers_reset`), US2 (T036 emit `peer_identity_bound` / `compid_authorization_failed`), US4 (T043 emit `credentials_rotated`) can write to a Session-side ring buffer without depending on Phase 5 artifacts — closes `/speckit-analyze` F3 / F6 / B1 / C2 cross-story dependency 2026-05-28 + locks in **FR-028 Option A — `ListenerEvents` ring** (orchestrator decision 2026-05-28; alternative "post-Session replay" rejected as adding state-machine complexity without public-API benefit). Two parts:
  - **(a) Extend `include/fixpp/session/session.hpp`** to add `Session::recent_events_` private fixed-capacity ring field (capacity 16 per data-model E-6 + 010 F-04 `fsm_visit_history()` shape parallel) + `Session::emit_event(SessionEvent) noexcept` private method declaration (write path; per-session strand-serialised per `[const §XI.4]`) + `Session::recent_events() const noexcept -> std::span<const SessionEvent>` public accessor declaration (body implemented in T040 Phase 5). US1/US2/US4 implementations (T027 / T036 / T043) call `emit_event(...)` to write; the public `recent_events()` accessor read-side body lands in T040. **Pin the ring capacity constant** (closes `/speckit-analyze` E1 2026-05-28): declare `inline constexpr std::size_t kSessionEventRingCapacity = 16;` in `session.hpp` (or in `session_event.hpp`), use that constant for the `Session::recent_events_` declaration AND for the `ListenerEvents` template parameter in (b) below, AND add `static_assert(kSessionEventRingCapacity == 16, "FR-035 v1.0 contract: ring capacity is 16 entries; future capacity changes require a spec amendment + Gate A re-review");` to guard the constant against silent drift (parallel to FR-026's `static_assert(tls_handshake_failed == 88)` discipline).
  - **(b) Create `include/fixpp/transport/listener_events.hpp`** declaring the `ListenerEvents` ring buffer type — the pre-Session event-channel sink per FR-028 Option A. Shape: `class ListenerEvents { public: void emit(SessionEvent) noexcept; [[nodiscard]] std::span<const SessionEvent> recent_events() const noexcept; private: <ring<SessionEvent, kSessionEventRingCapacity>> events_; };` — reuses the `kSessionEventRingCapacity` constant from (a) so both Session and Listener rings stay in sync. `ListenerEvents` is OWNED by `asio_listener::Config` (parallel to existing `accepted_transport_config` field per 012 shipped layout); its address is published to the OpenSSL `verify_peer_trampoline` via `SSL_set_ex_data` at `async_accept`-time so the trampoline (T039) can `emit(...)` without any Session instance existing. T038 (taxonomy test) reads via `Listener::recent_events()` parallel accessor. Document the lifetime invariant: `ListenerEvents` outlives every transient pre-Session `verify_peer` callback (owned by the listener's Config which outlives every accept attempt per `[arch §5.6]` frozen-at-open).

**Checkpoint**: Foundation ready — US1 / US2 / US3 / US4 can now begin in parallel (each consumes the `error::session_*` slots, `SessionEvent`, `CompIdAuthorizationPolicy` skeleton, 4 new `SessionConfig` fields, `TransportFactory::reload_credentials` pure-virtual, `Session::emit_event` write path + `recent_events_` ring field, and `ListenerEvents` pre-Session sink).

---

## Phase 3: User Story 1 — Recovery-active reconnect after transient disconnect (Priority: P1) 🎯 MVP

**Goal**: Ship the `ReconnectFsm` driver layer + the recovery sub-protocol (ResendRequest issue + reply, SequenceReset-GapFill admin-span collapse, store-horizon edge, PossDupFlag dedup) + the 3-mode `ResetSeqNumFlag(141)` handshake matrix + Heartbeat/TestRequest cadence + initiator-graceful Logout. Discharges the 2e-recovery upgrade obligation (amends 005 FR-008 from fatal-disconnect to recovery-active) and unblocks the 014 interop harness.

**Independent Test** (per spec.md US1 Independent Test): Drive a Logon-Resend-Active round-trip against `mock_transport` (deterministic; no OpenSSL / ASIO networking) where the peer scripts a 3-message inbound gap; verify `ResendRequest` issue, peer replay with `PossDupFlag(43)=Y`, and Active state without operator intervention. Live-engine matrix (QFC / QFJ / Fix8) deferred to 014 interop harness per `[[project_release_interop_quickfix_fix8]]`.

### Tests for User Story 1 (write FIRST — must FAIL before implementation)

- [X] T014 [P] [US1] Author `tests/session/test_reconnect_happy_path.cpp` per plan.md test plan row (`FR-001 / FR-002 / US1 AC1`) — initiator reconnect after transient disconnect; FSM destroys dead Transport → walks `ReconnectPolicy` schedule → mints fresh Transport via `TransportFactory::make(...)` per attempt → re-Logon → issues `ResendRequest{BeginSeqNo, EndSeqNo}` for missing-inbound range → receives replayed messages → reaches Active. Drives bytes through FSM via `mock_transport::Script` (no OpenSSL / ASIO networking per `[[project_005_phase8_completeness_false_pass]]` production-shaped exercise rule + FR-038).
- [X] T015 [P] [US1] Author `tests/session/test_recovery_admin_span_gapfill.cpp` (FR-011 / Edge case "all-admin span collapse") — peer asks us to resend `[10..20]` where every message in the span is admin (Heartbeat / TestRequest); verify we emit ONE `SequenceReset{NewSeqNo=21, GapFillFlag(123)=Y}` rather than re-sending admin bodies (FIX-SL §4.3.5 admin-replay-forbidden); witness against `mock_transport::Script::expected_outbound_writes`.
- [X] T016 [P] [US1] Author `tests/session/test_recovery_store_horizon.cpp` (FR-012 / Edge case "store-horizon") — peer asks us to resend `[10..20]` but our 008 MessageStore only has `[15..20]`; verify `SequenceReset{NewSeqNo=15, GapFillFlag=Y}` emitted FIRST then replay `[15..20]` with `PossDupFlag(43)=Y` + `OrigSendingTime(122)`; witness against `mock_transport::Script::expected_outbound_writes`.
- [X] T017 [P] [US1] Author `tests/session/test_reset_seqnum_policy_matrix.cpp` (FR-017 / US1 AC7 / Clarifications Q1=A) — **6 cells** = 3 modes (`bilateral_strict`, `bilateral_lenient`, `unilateral`) × 2 roles (initiator, acceptor) per `[[feedback_half_restructure_symmetric_api]]`. `bilateral_strict` mismatch surfaces `session_seqnum_reset_mismatch` (slot 116 per T006); `bilateral_lenient` auto-mirrors `141=Y`; `unilateral` honours unconditionally; success advances `next_expected_inbound` + `next_expected_outbound` to 1 + emits `session_event_sequence_numbers_reset{by_peer_request}`. Production-shaped: drive Logon bytes through `mock_transport::Script::inbound_bytes` for ALL 6 cells; assert FSM behaviour from outbound writes + `Session::recent_events()` ring.
- [X] T018 [P] [US1] Author `tests/session/test_heartbeat_cadence_8cell.cpp` (FR-003 / FR-004 / FR-005 / FR-006 / FR-007 / US1 AC4) — **8 cells** = 4 cadence axes {emit-Heartbeat, emit-TestRequest, heartbeat_timeout, TestReqID-echo} × 2 executor modes {`per_session_strand`, `direct_executor`} per `[2d §4.8]`. Use `mock_clock` (007) for deterministic time advance; assert outbound `Heartbeat(0)` / `TestRequest(1)` admin builders fire at correct boundaries; assert `error::session_test_request_unanswered` (slot 74, 005-era reused per F1/D1 2026-05-28) surfaces after `2 × HeartBtInt`; assert `error::session_testreqid_mismatch` (slot 118) on mismatched echo. Apply `[[feedback_subagent_phase_verification_two_traps]]` (no `SUCCEED()` placeholders — body audits).
- [X] T019 [P] [US1] Author `tests/session/test_logout_timeout.cpp` (FR-008 / US1 AC5) — **symmetric cells per `[[feedback_half_restructure_symmetric_api]]`**: (a) initiator-graceful `Session::logout(timeout)` emits `Logout(5)`, awaits peer reply for `logout_disconnect_timeout_ms` (default 2000 ms per D-13 / Clarifications Q5=A), closes Transport, transitions to Disconnected; (b) timeout-elapsed cell surfaces `error::session_logout_timeout` (slot 73, 005-era reused per F1/D1 2026-05-28); (c) acceptor-force-disconnect cell (peer-initiated Logout reply within timeout). Drive via `mock_clock` + `mock_transport::Script`.
- [X] T020 [P] [US1] Author `tests/perf/test_session_recovery_alloc_guard.cpp` per plan.md §Performance Goals + §Constraints — **DUAL gate** per `[[feedback_tracking_pmr_resource_false_pass]]`: (a) `counting_resource` PMR-routed accounting AND (b) mallocnesia LD_PRELOAD interception of global `operator new` / `malloc` (path `tools/mallocnesia/libmallocnesia.so`); verify ZERO global `new`/`delete`/`malloc` on the FSM `Active ↔ AwaitingResend` transition + Heartbeat emit path. 10⁴-iteration test. Single-gate witness is REJECTED per the recurring false-pass mode (T028 binding).
- [X] T021 [P] [US1] Author `tests/fuzz/fuzz_session_recovery_admin_parse.cpp` per plan.md §Testing + `[const §IX.4]` — libFuzzer; random byte streams via `mock_transport::Script::inbound_bytes` into Session FSM through 2b `Framer` covering Logon-with-`ResetSeqNumFlag(141)=Y` + `ResendRequest(2)` + `SequenceReset-GapFill(4)` + `Heartbeat-with-TestReqID(112)` + `Logout(5)` admin parses; ASan + UBSan invariants; no crash / UAF / UB on adversarial inputs.
- [X] T022 [P] [US1] Author `bench/session/bench_heartbeat_cadence.cpp` per plan.md §Performance Goals — Heartbeat cadence emit on Active session, warm-cache: **≤ 2 µs p99** for the FSM-side strand-dispatch + admin builder + `Transport::async_write` issue (per `[2h §6.3]` row 2 ≤ 200 ns + builder + clock); CI fails > 5 % regression per `[const §VIII.2]`. SC-004 measures CADENCE accuracy (≤ 1.05× `HeartBtInt` per second on quiescent link), not per-emit latency.

### Implementation for User Story 1

- [X] T023 [US1] Create `src/session/reconnect_fsm.cpp` implementing the `ReconnectFsm` driver per data-model E-1 + plan.md Summary item 1. Methods: `drive_reconnect_attempt(...)` — destroy dead Transport → walk `policy_.delay_for_attempt(attempt_index_)` → call `factory_->cert_source_snapshot()` to capture strong-ref `shared_ptr<cert_source>` BY VALUE per FR-033 D-11 strong-ref invariant → build `SslCtxConfig` from snapshot → call `factory_->make(exec_, ssl_cfg, mr_)` per attempt (FR-002) → on `transport_handshake_failed` retry per FR-001; surface `transport_reconnect_limit_exceeded` on `attempt_index_ >= policy_.max_attempts`. `enter_awaiting_resend(begin, end)` — set `awaiting_resend_=true`, populate `resend_state_`, emit `ResendRequest(2){BeginSeqNo, EndSeqNo}` via admin builder (FR-009). `exit_awaiting_resend()` — `awaiting_resend_=false`, reset `resend_state_`. `on_inbound_heartbeat(msg)` — advance liveness clock + validate `TestReqID(112)` matches `last_outbound_testreqid_` (FR-006; mismatch → `session_testreqid_mismatch` slot 118). `on_inbound_test_request(msg)` — emit `Heartbeat(0)` with `TestReqID` echoed (FR-007). `arm_heartbeat_timer()` / `arm_test_request_timer()` / `arm_logout_timer()` — ASIO `steady_timer` `async_wait` with `cancellation_type::total` slot per `[const §XI.2]` + D-17 reset. **Apply `[[feedback_asio_post_resume_bounces_to_spawn_executor]]` D-18**: use nested `asio::co_spawn(strand, fn, use_awaitable)` (NOT `co_await asio::post`) for cross-executor hops. Per-session strand serialisation per `[const §XI.4]`; ZERO global new/delete on `Active ↔ AwaitingResend` transition (verified by T020). NOTE in body: `ResendState` is embedded by-value in the FSM, so transition allocates nothing.
- [X] T024 [US1] Create `src/session/resend_state.cpp` for any out-of-line `ResendState` helper bodies (mostly header-only per data-model E-2); ship the file even if minimal so CMake target list is uniform. Validate `outstanding_begin_ <= outstanding_end_ OR outstanding_end_ == 0` in any non-trivial accessor.
- [X] T025 [US1] Extend `src/session/seqnum_manager.cpp` per plan.md §Implementation: (a) **FR-013 advance-without-store path** for inbound `SequenceReset(4){GapFillFlag=Y}` — advance `next_expected_inbound` to `NewSeqNo(36)` without storing the synthetic gap-fill body; (b) **FR-015 `PossDupFlag(43)=Y` dedup** — on inbound application message with `PossDupFlag=Y`, query 008 `MessageStore::retrieve(MsgSeqNum)`; if present, suppress dispatch to `Application::fromApp` (idempotent recovery); if absent, RE-STORE to preserve recovered timeline.
- [X] T026 [US1] Extend `src/session/session.cpp` to wire FR-008 + FR-009 entry-points per data-model E-6: (a) `logout(std::chrono::milliseconds timeout) noexcept -> asio::awaitable<expected_t<void>>` emits `Logout(5)` via admin builder → arms `logout_timer_` with `timeout` → awaits peer reply via `co_await transport_->async_read_some(...)` → on timeout surfaces `session_logout_timeout` (slot 73, 005-era reused per F1/D1 2026-05-28), closes Transport, transitions to `fsm_state::Disconnected`; (b) `logout()` convenience overload reads `cfg_.logout_disconnect_timeout_ms`; (c) on inbound `MsgSeqNum > next_expected_inbound`, call `reconnect_fsm_.enter_awaiting_resend(next_expected_inbound, inbound_msgseqnum - 1)` per FR-009 (amends 005 FR-008 in production code; 005 spec.md text amendment lands in T044). Apply `[[feedback_subagent_phase_verification_two_traps]]` — drive bytes through `mock_transport`, NOT direct internal helper calls.
- [X] T027 [US1] Implement the **3-mode `ResetSeqNumFlag(141)` handshake matrix** in the Logon-handling path of `src/session/session.cpp` per FR-017 + D-6 / D-7 / Clarifications Q1=A: switch on `cfg_.reset_seqnum_policy`: `bilateral_strict` (default) verifies peer's response Logon carries `141=Y` when ours did; mismatch surfaces `error::session_seqnum_reset_mismatch` (slot 116) + Logout; `bilateral_lenient` auto-mirrors `141=Y` in our response Logon; `unilateral` honours any received `141=Y` regardless of our outbound flag. Successful reset emits `SessionEvent::session_event_sequence_numbers_reset{by_peer_request}` via the new ring-buffer accessor (T040). Symmetric initiator + acceptor coverage per FR-024 + `[[feedback_half_restructure_symmetric_api]]`.
- [X] T028 [US1] Wire new + extended `src/session/*.cpp` files into `src/session/CMakeLists.txt`; register T014..T019 in `tests/session/CMakeLists.txt`; register T020 in `tests/perf/CMakeLists.txt`; register T021 in `tests/fuzz/CMakeLists.txt`; register T022 in `bench/session/CMakeLists.txt` (create stub if missing). Run the US1 test list GREEN under Clang Debug + ASan + UBSan + TSan + Coverage + GCC Release sanity per `[const §IX.2]`; run T022 bench and confirm `≤ 2 µs p99` ceiling; build T021 fuzz harness + smoke-run ≥ 30 s with no crash / UAF per `[const §IX.4]`. **DUAL alloc-guard T020 MUST run with both `counting_resource` and `mallocnesia` LD_PRELOAD active in CI per `[[feedback_tracking_pmr_resource_false_pass]]`**.

**Checkpoint**: US1 fully functional and independently testable. Recovery sub-protocol + ResetSeqNumFlag 3-mode matrix + Heartbeat/TestRequest cadence + initiator-graceful Logout shippable as MVP slice if `/implement` stops here. The 2e-recovery upgrade obligation is discharged in production code (T023..T027); the 005 FR-008 spec amendment is **T048**; the catalogue + coverage-index flips are **T051**; both happen at MERGE-TIME per pipeline.md step 19a (corrected per `/speckit-analyze` F2 closure 2026-05-28 — prior draft incorrectly cited "T044 / T048" where T044 is the US4 `Session::reload_credentials` implementation).

**Phase 3 carry-forward (2026-05-28) — CLOSED 2026-05-29 (during `/speckit-verify` quality review)**: T016 `recovery_store_horizon` (3 cells) previously shipped **RED** as a documented Phase-3 carry-forward (FR-010/FR-012 reply-side store-walk was a `reply_to_inbound_resend_request` stub; a peer's ResendRequest got a blanket SequenceReset-GapFill instead of replayed application messages → silent counterparty data loss). **Closure**: implemented the reply-side store-walk INLINE in `src/session/session.cpp` `on_inbound_frame` (the emit logic lives with the session's `store_`/`transport_send_`/`scan_frame_header`, per the established recovery-emit partition; `ReconnectFsm::reply_to_inbound_resend_request` stays a no-op state hook). Per-slot walk over `[BeginSeqNo, eff_end]` (EndSeqNo=0 / over-horizon clamped to `next_seqnum(outbound)-1`): stored **application** messages replay with `PossDupFlag(43)=Y` + `OrigSendingTime(122)` (original MsgSeqNum preserved; transmit-only, no live-counter advance, no re-store); absent (pre-/post-horizon, internal gap) **and admin** slots collapse into one `SequenceReset-GapFill{NewSeqNo}` (FR-011). Added `build_replay_frame` (re-serialize stored frame via `wire::Writer`) + a `CaptureVisitor`. The no-store path keeps the prior single-GapFill behavior (empty-store CHK032). `recovery_store_horizon` rewritten with an in-test `HorizonStore` double modelling outbound `[7..12]` + `cfg.store_factory`. **Result: all 3 cells GREEN across Debug + ASan + UBSan + TSan (19/19 013 tests, 0 sanitizer findings); `recovery_admin_span_gapfill` still GREEN; session.cpp coverage 65.45→68.72% line / 53.86→55.77% branch despite +131 lines.** All other US1 cells (29 cells × 6 presets = 174 cell-passes) GREEN. T020 DUAL gate (`counting_resource` + `mallocnesia`) PASS. T022 bench 0.42 µs (under 2 µs p99). 2e-recovery markers dropped 12 → 5 (2 ABI-doc + 3 narrative/closure — appropriate to keep). NOTE: the resend-reply GapFill omits an explicit `PossDupFlag(43)=Y` (FIX-SL §4.3.4 recommends it on a resend GapFill) — minor, not asserted by the witnesses; tracked as a small follow-up alongside the slot-74 cleanup.

---

## Phase 4: User Story 2 — CompID-to-TLS-identity authorization at Logon (Priority: P1, co-equal with US1)

**Goal**: Ship the `CompIdAuthorizationPolicy` value type (allow-list-only per FR-023 / Clarifications Q3=A) + canonical-fixed principal extraction (`CN → SAN-DNS → SAN-URI → SHA-256-fingerprint` per FR-022 / Clarifications Q2=A) + Logon-time binding hookup against `peer_identity` (from 011's `verify_peer` via 012's `handshake_result.peer_id`) + symmetric initiator + acceptor coverage per `[[feedback_half_restructure_symmetric_api]]`. Closes the T-041 (FIXS §4.4 "Authorization linked to authentication") catalogue row.

**Independent Test** (per spec.md US2 Independent Test): Configure an acceptor `CompIdAuthorizationPolicy` with `{principal="ACME-PROD-01"} → SenderCompID="ACME01"`; verify (a) matching Logon reaches Active + emits `session_event_peer_identity_bound`; (b) mismatched-CompID Logon rejected with `session_compid_unauthorized` (slot 117) + emits `session_event_compid_authorization_failed`; (c) symmetric initiator-side rule applies.

### Tests for User Story 2 (write FIRST — must FAIL before implementation)

- [X] T029 [P] [US2] Author `tests/session/test_compid_binding_default_deny.cpp` (FR-023 / US2 AC6) — empty `CompIdAuthorizationPolicy` rejects EVERY Logon with `error::session_compid_unauthorized` (slot 117); fails CLOSED on misconfigured deploy (never OPEN); witness `session_event_compid_authorization_failed{cn, asserted_compid, expected_compids=[]}` emits via `Session::recent_events()`.
- [X] T030 [P] [US2] Author `tests/session/test_compid_binding_principal_extraction.cpp` (FR-022 / US2 AC5 / Clarifications Q2=A) — **4 cells**, one per `bound_principal::source` (CN / SAN_DNS / SAN_URI / SHA256_FINGERPRINT); canonical-fixed order first-non-empty wins; cell A: cert with only CN populated → `principal_source=CN`; cell B: cert with empty CN + SAN-DNS → `principal_source=SAN_DNS`; cell C: cert with empty CN/SAN-DNS + SAN-URI → `principal_source=SAN_URI`; cell D: cert with no CN / SAN → `principal_source=SHA256_FINGERPRINT`. Assert `session_event_peer_identity_bound::principal_source` carries the bound field name.
- [X] T031 [P] [US2] Author `tests/session/test_compid_binding_symmetric.cpp` (FR-024 / US2 AC4 / `[[feedback_half_restructure_symmetric_api]]`) — **2 cells**: (a) acceptor-side binding (peer's client cert ↔ peer's asserted `SenderCompID(49)`); (b) initiator-side binding (peer's server cert ↔ peer's asserted `TargetCompID(56)`). Same policy contract; same code path; assert no divergence (counter-witness against the 012 RC#B half-restructure trap).
- [X] T032 [P] [US2] Author `tests/session/test_session_invariant_counter_witness.cpp` per plan.md §Project Structure + `[[feedback_half_restructure_symmetric_api]]` — invariant-counting regression: counter on `cert_source::load_credentials()` call site; assert `calls = 1` across `N` initiator + `N` acceptor handshakes after cert-source caching invariant (012 FR-026 + 013 D-11). Witness covers both halves through the factory-IS-symmetric-authority claim (RC#4).
- [X] T033 [P] [US2] Author `bench/session/bench_compid_authorize.cpp` per plan.md §Performance Goals — `CompIdAuthorizationPolicy::authorize(peer_identity, asserted_compid)` lookup: **≤ 5 µs p99** (sub-microsecond fraction of the ≤ 5 ms p99 handshake-to-Logon-reject latency budget per SC-003 + `[const §VIII.2]`); storage `std::pmr::unordered_map<principal_view, compid_set>` with monotonic-arena backing; CI fails > 5 % regression.

### Implementation for User Story 2

- [X] T034 [US2] Create `src/session/compid_authorization_policy.cpp` implementing the `CompIdAuthorizationPolicy` class per data-model E-3 + contracts/compid_authorization_policy.hpp. Methods: (a) constructors (default = empty allow-list = default-deny per D-9); (b) `add_binding(principal, compid)` — validates non-empty; inserts into `bindings_` (`std::pmr::unordered_map<std::pmr::string, std::pmr::flat_set<std::pmr::string>>`); throws `std::invalid_argument` per `[arch §5.3]` construction-time carve-out on empty inputs; (c) `authorize(peer_identity const& pid, std::string_view asserted_compid) const noexcept -> expected_t<bound_principal>` — extract principal via canonical-fixed-order helper (T035), look up in `bindings_`, on miss / empty-policy / unmatched-compid return `expected_t::unexpected(error::session_compid_unauthorized)` (slot 117 per T006); on match return `bound_principal{value, from=source}`. PMR cost acceptable at SessionConfig-copy time per data-model E-3 copy semantics. Copy ctor + assignment deep-copy the underlying PMR map (010 W-5 invariant).
- [X] T035 [US2] Implement the **canonical-fixed-order principal-extraction helper** inside `src/session/compid_authorization_policy.cpp` (free function `extract_principal(peer_identity const&) noexcept -> bound_principal`) per FR-022 / D-8 / Clarifications Q2=A: order `CN → SAN-DNS → SAN-URI → SHA-256-fingerprint`, first-non-empty wins. Apply on the peer_identity CN view (the shipped `peer_identity` exposes `subject_dn_view()` for the full DN; CN extraction requires parsing the RDN sequence for the `commonName` attribute — fixpp may need a small helper for this if 011 didn't ship one) then `peer_identity::san_dns_names()` then `peer_identity::san_uris()` then fall back to `peer_identity::leaf_fingerprint` (`std::array<std::byte, 32>` raw — convert to 64-char lowercase hex per FR-022's SHA-256 representation table, then use as the `bound_principal.value` `std::string_view` over PMR-arena storage). Order is invariant and NOT operator-overridable in v1.0 per FR-022 (one-way restriction). **Pre-/implement verification** (closes `/speckit-analyze` R-F5a 2026-05-28): confirm via `codegraph_search peer_identity` + `codegraph_node` the shipped `peer_identity` surface — specifically whether `subject_dn_view()` and `leaf_fingerprint` (raw bytes) are present and whether 011 ships any helper for CN-from-DN extraction. If absent, this task body MUST author a private CN-from-DN parsing helper in `compid_authorization_policy.cpp` (parse the OpenSSL `X509_NAME` text-form or fall back to scanning the DN string for `"CN="`); the hex conversion of `leaf_fingerprint` is a one-shot loop into PMR-arena `std::pmr::string`. NEVER add a public method to 011's `peer_identity` from 013 (would touch a shipped contract); helpers are 013-local. Same pre-implement verification pattern as T043's `cert_source::fingerprint()` check.
- [X] T036 [US2] Extend `src/session/session.cpp` to invoke `CompIdAuthorizationPolicy::authorize(...)` at Logon-time per FR-019 + FR-020 + FR-021 + FR-024: after `peer_identity` capture (from 012's `handshake_result.peer_id`), BEFORE FSM transition to `Active`, call `cfg_.compid_authorization_policy.authorize(handshake_result.peer_id, asserted_compid)`; on success emit `session_event_peer_identity_bound{cn, sans, sha256_fingerprint, cipher, bound_compid, principal_source}` via the new ring-buffer (T040), proceed to `Active`; on `expected_t::unexpected(session_compid_unauthorized)` emit `session_event_compid_authorization_failed{cn, asserted_compid, expected_compids[]}`, surface the error via `SessionStatus(1409)=session_compid_unauthorized` (slot 117), close Transport, transition to `Disconnected`. **Symmetric coverage** per FR-024: initiator path applies the SAME rule to peer's server cert ↔ peer's asserted `TargetCompID(56)`.
- [X] T037 [US2] Wire new `src/session/compid_authorization_policy.cpp` into `src/session/CMakeLists.txt`; register T029..T032 in `tests/session/CMakeLists.txt`; register T033 in `bench/session/CMakeLists.txt`. Run the US2 test list GREEN under Clang Debug + ASan + UBSan + TSan + Coverage + GCC Release sanity per `[const §IX.2]`; run T033 bench and confirm `≤ 5 µs p99` ceiling.

**Checkpoint**: US2 fully functional. CompID-binding allow-list + canonical-fixed principal extraction + symmetric initiator-AND-acceptor coverage + invariant-counter half-restructure witness all GREEN. T-041 row ready to flip `done` at MERGE-time (T048).

**Phase 4 carry-forward (2026-05-29 — orchestrator + user decision)**: US2 ships the authorization LOGIC fully and tested — `CompIdAuthorizationPolicy` (allow-list, default-deny) + canonical-fixed `extract_principal` (CN→SAN_DNS→SAN_URI→SHA256-hex; CN parsed from `peer_identity::subject_dn_view()`, fingerprint hex-encoded from `leaf_fingerprint` — the shipped 011 surface has NO `cn`/hex helper, so these are 013-local helpers) + symmetric initiator+acceptor Logon-time `authorize()` gate (both arms wired in one pass per `[[feedback_half_restructure_symmetric_api]]`) + `session_event_peer_identity_bound` / `session_event_compid_authorization_failed` emission. Verified GREEN: `compid_binding_default_deny` (2 cells), `compid_binding_principal_extraction` (4 unit + 1 integration), `compid_binding_symmetric` (3 cells), `session_invariant_counter_witness` (3 cells) under linux-clang-debug; `bench_compid_authorize` 0.515 µs p99 (≪ 5 µs); check_layers PASS. `bound_principal.value` lifetime resolved by returning a view into the matched `bindings_` key (stable for SessionConfig lifetime, all 4 sources uniform). Storage uses `std::map<string, sorted vector<string>>` (flat_set is C++23 — "approximating the flat_set shape" per header).

**KNOWN GAP — production peer_identity SOURCE not yet wired (fail-OPEN)**: the Logon-time `authorize()` gate is conditioned on `cfg_.logon_peer_identity_override` (a test seam on `SessionConfig`). In production (real TLS, override unset) CompID authorization is currently SKIPPED — fail-OPEN, NOT fail-closed. The real wiring (`handshake_result.peer_id` → Session at Logon, fail-CLOSED when TLS/mTLS active) requires the Session to own a live `TlsTransport` and lands with the transport-active integration slice (014 interop per `[[project_release_interop_quickfix_fix8]]`). Mirrors the 011/012 phased T-039/040/041 split. **T-041 catalogue row stays NOT-done** until that wiring closes (do NOT flip at T048 merge — only the policy/extraction/event half is shippable). T032's `cert_source::load_credentials()` counter witness deferred to the same slice (012 RC#G live-handshake fixture); current T032 proves the equivalent authorize()-once-per-Logon + binding_count-stable invariant. A hostile Gate B review WILL flag the fail-open gate — this note is the explicit disposition.

---

## Phase 5: User Story 3 — TLS validation outcome as session-level event (Priority: P2)

**Goal**: Ship the `session_event_tls_validation_failed{code, sub_reason, peer_endpoint, reason_string}` surface — wire 011's master-enum `error::tls_*` 6-cell outcome (per shipped `include/fixpp/core/error.hpp:403-429`: `tls_handshake_failed = 88` GROUPING + `tls_rsa_key_too_large = 89` + `tls_cert_der_too_large = 90` + `tls_san_entries_exceeded = 91` + `tls_pin_mismatch = 92` + `tls_load_cancelled = 93`) PLUS 011's thread-local `last_handshake_sub_reason()` diagnostic into the new SessionEvent ring-buffer. Event channel bound to `Listener` / `Session` config (no Session instance need exist) per FR-028 — handshake-fail-before-Logon still surfaces. Closes T-039 second half.

**Independent Test** (per spec.md US3 Independent Test): Stand up an acceptor with `SecurityProfile::mtls_pinned`; fault-inject peers with (a) expired cert, (b) SAN mismatch, (c) cert not in Pinset, (d) cipher outside policy; verify matching `session_event_tls_validation_failed{code, sub_reason, ...}` surfaces with the precise master-enum + sub_reason discriminator.

### Tests for User Story 3 (write FIRST — must FAIL before implementation)

- [X] T038 [P] [US3] Author `tests/session/test_tls_validation_failed_taxonomy.cpp` per plan.md test plan row (FR-026 / FR-027 / SC-007 / US3 AC1+AC2+AC3) — **6 master-enum cells** = one per shipped `include/fixpp/core/error.hpp:403-429` variant: `tls_handshake_failed` GROUPING + `tls_rsa_key_too_large` + `tls_cert_der_too_large` + `tls_san_entries_exceeded` + `tls_pin_mismatch` + `tls_load_cancelled`; PLUS **3 representative sub_reason cells** within the `tls_handshake_failed` GROUPING: `expired` (peer-cert), `tls_pin_empty_at_open` (operator-config — FR-027 distinction), `sigalg_disallowed` (cipher-policy). File name encodes the 6 + 3 taxonomy (master-enum + sub_reason axes) per `[[feedback_trap_throw_pmr_witness_enumerate_sites]]` cardinality-axis convention. Fault-injection via 011 fixtures (`tests/tls/fixtures/`) + scripted `mock_transport::Script::handshake_succeeds = false` with seeded `peer_identity` rejection material; assert `session_event_tls_validation_failed{code, sub_reason, peer_endpoint, reason_string}` surfaces with matching `code` + `sub_reason` per FR-027 discriminator rule.

### Implementation for User Story 3

- [X] T039 [US3] Wire 011's `verify_peer` outcome into `SessionEvent` emission per FR-026 + FR-027 + FR-028 + D-15: in the `verify_peer_trampoline` site (012's `src/transport/asio_tls_transport.cpp`) — on `verify_peer` returning `expected_t::unexpected(tls_*)`, capture the precise `code` (master-enum value) + read `last_handshake_sub_reason()` thread-local diagnostic (`include/fixpp/tls/security_profile.hpp:144`) + capture `peer_endpoint` from the socket + format `reason_string` (operator-readable summary). **Emit `session_event_tls_validation_failed{code, sub_reason, peer_endpoint, reason_string}` via the `ListenerEvents::emit(...)` sink (T013a part (b) Option A pin per orchestrator decision 2026-05-28).** The trampoline retrieves the `ListenerEvents*` from `SSL_get_ex_data` (published by `asio_listener::async_accept` from its owned `asio_listener::Config::events_` field at handshake-start, per T013a (b) lifetime invariant). NO Session instance needed — handshake-fail-before-Logon surfaces directly to the `Listener::recent_events()` operator-observation surface, satisfying FR-028. **Sub-reason capture semantics** per data-model E-5: COPY the sub_reason string into a listener-arena `string_view` at event-emit time (NOT raw view into 011's thread-local storage — preserves value across thread-of-emit boundaries per E-5 sub-reason capture semantics; arena is the `ListenerEvents`-owning Config's PMR upstream). NO C-ABI surface added — pure C++-only wiring. Apply `[[feedback_trap_throw_pmr_witness_enumerate_sites]]` for the trampoline boundary — any PMR throw in the sub_reason-copy path routes through `[2a §4.2]` `trap_throw`.
- [X] T040 [US3] Implement `Session::recent_events() const noexcept -> std::span<const SessionEvent>` accessor BODY per data-model E-6 / FR-035 (declaration shipped in T013a (a); this task lands the implementation). Body returns a `std::span` view over the underlying fixed-capacity ring (`recent_events_` field) — straight read, `noexcept`, no allocation. Membership-witness semantics per 010 F-04 contract (NOT chronologically ordered; tests assert via `Contains` / `std::find` / `std::visit` pattern-match). DISTINCT from 010 F-04's `fsm_visit_history()` accessor; both coexist + UNCHANGED 010 surface. **No pre-Session bridging logic needed in `Session::recent_events()`** — FR-028 pre-Session events surface via the parallel `Listener::recent_events()` accessor (T013a (b) + T039 path per Option A); Session-bound events (US1/US2/US4 emits via T013a (a) `emit_event` path) populate `Session::recent_events()`. The two surfaces are independent (closes `/speckit-analyze` F6 / B1 / C2 cross-layer wiring concern 2026-05-28). Also implement the public `Listener::recent_events() const noexcept -> std::span<const SessionEvent>` accessor on `asio_listener` returning a view over the listener's `Config::events_` (`ListenerEvents`) ring, parallel shape to `Session::recent_events()`. **Add a ring-overflow drop-oldest test cell** (E1 anchor-added 2026-05-28 per `/speckit-analyze` second pass — closes the FR-035 drop-oldest coverage gap): author `tests/session/test_session_event_ring_overflow.cpp` exercising both ring surfaces — `Session::recent_events_` AND `ListenerEvents::events_` — emit 17 events; assert `recent_events()` returns 16 entries; assert the FIRST entry emitted is gone (overwritten by the 17th); assert the LAST 16 entries are the 2nd through 17th in emit order. Parallel cell for `ListenerEvents`. Together this is 2 cells × {Session, Listener} = 2 cells per surface (overflow + non-overflow witness). Apply `[[feedback_subagent_phase_verification_two_traps]]` — assert via `recent_events()` returned span values, never `SUCCEED()` placeholder.
- [X] T041 [US3] Register T038 in `tests/session/CMakeLists.txt`; run the US3 test list GREEN under Clang Debug + ASan + UBSan + TSan + Coverage + GCC Release sanity per `[const §IX.2]`. Verify the existing 011 + 012 tests still GREEN (no regression on the `verify_peer` outcome path now that the trampoline emits an additional SessionEvent).

**Phase 5 carry-forward (2026-05-29 — orchestrator + user decision)**: US3 ships the TLS-validation-event surface — `session_event_tls_validation_failed{code, sub_reason, peer_endpoint, reason_string}` emitted from the C++ accept-handshake failure branch (NOT the noexcept C trampoline) into a listener-owned `ListenerEvents` ring; `asio_listener::recent_events()` accessor added; sub_reason/peer_endpoint/reason_string COPIED into a listener-owned parallel owning-string ring (lifetime = ring slot, ASan-clean) per data-model E-5. Verified GREEN: `transport_tls_validation_failed_taxonomy` (7 cells) + `session_event_ring_overflow` (4 cells) under linux-clang-debug + ASan; transport/tls regression 42/42 PASS (no 011/012 regression); check_layers PASS. Emit seam touches shipped 012 transport additively (+232 lines).

**KNOWN GAPS — taxonomy coverage (user decision 2026-05-29: document, do not fake)**: (1) `tls_load_cancelled` (6th master-enum cell) is architecturally N/A for the listener TLS-validation-event path — it fires at factory `cert_source::load_credentials()` BEFORE any TCP accept, so there is no handshake and nothing to emit to the listener ring; **5/6 master cells exercised via real TLS**. (2) `sigalg_disallowed` (3rd sub_reason cell) is fixture-deferred — no Ed25519/Ed448 (or unknown-EVP_PKEY) fixture exists to trigger a genuine cipher/sigalg-policy rejection; **2/3 sub_reason cells** (`expired`, `tls_pin_empty_at_open`) exercised. A real cipher-policy-rejection fixture is a carry-forward to the 014 interop / TLS-fixture-tooling slice. No shape-only/fake cells added (code is an enum value + sub_reason a free diagnostic string — the 5 real cells already prove the event surface). A hostile Gate B may probe the 6+3-vs-7-cell delta — this note is the explicit disposition.

**Checkpoint**: US3 fully functional. TLS validation outcome → `session_event_tls_validation_failed` event chain wired with all 6 master-enum cells + 3 sub_reason cells covered. T-039 second half ready to flip `done` at MERGE-time (T048).

---

## Phase 6: User Story 4 — In-process credential rotation without session restart (Priority: P3)

**Goal**: Ship the `Session::reload_credentials(new_cert_source)` operator-facing forwarder + the `TransportFactory::reload_credentials(new_cert_source)` atomic-swap entry per FR-030..FR-034 / D-11 / D-12 / RC#4. Closes the 2j-controlplane IN-PROCESS variant `[2j §3.12]` reservation; closes T-040 second half. gRPC trigger remains deferred to v1.x per 2j RC#5 + Article XVIII roadmap.

**Independent Test** (per spec.md US4 Independent Test): Open Active initiator session; trigger `Session::reload_credentials(new_cert_source)`; verify (a) Active session unaffected (no Logout, no TLS teardown); (b) `TransportFactory::cert_source_snapshot()` returns NEW source post-swap; (c) on next reconnect (peer-side disconnect), new Transport handshake presents the rotated cert + `session_event_credentials_rotated{old_sha256, new_sha256}` emits.

### Tests for User Story 4 (write FIRST — must FAIL before implementation)

- [X] T042 [P] [US4] Author `tests/session/test_reload_credentials_in_flight.cpp` per plan.md test plan row (FR-030 / FR-031 / FR-032 / FR-033 / US4 AC1+AC2+AC3 / Clarifications Q4=A / D-11) — Active session unaffected by `reload_credentials`; rotation deferred until in-flight handshake completes (atomic store on the factory-internal `cert_source_slot_` via `TransportFactory::reload_credentials(...)` entry per RC#4 corrected design — NOT at `make(...)` entry); `session_event_credentials_rotated{old_sha256, new_sha256}` emits before the first handshake on the rotated source. **Strong-ref invariant cell** per `[[feedback_weak_ptr_cache_needs_owning_context]]`: assert the captured `shared_ptr<cert_source>` (captured BY VALUE COPY via `cert_source_snapshot()` before the swap) keeps OLD `cert_source` alive past the slot's `store()`; the in-flight handshake completes on OLD; the NEXT `drive_reconnect_attempt` reads NEW via `cert_source_snapshot()`. Witness via mock fault-injection on `cert_source::load_credentials` call-count. 4 cells GREEN (NullReject + ForwarderDelegates + StrongRefInvariant + CallCountWitness); sha256 values carry-forward noted (cert_source::load_credentials() is async, no synchronous fingerprint accessor; old_sha256/new_sha256 emitted as all-zeros sentinel — 014 interop slice).

### Implementation for User Story 4

- [X] T043 [US4] Extend `src/transport/transport_factory.cpp` — AUDITED: `asio_tls_transport_factory::reload_credentials` body and `cert_source_snapshot` body both ALREADY SHIPPED in Phase 2 (T012). Pre-implement verification: `fixpp::tls::cert_source` has NO synchronous `fingerprint()` — only async `load_credentials()`. SHA-256 emit carry-forward (see T042 note). Factory symmetric authority verified: both initiator and acceptor paths route through the same `cert_source_slot_` (no separate `asio_listener::reload_credentials`). No edits required to `transport_factory.cpp` for T043.
- [X] T044 [US4] Extend `src/session/session.cpp` to add `Session::reload_credentials(shared_ptr<cert_source>) noexcept -> expected_t<void>`: operator-facing forwarder declared in `session.hpp` + body in `session.cpp`. Validates nullptr → session_invalid_argument (slot 119); validates no-factory-override → session_invalid_argument; delegates to `cfg_.transport_factory_override->reload_credentials(new_source)` (pure forwarder, no emit). Session is forwarder-only (factory IS the symmetric authority per [[feedback_half_restructure_symmetric_api]]). Active session / Transport state UNAFFECTED. CARRY-FORWARD: `session_event_credentials_rotated` emission deferred to 014 — the event must fire BEFORE the first handshake on the rotated cert_source (data-model E-7) with the real cert SHA-256 fingerprint; both the correct emit-site (drive_reconnect_attempt) and the fingerprint (async load_credentials() only) require the live-transport lifecycle, which is a stub in 013. [[project_013_carryforwards_to_014]] / FR-032 / data-model §E-7.
- [X] T045 [US4] Registered `session_reload_credentials_in_flight` in `tests/session/CMakeLists.txt`. 4/4 cells GREEN under linux-clang-debug; TSan PASS (no races on atomic-swap + snapshot read). check_layers PASS. No event-emission assertions (deferred to 014 per T044 carry-forward).

**Checkpoint**: US4 fully functional. In-process credential rotation via factory-internal atomic-swap shippable; strong-ref invariant pins the closure of `[[feedback_weak_ptr_cache_needs_owning_context]]` axis. T-040 second half ready to flip `done` at MERGE-time (T048).

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Operator documentation + 005 FR-008 spec amendment + 4 in-code 2e-recovery marker removal + 44 ±N `fixpp gap` cross-comms scenario-tag drop (`/speckit-implement`-time cross-check against the live catalogue per `[[project_2e_recovery_v1_upgrade_obligation]]`) + feature-catalogue + coverage-index updates AT MERGE-TIME per `[[feedback_pipeline_mark_done_step]]` (pipeline.md step 19). Run after Phase 3..6 GREEN.

- [X] T046 [P] Author `docs/src/session-recovery-quickstart.md` per plan.md §Documentation — operator quickstart (mdBook) — recovery sub-protocol walkthrough using `mock_transport` deterministic example + reconnect-policy schedule observation + `Session::recent_events()` ring polling for `session_event_sequence_numbers_reset` observability. Path lives at parent repo per `[[project_book_lives_at_parent_repo]]` (`research/G19-fix-fpml-iso20022/book/src/session-recovery-quickstart.md`); 011 / 012 precedent governs.
- [X] T047 [P] Author `docs/src/compid-binding-quickstart.md` per plan.md §Documentation — operator quickstart (mdBook) — CompID binding allow-list construction + canonical-fixed principal-extraction order + `reload_credentials` operator-facing walkthrough + `Session::recent_events()` polling for `session_event_peer_identity_bound` / `compid_authorization_failed` / `tls_validation_failed` / `credentials_rotated`. Path lives at parent repo per `[[project_book_lives_at_parent_repo]]`.
- [ ] T048 **Amend `specs/005-session-establishment-fsm/spec.md` FR-008 in-place** from "fatal-disconnect on any inbound sequence gap" to the recovery-active form per FR-009 / FR-016 / SC-008 / plan.md §Phase 2 task row. Single edit (NOT a fork or version-bump) preserves audit-trail integrity. Treat as high-risk completeness-audit row per `[[feedback_simplify_pass_catches_9th_burn]]`; cross-reference with `[[project_005_phase8_completeness_false_pass]]` (8th burn) — body audit, not file-name audit.
- [X] T049 [P] **Remove the in-code `2e-recovery` upgrade markers** per FR-016 — `grep -rn "2e-recovery" include/ src/ tests/` baseline count (T001 closure 2026-05-28) = **2** (production code only; `specs/010-session-cfg-lifetime/simplify-fix-plan.md` planning-doc references out of scope). After Phase 3+ implementation, run the same grep + remove the markers in-place (each marker should have an adjacent FR-009..FR-016 / S-005..S-014-FSM-half catalogue-row anchor citation that supersedes it). Verify post-removal `grep -rn "2e-recovery" include/ src/ tests/` returns 0; planning-doc markers in `specs/` remain unchanged.
- [X] T050 [P] **Drop `fixpp gap` cross-comms scenario tags** per FR-016 + SC-002 — `/speckit-implement`-time cross-check the actual count against the live catalogue per `[[project_2e_recovery_v1_upgrade_obligation]]`; nominal **44 ±N**; the count may shift between Gate A and merge as catalogue evolves. After Phase 3+ implementation discharges the recovery surfaces, sweep `spec/feature-catalogue.md` / `spec/coverage-index.md` / cross-comms test-plan files for `fixpp gap` markers tied to recovery scenarios + flip to executable rows. Record the actual count in this row's completion note for audit.
- [ ] T051 **Append 013 row to `spec/feature-catalogue.md` + `spec/coverage-index.md`** per `[[feedback_feature_completeness_gate]]` + plan.md §Phase 2 task row — append 013-session-reconnect-binding row; flip catalogue rows `S-005` (ResendRequest issue), `S-006` (ResendRequest reply), `S-014-FSM-half` (recovery FSM transitions), `S-024` (SequenceReset GapFill), `T-039` (full row — TLS validation outcome), `T-040` (full row — cert-source rotation), `T-041` (full row — CompID↔TLS-identity binding) from `implementing` / `backlog` / `gap` → `done`. **AT MERGE-TIME** (NOT pre-merge) per pipeline.md step 19a / `[[feedback_pipeline_mark_done_step]]` — premature flips violate the "done = merged" semantic.
- [ ] T052 Rotate `library/CLAUDE.md` "Active feature" pointer between the `<!-- SPECKIT START -->` and `<!-- SPECKIT END -->` markers — replace 013-session-reconnect-binding "in flight" framing with "(none currently in flight) — next planned: 014 interop harness per `[[project_release_interop_quickfix_fix8]]` (transport + session-Phase-4 now both SHIPPED — full QFC / QFJ / Fix8 matrix unblocked)". Move 013 row into the "Recently merged" section with PR # + squash hash (filled at merge-time). Per `/speckit-implement` step 5 + Gate A round-2 NEW P3#C closure precedent.
- [X] T053 [P] Run the full `tools/check_layers.py` Tier-1 sweep — confirm PASS post-013 (load-bearing per `[[feedback_gate_b_check_layers_post_fixer]]`); confirm `architecture.md §4.4` session-module surface inventory still consistent with new headers (`reconnect_fsm.hpp`, `resend_state.hpp`, `compid_authorization_policy.hpp`, `session_event.hpp` all under `fixpp::session::` namespace per `architecture.md`); confirm `TransportFactory::reload_credentials` pure-virtual added to the same `transport_factory.hpp` location as `make` (no namespace drift).
- [ ] T054 [P] Run `/speckit-checklist-audit` MANDATORY gate per pipeline.md step 9 / `[const §XVI.3]` — disposition every CHK item in `specs/013-session-reconnect-binding/checklists/requirements.md` as `SPEC-FIXED` / `DD-DECIDED §X` / `WAIVED:<reason>`; spot-verify design-doc anchors via `codegraph_search` + `codegraph_node` for symbol-existence checks on prior-phase types (e.g., `verify_peer`, `cert_source`, `Transport`, `TransportFactory`). Re-run `/speckit-analyze` if anything is `SPEC-FIXED` per pipeline.md step 9 contract. Spawn `checklist-auditor` agent per pipeline.md step `[I]`.
- [ ] T055 Run `/speckit-verify` per pipeline.md step 18 — Tier-1 local mirror of CI: sanitizer presets (ASan+UBSan+TSan+Coverage), coverage gate (lcov DA/BRDA per `[[feedback_coverage_gate_lcov_basis]]` + `[[feedback_codecov_patch_vs_lcov_da_brda_gate]]`), static analysis (cppcheck + iwyu — W-2 / W-3 carry-forwards documented), ABI hygiene (`[const §X.2]` — N/A for 013, no C-ABI surface), alloc discipline (DUAL gate T020), fuzz (T021 smoke ≥ 30 s), bench (T022 + T033 ceilings). **Record verify-time YELLOW waiver candidates per `/speckit-analyze` E1 + E2 closure 2026-05-28** with written rationale per `[const §IX.1]` written-justification precedent + PR #73 / #74 / #77 / #82 / #84 / #85 prior YELLOW-waiver pattern: (a) **SC-003 full handshake-to-Logon-reject p99** — T033 measures `CompIdAuthorizationPolicy::authorize(...)` lookup at ≤ 5 µs p99, but the full handshake (TLS 1.3 1-RTT) + Logon parse + authorize + Logon reject + event emit p99 against the SC-003 ≤ 5 ms budget is NOT directly benched (rationale: bench ceiling at 5 µs lookup + async-path overhead within typical 5 ms TLS handshake makes the budget plausible; integration-bench deferred to 014 interop harness); (b) **SC-004 cadence accuracy** — T018 8-cell matrix exercises Heartbeat / TestRequest cadence functionally; T022 measures per-emit latency ≤ 2 µs p99; cadence accuracy "≤ 1.05× HeartBtInt per second on quiescent link" is NOT a dedicated bench (rationale: implicit in functional cells, additional bench is YAGNI for v1.0); (c) **SC-005 1000-message recovery throughput ≥ 50% of nominal** — covered implicitly by T014..T016 functional tests against `mock_transport`; live-throughput measurement against reference engines deferred to 014 interop harness. Produce `.specify/decisions/013-session-reconnect-binding-verify.md` paired-evidence per `[const §XVII.8]`. REQUIRED before `/gate-b`.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately.
- **Foundational (Phase 2)**: Depends on Setup. **BLOCKS all user stories** — the `error::session_*` slots + `SessionEvent` shape + `CompIdAuthorizationPolicy` skeleton + 4 new `SessionConfig` fields + `TransportFactory::reload_credentials` pure-virtual are consumed by US1/US2/US3/US4 tests + implementations.
- **User Stories (Phase 3+)**: All depend on Foundational completion. US1 + US2 are co-equal P1; US3 P2; US4 P3. Stories can proceed in parallel (if staffed) or sequentially (P1 first).
- **Polish (Phase 7)**: Depends on US1..US4 completion. T048 (005 FR-008 amendment) + T051 (catalogue flips) AT MERGE-TIME per pipeline.md step 19a; T054 (`/speckit-checklist-audit`) + T055 (`/speckit-verify`) before Gate B.

### User Story Dependencies

- **US1 (Recovery)**: Can start after Phase 2 — depends on `error::session_*` slots 73 + 74 + 118 + 116 (reused 005-era slots 73/74 per F1/D1 2026-05-28; slot 118 is `session_testreqid_mismatch`, renumbered from 120) + `SessionEvent` + 4 new `SessionConfig` fields.
- **US2 (CompID binding)**: Can start after Phase 2 — depends on `error::session_*` slot 117 + `SessionEvent` + `CompIdAuthorizationPolicy` skeleton + `peer_identity` (consumed from 011).
- **US3 (TLS validation event)**: Can start after Phase 2 — depends on `SessionEvent::session_event_tls_validation_failed` shape (T007) + 011's `verify_peer` outcome + `last_handshake_sub_reason` (already shipped); T040 `recent_events()` accessor needed for emit path.
- **US4 (reload_credentials)**: Can start after Phase 2 — depends on `TransportFactory::reload_credentials` pure-virtual (T012) + `cert_source_slot_` field + `cert_source_snapshot()` accessor (T012).

Cross-story (resolved by T013a per `/speckit-analyze` F3 closure 2026-05-28): the `Session::emit_event(...)` write path is shared by US1 (T027 emit `sequence_numbers_reset`), US2 (T036 emit `peer_identity_bound` / `compid_authorization_failed`), and US4 (T043 emit `credentials_rotated`). T013a (a) lands the field + private emit method declaration in Phase 2, so US1/US2/US4 implementations can write events without depending on Phase 5 artifacts. T040 (Phase 5, US3) implements the public `recent_events()` accessor body + the parallel `Listener::recent_events()` accessor over the `ListenerEvents` ring. The `ListenerEvents` ring itself lands in Phase 2 via T013a (b) — used by T039 (Phase 5) for the FR-028 pre-Session TLS-validation-failure surface (Option A pin).

### Within Each User Story

- Tests (per `[const §VII.1]` / `[const §VII.3]`) MUST be written FIRST and FAIL before implementation.
- Headers (skeletons / contracts) before `.cpp` bodies.
- `src/session/<entity>.cpp` before downstream `src/session/session.cpp` wire-up.
- Wire to CMake + run sanitizer matrix LAST per phase.

### Parallel Opportunities

- All Phase 1 tasks marked [P] (T002 / T003 / T004) parallelizable.
- All Phase 2 header authoring marked [P] (T007 / T008 / T009 / T010 / T012) parallelizable; T011 (SessionConfig extension) is sequential per `[arch §5.6]` care + T006 (error.hpp append) must complete first if any T007..T012 references the new slots inline.
- All US1 test authoring T014..T022 parallelizable (different files, no inter-dependencies).
- All US2 test authoring T029..T033 parallelizable.
- US1 + US2 + US3 + US4 implementation phases can interleave once Phase 2 completes (different `src/session/*.cpp` files or different sections of `session.cpp` — coordinate via merge-rebase, NOT branch divergence).
- Phase 7 polish T046 / T047 (docs) + T049 / T050 (marker / tag sweep) parallelizable; T048 (005 amendment) + T051 (catalogue) sequential at MERGE-TIME; T053 / T054 parallelizable; T055 LAST (consumes T046..T054 evidence).

---

## Parallel Example: User Story 1 Tests

```bash
# Launch all US1 test scaffolds in parallel (different files):
Task: "T014 [P] [US1] Author tests/session/test_reconnect_happy_path.cpp"
Task: "T015 [P] [US1] Author tests/session/test_recovery_admin_span_gapfill.cpp"
Task: "T016 [P] [US1] Author tests/session/test_recovery_store_horizon.cpp"
Task: "T017 [P] [US1] Author tests/session/test_reset_seqnum_policy_matrix.cpp"
Task: "T018 [P] [US1] Author tests/session/test_heartbeat_cadence_8cell.cpp"
Task: "T019 [P] [US1] Author tests/session/test_logout_timeout.cpp"
Task: "T020 [P] [US1] Author tests/perf/test_session_recovery_alloc_guard.cpp"
Task: "T021 [P] [US1] Author tests/fuzz/fuzz_session_recovery_admin_parse.cpp"
Task: "T022 [P] [US1] Author bench/session/bench_heartbeat_cadence.cpp"
```

---

## Implementation Strategy

### MVP First (US1 Only — Recovery)

1. Complete Phase 1 (Setup) — T001..T005.
2. Complete Phase 2 (Foundational) — T006..T013 (CRITICAL — blocks all stories).
3. Complete Phase 3 (US1: Recovery) — T014..T028.
4. **STOP and VALIDATE**: US1 independently testable; recovery sub-protocol + ResetSeqNumFlag matrix + Heartbeat cadence + initiator-graceful Logout all GREEN against `mock_transport`. 014 interop harness still blocked (US2 CompID binding not yet shipped).
5. Recovery alone discharges the 2e-recovery upgrade obligation in production code; the spec amendment (T048) + catalogue flips (T051) wait for merge.

### Incremental Delivery

1. Phase 1 + Phase 2 → Foundation ready.
2. Add US1 (Recovery) → Test independently → Demo MVP (recovery against `mock_transport`).
3. Add US2 (CompID binding) → Test independently → Demo full P1 surface (recovery + binding).
4. Add US3 (TLS validation event) → Test independently → Demo P2 (observability).
5. Add US4 (reload_credentials) → Test independently → Demo P3 (operator-burden reduction).
6. Phase 7 polish + MERGE.

### Parallel Team Strategy

With multiple developers:

1. Team completes Phase 1 + Phase 2 together.
2. Once Foundational done:
   - Developer A: US1 (Recovery) — `reconnect_fsm.cpp` + `resend_state.cpp` + `seqnum_manager.cpp` extension + Heartbeat/Logout in `session.cpp`.
   - Developer B: US2 (CompID binding) — `compid_authorization_policy.cpp` + binding hookup in `session.cpp`.
   - Developer C: US3 (TLS validation event) — trampoline wiring in `src/transport/asio_tls_transport.cpp` + `recent_events()` in `session.cpp`.
   - Developer D: US4 (reload_credentials) — factory atomic-swap in `src/transport/transport_factory.cpp` + Session forwarder in `session.cpp`.
3. Coordinate `session.cpp` extension edits via merge-rebase (multiple developers touching the same file — keep edits surgical per `[[karpathy-guidelines §3]]`).
4. Stories integrate independently; Phase 7 polish runs once all 4 stories GREEN.

---

## Notes

- [P] tasks = different files, no dependencies.
- [Story] label maps task to a user story (US1 / US2 / US3 / US4) for traceability.
- Each user story is independently completable + testable against `mock_transport`.
- **Tests FIRST per `[const §VII.1]`** — every test row T014..T022 / T029..T033 / T038 / T042 must be authored + RED before its corresponding implementation row.
- Commit after each task or logical group per `[[feedback_pipeline_mark_done_step]]`.
- **Plan.md test-plan binding rule** per `[[feedback_speckit_pipeline_order_gate_a_before_tasks]]`: the 10 named tests + 1 fuzz + 1 perf + 2 benches are re-emitted by NAME from plan.md §Project Structure — DO NOT re-derive from FRs.
- **AT MERGE-TIME bookkeeping** per pipeline.md step 19a: T048 (005 FR-008 amendment) + T051 (catalogue + coverage-index flips) + T052 (`library/CLAUDE.md` Active-feature pointer rotation) all run on the merge commit, NOT pre-merge. Premature flips violate the "done = merged" semantic.
- **Half-restructure discipline** per `[[feedback_half_restructure_symmetric_api]]`: every initiator / acceptor symmetric contract (FR-008 Logout symmetry, FR-024 CompID-binding symmetry, FR-030 reload_credentials factory-IS-symmetric-authority) implemented + tested + audited in BOTH halves; T032 invariant-counter witness is the binding regression test.
- **DUAL alloc-guard** per `[[feedback_tracking_pmr_resource_false_pass]]`: T020 MUST run with `counting_resource` AND mallocnesia LD_PRELOAD active. Single-gate witness is REJECTED.
- **Strong-ref invariant** per `[[feedback_weak_ptr_cache_needs_owning_context]]`: T042 cell witnesses captured `shared_ptr<cert_source>` BY VALUE COPY keeping OLD source alive past atomic swap.
- **Production-shaped exercise** per `[[project_005_phase8_completeness_false_pass]]`: every US1..US4 test drives bytes through the FSM via `mock_transport::Script`, NOT direct method calls on internal helpers; `SUCCEED()`-placeholder tests count as MISSING coverage.

---

## Extension Hooks

**Optional Hook**: git
Command: `/speckit-git-commit`
Description: Auto-commit after task generation

Prompt: Commit task changes?
To execute: `/speckit-git-commit`
