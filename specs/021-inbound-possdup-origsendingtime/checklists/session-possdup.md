# Session PossDup / OrigSendingTime — Requirements Quality Checklist

**Purpose**: Unit-tests-for-English over the inbound PossDup/OrigSendingTime requirements — validate completeness, clarity, consistency, measurability, and coverage of the spec/plan/contracts BEFORE implementation. Not implementation tests.
**Created**: 2026-06-04
**Feature**: [spec.md](../spec.md) (session-protocol + interop domain)

## Requirement Completeness

- [x] CHK001 Are requirements defined for every inbound PossDup disposition arm (admin-ignore, app-drop, app-redeliver, Arm-B fatal, Arm-C required-tag, Arm-D accuracy, Arm-E reset-exempt)? [Completeness, Spec §FR-001..FR-006/FR-010, data-model §1] — PASS: FR-001 (Arm A tolerate), FR-002 (no seqnum advance/no re-apply), FR-003 (Arm B fatal preserved), FR-004 (Arm C required-tag Reject), FR-005 (Arm D accuracy Reject+Logout), FR-006 (Arm E SequenceReset exempt), FR-010 (admin-ignore/app-drop/app-redeliver knob); all arms present in data-model §1 two-stage table and contract C1.
- [x] CHK002 Is the admin-vs-application message distinction defined for the tolerated arm (admin always ignored; app governed by the knob)? [Completeness, Spec §FR-010] — PASS: FR-010 explicitly states "Administrative possible-duplicates are always ignored irrespective of this knob"; data-model §2 confirms "Admin always ignored"; contract §C2 comment repeats this.
- [x] CHK003 Is the expected-inbound-sequence-number behavior specified for EVERY surviving disposition (tolerated arms AND the Arm-C reject), not just the disconnect arms? [Completeness, Spec §FR-002/FR-004, contract §C1] — PASS: FR-002 "MUST NOT advance" for tolerated admin arm; FR-004 "MUST NOT advance the expected inbound sequence number" for Arm-C (verify-returns-false); contract §C1 rows explicitly show "expected stays N" for Arm A (admin), Arm A (app-drop), Arm A (app-redeliver), Arm C; INV-1 in data-model pins no-advance to tolerated rows.
- [x] CHK004 Are requirements stated for the `OrigSendingTime(122)` validation scope (which message types/seqnum positions it applies to)? [Completeness, Spec §FR-004/FR-005] — PASS: FR-004/FR-005 both state "any 43=Y non-SequenceReset(35=4) message — regardless of its MsgSeqNum relative to expected, including at-expected (34==N)"; data-model §1 Stage 1 table row 0 (SequenceReset exempt), row 1 (non-PossDup skip); research D2b gives full rationale; US2 AS4/AS5 pin the at-expected case.
- [x] CHK005 Is the `redeliver_poss_dup` configuration knob's default and both settings' behavior fully specified? [Completeness, Spec §FR-010, contract §C2] — PASS: FR-010 specifies default=drop (no Application callback), opt-in=redeliver to fromApp flagged as possible duplicate; both settings specify no seqnum advance and no disconnect; contract §C2 shows the struct definition with default `false`; data-model §2 table gives default, governs, and admin-always-ignored caveat.
- [x] CHK006 Does the spec state what observable surface (or intentional silence) each disposition produces? [Completeness, Spec §FR-009] — PASS: FR-009 explicitly designates reject/terminate arms (C/D) as surfaced via "existing structured reject / session-event reporting surface" and explicitly designates tolerated arms (admin-ignore, app-drop) as "intentionally silent at the session-event surface — they have no wire/event emission by design"; quickstart §1 confirms assert-absence for Arm A; contract §C1 shows "nothing" for admin/app-drop rows.

## Requirement Clarity

- [x] CHK007 Are the exact `Reject(35=3)` field tags specified unambiguously (`371=RefTagID`, `373=SessionRejectReason`) with the concrete reason values (`1` RequiredTagMissing, `10` SendingTimeAccuracyProblem)? [Clarity, Spec §FR-004/FR-005, contract §C1] — PASS: FR-004 states "SessionRejectReason = RequiredTagMissing(373=1) and RefTagID(371)=122"; FR-005 states "SessionRejectReason = SendingTimeAccuracyProblem(373=10) and RefTagID(371)=122"; contract §C1 shows the rows verbatim; contract note explicitly states "380 is BusinessRejectReason (35=j only) and is NOT emitted"; confirmed against live `build_reject` at `admin_messages.cpp:540` which emits `371=RefTagID`, `372=RefMsgType`, `373=SessionRejectReason` (not 380).
- [x] CHK008 Is the `OrigSendingTime > SendingTime` comparison defined as strict (`>`), with the equality (`122 == 52`) case explicitly stated as accepted? [Clarity, Spec §Edge Cases, §FR-005] — PASS: FR-005 says "strictly later than"; spec Edge Cases states "equality is allowed (only strictly-greater is the accuracy-problem reject)"; data-model §1 Stage 1 row 3 says "parse(122) > parse(52) (strict)"; INV-4 states "122 == 52 is NOT Arm D (strict > only)"; US2 AS2 says "strictly later"; contract §C1 row says "122 > 52 (strict)"; T007/contract C3 explicitly pin the boundary case.
- [x] CHK009 Is "possible duplicate" defined by an objective condition (`MsgSeqNum < expected` AND `PossDupFlag(43)=Y`) rather than a vague description? [Clarity, Spec §FR-001] — PASS: FR-001 states "MsgSeqNum below the next expected inbound sequence number AND PossDupFlag(43)=Y"; data-model §1 Stage 2 precondition is "MsgSeqNum < expected, after Stage 1 validates"; US1 AS1 Given/When formalizes "MsgSeqNum < N and PossDupFlag(43)=Y"; this is an objective, measurable condition.
- [x] CHK010 Is the Arm-B behavior stated precisely as `→Disconnected` with **no Logout wire frame** (not the looser "Logout + disconnect")? [Clarity, Spec §FR-003, contract §C3] — PASS: FR-003 states "transition to Disconnected with no Logout wire frame emitted, preserved byte-identical to the current behavior (session.cpp:1860-1862 emits no Logout — only record_state_transition_(fsm_state::Disconnected))"; contract §C3 "A too-low message without 43=Y still transitions to Disconnected with NO Logout wire frame (Arm B preserved) — regression pin"; data-model §1 Stage 2 row 5 Emits column = "nothing (NO Logout wire frame — record_state_transition_ only)"; INV-2 pins byte-identity.
- [x] CHK011 Is "flagged as a possible duplicate" for the redeliver path defined in terms of an observable (the raw `43`/`122` fields visible to `fromApp`) rather than an unspecified flag parameter? [Ambiguity, Spec §FR-010] — PASS: The redeliver path "reuses the existing 019/020 Application::fromApp invocation site" (plan Technical Context + Assumptions); fromApp receives the raw inbound frame, which carries 43=Y and 122 as parsed fields — no new out-of-band flag parameter is introduced. The Ambiguity concern (an opaque boolean flag) does not apply because the existing raw-frame delivery mechanism IS the observable; "flagged as a possible duplicate" means 43=Y is present in the delivered frame, consistent with the existing fromApp contract from 019/020.

## Requirement Consistency

- [x] CHK012 Do the reject field tags agree across spec, contract, data-model, and quickstart (no surviving `380` or `373=122`)? [Consistency, Spec §FR-004/005, contract §C1, data-model §1] — PASS: Cross-artifact sweep: spec FR-004/005 = "373=1"/"373=10" + "371=122"; contract §C1 rows show "371=122, 373=1" and "371=122, 373=10"; contract note says "380 is BusinessRejectReason (35=j only) and is NOT emitted"; data-model §1 Stage 1 table Emits column = "Reject(35=3, 371=122, 373=1)" and "Reject(35=3, 371=122, 373=10)"; quickstart §1 states "Reject(35=3) with 371=122, 373=1" and "Reject(35=3) 371=122, 373=10"; research D4/D5 confirm same tags. No 380 or 373=122 anywhere.
- [x] CHK013 Is the two-stage ordering (validation Arms C/D before too-low tolerance Arms A/B) consistent between data-model §1, the contract, and the FR text? [Consistency, data-model §1, Spec §Clarifications] — PASS: data-model §1 explicitly sequences "Stage 1 — PossDup validation (runs for ALL 43=Y, non-35=4, any seqnum)" then "Stage 2 — too-low tolerance (runs only when MsgSeqNum < expected, after Stage 1 validates)"; spec Clarifications Gate A round 1 states "PossDup validation … runs for any 43=Y inbound message including those at the expected sequence number … The too-low seqnum-tolerance disposition runs after this validation"; contract §C1 note "Validation (Arms C/D) is seqnum-independent — it fires for any 43=Y non-35=4 inbound … the too-low tolerance (Arm A/B) applies only when 34 < N"; tasks §Dependencies describes the ordering; all three sources agree on Stage-1-before-Stage-2.
- [x] CHK014 Is the at-expected validation behavior (validation fires at `34==N`) consistent between FR-004/005, the contract table, and US2 acceptance scenarios AS4/AS5? [Consistency, Spec §FR-004/005, §US2] — PASS: FR-004 parenthetical "including the at-expected 34==N case"; FR-005 "(Validation is seqnum-independent)"; contract §C1 Arm C row "any seqnum incl. 34==N" and Arm D row same; US2 AS4 "Given an inbound 43=Y message at the expected sequence number (34==expected) with no OrigSendingTime(122) … validation still fires"; US2 AS5 same for Arm D; research D2b details the QFJ source; all sources are internally consistent.
- [x] CHK015 Is the no-seqnum-advance rule consistent between the too-low tolerated arms (INV-1) and the Arm-C reject (newly added)? [Consistency, Spec §FR-002/FR-004, data-model INV-1] — PASS: INV-1 in data-model §1 states "the too-low tolerated rows (6/7/8) never advance seqnum_mgr_" and explicitly notes "a validated at-expected 43=Y IS processed-once and DOES advance"; FR-004 separately pins the Arm-C no-advance: "MUST NOT advance the expected inbound sequence number (the reject path is verify-returns-false; the seqnum is not consumed — matches QuickFIX-J Session.java:1843, including the at-expected 34==N case)"; contract §C1 Arm-C row "expected stays N (no advance — verify-returns-false, QFJ Session.java:1843)"; the no-advance rule is stated consistently for both the tolerated arms (INV-1) and the Arm-C reject path (FR-004/contract §C1).

## Acceptance Criteria Quality & Measurability

- [x] CHK016 Are the success criteria measurable without implementation detail (e.g., "session remains established in 100% of replay scenarios")? [Measurability, Spec §SC-001..SC-004] — PASS: SC-001 "100% of recovery scenarios … fixpp session remains established (no spurious Logout/disconnect)"; SC-002 "malformed possible-duplicate … produces a session-level reject AND the session survives"; SC-003 "100% of existing sequence number too low (no-PossDup) tests continue to pass unchanged"; SC-004 "fixpp interops cleanly … across the PossDup replay scenarios in both initiator and acceptor roles (live interop cells green under normal + sanitizer builds)". All four are behavioral outcomes checkable against real observable artifacts; no implementation-detail leak.
- [x] CHK017 Is SC-002's "matching QuickFIX-cpp/QuickFIX-J disposition" anchored to a verifiable reference (named engine versions + the specific reject)? [Measurability, Spec §SC-002] — PASS: SC-002 cites "QuickFIX-cpp v1.16.0 and QuickFIX-J 3.0.1" (exact named versions); the specific reject is "session-level reject AND the session survives"; research D4 details the exact QFJ/QFcpp reject behavior at named source locations (Session.java:2590, Session::doPossDup); tasks T009 specifies "run QFJ with its default RequiresOrigSendingTime=Y" to make the cell reproducible. Verifiable.
- [x] CHK018 Is SC-003's "zero regression" criterion tied to an objectively checkable set (the existing too-low tests + the Arm-B pin)? [Measurability, Spec §SC-003] — PASS: SC-003 "100% of existing sequence number too low (no-PossDup) tests continue to pass unchanged — zero regression in the fatal-too-low path"; tasks T004 adds the explicit "Arm B regression pin" test; quickstart §SC-003 mapping: "Arm B unit regression pin + existing seqnum-too-low tests still green"; tasks T012 (unfiltered Tier-1 ctest) closes the regression gate. The checkable set is concrete: the existing named tests + the new regression pin.
- [x] CHK019 Does each US acceptance scenario state a Given/When/Then that can be objectively evaluated? [Acceptance Criteria, Spec §US1/US2] — PASS: All 8 AS entries (US1 AS1/AS2/AS3 + US2 AS1/AS2/AS3/AS4/AS5) use explicit Given/When/Then structure with measurable assertions (e.g., "session remains Active", "Reject(35=3) with SessionRejectReason(373)=RequiredTagMissing(1) and RefTagID(371)=122", "expected sequence number is NOT advanced", "Logout and disconnect"). Each can be evaluated from observable wire/state output.

## Scenario & Edge-Case Coverage

- [x] CHK020 Are requirements defined for the `SequenceReset(35=4)` + PossDup exemption (Arm E does not reject)? [Coverage, Spec §FR-006, §US2 AS3] — PASS: FR-006 "An inbound SequenceReset(35=4) carrying PossDupFlag MUST remain exempt from the OrigSendingTime requirement; the existing gap-fill / sequence-reset recovery path MUST handle it unchanged"; US2 AS3 "Given an inbound SequenceReset(35=4) carrying PossDupFlag … Then it is exempt from the OrigSendingTime requirement and the existing gap-fill recovery path handles it unchanged (no new reject)"; data-model §1 Stage 1 row 0 "Arm E exempt — skip Stage 1"; contract §C3 "35=4 PossDup never triggers Arm C/D"; research D6 details the rationale.
- [x] CHK021 Are requirements defined for a too-low `43=Y` **Heartbeat(0)** (handled by the existing silent-ignore exception, not Arm A)? [Edge Case, tasks §T005, Spec §Edge Cases] — PASS: tasks T005 explicitly states "A too-low 43=Y Heartbeat(0) is caught by the HB silent-ignore exception first (it precedes the Arm-A branch) — identical observable outcome, and this ordering keeps the HB exception from becoming dead code; Arm A intercepts only non-Heartbeat too-low 43=Y frames"; plan Summary Note also covers this; the observable outcome (HB ignore precedes Arm A) is stated. The spec defers to the existing T020-A warmup behavior; this is an edge case addressed by the tasks with explicit ordering rationale.
- [x] CHK022 Is the `MsgSeqNum > expected` + `43=Y` (possdup during an open resend window) case addressed (deferred to the existing too-high path)? [Coverage, Spec §Edge Cases] — PASS: spec Edge Cases section explicitly states "Possible-duplicate during an open resend window (MsgSeqNum > expected with 43=Y): handled by the existing too-high/queue path; the duplicate flag does not change too-high disposition". This is a complete and unambiguous disposition — deferred to an existing path, not an unaddressed gap.
- [x] CHK023 Is role-symmetry (initiator AND acceptor) stated as a requirement, not assumed? [Coverage, Spec §FR-007, §Edge Cases] — PASS: FR-007 "The inbound possible-duplicate tolerance MUST hold against live QuickFIX-cpp and QuickFIX-J counterparties … in both initiator and acceptor roles"; spec Edge Cases section "Both roles: the behavior must hold whether fixpp is the initiator or the acceptor"; contract §C1 note "Behavior is role-symmetric (initiator and acceptor)"; SC-004 "in both initiator and acceptor roles"; tasks T006/T009 specify "both engines × both roles".
- [x] CHK024 Is the idempotent-ignore requirement for an already-applied admin duplicate specified (no double side-effect)? [Coverage, Spec §US1 AS2/FR-002] — PASS: FR-002 "MUST NOT have its session-level side effects re-applied (idempotent ignore)"; US1 AS2 "Given an established session, When a possible-duplicate admin message that was already applied arrives again, Then its session-level side effects are not applied a second time (idempotent ignore)"; tasks T004 "AS2 idempotent — an already-applied possdup admin frame replayed → side effects not re-applied". Both requirement and acceptance scenario are stated.
- [x] CHK025 Are the at-expected `43=Y` malformed cases (missing `122`, late `122`) covered as explicit acceptance scenarios? [Coverage, Spec §US2 AS4/AS5] — PASS: US2 AS4 is an explicit Given/When/Then for "34==expected, 43=Y, no 122 → still Arm C Reject 371=122,373=1 … expected seqnum NOT advanced"; US2 AS5 for "34==expected, 43=Y, 122>52 → Arm D Reject(373=10)+Logout+disconnect"; both are in the spec §US2 section with explicit test labels "(Test: 34==expected, 43=Y, no 122)" and "(Test: 34==expected, 43=Y, 122>52)"; tasks T007 maps them to the RED test cells.

## Interop Requirements Quality

- [x] CHK026 Do the interop requirements specify both counterparty engines AND both roles, with the skip-without-counterparty contract? [Completeness, Spec §FR-007/SC-004, quickstart §2] — PASS: FR-007 "live QuickFIX-cpp and QuickFIX-J counterparties … in both initiator and acceptor roles"; SC-004 "QuickFIX-cpp v1.16.0 and QuickFIX-J 3.0.1 … both initiator and acceptor roles"; tasks T006 "both engines × both roles … skip cleanly when no counterparty"; tasks T009 same; quickstart §2 shows skip-without-counterparty run semantics. Both engines, both roles, skip contract all stated.
- [x] CHK027 Is the QuickFIX-J `RequiresOrigSendingTime` configuration dependency documented so the malformed-dup cell is reproducible? [Dependency, research §D4, tasks §T009] — PASS: research D4 states "Whether QFJ rejects is gated by its RequiresOrigSendingTime setting (Session.java:2590), whose live DefaultSessionFactory default is true (DefaultSessionFactory.java:122)"; tasks T009 specifies "run QFJ with its default RequiresOrigSendingTime=Y" to ensure reproducibility; the dependency is documented with source-line grounding and an explicit fixture instruction.
- [x] CHK028 Are the live-cell non-deterministic-field normalization expectations (timestamps, seqnums) specified for golden comparison? [Clarity, quickstart §2, Gap] — SPEC-FIXED: The quickstart §2 referenced "capture goldens" (T006/T009) but did not specify which non-deterministic fields (SendingTime(52), OrigSendingTime(122), CheckSum(10)) are canonicalized before golden diff — a genuine Clarity gap since 122 is a timestamp and appears in PossDup replay wire frames. Fixed by adding a normalization note to quickstart.md §2 (see edit below).

## Dependencies & Assumptions

- [x] CHK029 Is the `PossResend(97)` out-of-scope boundary stated explicitly so S-010 is not mis-closed? [Assumption, Spec §Assumptions, plan §VI delta] — DD-DECIDED D6/research §D: research.md "Open follow-ups" explicitly records "PossResend(97) application-resend semantics — the other half of catalogue row S-010; deferred to a later G3 slice (keeps S-010 backlog with a 021 partial-delivery note)"; spec Assumptions "PossResend(97) is OUT of scope — this slice delivers PossDupFlag(43) inbound duplicate semantics only … S-010 therefore stays backlog with a 021 partial-delivery note"; plan §VI delta says "S-010 … stays backlog" with the exact partial note and "Do NOT flip S-010 to done". Locked at Gate A. All three artifacts agree.
- [x] CHK030 Is the FR-008 / AllowPossDup send-knob deferral documented with rationale and no in-scope reference? [Assumption, Spec §FR-008/§Deferred Follow-up] — DD-DECIDED research D7: FR-008 is marked DEFERRED throughout spec (§FR-008, §Deferred Follow-up section, §Clarifications Gate A round 1), research D7 records the de-scope rationale (opaque send_impl, delimiter-injection hazard, separable hardening), plan Complexity Tracking row 2 = "DEFERRED (FR-008/research D7)", tasks.md Scope "FR-008 / User Story 3 (AllowPossDup send knob) is DE-SCOPED (Gate A round 1) — no tasks here". No in-scope task, spec section, or implementation surface references the send-path knob. Locked at Gate A round 1.
- [x] CHK031 Is the "no new persistent dedup store / no new error slot / reuse existing machinery" assumption stated and bounded? [Assumption, Spec §Assumptions, plan Constitution Check §X] — PASS: spec Assumptions "No new persistent dedup store: 'already applied' is determined from the existing expected-inbound-sequence-number state"; "Existing engine machinery is reused: the reject, Logout, disconnect, sequence-number, and structured session-event surfaces from the session FSM and runtime engine already exist"; plan Technical Context "No new infrastructure, no new persistent dedup store, no codegen change, no new error::core enum slot"; plan Constitution Check Article X row "no new error enum slot (no fixpp_error_t change)"; tasks T004 note "(h) no new error:: slot is required". Bounded and stated in all four artifacts.
- [x] CHK032 Is the three-divergence reference-engine grounding (app-redeliver, at-expected validation, AllowPossDup default) recorded as the basis for fixpp's choices? [Traceability, research §D2/D2b/D7, Spec §Assumptions] — PASS: spec Assumptions paragraph explicitly records "three axes" of divergence: (1) inbound application-duplicate redelivery — resolved configurable default drop; (2) PossDup validation at the expected sequence number — fixpp chooses QFJ superset; (3) send-path AllowPossDup default — DEFERRED. Research D2, D2b, and D7 each document the divergence and resolution with QFJ/QFcpp source line references. Plan Summary repeats the same three-divergence table with Arm assignments. Traceability chain is complete.

## Ambiguities & Conflicts

- [x] CHK033 Does any requirement still imply Arm B emits a Logout, conflicting with FR-003's no-Logout statement? [Conflict, Spec §FR-003] — PASS: Full sweep of spec, plan, contract, data-model, tasks, quickstart: FR-003 states "transition to Disconnected with no Logout wire frame emitted, preserved byte-identical to the current behavior"; data-model §1 Stage 2 row 5 Emits = "nothing (NO Logout wire frame — record_state_transition_ only)"; contract §C3 "no Logout wire frame (Arm B preserved)"; contract §C1 row for 34<N no-43=Y = "nothing (NO Logout wire frame — →Disconnected only)"; plan Complexity Tracking "Arm B (fatal too-low, →Disconnected NO Logout frame) must be preserved byte-identically"; research D3 "fixpp's existing, correct behavior is a bare →Disconnected with no Logout frame". No surviving text implies Arm B emits a Logout.
- [x] CHK034 Is there any residual claim that the engines are "identical" on Arm-D's RefTagID (they differ; fixpp chooses `371=122`)? [Conflict, research §D5, plan Summary] — PASS: Full sweep: plan Summary table Arm D Engines column = "agree on reason; at-expected firing follows QFJ" (no "identical"); research D5 explicitly states "RefTagID=122 matches QFJ (Session.java:2583 passes OrigSendingTime.FIELD) and is more specific than fixpp's existing §1699 SendingTime-accuracy reject (which references 52, the SendingTime field)"; contract §C1 note "Arm D's 371=122 … matches QFJ … and is more specific than fixpp's existing §1699 SendingTime-accuracy reject (which references 52)". No surviving "identical" overclaim; the RefTagID divergence is documented and resolved as QFJ-parity.
- [x] CHK035 Are all `[FIX-SL §X]` / `[const §X]` references exact (not vague), per `[const §VI.5]`? [Traceability, Spec §Normative References] — PASS: Spot-verified all references in spec.md Normative References section and plan.md Constitution Check: `[FIX-SL §4.8.4] Possible duplicates` resolves in coverage-index.md line 80; `[FIX-SL §4.5.4] SessionRejectReason` resolves at coverage-index.md line 67; `[const §VI.5]` resolves at constitution.md line 80 ("Every /specify artifact must include a Normative References section…"); `[const §II]` = Article II Language (line 24); `[const §VII]` = Article VII Testing (line 85+); `[const §VII.6]` = Article VII §6 Interop (line 92); `[const §VIII.5]` = Article VIII §5 Allocator policy (line 106); `[const §IX.1]` = Article IX §1 Coverage thresholds (line 113); `[const §XIV.2]` = Article XIV §2 Interface surfaces ≤5 pure-virtual (line 199); `[const §XV.9]` = Article XV §9 std::mutex in coroutine context (line 217); `[const §XV.15]` = Article XV §15 Application-layer message drops (line 223); `[const §XVI.3]` = Article XVI §3 /clarify MANDATORY (line 236); `[const §XVII.1]` = Article XVII §1 Gate A (line 247); `[const §XVII.8]` = Article XVII §8 Verification gate (line 272). All resolve in the signed-off constitution.md. No vague refs.

---

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 32 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 2 |
| WAIVED | 0 |
| **Total** | **35** |

### SPEC-FIXED items
- CHK028 — live-cell golden normalization profile for PossDup replay cells not specified; edited `quickstart.md §2` to add the `{52, 122, 10}` canonicalization profile with gate-biting semantics; affected: `specs/021-inbound-possdup-origsendingtime/quickstart.md` (§2, after the "Malformed dup rejected" bullet).

### DD-DECIDED items
- CHK029 — anchor `research.md "Open follow-ups" + spec Assumptions + plan §VI delta`; rationale: PossResend(97) out-of-scope boundary is a Gate-A-converged frozen decision, documented in all three artifacts with identical text; S-010 stays `backlog` with a 021 partial-delivery note.
- CHK030 — anchor `research.md D7 + spec §FR-008/§Deferred Follow-up + plan Complexity Tracking row 2`; rationale: FR-008 AllowPossDup send-knob de-scope is a Gate-A round-1 frozen decision, documented with full rationale (opaque-send hardening, delimiter-injection hazard) in all three artifacts.

### WAIVED items
(none)

Anchors spot-verified: `[FIX-SL §4.8.4]` resolves at `spec/coverage-index.md:80`; `[FIX-SL §4.5.4]` resolves at `spec/coverage-index.md:67`; `[const §VI.5]` at `.specify/constitution.md:80`; `[const §II]` at `:24`; `[const §VII]` at `:85`; `[const §VII.6]` at `:92`; `[const §VIII.5]` at `:106`; `[const §IX.1]` at `:113`; `[const §XIV.2]` at `:199`; `[const §XV.9]` at `:217`; `[const §XV.15]` at `:223`; `[const §XVI.3]` at `:236`; `[const §XVII.1]` at `:247`; `[const §XVII.8]` at `:272` — all resolve in signed-off constitution.md v0.1.
