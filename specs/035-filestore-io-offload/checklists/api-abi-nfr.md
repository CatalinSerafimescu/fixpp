# API / ABI / NFR Requirements Quality Checklist: Real file_io_executor offload for FileStore

**Purpose**: Validate the surface-stability and non-functional requirements quality before implementation. Audience: Gate B reviewer.
**Created**: 2026-06-13
**Feature**: [spec.md](../spec.md)
**Focus**: "no new surface" claim, the store_* error-set freeze, the bounded-frame alloc ceiling, durable-before-send, the Art. VIII bench budget.

## API / ABI surface

- [x] CHK001 - Is the "no public signature / error variant / config field / wire byte change" claim stated as an explicit requirement Gate B can check against the diff (the change is confined to FileStore's internal execution)? [Completeness, Spec §FR-009 / contracts §C6] — PASS: spec.md §FR-009 states "No public `MessageStore`/`retrieve_visitor`/factory signature, no error variant, no configuration field, and no wire byte MUST change as a result of this feature." contracts/file-offload.md §C6 restates: "No public `MessageStore` / `retrieve_visitor` / factory signature, no error-taxonomy C-ABI slot … no `EngineConfig`/`FileStore::Config` field, no codegen, no wire byte changes. `MemoryStore` behaviour is byte-identical." The no-surface-change requirement is explicit and enumerated (public sigs, error variants, config fields, wire bytes, codegen), sufficient for Gate B to check against the diff.
- [x] CHK002 - Is it stated that the offload helper is FILE-LOCAL in `file_store.cpp` (no exported/public header surface), so Article X is provably preserved and `tools/check_layers.py` is unaffected? [Clarity, plan §Structure Decision / contracts §C6] — PASS: plan.md §Structure Decision states "The offload helper is a file-local `static` coroutine in `file_store.cpp` (not a public header) so it adds no exported surface (Art. X)." contracts/file-offload.md §C6 states "`tools/check_layers.py` unaffected (the offload helper is file-local in `file_store.cpp`)." Both the file-local scope and the Art. X / check_layers consequence are explicit.
- [x] CHK003 - Is the only structural addition (`FileStoreImpl::generation_`) scoped as an INTERNAL pimpl field (not a public type/signature), so it carries no ABI obligation? [Consistency, data-model §1 / plan §Surface delta] — PASS: data-model.md §1 presents `generation_` in the "FileStoreImpl delta" (pimpl, implementation file only). The preamble states "No public type, no wire field, no `MessageStore` signature, no `EngineConfig`/`FileStore::Config` field changes (FR-009)." plan.md §Surface delta states "The only addition is **internal**: one `FileStoreImpl` field (store-generation)." Consistent across artifacts; the pimpl placement carries no ABI obligation. CodeGraph: `generation_` ABSENT from `src/session/file_store.cpp` — confirms it is genuinely new and does not yet appear in any public header.
- [x] CHK004 - Is the `file_io_executor` confirmed to be a PRE-EXISTING config field (008 FR-024a) that this feature merely makes effective, rather than a new configuration knob? [Clarity, Spec §Assumptions / plan §Surface delta] — PASS: spec.md §Assumptions states "No new configuration knob and no wire change. The `file_io_executor` field already exists on the engine/store config (008 FR-024a); this feature only makes the existing field actually take effect." plan.md §Surface delta states "no `SessionConfig`/`EngineConfig` field (the `file_io_executor` field already exists, 008 FR-024a)." The pre-existing nature of the field is explicit in both. (008 FR-024a is confirmed in 008 spec.md §FR-024a as the feature that added `asio::any_io_executor file_io_executor` to `EngineConfig`.)

## Error-set freeze

- [x] CHK005 - Is the "no new error variant" decision pinned as a requirement with the exact rationale (mid-walk-reset reuses the existing `store_io_failure`; the FR-021 10-variant `store_*` freeze is preserved), rather than left open? [Completeness, data-model §5 / plan §Constitution Check X] — PASS: data-model.md §5 (PINNED heading) states "008 FR-021 freezes the store error set at **exactly 10** `store_*` variants (`error.hpp:165–230`); a new variant is out (breaching FR-021 is a Gate-A blocker). The mid-walk-`reset()` clean-failure value is **`store_io_failure` (56)**." plan.md §Constitution Check (Art. X row) states "**No** public type/signature change, and **no new error variant**: the mid-walk-reset clean-failure reuses the existing `store_io_failure` (Phase 0 decision #4, against the FR-021 10-variant freeze, `error.hpp:165–230`)." The rationale is exact and the requirement is explicit. The orchestrator-verified count (10 contiguous, 56–65) confirms the freeze. `store_io_failure=56` confirmed in `error.hpp:190`.
- [x] CHK006 - Is the choice of `store_io_failure` over `store_seqnum_gap` for the reset-race justified as a requirement (a swapped/truncated handle is an I/O-state failure, NOT a logical gap, so a reset-race cannot mask a real gap bug)? [Clarity, data-model §5] — PASS: data-model.md §5 states the choice "deliberately does **NOT** reuse `store_seqnum_gap` (57, 'retrieve over a never-persisted gap'): keeping the two distinct prevents a reset-race from masquerading as a logical gap and masking a real gap bug (advisor note)." `store_seqnum_gap=57` confirmed in `error.hpp:194`. The justification is explicit and a discriminating test is named as the witness. The requirement is clear and its rationale auditable.
- [x] CHK007 - Is a freeze guard (compile-time / enum-count assertion that the `store_*` set stays at exactly 10) specified, so an accidental 11th variant is caught mechanically? [Coverage, plan §Surface delta / data-model §5] — PASS: plan.md §Project Structure (Source Code block for `include/fixpp/core/error.hpp`) states "add a compile/enum guard asserting the `store_*` set stays at exactly 10 (FR-021 freeze, :165–230)." tasks.md T004 states "Add a compile-time freeze guard in `include/fixpp/core/error.hpp` (`:165–230`) asserting the `store_*` variant set stays frozen per FR-021 / data-model §5 … A `static_assert` on the enumerator count / a `case`-exhaustive guard." The mechanical guard is specified as a task, is named in the source file plan, and is non-negotiable per the §5 PINNED decision.

## Allocation NFR

- [x] CHK008 - Is the per-op allocation budget quantified (≤ 1 bounded O(1) ~48 B coroutine frame per offloaded op) and tied to its authority (the `[const §XV.1]` v0.2 §XV.4-offload exemption, `constitution.md:224`), rather than asserted as "low alloc"? [Measurability, plan §Constitution Check XV.1 / research Decision 1] — PASS: plan.md §Constitution Check (Art. XV §1 row) states "**≤ 1 frame/op (O(1) exemption ceiling)**" with the exact authority "**COMPLIANT by the §XV.1 v0.2 §XV.4-offload exemption** (`constitution.md:224`, amended 2026-06-13)". data-model.md §2 (offload helper invariants) states "One ~48 B PMR-opaque frame allocation per call (Decision 1) — **permitted by the `[const §XV.1]` v0.2 §XV.4-offload exemption**". tasks.md T018 specifies the alloc gate: "assert `FileStore::store` (and the other offloaded ops) allocate **≤ 1 coroutine frame/op — O(1), the §XV.1 v0.2 exemption ceiling**". The budget is numeric, bounded, and tied to constitutional authority. constitution.md:224 confirmed to contain the exemption text (spot-verified above).
- [x] CHK009 - Is `MemoryStore::store` zero-allocation stated as an unchanged requirement (the offload is FileStore-only), with a named alloc-gate witness (counting_resource + mallocnesia)? [Completeness, Spec §FR-009 / SC-006] — PASS: tasks.md T018 states "assert **`MemoryStore::store` global `new`/op == 0** (unchanged)." plan.md §Constitution Check (Art. XV §1 row) states "`MemoryStore::store` **== 0** (unchanged)." tasks.md T018 names the witness mechanism: "counting_resource + mallocnesia LD_PRELOAD" (consistent with `[[feedback_tracking_pmr_resource_false_pass]]` — dual gate). spec.md §FR-009 states "MemoryStore MUST be unchanged". The zero-allocation requirement and the named witness are both present.
- [x] CHK010 - Is the rejected persistent-worker alternative documented with its empirical basis (~10 allocs/op, worse than the nested-co_spawn 1/op), so the alloc decision is auditable rather than assumed? [Clarity, research Decision 1] — PASS: research.md Decision 1 states "Persistent-worker alternative — empirically rejected. A long-lived per-store I/O worker … measured **worse** (≈10 global `new`/op — the handoff request/completion nodes), not the 'zero per-op frame' it was first assumed to give … Not pursued." The empirical basis (≈10/op vs 1/op for nested-co_spawn, measurement at 5000 warm iterations) is documented in the probe table in Decision 1. The alloc decision is auditable.

## Durability NFR

- [x] CHK011 - Is durable-before-send stated as an invariant the offload preserves (store completes success only AFTER fdatasync/FlushFileBuffers returns; the offload changes WHERE the flush runs, never WHETHER it precedes completion)? [Clarity, Spec §FR-003 / contracts §C2] — PASS: spec.md §FR-003 states "Durable-before-send MUST be preserved: under `commit_per_message`, `store()` MUST still complete the awaitable with success only **after** `fdatasync`/`FlushFileBuffers` has returned success (the offload changes *where* the flush runs, never *whether* it runs before completion)." contracts/file-offload.md §C2 restates the invariant verbatim. The "where not whether" framing is explicit and unambiguous.
- [x] CHK012 - Is crash-survival preservation tied to a concrete unchanged witness (008 SC-002: N commit_per_message stores then SIGKILL → all N recovered, 0% loss; on-disk format byte-unchanged)? [Measurability, Spec §SC-003 / contracts §C2] — PASS: spec.md §SC-003 states "Durability is preserved — a `FileStore` host SIGKILL'd after N successful `commit_per_message` `store()` calls returns all N frames on restart with **0%** loss (008 SC-002 continues to pass unchanged)." contracts/file-offload.md §C2 states "Crash-survival (008 SC-002): N `commit_per_message` stores then SIGKILL → all N recovered on restart, 0% loss. The on-disk format and the restart-scan/torn-write algorithm are byte-unchanged." The witness is the unchanged 008 SC-002 seam (`test_file_store_crash_survival.cpp`), explicitly cited. Measurably quantified (0% loss, N-store SIGKILL).

## Performance NFR

- [x] CHK013 - Is the performance claim quantified against a baseline ("store p-latency unchanged vs the §6.6 ≤250 µs budget; the flush floor moves off the strand, it does not grow") rather than a vague "no slowdown"? [Measurability, plan §Performance Goals] — PASS: plan.md §Technical Context states "session strand performs **zero** disk syscalls on the send path; FileStore `store` p-latency unchanged vs the design's §6.6 ≤250 µs (the flush floor moves off the strand, it does not grow)." tasks.md T020 notes the `co_spawn` scheduling hop "shifts the floor" and states "confirm within ±5% **or** update the baseline **in this PR** with rationale". The latency claim is tied to the §6.6 ≤250 µs budget (2e §6.6) and the "moves off, does not grow" characterisation is measurable (baseline comparison). Not a vague "no slowdown".
- [x] CHK014 - Is the Art. VIII §2 ±5% bench-regression obligation specified for the affected `store+fdatasync` path (`bench_file_store BM_FileStore_Store_CommitPerMessage`), including the same-PR baseline-update-with-rationale path if the co_spawn hop shifts the floor? [Coverage, tasks §T020 / `[const §VIII.2]`] — PASS: tasks.md T020 states "**Art. VIII §2 bench-regression**: run `bench/session/bench_file_store BM_FileStore_Store_CommitPerMessage` against `bench/baselines/`; the `store+fdatasync` `co_await` now crosses to the pool and back (the ~48 B frame + `co_spawn` scheduling hop), so confirm within ±5% **or** update the baseline **in this PR** with rationale". The benchmark name, the ±5% threshold, the baseline-update path, and the rationale clause are all present. The Art. VIII §2 obligation is explicitly cited.

## Notes

- Check items off as dispositioned by `/speckit-checklist-audit`: `[x]` = PASS / SPEC-FIXED / DD-DECIDED / WAIVED with rationale.
- Items test the QUALITY of the requirements (are they written completely/clearly/consistently/measurably), not whether the implementation works.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 14 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **14** |

### SPEC-FIXED items

None.

### DD-DECIDED items

None. All items are resolved by artifacts within this feature bundle (spec.md, plan.md, data-model.md, contracts/file-offload.md, research.md, tasks.md). Design-doc and constitutional anchors (2e §4.3.2, §6.6, Art. VIII §2, `[const §XV.1]` v0.2) are corroborating authority cited by the spec, but the requirements are stated directly in the feature's own artifacts; no item required delegation to the anchor alone.

### WAIVED items

None.

Anchors spot-verified: `2e §4.3.2` (`:683–696` — `file_io_executor` pre-existing field, 4-thread pool, saturation/backpressure policy: RESOLVES), `2e §6.6` (`:55` + plan.md §Performance Goals — ≤250 µs per-call latency ceiling: RESOLVES by §1.2 and plan.md cross-refs), `[const §XV.1]` v0.2 exemption (`:224` in `constitution.md` — "a single bounded O(1) coroutine frame per offloaded I/O op": RESOLVES), `[const §VIII.2]` (Art. VIII §2 ±5% bench-regression obligation — confirmed present in constitution.md; cited by tasks.md T020: RESOLVES). All resolve in signed-off revision `.specify/2e-msgstore.md v0.5` / `constitution.md v0.2`.

Realizability sub-check: The only structural addition is `FileStoreImpl::generation_` = `std::uint64_t` on the existing pimpl (implementation-file-private type, complete since 008). No new public value-typed entity. No `unique_ptr<incomplete>` or by-value deferred-spec member introduced. CLEAN.
