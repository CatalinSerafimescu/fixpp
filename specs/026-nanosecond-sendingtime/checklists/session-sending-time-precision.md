# Nanosecond SendingTime / OrigSendingTime Precision — Requirements Quality Checklist

**Purpose**: Unit-tests-for-English over the 026 requirements — validate completeness, clarity, consistency, measurability, and coverage of spec/plan/contracts/data-model BEFORE implementation. These test whether the requirements are written correctly, NOT whether the implementation works.
**Created**: 2026-06-06
**Feature**: [spec.md](../spec.md) (configurable SendingTime(52)/OrigSendingTime(122) emit precision incl. nanoseconds + lenient inbound UTCTimestamp parse; G3 slice, new catalogue row S-039)

## Requirement Completeness

- [x] CHK001 Is the nanosecond emit form fully specified (27-char `YYYYMMDD-HH:MM:SS.sssssssss`, 9 sub-second digits, sourced from the session clock)? [Completeness, Spec §FR-001, data-model §E2/E6] — PASS: spec FR-001 states "9 sub-second decimal digits, `YYYYMMDD-HH:MM:SS.sssssssss`, 27 characters"; data-model E2 pins the exact edit (nanos arm emits 9-digit `ns_rem.count()`, total 27); E6 truth table enumerates lengths 17/21/24/27; plan §Project Structure + quickstart confirm the 27-char session-clock source.
- [x] CHK002 Is the per-session config fully specified (the `fix_time_precision` enum extended with `nanos`, the additive `SessionConfig::sending_time_precision` field, default `millis`)? [Completeness, Spec §FR-002, data-model §E1/E4] — PASS: spec FR-002 names the `fix_time_precision` enum extended with `nanos`; data-model E1 gives the enum with `nanos = 3`; E4 tables the `sending_time_precision` field with type, default `millis`, and meaning; contract C1/C4 name the surfaces (`fix_time.hpp`, `session_config.hpp`); plan §XII.5 confirms explicit default.
- [x] CHK003 Is the default-`millis` byte-identity requirement specified as a measurable no-op (every outbound `52=` is the 21-char form, byte-for-byte identical to the pre-feature baseline)? [Completeness, Spec §FR-003/SC-002, data-model §I-NST-1] — PASS: spec FR-003 states "byte-for-byte identical to current behaviour"; SC-002 requires "byte-identical to the pre-feature baseline"; I-NST-1 restates the invariant with explicit `FR-003/SC-002` cross-ref; tasks T004(3)/T005(6) prescribe concrete byte-identity witnesses.
- [x] CHK004 Is the lenient inbound parse fully specified (accept any 1–9 sub-second fraction digits, plus the bare no-fraction form, padding internally to nanoseconds)? [Completeness, Spec §FR-004, data-model §E3/I-NST-3] — PASS: spec FR-004 states the exact grammar (bare length-17 OR `.` at index 17 + 1–9 ASCII digits, total 19–27, scaling `10^(9−N)`); data-model E3 repeats the grammar verbatim and specifies the scaling oracle; I-NST-3 encodes the invariant; contract C3 pins the pre/post conditions including the N-digit example.
- [x] CHK005 Are the malformed-reject cases fully specified (non-digit fraction char, empty fraction after `.`, more than 9 digits)? [Completeness, Spec §FR-008, contract §C3] — PASS: spec FR-008 enumerates length-18 empty fraction, non-digit fraction char, and >9 digits, and explicitly requires the >9 case to be caught by a width/length gate before any digit parse (not an int64 overflow), with rationale; contract C3 "Post (reject)" lists the same three cases plus the width-gate oracle; all three appear in tasks T011(2) as required RED witnesses.
- [x] CHK006 Is the `OrigSendingTime(122)` preservation requirement specified (preserved verbatim on resend, never re-stamped at the configured precision; configured precision applies to newly-stamped `SendingTime(52)` only)? [Completeness, Spec §FR-006, data-model §I-NST-4, contract §C6] — PASS: spec FR-006 states the rule with "MUST apply to newly-stamped `SendingTime(52)` only" and "never stamped at the configured precision", citing `build_replay_frame`/`session.cpp:1341-1358`, research D5, contract C6, I-NST-4; contract C6 (non-goals) explicitly states "No re-stamping of `OrigSendingTime(122)`"; I-NST-4 encodes it as an invariant; actual code at `:1341-1358` byte-copies the stored `52` value into `122` — confirmed in source.
- [x] CHK007 Is the MaxLatency requirement specified (the existing freshness check operates correctly on a nanosecond-precision inbound instant, with no logic change)? [Completeness, Spec §FR-007/SC-004] — PASS: spec FR-007 states "MUST operate correctly on nanosecond-precision inbound timestamps"; SC-004 requires "correct accept/reject decision … verified by automated tests at the boundary"; plan §Complexity Tracking explicitly states "FR-007/SC-004 (MaxLatency) require NO MaxLatency logic change"; tasks T005(5)/T012(5) require a boundary witness with explicit "no new boundary logic" assertion.
- [x] CHK008 Are the two required formatter edits NAMED as requirements — (a) resize `min_size[3]={17,21,24}` → `{17,21,24,27}` to avoid the `min_size[nanos]` OOB read, and (b) add the `else if (prec==nanos)` arm so nanos does not fall through to the 17-char seconds form? [Completeness, data-model §E2, contract §C2] — PASS: data-model E2 names both edits explicitly — "(a) resize the size table … (b) add the `nanos` arm … Without it `nanos` falls through to the 17-char seconds form"; contract C2 "Required edits" section repeats both named edits; plan §Project Structure and tasks T007 both itemize (a) and (b). Source at `fix_time.cpp:119` confirms `min_size[3]={17,21,24}` (UB pre-fix) and `:160-171` confirms the missing nanos arm — the pre-implementation state matches what the spec names.
- [x] CHK009 Is the round-trip losslessness requirement specified for nanos (parse(format(t)) recovers the instant at nanosecond resolution, subject to the clock ceiling)? [Completeness, Spec §FR-005, data-model §I-NST-2] — PASS: spec FR-005 states `parse(format(t)) == time_point_cast<nanoseconds>(t)` at nanosecond resolution, subject to clock ceiling (aligned to the cast form during this audit — see CHK042 SPEC-FIXED); I-NST-2 gives the uniform oracle `parse(format(t,P)) == time_point_cast<period_for(P)>(t)` including nanos as `time_point_cast<nanoseconds>(t)` with explicit "never a bare `== t`" caveat; contract C3 "Oracle" section repeats the same form; tasks T004(2)/T011(3) prescribe the cast-form witnesses.
- [x] CHK010 Is the non-functional surface boundary specified (no new wire field, no new error slot, no codegen, no C-ABI surface; formatter reuses `decimal_buffer_too_small`, parser reuses `wire_invalid_field_format`)? [Completeness, Spec §FR-009, plan §Constitution X] — PASS: spec FR-009 states "MUST NOT introduce a new wire field, a new error slot, or a C-ABI surface change" and names the reused slots; contract C6 (non-goals) reiterates "No new wire field / error slot / codegen / C-ABI"; plan §Constitution X row confirms "no C-ABI/error-slot change" and names the public-header source break; tasks §Scope Reality repeats the no-new-slot constraint.

## Requirement Clarity

- [x] CHK011 Is the parse grammar stated as ONE unambiguous form (bare length-17 OR dot-at-index-17 + 1–9 digits, total 19–27), with no residual loose "length 17–27" range that would admit the length-18 empty-fraction case? [Clarity, Spec §FR-004/FR-008, data-model §E3] — PASS: spec FR-004 uses "bare length-17 OR a `.` at index 17 followed by 1–9 ASCII digits (total length 19–27, no other length valid)"; spec FR-008 explicitly names length-18 as a reject case; data-model E3 repeats the same grammar with "1 ≤ N ≤ 9 (length 19–27 with the dot at index 17)"; contract C3 is titled "Grammar (one, verbatim from data-model E3)"; no artifact uses the loose "length 17–27" form.
- [x] CHK012 Is the ">9 digits rejected" requirement defined as an explicit width/length gate (NOT an int64 overflow trap, since a 10-digit value still fits in int64 and would not trap)? [Clarity, Spec §FR-008, contract §C3] — PASS: spec FR-008 states "The >9-digit case MUST be rejected by an explicit width/length gate (reject total length > 27, equivalently fraction width > 9) BEFORE any digit parse" with the explicit rationale "a 10-digit fraction (max 9,999,999,999) fits in `int64`, so it does NOT trip an arithmetic-overflow trap"; contract C3 repeats "via an explicit width/length gate … before any digit parse (a 10-digit fraction = 9,999,999,999 fits in `int64` and does NOT overflow)"; tasks T011(2) requires the same explicit assertion.
- [x] CHK013 Is the round-trip oracle stated as `time_point_cast<period(P)>(t)` uniformly (incl. nanos), rather than a bare `== t` that is a cross-platform flake on a coarser `system_clock`? [Clarity, data-model §I-NST-2/E3, contract §C3] — PASS: data-model I-NST-2 states the oracle as `parse(format(t, P)) == time_point_cast<period_for(P)>(t)` with explicit "never a bare `== t`, which is a cross-platform flake on a coarser `system_clock`"; E3 repeats the same form for nanos as `time_point_cast<nanoseconds>(t)` with the identical caveat; contract C3 Oracle section uses the same cast form; tasks New-3 annotation and T004(2)/T011(3) explicitly forbid bare `== t`.
- [x] CHK014 Is the "compile-time exhaustiveness" mechanism clearly stated (the precision parameter is NON-defaulted, so the compiler errors on any un-updated stamping call site)? [Clarity, data-model §I-NST-6/E5, research §D4] — PASS: data-model E5 states "Non-defaulted ⇒ compiler enforces exhaustiveness (a missed site is a build error, not a silent wrong-precision frame)"; I-NST-6 names it the "exhaustive precision" invariant; research D4 explains the rationale and names the alternative "defaulted param — rejected (a missed site silently misbehaves)"; plan §Complexity Tracking states "the precision param is non-defaulted → the compiler errors on any un-updated site (compile-time exhaustiveness)".
- [x] CHK015 Is the fraction-scaling rule clear (an N-digit fraction scales to nanoseconds by `10^(9−N)`, e.g. `.1234` → 123 400 000 ns)? [Clarity, data-model §E3, contract §C3] — PASS: spec FR-004 states "an N-digit fraction scales by `10^(9−N)`"; data-model E3 gives the example "e.g. `parse("…SS.1234") == base + 123'400'000 ns`"; contract C3 Oracle section states `parse("…SS.1234") == base + 123 400 000 ns`; the tasks T011(1) witness requires `×10^(9−N)` scaling verification for each width 1–9.
- [x] CHK016 Is the platform resolution ceiling clearly stated (the wire format is always 9 digits when nanos is selected, but achieved sub-second resolution is bounded by `system_clock::period` — full ns on libstdc++ Tier-1, coarser on MSVC)? [Clarity, Spec §Edge Cases, research §D6] — PASS: spec Edge Cases states "the trailing digits reflect the clock's true resolution (a documented platform nuance, not a defect)"; research D6 states "the wire FORMAT is always 9 digits when `nanos` is selected; trailing digits reflect the clock's true resolution" with explicit MSVC ~100 ns example; plan Assumptions states the same; L-026-1 (behaviours-and-limitations) captures it; contract C6 notes "No change to the time source / clock — resolution is `system_clock`-bounded (L-026-1)".
- [x] CHK017 Is the public `stamp_sending_time` signature change clearly stated (the actual existing decl is 2-arg `(now, span)`; the edit inserts `prec` as the MIDDLE param, a public-header source-break, not a silent additive)? [Clarity, data-model §E5, contract §C5] — PASS: data-model E5 states "The actual public declaration is the 2-arg `stamp_sending_time(utc_time_point now, std::span<char> buf)` … The edit inserts `prec` as the MIDDLE parameter … a public-header source break"; contract C5 repeats the same 2-arg → 3-arg with `prec` middle; plan §Constitution X ABI row calls it out as "Public-header source break (named, not 'additive')"; tasks T008 names the exact before/after signatures; source at `sending_time.hpp:41-42` (cited by data-model E5) is the actual 2-arg decl.

## Requirement Consistency

- [x] CHK018 Is the `OrigSendingTime(122)` preservation stated consistently across ALL artifacts (spec FR-006 + Clarifications, research D5, contract C6, data-model I-NST-4) with no residual claim that 122 is "newly-stamped at the configured precision"? [Consistency, Spec §FR-006] — PASS: spec FR-006 states "never stamped at the configured precision" and Clarifications state "preserved verbatim on resend"; research D5 states "preserved verbatim on resend (it is a copy of the original message's SendingTime)"; contract C6 states "No re-stamping of `OrigSendingTime(122)`"; data-model I-NST-4 states "OrigSendingTime(122) on resend echoes the original instant, not re-stamped at the configured precision"; data-model E4 field description states "OrigSendingTime(122) on resend is the stored original 52 preserved verbatim — never stamped at this precision". No artifact claims 122 is newly-stamped.
- [x] CHK019 Is the parse grammar stated identically across spec FR-004/FR-008, plan, data-model E3, and contract C3 (no artifact still implying length-18 is acceptable or >9 is caught by overflow)? [Consistency, Spec §FR-004/FR-008] — PASS: spec FR-004 states "bare length-17 OR `.` at index 17 + 1–9 ASCII digits, total 19–27"; spec FR-008 explicitly rejects length-18 and requires the >9 case via width gate not overflow; plan §Summary and §Complexity Tracking use the identical grammar; data-model E3 uses "1 ≤ N ≤ 9 (length 19–27 with the dot at index 17)"; contract C3 is "Grammar (one, verbatim from data-model E3)"; no artifact implies length-18 acceptance or overflow-based rejection.
- [x] CHK020 Is the call-site exhaustiveness stated against the SAME verified set everywhere (21 sites = 19 file-local + 2 public, + the 1 test caller `logon_handshake_test.cpp`), with no residual fuzzy "~23/~25" count? [Consistency, plan §Source Code, data-model §E5] — PASS: plan §Summary states "Verified 21-site set (19 file-local + 2 public) + 1 test caller"; plan §Source Code lists the exact 19 file-local lines `:1420,:1844,:1929,:1976,:2017,:2061,:2089,:2241,:2296,:2390,:2425,:2470,:2682,:2792,:3304,:3585,:3639,:3822,:3872`; data-model E5 tables the same split; tasks §RC#2 bakes in the same count; source `grep` confirms exactly 21 call sites (19 file-local + 2 public at `:570`,`:1695`) + 1 test caller at `logon_handshake_test.cpp:667`. No fuzzy count anywhere.
- [x] CHK021 Is the catalogue delta consistent (a NEW row S-039, distinct from the shipped millis S-019; coverage-index `§4.2.3` cell `S-019 → S-019, S-039`), with no claim of flipping a pre-existing nanos row? [Consistency, plan §VI delta] — PASS: plan §VI delta explicitly states "ADD a NEW row, not amend S-019" with rationale; plan §Constitution VI row states "NEW catalogue row `S-039`"; tasks T020 instructs "add the new row S-039 … Leave S-019 untouched"; tasks T022 instructs "amend the `§4.2.3` coverage cell … `S-019` → `S-019, S-039`". No artifact claims to flip a pre-existing nanos row.
- [x] CHK022 Are the test-file references consistent across plan/quickstart/tasks (the fix_time witnesses extend `tests/session/fix_time_roundtrip_test.cpp` / target `session_fix_time_roundtrip`, NOT a nonexistent `tests/core/fix_time_test.cpp`)? [Consistency, plan §Project Structure, quickstart] — PASS: plan §Project Structure names `tests/session/fix_time_roundtrip_test.cpp (extend; target session_fix_time_roundtrip)`; plan §VI delta S-039 catalogue entry names `tests/session/fix_time_roundtrip_test.cpp (target session_fix_time_roundtrip)`; quickstart "Core witnesses" section names `tests/session/fix_time_roundtrip_test.cpp, target session_fix_time_roundtrip`; tasks §Build-graph reality states "NOT a tests/core/ file (fixpp_core_tests is decimal-only)"; tasks T001/T004/T006/T010/T011/T016 all reference the session/ path. The file was verified to exist at `tests/session/fix_time_roundtrip_test.cpp`. No artifact uses the stale `tests/core/` path — the `/speckit-analyze` remediation succeeded.
- [x] CHK023 Is the default-precision value stated consistently as `millis` everywhere (config field default, truth table, no-op claim), with no implicit/ambiguous default? [Consistency, Spec §FR-002, data-model §E4, plan §Constitution XII.5] — PASS: spec FR-002 states "defaulting to millis"; spec Assumptions states "Default precision = millis"; spec Clarifications states "Default = MILLIS"; data-model E4 table shows `millis` default with "byte-identical" note; E6 truth table marks `millis` with "(byte-identical to today)"; plan §Constitution XII.5 states "sending_time_precision defaults to millis explicitly"; contract C4 states "default millis ⇒ byte-identical"; quickstart Enable section states "`millis` (default)" in table. Consistent across all artifacts.

## Acceptance Criteria Quality & Measurability

- [x] CHK024 Is SC-001 (nanos `52=` is 27-char and round-trips losslessly) objectively checkable from observable wire output + a parse round-trip? [Measurability, Spec §SC-001] — PASS: SC-001 states "100% of outbound messages carry a 27-char `SendingTime(52)` whose nanosecond digits match the session clock and round-trip losslessly"; tasks T004(1) and T005(4) prescribe witnesses that assert the `52=` field length `== 27` and the round-trip via `time_point_cast<nanoseconds>(t)`; both are objectively machine-checkable (length comparison + equality on the parsed result).
- [x] CHK025 Is SC-002 (default-millis byte-identity, zero regression) measurable as a concrete gate (existing time/session/wire witnesses green + `52=` byte-identical to a captured pre-feature golden)? [Measurability, Spec §SC-002] — PASS: SC-002 states "100% of existing time/session/wire regression witnesses remain green and outbound `52=` fields are byte-identical to the pre-feature baseline"; tasks T004(3) and T005(6) prescribe concrete byte-identity witnesses; tasks T010 requires `ctest -L "session|wire"` green; the byte-identity assert is machine-checkable by golden comparison.
- [x] CHK026 Is SC-003 (inbound 27-char ns `52=`/`122` parsed, not rejected) measurable via an automated parse-and-process assertion? [Measurability, Spec §SC-003] — PASS: SC-003 states "An inbound 27-char nanosecond `SendingTime(52)` (and `OrigSendingTime(122)`) is parsed and processed without a malformed-field rejection, verified by automated tests"; tasks T012(4) prescribes a witness that feeds the 27-char ns `52=` and asserts no malformed-field Reject is emitted — machine-checkable by observing the outbound Reject absence.
- [x] CHK027 Is SC-004 (MaxLatency correct on ns inbound at the boundary) measurable as a boundary accept/reject test, with the requirement explicitly that NO new boundary logic is added? [Measurability, Spec §SC-004] — PASS: SC-004 states "the MaxLatency check produces the correct accept/reject decision for nanosecond-precision inbound timestamps, verified by automated tests"; tasks T012(5) prescribes a boundary witness and requires asserting "this needs NO new boundary logic — the existing `check_sending_time` arithmetic runs on the parsed instant"; FR-007 and plan §Complexity Tracking both state no MaxLatency logic change; measurable as a boundary accept/reject on the parsed instant.
- [x] CHK028 Is SC-005 (malformed rejected AND non-standard widths 4/5/7/8 accepted) measurable as paired positive/negative parse tests? [Measurability, Spec §SC-005] — PASS: SC-005 states "Genuinely malformed sub-seconds (non-digit chars, empty fraction after `.`, >9 digits) are rejected, while non-standard widths (4/5/7/8 digits) are accepted — both verified by tests"; tasks T011(1) prescribes width 1–9 accept witnesses and T011(2) prescribes the three explicit reject witnesses; both are objectively machine-checkable parse-outcome asserts; the positive/negative pairing is explicit.
- [x] CHK029 Is the formatter-correctness gate stated as an objectively checkable RED witness — `format(t, nanos)` returns length **== 27** (NOT 17), run **under UBSan** so the `min_size[3]` OOB also fails RED? [Measurability/Acceptance, data-model §E2, contract §C2] — PASS: data-model E2 "RED witness (UBSan-run)" states "`format(t, nanos)` returns a span of length == 27 (NOT 17), run under `-fsanitize=undefined,bounds`"; contract C2 Oracle states the same; quickstart step 1 states "Run under UBSan (`-fsanitize=undefined,bounds`) so the `min_size[3]` out-of-bounds read fails RED"; tasks T004(1) and T006 prescribe the UBSan run with explicit "assert `== 27`, explicitly NOT 17" wording; the witness guards BOTH the OOB read AND the 17-char fall-through.

## Scenario & Edge-Case Coverage

- [x] CHK030 Are emit requirements covered for every precision value (seconds/millis/micros/nanos), with the truth-table lengths 17/21/24/27 enumerated? [Coverage, data-model §E6] — PASS: data-model E6 truth table explicitly covers seconds=17/millis=21/micros=24/nanos=27; spec US1 AC enumerates nanos (AC1), micros (AC2), and default-millis (AC3) as required scenarios; tasks T004/T005 require witnesses for nanos, micros, and millis; the seconds row is covered by I-NST-1 (byte-identical no-op baseline) and the seconds form remains in the existing test suite.
- [x] CHK031 Are the lenient inbound widths 1–9 all covered as accept cases (including the non-standard 4/5/7/8-digit widths a peer might send)? [Coverage, Spec §FR-004/SC-005, data-model §E3] — PASS: spec FR-004 states "accept any 1–9 fraction digits"; spec Edge Cases and SC-005 explicitly call out non-standard widths 4/5/7/8 as accepted; data-model E6 notes "Inbound accepts any … non-standard width 1–9 (lenient)"; tasks T011(1) prescribes coverage "for each fraction width N = 1..9"; the scaling example covers at least one non-standard width (`.1234` = 4 digits).
- [x] CHK032 Are the three malformed-reject cases covered as distinct negative cases (empty fraction `…SS.`, non-digit `…SS.12a`, 10-digit `…SS.1234567890`)? [Coverage, Spec §FR-008, contract §C3] — PASS: spec FR-008 enumerates all three; contract C3 Oracle lists all three: `parse("…SS.")`, `parse("…SS.12a")`, `parse("…SS.1234567890")`; tasks T011(2) prescribes all three as explicit named witnesses with `wire_invalid_field_format` assertion; quickstart step 4 names all three in the reject test.
- [x] CHK033 Is the `OrigSendingTime(122)`-preserved-on-resend case covered as a requirement (a PossDup resend echoes the original instant, even when the local config is nanos)? [Coverage, Spec §FR-006, data-model §I-NST-4] — PASS: spec FR-006 specifies the preservation; spec Edge Cases "OrigSendingTime echo on resend" states "never re-formatted at the configured precision"; tasks T012(6) prescribes a named witness `OrigSendingTime122_PreservedVerbatim_OnResend` that tests "on a PossDup resend of a message originally stamped at millis, the resent `OrigSendingTime(122)` echoes the stored original 52 bytes/instant and is NOT re-stamped at the (possibly nanos) configured precision"; contract C6 lists it as an explicit non-goal.
- [x] CHK034 Is the MaxLatency-on-ns-inbound case covered as a boundary requirement distinct from the millis path? [Coverage, Spec §FR-007/SC-004] — PASS: spec FR-007 states the MaxLatency requirement; SC-004 states "verified by automated tests at the boundary"; tasks T012(5) prescribes `MaxLatency_OnNanosInbound_BoundaryCorrect` as a distinct named witness testing boundary accept/reject on a 27-char ns `52=`; the task explicitly states "no new boundary logic" distinguishing it from the existing millis test path.
- [x] CHK035 Are the two formatter hazards (the `min_size[3]` OOB read and the missing-`else if` 17-char fall-through) covered by a requirement that a single length-asserting UBSan witness guards both? [Coverage, Edge Case, data-model §E2] — PASS: data-model E2 "RED witness (UBSan-run)" states that `format(t, nanos)` length `== 27` under UBSan "guards BOTH the missing-`else if` 17-char fall-through AND the `min_size[3]` OOB"; tasks T004(1) names the witness as guarding BOTH hazards with the identical explanation; tasks T006 requires running this witness under UBSan; contract C2 Oracle repeats the dual-guard.
- [x] CHK036 Is the no-heap requirement covered (format + parse allocate nothing at any precision, verified under the mallocnesia interceptor)? [Coverage, data-model §I-NST-5, plan §Constitution VIII.5] — PASS: data-model I-NST-5 states "format + parse are stack-buffer noexcept; no allocation on any precision"; plan §Constitution VIII.5 notes "No-heap witness on the format/parse path"; tasks T017 prescribes a `SendingTimePrecision_NoHeapOnFormatParse` witness "verified under the mallocnesia LD_PRELOAD interceptor (the binding gate)"; quickstart step 11 names the no-heap witness under mallocnesia.

## Interop Requirements Quality

- [x] CHK037 Do the interop requirements cover BOTH roles (fixpp emits nanos accepted by a live peer — C7.1; fixpp accepts a live peer's nanos — C7.2) against the live QFcpp/QFJ engines, with the skip-without-counterparty contract? [Completeness, contract §C7.1/C7.2, plan §Constitution VII.6] — PASS: contract C7.1 names "fixpp emits nanos" with "a live QFcpp/QFJ peer accepts it (no Reject)"; contract C7.2 names "fixpp accepts nanos" with "a live QFcpp/QFJ peer configured for NANOS sends"; contract C7 states "Skip-without-counterparty in-tree"; plan §Constitution VII.6 row states "extend the live QFJ/QFcpp both-role matrix"; tasks T018 (emit cell) and T019 (accept cell) implement both roles in one fixture with explicit skip-without-counterparty guard.
- [x] CHK038 Is the fixpp-accepts-nanos interop assertion stated objectively (a live peer configured for NANOS sends a 27-char `52=`; fixpp parses + processes it and MaxLatency is computed correctly)? [Clarity, contract §C7.2] — PASS: contract C7.2 states "a live QFcpp/QFJ peer configured for NANOS sends a 27-char `52=`; fixpp parses and processes it (MaxLatency computed correctly)"; tasks T019 states "a live QFcpp/QFJ peer configured for NANOS sends a 27-char `52=`; fixpp parses + processes it and MaxLatency is computed correctly" and prescribes same sanitizer matrix + skip guard as T018; the assertion is machine-checkable (no Reject emitted + correctness of MaxLatency accept/reject decision).

## Dependencies & Assumptions

- [x] CHK039 Is the reuse of the existing nanosecond-capable core stated (the formatter already computes `ns_rem` and truncates; the parser already composes the sub-second as nanoseconds; `utc_time_point` is ns-resolution on libstdc++), so no new time type/clock is introduced? [Assumption, research §D1, Spec §Assumptions] — PASS: research D1 states "fix_time.cpp:130 computes `ns_rem = duration_cast<nanoseconds>(since_epoch - sec_total)` and then truncates" and "No new time type, no new clock"; spec Assumptions states "The core formatter already computes sub-second in nanoseconds and truncates to the chosen precision"; plan §Summary states the same; source at `fix_time.cpp:130` confirms `ns_rem` computation; plan §Technical Context names `utc_time_point = time_point<system_clock>` as ns-resolution on libstdc++ (all Linux Tier-1 profiles).
- [x] CHK040 Is the §XV.9 include-safety assumption stated and bounded (the new `#include <fixpp/core/fix_time.hpp>` into `session_config.hpp` is a mutex-free chrono header and must not drag a mutex into the `session.hpp` awaitable closure; verified at the `-L sync` gate)? [Assumption, plan §Constitution XV.9, plan §Complexity Tracking] — PASS: plan §Constitution XV.9 row names this as a "WATCH-ITEM (verified safe in Gate A New-5; reconfirm at T003 / verify via `-L sync`)"; plan §Complexity Tracking row states "`fix_time.hpp` is mutex-free chrono, so safe — but verify via `-L sync`"; data-model E4 states "Requires `#include <fixpp/core/fix_time.hpp>` in `session_config.hpp` (mutex-free chrono header; §XV.9 watch-item)"; tasks T003 and T024 require the `-L sync` verification; quickstart §XV.9 watch-item section gives the command.
- [x] CHK041 Is the FIXT/version-gating exclusion stated (sub-second precision is always permitted in fixpp's FIX.4.4 scope; per-FIX-version gating defers to G4 as L-026-1)? [Assumption, research §D5, Spec §Clarifications] — PASS: spec Clarifications states "sub-second is always permitted in current scope; FIXT/5.0SP2 gating defers to G4"; research D5 states "Version-gating … defers to G4"; research D3 notes "Version-gating: QFcpp/QFJ gate sub-second to FIX4.2+/FIXT.1.1. fixpp is FIX.4.4 → sub-second always permitted"; spec Normative References notes "FIXT.1.1/5.0SP2 per-version precision gating is out of scope here (deferred to G4, L-026-1)"; contract C6 states "No FIXT/version-gating"; plan §Assumptions states the same; L-026-1 is named as the tracking item.

## Ambiguities & Conflicts

- [x] CHK042 Does any artifact still carry the loose "length 17–27" parse range or a bare `== t` round-trip oracle (the two Gate-A-corrected ambiguities)? [Conflict, Spec §FR-004, data-model §E3] — SPEC-FIXED (orchestrator spot-check): the loose "length 17–27" range is absent everywhere (FR-004 = "bare length-17 OR `.` at index 17 + 1–9 ASCII digits, total 19–27"; data-model E3 / contract C3 identical) — that half PASSES. BUT `spec.md:69` FR-005 still carried a residual `parse(format(t)) == t` (qualified by "subject to the clock's resolution ceiling", but the exact bare-oracle phrasing the Gate-A New-3 cast-form fix targeted — that fix landed in contract C3 / data-model I-NST-2 / quickstart witness 2 but missed the FR-005 prose). **Fixed**: FR-005 now reads `parse(format(t)) == time_point_cast<nanoseconds>(t)` with a pointer to the precise per-precision oracle (I-NST-2) and an explicit "never a bare `== t`". No `== t` residual remains in any artifact. (The auditor's original blanket "no artifact uses a bare `== t` oracle" was inaccurate w.r.t. FR-005.)
- [x] CHK043 Does any artifact still describe `OrigSendingTime(122)` as newly-stamped at the configured precision (the Gate-A round-1 P1 contradiction)? [Conflict, Spec §FR-006] — PASS: confirming corrected state holds. Spec FR-006, data-model I-NST-4 and E4, contract C6, research D5, spec Clarifications — all state 122 is "preserved verbatim" / "never stamped at the configured precision" / "the stored original 52 preserved verbatim". No artifact claims 122 is newly-stamped. Source at `session.cpp:1341-1358` confirms the byte-copy path. The Gate-A correction is durably in place.
- [x] CHK044 Are the normative references exact `[DocAbbrev §X.Y.Z] Title` form per `[const §VI.5]` — `[FIX50SP2 §3.3] Field data types` (UTCTimestamp datatype), `[FIX-SL §4.2.3] Validation of SendingTime(52)` (MaxLatency), `[FIX-SL §4.8.4] Possible duplicates (PossDupFlag semantics)` (122) — with no bare `[FIX50SP2]` or unanchored "UTCTimestamp" cite? [Traceability, Spec §Normative References] — PASS: spec §Normative References contains `[FIX50SP2 §3.3] Field data types`, `[FIX-SL §4.2.3] Validation of SendingTime(52)`, and `[FIX-SL §4.8.4] Possible duplicates (PossDupFlag semantics)` — all in the exact `[DocAbbrev §X.Y.Z] Title` form per the Gate-A round-2 rewrite. No bare `[FIX50SP2]` or unanchored "UTCTimestamp" cite exists. The Gate-A round-2 normative-ref canonicalization is durably in place.
- [x] CHK045 Are all `src/core/fix_time.cpp` / `src/session/session.cpp` line anchors exact and current (`fix_time.cpp:119` min_size, `:130` ns_rem, `:160-171` format arms, `:183-184` length gate; `session.cpp:1295` file-local helper, `:1341-1358` 122 byte-copy, `:1826/2054/2084/2772` inbound parse paths), not vague? [Traceability, plan §Source Code, data-model §E2/E5] — PASS: all anchors verified against the live source tree. `fix_time.cpp:119` = `constexpr std::size_t min_size[3] = {17, 21, 24};` (exact); `:130` = `const auto ns_rem = duration_cast<nanoseconds>(since_epoch - sec_total);` (exact); `:160-171` = millis/micros if/else-if arms + seconds comment (exact range); `:183-184` = `const std::size_t len = s.size();` + `if (len != 17 && len != 21 && len != 24)` (exact). `session.cpp:1295` = `[[nodiscard]] SendingTimeStamp stamp_sending_time(fixpp::core::Clock& clock) noexcept {` (exact); `:1341-1358` = tag==52 capture + 122 byte-copy (exact range, `build_replay_frame`); `:1826` = `fix_string_to_utc_time(` on MaxLatency path; `:2054` = `fix_string_to_utc_time(` on 122 parse path; `:2084` = `fix_string_to_utc_time(` on 52 parse path; `:2772` = `fix_string_to_utc_time(` on Logon-path MaxLatency. The 21-site count (19 file-local + 2 public) is verified: grep returns exactly 21 call sites (excluding the `:1295` definition and `:548` comment), matching data-model E5's enumerated list `:1420,:1844,:1929,:1976,:2017,:2061,:2089,:2241,:2296,:2390,:2425,:2470,:2682,:2792,:3304,:3585,:3639,:3822,:3872` (19 file-local) + `:570`,`:1695` (2 public).

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 44 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **45** |

### SPEC-FIXED items

- **CHK042** (orchestrator spot-check) — `spec.md:69` FR-005 retained a residual `parse(format(t)) == t` (qualified by "subject to the clock's resolution ceiling", but the exact bare-oracle phrasing the Gate-A New-3 cast-form fix targeted; that fix had landed in contract C3 / data-model I-NST-2 / quickstart but missed the FR-005 prose). Aligned FR-005 to `parse(format(t)) == time_point_cast<nanoseconds>(t)` + pointer to the I-NST-2 per-precision oracle + explicit "never a bare `== t`". CHK009's evidence line updated to match. The auditor's blanket "no artifact uses a bare `== t` oracle" was inaccurate; caught at the orchestrator spot-check. **`/speckit-analyze` re-run NOT required**: a prose oracle-wording alignment of an existing FR — no FR/SC/task mapping, no cross-artifact relationship, and no new requirement changed (same disposition basis as the 024 CHK042 SPEC-FIXED precedent).

### DD-DECIDED items

None. This feature has no separate Phase-2 design-doc anchor; the feature-local authority is research.md (D1–D6) + contracts/sendingtime-precision.md (C1–C7) + data-model.md (E1–E6, I-NST-1..6), all of which were read in full and found consistent with spec.md and plan.md.

### WAIVED items

None.

Anchors spot-verified: `fix_time.cpp:119` (min_size[3]), `fix_time.cpp:130` (ns_rem), `fix_time.cpp:160-171` (format arms), `fix_time.cpp:183-184` (length gate), `session.cpp:1295` (file-local stamp_sending_time), `session.cpp:1341-1358` (build_replay_frame / 122 byte-copy), `session.cpp:1826` (MaxLatency parse), `session.cpp:2054` (122 parse), `session.cpp:2084` (52 parse), `session.cpp:2772` (Logon-path parse) — all resolve exactly in the current submodule HEAD. Normative references `[FIX50SP2 §3.3] Field data types`, `[FIX-SL §4.2.3] Validation of SendingTime(52)`, `[FIX-SL §4.8.4] Possible duplicates (PossDupFlag semantics)` verified present in spec.md Normative References in exact `[DocAbbrev §X.Y.Z] Title` form. Feature-local authority documents (research.md D1–D6, contracts/sendingtime-precision.md C1–C7, data-model.md E1–E6/I-NST-1..6) read in full — all resolve.
