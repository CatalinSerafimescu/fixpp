# Drift-Closure Checklist: 009-session-fsm-finalize

**Purpose**: Validate the requirements-writing quality of the drift-closure slice that closes PR #81 round-1's 7 P1 binding-contract drifts against the Gate-A-converged 005 design. Release-gate depth — these items are "unit tests for English" applied to `spec.md` / `data-model.md` / `tasks.md`, **not** verifications of behavior. Behavior verification is `/speckit-verify`'s job; the feature-completeness audit (T027 + `[const §XVII.8]`) audits BODIES of tests for matching contract assertions. This checklist audits the SPEC itself for the qualities that make the 008-class false-PASS pattern recoverable: any vague FR, any missing edge case, any unmeasurable SC, any unstated assumption is a defect this checklist catches BEFORE /speckit-implement.

**Created**: 2026-05-22
**Feature**: [spec.md](../spec.md)
**Audience**: orchestrator + the `checklist-auditor` subagent invoked at pipeline step 9 (`/speckit-checklist-audit`, MANDATORY blocking gate per `[const §XVII.8]`).
**Depth**: release-gate (matches 007-gate.md / 008-implementation-readiness.md precedent).

## Requirement Completeness

- [x] CHK001 Are all 7 PR #81 round-1 RCs (RC#1–RC#7) bound to at least one FR with a `Closes RC#N` annotation in `spec.md` Functional Requirements? [Completeness, Spec §Requirements] — PASS: RC#1 → FR-001; RC#2 → FR-004+FR-005; RC#3 → FR-006; RC#4 → FR-002+FR-003; RC#5 → FR-007+FR-008+FR-009; RC#6 → FR-010; RC#7 → FR-011. All 7 covered by explicit `Closes RC#N` annotations. Refs: spec.md §Functional Requirements.
- [x] CHK002 Are the 5 admin builders (`build_logon`, `build_logout`, `build_heartbeat`, `build_test_request`, `build_reject`) enumerated in FR-002 and FR-003 by name (not "the admin builders" generically)? [Completeness, Spec §FR-002, §FR-003] — PASS: FR-002 explicitly lists all 5 by name in parentheses; FR-003 refers to "any admin builder" after having named them in FR-002 ("every admin builder (`build_logon`, `build_logout`, `build_heartbeat`, `build_test_request`, `build_reject`)"). spec.md §FR-002/FR-003.
- [x] CHK003 Are both halves of FR-002 (acceptor role in `Session::open` + acceptor row reachability in `on_inbound_frame`) covered by distinct FRs (FR-004 + FR-005)? [Completeness, Spec §FR-004, §FR-005] — PASS: FR-004 covers `Session::open` branch on `cfg_.role`; FR-005 covers the `NotConnected → LogonReceived → Active` matrix row reachability from `on_inbound_frame`. Realizability sub-check: `session_role` is a scoped enum (trivial type, no dependencies, no special members). No completeness gap. spec.md §FR-004/FR-005.
- [x] CHK004 Are the three SendingTime guard sites — (Active inbound, LogonReceived inbound, LogonSent inbound) — each addressed by a dedicated FR (FR-007 + FR-008 + FR-009)? [Completeness, Spec §FR-007..§FR-009] — PASS: FR-007 addresses Active+LogonReceived missing-52; FR-008 addresses Active+LogonReceived malformed-52; FR-009 addresses LogonSent empty-OR-malformed-52 with the D-3 Logout-only path. All three guard sites covered. spec.md §FR-007/FR-008/FR-009.
- [x] CHK005 Does the spec define what `Session::send` returns on each failure mode of the 5-step pipeline (assign / stamp / build / store / transport_send)? [Completeness, Spec §FR-001] — PASS: FR-001 states "Returns `expected_t<void>` with a defined error on any failure." spec.md §FR-001 + US1 AC3 (transport cancellation defined error). The return type contract is explicit; the per-step error variants flow through existing 005-pinned slots per plan.md §[const §X.4] row. Sufficient for implementation without ambiguity.
- [x] CHK006 Are the **9 call sites** of admin builders in `src/session/session.cpp` (per T007) enumerated in tasks.md or implicitly via "every call site" with a search criterion the implementer can grep on? [Completeness, Spec §FR-002, §FR-003 + Tasks §T007] — PASS: T007 provides both: (a) explicit line numbers from Opus triage (322, 657, 671, 715, 880, 1054, `run_logout_phase1` etc.), and (b) a grep criterion ("search for `build_logon(`, `build_logout(`, `build_heartbeat(`, `build_test_request(`, `build_reject(`"). CodeGraph verification: 8 actual call sites found in session.cpp (build_logon call deferred — T026 comment explains Logon emission wired separately); T007 says "plus `run_logout_phase1` etc." covering all. The grep criterion is deterministic. tasks.md §T007. <!-- [PR #136: one build_heartbeat call site retired; now 7] The inbound-Heartbeat→Heartbeat echo site was removed by PR #136 (`fix/no-heartbeat-echo-on-inbound-heartbeat`); `build_heartbeat` is still called at the TestRequest(35=1)→reply site + the liveness-emit site. This is a point-in-time PASS record; the disposition is unchanged. -->
- [x] CHK007 Does the spec define the conformance-test extension scope — which `tc_*_test.cpp` files MUST gain tag-8 + tag-52 assertions — by enumerating them in FR-013? [Completeness, Spec §FR-013] — PASS: FR-013 explicitly enumerates: `tc_logout_test.cpp`, `tc_reject_test.cpp`, `tc_establishment_test.cpp`, `tc_liveness_test.cpp`, `tc_sendingtime_test.cpp`. Five files named by path. spec.md §FR-013.
- [x] CHK008 Is the test-bodies discipline contract documented as a binding FR (not merely as a note or assumption)? [Completeness, Spec §FR-012] — PASS: FR-012 is a first-class functional requirement ("the slice MUST land at least one runtime-behavior test… The completeness audit T067-equivalent… MUST verify test BODIES match the contract assertion"). The requirement uses normative MUST language. spec.md §FR-012.
- [x] CHK009 Are pre-Gate-B / pre-`/speckit-implement` blocking dependencies on prior 005 / 006 / 007 / 008 work enumerated in Dependencies? [Completeness, Spec §Dependencies] — PASS: spec.md §Dependencies lists the 005 Gate-A-converged bundle as the binding design input (first bullet), plus 006/007/008 as "already merged, consumed unchanged" (last bullet). The review artifacts are listed with exact paths. spec.md §Dependencies.
- [x] CHK010 Are the `Normative References` consolidated under the canonical heading per `[const §VI.5]` (not just sprinkled inline)? [Completeness, Spec §Normative References] — PASS: spec.md has a dedicated `## Normative References` section with four subsections (FIX specification, Project Phase-2 design anchors, Source contracts, Constitutional invariants, Source-layer invariants). Per `[const §VI.5]` requirement satisfied. spec.md §Normative References.
- [x] CHK011 Does the spec list the Source-layer invariants (`async_mutex.hpp:155-160,683-690`, `seqnum_manager.hpp:50-53,96-100`) with explicit line ranges, so the implementer cannot accidentally pick a different precondition? [Completeness, Spec §Normative References] — PASS: spec.md §Normative References "Source-layer invariants" subsection lists both with exact line ranges: `library/include/fixpp/core/sync/async_mutex.hpp:155-160,683-690` and `library/include/fixpp/session/seqnum_manager.hpp:50-53,96-100`. Verified: async_mutex.hpp lines 155-160 = destructor precondition block; lines 683-690 = terminate() implementation. seqnum_manager.hpp lines 50-53 = drain lifecycle note; lines 96-100 = drain() method. All ranges confirm. spec.md §Normative References.

## Requirement Clarity

- [x] CHK012 Is "durable-before-transmit" quantified to "store completes BEFORE `transport_send_` is invoked" (the strict ordering, not just "during emit")? [Clarity, Spec §FR-001] — PASS: FR-001 step (d) reads "durably persist the frame via `store_->store(seq, committed, outbound)` BEFORE handing it to the transport per the `[2e §4.1]` durable-before-transmit invariant". The word "BEFORE" is explicit. spec.md §FR-001.
- [x] CHK013 Is "session-level Reject" specified by exact field-value pair (`SessionRejectReason=10, RefTagID=52`) so two implementers cannot pick different reason codes? [Clarity, Spec §FR-007, §FR-008] — PASS: FR-007 specifies "emit `Reject(SessionRejectReason=10, RefTagID=52)`"; FR-008 repeats the same field-value pair identically. Both FRs and US3 §Independent Test reinforce `SessionRejectReason=10, RefTagID=52`. spec.md §FR-007/FR-008.
- [x] CHK014 Is the LogonSent-special "no standalone Reject" rule explicit (i.e., it is NOT enough to say "follows D-3" — the spec spells out that pre-establishment goes Logout-with-error, not Reject)? [Clarity, Spec §FR-009] — PASS: FR-009 states "it MUST emit `Logout(58=<error text>)` and reach `Disconnected` (no standalone Reject before establishment) — the LogonSent-special D-3 path identical to the existing stale-SendingTime-on-Logon branch." The prohibition on standalone Reject is explicit in the requirement text, not delegated to D-3 by reference alone. spec.md §FR-009.
- [x] CHK015 Is the per-session TestReqID uniqueness contract specified as "within-Session over Session lifetime" vs "cross-restart" vs "cross-Session"? [Clarity, Spec §FR-010 + §Edge Cases] — PASS: FR-010 reads "within-Session uniqueness over the Session's lifetime is sufficient (wrap-around at `UINT32_MAX` is acceptable)." Edge Case fourth bullet confirms "uniqueness within a single session lifetime is the contract, not cross-restart uniqueness." Both scopes are explicitly excluded. spec.md §FR-010 + §Edge Cases.
- [x] CHK016 Is the drain-failure policy quantified ("logged-then-proceed; close still reports `closed_drained`") rather than "handle drain failure"? [Clarity, Spec §FR-011 + §Edge Cases] — PASS: FR-011 reads "Drain failures MUST be logged-then-proceed; the destructor of `Session` MUST be safe to invoke after `close` returns regardless of drain outcome." Edge Case fifth bullet adds "`close()` still reports `closed_drained` and the destructor still completes safely." Both the policy (logged-then-proceed) and the post-state (`closed_drained`) are specified. spec.md §FR-011 + §Edge Cases.
- [x] CHK017 Is "non-RED" defined for the SC-005 verify acceptance criterion — i.e., does the spec distinguish GREEN vs YELLOW-with-waivers, and is YELLOW explicitly acceptable? [Clarity, Spec §SC-005] — PASS: SC-005 reads "A second `/speckit-verify` run… reports a **non-RED** verdict." The term "non-RED" is a defined term in the Spec-Kit pipeline (GREEN or YELLOW-with-waivers); plan.md explicitly carries the 005 W-1..W-4 waivers as fallback ("carry forward only as fallback if measurement still shows residual gaps"), making YELLOW-with-waivers an acceptable outcome. The spec does not restrict to GREEN-only. spec.md §SC-005 + plan.md §[const §IX.1] row.
- [x] CHK018 Is "negotiated `BeginString`" defined unambiguously (sourced from `SessionConfig.begin_string`, not the wire-observed peer field) so the implementer threading the new param picks the correct source? [Clarity, Spec §FR-002] — PASS: FR-002 reads "carry `BeginString(8) = <negotiated_begin_string>` (from `SessionConfig.begin_string`)" — the source field is named explicitly. data-model.md §admin_messages builders table also records "`begin_string` threaded from `cfg_.begin_string` at the call site." No ambiguity. spec.md §FR-002 + data-model.md §admin_messages.
- [x] CHK019 Is "intermediate state observable" for the acceptor `LogonReceived` traversal pinned to a specific observation mechanism (state-trace hook OR `state()` snapshot under deterministic dispatch)? [Clarity, Spec §US2 §Independent Test] — PASS: US2 §Independent Test reads "observe the intermediate state via a state-trace hook OR via a synchronous deterministic `state()` snapshot after `on_inbound_frame` returns and before the strand re-enters." T010 in tasks.md restates both mechanisms. Two concrete options given; implementer cannot be blocked. spec.md §US2 + tasks.md §T010.
- [x] CHK020 Does the spec quantify "10⁴-iteration sample" for the TSan TestReqID race assertion (SC-003) so the audit can verify the test actually iterates that many times? [Clarity, Spec §SC-003] — PASS: SC-003 reads "runs on `asio::thread_pool` with `HeartBtInt=1s` + `mock_clock` advance, and TSan reports zero data races on the TestRequest ID counter over a **10⁴-iteration sample**." T019 in tasks.md restates "tick `mock_clock` forward 10⁴ times so each session emits ≥ 10⁴ TestRequests." The number is deterministic and grep-able. spec.md §SC-003 + tasks.md §T019.

## Requirement Consistency

- [x] CHK021 Do the FR-002 builder enumeration, the FR-003 builder enumeration, and the data-model.md "admin builders" section list the SAME 5 builders in the same names (no drift between spec and data-model)? [Consistency, Spec §FR-002, §FR-003, Data-model §admin_messages] — PASS: spec.md FR-002 lists `build_logon`, `build_logout`, `build_heartbeat`, `build_test_request`, `build_reject`; data-model.md §admin_messages builders section lists the same 5 in the same order. FR-003 refers to "any admin builder" without re-listing. All consistent. spec.md §FR-002/FR-003 + data-model.md §admin_messages.
- [x] CHK022 Are the matrix-cell citations consistent — does every FR that touches a `[FIX-SL §4.10]` row reference `data-model.md:19` (or the 005 equivalent) the same way? [Consistency, Spec §FR-005, §FR-006] — PASS: FR-005 cites "per `data-model.md:19`"; FR-006 cites "per `data-model.md:19` row `NotConnected × inbound Logon (refused)`". Both reference the same row anchor consistently. No drift. spec.md §FR-005/FR-006.
- [x] CHK023 Do the FR `Closes RC#N` annotations match the RC-to-FR map in Opus triage (`opus_pr81_1_triage.md`) — no FR claims to close an RC that triage assigned to a different fix? [Consistency, Spec §Functional Requirements ↔ Triage doc] — PASS (spot-checked 5 of 7 RCs): RC#1 (Opus: session.cpp:941-949 no-op send) → FR-001 (correct); RC#2 (Opus: acceptor role missing) → FR-004+FR-005 (correct); RC#4 (Opus: admin builders hardcode FIX.4.2 + placeholder time) → FR-002+FR-003 (correct); RC#6 (Opus: static tr_counter) → FR-010 (correct); RC#7 (Opus: SeqnumManager never drained) → FR-011 (correct). RC#3 → FR-006 (refused Logon → Disconnected, matches triage finding 3). RC#5 → FR-007+FR-008+FR-009 (missing/malformed SendingTime, matches finding 5). All 7 RC annotations verified against opus_pr81_1_triage.md Fix Queue. spec.md §Functional Requirements + opus_pr81_1_triage.md §Fix Queue.
- [x] CHK024 Is FR-001's pipeline ordering (assign → stamp → build → store → transport) presented identically in spec.md, data-model.md, and tasks.md T008 (no step omitted or reordered between docs)? [Consistency, Spec §FR-001, Data-model §E1, Tasks §T008] — PASS: spec.md FR-001 lists (a) assign seqnum, (b) stamp SendingTime, (c) build framed bytes, (d) store BEFORE transport, (e) transport. data-model.md §E1 "Modified methods" for `Session::send` states "NO-OP body → wired end-to-end pipeline (assign seqnum → stamp SendingTime → build → store → transport_send)". T008 specifies: (1) `assign_outbound`, (2) `stamp_sending_time`, (3) build framed bytes, (4) `store_` BEFORE transport, (5) `transport_send_`. Ordering identical across all three. spec.md §FR-001 + data-model.md §E1 + tasks.md §T008.
- [x] CHK025 Does the spec's Edge Case for "concurrent `Session::send` calls" align with the strand-reentrancy contract referenced in `[const §XI.1-4]` (i.e., the slice does NOT introduce new parallelism semantics)? [Consistency, Spec §Edge Cases] — PASS: Edge Case "Concurrent `Session::send` calls" explicitly states "the per-session strand serializes calls. The slice does not introduce parallel `send()` semantics; existing strand reentrancy contract (FR-016, `test_direct_executor_reentrancy.cpp`) is preserved." Consistent with `[const §XI.4]` per-session strand. spec.md §Edge Cases.
- [x] CHK026 Are the priorities consistent: US1 + US2 marked P1 (blocker for shipping), US3 + US4 marked P2 (non-blocking but conformance / safety) — and do their `Why this priority` notes match the priority assignment? [Consistency, Spec §User Scenarios] — PASS: US1 "Priority: P1" — "Why this priority: Without this, `Session::send` is a no-op… The library cannot send any application traffic." P1 blocker rationale is correct. US2 "Priority: P1" — "acceptor is half of FR-002… library is initiator-only." P1 blocker rationale correct. US3 "Priority: P2" — "P2, not P1, because both gaps are spec-conformance correctness issues that do not block the library from running but DO block conformance certification." P2 rationale matches. US4 "Priority: P2" — "P2 because no existing test exercises concurrent multi-session traffic… Both are real defects that will fire under production load." P2 rationale matches. spec.md §User Scenarios.
- [x] CHK027 Is the `gate-a-done` transitivity claim consistent across artifacts (spec.md Assumptions, plan.md Constitution Check, tasks.md T030)? [Consistency, Spec §Assumptions, Plan §Constitution Check, Tasks §T030] — PASS: spec.md Assumption 1 states "Gate A is NOT re-run for 009." plan.md §Summary and §[const §XVII.1] row both state "Gate A NOT re-run. Per `[const §XVII.1]` Gate A is triggered by design artifacts; a drift-closure slice that does not edit those does not re-trigger." T030 states "apply `gate-a-done` (transitively from 005's gate-a sign-off per `[const §XVII.8]` paired evidence)." All three artifacts are consistent. spec.md §Assumptions + plan.md §Constitution Check + tasks.md §T030.

## Acceptance Criteria Quality / Measurability

- [x] CHK028 Can SC-001 ("100% of RCs have a landed runtime-behavior test") be objectively counted by the T027 audit script, or does it depend on subjective judgment? [Measurability, Spec §SC-001] — PASS: SC-001 is verifiable: 7 RCs × each must map to a test file + the T027 audit checks test bodies (not just file names). The count is 7 (RC#1..RC#7). FR-012 binds the "runtime-behavior test" requirement. T027 §(c) explicitly prohibits `static_assert`/`SUCCEED()` placeholders. The audit is deterministic. spec.md §SC-001 + §FR-012 + tasks.md §T027.
- [x] CHK029 Can SC-002 ("100% of `tc_*` outbound assertions read tag 8 + tag 52") be verified by grep on the test files (deterministic) rather than by reviewer judgment? [Measurability, Spec §SC-002] — PASS: SC-002 is grep-verifiable: the 5 enumerated `tc_*_test.cpp` files (per FR-013) can be grepped for `extract_field(frame, 8)` and `extract_field(frame, 52)` assertions adjacent to every existing outbound-frame assertion block. The T005 `extract_field` helper is the shared mechanism. The count per file is objective. spec.md §SC-002 + §FR-013.
- [x] CHK030 Is SC-005 ("coverage rises into the W-1..W-4 envelope or better") measurable against the existing 005 verify record's quantified envelope, or does "envelope" need numerical thresholds? [Measurability, Spec §SC-005] — PASS: The W-1..W-4 waivers are in `005-session-establishment-fsm-verify.md` with quantified thresholds (the "prior verify record's quantified envelope"). Plan.md §[const §IX.1] row states the expectation is "RISES into the prior 005 W-1..W-4 waiver envelope or better." The baseline is the 005 verify record (a concrete document on disk). The comparison is lcov DA/BRDA per `[[feedback_coverage_gate_lcov_basis]]`. Measurable against the existing record. spec.md §SC-005 + plan.md §[const §IX.1].
- [x] CHK031 Is SC-006's "test BODIES match contract assertions for all 18 FRs from 005's spec" runnable as a deterministic grep, with FR-to-assertion-content mappings spelled out somewhere (here or T027)? [Measurability, Spec §SC-006 + Tasks §T027] — SPEC-FIXED: T027 §(b) provides example mappings ("e.g., FR-001 requires assertions on tags 34 AND 52 AND 8 AND store-before-transport") but not a complete tabular map for all 18 FRs from 005's spec. Without the complete FR-to-assertion-content table, the T027 audit for the 005-owned FRs (FR-001 through FR-018 of 005) cannot be run deterministically — a reviewer must independently derive each assertion content, creating subjective judgment risk (precisely the pattern that caused the false-PASS in T067). Added a one-paragraph mapping note to tasks.md T027 linking to spec.md FR-001..FR-013 for the 9 new 009 FRs, and directing the auditor to 005/spec.md §Requirements for the 9 inherited FRs (FR-002/003/004/005/006/007/008/009/010 from 005 that are NOT in scope of 009 FRs). Edit: appended to tasks.md §T027 §(b): "For the 9 new 009 FRs (FR-001..FR-013 above excluding FR-012 which is meta): use the assertion-content from the FR text itself (e.g., FR-001: assert 34 incremented by 1 AND 52=mock_now AND 8=config_begin_string AND store before transport; FR-006: assert Disconnected not NotConnected; FR-007/008: assert Reject(371=52,373=10) then Logout then Disconnected; FR-009: assert Logout(58=text) no Reject; FR-010: assert per-session counter no static; FR-011: assert no terminate). For the 9 005-inherited FRs included in SC-006, map via 005/spec.md §Functional Requirements using the same assertion-body rule." tasks.md §T027.
- [x] CHK032 Is SC-007's "P1=0 AND P2_unwaived=0 within Sonnet × 2 cap" expressed using the same labels as the gate-b skill counts (so converged outcome is unambiguous)? [Measurability, Spec §SC-007] — PASS: SC-007 reads "converges with `P1 = 0` AND `P2_unwaived = 0` within the standard Sonnet × 2 cap (no Codex-fixer escalation required)." These labels match the gate-b skill's documented convergence gate (P1/P2_unwaived counters, Sonnet × 2 cap per `[[project_005_phase8_completeness_false_pass]]` precedent). The label `P2_unwaived` is the canonical term. spec.md §SC-007.

## Scenario Coverage

- [x] CHK033 Is the **Primary** flow for each user story (US1–US4) specified by an Acceptance Scenario in the Given/When/Then format? [Coverage, Spec §User Scenarios] — PASS: US1 has 4 ACs in Given/When/Then format; US2 has 4 ACs; US3 has 5 ACs; US4 has 3 ACs. All primary flows have at least one AC1 in G/W/T format. spec.md §User Scenarios.
- [x] CHK034 Are **Exception/Error** flows defined for `Session::send` (transport cancellation, store failure)? [Coverage, Spec §US1 §AC3 + §Edge Cases] — PASS: US1 AC3 explicitly covers "transport cancels mid-`async_write`" → "defined error returned, session reaches Disconnected." Edge Case first bullet covers "cancellation during `Session::send`" with "MUST return a defined error from `Session::send`." Store failure is covered by FR-001 "Returns `expected_t<void>` with a defined error on any failure." spec.md §US1 §AC3 + §Edge Cases.
- [x] CHK035 Is the **Recovery** scope-boundary stated (the slice does NOT do session-recovery on a phantom committed frame — that is deferred to the session-recovery feature)? [Coverage, Spec §Edge Cases first bullet] — PASS: Edge Case first bullet reads "The slice does not attempt session-level recovery — that is the deferred session-recovery feature's responsibility per `[2e §3.1]` / `[2e §4 last bullet]`. The slice MUST return a defined error from `Session::send` and leave the session in a state consistent with the existing close-path contract; it MUST NOT silently succeed." The scope boundary is explicit. spec.md §Edge Cases.
- [x] CHK036 Are **Non-Functional** scenarios (TSan-clean concurrent liveness, gtest-death-test or surviving-harness teardown) defined for US4? [Coverage, Spec §US4 §Independent Test] — PASS: US4 §Independent Test specifies TSan-targeted concurrent liveness (two sessions on `asio::thread_pool`, 10⁴ iterations) and the teardown test ("gtest death-test OR by surviving destructor in normal pipeline"). SC-003 and SC-004 bind these as measurable SCs. spec.md §US4 + §SC-003 + §SC-004.
- [x] CHK037 Is the regression scenario "initiator path still works" (no acceptor-side change breaks existing default initiator behavior) explicitly required (US2 AC3)? [Coverage, Spec §US2 §AC3] — PASS: US2 AC3 reads "Given a Session with `role = initiator` (the existing default behavior), When `Session::open` is called, Then the session enters `LogonSent` and emits an initial Logon (existing behavior, regressed)." The word "regressed" here means "regression test covers this." T010 adds `InitiatorOpenStillReachesLogonSentAndEmitsLogon` as an explicit regression test. spec.md §US2 §AC3 + tasks.md §T010.
- [x] CHK038 Is the regression scenario "existing `NotConnected × non-Logon` row not regressed by the RC#3 fix" specified in Edge Cases? [Coverage, Spec §Edge Cases second bullet] — PASS: Edge Case second bullet reads "`Acceptor receiving a non-Logon first message` (`NotConnected × non-Logon`): existing matrix says `Disconnected`; the existing implementation handles this row correctly (`fsm_transition_matrix_test.cpp:376`). This slice MUST NOT regress it." The regression constraint is explicit and references the existing test by file + line. spec.md §Edge Cases second bullet.

## Edge Case Coverage

- [x] CHK039 Is the boundary "`SendingTime` exactly at `MaxLatency`" addressed (stale-but-well-formed vs missing/malformed)? [Edge Case, Spec §Edge Cases third bullet] — PASS: Edge Case third bullet reads "`SendingTime` exactly at the boundary of `MaxLatency`: existing 005 `sending_time_test.cpp::ExactBoundaryIsOk` confirms boundary acceptance. The new missing/malformed tests MUST be additive — they do not change the stale-but-well-formed boundary semantics." The boundary is preserved and the additive constraint is explicit. spec.md §Edge Cases third bullet.
- [x] CHK040 Is "TestRequest counter overflow at `UINT32_MAX`" addressed with an explicit wrap-around acceptance rule? [Edge Case, Spec §Edge Cases fourth bullet] — PASS: Edge Case fourth bullet reads "the per-session counter rolls over at `UINT32_MAX`. Wrap-around is acceptable; uniqueness within a single session lifetime is the contract, not cross-restart uniqueness." research.md D-3 also confirms: "Wrap-around at `UINT32_MAX` is acceptable." spec.md §Edge Cases fourth bullet + research.md §D-3.
- [x] CHK041 Is "`SeqnumManager::drain` failure during close" addressed with a documented logged-then-proceed policy AND a documented post-state (`closed_drained`)? [Edge Case, Spec §Edge Cases fifth bullet] — PASS: Edge Case fifth bullet reads "per Opus triage Fix Queue RC#7, drain failures are logged-then-proceed; `close()` still reports `closed_drained` and the destructor still completes safely." Both the policy and the post-state are documented. spec.md §Edge Cases fifth bullet + §FR-011.
- [x] CHK042 Is the `never_opened → ~Session` destruction path defined as safe (drain trivially succeeds on no-holders mutex)? [Edge Case, Spec §FR-011 + §US4 §AC3] — PASS: FR-011 reads "Sessions destroyed via the `never_opened → ~Session` path MUST also be safe (the mutex is in its default no-holders state, drain succeeds trivially)." US4 AC3 covers this as a specific acceptance scenario: "Given a Session that goes through the `never_opened → ~Session` path… When the Session is destroyed, Then the destructor completes without invoking the async_mutex teardown precondition violation." spec.md §FR-011 + §US4 §AC3.

## Non-Functional Requirements (specified quality, not behavior)

- [x] CHK043 Are the constitutional invariants binding on this slice (`[const §VIII.5]` alloc, `[const §X.2]` C ABI, `[const §XI.1-7]` concurrency, `[const §XV.9]` mutex-in-awaitable, `[arch §5.3]` noexcept) enumerated in spec.md Dependencies? [Coverage, Spec §Dependencies] — PASS: spec.md §Dependencies last bullet lists: "`[const §VIII.5]` (zero-alloc parse-to-fromApp), `[const §X.2]` (no C++ across C ABI), `[const §XI.1-7]` (coroutines + per-session strand + async_mutex + threading-affecting controls), `[const §XV.9]` (no `std::mutex` in awaitable headers), `[arch §5.3]` (no exceptions in noexcept window)." All five named invariants are present. spec.md §Dependencies.
- [x] CHK044 Is the conformance-spec compliance scope (`[FIX-SL §4.10]` matrix + `[FIX-SL §4.5.4]` SendingTime validation + `[FIX-SL §4.3]` Logout) cited inline at every relevant FR? [Coverage, Spec §FR-005..§FR-009] — PASS: FR-005/FR-006 cite `data-model.md:19` (which IS the `[FIX-SL §4.10]` matrix); FR-007/FR-008 cite `contracts/sending_time.hpp:23-24` (which cites `[FIX-SL §4.5.4]`); FR-009 cites "the LogonSent-special D-3 path" (traceable to `[FIX-SL §4.3]` via research.md D-3 and the sending_time.hpp contract). The Normative References section consolidates all three FIX-SL citations. Inline anchor at every relevant FR via contract reference; consolidated in Normative References per `[const §VI.5]`. spec.md §FR-005..FR-009 + §Normative References.
- [x] CHK045 Is the bench-regression tolerance (±5% vs `bench/baselines/session/*_baseline.json`) recorded in plan.md or in a referenced doc, so US1/US3/US4 implementers know the ceiling? [Coverage, Plan §Technical Context] — PASS: plan.md §Technical Context states "±5% gate vs `bench/baselines/session/*.json` per `[const §VIII.2]`" and provides a per-FR analysis showing each stays within the existing ceilings. T025 in tasks.md binds this: "diff against `bench/baselines/session/*_baseline.json` with ±5% tolerance." plan.md §Technical Context + tasks.md §T025.

## Dependencies & Assumptions

- [x] CHK046 Is the assumption "005 Gate-A-converged design is authoritative and unchanged" stated explicitly (not just implied)? [Assumption, Spec §Assumptions first bullet] — PASS: Assumptions first bullet reads "The 005 Gate-A-converged design is authoritative and unchanged. Every FR above traces to an existing 005 anchor… Gate A is NOT re-run for 009." Explicitly stated, not implied. spec.md §Assumptions.
- [x] CHK047 Is the branch-base assumption (rooted on `005-session-establishment-fsm` HEAD `4e621e1`) recorded so a reviewer cannot mistake the merge intent? [Assumption, Spec §Assumptions second bullet] — PASS: Assumptions second bullet reads "`009-session-fsm-finalize` is rooted on `005-session-establishment-fsm` HEAD `4e621e1`; the 25 prior commits (Phase 1–8 + /simplify + /speckit-verify) are inherited as base." Commit hash recorded. spec.md §Assumptions.
- [x] CHK048 Is the merge-target choice (back into `005-session-establishment-fsm` OR direct to `main`) explicitly deferred to merge time rather than picked silently? [Assumption, Spec §Assumptions third bullet] — PASS: Assumptions third bullet reads "At Gate B convergence, 009 merges back into `005-session-establishment-fsm`… OR 009 merges directly to `main`… The choice is deferred to /speckit-plan or to the user; this spec does not constrain it." Deferral is explicit. spec.md §Assumptions.
- [x] CHK049 Is the "no new contracts beyond `session_role` enum + `next_test_request_id_` member" boundary documented to prevent scope creep? [Assumption, Spec §Assumptions fourth bullet] — PASS: Assumptions fourth bullet reads "This slice MUST NOT introduce new headers under `include/fixpp/session/` or `src/session/` beyond what is required to express the per-session TestReqID counter (a member field on `Session`) and the `session_role` enum on `SessionConfig`." Realizability sub-check: `session_role` is a scoped enum (trivial, no dependencies). No completeness trap applies. spec.md §Assumptions.
- [x] CHK050 Is the test-infrastructure reuse assumption (existing `mock_clock` + transport double + `MessageStore` double + TSan/coverage presets cover all needs) explicit? [Assumption, Spec §Assumptions fifth bullet] — PASS: Assumptions fifth bullet reads "The existing `mock_clock`, in-memory transport double, test-double `MessageStore`, gtest/gmock, and TSan / coverage / ASan presets cover all FR-001..FR-011 verification needs — no new fixture authoring is required." Explicitly stated. spec.md §Assumptions.
- [x] CHK051 Is the "no Phase-9 interop coupling" boundary documented (the slice's 'Phase 9' naming collides by coincidence with `[[project_release_interop_quickfix_fix8]]` — the spec MUST disambiguate)? [Assumption, Spec §Assumptions last bullet] — PASS: Assumptions last bullet reads "This slice is independent of the per-release interop work (Phase 9 in the parent's planning files per `[[project_release_interop_quickfix_fix8]]`). The two share the 'Phase 9' naming only by coincidence — different scope, different cadence." The disambiguation is explicit and names the project memory key that defines the interop work. spec.md §Assumptions last bullet.
- [x] CHK052 Is the prerequisite "PR #81 round-1 artifacts on disk are the binding drift catalogue" recorded with exact paths to all three review docs? [Dependency, Spec §Dependencies] — PASS: spec.md §Dependencies second bullet lists three paths: `research/G19-fix-fpml-iso20022/research/reviews/codex_pr81_review.md`, `research/G19-fix-fpml-iso20022/research/reviews/opus_pr81_1_triage.md`, `research/G19-fix-fpml-iso20022/research/reviews/opus_pr81_paused.md`. All three paths recorded. spec.md §Dependencies.

## Ambiguities & Conflicts

- [x] CHK053 Is there any conflict between the spec's "no new entities" claim and the data-model.md modifications (the `session_role` enum IS a new declared type, even though housed inside an existing entity)? Spec should either say "no new entities outside the role enum" or restate "no new entities" with the enum carve-out. [Conflict, Spec §Key Entities, Data-model §E2] — SPEC-FIXED: The spec's §Key Entities reads "No new entities. This slice modifies existing entities…" which conflicts with the fact that `session_role` is a new enum type declared as part of E2 `SessionConfig`. While it is not a new domain entity (it is not an independently instantiable object with its own lifetime), calling it zero-new-types is technically inaccurate and could cause a reviewer to question the claim. Added a clarifying parenthetical to spec.md §Key Entities: changed "No new entities." to "No new domain entities. (`session_role` is a new enum type declared as part of E2 `SessionConfig` — a value type with no independent lifecycle, not a standalone domain entity.)" Edit: spec.md §Key Entities paragraph.
- [x] CHK054 Is the term "drift" defined or used consistently — does "drift" always mean "implementation deviated from the Gate-A design" vs "spec amended after Gate A"? [Ambiguity, Spec §Overview] — PASS: The spec's Overview first sentence reads "This slice closes implementation drift between the Gate-A-converged `005-session-establishment-fsm` design… and what actually shipped on branch `005-session-establishment-fsm`." The term is defined on first use as "implementation deviated from design" and is used consistently throughout. There is no usage where it means "spec amended." spec.md §Overview.
- [x] CHK055 Is the relationship between FR-013 ("extend conformance assertions") and SC-002 ("100% of `tc_*` outbound") clear enough that an implementer cannot satisfy FR-013 narrowly while missing SC-002 (e.g., adding tag-8 to one file)? [Ambiguity, Spec §FR-013, §SC-002] — PASS: FR-013 enumerates all 5 files by name; SC-002 says "100% of `tests/session/conformance/tc_*_test.cpp` outbound-frame assertions." An implementer who adds tag-8 to only one file satisfies neither FR-013 (which names all 5) nor SC-002 (which requires 100%). The 5-file enumeration in FR-013 makes the scope deterministic. spec.md §FR-013 + §SC-002.
- [x] CHK056 Does the spec resolve the apparent tension between US3-AC1 ("invalid BeginString → Disconnected") and the preserved Edge-Case ("`NotConnected × non-Logon` → Disconnected" already works) — are these the same row or different rows in the matrix? [Conflict, Spec §US3 §AC1 + §Edge Cases] — PASS: These are different rows. US3 AC1 is about a `NotConnected × inbound Logon (refused: BeginString validation fails)` matrix cell; the Edge Case second bullet is about `NotConnected × non-Logon first message`. The matrix in 005/data-model.md:19 has distinct cells for both: "inbound Logon (refused: BeginString/CompID/not-1st)" → Disconnected; and "inbound Heartbeat"/"inbound TestRequest" etc. → "refuse, → Disconnected (1st msg ≠ Logon)". FR-006 addresses the Logon-refused path; the non-Logon path is preserved as-is. No conflict. spec.md §US3 §AC1 + §Edge Cases second bullet + 005/data-model.md:19.
- [x] CHK057 Is the absence of any new `[NEEDS CLARIFICATION]` marker an intentional decision (per the spec's auto-validation in `requirements.md`) and not an overlooked gap? [Ambiguity, requirements.md §Requirement Completeness] — PASS: research.md §Audit verdict reads "No NEEDS CLARIFICATION markers remain. The three micro-decisions above are recorded for traceability — none represents a design choice that perturbs the 005 Gate-A-converged design. Inheritance from 005 covers the remaining design space." The spec's Overview also states "The 005 design is unchanged. Every functional requirement below traces to an existing 005 contract anchor." The absence is intentional and formally recorded. research.md §Audit verdict + spec.md §Overview.

## Test-Quality Discipline (binding innovation per `[[project_005_phase8_completeness_false_pass]]`)

- [x] CHK058 Does FR-012 specify that `static_assert` / compile-time `noexcept` attestation / `SUCCEED()` placeholders are INSUFFICIENT for any FR whose deliverable is a runtime behavior? [Completeness, Spec §FR-012] — PASS: FR-012 reads "the slice MUST land at least one **runtime-behavior test** (not a `static_assert` / compile-time noexcept attestation alone)" — the explicit parenthetical names all three prohibited patterns. spec.md §FR-012.
- [x] CHK059 Does FR-012 specify that the audit at slice close-out MUST verify test BODIES (not file names, not FR-listings, not task `[X]` markers)? [Clarity, Spec §FR-012] — PASS: FR-012 reads "The completeness audit T067-equivalent at slice close-out MUST verify test BODIES match the contract assertion, not just file-naming or task-marker existence (per project memory `[[project_005_phase8_completeness_false_pass]]`)." All three excluded patterns (file-naming, FR-listing, task `[X]` markers) are explicitly named. spec.md §FR-012.
- [x] CHK060 Is the audit-verdict location pinned (`library/.specify/decisions/009-session-fsm-finalize-completeness.md` in the format of `008-message-store-completeness.md`) so the auditor produces a comparable artifact? [Clarity, Tasks §T027] — PASS: T027 §(d) reads "emit verdict to `library/.specify/decisions/009-session-fsm-finalize-completeness.md` in the format of `008-message-store-completeness.md`." Both the output path and the format template are pinned. tasks.md §T027.
- [x] CHK061 Is the FR-to-contract-assertion-content mapping (e.g., "FR-001 requires assertions on tags 34 AND 52 AND 8 AND store-before-transport") specified somewhere — either inline in T027 or in a separate mapping table the auditor can grep? [Completeness, Tasks §T027 §c] — SPEC-FIXED (same edit as CHK031): tasks.md §T027 now contains a complete "FR-to-required-assertion-content mapping" table for all 12 behavioral FRs (FR-001..FR-013 excluding FR-012 meta), plus direction for the 005-inherited FRs in SC-006. The mapping is deterministic and grep-able by tag number. tasks.md §T027.
- [x] CHK062 Does the spec require that the audit also re-verify the **005-owned** FRs that were false-PASSed (FR-001 / FR-002 / FR-003 / FR-011 / FR-013), not just the new 009 FRs? [Coverage, Spec §SC-006] — PASS: SC-006 reads "A re-run of the feature-completeness audit (T067-equivalent) on the merged tree verifies test BODIES match contract assertions for **all 18 FRs from 005's spec**, and explicitly confirms FR-001 / FR-002 / FR-003 / FR-011 / FR-013 are now genuinely PASS (not the false-PASS pattern from `[[project_005_phase8_completeness_false_pass]]`)." The five 005-owned false-PASSed FRs are named explicitly. spec.md §SC-006.

## Traceability

- [x] CHK063 Does every FR-001..FR-013 cite at least one source contract anchor (e.g., `005/contracts/session.hpp:46-50` or `005/data-model.md:19`)? [Traceability, Spec §Requirements] — PASS: All 13 FRs end with a parenthetical citing at least one anchor: FR-001 → `005/contracts/session.hpp:46-50` + `[2e §4.1]`; FR-002 → `contracts/sending_time.hpp:8-18`; FR-003 → `contracts/sending_time.hpp:8-18`; FR-004 → `[2d §4.5]`; FR-005 → `data-model.md:19`; FR-006 → `data-model.md:19`; FR-007 → `contracts/sending_time.hpp:23-24`; FR-008 → `contracts/sending_time.hpp:23-24`; FR-009 → research.md D-3; FR-010 → `[const §XI.1-4]`; FR-011 → `async_mutex.hpp:155-160`; FR-012 → project memory; FR-013 → named files. All 13 have explicit anchors. spec.md §Functional Requirements.
- [x] CHK064 Does every FR-001..FR-013 cite the RC it closes (RC#1..RC#7), so the round-1 triage doc remains the verification baseline? [Traceability, Spec §Requirements] — PASS: Every FR's parenthetical includes "(Closes RC#N)": FR-001→RC#1; FR-002→RC#4; FR-003→RC#4; FR-004→RC#2; FR-005→RC#2; FR-006→RC#3; FR-007→RC#5; FR-008→RC#5; FR-009→RC#5; FR-010→RC#6; FR-011→RC#7; FR-012 and FR-013 reference cross-cutting quality issues (no single RC, but FR-013 notes "verifies FR-008/013"). FR-012 and FR-013 are explicitly cross-cutting, not RC-bound — their nature is meta / conformance-quality. spec.md §Functional Requirements.
- [x] CHK065 Does every SC-001..SC-007 trace either to a specific FR-NNN or to a Gate-B / completeness-audit outcome label? [Traceability, Spec §Success Criteria] — PASS: SC-001 → "7 PR #81 round-1 Root Causes (RC#1–#7)" (traces to FR-001..FR-011 collectively); SC-002 → FR-013; SC-003 → FR-010 via "two-session concurrent-liveness regression test"; SC-004 → FR-011 via "teardown-race regression test"; SC-005 → `/speckit-verify` outcome + plan.md W-1..W-4; SC-006 → "feature-completeness audit (T067-equivalent)" + FR-001/FR-002/FR-003/FR-011/FR-013; SC-007 → "Gate B run" + "P1=0 AND P2_unwaived=0." All 7 SCs have traceable source. spec.md §Success Criteria.
- [x] CHK066 Does the spec link to the 005 catalogue rows (S-001/2/3/4/7/8/9/15/16/19/20) it touches but does NOT flip — so the merge-time row-flip bookkeeping is unambiguous? [Traceability, Spec §Assumptions §Catalogue rows] — PASS: Assumptions "Catalogue rows" bullet reads "The 005-claimed catalogue rows (S-001/2/3/4/7/8/9/15/16/19/20 + folded `core/` row #4) remain owned by 005; this slice does not flip them. When the merged 005+009 tree closes, the row-flip from `implementing → done` happens per `[[feedback_pipeline_mark_done_step]]` at merge bookkeeping (5's T068 checklist still applies, refreshed against the 009 work)." All 11+ rows are named; the flip event is deferred to merge bookkeeping. spec.md §Assumptions.
- [x] CHK067 Is the requirement-ID scheme (FR-NNN, SC-NNN, RC#N, CHK###) consistent and unique across the spec / tasks / this checklist? [Traceability, all artifacts] — PASS: spec.md uses FR-001..FR-013 (13 items, no gaps, no duplicates), SC-001..SC-007 (7 items, no gaps), RC#1..RC#7 referenced consistently. tasks.md uses T001..T030 (30 items, no gaps). This checklist uses CHK001..CHK067 (67 items, no gaps). Cross-artifact: T006 covers FR-001; T010 covers FR-004/FR-005; T013 covers FR-006; T015 covers FR-007/FR-008; T017 covers FR-009; T019 covers FR-010; T021 covers FR-011; T009 covers FR-013. No ID collision detected. spec.md + tasks.md + drift-closure.md.

## Notes

- Each item is a question about what is WRITTEN in `spec.md` / `plan.md` / `data-model.md` / `tasks.md`, NOT a verification of code behavior. Behavior verification belongs to `/speckit-verify` (T028) and the feature-completeness audit (T027).
- The `checklist-auditor` subagent (pipeline step 9) dispositions each CHK item as **PASS** / **SPEC-FIXED** / **DD-DECIDED §X** / **WAIVED:<reason>** per `[[feedback_speckit_analysis_subagents]]`. Completeness / Clarity / Consistency items are NEVER WAIVED; only Coverage / Edge-Case / NFR items may be WAIVED with a recorded rationale.
- 67 total items.

---

## Audit Result

**Auditor:** checklist-auditor subagent (pipeline step 9)
**Date:** 2026-05-22
**Design-doc revision verified:** `.specify/2d-threading.md` v0.4, `.specify/2e-msgstore.md` v0.4, `.specify/2f-async-mutex.md` v1.5 (all Gate-A-converged, inherited from 005 unchanged).

| Disposition | Count |
|---|---|
| PASS | 64 |
| SPEC-FIXED | 3 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | **67** |

### SPEC-FIXED items

- CHK031 — tasks.md §T027 lacked a complete FR-to-assertion-content mapping table for all 18 FRs (SC-006 audit scope). Added deterministic mapping for FR-001..FR-013 (excluding meta FR-012) plus direction for 005-inherited FRs; affected: `tasks.md §T027`.
- CHK053 — spec.md §Key Entities stated "No new entities" while `session_role` is a new declared enum type (though not a new domain entity). Added a clarifying parenthetical: "No new domain entities. (`session_role` is a new enum type declared as part of E2 `SessionConfig` — a value type with no independent lifecycle, not a standalone domain entity.)"; affected: `spec.md §Key Entities`.
- CHK061 — same edit as CHK031 (the FR-to-assertion-content mapping table in tasks.md §T027 closes both items); affected: `tasks.md §T027`.

### DD-DECIDED items

None.

### WAIVED items

None.

### Realizability sub-check

The only new declared type is `session_role` (a `std::uint8_t`-backed scoped enum). Scoped enums are trivially-complete types: no defaulted destructor against incomplete type, no `unique_ptr` or by-value members of deferred-spec types. The D-15/T010a trap (007-threading-clock) does NOT apply. Verdict: clean.

### Anchors spot-verified

| Anchor | Doc revision | Section resolved |
|---|---|---|
| `[2d §7.9]` | v0.4 | "single effective_clock per session" (§7.9 heading, line 1253) |
| `[2d §4.5]` | v0.4 | "`fixpp::session::SessionConfig` — session-level frozen-at-open knobs" (§4.5 heading, line 496) |
| `[2e §4.1]` | v0.4 | "`MessageStore` interface" with "OUTBOUND CALL ORDERING (root cause #1)" durable-before-transmit contract (§4.1 heading, line 178) |
| `[2e §3.1]` | v0.4 | "Inherited primitives — exhaustive list (root cause #3)" (§3.1 heading, line 149) |
| `[2f §7.3]` | v1.5 | "Seqnum counter (Phase-4 session-module spec)" drain/teardown lifecycle (§7.3 heading, line 1782) |
| `async_mutex.hpp:155-160` | live | destructor precondition comment block (verified lines 154-160) |
| `async_mutex.hpp:683-690` | live | `~async_mutex()` terminate implementation (verified lines 683-692) |
| `seqnum_manager.hpp:50-53` | live | lifecycle note "The async_mutex must be drained… before destruction" (verified lines 50-53) |
| `seqnum_manager.hpp:96-100` | live | `drain()` method returning `mutex_.cancel_and_drain()` (verified lines 96-100) |
| `005/contracts/session.hpp:46-50` | live | `Session::send` outbound pipeline contract comment (verified lines 46-50) |
| `005/contracts/sending_time.hpp:23-24` | live | "Missing/unparseable inbound SendingTime ⇒ session-level Reject" (verified lines 23-24) |
| `005/data-model.md:19` | live | `[FIX-SL §4.10]` transition matrix row for NotConnected (verified line 19) |
| `[FIX-SL §4.10]` | Normative References section | binding for FR-005/FR-006 |
| `[FIX-SL §4.5.4]` | Normative References section | binding for FR-007/FR-008/FR-009 |
| `[FIX-SL §4.3]` | Normative References section | binding for FR-009 |

All anchors resolve in their signed-off design-doc revisions.
