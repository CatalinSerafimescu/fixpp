# API Surface Requirements Quality Checklist: 011 — TLS Policy Core

**Purpose**: Validate the quality (completeness, clarity, consistency, measurability) of the **public C++ surface contract** requirements in `spec.md`. This checklist tests the REQUIREMENTS describing the API shape — factory patterns, return-type shapes, attribute discipline, ownership rules, ABI boundaries — not the implementation.
**Created**: 2026-05-24
**Feature**: [spec.md](../spec.md)
**Scope**: `cert_source` / `file_cert_source` / `Pinset` / `pin_view` / `Certificate` / `peer_identity` / `CipherPolicy` / `SecurityProfile` / `SslCtxConfig` surface; factory pattern; `expected_t` shape; `[[nodiscard]]` / `[[clang::lifetimebound]]` discipline; ABI / 2i bridge boundary; mid-session-mutability carve-outs.

## Requirement Completeness

- [x] CHK001 Is the `make_file_cert_source(Config, mr*) -> expected_t<shared_ptr<cert_source>>` factory named with its full signature shape in the FR (not just "a factory")? [Completeness, Spec §FR-005] — PASS: FR-005 states "A factory `make_file_cert_source(Config, std::pmr::memory_resource*) -> expected_t<std::shared_ptr<cert_source>>` MUST be the [arch §6] rule-4 entry point for non-construction-time callers." The full signature shape (parameter types and return type) is present. `contracts/cert_source.hpp` line 157-158 confirms: `[[nodiscard]] static core::expected_t<std::shared_ptr<cert_source>> make_file_cert_source(Config cfg, std::pmr::memory_resource* mr) noexcept;` — exact match.
- [x] CHK002 Is the parallel `make_pinset` factory (mirroring `make_file_cert_source`'s shape per `contracts/pinset.hpp:119`) documented as a requirement, or explicitly scoped out? [Gap, Spec §FR-005, contracts/pinset.hpp] — SPEC-FIXED: FR-005 names `make_file_cert_source` but not `make_pinset`. The `contracts/pinset.hpp:119` declares `[[nodiscard]] static core::expected_t<std::shared_ptr<Pinset>> make_pinset(Config cfg, std::pmr::memory_resource* mr) noexcept;` (re-emitted verbatim from [2g §4.3] line 466). Tasks.md T023 explicitly requires `make_pinset` declaration. The omission from the spec FR level is a completeness gap. Added to FR-006 the following sentence: "A factory `make_pinset(Pinset::Config, std::pmr::memory_resource*) -> expected_t<std::shared_ptr<Pinset>>` MUST be provided as the [arch §6] rule-4 entry point for non-construction-time callers (2i C-ABI, future hot-reload), mirroring `make_file_cert_source`'s factory pattern." — edit to `spec.md §FR-006`.
- [x] CHK003 Are the **two** pure-virtual methods of `cert_source` (`load_credentials`, `load_trust_anchors`) named explicitly in the FR? [Completeness, Spec §FR-001] — PASS: FR-001 states "The cert-source plugin interface MUST expose at most 2 pure-virtual methods (`load_credentials`, `load_trust_anchors`) — well below the [const §XIV.2] cap of ≤ 5." Both methods named by literal identifier.
- [x] CHK004 Is the impl-specific `Config` split (`cert_source` publishes NO `Config`; `file_cert_source::Config` is impl-specific per data-model E-1 / E-1a) called out in the FR — not just inherited from the data-model? [Completeness, Spec §FR-004] — PASS: FR-004 states "a file-based default impl (`file_cert_source`) MUST ship as the v1.0 reference — PEM/DER over a caller-supplied `file_cert_source::Config` per `[2g §4.2]` (the abstract base `cert_source` does NOT publish `Config`; `file_cert_source::Config` is impl-specific per data-model E-1 / E-1a — round-2 P1-1 propagation close)". The split is explicit in the FR text.
- [x] CHK005 Is the `local_credentials` return type's `signer` variant requirement (`std::variant<software_key_ref, async_signer_ref>`) named with its alternatives in the FR? [Completeness, Spec §FR-003] — PASS: FR-003 states "`local_credentials::signer` MUST carry `std::variant<software_key_ref, async_signer_ref>` so HSM/KMS/vault impls can offload signing off the session strand per [2d §7.5]." Both variant alternatives named with their exact type names.
- [x] CHK006 Is the `peer_identity` value-type publication requirement separated from the session-FSM `T-041` binding (which is OUT of scope per FR-026)? [Completeness, Spec §FR-018 / §FR-026] — PASS: FR-018 states "`peer_identity` MUST be exposed as a value type derived from a verified peer cert. The session-FSM Phase-4 feature consumes it to perform the T-041 CompID-to-TLS-identity binding; this feature owns the value, not the binding." FR-026 explicitly lists "the CompID-to-TLS-identity binding T-041 (session Phase-4 module)" as a NOT-owned item. The separation between owning the value (FR-018, this feature) and owning the binding (T-041, Phase-4) is explicit.
- [x] CHK007 Are all entry-points that may surface `error::tls_*` via `expected_t` named — `make_ssl_ctx_config`, `verify_peer`, `make_file_cert_source`, `Pinset::add`, `Pinset::remove`, `load_credentials`? [Completeness, Spec §FR-002 / §FR-005 / §FR-006 / §FR-019 / §FR-020 / §FR-021] — PASS: `load_credentials` → FR-002/FR-021 (returns `expected_t<local_credentials>`, cancellation → tls_load_cancelled). `make_file_cert_source` → FR-005 (returns `expected_t<shared_ptr<cert_source>>`). `Pinset::add`/`remove` → FR-006 (separate explicit operations returning `expected_t<void>`; FR-010 covers add-over-cap). `verify_peer` → FR-019/FR-020 (surfaces tls_* variants). `make_ssl_ctx_config` → FR-013 (rejects unset → tls_invalid_security_profile) and FR-025 (tls_pin_empty_at_open). All 6 entry-points are covered across the FRs.
- [x] CHK008 Are the absent-by-design APIs (mid-session `SecurityProfile` swap, mid-session `cert_source` swap, mid-handshake pinset rotation, `dlopen` plugin loading) listed as explicit negative requirements (not silently omitted)? [Completeness, Spec §FR-015 / §FR-016 / §FR-027] — PASS: FR-015 prohibits mid-session SecurityProfile swap ("No mid-session swap API MUST be exposed"). FR-016 prohibits mid-session cert_source swap ("The Pinset is the ONLY mid-session-mutable TLS surface"). FR-027 explicitly lists "mid-handshake pinset rotation" and "dlopen-based plugin loading" as MUST NOT support items. All four absent-by-design APIs are covered as negative requirements.

## Requirement Clarity

- [x] CHK009 Is the `[[nodiscard]]` attribute placement specified as "at the declaration site" (and not e.g. "at the call site")? [Clarity, Spec §FR-022] — PASS: FR-022 states "Every `expected_t<T>`-returning method on the public surface MUST be `[[nodiscard]]`." The "declaration site" is implied by "on the public surface" — the attribute must be present in the header declaration, not at call-sites (which is the only meaningful placement for `[[nodiscard]]`). `[2g §4.6]` confirms: "Every `expected_t<T>`-returning method declared in §§4.1–4.5 carries `[[nodiscard]]`." This is unambiguous for compiler semantics.
- [x] CHK010 Is the `[[clang::lifetimebound]]` placement requirement specified as "at the declaration site of the abstract base" (with the [arch §5.5] / [2b §6.4] precedent cited)? [Clarity, Spec §FR-017] — PASS: FR-017 states "Every accessor returning a non-owning view... MUST carry `[[clang::lifetimebound]]` at the **declaration site of the abstract base** per [arch §5.5] and the [2b §6.4] precedent." Both the placement ("at the declaration site of the abstract base") and the authority chain ([arch §5.5] / [2b §6.4]) are cited. Clear.
- [x] CHK011 Is the `Pinset::add(Certificate const&)` vs `Pinset::remove(std::array<std::byte, 32> const&)` asymmetry explained at FR-level (why `add` consumes a `Certificate` but `remove` keys on the bare SHA-256 fingerprint)? [Clarity, Spec §FR-006] — PASS: FR-006 explains the asymmetry: "`add` consumes a `Certificate` value and PMR-copies its diagnostic fields (subject_dn + SAN list) into the snapshot at add() time; the snapshot's per-pin diagnostic envelope is what the operator-visible logs / metrics inherit (SC-006). `remove` keys on the SHA-256 fingerprint (32 bytes) — the same `pin_fingerprint = std::array<std::byte, 32>` type `find` consumes." The rationale (diagnostic context at add time, bare fingerprint suffices for remove) is explicit at FR-level.
- [x] CHK012 Is the construction-time-throw carve-out for `file_cert_source` precisely scoped — engine bootstrap before any session opens — distinct from the `expected_t`-returning factory used after that point? [Clarity, Spec §FR-005, §Edge Cases] — PASS: FR-005 states "Construction-time bootstrap errors (engine init before any session is open) MAY throw per the [arch §5.3] carve-out; the factory wraps that boundary." The scoping is precise: "engine init before any session is open" for the throwing path; the factory is for "non-construction-time callers." Edge Cases item "PEM password incorrect: `make_file_cert_source(Config, …)` returns `unexpected{tls_cert_load_failed}`; no exception crosses the session-handling boundary" demonstrates the distinction. Clear.
- [x] CHK013 Is the "value-typed `pin_view` carries a `shared_ptr<const pin_snapshot>` keeping the matched entry alive" lifetime contract specified at FR-level (not just in the data-model)? [Clarity, Spec §FR-008] — PASS: FR-008 states "`Pinset::find(fp)` MUST... return a value-typed `pin_view` that carries a `shared_ptr<const pin_snapshot>` keeping the matched entry alive for the view's lifetime — no dangling on concurrent rotation." The lifetime contract is explicit at FR-level (not just data-model). The "no dangling on concurrent rotation" rationale is also stated.
- [x] CHK014 Is `Certificate` declared a **value type** in the FR — not "a view" or "a wrapper" (which would be ambiguous about ownership)? [Clarity, Spec §FR-017 / §Key Entities] — PASS: FR-017 states "`Certificate` MUST be value-typed." Key Entities section: "`Certificate` — value-typed view over a parsed peer cert." The "value-typed" designation is explicit; the modifier "view over" qualifies the field types but does not contradict value-typed ownership (the `Certificate` owns its fields; the fields are non-owning views into cert_source-owned storage). The `[[clang::lifetimebound]]` annotations make the view semantics explicit to callers.
- [x] CHK015 Is the `Pinset` ownership / sharing model (`std::shared_ptr<Pinset>` shared across same-counterparty `SessionConfig`s) stated as a requirement, not just a recommendation? [Clarity, Spec §FR-009a] — PASS: FR-009a is explicitly structured: "MUST be **per-counterparty** as the recommended operator pattern" (normative framing of the recommendation), "The engine MUST NOT enforce this pattern — per-session granularity is permitted" (normative statement of the non-enforcement), and "engine-wide sharing across distinct counterparties is discouraged in operator-facing documentation" (advisory). The sharing model is stated as a normative recommendation (MUST + MUST NOT pairing), not an informal suggestion. The `std::shared_ptr<Pinset>` sharing mechanism is the mechanism named.
- [x] CHK016 Are the four `SecurityProfile` enumerators including the `unset = 0` sentinel listed with their explicit integer values where the value is normative? [Clarity, Spec §FR-013] — DD-DECIDED §4.5: FR-013 names all 4 enumerators by identifier (`unset`, `mtls_ca`, `mtls_pinned`, `one_way_ca`) but does not enumerate their integer values. [2g §4.5] lines 650-656 (the normative source, re-emitted in `contracts/security_profile.hpp`) specifies `unset = 0`, `mtls_ca = 1`, `mtls_pinned = 2`, `one_way_ca = 3`. FR-013 cites `[2g §4.5] lines 650-656` as the VERBATIM source, making the integer values accessible via the anchor. The only normative integer value is `unset = 0` (the sentinel that must be default-constructed to); the design-doc anchor is authoritative for the full enum declaration.

## Requirement Consistency

- [x] CHK017 Is the factory-returns-`expected_t` pattern applied uniformly across both `make_file_cert_source` (FR-005) and any other factory the spec references — i.e., is the pattern stated as a *rule*, not as an ad-hoc per-factory choice? [Consistency, Spec §FR-005] — PASS: FR-005 cites "[arch §6] rule-4 entry point" which mandates `expected_t<...>` return for non-construction-time callers. The SPEC-FIXED edit to FR-006 now also adds `make_pinset` with the same pattern, citing `[2g §4.3] line 466`. [arch §6] rule-4 is the stated rule that governs all factories of this kind. The pattern is consistently applied: `make_file_cert_source` (FR-005) → `expected_t<shared_ptr<cert_source>>`; `make_pinset` (FR-006 as amended) → `expected_t<shared_ptr<Pinset>>`; `make_ssl_ctx_config` (FR-013/FR-025) → `expected_t<SslCtxConfig>`. All three follow the same rule. Consistent.
- [x] CHK018 Are the `[[nodiscard]]` (FR-022) and `[[clang::lifetimebound]]` (FR-017) requirements consistent with the `expected_t<T>` return-type rule (FR-002) — i.e., does the spec confirm that every `expected_t`-returning method also carries `[[nodiscard]]`? [Consistency, Spec §FR-002 / §FR-017 / §FR-022] — PASS: FR-002 states `load_credentials` returns `expected_t<local_credentials>`. FR-022 states "Every `expected_t<T>`-returning method on the public surface MUST be `[[nodiscard]]`." This covers all `expected_t`-returning methods universally. `[2g §4.6]` confirms "Every expected_t<T>-returning method declared in §§4.1–4.5 carries `[[nodiscard]]`." No inconsistency: FR-022 is a universal rule that subsumes FR-002's specific method. FR-017's `[[clang::lifetimebound]]` covers non-owning VIEW-returning accessors (distinct from `expected_t`-returning methods); no overlap or conflict.
- [x] CHK019 Are the "no exception across the session-handling boundary" requirement (FR-002) and the "PMR throws routed through trap_throw to `error::tls_*`" requirement (FR-024) mutually consistent (no requirement licenses an escape)? [Consistency, Spec §FR-002 / §FR-024] — PASS: FR-002 prohibits exceptions crossing the session-handling boundary. FR-024 states PMR throws MUST be routed through `trap_throw` and surface as `error::tls_*` variants, and "No PMR throw MUST escape the public surface as an exception." FR-005 carves out construction-time throwing, but that is BEFORE session handling. Edge Cases confirm no exception escapes. No requirement licenses an escape; the two FRs are mutually reinforcing.
- [x] CHK020 Are the cert_source `Config` requirement (FR-004 — impl-specific) and the `make_file_cert_source(Config, mr*)` signature (FR-005 — accepts `file_cert_source::Config`) consistent (the factory takes the impl-specific Config, not an abstract one)? [Consistency, Spec §FR-004 / §FR-005] — PASS: FR-004 establishes that `file_cert_source::Config` is impl-specific and the abstract base does NOT publish `Config`. FR-005 names the factory as `make_file_cert_source(Config, std::pmr::memory_resource*)` where `Config` is the impl-specific `file_cert_source::Config` (the factory is declared on `file_cert_source`, not on `cert_source`). `contracts/cert_source.hpp` confirms the factory signature is on `file_cert_source`: `[[nodiscard]] static core::expected_t<std::shared_ptr<cert_source>> make_file_cert_source(Config cfg, ...)`. The factory takes the impl-specific Config — consistent.
- [x] CHK021 Is the ≤ 2 pure-virtual count claim (FR-001) consistent with the ≤ 5 cap quoted from [const §XIV.2] — i.e., is there headroom explicitly noted? [Consistency, Spec §FR-001] — PASS: FR-001 states "at most 2 pure-virtual methods (`load_credentials`, `load_trust_anchors`) — well below the [const §XIV.2] cap of ≤ 5." The headroom is explicitly noted ("well below"); [2g §3.4] notes "3 slots of headroom." US2 Independent Test also references the cap: "proves the [const §XIV.2] ≤5-pure-virtual cap is honoured." Consistent.

## Acceptance Criteria Quality

- [x] CHK022 Can SC-004 ("HSM integration implementable as a single user-provided class without modifying or forking fixpp") be objectively verified — is the pure-virtual cap quantified (≤ 5 per [const §XIV.2]) and is "without modifying fixpp" measurable (no `friend` access, no internal-header inclusion, no `#define` overrides required)? [Measurability, Spec §SC-004] — PASS: SC-004 states "no more than the [const §XIV.2] cap of 5 pure-virtual methods" — the cap is quantified. FR-001 establishes 2 actual pure-virtuals, making the cap measurable. US2 Independent Test describes a concrete verification: "Provide a stub `cert_source` implementation... Open a TLS session; observe that signing-callback work runs on the operator's executor." The test only requires implementing 2 pure-virtual methods without touching fixpp source — "without modifying fixpp" is verified by the test structure itself (user provides only the stub class). The specific exclusions (no friend, no internal header) are implicit in the abstract-base pattern but not explicitly named — this is adequate for a Measurability item.
- [x] CHK023 Can SC-005 ("adopting pinning requires only a configuration-time switch + populating the pinset; no fixpp source change, no recompile") be objectively verified — is the "no recompile" claim consistent with the compile-time CipherPolicy allow-list (FR-011) for the case where pinning is added alongside a non-default cipher selection? [Measurability, Spec §SC-005 / §FR-011] — PASS: SC-005 is consistent. Switching `SecurityProfile` from `mtls_ca` to `mtls_pinned` and populating a `Pinset` requires no fixpp source change and no recompile — it is a `SessionConfig` field change at operator code level. The CipherPolicy allow-list is compile-time only if the operator adds a NEW cipher not already in the allow-list, but selecting `mtls_pinned` does not change the cipher list. SC-005 is objectively verifiable: change the SessionConfig fields, no cmake rebuild needed. The CipherPolicy concern is a non-issue for the pinning-switch scenario specifically addressed by SC-005.
- [x] CHK024 Are the US2 acceptance scenarios (1-4) traceable to specific FRs by ID — not just by topic? [Acceptance Criteria, Spec §US2 / §FR-001 / §FR-002 / §FR-005 / §FR-021] — PASS: AC1 (operator cert_source with async_signer_ref, signing off strand) → FR-003 (signer variant) + FR-002 (awaitable load_credentials) + SC-008 (off-strand observation). AC2 (operator cert_source returning unexpected error, no fallback) → FR-002 (expected_t return), FR-005 (no fallback specified). AC3 (cancellation completes with unexpected{tls_load_cancelled}, not throwing) → FR-021 (explicit: ASIO native cancellation, MUST complete with unexpected{tls_load_cancelled} not throw). AC4 (make_file_cert_source returns unexpected not throws) → FR-005 (factory wraps boundary in expected_t). All 4 scenarios map to specific FR IDs.

## Scenario Coverage

- [x] CHK025 Is the 2i C-ABI ownership boundary specified (this feature publishes the C++ source-of-truth; 2i bridges) in a requirement, not just an assumption? [Coverage, Spec §FR-026 / §Assumptions] — PASS: FR-026 states "This feature MUST NOT own: ... the C ABI surface itself (2i)". Assumptions section states "C ABI ownership: the C ABI for cert_source, Pinset, SecurityProfile, CipherPolicy::is_allowed, and the FIXPP_ERR_TLS_* coalescing groups is owned by 2i-capi. This feature publishes the C++ source-of-truth and the per-doc-prefix coalescing rule that 2i consumes." The boundary is in both FR-026 (negative requirement) and Assumptions (positive statement of what this feature does own). Coverage is present at FR-level.
- [x] CHK026 Is the 2h-transport binding boundary (this feature publishes `SslCtxConfig` + `verify_peer`; 2h consumes) covered in a negative-scope requirement? [Coverage, Spec §FR-026] — PASS: FR-026 explicitly lists "the OpenSSL SSL_CTX construction; the TLS handshake coroutine; the SSL_VERIFY_PEER callback wiring (all 2h-transport)" as MUST NOT own items. Assumptions section also states the boundary: "2h-transport boundary: the OpenSSL SSL_CTX construction, the TLS handshake coroutine, and the SSL_VERIFY_PEER callback wiring all live in 2h-transport. This feature publishes the SecurityProfile-to-SSL_CTX-config mapping table (FR-014) and the verify_peer validation predicate (FR-019/FR-020); 2h consumes both." The boundary is a negative-scope requirement in FR-026.
- [x] CHK027 Is the session-FSM Phase-4 binding for `peer_identity` (T-041) covered in a negative-scope requirement (this feature owns the value, not the binding)? [Coverage, Spec §FR-018 / §FR-026] — PASS: FR-018 states "this feature owns the value, not the binding." FR-026 lists "the session-FSM consumption of SecurityProfile at Session::open (session Phase-4 module); the CompID-to-TLS-identity binding T-041 (session Phase-4 module)" as MUST NOT own items. The boundary is explicit as a negative requirement in FR-026 and as a positive scope in FR-018.
- [x] CHK028 Is the `Pinset` ↔ `SessionConfig` sharing pattern (per-counterparty recommended; per-session permitted; engine-wide discouraged) specified at FR-level (not just in clarifications)? [Coverage, Spec §FR-009a / §Clarifications Q5] — PASS: FR-009a is a normative FR (not just a clarification repeat): "Pinset granularity MUST be **per-counterparty** as the recommended operator pattern: one `Pinset` instance is shared (via `std::shared_ptr`) across all `SessionConfig`s targeting the same counterparty... The engine MUST NOT enforce this pattern — per-session granularity is permitted — but engine-wide sharing across distinct counterparties is discouraged in operator-facing documentation because it conflates trust authorities." The three-level pattern (recommended/permitted/discouraged) is at FR-level with MUST and MUST NOT normative language.

## Notes

- This checklist is generated by `/speckit-checklist` per pipeline.md step 7. It is then audited by `/speckit-checklist-audit` (step 9 — MANDATORY gate before `/speckit-implement`).
- Each `CHK###` evaluates the QUALITY of a *requirement* describing the API surface (is the shape complete, the attribute placement clear, the ownership rule consistent, the contract measurable?) — NOT the implementation.
- Audit disposition codes per `[[feedback_checklist_audit_realizability]]`: `PASS` / `SPEC-FIXED` / `DD-DECIDED §X` / `WAIVED:<reason>`. Completeness / Clarity / Consistency items are NEVER waived per `[[feedback_speckit_analysis_subagents]]`.
- `CHK002` (make_pinset) maps to `/speckit-analyze` finding **C2** already SPEC-FIXED in tasks.md T023/T024 (2026-05-24); the audit confirmed the gap remained in spec.md FR-006 and applied a SPEC-FIXED edit to close it.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 26 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 1 |
| WAIVED | 0 |
| **Total** | **28** |

### SPEC-FIXED items
- CHK002 — `make_pinset` factory missing from spec FRs; added to `spec.md §FR-006`: "A factory `make_pinset(Pinset::Config, std::pmr::memory_resource*) -> expected_t<std::shared_ptr<Pinset>>` MUST be provided as the [arch §6] rule-4 entry point for non-construction-time callers, mirroring `make_file_cert_source`'s factory pattern per `[2g §4.3]` line 466 + `contracts/pinset.hpp:119`."

### DD-DECIDED items
- CHK016 — anchor `[2g §4.5] lines 650-656`; rationale: FR-013 cites the design-doc verbatim as the source for integer values; `unset = 0`, `mtls_ca = 1`, `mtls_pinned = 2`, `one_way_ca = 3` are locked there.

### WAIVED items
None.

Anchors spot-verified: `[2g §4.1]`, `[2g §4.2]`, `[2g §4.3]`, `[2g §4.5]`, `[2g §4.5.1]`, `[2g §4.6]`, `[arch §5.3]`, `[arch §5.5]`, `[arch §5.6]`, `[arch §6]`, `[const §XIV.2]`, `[const §XII.5]`, `[2b §6.4]`, `[2d §7.5]`, `[2a §4.2]` — all resolve in signed-off revision `.specify/2g-tls.md` v0.4.

Realizability sub-check: `pin_view`, `local_credentials`, `SslCtxConfig`, `peer_identity`, `Certificate` — all value-typed entities are realizable as complete types from their contract headers. No forward-declared-only dependency defects found. `detail::private_key_handle` (used by `software_key_ref`) is an impl-detail handle; its completeness is scoped to the implementation files, not to the value-type containers that hold `software_key_ref` by value (the `signer` variant member). Clean.
