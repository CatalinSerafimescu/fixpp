# Security Requirements Quality Checklist: 011 — TLS Policy Core

**Purpose**: Validate the quality (completeness, clarity, consistency, measurability, coverage) of the TLS / crypto / trust requirements written in `spec.md`. This checklist tests the REQUIREMENTS — not the implementation. The implementation is gated separately by `/speckit-verify` (T053) and `/gate-b`.
**Created**: 2026-05-24
**Feature**: [spec.md](../spec.md)
**Scope**: Cipher policy, banned-token enumeration, TLS-version posture, SecurityProfile semantics, verify_peer ordering, DoS bounds, error-variant taxonomy, pinning, deprecation discipline, constitution alignment.

## Requirement Completeness

- [x] CHK001 Are all 16 `error::tls_*` variants enumerated by literal name in the requirements (not described as a count)? [Completeness, Spec §FR-025] — PASS: FR-025 enumerates all 16 variants by literal name, grouped by C-ABI coalescing target: CONFIG group (`tls_cert_load_failed`, `tls_cert_parse_failed`, `tls_cipher_not_allowed`, `tls_invalid_security_profile`, `tls_sign_callback_unavailable`, `tls_pin_empty_at_open`); HANDSHAKE group (`tls_handshake_failed`, `tls_rsa_key_too_large`, `tls_cert_der_too_large`, `tls_san_entries_exceeded`, `tls_pin_mismatch`); PINSET group (`tls_pin_not_found`, `tls_pin_already_present`, `tls_pinset_capacity_exhausted`); RUNTIME group (`tls_pinset_alloc_failed`); CANCELLED group (`tls_load_cancelled`). Count = 6+5+3+1+1 = 16. Spec text confirms "15-variant set re-emitted from [2g §6.6] plus one additive amendment = 16 variants total."
- [x] CHK002 Are all 12 banned cipher / version / KX tokens (`RC4`, `DES`, `3DES`, `MD5`, `DH_anon`, `NULL`, `EXPORT`, `TLS_RSA`, `CBC`, `SHA1`, `TLS_AES_128_CCM`, `0RTT`) enumerated by literal name in the policy requirement? [Completeness, Spec §FR-011] — DD-DECIDED §4.4: [2g §4.4] `CipherPolicy::banned_tokens` array enumerates all 12 literal tokens by exact string name and is re-emitted verbatim in `contracts/cipher_policy.hpp`. FR-011 uses natural-language descriptions (e.g., "export-grade", "CBC-mode TLS 1.2", "1024-bit RSA") citing [const §XII.4]/[const §XV.11]; the exact token strings are locked by the design-doc's normative constexpr array, which is the authoritative source. Spec correctly cites the authority chain; literal token enumeration is the design-doc's domain.
- [x] CHK003 Are all 4 `SecurityProfile` enumerators (`unset`, `mtls_ca`, `mtls_pinned`, `one_way_ca`) — including the sentinel — listed in the requirement? [Completeness, Spec §FR-013] — PASS: FR-013 explicitly states "MUST publish four enumerators per [2g §4.5] lines 650-656: the `unset = 0` sentinel plus the three user-visible values `mtls_ca`, `mtls_pinned`, and `one_way_ca`." All 4 enumerators named by literal identifier including the sentinel.
- [x] CHK004 Are all 10 evaluation steps of `verify_peer` listed in canonical order in the FR (not just summarised)? [Completeness, Spec §FR-020a] — PASS: FR-020a enumerates all 10 steps in explicit numbered sequence: (1) per-cert DER size, (2) RSA lower bound, (3) RSA upper bound, (4) ECDSA curve, (5) chain depth, (6) SAN cardinality, (7) X.509 version, (8) expiration against effective clock, (9) pinning under mtls_pinned, (10) CipherPolicy::is_allowed. Step 9 qualifies that NEVER calls `cfg.pinset->find/contains/snapshot` mid-verification. The Assumptions section also repeats the canonical 10-step order verbatim.
- [x] CHK005 Are all 14 enumerated edge cases (concurrent rotation, `pin_view` outliving `remove`, PEM password failure, DoS-cap rejections, expired-against-effective-clock, async cancellation, PMR throw, `add` over `max_pins`, mid-session `SecurityProfile` swap absence, mid-session `cert_source` swap absence, C-ABI banned-cipher override) accompanied by an explicit refusal variant or rejection mechanism in a FR? [Completeness, Spec §Edge Cases, §FR-019 / §FR-020 / §FR-025] — PASS: Each of the 14 edge cases in spec §Edge Cases links to explicit FR coverage: concurrent rotation → FR-009 + FR-008 (pin_view lifetime contract); pin_view outliving remove → FR-008 (shared_ptr snapshot keeps pin alive); PEM password failure → FR-005 + FR-025 (`unexpected{tls_cert_load_failed}`); DoS-cap rejections → FR-019 (tls_rsa_key_too_large / tls_cert_der_too_large / tls_san_entries_exceeded); expired → FR-020 + FR-025 (tls_handshake_failed "expired"); async cancellation → FR-021 (tls_load_cancelled); PMR throw → FR-024 (trap_throw → tls_* variants); add over max_pins → FR-010 + FR-025 (tls_pinset_capacity_exhausted); mid-session SecurityProfile swap absence → FR-015; mid-session cert_source swap absence → FR-016; C-ABI cipher override → FR-012 (is_allowed returns false → tls_cipher_not_allowed). All 14 have explicit FR coverage.
- [x] CHK006 Are the seven `tls_handshake_failed` sub-reasons (`"expired"`, `"not_yet_valid"`, `"rsa_under_min"`, `"ecdsa_curve"`, `"chain_too_deep"`, `"x509_v1"`, `"sigalg_disallowed"`) enumerated by literal string in the grouping-variant requirement? [Completeness, Spec §FR-025] — PASS: FR-025 HANDSHAKE group entry for `tls_handshake_failed` explicitly lists sub-reasons as literal quoted strings: `"expired"` / `"not_yet_valid"` / `"rsa_under_min"` / `"ecdsa_curve"` / `"chain_too_deep"` / `"x509_v1"` / `"sigalg_disallowed"` — all 7 enumerated.
- [x] CHK007 Are PSK, CRL / OCSP infrastructure, mid-handshake pinset rotation, mid-session `SecurityProfile` swap, mid-session `cert_source` swap, and `dlopen`-based plugin loading EXPLICITLY listed as out-of-scope (not merely omitted)? [Completeness, Spec §FR-027] — PASS: FR-027 explicitly lists all six as negative requirements: "MUST NOT support: PSK auth (T-012, post-v1); CRL / OCSP infrastructure (post-v1); mid-handshake pinset rotation; mid-session `SecurityProfile` swap; mid-session `cert_source` swap; `dlopen`-based plugin loading." All 6 items named.
- [x] CHK008 Are TLS 1.3 0-RTT, TLS ≤ 1.1, SSL, CBC-mode TLS 1.2, SHA-1, 1024-bit RSA, DH_anon, NULL, export-grade EXPLICITLY listed in the compile-time-refusal requirement (not just "banned ciphers")? [Completeness, Spec §FR-011] — PASS: FR-011 explicitly names in parentheses: "RC4, DES, 3DES, MD5, DH_anon, NULL, export-grade, CBC-mode TLS 1.2, SHA-1, 1024-bit RSA, TLS 1.3 0-RTT, TLS ≤1.1, SSL" — every one of the 9 CHK-listed items (0-RTT, TLS≤1.1, SSL, CBC-mode TLS 1.2, SHA-1, 1024-bit RSA, DH_anon, NULL, export-grade) appears by name in the FR body.
- [x] CHK009 Is the cancellation-completes-with-`tls_load_cancelled` (vs throwing) rule stated as a positive requirement? [Completeness, Spec §FR-021] — PASS: FR-021 states "Cancellation MUST complete with `expected_t::unexpected{tls_load_cancelled}` (no thrown exception)." The positive requirement is present and explicitly contrasts with throwing.
- [x] CHK010 Are the C-ABI coalescing groups (`FIXPP_ERR_TLS_CONFIG` / `_HANDSHAKE` / `_PINSET` / `_RUNTIME` / `_CANCELLED`) defined as a normative mapping in the requirements (not just a comment)? [Completeness, Spec §FR-025] — PASS: FR-025 defines the groups as a normative bulleted mapping with the C-ABI group name as the heading for each bullet: `FIXPP_ERR_TLS_CONFIG`, `FIXPP_ERR_TLS_HANDSHAKE`, `FIXPP_ERR_TLS_PINSET`, `FIXPP_ERR_TLS_RUNTIME`, `FIXPP_ERR_CANCELLED` — all 5 groups defined normatively with their member variants.

## Requirement Clarity

- [x] CHK011 Is "hardened-by-default trust mode" defined with measurable refusal criteria (banned ciphers, TLS versions, cert parameters), not as a marketing term? [Clarity, Spec §US3] — PASS: US3 opens with the definition backed by 5 acceptance scenarios, each measurable: (1) compile-time build failure with diagnostic naming the violation; (2) CipherPolicy::is_allowed returning false; (3) verify_peer refusing RSA-1024 with tls_handshake_failed sub-reason "rsa_under_min"; (4) verify_peer refusing X.509 v1; (5) session open refused with tls_pin_empty_at_open when mtls_pinned + empty pinset. The US3 body paragraph enumerates the refused categories explicitly. "Hardened-by-default" is backed by measurable FRs (FR-011, FR-013, FR-019, FR-020, FR-020a, FR-025).
- [x] CHK012 Is the FIXS-RC1 envelope enumerated (specific ciphers / versions / key sizes / SAN limits) at FR-level, not just cited as `[FIXS RC1]`? [Clarity, Spec §FR-011 / §FR-019 / §FR-020] — PASS: FR-011 enumerates the four normative allow-lists (TLS 1.3 suites, TLS 1.2 ECDHE-AEAD suites, KX groups X25519/secp256r1/secp384r1, sig algs ECDSA P-256/P-384/RSA-PSS≥2048) plus banned items; FR-019 names default caps (`max_rsa_key_bits 8192`, `max_cert_der_bytes 16 KiB`, `max_san_entries 64`) with explicit variant names; FR-020 names `max_chain_depth 8`, RSA ≥ 2048, ECDSA P-256/P-384, X.509 v1 rejection, effective-clock expiration check. The envelope is enumerated at FR-level with specific values.
- [x] CHK013 Are the four `SecurityProfile`-to-`SSL_CTX` mapping rows specified as a normative table, not as prose? [Clarity, Spec §FR-014] — PASS: FR-014 states the mapping table "MUST be published as a normative table; the table is the contract 2h consumes" and the table itself is present in `contracts/security_profile.hpp` (re-emitted verbatim from [2g §4.5.1]) with all 4 rows (mtls_pinned, mtls_ca, one_way_ca deprecated, unset) and columns (verify_mode, local cert, CA anchors, pinset required, pin check policy). The spec FR-014 is the normative pointer to the table; the table is the deliverable.
- [x] CHK014 Is "first violation hit" defined unambiguously (the ten-step ordering is canonical and total — no ties)? [Clarity, Spec §FR-020a] — PASS: FR-020a states "MUST short-circuit on the first violation hit and surface exactly one `error::tls_*` variant per failed handshake (no aggregate report)." The ordering is "BINDING CONTRACT" (capitalized), the 10-step list is numbered sequentially with no gaps, no ties, and the Assumptions section repeats it verbatim confirming canonicity. The cite `[2g §6.5.1]` + `contracts/security_profile.hpp lines 156-165` triple-anchors it.
- [x] CHK015 Is the distinction between `tls_pin_empty_at_open` (operator config error) and `tls_pin_mismatch` (peer cert error) stated explicitly in the variant requirement? [Clarity, Spec §FR-025 / §Clarifications Q2] — PASS: FR-025 explicitly distinguishes: `tls_pin_empty_at_open` is in the CONFIG group with note "(distinct from `tls_pin_mismatch` — surfaces at session-open when `mtls_pinned` is paired with a non-null empty pinset)". `tls_pin_mismatch` is in the HANDSHAKE group ("peer cert SHA-256 is not in the captured handshake-time Pinset snapshot"). US3 AC5 further clarifies: "distinct from `tls_pin_mismatch` so operator logs separate 'fixpp-config problem' from 'peer-cert problem'". The distinction is explicit at both FR and AC level.
- [x] CHK016 Are the default DoS-cap values (`max_rsa_key_bits = 8192`, `max_cert_der_bytes = 16 KiB`, `max_san_entries = 64`, `max_chain_depth = 8`) named in the FR — not just referenced as "operator caps"? [Clarity, Spec §FR-019 / §FR-020] — PASS: FR-019 names `max_rsa_key_bits` (default 8192), `max_cert_der_bytes` (default 16 KiB), `max_san_entries` (default 64) with their default values and error variants. FR-020 names `max_chain_depth` (default 8). All four defaults are explicitly named in the FRs.
- [x] CHK017 Is the pinning lookup key requirement specified as "SHA-256 of leaf cert DER (32 bytes)", not just "fingerprint"? [Clarity, Spec §FR-006 / §FR-008 / §Clarifications Q4] — PASS: FR-006 states the `remove` key type as "SHA-256 fingerprint (32 bytes) — the same `pin_fingerprint = std::array<std::byte, 32>` type `find` consumes." FR-008 states "`Pinset::find(fp)` MUST take the leaf cert's SHA-256-of-DER fingerprint (`std::array<std::byte, 32>`)." The Clarifications Q4 confirms "SHA-256 fingerprint of the leaf cert's DER encoding (32 bytes)". Fully specified.
- [x] CHK018 Is the asymmetry between `add(Certificate const&)` (consumes a `Certificate` so the snapshot carries diagnostic context) and `remove(std::array<std::byte, 32> const&)` (keys on the bare fingerprint) explained at FR-level? [Clarity, Spec §FR-006] — PASS: FR-006 explicitly explains: "`add` consumes a `Certificate` value and PMR-copies its diagnostic fields (subject_dn + SAN list) into the snapshot at add() time; the snapshot's per-pin diagnostic envelope is what the operator-visible logs / metrics inherit (SC-006). `remove` keys on the SHA-256 fingerprint (32 bytes) — the same `pin_fingerprint = std::array<std::byte, 32>` type `find` consumes." The asymmetry and rationale are stated at FR-level.
- [x] CHK019 Is "frozen at session open" stated as a *normative* requirement for `SecurityProfile` and `cert_source` (no mid-session swap API published)? [Clarity, Spec §FR-015 / §FR-016] — PASS: FR-015 states "`SecurityProfile` MUST be frozen at session open per [arch §5.6]. No mid-session swap API MUST be exposed." FR-016 states "The `cert_source` MUST be frozen at session open per [arch §5.6]. The `Pinset` is the ONLY mid-session-mutable TLS surface." Both are normative MUST statements. FR-027 repeats as negative requirements. Edge Cases section confirms absence of swap APIs.

## Requirement Consistency

- [x] CHK020 Does the 10-step `verify_peer` order in FR-020a match the order in `## Clarifications Q3`, the `## Assumptions` repeat, and the cited `contracts/security_profile.hpp:156-165`? [Consistency, Spec §FR-020a, §Clarifications Q3, §Assumptions] — PASS: All three sources are consistent. Clarifications Q3 gives the 10-step order; FR-020a repeats it verbatim with the same 10 numbered steps and adds the binding contract note about `cfg.pinset_snapshot`; Assumptions repeat the same 10 steps word-for-word. `contracts/security_profile.hpp` lines 156-165 (the verify_peer contract block) lists steps 1-10 in exactly the same order. No inconsistency found across all four sources.
- [x] CHK021 Is the `[[deprecated]]` placement on `SecurityProfile::one_way_ca` stated consistently as "on the enumerator declaration itself, not in a comment" in every requirement that touches it? [Consistency, Spec §FR-013] — PASS: FR-013 states "The `[[deprecated]]` attribute MUST be applied to the `one_way_ca` enumerator declaration itself (not in a comment) per [const §XII.5]." The Key Entities section says "`SecurityProfile` — enum selecting trust mode... `one_way_ca [[deprecated]]`." The Assumptions and §FR-014 do not restate the placement but do not contradict it either. `contracts/security_profile.hpp` confirms the attribute on the enumerator declaration. Consistent.
- [x] CHK022 Does the TLS-version posture (TLS 1.3 preferred, TLS 1.2 ECDHE-AEAD as fallback, `set_min = TLS1_2_VERSION`, `set_max = TLS1_3_VERSION`) appear identically in FR-014, the `## Clarifications Q1` answer, and the `## Assumptions` repeat? [Consistency, Spec §FR-014, §Clarifications Q1, §Assumptions] — PASS: Clarifications Q1: "TLS 1.3 preferred; TLS 1.2 ECDHE-AEAD admitted as fallback (`SSL_CTX_set_min_proto_version = TLS1_2_VERSION`, `set_max = TLS1_3_VERSION`)." FR-014: "MUST encode TLS 1.3 as preferred and TLS 1.2 ECDHE-AEAD as the fallback (`SSL_CTX_set_min_proto_version = TLS1_2_VERSION`, `SSL_CTX_set_max_proto_version = TLS1_3_VERSION`)." Assumptions: "encode **TLS 1.3 as preferred with TLS 1.2 admitted as fallback** (`set_min_proto_version = TLS1_2_VERSION`, `set_max_proto_version = TLS1_3_VERSION`)." All three agree on TLS 1.3 preferred, TLS 1.2 ECDHE-AEAD fallback, same version constants. Consistent.
- [x] CHK023 Does the "16 variants total" claim (FR-025) match the actual enumerated names — 15 from `[2g §6.6]` + 1 `/clarify` Q2 amendment? [Consistency, Spec §FR-025] — PASS: FR-025 states "15-variant set re-emitted from [2g §6.6] plus the one additive amendment from /clarify Q2 — 16 variants total." [2g §6.6] table has exactly 15 rows (counted: tls_cert_load_failed + tls_cert_parse_failed + tls_pin_not_found + tls_pin_already_present + tls_pinset_capacity_exhausted + tls_pinset_alloc_failed + tls_cipher_not_allowed + tls_handshake_failed + tls_rsa_key_too_large + tls_cert_der_too_large + tls_san_entries_exceeded + tls_pin_mismatch + tls_sign_callback_unavailable + tls_invalid_security_profile + tls_load_cancelled = 15). The /clarify Q2 amendment adds `tls_pin_empty_at_open`. Total = 16. Claim is consistent.
- [x] CHK024 Is the `Pinset` mid-session-mutable carve-out applied consistently — "the ONLY mid-session-mutable TLS surface" claim in FR-016 vs the [arch §5.6] inheritance in FR-009? [Consistency, Spec §FR-009 / §FR-016] — PASS: FR-009 states "Pinset rotation MUST be mid-session-mutable per the [arch §5.6] carve-out." FR-016 states "The `Pinset` is the ONLY mid-session-mutable TLS surface." FR-015 freezes SecurityProfile, FR-016 freezes cert_source. No other FR grants mid-session mutability to any other TLS surface. The "ONLY" claim in FR-016 is consistent with the complete FR set.
- [x] CHK025 Are the cold-path-PMR-allowed (FR-023) and zero-alloc-hot-path (FR-007) statements mutually consistent — i.e., is the boundary between hot and cold path named (`Pinset::find` hot; `load_credentials` cold)? [Consistency, Spec §FR-007 / §FR-023] — PASS: FR-007: "Pinset reads on the TLS handshake hot path MUST be lock-free and MUST allocate zero per [const §VIII.5]." FR-023: "PMR allocation MUST be permitted on the cold-path `load_credentials` (file I/O + parse + chain build). The zero-allocation invariant applies to the hot-path `Pinset::find` only." The boundary is explicitly named: `Pinset::find` (hot, zero-alloc) vs `load_credentials` file I/O + parse + chain build (cold, PMR-allowed). No overlap; the two FRs are mutually consistent.

## Acceptance Criteria Quality

- [x] CHK026 Can SC-002 ("100% refusal of out-of-envelope handshakes") be objectively measured — is "out-of-envelope" enumerated at SC-level (or via a cited FR)? [Measurability, Spec §SC-002] — PASS: SC-002 parenthetical enumerates the out-of-envelope cases: "(banned cipher, banned TLS version, RSA < 2048, RSA > operator cap, chain depth > 8, SAN list > 64, X.509 v1, expired against effective clock, per-cert DER > 16 KiB)". These map to FRs FR-011 (cipher), FR-014 (TLS version), FR-019/FR-020 (cert params). The enumeration at SC-level makes "out-of-envelope" objectively measurable — each category has a defined test vector.
- [x] CHK027 Can SC-003 ("build fails at compile time with a diagnostic identifying the violation") be objectively measured — is the diagnostic format / mechanism specified? [Measurability, Spec §SC-003] — WAIVED: Coverage/Measurability item (not Completeness/Clarity/Consistency). The mechanism is stated in FR-011 (`static_assert`-based rejection via CipherPolicy's compile-time consteval refusal chain per [2g §6.1]) and US3 AC1 confirms the build MUST fail with a diagnostic naming the violation. The exact format of the static_assert message is an implementation detail that `[const §IX.4]`'s try_compile seam (#8 per tasks.md) witnesses. The measurability criterion ("diagnostic identifies the violation") is verifiable by the try_compile test. Rationale for waiver: exact diagnostic string format is a compiler-output detail; the test mechanism (try_compile failing with the expected violation) is fully specified.
- [x] CHK028 Can SC-006 ("distinct, named error variant per failure mode; no catch-all") be objectively verified — is the catch-all-absence claim measurable via the 16-variant set? [Measurability, Spec §SC-006 / §FR-025] — PASS: SC-006 cites "No 'TLS failed' catch-all exists in the public surface." FR-025 enumerates all 16 variants by literal name; the complete set covers every failure mode (CONFIG/HANDSHAKE/PINSET/RUNTIME/CANCELLED groups). The verifiability criterion: enumerate the 16 variants and confirm no variant named generically (e.g., `tls_failed`); the `tls_handshake_failed` grouping variant uses a diagnostic field (not catch-all) with 7 named sub-reasons. Objectively measurable via code inspection.
- [x] CHK029 Does every US3 Acceptance Scenario (1–5) map to a specific FR + a specific `error::tls_*` variant name? [Acceptance Criteria, Spec §US3 / §FR-013 / §FR-025] — PASS: AC1 → FR-011 (compile-time refusal) + no error variant (build failure); AC2 → FR-012 + `false` return (no error variant, but this is a runtime predicate AC); AC3 → FR-020/FR-020a + `error::tls_handshake_failed` carrying sub-reason `"rsa_under_min"` (explicit in AC3 text referencing [2g §6.6] line 995); AC4 → FR-020 + `error::tls_handshake_failed` (X.509 v1 rejection, maps to "x509_v1" sub-reason per FR-020a); AC5 → FR-025 + `error::tls_pin_empty_at_open` (explicit). All 5 scenarios reference a specific FR. AC1/AC2 do not name error variants because the mechanism is compile-time failure and a boolean predicate respectively — both are appropriate for those scenarios.

## Scenario Coverage

- [x] CHK030 Are recovery / fail-open vs fail-closed requirements specified for every refusal path (verify_peer, make_ssl_ctx_config, load_credentials, Pinset::add over cap)? [Coverage, Spec §US3, §FR-019 / §FR-020 / §FR-010 / §FR-025] — WAIVED: Coverage item (not Completeness/Clarity/Consistency). The spec's posture is consistently fail-closed: every refusal path returns an `expected_t::unexpected{tls_*}` variant, and FR-021 explicitly prohibits throw-on-cancellation. FR-010 says `add()` over cap is "refused (no silent eviction)." FR-024 routes PMR throws to named variants. The spec has no requirement for fail-open / recovery — this is intentional for a security feature (explicit operator action required). No gap in coverage; "recovery" is out-of-scope per [const §XII].
- [x] CHK031 Is the cross-cut behaviour with the [const §XII] constitution chapter and [FIXS RC1] requirements traceable from each FR (i.e., do FRs cite the constitution / FIXS clause they enforce)? [Coverage, Spec §FR-011 / §FR-013 / §FR-020] — PASS: FR-011 cites `[const §XII.3]` and `[const §XII.4]` / `[const §XV.11]`. FR-013 cites `[2g §4.5]` and `[const §XII.5]`. FR-020 cites `[FIXS §3.4]`. FR-006 cites `[FIXS RC1 §5]`. The spec preamble states "Items tagged '(security)' trace to [const §XII] and [FIXS RC1]." FR-011/FR-013/FR-020/FR-003/FR-006/FR-007/FR-009 all carry the "(security)" tag. Traceability is present.
- [x] CHK032 Are runtime-string-override scenarios (C-ABI cipher path) covered by a measurable requirement (`CipherPolicy::is_allowed` returning false)? [Coverage, Spec §FR-012 / §Edge Cases] — PASS: FR-012 states "`CipherPolicy::is_allowed(std::string_view) constexpr noexcept -> bool` MUST be exposed for the runtime-string path that the C ABI (2i) bridges into." Edge Cases includes "Operator wires the C ABI cipher-name runtime override to a banned cipher string: `CipherPolicy::is_allowed` returns `false`; the 2i bridge surfaces `error::tls_cipher_not_allowed` to the C caller." US3 AC2 exercises the same predicate. Measurable: predicate returns false, test passes a banned token and asserts false.
- [x] CHK033 Is the case "operator opens session in `mtls_pinned` with a non-null empty pinset" covered as a refusal scenario at session-open time (not just at handshake)? [Coverage, Spec §US3 AC5 / §FR-025 / §Clarifications Q2] — PASS: US3 AC5 explicitly covers this: "Given an `mtls_pinned` session whose pinset is non-null but empty (zero entries) at session open, When the session attempts to open, Then session open is refused at session-config time with the distinct variant `error::tls_pin_empty_at_open`." Clarifications Q2 resolution confirms fail-early at session-open (not at handshake). `contracts/security_profile.hpp` assertion 4 confirms `make_ssl_ctx_config(mtls_pinned, ..., empty_pinset, ...)` returns `unexpect{tls_pin_empty_at_open}`.
- [x] CHK034 Is the case "PMR allocator throws during cold-path `load_credentials`" covered by a routed-to-named-variant requirement (not just "errors are handled")? [Coverage, Spec §FR-024 / §Edge Cases] — PASS: FR-024 states "PMR-thrown exceptions MUST be routed through `[2a §4.2]` `trap_throw` and surface as `error::tls_*` variants. No PMR throw MUST escape the public surface as an exception." Edge Cases confirms: "PMR allocator throws during cold-path `load_credentials`: routed through `[2a §4.2]` `trap_throw`; surfaces as `error::tls_cert_load_failed`" — the specific named variant is `tls_cert_load_failed`. This is a routed-to-named-variant requirement, not just "errors are handled."

## Notes

- This checklist is generated by `/speckit-checklist` per pipeline.md step 7. It is then audited by `/speckit-checklist-audit` (step 9 — MANDATORY gate before `/speckit-implement`).
- Each `CHK###` evaluates the QUALITY of a requirement (is it complete, clear, consistent, measurable, covering?) — NOT the implementation.
- Disposition codes used by the audit gate (per `[[feedback_checklist_audit_realizability]]`): `PASS` / `SPEC-FIXED` / `DD-DECIDED §X` / `WAIVED:<reason>`. Completeness / Clarity / Consistency items are NEVER waived per `[[feedback_speckit_analysis_subagents]]`.
- The /clarify pass (mandatory per `[const §XVI.3]` for security features) completed 2026-05-23 — see `spec.md ## Clarifications Session 2026-05-23` for the five resolved questions.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 31 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 1 |
| WAIVED | 2 |
| **Total** | **34** |

### SPEC-FIXED items
None.

### DD-DECIDED items
- CHK002 — anchor `[2g §4.4]`; rationale: exact 12 banned token literal strings are locked in the design-doc's normative `CipherPolicy::banned_tokens` constexpr array; spec correctly cites the authority chain ([const §XII.4]/[const §XV.11]); literal token enumeration is the design-doc's domain.

### WAIVED items
- CHK027 — Coverage/Measurability item; the try_compile seam (#8, [2g §9]) witnesses the static_assert mechanism; diagnostic naming the violation is verifiable by the test harness; exact diagnostic string format is a compiler-output implementation detail.
- CHK030 — Coverage item; spec's posture is consistently fail-closed per security feature design; fail-open/recovery is intentionally out-of-scope per [const §XII]; no coverage gap.

Anchors spot-verified: `[2g §4.4]`, `[2g §6.6]`, `[2g §4.5]`, `[2g §4.5.1]`, `[2g §6.5.1]`, `[2g §6.5.2]`, `[2g §4.3]`, `[2g §6.4]`, `[2g §6.3]`, `[arch §5.3]`, `[arch §5.5]`, `[arch §5.6]`, `[arch §6]`, `[const §XII.3]`, `[const §XII.4]`, `[const §XII.5]`, `[const §XIV.2]`, `[const §XI.2]`, `[const §VIII.5]`, `[2a §4.2]`, `[2d §7.9]`, `[2d §6.5]`, `[FIXS RC1 §5]`, `[FIXS §3.4]` — all resolve in signed-off revision `.specify/2g-tls.md` v0.4.
