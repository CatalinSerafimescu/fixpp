# Wire / Shape-Oracle Requirements Quality Checklist: Typed Application Messages (061-slim)

**Purpose**: Validate the *requirements* for body-only framing, canonical encoding, the external-golden anchor, and the witness/shape-oracle discipline — the load-bearing correctness surface of this feature. Audience: Gate B reviewer.
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) · [contracts/builder-shape-oracle.md](../contracts/builder-shape-oracle.md) · [data-model.md](../data-model.md)

## Body-Only Framing (INV-2)

- [x] CHK016 Is the body-only output contract stated exactly (leads `35=<MsgType>\x01`, business fields only, NONE of `8/9/34/49/52/56/10`) and is the multi-char MsgType case (`AS`) explicitly covered? [Completeness, contracts §C1, Spec §FR-002] — PASS: contracts §C1 states the exact contract (leading `35=<MsgType>\x01`, "MsgType may be multi-char, e.g. `AS`", only business-body fields, none of `8,9,10,34,49,52,56`); FR-002 restates it. Verified against the real `AllocationReport` flyweight (`Messages.hpp:13206`, `msg_type_v = "AS"`) — the 2-char case is a real, existing MsgType, not hypothetical.
- [x] CHK017 Is the header/trailer/BodyLength/CheckSum justification (engine stamps them at frame time) anchored to a coverage-index row (`[FIX50SP2 §3.1]` W-002..W-005), not left as an unsourced claim? [Traceability, Spec §Normative References] — PASS: spec.md Normative References cites `[FIX50SP2 §3.1]` rows W-002..W-005 verbatim for header/BodyLength/CheckSum/trailer. Verified against the real `spec/coverage-index.md:193`: "`[FIX50SP2 §3.1] ↔ W-002 (header), W-003 (trailer), W-004 (BodyLength), W-005 (CheckSum)`" — the citation resolves exactly, row-for-row, not merely plausible.

## Canonical Encoding (INV-3)

- [x] CHK018 Is "canonical decimal" defined with objective criteria (locale-independent, no scientific notation, trailing-zero canonical) rather than the word "canonical" alone? [Clarity, Spec §FR-004, contracts §C2] — PASS: contracts §C2 gives objective criteria — "locale-independent, no scientific notation, trailing-zero canonical (`190.50` and `190.5` encode identically)". FR-004 restates the same via `decimal_t::format`. Not a bare adjective.
- [x] CHK019 Is the requirement that both the golden-diff AND the parse round-trip compare decimals *by value* — and therefore CANNOT catch a wrong *format* — stated as the explicit rationale for the separate byte-exact assertion? [Consistency, Spec §FR-004, data-model §4] — PASS: FR-004 states this as the direct rationale verbatim ("Because both the external golden diff and the read-back parse compare decimals **by value** (neither can catch a wrong canonical *format* — `1.9E2`, locale artifacts, non-canonical trailing zeros are all value-equal)..."); data-model §4 repeats the identical rationale. Explicit causal chain, not merely juxtaposed facts.
- [x] CHK020 Is the byte-exact decimal assertion quantified (≥1 decimal field per exemplar, exact `<tag>=<ascii>\x01` bytes, with a concrete example e.g. `44=190.50\x01`)? [Measurability, contracts §C2] — PASS: FR-004/contracts §C2/data-model §4 all state "≥1 decimal field... `<tag>=<ascii>\x01` bytes... exactly"; quickstart.md gives the concrete worked example `assert(contains_bytes(d_body, "44=190.50\x01"))`. Quantified and concretely exampled, not left abstract.

## External-Golden Anchor (non-tautological)

- [x] CHK021 Is the golden sourcing uniform and unambiguous (all 5 body-only, in-submodule, QuickFIX-cpp-authored offline; D & 8 re-derived body-only; parent `BM-*` full-frame transcripts explicitly NOT consumed)? [Consistency, Spec §FR-006, Assumptions] — PASS: Clarifications (2026-07-08 re-scope, Q4) + FR-006 + Assumptions "Reused exemplars" all state the uniform in-submodule mechanism and explicitly forbid the parent `phase-9-harness/BM-*` full-frame transcripts as anchors ("do NOT reach into the parent `phase-9-harness/BM-*` full-frame transcripts... the mismatch would split the harness"), and explicitly require NEW body-only goldens even for the 2 reused D/8 builders. No ambiguity across the 3 citations — fully consistent.
- [x] CHK022 Is the `shape_oracle_profile()` exclusion set defined exactly (`{8,9,10,34,52}` only) AND explicitly contrasted with `default_normalization_tags()` `{9,10,34,52,60,112,122}` so business tag `60` is matched verbatim? [Clarity, Spec §FR-006, data-model §4] — PASS: FR-006/data-model §4/contracts §C5 all state the exact set `{8,9,10,34,52}` and explicitly contrast it against `default_normalization_tags()` `{9,10,34,52,60,112,122}`, calling out that the latter drops business tag `60`. Verified against the REAL `default_normalization_tags()` in `tests/interop/support/golden_diff.hpp:36-40`: `static const std::set<int> tags{9, 10, 34, 52, 60, 112, 122};` — the spec's claimed set matches the actual code byte-for-byte. `shape_oracle_profile()` itself does not exist yet (it is task T009, unimplemented pre-`/speckit-implement`) — expected at this pipeline stage.
- [x] CHK023 Is the golden-frame *format* requirement specified so the diff harness can consume it (the `> `/`< ` direction-prefix convention required by `diff_transcripts`, and the single-`GoldenFrame` wrapping of builder output)? [Completeness, tasks §T009/T017/T020 — post-F2] — PASS: tasks.md T009 states the convention precisely, cross-referencing the real code (`golden_diff.cpp:180,220-224`); T017/T020 apply it (goldens stored as a single `> `-prefixed line; builder output wrapped as `GoldenFrame{'>', bytes}` before `diff_transcripts`). Verified against the REAL `golden_diff.cpp`: `parse_golden` requires `is_valid_dir(line[0]) && line[1] == ' '` at line 180 (an unprefixed line falls through to `dir='?'`); `diff_transcripts`'s direction check at lines 220-224 rejects `dir='?'` via `mismatch('?', frame_index, "direction")` BEFORE any field compare — exactly as cited. Both line numbers resolve exactly.
- [x] CHK024 Is "no live-QuickFIX runtime dependency in the unit harness" stated as a constraint on the harness, not just an authoring note? [Clarity, Spec §FR-006] — PASS: FR-006's final sentence states it as a requirement-level constraint verbatim: "No live-QuickFIX runtime dependency in the unit harness." Distinct from the separate authoring-note framing in research.md Decision 5 ("QuickFIX-cpp... used offline to author the goldens, NOT at test runtime" — plan.md Testing section) — both levels agree, and the FR-level sentence is unambiguously a requirement, not merely a note.

## Repeating-Group / Nested Shape (SC-002/003)

- [x] CHK025 Are the exact group chains specified per grouped exemplar (E: 3-level `73→453→802`; AS: 2-level `453→802`) with the seeded per-entry field sets, and the accessor names verified against the real generated header (post-F1 `party_i_ds()`/`party_sub_i_ds()`)? [Consistency, data-model §3, Spec §SC-003] — PASS (independently re-verified, not merely trusting the F1 note): data-model §3.1/§3.2 state the exact chains and per-entry field sets. Re-verified against the REAL generated `Messages.hpp` (not just the F1 fix's say-so): `NewOrderList::orders()` → `group_view<groups::G_73>` (tag 73, `Messages.hpp:19098-19099`); `G_73` (class body `Messages.hpp:1896-2433`) has scalar accessors `cl_ord_id()`(11)/`list_seq_no()`(67)/`symbol()`(55)/`side()`(54)/`order_qty()`(38) matching the per-order seed, AND a nested `party_i_ds()` → `group_view<G_453>` (tag 453, `Messages.hpp:2408-2412`) — NOT `nested2_party_i_ds()` (a different accessor on the same `G_73` class, tag 756, confirmed NOT the one used); `G_453` (`Messages.hpp:808-832`) has `party_id_source()`(447)/`party_id()`(448)/`party_role()`(452) + nested `party_sub_i_ds()` → `group_view<G_802>` (tag 802); `G_802` (`Messages.hpp:791-807`) has `party_sub_id()`(523)/`party_sub_id_type()`(803). Full 3-level chain confirmed real. `AllocationReport` (`Messages.hpp:13206`, `msg_type_v="AS"`) confirmed to carry a top-level `group_view<groups::G_453>` directly (2-level `453→802` chain, same `G_453`/`G_802` types). The F1 fix (`party_i_ds`/`party_sub_i_ds`, not `parties`/`party_sub_ids`) is correct.
- [x] CHK026 Is the count-of-zero requirement pinned to an OPTIONAL group (`NoPartyIDs(453)`, `required='N'`) and kept distinct from the never-opened (emit no `No<Group>` tag) case? [Clarity, Spec §FR-005, contracts §C3] — PASS: FR-005 + contracts §C3 state this exactly, with the never-opened case bracketed and explicitly distinguished. Verified against the REAL `dictionaries/FIX44.xml`: E's per-order `Parties` component reference is `<component name='Parties' required='N' />` and AS's top-level `Parties` reference is likewise `<component name='Parties' required='N' />` (both grep-confirmed near the message defs) — the "optional group" claim is dictionary-accurate, not asserted.
- [x] CHK027 Is the discriminating depth measure for SC-003 stated as an exact read chain at an exact seeded value (not "nesting works"), for BOTH E and AS? [Measurability, Spec §SC-003] — PASS: SC-003 names the exact accessor chains for both — E's `orders()[i].party_i_ds()[j].party_sub_i_ds()[k].party_sub_id()` (3-level) and AS's `party_i_ds()[j].party_sub_i_ds()[k]` (2-level) — "at its exact seeded value", not a generic "nesting works" claim. Both chains independently re-verified against the real generated header under CHK025 above.

## Witness Discipline (non-tautological requirement)

- [x] CHK028 Is it stated that a pure build→parse round-trip is insufficient (tautological) and that the external golden is the required independent anchor? [Completeness, Spec §US2, contracts §C5] — PASS: US2's "Why this priority" states it verbatim ("Round-trip alone is tautological; the external golden is what makes the exemplar a trustworthy shape-oracle"); FR-006's final sentence repeats it ("A pure build→parse round-trip without the external anchor is explicitly insufficient"); contracts §C5 restates it as "the independent oracle — round-trip alone is insufficient (non-tautological requirement)". Stated at 3 independent layers, consistently.
- [x] CHK029 Is the independent inbound read witness required to use HAND-AUTHORED wire (NOT builder output), so read and build are genuinely cross-checked? [Clarity, Spec §FR-007] — PASS: FR-007 states it verbatim ("a hand-authored wire body (NOT builder output) → framed → parsed via the dict-backed path..."); US3's "Why this priority" restates the cross-check rationale ("proven with an independently hand-authored inbound wire (not builder output), so read and build are cross-checked").
- [x] CHK030 Are the read-back assertions required to be discriminating (exact typed value per field, incl. a nested entry for E), with a missing-required-field → typed-error case? [Measurability, Spec §FR-007, §SC-002] — PASS: FR-007 requires "asserting the EXACT typed value of each read field (incl. ≥1 group entry, and a nested entry for E), plus a missing-required-field → typed-error assertion"; US3 Acceptance Scenario 1 reinforces "discriminating, not 'parse succeeded'"; SC-002 states "100% of read and round-trip witnesses are discriminating (exact field values, not 'parse succeeded')".

## Dictionary-Backed Read Path & Prereqs

- [x] CHK031 Is the read path pinned to the 5-arg dict-backed `Parser<Index>::parse` (the 2-arg heuristic cannot enumerate groups), and are the prereqs 062/063 stated as merged? [Consistency, Spec §Assumptions, data-model §5] — PASS: spec.md Assumptions "Read path" states this exactly; data-model §5 restates it for the `app_message_read_scaffold.hpp` design; the spec header states "Prerequisites 062 (PR #168...) and 063 (PR #176...) are merged" and research.md Decision 6 gives the concrete citation. Verified against the REAL `include/fixpp/wire/parser.hpp`: the 5-parameter dict-backed `MessageView` ctor (`frame, mr, opaque_dict, classify_fn, group_member_fn`) is at line 95-115 and the 2-arg heuristic ctor (`frame, mr`) is at line 135 — both exact line numbers cited in research.md Decision 6 (`parser.hpp:95-115`, `:135`) resolve precisely against the real header.
- [x] CHK032 Is the v42-no-typed-groups limitation (why all 5 exemplars are v44) documented as a requirement to record a B-/L- row, and scoped as an FR-015a prerequisite (not 061 work)? [Coverage, Spec §FR-010, §Out of Scope] — PASS: FR-010 requires the B-/L- row ("`spec/behaviors-and-limitations.md` MUST record the new B-/L- entries (incl. the v42-no-typed-groups limitation)"); Out of Scope states the FR-015a-prerequisite scoping verbatim ("v42 (market-data) grouped/nested writes — blocked by the verified finding that v42 codegen emits zero typed groups; expressing grouped market-data messages is an **FR-015a prerequisite**... not 061-slim work"). Research.md Decision 2 records the underlying verified finding (v42 `group_view` occurrences = 0).

## Regression / Scope Boundary

- [x] CHK033 Are the D & 8 refactors required to be byte-identical to their existing 020 output (existing read tests as the guard), so "refactor onto `body_builder`" is not a silent behavior change? [Consistency, tasks §T013/T014] — SPEC-FIXED: the ORIGINAL tasks.md T013/T014 wording ("MUST stay byte-identical to the existing 020 output (existing read tests remain green)") was internally inconsistent — verified against the REAL `tests/session/test_business_messages_build.cpp`: its `extract_field_value`/`body_contains_tag` helpers (lines 110-149) are SOH-boundary presence/value scans that find a `tag=value` token ANYWHERE in the body and are field-order-insensitive; they do not assert exact byte sequence/order, so "existing read tests remain green" cannot actually discriminate byte-identity (a silent field-reorder from the `body_builder` refactor would still pass). Edited `tasks.md` T013/T014 to name the real byte-identity guard: capture the pre-refactor output for the exact seed already used by `AS1_NOS_HappyPath_ParseBack`/`AS2_ExecRpt_HappyPath_ParseBack` as a literal expected byte string, and assert the post-refactor output equals it byte-for-byte; the existing read tests remaining green is reframed as a necessary-but-not-sufficient regression net, not the byte-identity guard itself. **CORRECTION (implement-time, 2026-07-08):** the byte-identity-to-020 premise itself was WRONG — the 020 builders emit NON-ascending field order (D `11,55,54,38,40,44,60`) while the QuickFIX golden (SC-001's anchor) is ASCENDING and `diff_transcripts` is order-sensitive, so byte-identity to 020 directly CONFLICTS with SC-001 (golden-match). **SC-001 wins**: T013/T014 now require matching the golden (ascending order), preserving field VALUES (020 build/read tests are order-insensitive → stay green; no production callers). This CHK is now `Consistency`-satisfied by the golden-match round-trip witness (T020 D/8 rows), not a byte-identity-to-020 compare. See the CORRECTION note in tasks.md above T013.
- [x] CHK034 Is "no regressions — Tier-1 sanitizer/analysis matrix green" (SC-005) tied to a concrete verification owner (`/speckit-verify`) rather than left as an aspiration? [Measurability, Spec §SC-005] — PASS: SC-005 states the requirement; tasks.md T027 makes it a task ("Confirm SC-005: full Tier-1 sanitizer/analysis matrix green... deferred to `/speckit-verify` for the sanitizer legs"); plan.md's Article XVII §8 binding names the concrete owner and artifact ("`/speckit-verify` mandatory after `/implement`, producing `.specify/decisions/061-verify.md`; `/gate-b` refuses without it"). Concrete owner + artifact + gate, not an aspiration.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 18 |
| SPEC-FIXED | 1 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 19 |

### SPEC-FIXED items

- CHK033 — edited `tasks.md` T013/T014: the claimed "byte-identical... (existing read tests as the guard)" was inconsistent with the real `test_business_messages_build.cpp` (its `extract_field_value`/`body_contains_tag` helpers are order-insensitive presence/value scans, not a byte-identity check). Added an explicit pre-/post-refactor literal-byte-string regression requirement, reframing "existing read tests remain green" as necessary-but-not-sufficient. Affected: `specs/061-typed-app-messages/tasks.md` T013/T014.

### DD-DECIDED items

None.

### WAIVED items

None.

Anchors spot-verified (all resolve against the real submodule tree):
- `spec/coverage-index.md:193` (`[FIX50SP2 §3.1] ↔ W-002..W-005`) — exact row-for-row match to spec.md's cite.
- `tests/interop/support/golden_diff.hpp:36-40` (`default_normalization_tags() == {9,10,34,52,60,112,122}`) — matches spec's claimed set exactly.
- `tests/interop/support/golden_diff.cpp:180,220-224` (direction-prefix parse + reject logic) — exact line numbers cited in tasks.md T009 resolve.
- `include/fixpp/wire/parser.hpp:95-115,135` (5-arg dict-backed `MessageView` ctor vs. 2-arg heuristic ctor) — exact line numbers cited in research.md Decision 6 resolve.
- `Messages.hpp` full E chain (`orders()`→`G_73.party_i_ds()`→`G_453.party_sub_i_ds()`→`G_802`) and AS's top-level `G_453` — independently re-walked field-by-field against the real generated header (not merely trusting the F1 analyze-fix note); tags 11/67/55/54/38 (order), 448/447/452 (party), 523/803 (sub) all confirmed.
- `dictionaries/FIX44.xml` message-def lines `:326`(D) `:404`+`:423`+`:2944`(E `ListOrdGrp`) `:115`(8) `:255`(9) `:1901`(AS), plus the `Parties required='N'` cites near E's per-order and AS's top-level component refs — all confirmed exact.
- `src/capi/message_write.cpp:682-701` (`validate_group_grammar`), `:106` (`kFrameCap`) — confirmed exact (shared with api.md).

Realizability sub-check: `wire::body_builder`'s internal state (open-group LIFO stack, recursive `AccumulatorEntry`/`GroupInstance`-shaped accumulator) is specified as data-model §1 "Internal state (not public)" — wholly owned and defined within 061's own header/TU (T005/T006), mirroring the C-ABI's own fully-defined (not forward-declared) `AccumulatorEntry` shape in `message_write.cpp`. `group_handle`/`entry_handle` are non-owning handles into that same self-contained state, not `unique_ptr<Dep>`/by-value members of a type forward-declared and owned by a DIFFERENT deferred spec. `GoldenFrame` (reused, pre-existing in `golden_diff.hpp:19-21`) is a trivial value type (`char` + `std::vector<std::byte>`), fully realizable with no forward-declared members. `ExemplarSeed` (data-model §3.2) is a self-contained test-only struct. No 007-threading-clock-D-15-class latent completeness defect found across either checklist's value-typed entities. Verdict: **clean**.
