# Implementation Plan: Typed Application Messages — write shape-oracle + witness harness (061-slim)

**Branch**: `061-typed-app-messages` | **Date**: 2026-07-08 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `specs/061-typed-app-messages/spec.md` (re-scoped 2026-07-08 per D3)

## Summary

Deliver a **5-exemplar write shape-oracle** for the typed-application-message workstream: a shared
`wire::body_builder` (group-capable, body-only serializer mirroring the C-ABI `OutboundAccumulator`
LIFO discipline), five hand-written exemplar builders in `fixpp::v44` spanning the hard write axes
(D flat-scalars, 8 flat-scalars, 9 group-free, **E simple+3-level-nested — pivotal**, AS multi-char+nested),
and a table-driven round-trip witness harness anchored to **static checked-in body-only QuickFIX
goldens** (non-tautological). Plus the FR-008 header-install packaging fix. The remaining ~28 OFFICIAL
builders are generated by the follow-on FR-015a feature; 061-slim is its prerequisite (the shape-oracle
+ `body_builder`), not its implementation.

## Technical Context

**Language/Version**: C++23 (fixpp library).
**Primary Dependencies**: existing generated flyweights under `fixpp::v44` (no codegen regen — read path
landed via 062/063); `wire::Framer`/`MessageView`/`OffsetTable`/`Parser<Index>` (5-arg dict-backed);
`wire::group_view` + generated `G_<n>` entry accessors (062/063); `decimal_t`; `core::fix_time`;
`core::expected_t`. Reference (read-only, to mirror): C-ABI `OutboundAccumulator` in `src/capi/message_write.cpp`.
**Storage**: N/A.
**Testing**: GoogleTest under `tests/session/` + a factored `tests/support/` read-scaffold (BeginString-
parameterized) + static goldens under `tests/session/golden/`; golden-diff via `tests/interop/support/golden_diff.hpp`.
QuickFIX-cpp (cloned reference engine) used **offline** to author the goldens, NOT at test runtime.
**Target Platform**: Linux (Tier-1) + Windows (Tier-2).
**Project Type**: single C++ library.
**Performance Goals**: N/A (correctness feature; builders are not on a measured hot path here).
**Constraints**: fail-closed atomic builders (INV-4); caller-provided output span (no per-call arena burn);
canonical decimals (INV-3); body-only output, no framing tags 8/9/34/49/52/56/10 (INV-2).
**Scale/Scope**: 1 new `wire::body_builder` (header+TU) + 5 exemplar builders (3 new: 9, E, AS; 2 refactored: D, 8)
+ 1 witness harness + 5 goldens + 1 CMake install rule. Grouped/nested builders (E, AS) are the LoC-heavy part.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

**Appendix A mandatory-trigger evaluation** (the four controls: `/clarify`, `/analyze`, Codex Gate A, user `/plan` sign-off):

| Trigger | Applies? | Basis |
|---|---|---|
| ABI surface change (C ABI) | **No** | FR-009 forbids new C-ABI surface; C-ABI GA-frozen at 1.5.0. |
| Public C++ API (Article XVII §1 Gate-A trigger) | **Yes** | New public `wire::body_builder` + 5 exemplar builder declarations in `business_messages.hpp`. |
| Wire format / parser | **Borderline → treat as Yes** | `body_builder` is a NEW serializer emitting FIX body bytes. It does NOT change framing/offset-table/validator *semantics* (those landed in 062/063), but "when in doubt, run Gate A." |
| Codegen layout | **No** | FR-009: does not touch the codegen writer-emitter (that is FR-015a) or dictionary loader. FR-008 header-install is CMake/packaging, not codegen layout. |
| Error semantics | **No** | Reuses existing `wire_*` errors; no new `fixpp_error_t` value (FR-009). |
| Threading / concurrency | **No** | Synchronous serialization; no awaitable/strand/lock change. |
| Session FSM | **No** | N-002/003 out of scope. |
| Security | **No** | No TLS/cert/PSK surface. |

**Verdict**: Gate A **applies** (public C++ API + new wire serializer). Therefore the full pipeline runs:
`/clarify` ✅ done (2 sessions), `/analyze` mandatory (before `/implement`), **Codex Gate A after this plan,
before `/tasks`** (per `.specify/pipeline.md` order), user `/plan` sign-off (this plan bundle).

**Other constitutional bindings**:
- **§XVIII.7** — v1.0 typed-message scope names A-001..A-013 + M-/P- families; the 5 exemplars are a subset.
  The D3/D7 re-scope (FR-015a into v1.0; drop stale C-/R- typed-scope) is a **constitution amendment**
  (Article XX) — a **separate close-out PR with its own Gate A**, NOT bundled into 061-slim's code diff.
  Flagged in FR-010 and Complexity Tracking.
- **Article XVII §7** — local pre-PR build gate (linux-clang-debug green + confirmation line); §7 resource
  gate: agent MUST `AskUserQuestion` before running local builds. Honored at `/implement` + `/speckit-verify`.
- **Article XVII §8** — `/speckit-verify` mandatory after `/implement`, producing `.specify/decisions/061-verify.md`;
  `/gate-b` refuses without it.
- **Layer discipline** (`tools/check_layers.py`): `wire` → {core, dictionary}; `session` → {…, wire}.
  `wire::body_builder` lives in the wire layer (wire→core only; may consult `dictionary` for group ordering
  if needed, also allowed). `session/business_messages` (session layer) depends on wire — clean.

**Gate result: PASS** (no unjustified violations; Gate A scheduled; §XVIII.7 amendment tracked as a separate PR).

## Project Structure

### Documentation (this feature)

```text
specs/061-typed-app-messages/
├── plan.md              # This file
├── research.md          # Phase 0 (regenerated — supersedes the paused 33-msg research)
├── data-model.md        # Phase 1 — body_builder API + seed-table + golden entities
├── quickstart.md        # Phase 1 — build an exemplar + run the witness harness
├── contracts/           # Phase 1 — builder-shape-oracle contract (what FR-015a must match)
│   └── builder-shape-oracle.md
├── checklists/          # requirements.md (regenerated at /speckit-checklist)
└── tasks.md             # Phase 2 (/speckit-tasks — not created here)
```

### Source Code (repository root = library submodule)

```text
include/fixpp/wire/body_builder.hpp        # NEW — shared body-only serializer + LIFO group API
src/wire/body_builder.cpp                   # NEW — impl (lifts 020 wfield/wchar/wdecimal; group accumulate→serialize)
include/fixpp/session/business_messages.hpp # EXTEND — 5 exemplar builder declarations (3 new, 2 refactored)
src/session/business_messages.cpp           # EXTEND — 5 exemplar builders on top of body_builder
tests/support/app_message_read_scaffold.hpp # NEW — make_frame/parse (5-arg dict path), BeginString-parameterized
tests/session/test_exemplar_roundtrip.cpp   # NEW — table-driven round-trip + golden-diff harness (5 exemplars)
tests/session/test_exemplar_read.cpp         # NEW — independent inbound read witnesses (hand-authored wire)
tests/session/golden/*.fix                   # NEW — 5 static body-only QuickFIX-authored goldens
tests/consumer/                              # NEW — external-consumer compile witness (FR-008)
CMakeLists.txt                               # EXTEND — install(DIRECTORY _codegen/include) (_dispatch + vt11 excluded)
spec/feature-catalogue.md, coverage-index.md, behaviors-and-limitations.md  # FR-010 evidence + B/L rows
```

**Structure Decision**: single-library layout. The one architectural choice is placing `body_builder`
in the **wire layer** (`include/fixpp/wire/` + `src/wire/`) — it is a reusable serialization primitive
(FR-015a-generated builders will also call it), and wire→core/dictionary is layer-legal. Exemplar
builders stay in the existing 020 `session/business_messages` TU/header (session→wire is legal).

## Complexity Tracking

| Decision | Why | Note |
|----------|-----|------|
| New `wire::body_builder` (3rd outbound serializer) | 020 helpers are TU-anon + flat-only; grouped body-only builders need a shared group-capable primitive; `wire::Writer` injects 8/9/10 (unusable body-only) | Mirrors C-ABI `OutboundAccumulator` LIFO (accumulate→serialize, count-precedence). **Tracked v1.x debt**: converge the 3 serializers (020 helpers / C-ABI accumulator / body_builder) onto one core — recorded, not done here (100pct-plan §Phase-2). |
| §XVIII.7 amendment split into a separate PR | It is a constitution amendment (Article XX) needing its own Gate A + user sign-off; bundling it into a code diff muddies both reviews | FR-010 records the obligation; the amendment PR lands alongside/after 061-slim. |
| v42-no-typed-groups → FR-015a prerequisite | Verified: v42 codegen emits zero typed groups, so market-data grouped writes are inexpressible in v42 today | Out of 061-slim scope; flagged in spec Out-of-Scope + parent tracker so FR-015a planning accounts for it. |
