# Tasks: Group Delimiter Resolution

**Input**: Design documents from `specs/083-group-delimiter-resolution/`
**Prerequisites**: plan.md (Gate A CONVERGED, fresh loop round 2), spec.md, research.md (D-1..D-13, **D-12 OPEN → T013**), data-model.md, quickstart.md, contracts/{group_ctx_delims,consume_group,loader_tolerant_mode,typed_read_splitter,capi_group_grammar}.md
**Branch**: `083-group-delimiter-resolution` | **Repo root** = the library submodule (`research/G19-fix-fpml-iso20022/library/`); every path below is submodule-relative.

**Tests**: **MANDATORY, not optional** (`[const §VII.3]`, `[const §VII.4]`). This bundle is explicitly RED-first. Every phase's RED tasks MUST be **observed failing with the failure counts recorded** before the corresponding implementation task runs. A pin never observed red proves nothing (FR-014, SC-015, quickstart §3).

**Organization**: grouped by user story, but **ordered by the feature's correctness constraint, not by naive priority** — see "Ordering constraint" below.

## Format: `[ID] [P?] [Story?] Description with exact file path`

- **[P]** = parallelisable (different file, no incomplete dependency).
- `[US1]`..`[US5]` appear **only** in user-story phases. Setup / Foundational / Polish carry no story label.
- Every task names a concrete file path. Non-code tasks name their record artifact.

## Build constraints (read before any build task)

- **`-j2` maximum.** Wide parallel C++ builds OOM-kill the session.
- **Never `cmake --preset`.** Conan's `CMakeUserPresets.json` carries a duplicate `conan-debug` collision that makes CMake refuse to read *any* preset. Configure with `-B` instead:
  `cmake -B build/linux-clang-debug -S . -DCMAKE_BUILD_TYPE=Debug …`
- Local verification is **Clang-only**; the GCC-Release and MSVC Tier-2 jobs are CI-only and can fail on things a local run cannot see.
- `ctest -L codegen` is **required**, not optional (quickstart §5): this feature moves the FIX50SP2 registered-group count **502 → 505**, and a label-filtered run that covers a changed subsystem's plausible gates can still miss that subsystem's count pin.

## Ordering constraint — why the story phases are not in priority order

`plan.md`'s Implementation Phasing and `contracts/consume_group.md` ("Ordering constraint — this contract lands FIRST") state one **non-negotiable correctness constraint**:

> **FR-007 (descend-at-delimiter) MUST land before FR-003 (recursive resolution).**

Landing the loader recursion first converts a wrong-delimiter defect into a **false rejection across 232 measured FIX50SP2 contexts plus the 30 that newly register** — strictly worse than today. Therefore:

- **US2 (P1, receiver)** is Phase 3 — before US1's loader leg, even though US1 is the headline story.
- **US5 leg 1 (C-8.0c / FR-021e, the offset table's extent walk)** is Phase 4 — pulled ahead of the loader phase because `plan.md`'s **Phase-3 gate leg (iv)** requires **W-10a green at the Phase-3 exit**, and because leaving it later leaves reachable **silent instance loss** on the 262 mode-(c) contexts once Phase 2's FR-007 makes validation accept them (C-8.0c.5). C-8.0c is not *blocked by* Phase 3 (its delimiter is wire-derived and it needs no per-context record), so it can land any time from Phase 2 onward.
- **US1 and US5 each split into two legs** across the loader/consumer boundary; each leg is marked. US5's split is *forced* by the two clauses above. US1's split (loaders → C-ABI) follows `plan.md`'s own Phase-3/Phase-4 content division.

**Intermediate phase exits are not releasable states for this feature** (`plan.md`, C-8.0c.5). The merge unit is the single PR; Gate B and all three tier matrices run on the PR head, not on phase boundaries. A phase gate here is a construction-order discipline, not a release gate.

## Two reconciled placement decisions (stated, not silently taken)

1. **`as_table_view()`'s C-3.1 consume lands in the US1 loader block (T031), not later.** `plan.md` lists it under Phase-4 *content*, while the **Phase-3 gate** requires "delimiter pin green on all ten". The pin measures the **registered store**, not Entity 2 directly — C-3.4 says so explicitly (*"invisible to FR-012's pin if that pin measures the table rather than the registered store"*) and FR-015 makes member-set exactness fall out of the same pin, and member sets live on `table_view`. So the Phase-3 gate leg is unsatisfiable unless C-3.1 has landed by that exit. This applies `plan.md`'s own stated warrant: *"a gate leg asserts the tree's state at a phase exit and does not say which phase authored the code."*
2. **The 082-side edits are recorded, not scheduled** (T073). `spec.md` Dependencies, `plan.md`'s demotion paragraph and research D-8 all forbid scheduling them as verifiable tasks — 082 is parked on an unbuildable branch, so such a task could only be marked done by inspection (the false-green close-out shape). T073 lands the **handoff line on this branch**, naming both items with their exact content.

## Artifact counts, reconciled against `plan.md`'s Structure Decision (a small unreconciled count is this project's silent-omission detector)

- **6 NEW test files**: `tests/dictionary/delimiter_census_test.cpp`, `tests/dictionary/loader_disposition_test.cpp`, `tests/wire/consume_group_nested_delim_test.cpp`, `tests/wire/delimiter_divergence_wire_test.cpp`, `tests/wire/typed_read_split_agreement_test.cpp`, `tests/capi/capi_group_delimiter_ctx_test.cpp`.
- **6 EDITED test/fuzz files**: `tests/dictionary/required_scope_oracle.hpp`, `tests/dictionary/reused_tag_census_test.cpp`, `tests/wire/nested_group_extent_test.cpp`, `tests/fuzz/fuzz_wire_validator.cpp`, `tests/fuzz/fuzz_dict_xml_loader.cpp`, `tests/fuzz/fuzz_orchestra_loader.cpp`.
- **3 benchmarks**: inbound validate, typed read, C-ABI commit, at `bench/wire/validate_group_bench.cpp`, `bench/wire/typed_read_group_bench.cpp`, `bench/capi/capi_commit_group_bench.cpp` — the repo's per-subsystem layout (`bench/wire/` exists; `bench/capi/` is NEW and needs its own `CMakeLists.txt`). *(Drift CLOSED 2026-07-31 at `/speckit-checklist`: `plan.md`'s tree wrote all three flat as `bench/<name>.cpp` and has been corrected to match these paths. This bullet previously recorded the disagreement; it now records the reconciliation.)*
- **`src/capi/session.cpp`** — C-9.2a mandates building the session `table_view` at `fixpp_session_open` (`:109-111`), the site where `dict_` is cached. T050 covers it. *(Drift CLOSED 2026-07-31 at `/speckit-checklist`: the file was absent from `plan.md`'s Source Code tree and has been added there. `plan.md` and `tasks.md` now name the same file set.)*

## Witness → task map (every W- has a home)

| Witness | Task(s) | File |
|---|---|---|
| W-1, W-1a | T010, T017 | `tests/wire/consume_group_nested_delim_test.cpp` |
| **W-2** | *(none — alias)* | `consume_group.md`: *"No case is named W-2."* Delivered by **W-9** + **W-10a** |
| W-3 | T016, T017 | `tests/wire/consume_group_nested_delim_test.cpp` |
| W-4, W-5 | T035, T036, T037 | `tests/dictionary/loader_disposition_test.cpp` |
| W-6 | T034, T035 | `tests/dictionary/loader_disposition_test.cpp` (C-7.1's checked-in twin) |
| W-7 | T043 | `tests/dictionary/delimiter_census_test.cpp::DelimiterCensus.RegisteredGroupCountMatchesCodegenFix50Sp2` |
| W-8 | T054 | `tests/wire/nested_group_extent_test.cpp` |
| W-9 (fixture **c** only — see amendment) | T055, T058 | `tests/wire/typed_read_split_agreement_test.cpp` |
| W-10 (**two** exclusions) | T056, T062 | `tests/wire/typed_read_split_agreement_test.cpp` |
| W-10a (legs 1–4) | T011, T020, T021, T022, T023 | `tests/wire/typed_read_split_agreement_test.cpp` |
| W-11, W-11a, W-11b, W-12, W-13 | T049, T050, T051, T052, T053 | `tests/capi/capi_group_delimiter_ctx_test.cpp` |
| W-14 | T068 | `tests/capi/abi_symbol_golden_test.cpp` (`nm` exported-symbol golden — the current gate; libabigail retired 2026-06-22) |

---

## Phase 1: Setup (shared infrastructure)

**Purpose**: build surface + the fixed inputs this feature measures against. No source change.

- [X] T001 Configure and build the Clang-Debug tree with `cmake -B build/linux-clang-debug -S . -DCMAKE_BUILD_TYPE=Debug` (**never** `cmake --preset` — `CMakeUserPresets.json` duplicate-`conan-debug` collision) and `cmake --build build/linux-clang-debug -j2`; confirm `ctest -L dictionary`, `-L wire`, `-L capi`, `-L codegen` each enumerate a non-empty set from `build/linux-clang-debug`.
- [X] T002 [P] Confirm the fixed dictionary population is present and loadable — the nine QuickFIX-XML files under `dictionaries/` (FIX40, FIX41, FIX42, FIX43, FIX44, FIX50, FIX50SP1, FIX50SP2, FIXT11) plus the Orchestra FIX Latest repository. These ten are the whole population (spec.md Assumptions); no dictionary outside them is considered.
- [X] T003 [P] Confirm the third-authority source for FR-013's non-circularity corroboration is reachable: the code generator's group-order data in `tools/codegen/fixpp-codegen/ir.cpp` (`walk_level` / `GroupOrderEntry`). Record which authority will be used for the documented sample (codegen data, or fixpp#208's tabulated Orchestra values).
- [X] T004 [P] Record the current documented exception sets of both loader fuzz harnesses verbatim — `tests/fuzz/fuzz_dict_xml_loader.cpp:50-72` and `tests/fuzz/fuzz_orchestra_loader.cpp:52-80` — so FR-006c's new throw can later be shown to land **inside** each set with **no harness widening** (the Orchestra harness catches the *derived* type).

---

## Phase 2: Foundational — plan.md Phase 1 (RED) ⚠️ BLOCKING

**Purpose**: `plan.md`'s Phase 1. Establish every measurement and every RED witness **before** any implementation. This is a blocking prerequisite for all five stories, so tasks here carry **no** story label even where their witness later serves one story.

**⚠️ CRITICAL**: no implementation task (T017 onward) may start until **T015** passes.

- [X] T005 Extend the census oracle **additively** in `tests/dictionary/required_scope_oracle.hpp` — add a NEW per-context `group_delims` map beside the existing `group_members`; do **NOT** reshape `group_members` (research D-8: its census pins are consumed by `082-structural-group-detection`, parked on an unbuildable branch where the breakage could not be observed). Populate it by a document-order walk twinned with `qfix_walk` / `orch_walk` but **sharing no code with the loaders** (FR-013 non-circularity).
- [X] T006 Create the all-ten regression pin `tests/dictionary/delimiter_census_test.cpp` (FR-002/FR-010/FR-012/FR-013/FR-015) — **standalone** `add_test` with its own per-test `TIMEOUT` (exact-set completeness gate, `[const §VII.8]`), registered in `tests/dictionary/CMakeLists.txt`. Assert, for **every** group context in **all ten** dictionaries and with **no carve-out, exclusion list or per-dictionary exemption**, that the resolved delimiter equals T005's declaration-order first member, and that the member set matches (FR-015 — same pin, not a second assertion). **MUST discriminate a context miss from a wrong answer** by comparing the returned span's `.data()` pointer against the bare span's (`group_ctx_delims.md` "Lookup-miss behaviour"; omitting this inflated fixpp#210's count by 10). Named cases: `DelimiterCensus.RegisteredGroupCountMatchesCodegenFix50Sp2` (SC-005/FR-017/W-7), `DelimiterCensus.NoChangeDictionariesUnchanged` (SC-008 — **FIX40 / FIX41 / FIXT11 only**; FIX42 *does* change: 8 wrong / 4 polluted), `DelimiterCensus.RedCountsReconcileWithSpecBaseline` (SC-015).
- [X] T007 Cross-check a **documented sample** of T006's expected delimiters against the third authority selected in T003 (FR-013) and record the sample and its outcome in `specs/083-group-delimiter-resolution/research.md` ("Measurement provenance"). If the oracle mirrors the fixed loader's logic, a green pin means nothing.
- [X] T008 **Baseline-measure the 30 currently-unregistered contexts** inside T006's pin — the ONE figure in `spec.md`'s Baseline table that is a *projection* rather than a measurement (population 335 → 365). The original probe `continue`s past them **before** the delimiter check; the pin must score them instead. Record the measured value in `research.md` and, if it differs from 30, amend `spec.md` (SC-001/SC-015). This must be **measured before the fix, not asserted after it**.
- [X] T009 [P] Create `tests/wire/delimiter_divergence_wire_test.cpp` and observe it **RED** (registered in `tests/wire/CMakeLists.txt`, existing `wire` bucket): the **FR-001 premise repro** (quickstart §1 — a divergent context, `CollateralRequest(AX)` / `NoExecs(124)` is cleanest, is REJECTED today while the first-seen context of the same count tag is ACCEPTED; **if both accept, the feature's premise is wrong — say so and stop**), plus `DelimiterDivergenceWire.PollutedContextAcceptsInjectedTagTodayRejectsAfter` (FR-010a / SC-013 / #210 C1), `DelimiterDivergenceWire.TrailingMessageFieldNotSwallowedIntoLastInstance` (FR-007a / SC-013 / #210 C2 — **or**, if the swallow proves unreachable, record the shapes tried and the negative result in `spec.md` Assumptions; do not quietly drop it), and `DelimiterDivergenceWire.NamedCountTagSubsetAccepts` (SC-004's eight tags 1677, 1772, 40204, 41599, 42060, 1499, 1669, 1919 — RED now, required green at the Phase-3 exit).
- [X] T010 [P] Create `tests/wire/consume_group_nested_delim_test.cpp` and observe it **RED** — #208's B-2 two-instance repro (quickstart §2: `35=X | 100=2 | 200=1 201=A | 200=1 201=B`): **W-1** on a hand-built `table_view` (bare fallback, `include/fixpp/dict/table_view.hpp:364`) **and** `ConsumeGroupNestedDelim.NestedDelimiterDescendsOnPopulatedContextStore` (**W-1a** — the same shape with the outer group registered under a **non-empty parent path**, via `load_from_string` or a fixture seeded through `set_group_first_ctx`/`add_group_member_ctx`, so the descent resolves through `group_ctx_` rather than the bare fallback). W-1 alone is insufficient — it exercises only the bare path.
- [X] T011 [P] Create `tests/wire/typed_read_split_agreement_test.cpp` with **W-10a leg 1 only** and observe it **RED** (quickstart §2a): on the same bytes as T010, `group(no_tag)->entry_count()` spans **one** instance today. The expected post-fix extent MUST be **derived from the fixture's own entry layout** (delimiter count field + nested extent + members, per instance), **never captured from the implementation** — a golden captured post-fix enshrines whatever the fix did.
- [X] T012 **Observe every RED above and RECONCILE the counts** against `spec.md`'s **335 / 52 / 30 / 232** (SC-015, quickstart §3). Recording is not enough. A delta is not automatically a spec error: explain in `research.md` whether it falsifies the **probe**, the **pin**, or the **spec**, and amend `spec.md` **before Phase 3 (US2) begins**. From this task onward the checked-in pin — not the deleted `delim_probe3.cpp` scratch probe — is the **authority of record** for every figure in the Baseline table.
- [X] T013 **Resolve research.md D-12 — instrument the `fr.type` PREDICATE, not the call path.** On a FIX42 load, for `tag == 146` under message `R`: (1) log `fr.type` **as the `src/dictionary/dictionary.cpp:446` loop over `message_fields("R")` sees it** — the value the gate actually tests; (2) compare against `Dictionary::field_ref("R", 146).type` (`include/fixpp/dict/dictionary.hpp:95` documents this as a *composed* `FieldRef`); (3) log the same pair for control tag **382** in the same dictionary (measured as a genuine bare fallback) so a null result is distinguishable from broken instrumentation. Record the outcome in `research.md` D-12. **This MUST be sequenced ahead of the loader change (T026–T030)**: on branch (a) D-1's capture would not cover every context that exists today.
- [X] T014 **Apply D-12's branch-conditional amendment** in `specs/083-group-delimiter-resolution/research.md` (+ `contracts/group_ctx_delims.md`, `spec.md` as required): **branch (a)** (both report `Int` ⇒ an unaccounted population path) — amend **D-1 and D-3**, and **re-derive C-3.4a's checked set** against the newly-found path, **before Phase 5 (US1 loaders)**; C-3.4a's set-equality leg is explicitly conditional on branch (b). **branch (b)** (the loop sees `NumInGroup`, `field_ref` reports `Int` ⇒ type promotion) — amend the L-063-1 / L-066-1 disposition and the #196 / 082 handoff note (T073). Either outcome is recorded; neither is assumed.
- [X] T015 **PHASE-1 EXIT GATE** — record in `.specify/decisions/083-group-delimiter-resolution-verify.md` (or `research.md`) that ALL of the following hold: (i) T006, T009, T010, T011 observed **failing**, with counts; (ii) T012's reconciliation against 335 / 52 / 30 / 232 done and any delta explained + `spec.md` amended; (iii) T008's measurement of the 30 recorded; (iv) T013 answered and recorded and T014 applied. **No implementation task may start before this gate.**

**Checkpoint**: every measurement is checked in and every premise is witnessed RED. Implementation may begin.

---

## Phase 3: User Story 2 — a group whose first member is itself a group parses correctly (Priority: P1) 🎯 first increment

*(plan.md Phase 2 — Receiver. Placed FIRST by the correctness constraint above, ahead of the P1 headline story.)*

**Goal**: the receiver descends when an instance-opening delimiter is itself a nested group's count tag, and resumes one past that group's extent (FR-007/FR-008/FR-009). Touches `include/fixpp/wire/validator.hpp` **only**.

**Independent Test**: construct a dictionary in which an outer group's delimiter is a nested group's count tag, then validate a two-instance message — today it is rejected while the one-instance form is accepted; after this story both are accepted, and a declared count that disagrees with the instances present is **still** rejected.

- [X] T016 [US2] Add **W-3** — `ConsumeGroupNestedDelim.NestedDelimiterAtDepthCapIsBoundedNotUnbounded` — to `tests/wire/consume_group_nested_delim_test.cpp` (FR-009 / US2 scenario 4; it was the only witness for FR-009 and had no case until Gate A r3). Build the W-1 shape nested to the engine's supported limit; assert the validator returns a **defined rejection** rather than recursing without bound, and that behaviour at depths **below** the cap is unchanged. Record its pre-implementation disposition (the shape is rejected today by the un-descended scan) so the case is demonstrably non-vacuous.
- [X] T017 [US2] Implement **C-4.1 / C-4.2 / C-4.3** in `include/fixpp/wire/validator.hpp` (`consume_group`, `:357-406`): replace the bare `++i` at `:362` that consumes the instance-opening delimiter with query-before-consume — if `delim_tag` is itself a group **in child context**, recurse via `consume_group` and resume one past its extent; otherwise `++i` as today. The probe MUST reuse the **same shape** the post-delimiter descent already uses at `:376` (`dict_.group_first_field(ctx.msg_type, child_path, t) != 0`) and the **same `can_descend` / K=16 guard** — a symmetry repair, no new mechanism, no new recursion limit. Invariants that MUST NOT move (FR-008 / C-5.1..C-5.4): the delimiter's required-bit is marked **before** descent (`delim_k`, `:354`); instance-count enforcement at `:402`; extent termination at `:365`; a group opened with a tag that is not that context's delimiter still rejects. Makes T010 (W-1, W-1a) and T016 (W-3) green.
- [X] T018 [P] [US2] Seed `tests/fuzz/fuzz_wire_validator.cpp`'s corpus with a nested-delimiter shape exercising the new `consume_group` descent (`[const §VII.7]`).
- [X] T019 [US2] **PHASE-2 EXIT GATE** (record in `.specify/decisions/083-group-delimiter-resolution-verify.md`): **(i)** **W-1a green** — the descent proven on a **populated context store**, not only on the bare fallback (W-1 alone is insufficient: a hand-built `table_view` never populates `group_ctx_`, `include/fixpp/dict/table_view.hpp:346-349`). **(ii)** the ten dictionaries' **currently resolved delimiters are UNCHANGED** — run T006's pin and assert its RED counts are byte-identical to T012's. *(The old gate "no change to the delimiter pin's failure count" is deliberately NOT used: a validator-only phase cannot move a dictionary-level count, so it could never fire.)*

**Checkpoint**: the receiver descends. The loader recursion is now safe to land.

---

## Phase 4: User Story 5 — leg 1 of 2: the offset table's extent walk (Priority: P3 story, ordering-forced position)

*(FR-021e / SC-016 / C-8.0c — `contracts/typed_read_splitter.md`. `plan.md` lists C-8.0c under Phase-4 **content**, and requires its witness **W-10a green at the Phase-3 exit** (gate leg (iv)). It is not blocked by Phase 3 — its delimiter is wire-derived and it needs no per-context delimiter record — so it lands here, closing the window in which validation accepts (Phase 2) while the read path truncates.)*

**Goal**: `OffsetTable::consume_group_extent` descends at the instance-opening delimiter, so the extent spans all `declared` instances on the 262 contexts (232 measured + 30 newly registering) whose post-fix delimiter is a nested group's count tag.

**Independent Test**: quickstart §2a — take the T010 bytes through the **read** path: `group(100)->entry_count()` spans one instance today and all `declared` after; `group_slices()` reports 1 today and 2 after; the one-instance form still reports 1.

- [X] T020 [US5] Extend **W-10a** in `tests/wire/typed_read_split_agreement_test.cpp` with **leg 2** (instance count both directions — `group_slices_status(no_tag)` yields N = `declared` for the two-instance form; the **one-instance** form still yields exactly 1, the regression guard) and **leg 3** (agreement with validation — the same bytes validated inbound report the same N **and the same instance boundaries**, SC-016). Observe RED before T022.
- [X] T021 [US5] Add **W-10a leg 4** — the depth cap **returns immediately** rather than burning `declared` no-op iterations (C-8.0c.3) — to `tests/wire/typed_read_split_agreement_test.cpp`. **Leg 4 CANNOT be observed RED** (pre-fix the branch it discriminates does not exist), so its control is the discriminator, not a RED observation. MUST: (a) fixture nested at `kMaxGroupDepth` with `declared ≥ 2` **at the frame whose descent hits the depth cap — NOT the outermost frame** (the outermost count is invariant under the un-mirrored implementation too, so varying it false-greens); (b) supply a **counting wrapper** on the **existing** construction-time `group_member_fn_t` seam (`include/fixpp/wire/offset_table.hpp:79-80`, `:119-121` / `:126-128`) — no production change, no new seam; (c) assert the recorded invocation totals are **EQUAL** across that frame's `declared = 2` and `declared = 8`, the two runs differing in **exactly one digit of that frame's count field**, entry layout **byte-identical** and physical instance count unchanged; (d) `err_group_too_large` is returned — but the error code **alone is not the discriminator** (`overflow` is a `bool&` reaching `group()`'s `:551-553` either way); (e) the per-test `TIMEOUT` is a **BACKSTOP only**, never the discriminator. No absolute hand-derived invocation constant (16 enclosing frames' probes would make it un-re-derivable).
- [X] T022 [US5] Implement **C-8.0c** in `src/wire/offset_table.cpp` `consume_group_extent` (`:438-503`): before the bare `++k` at `:475`, probe whether `delim` heads a group in the **child** context already computed at `:466` (`ctx.pushed(group_no_tag)`), using the **same probe shape** as `:485-486` — `group_member_fn_(opaque_dict_, child, delim, entries_[k + 1U].tag)` under the `k + 1U < entries_.size()` bound — and if so advance via the existing `consume_group_extent` recursion with `static_cast<std::uint8_t>(depth + 1U)` under the existing `kMaxGroupDepth` guard (`:442-445`), **MIRRORING `:489-491`'s `if (overflow) { return k; }`**. **Do NOT** re-point `:454`'s wire-derived local or `:457`'s membership probe (C-8.0) — this is a traversal change, not a source change. Invariants (C-8.0c.4): per-instance entry cap at `:496-499`, `declared`-bounded outer loop at `:473`, extent termination at `:476-479`, and empty/absent nested groups behave exactly as the bare `++k` does today (`:461-462`, `:447-449`). Makes W-10a legs 1–3 green.
- [X] T023 [US5] **Demonstrate leg 4 is discriminating** — locally delete the `if (overflow) { return k; }` mirror added by T022, confirm T021's invocation-count equality **FAILS**, restore, and record the demonstration in `.specify/decisions/083-group-delimiter-resolution-verify.md`. This is FR-014's standard applied to the one leg that cannot be observed RED.
- [X] T024 [US5] **Assess and record** C-8.0c.4's open leg in `research.md` (D-4a): whether any shipped shape breaches `default_max_group_entries_per_instance = 4096` (`include/fixpp/wire/offset_table.hpp:28`) now that `(k - inst_start)` includes the entries of a nested group opened **at** the delimiter. A breach would be a **new rejection** under SC-007. The bundle records this as *inferred, to be confirmed by this task* — record "low, and here is why" with the shapes checked, not an assumption.

**Checkpoint**: the extent walk descends at both positions. The Phase-3 gate's leg (iv) is satisfiable.

---

## Phase 5: User Story 1 — leg 1 of 2: per-context delimiter resolution in both loaders (Priority: P1)

*(plan.md Phase 3 — Loaders, plus the `as_table_view()` consume the Phase-3 gate's "pin green" leg requires; see "reconciled placement decisions" above.)*

**Goal**: the delimiter is resolved from the group's own declaration in its own context `(msg_type, ancestor-path, no_tag)`, in **both** loaders, by capturing the first emission of the existing document-order walk — not by adding a second traversal.

**Independent Test**: build a wire message for a divergent context (FIX44 `AX` / `NoExecs(124)`) and observe **acceptance**; build the equivalent message for a *first-seen* context of the same `NumInGroup` tag and observe it is **still** accepted. The asymmetry between those two is the entire defect.

- [X] T025 [US1] Add the Entity-2 per-context delimiter side table + accessor to `src/dictionary/dictionary_internal.hpp`, keyed `(msg_type, parent_path, no_tag)` **identically** to `table_view::group_ctx_`'s `make_group_ctx_key` (C-2.2), PMR-allocated on the caller's `memory_resource` alongside the handle's other tables, **immutable after load** with no mutation path exposed (C-2.1), load-time allocation only (C-2.3). `parent_path` is outermost-first and **excludes** the group's own `no_tag` (C-1.3). `include/fixpp/dict/field_ref.hpp` stays **UNCHANGED** — `FieldRef` is ABI-pinned at 16 bytes and must not grow.
- [X] T026 [US1] Thread the ancestor path through `src/dictionary/xml_loader.cpp` `LoaderState::expand_field_list` (`:525-670`) — a `std::vector<std::uint16_t>& group_path` pushed/popped around the nested-group recursion at `:667` (research D-2). **Gate record emission on an output sink that is NULL at the two non-message-scoped call sites**: records are emitted **only** from the per-message expansion at `:927-931`, and **nothing** from the component-cache expansion at `:968` or the group-cache expansion at `:1056` (C-1.1). Emitting from those two manufactures contexts with no message type and corrupts the store.
- [X] T027 [US1] Implement the **D-1 first-emission capture** in `src/dictionary/xml_loader.cpp`: a per-group frame carrying `delim = 0`; the **first `FieldRef` emitted at that group's level** sets it; on pop, emit the record `(msg_type, path, no_tag) → delim` (C-1.2). **Do NOT write a document-order scan** — component members already expand inline at the enclosing level (`:576-579`) and a nested group's count tag is already pushed at the outer level before descent (`:594-597`), so FR-003 and FR-004 fall out. Adding a third traversal recreates the exact drift that caused the defect. Preserve **C-1.4a**: the loader **throws** on an unresolvable field/component/group reference (`:535-541`) — never skips; a silent skip would make the *second* child the captured delimiter. Recording `0` is not permitted (C-1.4 → FR-006 disposition, T036).
- [X] T028 [US1] Delete the one-level component scan at `src/dictionary/xml_loader.cpp:610-641` **and repopulate** the global `GroupDef.first_field_tag` (written at `:644`) as a **first-seen projection of Entity 2** (research D-10 / C-1.4b). It must NEVER be left at 0: the bare `group_first_field(no_tag)` doubles as an *is-this-tag-a-group* **predicate** at `src/capi/message_write.cpp:157`, `:812`, `:923`, at `src/dictionary/dictionary.cpp:398-405`, at `include/fixpp/wire/validator.hpp:458-459` and at `include/fixpp/dict/table_view.hpp:364`/`:377`; leaving it unpopulated would make the C ABI's `group_begin` reject **all** groups — a total regression through a GA-frozen ABI.
- [X] T029 [US1] Order the projection write **explicitly before** `finalize()`'s 072 nested/parent delimiter collision guard reads it — `src/dictionary/xml_loader.cpp:1016-1017` (the one consumer that **throws**). The guard must read post-projection values, never a half-populated global (D-10 / C-7.2's write-order leg).
- [X] T030 [US1] Implement the **symmetric twin** in `src/dictionary/orchestra_loader.cpp` (`OrchestraLoaderState::expand_field_list`, `:520-645`): same path threading, same first-emission capture replacing `first_member_tag` (`:629`), same projection, and the same write-order relative to the guard at `:895-896`. The same three properties hold there (component ref inline at `:573-577`; nested count tag pushed at `:606-609`; recursion after `:640`). **FR-005 / C-1.5 — a one-loader fix is a half-restructure**: Orchestra has zero broken-scan and zero unregistered groups yet still shows **30** wrong-delimiter contexts.
- [X] T031 [US1] Make `Dictionary::as_table_view()` source the delimiter for `set_group_first_ctx` from **Entity 2** — `src/dictionary/dictionary.cpp:510-511` → `:518` (C-3.1), replacing `group_first_field(no_tag)` with its `members.front()` fallback. **Do NOT remove `set_group_first_ctx`'s member injection** — the unconditional `add_group_member_ctx(msg_type, parent_path, no_tag, first)` at `include/fixpp/dict/table_view.hpp:645` is **RETAINED**; the pin asserts it has become a no-op (C-3.3 / D-5), and it remains load-bearing for the hand-built-fixture fallback at `:346-349`. Leave the legacy bare-store loop at `:398` alone (it is fed by T028's projection).
- [X] T032 [US1] Correct the **false** load-bearing comment at `src/dictionary/dictionary.cpp:507-508` — the sentence asserting the per-context **member set** "stays exact regardless" of divergent delimiters (FR-011 / C-3.2); it is the reason the defect survived inspection. **NOT `:503-505`** — that is TRUE text about `members.front()` and MUST be left alone (`plan.md` N24 flags this exact mis-citation as the trap `/speckit-tasks` would fall into).
- [X] T033a [US1] **UN-SKIP the parked L-063-3(b) pin** — `tests/wire/validator_nested_membership_test.cpp:304` `ValidatorNestedMembership.PerContextDelimiterResidual_L063_3b_SkipPending`. *(Added 2026-07-31 during Phase-1 execution; no task scheduled it and its absence would have been a silent coverage hole — see the verify record's amendment log.)* Its `GTEST_SKIP()` reason names **this feature** as its unblocking condition verbatim: *"as_table_view() stores the global first-seen delimiter (dictionary.cpp :421), not a per-context one. A GREEN assert here would ENSHRINE the bug… Un-skip when the per-context-delimiter follow-up lands (tracked via L-063-3)."* T031 makes `as_table_view()` source the delimiter per context and **T070 retires L-063-3**, so leaving this skipped would retire the B&L row while its own pin stays parked. The test body already exists below the skip and needs no authoring: **delete the `GTEST_SKIP()`**, confirm it passes, and rename it to drop `_SkipPending`. If it does **not** pass once T031 lands, that is a finding about the fix, not a reason to re-park it.
- [X] T033 [US1] Flip `tests/wire/delimiter_divergence_wire_test.cpp`'s cases (T009) from RED to GREEN and record the deltas: the FR-001 premise repro now **accepts** the divergent context while the first-seen context is **still accepted** (US1 scenarios 1–2); a group opened with a tag that is not that context's declared delimiter is **still rejected** (scenario 3); `PollutedContextAcceptsInjectedTagTodayRejectsAfter` now **rejects** (#210 C1) and `TrailingMessageFieldNotSwallowedIntoLastInstance` no longer swallows (#210 C2), or the negative result is recorded per T009.

**Checkpoint**: delimiters are per-context in both loaders and reach the registered store. Member-set pollution (52) falls out by construction.

---

## Phase 6: User Story 3 — groups the runtime silently ignored become known to it (Priority: P2)

*(plan.md Phase 3 — dispositions and preconditions; this phase ends at the Phase-3 EXIT GATE, which spans US1's loader leg, this story, and US5 leg 1.)*

**Goal**: no group is silently dropped. An unresolvable first member is a **fail-closed** load rejection by default with an explicit opt-in tolerant mode, the Entity-2 completeness invariant is enforced at `finalize()`, and FIX50SP2 registers **505** groups, matching the code generator.

**Independent Test**: count registered groups for FIX50SP2 before and after; assert the three previously-dropped groups (delimiters `1499→453`, `1669→1529`, `1919→1920`) resolve a non-zero delimiter and their nested children (1529, 1534, 1540, 1559, 1918, 1920) become reachable.

- [X] T034 [US3] **PRECONDITION C-7.1 — run BEFORE the FR-006 throw is enabled** (T036): confirm **all ten shipped dictionaries still load under the fail-closed default**, and classify every surviving zero-delimiter group into exactly one of C-6.1a's two classes — *reachable from message expansion with an unresolvable first member* (**C-6.1 fires**: record it and decide explicitly whether it is a dictionary defect to report or a case for tolerant mode) versus *declared but reachable from no message expansion, zero contexts* (**C-6.1 does NOT fire**; informational diagnostic only, FR-006d / C-3.6). Record the classification in `research.md` (D-7). Enabling C-6.1 without running this check is the one unacceptable outcome.
- [X] T035 [US3] Create `tests/dictionary/loader_disposition_test.cpp` (registered in `tests/dictionary/CMakeLists.txt`, existing `dictionary_pure_tests` bucket) and observe **RED**: **W-4** (a dictionary with an unresolvable group is **rejected** under the default, diagnostic **naming that group**), **W-5** (the same dictionary **loads** under tolerant mode with a diagnostic, group left unregistered), **W-6** (all ten shipped dictionaries load under the default — C-7.1's checked-in twin), `LoaderDisposition.DeclaredGroupWithZeroContextsDoesNotFailClosed` (FR-006d / C-6.1a), `LoaderDisposition.OrchestraRejectionIsOrchestraParseError` (FR-006c / C-6.1b).
- [X] T036 [US3] Implement FR-006's **fail-closed default** in `src/dictionary/xml_loader.cpp` and `src/dictionary/orchestra_loader.cpp`: a group whose first member cannot be resolved even after T027's recursion **rejects the load** with a diagnostic naming the offending group (C-6.1), mirroring the loader's existing disposition. **Behaviour symmetric, type per loader** (C-6.1b / FR-006c): `XmlLoader` throws `xml_parse_error`, `OrchestraLoader` throws `orchestra_parse_error` (derives `xml_parse_error`, `include/fixpp/dict/error.hpp:98`). **No `core::error` enum slot is added**, so `tests/core/test_020_error_completeness.cpp`'s slot pin is not engaged. A group producing **zero contexts** MUST NOT trip this (C-3.6 / FR-006d).
- [X] T037 [US3] Implement FR-006a's **explicit opt-in tolerant mode** in `include/fixpp/dict/xml_loader.hpp` and `include/fixpp/dict/orchestra_loader.hpp` — a **defaulted parameter or options object defaulting to fail-closed**, never a new required argument (C-6.6: existing source callers must compile unchanged; no C ABI symbol involved). Same option, same semantics, both loaders (C-6.4). Never implicit, never inferred, never the fallback on a parse difficulty (C-6.3). **Load-time only — no branch on the parse or validate path** (C-6.5). Makes T035's W-4/W-5 green.
- [X] T038 [P] [US3] Update the documented exception list at `include/fixpp/dict/orchestra_loader.hpp:33-37` **in the same change** as T036's throw (FR-006c).
- [X] T039 [P] [US3] Update the documented exception-set comments in `tests/fuzz/fuzz_dict_xml_loader.cpp:50-72` and `tests/fuzz/fuzz_orchestra_loader.cpp:52-80` to cover FR-006's new throw, and confirm against T004's record that **no harness widening is required** — the Orchestra harness catches the *derived* type, so a base `xml_parse_error` from `OrchestraLoader` would escape to its terminal rethrow and crash the fuzzer on every input with an unresolvable group.
- [X] T040 [US3] **PRECONDITION C-7.3 — run BEFORE the FR-023 check is enabled** (T041), as a **both-directions set diff** on all ten dictionaries: enumerate what `as_table_view()` actually registers and diff it against the checked set **C-3.4a** defines (a context is in the checked set iff, on `mt`'s deduped field run, the count tag's `FieldRef` type is `NumInGroup` **and** at least one `FieldRef` has `group_no_tag == no_tag` — the `!members.empty()` predicate at `src/dictionary/dictionary.cpp:463`). **Registered-with-no-record = the C-3.4 violation and a Phase-3 blocker** (D-12 branch (a) in a different disguise — not a reason to weaken the check). **Checked-set-not-registered = C-3.7 dead data: recorded, not failed.** Record both directions in `research.md`. This is the **third** new fail-closed load path and must not meet the shipped set for the first time in CI.
- [X] T041 [US3] Implement the **FR-023 completeness invariant** in both loaders' `finalize()`: every group context `as_table_view()` will enumerate and register MUST have an Entity-2 record; a violation is a **load rejection under FR-006's disposition**. It is enforced at `finalize()` and **NOT** at `as_table_view()`, which is contractually **non-throwing** (established by 072; L-063-4) and must remain so — so a consumer-side miss is unreachable by construction. **No silent fallback**: neither `group_first_field(no_tag)` (reinstates this feature's own defect) nor `members.front()` (reinstates a worse defect already fixed and pinned by `tests/wire/validator_production_table_view_test.cpp::ValidatorProductionTableView.GroupDelimiterFromWireNotTagSortedMember`). Tolerant mode operates at **load**, so a tolerantly-skipped group never enters the consumer's enumeration (FR-023a).
- [X] T042 [US3] Add the three FR-023 named cases to `tests/dictionary/loader_disposition_test.cpp` (RED before T041): `LoaderDisposition.ContextWithoutDelimiterRecordRejectedAtFinalize` (C-3.4), `LoaderDisposition.ContextWithoutDelimiterRecordTolerantModeSkipsGroup` (FR-023a — the tolerant twin, asserting the group is skipped rather than half-registered), `LoaderDisposition.ScalarReuseOfGroupTagIsNotACompletenessViolation` (C-3.4a's `!members.empty()` leg — a message reusing a `NumInGroup`-typed tag as a plain scalar contributes **no** context and must not trip C-3.4).
- [X] T043 [US3] FR-017 / SC-005 / **W-7**: make `tests/dictionary/delimiter_census_test.cpp::DelimiterCensus.RegisteredGroupCountMatchesCodegenFix50Sp2` green — the FIX50SP2 registered-group count agrees with the code generator's emitted count, and the **502 → 505** delta is **accounted for by naming the three groups** (count tags 1499, 1669, 1919) rather than asserted as a bare number. **Run `ctest -L codegen` for this task specifically** — the loader change is codegen-adjacent and a label-filtered run over the plausible gates can still miss this count pin.
- [X] T044 [US3] **C-7.2 / FR-012a / SC-014** — re-derive `tests/dictionary/reused_tag_census_test.cpp::NestedGroupDelimiterCensus` under the **POST-FIX** delimiters across all ten dictionaries and confirm nested-vs-parent delimiter collisions remain **0**. L-063-4's *"0 collisions"* audit was taken against the pre-fix delimiters this feature changes in **335** contexts, and the guard it corroborates (`src/dictionary/xml_loader.cpp:1016-1017`, `src/dictionary/orchestra_loader.cpp:895-896`) now derives its values from T028/T030's projection — without re-derivation the census goes **vacuous without failing**: it still passes and stops pinning. Confirm T029/T030's write-order (projection complete before `finalize()`'s guard reads it). The B&L row edit itself is T071.
- [X] T045 [US3] **PHASE-3 EXIT GATE** — record in `.specify/decisions/083-group-delimiter-resolution-verify.md`: T006's delimiter pin **green on all ten with no carve-out**; FIX50SP2 **502 → 505** with the delta accounted for by three named groups; **C-7.1** all ten load under the fail-closed default (T034); **(i)** SC-004's named eight-tag **wire** subset green **at this exit**, not deferred (T009's `NamedCountTagSubsetAccepts`); **(ii)** **C-7.2** re-census done with the write-order stated (T044); **(iii)** **C-7.3** both-directions set diff done (T040); **(iv)** **W-10a green at this exit** (T020–T022 landed). Legs (i) and (iv) exist so the tree cannot sit between phases in the strictly-worse state the ordering constraint forbids with every gate green.

**Checkpoint**: US1 + US3 delivered; the dictionary layer is correct and pinned on all ten.

---

## Phase 7: User Story 4 — the delimiter cannot silently regress again (Priority: P2)

**Goal**: a regression in delimiter resolution fails the build, and the pin is provably non-circular and provably capable of failing.

**Independent Test**: deliberately reintroduce a global-first-seen delimiter resolution and confirm the pin fails; restore and confirm it passes.

- [X] T046 [US4] **FR-014 / SC-006** — reintroduce global-first-seen resolution locally (revert T031's Entity-2 source at `src/dictionary/dictionary.cpp:510-511`), confirm `tests/dictionary/delimiter_census_test.cpp` goes **RED**, restore, confirm **GREEN**, and **record the demonstration** in `.specify/decisions/083-group-delimiter-resolution-verify.md`. Two different guards, both required: pin red **before** the fix (T012) proves it measures something; pin red **on reintroduction** proves it measures *this* something.
- [X] T047 [US4] **FR-012 / FR-016** — assert in `tests/dictionary/delimiter_census_test.cpp` that the pin carries **no carve-out, exclusion list or per-dictionary exemption**, and record in the test header plus `research.md` (D-9) that the collision-membership suite (`tests/dictionary/collision_membership_guards_test.cpp`) is **NOT** counted as delimiter coverage at any cardinality — its discriminator is derived independently of the delimiter (`first_tag_only_in`), and a sibling feature had to add an `exclude` parameter specifically so the injected delimiter would not be chosen as a discriminator.
- [X] T048 [US4] **SC-008** — `tests/dictionary/delimiter_census_test.cpp::DelimiterCensus.NoChangeDictionariesUnchanged` green: **FIX40, FIX41, FIXT11** show no behavioural change (0 affected contexts before and after). FIX42 is deliberately **not** in this set — it changes (8 wrong / 4 polluted), and the Gate A round-1 measurement that refuted N14 makes that omission correct.

**Checkpoint**: the pin is non-circular, proven red twice, and carve-out-free.

---

## Phase 8: User Story 1 — leg 2 of 2: the C-ABI construction path (Priority: P1)

*(plan.md Phase 4 — FR-018 / FR-018a / FR-018b, `contracts/capi_group_grammar.md`. One rule on both paths: a message the builder accepts is never rejected by the engine's own validation on delimiter grounds, and vice versa.)*

**Independent Test**: for a divergent **nested** context, build the group through the C ABI and validate the same bytes inbound — the two verdicts agree in **both** directions; and no exported C ABI signature changes.

- [X] T049 [US1] Create `tests/capi/capi_group_delimiter_ctx_test.cpp` (registered in `tests/capi/CMakeLists.txt`, `capi_pure_tests` bucket) and observe **RED**: **W-11** `CapiGroupDelimiterCtx.ConstructionAcceptsIffValidationAccepts` (a **divergent** context under a **non-empty** ancestor path, run for both the accepting and the rejecting delimiter placement); **W-11a** `CapiGroupDelimiterCtx.CommitDoesNotRebuildTableViewPerMessage` (open a session, commit **N > 1** group-bearing messages, assert `as_table_view()` was entered **once**). *(Seam route pinned 2026-07-31 at the checklist audit — previously named only by analogy, which is the same "route not named" gap C-9.2a was added to close.)* The seam is `fixpp::dict::detail::as_table_view_call_count()`, incremented in `Dictionary::as_table_view()`'s body in `src/dictionary/dictionary.cpp` and declared in **`src/dictionary/dictionary_internal.hpp`** — the internal header T025 already edits — **not** the public `include/fixpp/dict/dictionary.hpp`. This matches the `fixpp_capi::detail` live-state-counter precedent (`src/capi/capi_internal.hpp:496-503`), which is likewise declared in an *internal* header; `tests/capi/` already includes internal headers directly, so no installed public surface is touched. **BUILD DETAIL — do this or the witness will not compile**: the seam is the first `tests/capi/` → `src/dictionary/` include, and `FIXPP_CAPI_FEATURE_B_INCLUDES` (`tests/capi/CMakeLists.txt:42-46`) currently lists only `${CMAKE_SOURCE_DIR}/src/capi`, `${CMAKE_SOURCE_DIR}/tests` and the test dir itself — so **add `${CMAKE_SOURCE_DIR}/src/dictionary` to that list** (this file is in the `capi_pure_tests` bucket, which consumes it at `:80`). `tools/check_layers.py` has **no** opinion here — verified, it scans only `src/**` and `bindings/**` (`:172-176`), not `tests/`. **W-11b** `CapiGroupDelimiterCtx.DictFreeHandleStillSkipsDelimiterCheck`; **W-12** `CapiGroupDelimiterCtx.DisclosedDelimiterMoveRejectsOldOrder` (one of the five named groups: old opening tag ⇒ `FIXPP_ERR_TYPE_MISMATCH`, new ⇒ `FIXPP_ERR_OK`); **W-13** `CapiGroupDelimiterCtx.GroupBeginStillAcceptsEveryRegisteredGroup` (all ten dictionaries, every registered `no_tag` answers the bare predicate non-zero).
- [X] T050 [US1] Add the **session-cached `table_view`** member to `fixpp_session` in `src/capi/capi_internal.hpp`, beside `std::shared_ptr<const Dictionary> dict_` (declared at `:493`), and build it **once at `fixpp_session_open`** in `src/capi/session.cpp:109-111` — the point where `dict_` is already cached (C-9.2a / D-13). *(This file is missing from `plan.md`'s Source Code tree; the clause mandates the site.)* Exactly **one** `as_table_view()` per opened session, **zero** per message. It is **owned by the session**, not the message handle, and **not** stored in the session arena (same reason `dict_` is not, `capi_internal.hpp:261-266`). `dict->as_table_view()` inside the commit path is **BARRED** by `[const §XV.1]` — it is a constitution violation, not merely a slow path. Makes W-11a green.
- [X] T051 [US1] Copy a **non-owning `table_view const*`** into `fixpp_msg` at `fixpp_msg_create_outbound` — `src/capi/message_write.cpp:289-291`, exactly as `dict_` is copied into the member declared and documented at `src/capi/capi_internal.hpp:261-266`. The cached view is absent on **exactly** the handles whose `dict_` is null (inbound dispatch-window handles), so "no dictionary" and "no view" are **one state** and C-9.4's dict-free disposition is unchanged (W-11b). Commit reads **the session-cached view, never `owned_tv_`** (`capi_internal.hpp:267-285` — that is the clone-inbound membership copy, a different artifact). Internal structs only: no `fixpp_*` declaration, enum value, struct layout or `FIXPP_C_ABI_VERSION_*` constant moves (C-9.3 / FR-018a).
- [X] T052 [US1] Thread the context through `validate_group_grammar` in `src/capi/message_write.cpp:710-730`: add a `std::string_view msg_type` and an ancestor-path parameter to the **file-static** function, push `e.tag` onto that path at the existing `:725` recursion, and resolve the delimiter at `:719` through the T050 view's `group_first_field(msg_type, parent_path, no_tag)` instead of the context-free `dict->group_first_field(e.tag)` (C-9.1 / C-9.2). The message type is already available at both commit call sites (`:762`, `:781`). A **dictionary-present handle that cannot reach the view MUST fail the commit closed** — never fall back to the bare global, which reinstates exactly the defect FR-001 removes. Makes W-11 and W-12 green.
- [X] T053 [US1] **Do NOT convert** the three *is-this-a-group* **predicate** sites — `src/capi/message_write.cpp:157`, `:812`, `:923` — which keep the bare global query answered by T028/T030's repopulated projection (C-9.5). Making them context-keyed would reject a group whose context the caller has not yet established, i.e. **every** group, through the frozen ABI. Pin it with **W-13** over all ten dictionaries — D-10's total-regression scenario, whose whole hazard is that it would otherwise be caught only by a C-ABI client after release.

**Checkpoint**: construction and validation apply one identical rule; the ABI surface is byte-for-byte unchanged.

---

## Phase 9: User Story 5 — leg 2 of 2: the typed-read instance splitter (Priority: P3)

*(plan.md Phase 4 — FR-021/021a/021b/021c/021d, `contracts/typed_read_splitter.md` C-8.1..C-8.5. **C-8.6**: this leg lands after Phase 5's loaders — before them the dictionary-sourced value would be the global first-seen one and the splitter would be *less* correct than the wire-derived value it replaces.)*

**Goal**: typed-read and validation split group instances on the same dictionary-defined boundary, so a message that validates as N instances reads back as N instances **on the same boundaries**.

**Independent Test**: take a shape whose delimiter tag reappears at greater depth, validate to establish the instance count, then read it back through the typed API and compare count **and** boundaries.

- [X] T054 [US5] **W-8 — characterise FIRST** (FR-021a / FR-021d): add `NestedGroupExtent.SplitterWireDelimiterCharacterisation` to `tests/wire/nested_group_extent_test.cpp`, beside the existing synthetic at `:511-519`. Start from `spec/behaviors-and-limitations.md` **L-063-4** (issue **#180**), **not from zero** — it already names the flat splitter and the redundant cap loop by line, states the mechanism, records a measurement and points at that synthetic. Record, **with the shapes tried**, whether a mis-split is reachable on any shipped dictionary under the **post-fix** delimiters. Either outcome is the deliverable; a negative result is recorded, never asserted. **Do NOT inherit L-063-4's claim that `consume_group_extent()` computes `group_end` correctly** — it is false for the mode-(c) shape (T022 repairs it).
- [X] T055 [US5] **W-9 over TWO fixtures**, observed RED — `TypedReadSplitAgreement.ValidatedInstanceCountEqualsTypedReadInstanceCount` in `tests/wire/typed_read_split_agreement_test.cpp`: fixture **(b)** the outer delimiter tag **reappears at greater nesting depth** inside an instance, and fixture **(c)** the outer delimiter **IS a nested group's count tag**, ≥ 2 instances. Assert the same instance count **and the same instance boundaries** — not the count alone, since two different splits can yield the same count. **Both** fixtures MUST satisfy W-9's two constraints, asserted inside the case: a **divergent** context (the resolved delimiter differs from the bare global, or a fall-through to the global is unobservable) **and** a **non-empty `parent_path`** (so a key off by exactly one path element produces a different key, not an accidentally-equal one). Round 2's (b)-only fixture was structurally incapable of detecting the C-8.0c defect.
- [X] T056 [US5] **W-10 with BOTH exclusions asserted inside the case** — `TypedReadSplitAgreement.OutOfScopeWireProbesUnchanged`: on a **divergent** context, `consume_group_extent`'s extent bound, `group(no_tag)`'s returned `group_index` (`no_tag`, `first_entry()`, `entry_count()`) and `group_slices_reserve_bound()` are **unchanged from their pre-083 values** while `group_slices_status`'s split is not. Exclusion 1: the delimiter is **NOT** itself a registered group's count tag (the population C-8.0c provably does not touch). Exclusion 2: the fixture's **member sets — its own AND every nested context the extent walk descends through** — are identical pre/post-083, i.e. divergent but **not polluted** and not newly registering (the `table_view.hpp:645` injection becomes a no-op on the 52 polluted contexts, which moves the extent — and that movement is #210 Consequence 2, pinned as a **change** by T009). Both exclusions are **asserted, not assumed**: without them this pin's passing condition is that a defect is still present.
- [X] T057 [US5] **C-8.1** — add a `group_delim_fn_t` sibling of `group_member_fn_t` to `include/fixpp/wire/offset_table.hpp:79-80`, resolving through the same `void const* opaque_dict` to `table_view::group_first_field(msg_type, parent_path, no_tag)`. Supply it at **EVERY** construction site that supplies `opaque_dict` — `:119-121`, `:126-128`, **and `build_nested_subview` at `src/wire/offset_table.cpp:718-720`**. A missed site silently takes C-8.4's dict-free fallback on nested splits ("context seeded lazily on ONE path leaves sibling paths default"). Internal seam only; **no public signature changes** (C-8.3): `group_slices(no_tag)` / `group_slices_status(no_tag)` keep their signatures and `group_slices()` stays the one-line delegation at `:614-616`.
- [X] T058 [US5] **C-8.2** — re-point the splitter's boundary delimiter at `src/wire/offset_table.cpp:656` from `entries_[first].tag` to T057's callback, keyed on **`stored_group_context()`**'s path (`include/fixpp/wire/offset_table.hpp:327`, seeded by `set_group_context` at `:193-204`), i.e. `(msg_type, stored_group_context().parent_path, no_tag)` — the **same key** the validator descent uses. **NOT `group_context_for(no_tag)`** (`src/wire/offset_table.cpp:424-426`), which returns `stored_group_context().pushed(no_tag)` and would query one path element too long, violating Entity 1's "`parent_path` excludes `no_tag`" invariant. **C-8.4's fallback has exactly two cases**: no dictionary / no callback (`opaque_dict_ == nullptr || group_delim_fn_ == nullptr`, the guard `group()` already uses at `:528`) ⇒ today's `entries_[first].tag`, behaviour-preserving; dictionary present ⇒ **no wire fallback at all** (C-3.4's load-time invariant is the sole guarantee; a zero return means "not a group", which callers already handle as absent).
- [~] T059 [US5] **C-8.5** — make boundary detection **skip nested extents** in `src/wire/offset_table.cpp` `group_slices_status`: a candidate boundary inside a nested group's extent is not a boundary; advance past a nested count via the same `consume_group_extent` recursion the extent walk already uses, **before** testing the outer-delimiter condition. This is the **first** of L-063-4's two recommended legs and operates strictly inside the bound T022 now computes correctly (`:648` ← `:550`). Makes T055's W-9 green on both fixtures. It is **not** a discharge of #180 (T071).
- [X] T060 [US5] **Assess and record C-8.0a** in `research.md` (D-6): the reserve estimator at `src/wire/offset_table.cpp:597` sizes a reservation in the **fixed 16 KiB inbound arena** whose under-reserve failure mode is a *documented silent truncation* (`spec/behaviors-and-limitations.md` L-073-1 top-level, L-065-2 nested), and this feature changes the member sets it consumes. Record the assessment with evidence — "low, and here is why" — rather than assuming it benign. `:597` keeps its wire-derived membership-probe role (C-8.0).
- [X] T061 [US5] Record the **FR-021c four-site census outcome** in `research.md` (D-6) and confirm it against the shipped tree: `:656` is the only site whose delimiter **SOURCE** changes (T058); `:454`'s **traversal** is repaired (T022) while its wire-derived local and `:457`'s probe stay wire-derived; `:597` is a membership probe alone; **`:526` / `:559-570` remain untouched**, leaving **one** flat wire-derived instance-boundary rule at **`:561`** as the named residual for whoever takes #180.
- [ ] T062 [US5] **PHASE-4 EXIT GATE** — record in `.specify/decisions/083-group-delimiter-resolution-verify.md`: member-set exactness falls out of T006's pin (48 → 0) rather than as a second assertion (FR-015); **W-11 / W-11a / W-11b / W-12 / W-13** green; **W-8 (recorded NEGATIVE result) / W-9 (fixture (c) only) / W-10 (both exclusions) / W-10a (all four legs, re-run here)** green; the extent bound **CHANGES** on a mode-(c) shape and spans all `declared`, agreeing with validation, with the one-instance form still reporting one and the depth-capped form returning `err_group_too_large` **and returning immediately**. Run `ctest -L wire`, `-L capi`, `-L dictionary`, **`-L codegen`**.

> **AMENDMENT 2026-08-01 (T054's measured outcome — C-8.5 / T059 DESCOPED).** W-8's characterisation came back a **negative result on all ten dictionaries**: the mis-split precondition (an outer group's delimiter tag also appearing inside one of its own nested groups) occurs **zero** times, and the shape is not merely absent but **unparseable** — a tag that both opens outer instances and appears inside them has no unique parse, which is *why* no shipped dictionary carries one. Implemented literally, C-8.5 would also **break** the population that IS reachable: in FR-021 mode (c) the outer delimiter IS a nested group's count tag, so skipping past the nested extent before testing the boundary would skip the very tag that opens each instance. Consequences, applied above and below rather than left implicit:
> - **T059** is `[~]` (descoped with the reason and the shapes tried recorded in `tests/wire/typed_read_split_agreement_test.cpp`, the W-8/W-9 block).
> - **W-9 fixture (b)** is deliberately not implemented; its slot is discharged by that recorded negative result (FR-021a's own escape clause). **W-9 fixture (c)** is delivered and green.
> - **T062** records "W-9 (fixture (c) only)", never "both fixtures green".
> - **T067** branch (5) is re-pointed — see its own amendment note.
> - **T071** leg (a) is **NOT** done; **#180 keeps BOTH legs open.**

**Checkpoint**: every delimiter path in the engine — validation, extent walk, splitter, C-ABI construction — applies the same rule.

---

## Phase 10: Polish & cross-cutting concerns (plan.md Phase 5 — Evidence + close-out)

- [X] T063 [P] **Bench 1 / FR-022 path 1 (inbound validate)** — add `bench/wire/validate_group_bench.cpp` (registered in `bench/wire/CMakeLists.txt`): delimiter resolution is called per offset-table entry (`include/fixpp/wire/validator.hpp:276`, `:376`). `[const §VIII.3]` — the benchmark ships **in the same PR** as the fix; a narrative assertion that the budget was met is not acceptable in its place.
- [X] T064 [P] **Bench 2 / FR-022 path 2 (typed read)** — add `bench/wire/typed_read_group_bench.cpp`: the ctx-keyed delimiter callback (C-8.1) plus the nested-extent recursion (C-8.5) plus C-8.0c's extra `group_member_fn_` evaluation **per group instance**, all on `group_slices_status`. **The bench MUST include a mode-(c) shape** (delimiter IS a nested group's count tag) — without it it measures the path that never descends and reports a budget met over the cheap branch.
- [X] T065 [P] **Bench 3 / FR-022 path 3 (C-ABI commit)** — add `bench/capi/capi_commit_group_bench.cpp` plus a NEW `bench/capi/CMakeLists.txt` and `add_subdirectory(capi)` in `bench/CMakeLists.txt`: the context-keyed lookup and path maintenance through `validate_group_grammar`'s `:725` recursion, executed per group instance on every `fixpp_msg_commit`, **plus** the one config-time `as_table_view()` per session measured as **session-open cost, not per-message**.
- [X] T066 Update the recorded baselines under `bench/baselines/wire/` (e.g. `bench/baselines/wire/validate_group_bench.json`, `bench/baselines/wire/typed_read_group_bench.json`) and the new `bench/baselines/capi/capi_commit_group_bench.json` with rationale for any intentional change, and record that all **three** paths stay within the ±5% standing budget (SC-009, `[const §VIII.2]`, `[const §VIII.3]`).
- [X] T067 **Article IX §1 coverage** — measure lcov **DA/BRDA** ≥ 95% line / ≥ 85% branch on the touched modules (`src/dictionary/`, `include/fixpp/dict/`, `include/fixpp/wire/`, `src/wire/`, `src/capi/`) and record, in `.specify/decisions/083-group-delimiter-resolution-verify.md`, that each of the **eleven** enumerated new branches has its named witness landed: (1) FR-006 fail-closed rejection → W-4; (2) FR-006a tolerant skip → W-5; (3) `consume_group`'s descent-taken vs not-taken → W-1 **and** W-1a; (4) FR-023's completeness invariant → `ContextWithoutDelimiterRecordRejectedAtFinalize`; (5) *(**AMENDED 2026-08-01** — the row previously read "the splitter's nested-extent skip → W-8/W-9". C-8.5 is DESCOPED per T054's measured negative result, so **there is no such branch in the delivered code** and a witness cannot be named for it without inventing one. The eleven-branch map is therefore **ten landed branches plus this one explicitly retired**, recorded as retired rather than silently renumbered — an Article IX §1 map that carries a branch with no code is the false-green shape T079 exists to catch.)* the splitter's **dictionary-sourced boundary delimiter** (C-8.2 / T058) is the branch that DID land at this site → **W-10** (`OutOfScopeWireProbesUnchanged`, whose in-band pre-083 oracle exercises both the callback-present and callback-absent arms of C-8.4's two-case fallback) **and W-9 fixture (c)**; (6) C-6.1a/C-3.6's discriminator → `DeclaredGroupWithZeroContextsDoesNotFailClosed`; (7) FR-006c's per-loader throw type → `OrchestraRejectionIsOrchestraParseError`; (8) FR-023a's tolerant twin → `ContextWithoutDelimiterRecordTolerantModeSkipsGroup`; (9) C-3.4a's `!members.empty()` exclusion → `ScalarReuseOfGroupTagIsNotACompletenessViolation`; (10) the C-ABI dictionary-present vs dictionary-absent split → W-11 and W-11b; (11) C-8.0c's descend-taken vs not-taken plus its `overflow` early-return → W-10a legs 1–3 and leg 4, with W-10's fixture covering not-taken. No branch left without a witness.
- [X] T068 **W-14 — ABI hygiene / FR-018a**: confirm `tests/capi/abi_symbol_golden_test.cpp` (`capi_pure_tests` `AbiSymbolGolden.CabiSymbolSetUnchanged` / `AbiSymbolGolden.ErrorEnumUnchanged`) shows **no delta** — the `nm` exported-symbol golden is this repo's current ABI gate (libabigail retired 2026-06-22). No `fixpp_*` declaration, enum value, struct layout or `FIXPP_C_ABI_VERSION_*` constant moves; the 1.5.0 freeze holds byte-for-byte (C-9.3). Record as Phase-5 evidence, not as a new test.
- [X] T069 **FR-019 / FR-019a / FR-019b / C-9.6 — enumerate the disclosed behaviour change**: *(FR-019a and FR-019b added to this label 2026-07-31 — they were created at the checklist audit and this task's body already discharged both, but neither was cited by ID, which is exactly the unmapped-requirement shape T079's completeness audit and `/gate-b` pre-flight 4d exist to catch.)* add operator-facing rows to `spec/behaviors-and-limitations.md` (this repo's release-note artifact — there is no `CHANGELOG.md`, per FR-019) covering **all THREE classes of FR-019a** — **not** the five cause-2 groups alone. *(Scope corrected 2026-07-31 at the Gate A checklist audit; the previous wording said "the five groups whose delimiter moves".)* **(a)** the five cause-2 groups (`1677`, `1772`, `40204`, `41599`, `42060`) with old and new opening tag each; **(b)** the cause-1 tags (`73`, `124`, `146`, `268`, `295`, `420`, `2428`, `2474`, +`1677`/`1772` on Orchestra) enumerated **by count tag × affected message type** with old and new opening tag — their *global* value does not move, so they are invisible to an enumeration written against the global lookup, yet they are accepted-today-rejected-after exactly as (a); **(c)** the three unregistered groups (`1499`, `1669`, `1919`) + six nested children (`1529`, `1534`, `1540`, `1559`, `1918`, `1920`), which `group_begin` rejects outright today (`src/capi/message_write.cpp:812`, `:923`) and which become **buildable** — a widening, disclosed as such. So an integrator can diff their builder against a list rather than against prose. C-9.7: (a) and (b)'s newly-rejected construction orders are not schema-valid under declaration order, so SC-007 is not violated — and this enumeration is what lets the SC-007 audit tell an intended new rejection from a regression, which it can only do for classes it actually lists. (c) is a widening and is not an SC-007 exception. **No new witness is required** — W-11 already runs on a divergent context and pins class (b); W-12 pins class (a).
- [X] T070 **Retire L-063-3** in `spec/behaviors-and-limitations.md:1699` — residual (b), the global first-seen delimiter (FIX44 `NoMDEntries(268)`: 269 in `MDFullGrp` vs 279 in `MDIncGrp`), closed by FR-001.
- [X] T071 **Re-state L-063-4** (`spec/behaviors-and-limitations.md:1701`, `spec/coverage-index.md:995-999`, issue **#180**) with **THREE** dispositions, not two: (a) leg 1 *"make the splitter nesting-aware"* — *(**AMENDED 2026-08-01**: previously "**DONE** (T059)".)* **NOT DONE — DESCOPED with evidence.** T054's W-8 characterisation measured the target population at **zero across all ten dictionaries** and established the shape is unparseable; implemented literally it would also break FR-021 mode (c). Record it as descoped-with-reason, never as done; (b) leg 2 *"fold the redundant flat cap loop into the same traversal"* (`src/wire/offset_table.cpp`, planning `:559-570` → **shipped `:584-594`**) — **OUTSTANDING**; the residual is the one flat, wire-derived instance-boundary rule at planning `:561` → **shipped `:586`** (T061). **Both legs open ⇒ #180 STAYS OPEN**, and this feature is **NOT** recorded as closing it. What this feature DID land at the splitter is a different change — the delimiter's SOURCE (C-8.2), which L-063-4 never asked for and which the row should now name. (c) the row's own claim that `consume_group_extent()` correctly computes the nesting-aware `group_end` — **CORRECTED**: false for the delimiter-is-a-nested-count shape the row never audited and this feature makes reachable in 262 contexts, repaired by C-8.0c / FR-021e. Also re-derive the row's *"0 collisions"* measurement on the post-fix basis from T044 (FR-012a / SC-014).
- [X] T072 **L-063-1 / L-066-1 (issue #196)** in `spec/behaviors-and-limitations.md:1695` — amend **iff** T013 landed on **branch (b)** (the FIX40/41/42 `INT`-typed `NumInGroup` "group-blind" shorthand is narrower than stated); on branch (a), record T014's amendment instead. Either way, state the outcome rather than leaving the row as-is by default.
- [X] T073 **082 handoff line** — add ONE line to `spec/behaviors-and-limitations.md` naming **both** items with their exact content: (i) `082-structural-group-detection`'s **T017 / T021b** pins, currently narrowed to tolerate the injected delimiter, can be flipped to **plain equality** once this feature lands; (ii) 082's `implementation-notes.md` PARKED note *"5 of its 14 contexts fail"* becomes **2**. Add D-12's outcome to the same handoff if it landed on branch (b). **The 082-side edits are deliberately NOT scheduled as tasks** — 082 is parked on an unbuildable branch (research D-8), so such a task could only be marked done by inspection, which is the false-green close-out shape. Repeat the handoff in the phase-4 close-out record when the pipeline's MARK DONE step creates `research/G19-fix-fpml-iso20022/phases/phase-4/<module>/083-group-delimiter-resolution.md`.
- [X] T074 **FR-020 / FR-019b — interop-gate observation, OBSERVATIONAL ONLY**: record in `spec/behaviors-and-limitations.md` (this repo's release-note artifact — there is no `CHANGELOG.md`, per FR-019) that declaration order (FR-002) is **authoritative and unconditional**, that any external reference engine's divergent delimiter is recorded as a behaviour/limitation row and **never** alters resolution, and that **no** compatibility mode, per-session configuration surface or conditional branch on the inbound validation path is introduced (FR-020a). The reference engines are absent from the working copy; record that state rather than deferring the requirement.
- [ ] T075 **Close fixpp#210 and fixpp#208** (`gh issue close`, with the closure evidence also recorded in `.specify/decisions/083-group-delimiter-resolution-verify.md`), naming the landed witnesses: #210 → FR-001 (T027/T030/T031) with Consequence 1 = `PollutedContextAcceptsInjectedTagTodayRejectsAfter` and Consequence 2 = `TrailingMessageFieldNotSwallowedIntoLastInstance` (T009/T033); #208 → FR-003/FR-006 (T027, T036) and FR-007 (T017). **#180 is NOT closed** (T071).
- [ ] T076 Run **every** `quickstart.md` scenario end to end (§0 baseline, §1 + §1a, §2 + §2a, §3, §4's per-phase table, §6 "what done looks like") on `build/linux-clang-debug` with `-j2`, then run `ctest -L dictionary`, `ctest -L wire`, `ctest -L capi` and **`ctest -L codegen` (mandatory)**. Record the results before handing to `/simplify` and `/speckit-verify`.
- [X] T077 **PHASE-5 EXIT GATE** — record in `.specify/decisions/083-group-delimiter-resolution-verify.md` that Article VIII / Article IX evidence is complete for Gate B (three benches + baselines T063–T066; coverage + eleven-branch witness map T067; ABI hygiene T068; fuzz harnesses T018/T039), and that **the 082 handoff line exists on this branch and names both items** (T073). The 082-side edits are explicitly not attempted here.

### Mandatory close-out tasks (ALWAYS emit — Gate-B preconditions, `[const §XVII.8]`)

- [X] T078 [P] **Catalogue close-out** — flip every feature-owned OFFICIAL row in `spec/feature-catalogue.md` to `done` with its PR / evidence ref, and add or update the matching `spec/coverage-index.md` entry. *(Scope note per `plan.md`'s Article VI row: this feature introduces **no new OFFICIAL catalogue row** — every requirement is correctness of existing rows **W-001 / W-006 / W-007 / W-014 / D-010**, so the work is amending those rows' behaviour records and their `coverage-index.md:181` / `:184` / `:189` / `:995-999` entries, plus the B&L updates T069–T074 — not adding a row. State that disposition explicitly in the close-out rather than leaving the task looking skipped.)*
- [ ] T079 **Feature-completeness audit (MUST be the FINAL task)** — assert against the merged tree that: (i) every `tasks.md` row above is `[X]` or carries an explicit waiver rationale; (ii) every **FR-001..FR-023b** and every **SC-001..SC-016** maps to a **landed test AND a landed implementation** (including the negative-result escapes: FR-007a and FR-021a may be discharged by a recorded negative result with the shapes tried, never by silence); (iii) every feature-owned OFFICIAL catalogue row is `done` with a matching `coverage-index.md` entry (T078); (iv) the eleven Article IX §1 branches each have a landed witness (T067); (v) the three preconditions C-7.1 / C-7.2 / C-7.3 and D-12's Phase-1 resolution were each run **before** the thing they guard, with their records present. Record the verdict (**100% or fully-waived**) in `.specify/decisions/083-group-delimiter-resolution-verify.md` under a `## Completeness` section (or a sibling `.specify/decisions/083-group-delimiter-resolution-completeness.md`). This is the hard `/gate-b` precondition (`[const §XVII.8]` / pre-flight 4d).

---

## Dependencies & Execution Order

### SC-007 is discharged by distribution — named here so it has a home

SC-007 ("no schema-valid message accepted before this change is rejected after it, except the disclosed C-ABI construction-order change") has **no single owning task by design**: it is a whole-feature non-regression property, not a discrete activity. It is discharged by the union of **T009** (named-subset accepts), **T024** (per-instance cap-breach assessment — a breach would be a *new* rejection), **T033** (previously-accepted shapes still accepted), **T048** (FIX40/41/FIXT11 unchanged), and **T069** (enumeration of **FR-019a classes (a)+(b)** — the two SC-007-exception classes: the five groups whose global value moves, *and* the cause-1 tags whose lookup becomes context-sensitive in divergent contexts — which is what lets the audit distinguish an intended new rejection from a regression; class (c) is a widening and is not an SC-007 exception). *(Scope corrected 2026-07-31 at the `/speckit-analyze` re-run, F3: this paragraph exists so SC-007 has a readable home, and it still said "the five disclosed groups" after the checklist audit widened the exception — the one place an SC-007 auditor looks first.)*

**T077** (Phase-4 exit gate) and **T079** (completeness audit) MUST each confirm this set is green as a body before signing off; an auditor grepping for "the SC-007 task" will otherwise find none and read it as skipped. This mirrors the round-1 Gate A fix that pulled SC-004's wire leg into a named phase-exit leg.

### Story completion order (correctness-constrained, NOT priority-ordered)

```
Setup (P1) → Foundational RED (P2, BLOCKING)
   → US2  (receiver, P1)                    ← MUST precede the loader recursion
   → US5 leg 1 (extent walk, C-8.0c)        ← W-10a required green at the Phase-3 exit
   → US1 leg 1 (loaders + as_table_view, P1)
   → US3  (load dispositions + count, P2)   → PHASE-3 EXIT GATE (T045)
   → US4  (pin non-circularity + red-on-reintroduction, P2)
   → US1 leg 2 (C-ABI, P1)
   → US5 leg 2 (splitter, P3)               → PHASE-4 EXIT GATE (T062)
   → Polish (evidence, B&L, close-out)      → PHASE-5 EXIT GATE (T077)
```

### Phase dependencies

- **Setup (T001–T004)**: no dependencies.
- **Foundational (T005–T015)**: depends on Setup; **BLOCKS every story**. T015 is a hard gate — no implementation task starts before it.
- **US2 (T016–T019)**: after T015. Touches `include/fixpp/wire/validator.hpp` only.
- **US5 leg 1 (T020–T024)**: after T015 (T011 authored there); **not blocked by** the loaders — C-8.0c's delimiter is wire-derived and needs no per-context record. Must land **before** T045.
- **US1 leg 1 (T025–T033)**: after **T019** (the ordering constraint) and after **T014** (D-12's branch-conditional amendment, which may change D-1/D-3).
- **US3 (T034–T045)**: after US1 leg 1. **T034 (C-7.1) strictly before T036**; **T040 (C-7.3) strictly before T041**; T035/T042 RED before T036/T037/T041.
- **US4 (T046–T048)**: after T045 (the pin must be green before it is proven red on reintroduction).
- **US1 leg 2 (T049–T053)**: after US1 leg 1 (needs Entity 2 populated and the projection repopulated). T049 RED before T050–T052.
- **US5 leg 2 (T054–T062)**: after US1 leg 1 (**C-8.6** — before the loaders, the dictionary-sourced delimiter would be the global first-seen one and the splitter would be *less* correct) and after T022 (**C-8.5 operates strictly inside the bound C-8.0c computes**).
- **Polish (T063–T079)**: after all stories. **T078 then T079; T079 is the FINAL task.**

### Within each story

- Tests are written and **observed failing with counts recorded** before implementation — except **W-10a leg 4** (T021), which *cannot* be RED pre-fix (the branch it discriminates does not exist yet); its control is T023's mirror-deletion demonstration.
- Preconditions run **before** the path they guard: T034 → T036, T040 → T041, T013/T014 → T026–T030, T008 → T012.
- Both loaders move in the **same pass** (T027 ‖ T030 in review, not in file order) — a one-loader fix is a half-restructure.

### `[P]` collisions to avoid

- `src/dictionary/xml_loader.cpp` — T026/T027/T028/T029 all touch it; **never `[P]` together**.
- `src/wire/offset_table.cpp` — T022 (`:475` descent) and T058/T059 (`:656` splitter) are in **different phases**; never `[P]` together.
- The five multi-case test files (`delimiter_census_test.cpp`, `loader_disposition_test.cpp`, `consume_group_nested_delim_test.cpp`, `typed_read_split_agreement_test.cpp`, `capi_group_delimiter_ctx_test.cpp`) each receive cases from several tasks; tasks touching the same file are not `[P]`.

---

## Parallel execution examples

**Foundational**

```bash
# after T005–T008 land, the three RED wire/dict witnesses are independent files:
Task: "T009 — delimiter_divergence_wire_test.cpp RED (FR-001 premise + #210 C1/C2 + SC-004 subset)"
Task: "T010 — consume_group_nested_delim_test.cpp RED (W-1 + W-1a)"
Task: "T011 — typed_read_split_agreement_test.cpp RED (W-10a leg 1)"
```

**US2**

```bash
Task: "T018 — nested-delimiter corpus seed in tests/fuzz/fuzz_wire_validator.cpp"
# ‖ with T016/T017's review pass — different file, no shared symbol
```

**US5 leg 1**: none — T020/T021 share `typed_read_split_agreement_test.cpp`, and T022/T023/T024 are strictly sequential.

**US1 leg 1**: none inside the loader chain (T026–T029 are one file). T030 (`orchestra_loader.cpp`) is a different file but MUST be reviewed as one pass with T027.

**US3**

```bash
Task: "T038 — orchestra_loader.hpp:33-37 documented exception list"
Task: "T039 — fuzz_dict_xml_loader.cpp / fuzz_orchestra_loader.cpp exception-set comments"
```

**US5 leg 2**: T054 (`nested_group_extent_test.cpp`) ‖ T055/T056 (`typed_read_split_agreement_test.cpp`) — different files.

**Polish**

```bash
Task: "T063 — bench/wire/validate_group_bench.cpp"
Task: "T064 — bench/wire/typed_read_group_bench.cpp (MUST include a mode-(c) shape)"
Task: "T065 — bench/capi/capi_commit_group_bench.cpp (+ new bench/capi/CMakeLists.txt)"
```

---

## Implementation Strategy

### Minimum correct increment (this feature has no shippable MVP short of the whole PR)

**Intermediate phase exits are NOT releasable states** (`plan.md`; C-8.0c.5) — the merge unit is this feature's single PR, and Gate B and all three tier matrices run on the PR head. So "MVP" here means **minimum correct increment**, not a demo milestone.

**Minimum correct increment = Setup + Foundational + US2 + US5 leg 1 + US1 leg 1 + US3, i.e. through the PHASE-3 EXIT GATE (T045).**

That is the smallest tree state in which the feature's own coupling is resolved: FR-007 has landed before FR-003 (so the 232 + 30 mode-(c) contexts are not converted into false rejections), C-8.0c has landed with it (so validation accepting is not paired with the read path truncating), the loaders resolve per context, the three dropped groups register, and the all-ten pin is green with no carve-out. Stopping anywhere earlier leaves the tree in a state the ordering constraint explicitly forbids.

### Incremental delivery after that

1. **US4** — prove the pin red on reintroduction; the guard against recurrence costs one task and gates nothing else.
2. **US1 leg 2 (C-ABI)** — closes the last path applying a different rule to the same message; carries the **two** disclosed-regression classes (FR-019a (a)+(b)) plus the class-(c) widening, so it ships with its enumeration (T069). *(Corrected 2026-07-31, `/speckit-analyze` re-run F4 — was "the one disclosed regression (five groups)".)*
3. **US5 leg 2 (splitter)** — the P3 story; the last delimiter rule in the engine.
4. **Polish** — three benches in the same PR (`[const §VIII.3]`), the B&L/release-note disclosure set, the 082 handoff, then the two mandatory close-out tasks.

### Parallel team strategy

Genuine parallelism is limited by the ordering constraint, not by staffing. The safe splits are: Foundational's three RED witness files (T009/T010/T011); US3's documentation edits (T038/T039); US5 leg 2's two test files (T054 vs T055/T056); and Polish's three benches (T063/T064/T065). Everything else is chained by a phase gate or a shared file.

---

## Notes

- `[P]` = different file, no incomplete dependency. `[Story]` maps a task to its user story for traceability.
- Verify tests fail — **with counts** — before implementing. Reconciliation, not mere recording, is the Phase-1 exit criterion (SC-015).
- Commit after each task or logical group. Stop at any **gate** task to validate before proceeding; the gates, not the checkpoints, are the enforcement points.
- Things a reader will otherwise get wrong, collected: FR-011's false sentence is `dictionary.cpp:507-508` **not** `:503-505`; `table_view.hpp:645`'s member injection is **retained** and pinned as a no-op; the C-ABI predicate sites `:157`/`:812`/`:923` are **not** converted; `:526`/`:559-570` stays untouched and **#180 stays open** with the `:561` residual; records are emitted only from `xml_loader.cpp:927-931`, never from `:968` or `:1056`; `FieldRef` stays 16 bytes; SC-008's unchanged set is FIX40/FIX41/FIXT11 and **not** FIX42.
