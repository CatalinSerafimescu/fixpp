# Read-Path-Consistency Requirements Checklist: Group Delimiter Resolution

**Purpose**: Unit-tests-for-English on the requirements governing the read path — the offset table's wire-derived delimiter census, the typed-read instance splitter, the extent walk's descend-at-delimiter repair, and the witnesses that pin validation/read agreement. Tests whether these requirements are complete, clear, consistent, and measurable — NOT whether the code works.
**Created**: 2026-07-31
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md) · [research.md](../research.md) · [data-model.md](../data-model.md)
**Contracts in scope**: `contracts/typed_read_splitter.md` (owns `src/wire/offset_table.cpp`) · `contracts/consume_group.md` C-4.4
**Audience**: Gate A/B reviewer (PR) · **Depth**: Standard

> **Item derivation.** This is the surface that **exhausted Gate A loop 1**. The amendment cause — a site scoped out on a stated ground (*"its instance-boundary rule is not defective"*) that was **false at source** — plus **RC2** (a pin whose passing condition was that the defect is still present) and **RC3** (a `/clarify`-widened surface with no design depth at its centre) are what these items are pointed at. A second-order failure is in scope too: the amendment's own justification was wrong (a spin claim transplanted from a loop with no instance bound), which is why several items separate a requirement from its justification.

## Census of the Repeated Idiom, and the Grounds for Each Scope-Out

- [x] CHK001 Is **every** wire-derived delimiter derivation in the file censused with its **role(s)**, rather than only the site whose behaviour changes? [Completeness, Spec §FR-021c] — PASS: FR-021c censuses all four sites by line (`:454`, `:526`, `:597`, `:656`) with role(s) named for each (`:454`/`:526` carry two roles — membership probe + instance-boundary rule; `:597` is probe-only; `:656` is the boundary-rule source that changes); the contract's own table (`typed_read_splitter.md` lines 42-47) restates the same four rows verbatim-consistent with FR-021c and with `data-model.md` Entity 6's third table. Verified at source: all four line citations resolve exactly (`src/wire/offset_table.cpp:454,526,597,656`).
- [x] CHK002 Does each scope-out carry a stated ground that a reviewer can **check at source**, rather than an unfalsifiable assertion that the site is not defective? [Clarity, Spec §FR-021c / typed_read_splitter.md C-8.0b] — PASS: the surviving scope-out (`:526`) carries a checkable ground — "its only consumer is a `max_group_entries_per_instance` check `consume_group_extent` already applies over correct boundaries... exposure is a false positive/negative on a defense-in-depth DoS cap, never wrong returned data" — verified at source (`offset_table.cpp:496-499` is the cap check `consume_group_extent` applies; `:559-570` is the flat cap loop it feeds). The one scope-out that *was* an unfalsifiable-turned-false assertion (`:454`, "already nesting-aware... not defective") is explicitly withdrawn (struck through, reasoning preserved) rather than left as a silent gap, and replaced by C-8.0c bringing the site in scope — which is itself the record that C-8.0b's discipline (checkable grounds only) was enforced against its own prior failure.
- [x] CHK003 Is the site whose delimiter **source** changes distinguished from the sites whose **membership-probe** role must stay wire-derived, with the reason re-pointing the probe would collapse it into a tautology? [Clarity, Spec §FR-021c / C-8.0] — PASS: FR-021c states "`:656` is the only site whose delimiter SOURCE changes — the membership-probe role at all three other sites MUST remain wire-derived, since re-pointing that role at the dictionary would collapse it into a tautology and silently delete three real checks"; C-8.0 restates the tautology reason with the added note that C-8.0c (a **traversal** change) does not disturb it — `:454`'s wire-derived local and probe stay wire-derived after the amendment.
- [x] CHK004 Is the residual left in the file after this feature recorded by line, with an explicit statement that the referenced issue is **not** closed and which of its legs remains? [Completeness, Spec §FR-021c / §Normative References L-063-4] — PASS: FR-021c names the residual by line ("one flat, wire-derived instance-boundary rule left in the file, at `:561`"), states "this feature MUST NOT be described as discharging #180, whose recommended fix has a second leg (folding the flat cap loop into the same traversal) that this scoping still excludes" — naming the remaining leg precisely. The Normative References L-063-4 row and `data-model.md` Entity 6's producer table (`:561` row, "out of scope (C-8.0b); feeds a DoS cap only") repeat the same disposition. This is a deliberately-open scope decision (not a completeness gap in the requirement text, which fully states it) — the underlying "why leave it open" rationale is corroborated at `typed_read_splitter.md` C-8.0b (residual table + severity bound), cited here as supporting evidence rather than as the sole authority, since spec.md itself already carries the full statement.
- [x] CHK005 Is the effect of this feature's changed member sets on the arena **reserve estimator** required to be assessed and recorded, rather than assumed benign — given its documented failure mode is silent truncation? [Completeness, Spec §FR-021c / C-8.0a] — PASS: FR-021c states "`:597` sizes a reservation in the fixed 16 KiB inbound arena whose under-reserve failure mode is a documented silent truncation (L-073-1, L-065-2 for the nested twin)... the effect of the changed values on it MUST be assessed and recorded, not assumed benign." C-8.0a delivers the assessment ("risk is low, because the tag removed from a polluted member set is the global first-seen delimiter, which by definition is not what a message in that context puts on the wire... Recording 'low, and here is why' is the deliverable; a bare assumption is not"). Verified at source: `offset_table.cpp:597` (`entries_[e+1U].tag`) is exactly the reserve-estimator probe FR-021c/C-8.0a describe.

## The Splitter's Mechanism (RC3 — depth, not outcome)

- [x] CHK006 Is the splitter's route to the per-context delimiter named as a **mechanism** — which seam it reuses, and why no new parameter crosses the public read API — rather than stated as an outcome? [Completeness, Spec §FR-021 / C-8.1] — PASS: C-8.1 names the mechanism precisely — a sibling callback `group_delim_fn_t` of the same shape as the existing `group_member_fn_t` (`include/fixpp/wire/offset_table.hpp:79-80`), resolving through the same `void const* opaque_dict` — not merely "the splitter now reads the dictionary." Verified at source: `offset_table.hpp:79-80` declares exactly `group_member_fn_t` as `bool (*)(void const*, group_context const&, std::uint16_t, std::uint16_t) noexcept`, the shape C-8.1 describes as reused.
- [x] CHK007 Is the **key** the splitter resolves with specified precisely enough to distinguish it from the adjacent accessor that would produce a path one element too long? [Clarity, data-model.md Entity 2 / C-8.2] — PASS: C-8.2 names the key as `(msg_type, stored_group_context().parent_path, no_tag)` and explicitly rules out `group_context_for(no_tag)` as "the wrong value... `pushed()` appends `no_tag` to `parent_path`... one element too long, and a violation of Entity 1's 'parent_path excludes no_tag' invariant." `data-model.md` Entity 6 restates the same distinction verbatim. Verified at source: `offset_table.cpp:424-426` (`group_context_for`) does call `.pushed(group_no_tag)`, and `group_view.hpp:56-62`'s `pushed()` appends — confirming the off-by-one the clause warns against is real.
- [x] CHK008 Is the requirement to supply the new callback at **every** construction site stated, given that a missed site silently takes the dictionary-free fallback on nested splits? [Completeness, C-8.1 / tasks §T057] — PASS: T057 enumerates all three sites that need the sibling callback threaded — the two dict-aware constructor overloads (`offset_table.hpp:119-121`, `:126-128`) and the one call site that actually invokes one of them with a live `opaque_dict` (`build_nested_subview`, `offset_table.cpp:718-720`) — and names the failure mode explicitly ("a missed site silently takes C-8.4's dict-free fallback on nested splits"). Verified by grep: no other `OffsetTable(...)` construction call site exists outside `offset_table.hpp`/`.cpp`, so T057's enumeration is complete, not merely illustrative.
- [x] CHK009 Is the splitter's fallback disposition reduced to cases the splitter can **actually observe**, rather than including a state it cannot distinguish? [Measurability, C-8.4] — PASS: C-8.4 explicitly deleted the round-1 second disjunct ("or the context does not resolve") with the stated reason "the splitter *cannot observe* the state it branched on" — `table_view::group_first_field` has already fallen through to the bare global before the splitter sees a value and returns a bare scalar with no miss-discriminating second span (unlike `group_member_tags`). The table now states exactly two cases, both independently testable via the existing `opaque_dict_ == nullptr || group_delim_fn_ == nullptr` guard `group()` already uses at `:528`. Verified at source: `offset_table.cpp:528` (`opaque_dict_ != nullptr && group_member_fn_ != nullptr`) is the guard shape C-8.4 cites as precedent.
- [x] CHK010 Is the requirement that public read signatures do not change stated alongside the internal seam widening, so the two are not read as in tension? [Consistency, C-8.3] — PASS: C-8.3 sits in the same "lookup surface" section as C-8.1/C-8.2, states "`group_slices(no_tag)` and `group_slices_status(no_tag)` keep their signatures... every top-level caller... compiles and behaves identically apart from the corrected boundary. No C ABI symbol is involved" — placed directly adjacent to the internal-seam-widening clauses rather than in a separate section where the two could be read as contradictory.

## Characterisation Before Fix

- [x] CHK011 Is the characterisation obligation stated **symmetrically** — a reproduction of the mis-split **or** recorded evidence that none is reachable — so an unverified note cannot be carried forward a second time? [Completeness, Spec §FR-021a] — PASS: FR-021a's text is directly symmetric — "a reproduction demonstrating a mis-split, or evidence that none is reachable. Whichever it is MUST be recorded, so the conclusion is not carried forward as an assumption the way it was in fixpp#208" — the phrasing names the specific prior failure mode (fixpp#208's unverified note) it is written to prevent. This item asks whether the requirement's *form* is well-posed, not what the measurement outcome will be (that outcome is a Phase-1 task, not a spec-time fact) — the form is verifiably symmetric as written, so this is a direct PASS on spec.md text rather than a design-doc trace.
- [x] CHK012 Is the repository's existing record of this defect required to be **cited as the starting point** and simultaneously **not inherited uncritically**, with the specific claim inside it that is false named? [Consistency, Spec §FR-021d] — PASS: FR-021d requires L-063-4 be "cited by the design and updated at close-out," then states "L-063-4 MUST NOT be inherited uncritically" and names the specific false claim precisely — "the row also asserts that `consume_group_extent()` correctly computes the nesting-aware `group_end`, and that assertion is false for the delimiter-is-a-nested-group's-count-tag shape (FR-021e) — a shape L-063-4 never audited." Verified L-063-4 exists at `spec/behaviors-and-limitations.md:1701` and issue #180 is the cited tracker.
- [x] CHK013 Are the dispositions that record must carry at close-out enumerated, so a partially-discharged row is not re-stated as fully discharged? [Completeness, Spec §FR-021d / §Normative References] — **WAIVED: C-8.5 descoped 2026-08-01, T054 negative result (re-dispositioned 2026-08-02, Gate B r1 F2, fixpp#216 P1-2).** The original PASS rested on "splitter leg **done**, cap-loop leg outstanding, and the `consume_group_extent` correctness claim corrected" — but "splitter leg done" was false at source: C-8.5 (the splitter's nesting-aware boundary skip) was never implemented; `tasks.md` T054's characterisation measured its target population at **zero** on all ten dictionaries and T059 is `[~]` DESCOPED, not `[X]` done. `tasks.md` T071 now records the corrected three-way enumeration: (a) splitter leg **NOT done — DESCOPED with evidence**, (b) cap-loop leg **OUTSTANDING**, now tracked as fixpp#214, (c) the `consume_group_extent` correctness claim **CORRECTED** (repaired by C-8.0c). The item's *form* — three dispositions enumerated so a partial close-out cannot pass as complete — still holds, and `spec.md` FR-021d, the Normative References L-063-4 row, `plan.md`'s Phase-5 row and `contracts/typed_read_splitter.md`'s dated banner now all agree on this corrected enumeration rather than the original stale one, so this is a waiver on the original PASS's premise, not a fail on the item's form.

## The Extent-Walk Descent (the scope amendment)

- [x] CHK014 Is the descent stated as a requirement **separable from its justification**, so that correcting the justification does not reopen the scope decision? [Clarity, Spec §FR-021e] — PASS, and demonstrated rather than merely asserted: fresh loop round 1 corrected C-8.0c.3's failure-mode justification (spin → bounded DoS) at all eight loci while the MUST itself did not move — spec.md FR-021e's own correction note states this explicitly ("The requirement is unchanged; only its justification is"), and `plan.md`'s fresh-loop-round-1 disagreements section confirms "No instance-count cap was introduced... Keep the MUST — the requirement is unchanged, only its justification moves." The separability is not just claimed, it was exercised once already without reopening C-8.0c's scope.
- [x] CHK015 Is the failure mode of the un-repaired walk characterised by **what the loop actually does** — bounded no-op iterations under an attacker-controlled declared count — rather than by a claim transplanted from a differently-bounded sibling loop? [Measurability, Spec §FR-021e / C-8.0c.3] — PASS, verified at source. FR-021e / C-8.0c.3 state the corrected mechanism precisely: the outer loop is `while (k < entries_.size() && inst < declared && ...)` with an unconditional `++inst`, so an un-mirrored descent performs exactly `declared` no-op iterations, not a hang; `declared` saturates at `UINT32_MAX` via `parse_bounded_u32`, and no instance-count cap exists. Verified directly: `offset_table.cpp:473` (`inst < declared`), `:500` (unconditional `++inst`), `tag_scan.hpp:69-79` (`parse_bounded_u32` saturates at `0xFFFFFFFFU` on non-digit or overflow via `accumulate_bounded`). The sibling loop the claim was originally (wrongly) transplanted from — `validator.hpp:357`'s `while (i < end && ents[i].tag == delim_tag)` — is verified to carry no instance bound in its loop condition (the `declared_count` check is only post-loop at `:402`), confirming the asymmetry the correction describes is real.
- [x] CHK016 Is the descent required to **reuse** the walk's existing child context and existing depth guard rather than introduce a new mechanism, and is the overflow early-return's role in termination stated? [Completeness, Spec §FR-021e / C-8.0c.2 / C-8.0c.4] — PASS: C-8.0c.1 requires the "same `child`" already computed at `:466`; C-8.0c.2 requires the recursion be "bounded by the existing `kMaxGroupDepth` check at `:442-445`... No new recursion limit is introduced"; C-8.0c.3 states the overflow early-return's termination role in detail ("load-bearing for TERMINATION-IN-BOUNDED-TIME... a descent that swallows `overflow` at the delimiter position assigns a non-advancing `k`"). Verified at source: `offset_table.cpp:466` (`group_context const child = ctx.pushed(group_no_tag)`), `:442-444` (depth-cap branch returns `count_idx` unchanged), `offset_table.hpp:340` (`kMaxGroupDepth = 16`).
- [x] CHK017 Is the size of the population this repair affects stated with its two components (measured plus newly-registering), and does that figure agree wherever it appears? [Consistency, Spec §FR-021e / §SC-016] — PASS: 262 = 232 measured + 30 newly-registering, stated identically at FR-021e, SC-016, research.md D-4a, `typed_read_splitter.md` C-8.0b/C-8.0c/W-9/W-10a, `data-model.md` Entity 6, and `quickstart.md` §2a — checked across all these loci and found numerically consistent everywhere (no instance of 262 stated without both components, none where the components sum to a different total).
- [x] CHK018 Is the per-instance entry cap's interaction with an extent opened at the delimiter required to be **assessed and recorded** rather than assumed, given a breach would be a new rejection? [Completeness, Spec §FR-021e / §SC-007] — PASS: FR-021e states "whether any shipped shape can breach that cap MUST be assessed and recorded rather than assumed, because a breach would be a new rejection under SC-007"; C-8.0c.4 delivers the assessment ("Assessment, inferred and to be confirmed by the FR-021e task, not asserted as measured: no shipped shape breaches it, because the cap is 4096 and no group instance in the ten dictionaries approaches 4096 entries. It is called out because a breach would be a new rejection"). Verified at source: `offset_table.hpp:28` (`default_max_group_entries_per_instance = 4096`), and `offset_table.cpp:496-499` is the cap check the assessment refers to.
- [x] CHK019 Is the claim that this repair is **not blocked by** the loader phase supported by a reason that survives the sibling clause added in the same amendment? [Consistency, C-8.6a / C-8.0c.5] — PASS, and this is precisely N27's finding, confirmed applied: the original reason ("the membership sets it probes are already context-exact from 063") was refuted by W-10's own exclusion 2 (added in the *same* round-3 amendment — the member sets *do* move at Phase 3 on the 52 polluted + 30 newly-registering contexts). Read at source in both loci: C-8.0c.5 now states "The round-3 reason... is withdrawn as false" and replaces it with "C-8.0c's delimiter is wire-derived... and the repair needs no per-context delimiter record, so nothing it requires is produced by the Phase-3 loader change" — a reason about delimiter *source*, not member-set exactness, and therefore not contradicted by W-10's exclusion 2 (which is about member sets). C-8.6a carries the identical correction. Confirmed both loci actually contain the corrected text, not the withdrawn one.

## Witness Integrity (RC2 — pins whose passing condition is the defect)

- [x] CHK020 Are the exclusions on the invariance pin required to be **asserted inside the case** rather than secured by fixture choice alone? [Measurability, typed_read_splitter.md W-10] — PASS: W-10 (re-stated) states "under **two** exclusions, both asserted inside the case rather than assumed — exactly as W-9 pins its own divergence... The case resolves the context delimiter and asserts it is not a registered group's count tag" (exclusion 1) and "The case therefore asserts that the fixture's member sets... are identical pre- and post-083" (exclusion 2) — both are in-case assertions, not fixture-only guarantees.
- [x] CHK021 Are **both** exclusions specified — the shape whose extent must change, and the polluted contexts whose extent moves because the member set the termination test consults stops being polluted? [Completeness, W-10] — PASS: exclusion 1 is exactly "the delimiter is NOT itself a nested group's count tag" (the mode-(c) shape C-8.0c repairs); exclusion 2 is exactly the polluted-context case, with the mechanism named precisely ("`consume_group_extent`'s instance-termination test... consults the per-context member set... the injected tag... arrives via `set_group_first_ctx`'s unconditional `add_group_member_ctx`... once the delimiter is correct the injection is a no-op... the tag leaves the set"), and widened (N28) to cover every nested child context the walk descends through, not only the fixture's own. Verified at source: `table_view.hpp:645` (`add_group_member_ctx` call inside `set_group_first_ctx`) and `offset_table.cpp:478` (the membership test `group_member_fn_(opaque_dict_, ctx, group_no_tag, t)` W-10's exclusion 2 reasons about).
- [x] CHK022 Is the agreement witness required to run on a shape **structurally capable** of detecting this defect, rather than on the shape the walk already handled correctly? [Coverage, W-9] — PASS: W-9 is explicitly required to run over **two** fixtures — (b) round 2's (delimiter tag reappearing at depth, the shape `:485-488`'s existing descent already handles) and (c) round 3's new addition (delimiter *is* a nested group's count tag, C-8.0c's actual subject) — with the contract stating plainly "A W-9 with only [fixture (b)] is structurally incapable of detecting the C-8.0c defect."
- [x] CHK023 Is the leg that **cannot be observed RED** given a discriminator other than a signal the implementation returns either way, with any timeout named as a backstop rather than the discriminator? [Measurability, W-10a leg 4] — PASS: leg 4 is explicit that the error code alone ("`overflow` is a `bool&`... reaches `group()`'s check either way") cannot discriminate, and specifies a `group_member_fn_` invocation-count differential (equal totals at `declared` = 2 vs `declared` = 8, at the depth-capped frame) as the actual discriminator, with "The per-test `TIMEOUT` stays — as a BACKSTOP, never as the discriminator." The contract additionally states plainly why leg 4 itself cannot be observed RED pre-fix ("the branch whose omission it discriminates does not exist yet") and that this is not a discipline exemption, distinguishing it from leg 1's RED-first requirement.
- [x] CHK024 Are the two distinct read-path shapes — a mis-split *inside* a correct extent, and an extent *truncated before the split runs* — required to be witnessed separately, with substitution of one for the other forbidden? [Consistency, Spec §US5 scenario 3 / §SC-016] — PASS, near-verbatim match: US5 scenario 3 states "This is a distinct shape from scenario 1, whose delimiter tag merely reappears at depth: scenario 1's mechanism is a mis-split inside a correct extent, this one's is an extent truncated before the split runs. Neither may be substituted for the other." The witness split mirrors this exactly — W-9 (mode b, mis-split) vs W-10a (mode c, truncated extent).
- [x] CHK025 Is the expected extent required to be **derived from the fixture's own entry layout** rather than captured from the implementation after the fix? [Measurability, Spec §SC-016] — PASS: SC-016 states "the expected extent MUST be derived from the fixture's own entry layout rather than captured from the implementation — a value captured after the fix pins whatever the fix did, not what it should do"; W-10a leg 1 restates identically ("the expected value is computed from the fixture's own entry layout... not captured from the implementation. A golden captured post-fix would enshrine whatever the fix does").
- [x] CHK026 Is the previously-passing single-instance case required to keep passing, stated as its own obligation rather than implied? [Completeness, Spec §SC-016 / §US2 scenario 2] — PASS: SC-016 states "The one-instance form MUST still report exactly one, since single-instance groups are why the defect was invisible" as its own sentence, distinct from the N>1 requirement; W-10a leg 2 restates it as its own numbered leg ("the one-instance form still yields exactly 1 — the regression guard, since single-instance groups are why this defect was invisible").

## Notes

- Check items off as `[x]` with exactly one inline disposition tag: `PASS` / `SPEC-FIXED` / `DD-DECIDED §X` / `WAIVED: <reason>`.
- Items tagged **Completeness / Clarity / Consistency** may **not** be closed as `WAIVED` (pipeline.md step 9).

## Audit Result

Executed as pipeline.md step 9 (checklist audit gate), 2026-07-31, ahead of `/speckit-implement`. This checklist is the surface that exhausted Gate A loop 1 (round 3 escalation → user scope amendment → two-round fresh loop, CONVERGED). Every item was checked directly against `spec.md`, `research.md` (D-4a, D-6), `contracts/typed_read_splitter.md` (C-8.0–C-8.6a, W-8/W-9/W-10/W-10a), `contracts/consume_group.md` (C-4.4), `data-model.md` Entity 6, `plan.md`'s Gate A record and Phase 3/4 gates, `tasks.md` T057/T058, and live source (`src/wire/offset_table.cpp`, `include/fixpp/wire/offset_table.hpp`, `include/fixpp/wire/tag_scan.hpp`, `include/fixpp/wire/validator.hpp`, `include/fixpp/dict/table_view.hpp`).

| Disposition | Count |
|---|---|
| PASS | 26 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 26 |

### SPEC-FIXED items

None. No genuine Completeness/Clarity/Consistency defect was found in the audited artifact set — every gap this checklist was written to probe (the false `:454` scope-out ground, RC2's defect-preserving pin, RC3's absent/mis-labelled/wrongly-reasoned contract centre, the transplanted spin claim, the "context-exact from 063" reason contradicted by its own sibling clause) was already closed by Gate A's round-3 amendment and its two-round fresh loop, and every closure is source-verified present and correct at the cited artifact loci.

### DD-DECIDED items

None applied as a final disposition. Two items were considered for `DD-DECIDED` against the brief's framing before being dispositioned `PASS` on closer reading, recorded here for the orchestrator's review since the brief flagged both as landing on deliberately-open ground:

- **CHK004** (the `:561` residual, "not closed" and which leg remains) — considered `DD-DECIDED §C-8.0b`, dispositioned `PASS` instead: `spec.md` FR-021c itself states the full requirement verbatim ("this feature MUST NOT be described as discharging #180, whose recommended fix has a second leg (folding the flat cap loop into the same traversal) that this scoping still excludes"), naming both the line (`:561`) and the specific remaining leg directly in spec.md — not merely traced to a design-doc anchor. `C-8.0b`'s residual table is cited as corroborating source-level evidence for the underlying scope rationale, not as the sole authority the requirement rests on.
- **CHK011** (FR-021a's characterisation stated symmetrically) — considered `DD-DECIDED`, dispositioned `PASS` instead: the item asks whether the *requirement's form* is well-posed (symmetric obligation, not a premature answer to the still-open Phase-1 measurement), and `spec.md` FR-021a's text is directly and completely symmetric as written ("a reproduction demonstrating a mis-split, or evidence that none is reachable... so the conclusion is not carried forward as an assumption the way it was in fixpp#208") — a PASS on spec.md text, not a trace to an unresolved research.md branch.

If the orchestrator's convention for this bundle treats "spec.md restates a contract-level scope decision" as `DD-DECIDED` regardless of completeness in spec.md itself, both items retag cleanly to `DD-DECIDED §C-8.0b` (CHK004) and would need no anchor for CHK011 since none exists (it would become an escalation, not a retag) — flagging this explicitly rather than picking silently.

### WAIVED items

None.

### Source lookups performed

Direct `Read`/`grep` against live source (stronger evidence than a symbol-existence check for these items, since exact line numbers, loop conditions, and predicate content needed inspection):

- `src/wire/offset_table.cpp:438-503` (`consume_group_extent` — depth-cap `:442-444`, probe `:453-457`, `child` `:466`, outer loop `:473`, bare `++k` `:475`, inner loop `:476-479`, post-delimiter descent + overflow mirror `:485-491`, per-instance cap `:496-499`, unconditional `++inst` `:500`), `:505-572` (`group()` — `:526` delim local, `:541` probe, `:550` extent-bound call, `:559-570` flat cap loop, `:561` boundary test), `:574-609` (`group_slices_reserve_bound` — `:597` probe), `:614-690` (`group_slices`/`group_slices_status` — `:648` `group_end`, `:656` splitter delim local), `:696-725` (`build_nested_subview` — `:717` construction call, `:718-720` verbatim context seed).
- `include/fixpp/wire/offset_table.hpp:77-128` (`group_member_fn_t` `:79-80`, four constructor overloads `:107-128`), `:28` (`default_max_group_entries_per_instance = 4096`), `:340` (`kMaxGroupDepth = 16`).
- `include/fixpp/wire/tag_scan.hpp:62-79` (`parse_bounded_u32` saturation via `accumulate_bounded`).
- `include/fixpp/wire/validator.hpp:350-408` (`consume_group` — outer loop `:357` with no instance bound in its condition, post-delimiter descent `:376`, declared-count check only post-loop `:402`).
- `include/fixpp/dict/table_view.hpp:349-378` (`group_first_field`/`group_member_tags` context-keyed lookup + bare-global fallback), `:641-646` (`set_group_first_ctx` / `add_group_member_ctx` call at `:645`).
- `include/fixpp/wire/group_view.hpp:56-62` (`pushed()` append semantics — confirms C-8.2's off-by-one warning against `group_context_for`).
- `src/wire/offset_table.cpp:424-426` (`group_context_for`, confirmed to call `.pushed(group_no_tag)`).
- `spec/behaviors-and-limitations.md:1697-1701` (L-063-4 existence and text), `spec/coverage-index.md:995-999` (issue #180 cross-reference).
- `tests/wire/nested_group_extent_test.cpp` (exists, 22056 bytes, `:511-519` is the synthetic K=16-depth scoping-trick case L-063-4 points at — confirmed present, not a dangling citation).
- `tests/wire/typed_read_split_agreement_test.cpp`, `tests/wire/consume_group_nested_delim_test.cpp` — confirmed **absent** on this branch. Expected: these are planned artifacts this feature's `/speckit-implement` phase creates (named in `plan.md`'s Project Structure and `tasks.md` T057 onward); their absence pre-implementation is not a defect.
- `grep` across `src/` and `include/fixpp/wire/` for other `OffsetTable(...)` construction call sites — none found outside `offset_table.hpp`/`.cpp`, confirming T057's (CHK008) enumeration is exhaustive.
- `tasks.md:226-227` (T057/T058 — callback-seam construction-site enumeration, splitter re-point).

### Re-run /speckit-analyze?

**NO.** Zero `SPEC-FIXED` dispositions were applied — only this checklist file itself was edited (ticking boxes and appending this section), which `/speckit-analyze` does not consume. No `spec.md`/`plan.md`/`research.md`/`data-model.md`/contract/`tasks.md` edit was made by this audit.

### Verdict

**GREEN** — pipeline.md step 9 satisfied for this domain checklist (`read-path-consistency.md`); no blocker to `/speckit-implement` (step 10) from this file.

1. Zero un-dispositioned `[ ]` boxes: confirmed (26/26 ticked `[x]` with exactly one disposition tag each).
2. Every cited anchor verified to resolve: confirmed — all `src/`/`include/` line citations resolve exactly against live source; L-063-4, issue #180, and `tasks.md` T057/T058 all confirmed to exist and say what the checklist items assume they say.
3. No Completeness/Clarity/Consistency item closed as WAIVED: confirmed (0 WAIVED total; all 26 items closed PASS).

**Note for the orchestrator**: this checklist's overall PASS rate (26/26, no SPEC-FIXED) is expected rather than suspicious — this checklist was derived directly from Gate A's own round-3 escalation and two-round fresh loop over the exact surface it audits, so by the time checklist-audit runs, every gap it was written to catch had already been closed and source-verified during Gate A. The two items flagged above under "DD-DECIDED items" are the closest calls in the set; the orchestrator should specifically spot-check those two dispositions given the divergence from the brief's suggested tag.
