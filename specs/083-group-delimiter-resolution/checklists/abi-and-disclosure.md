# ABI-and-Disclosure Requirements Checklist: Group Delimiter Resolution

**Purpose**: Unit-tests-for-English on the requirements governing the GA-frozen C ABI's construction path, the operator-facing disclosure of a behaviour change reachable through that frozen surface, the interop policy, and the three-path performance obligation. Tests whether these requirements are complete, clear, consistent, and measurable — NOT whether the code works.
**Created**: 2026-07-31
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md) · [research.md](../research.md)
**Contracts in scope**: `contracts/capi_group_grammar.md`
**Audience**: Gate A/B reviewer (PR) · **Depth**: Standard

> **Item derivation.** The C-ABI surface is the second of the two surfaces `/clarify` widened onto and that **failed three consecutive Gate A rounds for want of design depth (RC3)** — the route from a `const Dictionary*` to the per-context delimiter was an outcome with no named mechanism until round 2. Disclosure items are pointed at this project's recorded *"undischargeable artifact re-entering through prose"* class (075 Gate A round 2), and the performance items at *"a narrative assertion in place of a bench"* (Article VIII §3).

## The C-ABI Construction Path — Route, Not Outcome (RC3)

- [x] CHK001 Is the route from the C-ABI handle to the per-context delimiter **named** — which construction is used, which is barred, and under which constraint? [Completeness, Spec §FR-018b / capi_group_grammar.md C-9.2a] — PASS: C-9.2a names the chosen route (session-cached `table_view` built once at `fixpp_session_open`, a non-owning raw pointer copied to the message handle) and the barred one (`dict->as_table_view()` at the check site, barred by `[const §XV.1]`). Both anchors verified at source: `include/fixpp/dict/dictionary.hpp:109-111` (Dictionary's *only* `group_first_field` is the bare overload — one match, confirmed), `include/fixpp/dict/table_view.hpp:290` (bare) and `:349-365` (context-keyed overload, confirmed at those exact lines) — C-9.2a's "so a route must be named" premise holds.
- [x] CHK002 Are the lifetime, ownership, and allocation disposition of the session-cached view specified, rather than left to the implementer? [Completeness, C-9.2a] — PASS: C-9.2a's "Allocation / lifetime disposition" block pins all five: allocation (once per session, zero per message/field), ownership (owned by `fixpp_session`, message holds a raw non-owning pointer), which member commit reads (the session-cached view, never `owned_tv_`), absent-view disposition (fail-closed, not a silent fallback), and exported-surface impact (none — both structs are internal). **Realizability sub-check**: `table_view` is default-constructible (`table_view() = default;` at `include/fixpp/dict/table_view.hpp:244`), so a bare by-value session member is a complete, representable type — no forward-declaration hazard, matching `capi_internal.hpp:285`'s `std::optional<fixpp::dict::table_view> owned_tv_`, which already proves `table_view` is a complete type at that header. Presence/absence is carried by the message-level raw pointer (nullable), mirroring the existing `dict_` shared_ptr pattern, not by the session member's own optionality — realizable as specified. Minor imprecision noted, not blocking: C-9.2a cites `Session::inbound_tv_` as "the identical pattern", but `inbound_tv_` (`include/fixpp/session/session.hpp:818`) is `std::optional<table_view>`, not a bare `table_view`; functionally equivalent since absence is gated by the raw pointer at the consumer layer, but the citation slightly overstates the parallel.
- [x] CHK003 Is the behaviour of a handle carrying **no dictionary** stated as explicitly unchanged, so the new route cannot alter an existing accepted case? [Consistency, Spec §FR-018b / C-9.4] — PASS: C-9.4 states the `dict == nullptr` disposition is unchanged (empty-instance check only, no delimiter check), pinned by W-11b. Verified against source: `message_write.cpp:718` (`if (dict) { ... }`) gates the delimiter leg exactly as C-9.4 describes.
- [x] CHK004 Is a dictionary-present handle that cannot reach the context-keyed view required to **fail closed**, with the reason a context-free fallback would reinstate the very defect being removed? [Consistency, Spec §FR-018b] — PASS: FR-018b ("MUST therefore be a config-time-built, session-owned context-keyed view... A dictionary-present handle that cannot reach the context-keyed view MUST fail closed rather than fall back to the context-free lookup, which would reinstate the defect FR-001 removes") and C-9.2a's Absent-view-disposition bullet both state this, with the identical reason (would reinstate the global first-seen defect).
- [x] CHK005 Is the ancestor path stated as **already available** at the construction check — carried through an existing recursion — rather than requiring new plumbing across the boundary? [Clarity, Spec §FR-018 / C-9.2] — PASS: FR-018 and C-9.2 both state the construction-side check already recurses through nested instances and already has the message type available; only an added `msg_type` + ancestor-path parameter is needed. Verified at source: `message_write.cpp:725` (`validate_group_grammar(inst.fields, dict)`) is the existing recursion cited.
- [x] CHK006 Are the call sites that are deliberately **not** converted enumerated, so a class-fix does not sweep them in? [Completeness, C-9.5] — PASS: C-9.5 names `:157`, `:812`, `:923`. Verified by exact grep of `group_first_field` in `src/capi/message_write.cpp` — matches precisely (`:157` in `is_group_collision`, `:812` and `:923` in the two `group_begin`-family functions), plus the two in-scope call sites (`:719` inside `validate_group_grammar`, being converted) — the enumeration is exhaustive over the file's five call sites.
- [x] CHK007 Is the "one rule, two paths" property stated as a **bidirectional** criterion — accepted at construction if and only if inbound validation would accept the same placement? [Clarity, Spec §SC-012 / C-9.1] — PASS: C-9.1 ("A message the builder accepts is never rejected by the engine's own validation on delimiter grounds, and vice versa") and SC-012 ("accepted at construction if and only if the engine's own inbound validation would accept the same delimiter placement") both state it bidirectionally, and W-11 explicitly witnesses both directions ("Run for both the accepting and the rejecting delimiter placement, so agreement is proven in both directions rather than by a single accept").

## Frozen-ABI Integrity and Disclosure

- [x] CHK008 Is the no-exported-signature-change requirement stated with the **concrete gate** that checks it, and is that gate one this repository currently runs? [Measurability, Spec §FR-018a / W-14] — DD-DECIDED §W-14: spec.md's FR-018a states the no-signature-change requirement but does not itself name the gate; the contract's W-14 does ("The gate is `nm`-based exported-symbol comparison only — `abidiff` was retired 2026-06-22"). Verified live: `.github/workflows/abi-golden.yml` exists, runs `on: pull_request` (types opened/reopened/synchronize, all branches) and `push: branches: [main]`, and its header comment confirms the exact claim — "NOTE — was an abidiff layout gate; replaced with an nm symbol-set check 2026-06-22. The release `fixpp_capi.a` has no debug info... AND libabigail 2.4.0 (ubuntu-24.04 apt) asserts/core-dumps on debug-info-less archives. `nm` gives the same check". The gate job is still internally named `abidiff` (job id) with display name "ABI golden — fixpp_decimal_t" — cosmetic only, the check performed is nm-based per the same file's comments. `tests/capi/abi_symbol_golden_test.cpp` exists and is built in every sanitizer/release variant. Out-of-scope observation, not chased: Article X §4 (`constitution.md`) still says "CI verifies no re-definitions per the abidiff check `[const §IX.5]`" — a pre-existing constitution/artifact-drift W-14 explicitly disclaims as outside this feature's scope.
- [x] CHK009 Is the distinction between *a behaviour change reachable through a frozen surface* and *a surface change* stated explicitly, so the ABI-affecting gate treatment is not read as contradicting the freeze? [Clarity, Spec §FR-018a / §Assumptions] — PASS: FR-018a ("this is a behaviour change reachable through the existing surface, not a surface change") and the Assumptions section ("This feature changes behaviour reachable through a frozen ABI, not the ABI surface itself; it is nonetheless treated as ABI-affecting for gate purposes, since the observable contract changes for existing callers") both state the distinction and its consequence explicitly.
- [x] CHK010 Is the behaviour change **enumerated by group**, with the opening order that is accepted today and rejected after, rather than disclosed as a category? [Measurability, Spec §FR-019 / C-9.6] — DD-DECIDED §C-9.6: the per-group old/new-opening-tag enumeration is a **requirement on the deliverable** (C-9.6: "name the five groups whose delimiter moves and, for each, the old and new opening tag"), operationalized by T069 (writes the enumeration into `spec/behaviors-and-limitations.md`) and pinned for one instance by W-12 — this is design depth living in the contract, matching the team-lead brief's guidance that C-9.1…C-9.7 depth is DD-DECIDED, not re-spec'd. The *identity* of the five is not left as a bare category: spec.md's Baseline-measurement section (`§Baseline measurement`, "cause 2 — Broken one-level scan, FIX50SP2 only: 1677, 1772, 40204, 41599, 42060 (wrong delimiter)") names the five `NumInGroup` tags explicitly. Tightened FR-019 with a one-line cross-reference to that section for readability (spec.md edit, zero new data — see SPEC-FIXED items). Escalation note: it is not evident from the spec bundle *why* only these five (cause-2, FIX50SP2-only) are disclosed for the C-ABI path rather than the full 335-context cause-1+cause-2 set, since the same `dict->group_first_field()` global lookup underlies both causes on the construction path — flagged in Escalations, does not block GREEN (a scope/cardinality question about the measured baseline, not about the disclosure's *form*, which is what this item tests).
  > **RE-DISPOSITIONED BY THE ORCHESTRATOR → SPEC-FIXED (2026-07-31).** The escalation was correct and was resolved here rather than deferred to T069, because the requirement as written **capped** the deliverable at five: an implementer following it would have produced a five-row table and stopped, and the SC-007 audit would then have certified green over an under-enumerated exception — the RC2 shape (a gate stated over the wrong population). Verified at source before fixing: `src/capi/message_write.cpp:719-722` reads the **bare global** today and FR-018 re-points it at the **context-keyed** value, so the enumeration must be taken over *"whose resolved value moves"*, not *"whose global value moves"*. That admits two further classes. **(b)** the cause-1 tags — global unchanged (hence invisible to a global-lookup enumeration), per-context value moves in divergent contexts, accepted-today-rejected-after at the **same severity as (a)**. **(c)** the three unregistered groups + six nested children — `group_begin` rejects them outright today (`:812`, `:923`, `group_first_field == 0 ⇒ TYPE_MISMATCH`) and the commit check never runs (`:720` guards `delim != 0`), so registration makes them **buildable**: a widening, disclosed but not an SC-007 exception. Edits: `spec.md` FR-019 split into FR-019/**FR-019a**/FR-019b with the three classes; **SC-007**'s exception scope corrected to classes (a)+(b); `contracts/capi_group_grammar.md` **C-9.6** widened and **C-9.7** given the discriminator-width argument; `tasks.md` **T069** re-scoped to all three. **No new witness** — W-11 already runs on a divergent context and pins class (b), W-12 pins class (a); the defect was in the enumeration, not the pins.
- [x] CHK011 Is the artifact that carries the operator-facing disclosure named as one that **exists in this repository**, rather than as a deliverable with no home? [Gap, Spec §FR-019 / §FR-020] — SPEC-FIXED: genuine gap found. FR-019 and FR-020 (as written pre-audit) both said "and in the release notes" / "and a release-note entry" — no `CHANGELOG.md` or release-notes file exists anywhere in this repository (confirmed by search). This is the identical defect resolved in `specs/075-live-wire-enum-validation/spec.md:140` ("Resolved: the B-row IS the release-note artifact. `spec/behaviors-and-limitations.md` is this repo's operator-facing behavior-change record and the only one that exists... No second artifact is invented."). Edited `spec.md` FR-019/FR-020 to remove the phantom artifact and cite `spec/behaviors-and-limitations.md` as the sole disclosure vehicle, mirroring 075's resolution language. The phantom artifact also re-entered through `tasks.md` T069/T074, `contracts/capi_group_grammar.md` C-9.6, `plan.md` (Constitution Check row X + Gate 5 Evidence row), and `quickstart.md` — all edited to match, since leaving any one uncorrected would have carried the undischargeable instruction into `/implement` (the exact 075-precedent failure mode).
- [x] CHK012 Is the relationship between the disclosed rejection and the no-new-rejections criterion stated as an **enumerated exception** rather than left as an apparent conflict? [Consistency, Spec §SC-007 / C-9.7] — PASS: C-9.7 states this directly ("SC-007... is not violated: the newly-rejected construction orders are not schema-valid under declaration order. C-9.6's enumeration is what lets the SC-007 audit distinguish an intended new rejection from a regression"), and SC-007 itself carries the exception clause ("except for the disclosed C-ABI construction-order change, which is enumerated by group and documented").
- [x] CHK013 Is the newly-correct rejection of an over-permissive membership case likewise reconciled against the no-new-rejections criterion, rather than only the C-ABI case? [Consistency, Spec §US1 scenario 4 / §SC-007] — PASS: User Story 1 Acceptance Scenario 4 states this explicitly ("The message is not schema-valid, so this newly-correct rejection is an enumerated exception to SC-007, not a violation of it"), and SC-013 corroborates by requiring FR-010a's polluted-context rejection to be witnessed directly, not merely inferred from SC-002's cardinality.

## Interop Policy

- [x] CHK014 Is the interop gate's role stated as **observational**, with an explicit prohibition on it altering resolution, a configuration surface, or an inbound branch? [Clarity, Spec §FR-020 / §FR-020a] — PASS: FR-020 ("The per-release interop gate is therefore observational for this feature — it measures and documents divergence, it does not arbitrate it") and FR-020a ("No compatibility or leniency mode MAY be introduced... no per-session configuration surface and no conditional branch on the inbound validation path") together state the role and the three explicit prohibitions.
- [x] CHK015 Is the absence of any compatibility or leniency mode stated as a requirement, with its consequence for keeping the regression pin carve-out-free? [Consistency, Spec §FR-020a / §FR-012] — PASS: FR-020a states both the requirement and the consequence in one sentence ("so that resolution stays derivable from the dictionary alone and FR-012's pin can remain carve-out-free").
- [x] CHK016 Is the unavailability of the external reference engines stated as a bounded limitation on **corroboration** — with what is consequently discovered later — rather than as a blocker or as a silent omission? [Clarity, Spec §Assumptions / §Dependencies] — PASS: Assumptions ("It is not corroborated against an external reference engine, and the reference engines are absent from the working copy, so that corroboration cannot be obtained during this feature... any divergence a reference engine later shows is recorded as a documented behaviour rather than treated as a defect") and Dependencies ("because the gate is observational, their absence does not block this feature, but it does mean any divergence is discovered later rather than here") both state it as a bounded, non-blocking limitation with an explicit later-discovery consequence.
- [x] CHK017 Is the premise that any delimiter choice rejects some schema-legal shape stated, so that the unconditional rule is understood as a policy decision rather than a costless one? [Completeness, Spec §FR-020] — PASS: FR-020's opening clause states this directly ("Because every member of the affected groups is schema-optional, any delimiter choice rejects some shape another engine accepts. Declaration order (FR-002) is nonetheless authoritative and unconditional").

## Performance — Three Paths, Benches in the Same Change

- [x] CHK018 Are **all three** changed hot paths enumerated, each with the mechanism by which this feature makes it more expensive? [Completeness, Spec §FR-022] — PASS: FR-022 enumerates all three with mechanism: (1) inbound validation — "delimiter resolution is queried per received field"; (2) typed read — "replaces an array read of `entries_[first].tag` with a context-keyed dictionary lookup and adds nested-extent recursion"; (3) C-ABI construction — "replaces an `O(1)` bare lookup with a context-keyed lookup plus ancestor-path maintenance, executed per group instance on every message commit".
- [x] CHK019 Is a benchmark required **in the same change** for each of the three, with a narrative assertion explicitly refused as a substitute? [Measurability, Spec §FR-022 / §SC-009] — PASS: FR-022 ("a benchmark covering each MUST ship in the same change as the fix... A narrative assertion that the budget was met is not acceptable in place of the benchmark, on any of the three") and SC-009 ("on all three FR-022 paths — inbound validation, typed-read group access, and C-ABI commit — not on the inbound path alone"). Verified the three benches are named and distinct in the contract/plan: `bench/validate_group_bench.cpp`, `bench/typed_read_group_bench.cpp`, `bench/capi_commit_group_bench.cpp` (`plan.md` Constitution Check row VIII).
- [x] CHK020 Is any intentional baseline update required to carry its **rationale** in the same change? [Completeness, Spec §FR-022] — PASS: FR-022 ("a benchmark covering each MUST ship in the same change as the fix, together with any intentional baseline update and its rationale").
- [x] CHK021 Is the requirement that the C-ABI view be built **once per session, never per message** given a witness — given that no behavioural result distinguishes the correct route from the barred one? [Measurability, Spec §FR-022 / W-11a] — SPEC-FIXED (contract): FR-022's closing sentence and W-11a both require the witness and W-11a exists, but W-11a's counter mechanism was named only by analogy ("a test seam of the same kind as `fixpp_capi::detail`'s existing live-state counter") without a pinned location — and the analogy doesn't transplant cleanly: `as_table_view()` is defined in `src/dictionary/dictionary.cpp`, a different module from `src/capi/`, with no existing `detail`-namespace instrumentation to extend (checked — none found). This is exactly the "route not named" gap this checklist's own item-derivation note (RC3) warns against, so it is closed the same way CHK001 was rather than left as a citation gap. Edited `contracts/capi_group_grammar.md` W-11a to pin the counter's home: `fixpp::dict::detail::as_table_view_call_count()`, declared in `include/fixpp/dict/dictionary.hpp`, incremented in `Dictionary::as_table_view()`'s body in `src/dictionary/dictionary.cpp`, non-exported `detail` namespace — the same shape as the `fixpp_capi::detail` counter, homed in the module that actually owns the call site.
- [x] CHK022 Is the regression budget stated against a recorded baseline for each of the three paths, rather than for the inbound path alone? [Consistency, Spec §SC-009] — PASS: SC-009 states this explicitly ("Message-processing throughput stays within the project's standing regression budget against the recorded baseline on all three FR-022 paths — inbound validation, typed-read group access, and C-ABI commit — not on the inbound path alone").

## Notes

- Check items off as `[x]` with exactly one inline disposition tag: `PASS` / `SPEC-FIXED` / `DD-DECIDED §X` / `WAIVED: <reason>`.
- Items tagged **Completeness / Clarity / Consistency** may **not** be closed as `WAIVED` (pipeline.md step 9). CHK011 is deliberately tagged `[Gap]`, not `[Completeness]` — if the disclosure artifact genuinely does not exist, the honest resolution is a spec fix naming the one that does, but the tag does not pre-judge that.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 17 |
| SPEC-FIXED | 2 |
| DD-DECIDED | 3 |
| WAIVED | 0 |
| **Total** | 22 |

### SPEC-FIXED items

- **CHK011** — Phantom "release notes" artifact (no `CHANGELOG.md`/release-notes file exists in this repo — confirmed by search). Mirrored the identical defect's resolution at `specs/075-live-wire-enum-validation/spec.md:140` ("the B-row IS the release-note artifact"). Edited: `spec.md` FR-019 (also added a `§Baseline measurement, cause 2` cross-ref, discharging CHK010's readability tightening at zero new data) and FR-020; `tasks.md` T069 and T074; `contracts/capi_group_grammar.md` C-9.6; `plan.md` Constitution Check row X and Gate-5-Evidence row; `quickstart.md`'s summary line. All six files edited together — leaving any one uncorrected would carry the undischargeable instruction forward into `/implement`, the exact failure mode 075 named.
- **CHK021** — W-11a's counter-seam mechanism was named only by analogy to `fixpp_capi::detail`'s live-state counter, which lives in a different module (`src/capi/`) than the call site it must instrument (`Dictionary::as_table_view()` in `src/dictionary/dictionary.cpp`, no existing `detail`-namespace instrumentation there). Edited `contracts/capi_group_grammar.md` W-11a to pin the counter's actual home: `fixpp::dict::detail::as_table_view_call_count()`, declared in `include/fixpp/dict/dictionary.hpp`, incremented in `Dictionary::as_table_view()`'s body, non-exported `detail` namespace.

### DD-DECIDED items

- **CHK008** — anchor W-14 (`contracts/capi_group_grammar.md`). Rationale: spec.md's FR-018a doesn't name the concrete gate; W-14 does, and it verified live against `.github/workflows/abi-golden.yml` (nm-based, runs on `pull_request` + `push:main`, abidiff retired 2026-06-22 exactly as claimed).
- **CHK010** — anchor C-9.6 (`contracts/capi_group_grammar.md`) + T069/W-12 (`tasks.md`). Rationale: per-group old/new-opening-tag enumeration is design depth on the deliverable, not the requirements text; the five tags' *identity* is independently named in spec.md's `§Baseline measurement` (cause 2). This is the same "depth honestly lives in the contract" class the team-lead's brief pre-authorized as DD-DECIDED rather than SPEC-FIXED.
- **CHK012** — anchor C-9.7 (`contracts/capi_group_grammar.md`) + SC-007's own exception clause. Rationale: the reconciliation is stated at both the contract and the spec's success-criterion level; no re-spec needed.
- **CHK021** — see SPEC-FIXED above; the *witness's existence* (W-11a) is DD-DECIDED as adequate design depth; only its unpinned location was a defect, now closed.

### WAIVED items

None.

### Realizability sub-check

- **`fixpp_session`'s C-9.2a-added `table_view` member** (value-typed entity, held by-value per C-9.2a's prose). Verdict: **clean**. `table_view() = default;` at `include/fixpp/dict/table_view.hpp:244` confirms `table_view` is default-constructible, so a bare by-value session member is a complete, representable type with no forward-declaration hazard — reinforced by `capi_internal.hpp:285`'s existing `std::optional<fixpp::dict::table_view> owned_tv_`, which already proves `table_view` is complete at that header. Allocation/ownership/which-member-reads/absent-disposition/exported-surface are all pinned in C-9.2a's "Allocation / lifetime disposition" block. One non-blocking imprecision noted in CHK002's disposition: C-9.2a cites `Session::inbound_tv_` as "the identical pattern," but `inbound_tv_` (`session.hpp:818`) is `std::optional<table_view>`, not bare — functionally equivalent (absence is gated by the message-level raw pointer, not the session member's own optionality) but the citation slightly overstates the parallel. Did not require a SPEC-FIX.

### Source lookups performed

- `src/capi/message_write.cpp`: `:710-730` (`validate_group_grammar`, file-static, recursion at `:725`), `:719` (bare `group_first_field` lookup), `:157`/`:812`/`:923` (predicate sites, exact grep match against C-9.5's enumeration), `:289-291` (outbound `dict_` copy — confirmed the copy statement is at `:291` exactly, comment block `:289-290`), `:755` (commit call site), `:718` (dict-presence gate).
- `src/capi/session.cpp:109-111` (`dict_` cached at `fixpp_session_open`).
- `src/capi/capi_internal.hpp`: `:261-266` (`dict_` declaration+comment, exact match), `:266` (member), `:277-284` (destruction-order note), `:285` (`owned_tv_`), `:493` (`fixpp_session::dict_`), `:496-500` (live-state counter, confirmed module-local to `src/capi/`).
- `include/fixpp/dict/dictionary.hpp:109-111` (`Dictionary::group_first_field` — confirmed the *only* overload, one grep match), `:210-212` (`[const §XV.1]` annotation, exact text match).
- `include/fixpp/dict/table_view.hpp:244` (`table_view() = default;`), `:290` (bare `group_first_field`), `:349-365` (context-keyed overload, exact match).
- `src/dictionary/dictionary.cpp:357-358` (`[const §XV.1]` annotation, exact text match), `:359` (`as_table_view()` definition site — no existing `detail`-namespace instrumentation found).
- `include/fixpp/session/session.hpp:818` (`inbound_tv_` is `std::optional<table_view>`), `src/session/session.cpp:992` (`inbound_tv_` assignment).
- `.specify/constitution.md:220-225` (Article X §1/§6, exact text match), `:291` (Article XV heading, confirms `[const §XV.1]` resolves to "Banned Patterns").
- `.github/workflows/abi-golden.yml` (nm-based gate, trigger conditions, retirement-of-abidiff comment — all confirmed live).
- `tests/capi/abi_symbol_golden_test.cpp` (existence confirmed, built across sanitizer/release variants).
- Repo-wide search for `CHANGELOG`/release-notes files: none found (confirms CHK011's finding).
- `specs/075-live-wire-enum-validation/spec.md:140` and `:385` (precedent resolution for the identical phantom-artifact defect).
- fixpp#208 issue body (fetched via `gh issue view 208`, external corroboration only — not cited as a repo artifact; used to understand which five tags "cause 2" names, already independently confirmed at spec.md's own `§Baseline measurement` text).

### Re-run /speckit-analyze?

**YES.** Two `SPEC-FIXED` dispositions changed `spec.md` (FR-019, FR-020) and `contracts/capi_group_grammar.md` (C-9.6, W-11a), plus consistency edits to `tasks.md` (T069, T074), `plan.md` (two rows), and `quickstart.md`. The prior `/speckit-analyze` drift check predates these edits and must be re-run before `/speckit-implement`.

### Verdict

**GREEN** — pipeline.md step 9 satisfied for this checklist. All 22 items dispositioned; zero un-dispositioned boxes; every cited anchor (contract clause IDs, source line ranges, constitution articles, the CI workflow) spot-verified to resolve; no Completeness/Clarity/Consistency item closed as WAIVED (none were WAIVED at all).

### Escalations

- **CHK010 scope question** (does not block GREEN, informational for the orchestrator): the C-ABI disclosure names exactly five groups — the FIX50SP2-only "cause 2 / broken one-level scan" wrong-delimiter set (1677, 1772, 40204, 41599, 42060) — while the same `dict->group_first_field()` global lookup that the C-ABI construction path reads today is *also* affected by "cause 1" context-divergence contexts (73, 124, 146, 268, 295, 420, 2428, 2474, plus 1677/1772 on Orchestra) and the other 300 measured contexts across all ten dictionaries. It is not evident from spec.md/research.md/plan.md/the contract *why* the C-ABI disclosure is scoped to only these five rather than the full affected set reachable through `dict->group_first_field(e.tag)`. This may be entirely correct (e.g., most other contexts might be unreachable from any C-ABI builder call, or the "five" may simply be the illustrative subset carried over from fixpp#208's suggested-scope item 4 while the B&L row at implementation time is expected to enumerate more) — but the spec bundle does not state which, and I did not attempt to resolve it myself since it requires either implementation-time investigation or an explicit scope decision, not a documentation fix. Recommend the orchestrator confirm at `/implement` time (T069) whether "five" is deliberately narrower than the full affected-and-C-ABI-reachable set, and if so, have T069 state the reason in the B&L row rather than leave it implicit.

  > **RESOLVED — 2026-07-31, by the orchestrator, at the audit rather than at `/implement`.** The escalation was correct, and deferring it was not safe: the requirement as written **capped** the deliverable at five, so an implementer would have produced a five-row table and the SC-007 audit would then have certified green over an exception it never listed — the **RC2** shape (a gate stated over the wrong population), which this feature already burned a Gate A loop on. Verified at source: the check reads the **bare global** (`src/capi/message_write.cpp:719`) and FR-018 re-points it at the **context-keyed** value, so the enumeration is over *"whose resolved value moves"*. **Neither of the auditor's two hypotheses was the answer** — the other contexts are *not* C-ABI-unreachable, and "five" was *not* an illustrative subset. The correct set is **three classes**: (a) the five, (b) the cause-1 tags (same accepted-today-rejected-after severity; invisible to an enumeration written against the global lookup precisely because their *global* value does not move), and (c) `1499`/`1669`/`1919` + six children, which `group_begin` rejects outright today (`:812`, `:923`) and which become **buildable** — a widening, not an SC-007 exception. Landed as **FR-019a**, with SC-007's exception scope corrected to (a)+(b). **No new witness was needed** — W-11 already runs on a divergent context and pins class (b). The subsequent mandatory `/speckit-analyze` re-run then found **8 stale restatements** of the old five-group scope at loci this fix had missed (`data-model.md` entirely, plus second locations in `tasks.md` and `plan.md`, and the FR-count ledger); all 8 remediated. Flagging this rather than passing it silently is what made the fix possible.
