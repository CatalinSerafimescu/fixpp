# Delimiter-Resolution Requirements Checklist: Group Delimiter Resolution

**Purpose**: Unit-tests-for-English on the requirements governing this feature's primary axis — per-context delimiter resolution, its consumer contract, loader symmetry and fail-closed disposition, the receiver's descend-at-delimiter rule, member-set exactness, and the regression pin's non-circularity. Tests whether these requirements are complete, clear, consistent, and measurable — NOT whether the code works.
**Created**: 2026-07-31
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md) · [research.md](../research.md) · [data-model.md](../data-model.md)
**Contracts in scope**: `contracts/group_ctx_delims.md` · `contracts/loader_tolerant_mode.md` · `contracts/consume_group.md`
**Audience**: Gate A/B reviewer (PR) · **Depth**: Standard

> **Item derivation.** Items are derived from the Gate A convergence record's named root causes — **RC1** (the consumer of the per-context delimiter was never designed), **RC2** (phase gates stated over artifacts their phase does not move), **RC4** (traceability one pass behind) — and from this project's recorded anti-pattern classes (circular corpus, unproven-RED gate, subset-not-exact-set gate, half-restructure, silent fallback, count-delta asserted rather than constructed). Items derived by re-reading the FR text this orchestrator wrote would be PASS-by-construction; these are pointed at what actually failed five times.

## Per-Context Resolution and Its Consumer (RC1)

- [x] CHK001 Is the delimiter's defining rule stated once, unconditionally (declaration-order first member, resolved per context), rather than as a set of per-site behaviours a reader must reconcile? [Clarity, Spec §FR-001/§FR-002] — PASS: FR-001 states the rule (resolve from own declaration/context, not global first-seen) and FR-002 states the value (first member in declaration order) as two clean, unconditional sentences; no per-site enumeration anywhere else in spec.md restates or narrows it.
- [x] CHK002 Is the disposition of a consumer-side lookup miss on the per-context delimiter table specified — where completeness is enforced, and which fallback values are forbidden **by name**? [Completeness, Spec §FR-023] — PASS: FR-023 states enforcement is a load-time invariant in `finalize()` (not at `as_table_view()`, which stays non-throwing) and names both forbidden fallbacks explicitly — `group_first_field(no_tag)` and `members.front()` — with the defect each would reinstate.
- [x] CHK003 Are the non-throwing contract on `as_table_view()` and the load-time enforcement point stated together, so the pair cannot be satisfied by relocating the check to the consumer? [Consistency, Spec §FR-023 / group_ctx_delims.md C-3.4] — PASS: FR-023's own sentence pairs both halves ("enforced as a load-time invariant in the loaders' `finalize()` … *not* checked at `as_table_view()`, which is contractually non-throwing … and must remain so"); C-3.4 restates the same pairing with the L-063-4 citation establishing the non-throwing precedent, closing the "just move the check to the consumer" escape route on both sides at once.
- [x] CHK004 Is the **computation** of the set the completeness invariant checks over defined, rather than left as the unelaborated phrase "every context the consumer enumerates"? [Completeness, group_ctx_delims.md C-3.4a] — PASS: C-3.4a gives the exact two-predicate computation (`fr.type == NumInGroup` at `:446` AND `!members.empty()` at `:463`) the checked set must reproduce, and names the failure mode (checked set a strict superset without the second predicate) that motivates stating it precisely rather than the unelaborated phrase C-3.4 alone uses.
- [x] CHK005 Is the leg of that invariant which depends on an unresolved research question marked **conditional**, with the resolving work named and sequenced **ahead of** the change it gates? [Clarity, research.md §D-12 / C-3.4a] — DD-DECIDED §D-12: this is a deliberately-**open** item, not a gap. C-3.4a states the set-equality leg is "asserted CONDITIONALLY on D-12 resolving to branch (b)"; research.md D-12 is explicitly labelled "Status: OPEN … not settled and must not be read as one," names the resolving instrumentation task (log `fr.type` as the `:446` gate sees it vs. `Dictionary::field_ref(...).type`, plus a control tag), and states it is "sequenced ahead of the loader change" as a Phase-1 blocker for Phase 3. The conditional marking, the resolving work, and its sequencing are all present as required — the requirement is satisfied by stating the open question correctly, not by closing it.
- [x] CHK006 Is the interaction between tolerant mode and the completeness invariant stated as its own requirement, rather than left inferable from the two in combination? [Consistency, Spec §FR-023a / §FR-006a] — PASS: FR-023a is a standalone requirement ("FR-006a's tolerant mode MUST NOT be able to manufacture an FR-023 violation … Tolerant mode is not a licence to register a context whose delimiter is unknown"), not merely implied by juxtaposing FR-006a and FR-023; C-3.4's "Tolerant-mode interaction" paragraph restates the same rule.
- [x] CHK007 Is the invariant's premise identified as holding **by measurement rather than by construction**, and given a precondition instead of an assumption? [Clarity, Spec §FR-023b] — PASS: FR-023b states this in so many words — "the invariant holds **because of a measurement, not by pure construction** — and a measured premise gets a precondition, not an assumption" — and names the precondition (C-7.3, alongside C-7.1/C-7.2, all Phase-3 gates).
- [x] CHK008 Is the store's structural inability to represent more than one context per `(msg_type, no_tag)` recorded as an inherited limit with its mechanism, rather than presented as a property of this feature's design? [Completeness, Spec §Edge Cases / C-3.5] — PASS: spec.md's Edge Cases entry states it directly ("the runtime **cannot represent** it: `message_fields(mt)` is sorted and `unique`d **by tag** … This is a known structural limit of the 063 store, inherited not introduced"), and C-3.5 gives the mechanism in full (dedup at `xml_loader.cpp:877-885`, the `std::ranges::sort`-not-`stable_sort` detail) plus the measured-zero reachability that makes the limit currently inert. Verified against source: `dictionary.cpp:446` group-loop gate (`if (fr.type != field_data_type::NumInGroup) { continue; }`) matches the C-3.4a citation exactly, corroborating the surrounding claim.

## Loader Symmetry, Recursion, and the Fail-Closed Default

- [x] CHK009 Is cross-loader equivalence stated as a requirement with an explicit statement of what partial delivery would constitute, rather than as an aspiration? [Completeness, Spec §FR-005] — PASS: FR-005 states it directly — "Both dictionary sources … MUST implement FR-001 through FR-004 with equivalent behaviour. A change to one without the other is **not an acceptable partial delivery**" — a normative statement, not an aspiration.
- [x] CHK010 Is the fail-closed default specified to **mirror an existing loader disposition**, with the exception type per loader and both fuzz harnesses' documented exception sets named? [Completeness, Spec §FR-006c] — PASS (verified at source): FR-006c states the per-loader exception types (`xml_parse_error` / `orchestra_parse_error`) and names both fuzz harnesses by file. Verified directly: `include/fixpp/dict/error.hpp:98` declares `orchestra_parse_error : public xml_parse_error` exactly as cited; `tests/fuzz/fuzz_orchestra_loader.cpp:52-80` catches `orchestra_parse_error` (the derived type) and rethrows any other `std::exception`, while `tests/fuzz/fuzz_dict_xml_loader.cpp:50-72` catches the base `xml_parse_error` — confirming the requirement's stated hazard (a base `xml_parse_error` thrown from `OrchestraLoader` would escape its harness) is real, not hypothetical.
- [x] CHK011 Is the condition that triggers the fail-closed path distinguished, **at one granularity**, from a group that is declared but referenced by no message? [Clarity, Spec §FR-006d / loader_tolerant_mode.md C-6.1a] — PASS: FR-006d states the granularity explicitly ("FR-006 fires on an unresolvable **first member**, not on an unreferenced **declaration**"), and C-6.1a restates it as the governing clause with the reconciliation history (why the two predicates previously disagreed) recorded, so a reader lands on one unambiguous granularity.
- [x] CHK012 Is the requirement that all ten shipped dictionaries still load under the new default stated as a **gating measurement before the path is enabled**, not as an expected outcome? [Completeness, Spec §FR-006b] — PASS: FR-006b's own wording is gating, not descriptive — "**Before** FR-006 is enabled, all ten shipped dictionaries MUST be confirmed to still load under the default, so the fail-closed path cannot regress the shipped set" — and C-7.1 restates it as a numbered sequencing precondition with a defined answer space.
- [x] CHK013 Is the consequence of deleting the one-level component scan on the **global** first-field lookup specified, given that lookup still serves as an is-this-a-group predicate at other call sites? [Completeness, C-1.4b / research.md §D-10] — PASS (verified at source): C-1.4b states the repopulation requirement and points at D-10 for the consumer list; D-10 enumerates six consumer sites, including the one write-order-sensitive site (072's collision guard). Verified directly: `src/dictionary/xml_loader.cpp:1016-1017` and `src/dictionary/orchestra_loader.cpp:895-896` both read `first_field_tag` in the cited collision-throw shape, confirming the guard genuinely depends on the value this feature re-derives, exactly as D-10/C-1.4b describe. This item has no spec.md FR text of its own — the consequence lives entirely in the contract/research pair, which is the correct home for a Project-Structure-level dependency, not a defect.
- [x] CHK014 Is the ordering between writing the new per-context projection and the load-time collision guard that reads it stated as a requirement, rather than left to implementation order? [Consistency, loader_tolerant_mode.md C-7.2] — PASS: C-7.2 states the write-order requirement explicitly ("the global projection must be complete before `finalize()`'s guard reads it"), and `plan.md`'s Source Code tree (`dictionary.cpp` / `xml_loader.cpp` rows) restates the same ordering as an implementation note, so the two artifacts agree rather than leaving it to whichever order a task happens to land in.
- [x] CHK015 Is the source of declaration order named as an existing traversal the loaders already perform, rather than a new one — and is the reason a third traversal is rejected stated? [Clarity, research.md §D-1] — DD-DECIDED §D-1: this is a frozen design decision recorded only in research.md (no restatement needed in spec.md's FR text, which correctly stays behaviour-level). D-1 names the existing traversal (`expand_field_list`'s document-order emission) as the source, states "Do not write a document-order scan," and gives an explicit "Alternatives considered" rejecting a third traversal ("preserves two traversals that must stay in lockstep by convention rather than by construction — the arrangement that failed"). Verified at source: `src/dictionary/xml_loader.cpp:576-579` (component expansion passes `enclosing_group_no_tag` through unchanged), `:594-597` (nested group's count-tag `FieldRef` pushed at the outer level before the `:667` recursion), and the deleted one-level scan at `:610-641` — all match D-1's citations exactly.

## Reception — Descend at the Delimiter

- [x] CHK016 Is the ordering constraint between descend-at-delimiter and recursive resolution stated as a **hard prerequisite** with the consequence of violating it quantified? [Clarity, Spec §US2 "Why this priority" / §FR-007] — PASS: US2's "Why this priority" states it as a hard prerequisite ("a **blocking prerequisite**… This story must land before, or atomically with, Story 3") with the consequence quantified ("false rejection across **232 contexts**" — *the quoted figure was re-pointed 2026-08-02, Gate B r3, fixpp#216, when `spec.md:167` was corrected from "232 measured FIX50SP2 contexts … a further 30" to "485 contexts"; SC-016 (`spec.md:342-346`) withdrew the "232 + 30" decomposition as a category error. Re-pointed again 2026-08-02, Gate B r4, fixpp#216 IND-1 — the r3 swap over-applied the 485 shape-figure to this conversion clause: 485 sizes the unconditioned mode-(c) shape, not the population this clause's "converts" verb actually describes, so `spec.md:167`'s headline reverts to **232** (the FIX50SP2 subset this correction newly routes into the shape), with the further 30 stated separately in the sentence that already followed it. The item's own three assertions — hard-prerequisite framing, consequence quantified, contract restates it — were re-verified at source this round and all hold; this is a quotation re-point, not a re-disposition*); `consume_group.md`'s "Ordering constraint" section restates it and, per its own rewritten Gate A round-1 history, replaces a previously vacuous gate with two assertions the phase can actually move.
- [x] CHK017 Are the properties descent must not weaken **enumerated** (instance count, required members, extent termination), rather than compressed into "must not regress"? [Completeness, Spec §FR-008] — PASS: FR-008 enumerates exactly three named properties ("instance-count enforcement, required-member enforcement, or extent termination") rather than a compressed "must not regress"; `consume_group.md` C-5.1..C-5.4 restate each as its own clause plus a fourth (genuine-invalid-message-still-rejects).
- [x] CHK018 Is bounded depth behaviour specified with a **named witness and a file**, rather than being a requirement no artifact owns? [Coverage, Spec §FR-009 / consume_group.md W-3] — PASS: FR-009 states the requirement; `consume_group.md` W-3 names both the case (`ConsumeGroupNestedDelim.NestedDelimiterAtDepthCapIsBoundedNotUnbounded`) and the file (`tests/wire/consume_group_nested_delim_test.cpp`), and records why it was added at Gate A round 3 (previously "the only witness for FR-009 and User Story 2 scenario 4 and it had neither").
- [x] CHK019 Is the reception witness required to run against a **populated** context store, rather than one whose lookups resolve through the bare global fallback? [Measurability, consume_group.md W-1a] — PASS: W-1a is explicitly the Phase-2 exit witness and states its purpose in contrast to W-1 — "the identical two-instance shape, but with the outer group registered under a **non-empty parent path** so the descent resolves through `group_ctx_` rather than through the bare fallback." The contract's own "Gate between the two — REWRITTEN" section records this as a Gate A round-1 finding (W-1 alone only exercises the bare fallback) and the fix (W-1a added as the actual exit gate) — a documented root-cause repair, not an unexamined assertion.

## Membership Exactness and #210's Two Consequences

- [x] CHK020 Are #210's two consequences each required to be witnessed **by their own mechanism**, with substitution by the set-cardinality criterion explicitly forbidden? [Consistency, Spec §FR-010a / §FR-007a / §SC-013] — PASS: SC-013 states the forbidding rule verbatim ("fixpp#210's two consequences are closed by their own witnesses, not by SC-002's cardinality proxy"); FR-010a repeats it for Consequence 1 specifically ("MUST be closed by a **direct negative witness** on a named polluted context, not by SC-002's set-cardinality assertion alone"); FR-007a is the matching requirement for Consequence 2. All three agree on the same prohibition and the same named-context/named-witness mechanism.
- [x] CHK021 Is the negative outcome — the swallow proving unreachable — given a defined recording obligation, so the requirement cannot be silently dropped when the reproduction fails? [Completeness, Spec §FR-007a] — PASS: FR-007a states the obligation explicitly and names its location — "If measurement shows the swallow is unreachable, that negative result and the shapes tried MUST be recorded in Assumptions instead — the same standard FR-021a imposes on the splitter" — so a null result has a defined home rather than causing the requirement to quietly disappear.
- [x] CHK022 Is the false load-bearing source comment identified by **exact location** and distinguished from adjacent **true** text at the same site? [Clarity, Spec §FR-011] — PASS (verified at source): FR-011 states the comment is false and load-bearing but does not itself carry a line citation; `plan.md`'s Source Code tree supplies the exact location — "correct the FALSE sentence at `:507-508` … NOT `:503-505`, which is TRUE text about `members.front()` and must be left alone." Verified directly against `src/dictionary/dictionary.cpp`: lines `507-508` read "…so a reused tag with divergent per-context delimiters is an L-063-3 residual; the per-context MEMBER SET above stays exact regardless" — the false claim FR-011/C-3.2 target — while lines `503-505` describe `members.front()` being the lowest-tag member and are accurate as written. The citation is exact and the distinction holds at source (itself a correction plan.md records making twice before landing right, per its own note).
- [x] CHK023 Is the decision to **retain** the delimiter's member injection and pin it as a no-op stated, rather than leaving a reader to infer that the injection is deleted? [Clarity, C-3.3 / research.md §D-5] — PASS (verified at source): C-3.3 states the retention decision explicitly ("`set_group_first_ctx`'s existing member injection is **retained**… the call is at `include/fixpp/dict/table_view.hpp:645`… Once C-1.2 holds, the injected tag is always already a declared member, so the call is a no-op. The pin asserts this rather than the code removing it"), and research.md D-5 gives the rationale. Verified directly against `include/fixpp/dict/table_view.hpp:641-646`: the function signature spans `:641-642`, `set_group_bit` at `:643`, the `group_ctx_` write at `:644`, the `add_group_member_ctx` call at `:645`, closing brace at `:646` — matching C-3.3's citation exactly, after two recorded off-by-one corrections in the contract itself.

## Regression Pin — Non-Circularity, Proven RED, Exact Set

- [x] CHK024 Is the pin's independence specified as a **derivation rule** (expected values from a source sharing no code with the implementation, plus third-authority corroboration of a documented sample) rather than as the adjective "independent"? [Measurability, Spec §FR-013] — PASS: FR-013 states a rule, not an adjective — "The pin's expected values MUST be derived independently of the implementation under test, and a documented sample MUST be corroborated against a third independent authority (the code generator's own group-order data, or the recorded cross-loader values)." research.md D-8/D-9 name the concrete mechanisms (an additive `DictOracle.group_delims` map sharing no code with the loaders; codegen's `walk_level`/`GroupOrderEntry` or #208's tabulated values as the third authority), consistent with the FR text.
- [x] CHK025 Is the pin required to be **observed failing** against a reintroduced defect, with that demonstration recorded? [Measurability, Spec §FR-014 / §SC-006] — PASS: FR-014 states both halves — "the pin MUST be demonstrated to fail when the defect is reintroduced, and that demonstration MUST be recorded" — and SC-006/US4 Acceptance Scenario 3 restate it as the corresponding measurable outcome and test.
- [x] CHK026 Is member-set exactness required to ride the **same** pin rather than being emitted as a second half-pin? [Consistency, Spec §FR-015] — PASS: FR-015 states this with its own reasoning — "Per-context member-set exactness MUST be asserted by the same pin rather than as a separate assertion, since FR-002 makes it a consequence of FR-012 rather than an independent property" — matching D-5's "delimiter correctness subsumes member-set correctness" rationale in Assumptions.
- [x] CHK027 Is the prohibition on citing the existing collision-membership guards as delimiter coverage stated as a requirement, with the reason their green is a proxy gap? [Completeness, Spec §FR-016 / §US4] — PASS: FR-016 states the prohibition as a MUST NOT; US4's "Why this priority" gives the reason ("their discriminator tag is derived independently of the delimiter and is not necessarily the offending tag, so their green is a proxy gap rather than proof of absence").
- [x] CHK028 Is the pin's scope stated as **exact and carve-out-free** across all ten dictionaries, rather than as a subset or a per-dictionary exemption list? [Completeness, Spec §FR-012 / §SC-001] — PASS: FR-012 states it explicitly — "for every group context in all ten shipped dictionaries … with **no carve-out, exclusion list, or per-dictionary exemption**" — and US4 Acceptance Scenario 1 restates the same "no carve-out or exclusion list" language.
- [x] CHK029 Is the registered-group count change required to be justified **by naming the responsible groups**, rather than by asserting a bare number? [Measurability, Spec §FR-017 / §SC-005] — PASS: FR-017 states it ("any change to that count MUST be justified by naming the groups responsible"); SC-005 restates the measurable form ("the difference from today is fully accounted for by three named groups"), consistent with US3's own acceptance scenario wording.
- [x] CHK030 Is 072's load-time collision guard's dependence on the value this feature re-derives stated, together with the requirement to **re-derive** its census and re-state the inherited audit on the new basis? [Completeness, Spec §FR-012a / §SC-014] — PASS: FR-012a names the exact guard sites (`xml_loader.cpp:1016-1017`, `orchestra_loader.cpp:895-896`), states they read `first_field_tag`, "which this feature re-derives," and requires both the census (`reused_tag_census_test.cpp::NestedGroupDelimiterCensus`) and the L-063-4 "0 collisions" audit to be re-derived/re-stated on the post-fix basis; SC-014 restates the measurable outcome. C-7.2 in `loader_tolerant_mode.md` additionally states the write-ordering precondition this depends on (cross-referenced at CHK014).
- [x] CHK031 Are the pre-fix baseline figures required to **reconcile** against the checked-in pin's observed counts, with authority of record transferred from the scratch probe to the pin? [Measurability, Spec §SC-015] — PASS: SC-015 states the reconciliation requirement and the amendment path ("or every delta is explained in `research.md` and this spec is amended before Phase 2 begins"); research.md's "Measurement provenance" / "The probe is not checked in" section states the authority transfer explicitly — "from the moment `tests/dictionary/delimiter_census_test.cpp` first runs RED, **that pin is the authority** for every figure in spec.md's Baseline table, and `delim_probe3.cpp` is history."
- [x] CHK032 Is the one **projected rather than measured** figure distinguished from the measured ones, with the work that converts it named and sequenced first? [Clarity, Spec §SC-001 / §Baseline measurement] — DD-DECIDED §SC-015: deliberately-open item, not a gap. The Baseline measurement note states it directly — "The 335 figure excludes the 30 unregistered contexts … Their declared delimiters … are themselves nested-group count tags … the pin's post-fix denominator is 365, not 335. **This is the one figure in this table that is a projection rather than a measurement, and the first task of the feature is to convert it into one**" — and SC-001 restates the same 335-measured/30-projected/365-affected-set split with the conversion work named as sequenced through SC-015 (Phase-1 pin reconciliation, before Phase 2). The distinction and sequencing are stated; nothing here needs a spec edit.
- [x] CHK033 Do the dictionaries required to show no behavioural change form one set that agrees at every place it appears? [Consistency, Spec §SC-008] — PASS: the set {FIX40, FIX41, FIXT11} is identical at every appearance — spec.md's Edge Cases ("Dictionaries with zero affected contexts (FIX40, FIX41, FIXT11) must stay at zero"), SC-008 ("Dictionaries with no affected contexts today (FIX40, FIX41, FIXT11) show no behavioural change"), and the Baseline table, whose FIX40/FIX41/FIXT11 rows are independently all-zero across every column (wrong delimiter, polluted, unregistered). Note: the *correctness* of why these three are the unaffected set is a separate question research.md D-12 leaves open (branch (b) would narrow L-063-1's "group-blind" shorthand) — that is CHK005's territory, not this item's; this item only tests that the named set agrees everywhere it is used, which it does.

## Notes

- Check items off as `[x]` with exactly one inline disposition tag: `PASS` / `SPEC-FIXED` / `DD-DECIDED §X` / `WAIVED: <reason>`.
- Items tagged **Completeness / Clarity / Consistency** may **not** be closed as `WAIVED` (pipeline.md step 9).

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 31 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 2 |
| WAIVED | 0 |
| **Total** | 33 |

### SPEC-FIXED items

None. No defect requiring an edit to `spec.md` or an in-scope contract was found in this checklist's 33 items.

### DD-DECIDED items

- CHK005 — anchor `research.md §D-12`; rationale: D-12 is explicitly labelled OPEN, names its resolving Phase-1 instrumentation task, and states it is sequenced ahead of the Phase-3 loader change; `group_ctx_delims.md` C-3.4a marks the set-equality leg conditional on D-12's branch (b) exactly as the checklist item requires. Deliberately-open item per orchestrator brief — closing the open question is out of scope for this audit.
- CHK032 — anchor `Spec §SC-015` (Baseline measurement note / SC-001); rationale: spec.md's own Baseline note flags the 365 figure as "the one figure in this table that is a projection rather than a measurement," names the converting work ("the first task of the feature"), and SC-015 sequences its Phase-1 reconciliation before Phase 2. Deliberately-open item per orchestrator brief.

### WAIVED items

None.

### Source lookups performed

- `src/dictionary/dictionary.cpp:390-524` — legacy bare-store loop (`:398-420`), context-store loop and its gates (`:445-446`, `:463`), `immediate_parent` map (`:439-444`), delimiter resolution and the false/true comment boundary (`:498-511`, confirming FR-011's false sentence is at `:507-508` and `:503-505` is true text about `members.front()`).
- `src/dictionary/xml_loader.cpp:570-670` — component expansion passing `enclosing_group_no_tag` unchanged (`:576-579`), nested group's count-tag `FieldRef` pushed at the outer level before recursion (`:590-597`, recursion `:667`), the deleted one-level scan (`:610-641`), the collision guard (`:1016-1017`).
- `src/dictionary/orchestra_loader.cpp` — `expand_field_list` (`:520`), component expansion (`:573`), `first_member_tag` capture (`:629`) and recursion (`:650`), the collision guard (`:895-896`).
- `include/fixpp/dict/error.hpp:44,98` — `xml_parse_error` / `orchestra_parse_error` inheritance.
- `tests/fuzz/fuzz_orchestra_loader.cpp:52-80`, `tests/fuzz/fuzz_dict_xml_loader.cpp:45-72` — documented exception sets, confirming the Orchestra harness catches only the derived type.
- `include/fixpp/dict/table_view.hpp:330-378, 625-646` — `group_first_field`/`group_member_tags` bare-fallback shape, `set_group_first_ctx` (`:641-646`) with the `add_group_member_ctx` call at `:645`.
- `src/wire/offset_table.cpp:438-503` — `consume_group_extent`: bare `++k` at `:475`, inner-loop-only descent at `:485-488`, non-advancing overflow return at `:444`, unconditional `++inst` at `:500` (background verification for D-4a/FR-021e context; that requirement is outside this checklist's own CHK items but underlies CHK016's ordering-constraint citation of `consume_group.md`).
- `include/fixpp/dict/table_view.hpp:83-156` (`group_ctx_key`/`group_ctx_entry`) — realizability spot-check: fixed-size `std::array` key, no forward-declared-type ownership.

### Realizability sub-check

- **Entity 2 — Per-context delimiter table** (`data-model.md`): a new load-time PMR table keyed identically to the existing `table_view::group_ctx_` (fixed-size `std::array<std::uint16_t, K>` path, scalar `delimiter` field). Verified against the existing `group_ctx_key`/`group_ctx_entry` shape at `include/fixpp/dict/table_view.hpp:92-156`: no `unique_ptr`-to-forward-declared member, no by-value dependency on an incomplete type, no defaulted special member at risk. **Clean** — no realizability defect; this feature introduces no new value-typed class with an owned incomplete dependency in this checklist's scope.

### Anchors spot-verified

- `research.md D-1` (source-verified: `xml_loader.cpp:576-579,594-597,610-641,667`; `orchestra_loader.cpp:520,573,629,650`) — resolves.
- `research.md D-5` (source-verified: `table_view.hpp:641-646`) — resolves.
- `research.md D-10` (source-verified: `xml_loader.cpp:1016-1017`, `orchestra_loader.cpp:895-896`) — resolves.
- `research.md D-12` — resolves as an explicitly OPEN entry (verified it is stated as open, not silently treated as closed).
- `contracts/group_ctx_delims.md` C-1.4b, C-3.2 (source-verified: `dictionary.cpp:507-509`), C-3.3 (source-verified: `table_view.hpp:641-646`), C-3.4, C-3.4a (source-verified: `dictionary.cpp:446,463`), C-3.5, C-7.3 — all resolve.
- `contracts/loader_tolerant_mode.md` C-6.1a, C-6.1b (source-verified: `error.hpp:98`, both fuzz harnesses), C-7.1, C-7.2 — all resolve.
- `contracts/consume_group.md` C-4.1..C-4.4, W-1/W-1a/W-3 — all resolve; no source verification needed (Phase-2/4 code not yet written — these are forward-looking contract clauses for future implementation, not citations against existing code).

All anchors cited by this checklist's 33 items resolve in the signed-off bundle (`research.md`, the three in-scope contracts, `spec.md`, `data-model.md`) as of this audit.

### Re-run /speckit-analyze?

**NO.** Zero `SPEC-FIXED` dispositions — no edit was made to `spec.md` or any in-scope contract, so no prior `/speckit-analyze` drift check is invalidated by this audit.

### Verdict

**GREEN** — all 33 items dispositioned (31 PASS, 2 DD-DECIDED), zero un-dispositioned boxes, zero `Completeness`/`Clarity`/`Consistency` items closed as `WAIVED`, all cited anchors verified to resolve. Pipeline.md step 9 is satisfied for this checklist file.
