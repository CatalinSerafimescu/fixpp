# Implementation Plan: Group Delimiter Resolution

**Branch**: `083-group-delimiter-resolution` | **Date**: 2026-07-30 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/083-group-delimiter-resolution/spec.md`

## Summary

Resolve a repeating group's delimiter **per context** `(msg_type, ancestor-path, no_tag)` instead of dictionary-globally, in both loaders; make the receiver descend when an instance-opening delimiter is itself a nested group's count tag; align the C-ABI construction check and the typed-read instance splitter onto the same rule; and pin the result across all ten dictionaries with a carve-out-free document-order pin.

The central technical finding (research.md **D-1**) is that **no new document-order scan is required**. `LoaderState::expand_field_list` already emits `FieldRef`s in document order, expands components *inline* at the enclosing group's level, and pushes a nested group's own count-tag `FieldRef` at the **outer** level before descending. Therefore *the first `FieldRef` emitted at a group's level is, by construction, that group's document-order first member* — FR-003's component recursion and FR-004's nested-group delimiter both fall out for free. The fix is to capture that first emission per context, not to write a second traversal.

This matters beyond economy: the defect being fixed exists *because* the current `first_field_tag` scan (`xml_loader.cpp:610-641`) is a **separate** traversal that drifted out of step with the member expansion. Adding a third traversal would reproduce the failure mode. D-1 makes delimiter and member set derive from one walk, so they cannot disagree again.

## Technical Context

**Language/Version**: C++23 (Clang 22 primary, GCC Release sanity, MSVC Tier 2)

**Primary Dependencies**: pugixml (dictionary parsing, `src/` only — not exposed in public headers); GoogleTest; Google Benchmark

**Storage**: N/A — in-memory dictionary tables, PMR-allocated on a caller-supplied `memory_resource`

**Testing**: GoogleTest via `ctest -L <label>`; grouped isolation-safe buckets per Article VII §8

**Target Platform**: Linux (Tier 1: Clang Debug+Release, GCC Release), Windows/MSVC (Tier 2)

**Project Type**: C++ library (FIX engine) with a GA-frozen C ABI and SWIG Python bindings

**Performance Goals**: no regression beyond ±5% vs `bench/baselines/` (Article VIII §2), on **three** hot paths, not one *(scope corrected 2026-07-31, Gate A round 2 — N19)*:

1. **inbound validate** — delimiter resolution is called per offset-table entry (`include/fixpp/wire/validator.hpp:276`, `:376`);
2. **typed read** — C-8.1 replaces `std::uint16_t const delim = entries_[first].tag` (an array read) with a callback resolving to `table_view::group_first_field(msg_type, parent_path, no_tag)` — a `string_view` + path hash into `group_ctx_` (`include/fixpp/dict/table_view.hpp:349-365`) — and C-8.5 adds `consume_group_extent` recursion **inside** the boundary scan. Both land in `group_slices_status`, reached per group access from `MessageView::group<>()` and the C-ABI top-level group getter. **C-8.0c adds one further cost on the same path** *(Gate A round 3)*: one extra `group_member_fn_` evaluation **per group instance** at the delimiter position in `consume_group_extent`, plus a recursion where it fires. `consume_group_extent` is reached from `group()` (`:550`), which `group_slices_status` calls at `:642`, so `bench/wire/typed_read_group_bench.cpp` already covers it — but the bench MUST include a **mode-(c)** shape, or it measures the path that never descends and reports a budget met over the cheap branch;
3. **C-ABI commit** — C-9.1/C-9.2 replace the `O(1)` bare lookup at `src/capi/message_write.cpp:719` with a context-keyed lookup plus path maintenance through the `:725` recursion, executed per group instance on every `fixpp_msg_commit`; C-9.2a additionally adds one config-time `as_table_view()` per session (measured as session-open cost, not per-message).

Paths 2 and 3 did not exist when Gate A round 1 blessed Article VIII §3 as clean — the two contracts that create them were added *by* that round's rewrite. They are in scope for the bench, not carried as a narrative.

**Constraints**: zero `new`/`delete` between parse and `fromApp` (Article VIII §5); `FieldRef` is ABI-pinned at 16 bytes by `static_assert` and **must not grow**; C ABI GA-frozen at 1.5.0 — no exported signature may change; loader runs at init only, so construction-time exceptions are permitted there.

**Scale/Scope**: 10 dictionaries; FIX50SP2 alone has 25,897 group contexts and Orchestra 26,806 — the pin must be O(contexts) and cheap enough to run in Tier 1.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked after Phase 1 design.*

**Appendix A mandatory triggers** — this feature hits **four** trigger rows: *Wire format / parser* (validator changes, offset-table semantics), *Codegen layout* (dictionary loader), *ABI surface change* (behaviour reachable through the frozen C ABI), and arguably *Error semantics* (FR-006's new fail-closed load rejection). All four mandatory controls therefore apply:

| Control | Status |
|---|---|
| `/clarify` | ✅ Done — 4 questions, session 2026-07-30, recorded in spec.md |
| `/analyze` | ⏳ Pipeline step 6, after `/speckit-tasks` |
| Codex Gate A | ⏳ Pipeline step 4, **before** `/speckit-tasks` |
| User `/plan` sign-off | ✅ **Signed off by the user 2026-07-30**, after review of D-1 (first-emission capture), D-7/C-7.1 (fail-closed precondition) and D-6 (splitter characterise-first) |

| Article | Requirement | How this plan satisfies it |
|---|---|---|
| **VI §4, §5, §6** | Bidirectional traceability; **Normative References** in every `/specify` artifact; catalogue closure conditions | **Added at Gate A round 1 — this row was missing while the gate result asserted PASS.** §5: `spec.md` now carries a `## Normative References` section anchored at `spec/coverage-index.md:184` (§3.2 repeating groups), `:189` (§3 validator) and `:181` (§3 tag=value), plus the engaged B&L rows. Per the 079 adjudication (`specs/079-required-presence-scope/plan.md:101`) §5's operative reading is **`spec.md` only** — matching merged precedent 074/075/076/078 — so it is not replicated across plan/research/data-model/contracts. §4: **no new OFFICIAL catalogue row is introduced**, so no coverage-index entry must precede a landing row; every requirement is correctness of existing rows W-001/W-006/W-007/W-014/D-010. §6: closure conditions are met by the FR-019 B&L rows, the FR-017 count justification, and the L-063-3 / L-063-4 / L-063-1 row updates at close-out. |
| VII §3, §4 | TDD mandatory; no code without a test | Every phase below is RED-first. Phase 1 is *entirely* RED — including the wire repro that closes the spec's one open inference (FR-001 premise) — and is observed failing before any fix. SC-012's C-ABI witness and SC-010's typed-read witness now have named artifacts (Project Structure), which they did not at round 1. |
| VII §7 | Fuzzing for parser-touching modules | Three harnesses, named: `tests/fuzz/fuzz_wire_validator.cpp` gains a nested-delimiter corpus seed for the `consume_group` descent; `tests/fuzz/fuzz_dict_xml_loader.cpp` and `tests/fuzz/fuzz_orchestra_loader.cpp` each enumerate a **documented exception set** and rethrow anything else as an invariant violation, so FR-006's new throw must land **inside** each set. Per-loader type (FR-006c) achieves that with no harness widening — a shared base `xml_parse_error` thrown from `OrchestraLoader` would escape its harness's derived-type catch and crash the fuzzer on every unresolvable-group input. |
| VII §8 | Group isolation-safe tests; select by label | New dictionary tests join the existing `dictionary_pure_tests` bucket; `ctest -L dictionary` and `-L wire`. The census/pin test is an **exact-set completeness gate** → stays standalone per §8. |
| VIII §2, §3 | ±5% budget; **no perf change without a bench in the same PR** (`.specify/constitution.md:186`) | FR-022, **re-scoped at Gate A round 2 to all three hot paths this feature touches** (Performance Goals above). Benches added in this PR: `bench/wire/validate_group_bench.cpp` (inbound validate), `bench/wire/typed_read_group_bench.cpp` (typed-read group-slice split — C-8.1/C-8.5), `bench/capi/capi_commit_group_bench.cpp` (C-ABI commit group grammar — C-9.1/C-9.2/C-9.2a) — the repo's per-subsystem layout, agreeing with the Source Code tree below and with tasks T063/T064/T065. *(Paths corrected 2026-07-31 at the `/speckit-analyze` re-run, F6: this row still carried the flat `bench/<name>.cpp` paths after `/speckit-checklist` corrected the Source Code tree, so plan.md contradicted itself three sections apart.)* Baselines updated in-PR with rationale if a change is intentional. Round 1 recorded this row as a clean pass over one bench, which was correct **at the time** — `typed_read_splitter.md` and `capi_group_grammar.md` were added by that same rewrite and each created a hot path the single bench does not cover. FR-022's own wording (*"a narrative assertion that the budget was met is not acceptable in place of the benchmark"*) forecloses discharging the two new paths in prose. |
| IX §1 | ≥95% line / ≥85% branch on touched modules; no silent uncovered error path | Touched: `src/dictionary/`, `include/fixpp/dict/`, `include/fixpp/wire/`, `src/wire/`, `src/capi/`. **Scoped at Gate A round 1; enumeration completed at round 2 (N20)** — the original claim rested on FR-006b alone, and round 1's replacement said "all five" while the same rewrite introduced more than five. The **cardinality claim is dropped**; what matters is that the enumeration is exhaustive and each entry has a witness: (1) FR-006 fail-closed load rejection → FR-006b W-4; (2) FR-006a tolerant skip → FR-006b W-5; (3) the descent-taken vs descent-not-taken split in `consume_group` (C-4.1) → W-1 *and* W-1a, the latter on a populated context store; (4) the Entity-2 completeness invariant (FR-023, C-3.4) → `LoaderDisposition.ContextWithoutDelimiterRecordRejectedAtFinalize` (**AMENDED 2026-08-02, Gate B r1 F1**: the throw itself was unreachable by an ordinary dialect per C-7.3's zero-population measurement, so this case now uses a runtime `detail::` test seam — `set_force_incomplete_group_context_for_testing` / `maybe_drop_first_group_ctx_delim_run_for_testing`, `src/dictionary/dictionary_internal.hpp` — to truncate a real dictionary's delimiter run immediately before both loaders' check, so `find_incomplete_group_context` genuinely misses and the throw fires for cause; the Orchestra twin is `LoaderDisposition.ContextWithoutDelimiterRecordRejectedAtFinalizeOrchestra`, and `LoaderDisposition.FindIncompleteGroupContextDetectsMissingRecord` covers the detector's found-branch directly with no seam; the surviving property pin over all ten shipped dictionaries is renamed `LoaderDisposition.AllShippedContextsHaveADelimiterRecord`), enforced at `finalize()` so it reuses FR-006's covered disposition rather than adding an uncovered fatal path; (5) *(**AMENDED 2026-08-02, Gate B r1 F2** — this row previously read "the splitter's nested-extent skip (C-8.5) → W-8/W-9". C-8.5 was DESCOPED 2026-08-01 on T054's measured zero population across all ten dictionaries, so **there is no such branch in the delivered code** and a witness cannot be named for it without inventing one — the false-green shape T079 exists to catch. The eleven-branch map is therefore **ten landed branches plus this one explicitly retired**, recorded as retired rather than silently renumbered.)* the splitter's **dictionary-sourced boundary delimiter** (C-8.2 / T058) is the branch that DID land at this site → **W-10** (`OutOfScopeWireProbesUnchanged`) **and W-9 fixture (c)**; (6) C-6.1a / C-3.6's discriminator — "reachable from message expansion, first member unresolvable" (fail closed) vs "declared but reachable from no message expansion" (informational) → `LoaderDisposition.DeclaredGroupWithZeroContextsDoesNotFailClosed`; (7) FR-006c's per-loader throw type → `LoaderDisposition.OrchestraRejectionIsOrchestraParseError`; (8) FR-023a's tolerant-mode twin of branch (4) → `LoaderDisposition.ContextWithoutDelimiterRecordTolerantModeSkipsGroup` *(this was the one genuine gap: round 1 promised the twin in C-3.4's Witness prose and named no case)*; (9) C-3.4a's `!members.empty()` exclusion — scalar reuse of a group tag is not a completeness violation → `LoaderDisposition.ScalarReuseOfGroupTagIsNotACompletenessViolation`; (10) the C-ABI commit's dictionary-present vs dictionary-absent split under C-9.2a/C-9.4 → W-11 and W-11b; **(11) C-8.0c's descend-taken vs descend-not-taken split at `consume_group_extent`'s delimiter position, plus its `overflow` early-return** → W-10a legs 1–3 (taken) and leg 4 (overflow), with W-10's mode-(a)/(b) fixture covering not-taken *(added Gate A round 3: the scope amendment adds a branch, so an enumeration that claims to be exhaustive has to grow with it)*. D-10's global-projection repopulation is not a new branch — it re-sources an existing one — and is pinned by W-13. No branch is left without a witness. |
| IX §2 | ASan/UBSan/TSan Tier 1 | Recursion depth change in `consume_group` → ASan stack coverage matters; depth stays bounded by the existing K=16 cap (FR-009). |
| X §1, §6 | C ABI is a versioned contract; ABI-affecting ⇒ all four controls | FR-018a: **no exported signature changes**. `validate_group_grammar` is a file-static function; C-9.2a's added member lives on the internal `fixpp_session`/`fixpp_msg` structs in `src/capi/capi_internal.hpp`, which are not part of the C header. Behaviour change is disclosed via the FR-019 / **FR-019a** (three classes) / **FR-019b** (the artifact is `spec/behaviors-and-limitations.md`; there is no `CHANGELOG.md`) B&L row (this repo's release-note artifact — no `CHANGELOG.md` exists). |
| XV §1 | Banned patterns — no per-message/per-field heap allocation on the hot path (`.specify/constitution.md:295`); `as_table_view()` is config-time-only by its own `[const §XV.1]` annotation | No new `std::mutex` in awaitable headers; no hot-path allocation added — see D-3 storage decision. **Added at Gate A round 2**: FR-018b / C-9.2a explicitly refuses `dict->as_table_view()` inside `fixpp_msg_commit` and specifies a session-cached view built once at `fixpp_session_open` instead — one `as_table_view()` per session, zero per message. W-11a is the pin, because no behavioural witness can tell the two routes apart. |
| XVI §3 | `/clarify` mandatory before `/plan` | ✅ Completed. |
| XVII | Codex Gate A before `/tasks` | Planned as pipeline step 4. |

**Gate result: PASS** — no violations to justify, now over a table that evaluates **every** article this feature engages. (At Gate A round 1 this PASS was asserted over a table with no Article VI row while Article VI §5 was in fact unmet; both are fixed above.) Complexity Tracking section omitted deliberately (no entries).

**Three risks the gates do not catch, carried forward explicitly:**

1. FR-006's fail-closed default could refuse a dictionary that loads today. D-7 makes "all ten shipped dictionaries still load under the default" a **precondition task that runs before FR-006 is enabled**, not an after-the-fact check.
2. The bare global lookup is an *is-this-a-group* **predicate** at C-ABI construction sites, not only a delimiter source. Deleting the scan that feeds it without repopulating it would make the C ABI reject **all** groups — a total regression through a frozen ABI, in a change whose purpose is to fix rejections. D-10 makes repopulation an explicit decision, not a task-level detail; W-13 pins it.
3. **An unexplained population path may exist (research.md D-12).** FIX42's tag 146 is `INT`-typed (`dictionaries/FIX42.xml:2084`) and `field_ref("R",146).type` is `Int`, yet a genuine per-context entry for `(R,{},146)` demonstrably exists (measured: ctx size 32 vs bare size 19, distinct storage; tag 382 in the same dictionary resolves as a real bare fallback, so the discriminator works). The baseline numbers are verified either way, but the *mechanism* is not explained by the code path this bundle documents. **If contexts reach the store by a path D-1 does not cover, D-1's loader-side capture does not cover every context** and the FR-023 completeness invariant fails at load on a shipped dictionary. It also makes `group_ctx_delims.md` **C-3.4a**'s set-equality leg conditional: `finalize()` can only reproduce the consumer's enumeration if `fr.type == NumInGroup` means the same thing on both sides, which is exactly what D-12 unsettles. Resolving it is a **Phase-1 task ahead of the Phase-3 loader change** for that reason — **re-posed at Gate A round 2** to instrument the `fr.type` predicate rather than "which of two call paths", since there is one production call site for each and both sit behind the same `dictionary.cpp:446` gate, so the round-1 phrasing had no discriminating answer and the Phase-1 net would have collapsed into C-7.3's Phase-3 net alone.

## Project Structure

### Documentation (this feature)

```text
specs/083-group-delimiter-resolution/
├── plan.md              # This file
├── research.md          # Phase 0 — D-1..D-11 + D-13 decisions, + D-4a (Gate A r3); D-12 OPEN (Phase-1 task, deliberately NOT closed)
├── data-model.md        # Phase 1 — entities + the new side table
├── quickstart.md        # Phase 1 — how to reproduce, verify, and disprove
├── contracts/           # Phase 1 — surface contracts
│   ├── group_ctx_delims.md      # FR-001..005/006d/010/011/015/023/023a/023b — loader → handle → table_view delimiter path
│   │                            #   + Consumer contract C-3.4..C-3.7 (FR-023, Gate A r1); + C-3.4a (Gate A r2 — the checked SET at finalize())
│   ├── consume_group.md         # descend-at-delimiter contract (C-4.1; FR-007..009, W-1/W-1a/W-3)
│   │                            #   + C-4.4 (Gate A r3 — POINTER to the same asymmetry's second site, repaired by C-8.0c; adds no Phase-2 work)
│   ├── loader_tolerant_mode.md  # FR-006/006a/006b/006c/006d/012a/017, SC-005/011/014 — load disposition
│   ├── typed_read_splitter.md   # NEW (Gate A r1) — FR-021/021a/021b/021c/021d/021e, FR-022 path 2, SC-009/SC-010/SC-016
│   │                            #   + C-8.0b (Gate A r2 — the out-of-scope BOUNDARY roles, and why #180 is not discharged)
│   │                            #   + C-8.0c (Gate A r3 USER SCOPE AMENDMENT — C-8.0b's :454 bullet WITHDRAWN; the extent walk descends at the
│   │                            #     instance-opening delimiter. W-10 re-stated, W-10a added, W-9 widened to two fixtures)
│   └── capi_group_grammar.md    # NEW (Gate A r1) — FR-018/018a/018b, FR-022 path 3, SC-009/SC-012
│                                #   + C-9.2a (Gate A r2) — the named route from the C-ABI handle to Entity 2
├── checklists/
│   └── requirements.md  # closed at steps 1–2
└── tasks.md             # Phase 2 — NOT created by /speckit-plan
```

### Source Code (repository root)

```text
include/fixpp/dict/
├── field_ref.hpp              # UNCHANGED — 16-byte ABI static_assert must hold
├── table_view.hpp             # ctx delimiter accessor already exists; set_group_first_ctx KEEPS its member injection (D-5 / C-3.3) — the pin asserts it is a no-op, the code does not remove it
├── xml_loader.hpp             # + tolerant-mode load option (FR-006a)
├── orchestra_loader.hpp       # + same option, symmetric (FR-005); + documented exception list at :33-37 updated for FR-006c
└── error.hpp                  # UNCHANGED — no new type, no core::error slot (FR-006c; test_020 slot pin not engaged)

include/fixpp/wire/
├── validator.hpp              # consume_group: descend-at-delimiter (FR-007..009)
└── offset_table.hpp           # + group_delim_fn_t sibling of group_member_fn_t (:79-80), internal seam only (C-8.1)

src/dictionary/
├── xml_loader.cpp             # D-1 first-emission capture; D-2 path threading; delete the one-level scan at :610-641 AND repopulate the global from the new table (D-10); FR-023 completeness invariant in finalize(); projection written BEFORE the :1016 collision guard reads it (C-7.2)
├── orchestra_loader.cpp       # symmetric twin (verified same shape) — first_member_tag (:629) replaced by the same capture; same invariant + same guard ordering (:895-896)
├── dictionary_internal.hpp    # + per-context delimiter side table + accessor; + fixpp::dict::detail::as_table_view_call_count() test seam for W-11a
│                              #   (INTERNAL header, mirroring capi_internal.hpp:496-503's live-state counter — NOT the public dict/dictionary.hpp)
└── dictionary.cpp             # as_table_view(): consume the ctx delimiter at :510-511 → :518; correct the FALSE sentence at :507-508 (FR-011) —
                               #   "the per-context MEMBER SET above stays exact regardless". NOT :503-505, which is TRUE text about
                               #   members.front() and must be left alone (citation corrected Gate A r3, N24: this tree is what
                               #   /speckit-tasks reads, so the wrong range would have become a task editing the correct comment)

src/wire/
└── offset_table.cpp           # TWO edits (scope amended Gate A r3, N23 — round 2 said ":656 splitter ONLY"):
                               #   (1) :656 splitter — dictionary-sourced delimiter keyed on stored_group_context() (NOT group_context_for,
                               #       C-8.2), skip nested extents (FR-021, C-8.1..C-8.5)
                               #   (2) :475 consume_group_extent — DESCEND at the instance-opening delimiter instead of the bare ++k, when
                               #       `delim` heads a group in the `child` ctx already computed at :466; probe shape as :485-486, recurse via
                               #       consume_group_extent under the existing kMaxGroupDepth guard, and MIRROR :489-491's `if (overflow) return k;`
                               #       — the depth-cap branch returns count_idx UNCHANGED (:444), so omitting it makes the :473 loop burn `declared`
                               #       NO-OP ITERATIONS (it does NOT spin — the loop is bounded by inst < declared with an unconditional ++inst at
                               #       :500; corrected at Gate A fresh loop r1). `declared` saturates at UINT32_MAX (tag_scan.hpp:63-79) with no
                               #       instance-count cap, so the exposure is a DoS, not a hang — see C-8.0c.3
                               #       (FR-021e, C-8.0c, SC-016, W-10a). This is a TRAVERSAL change: :454's local and :457's probe stay wire-derived
                               #   :454 / :526 / :597 keep their MEMBERSHIP-PROBE role wire-derived (FR-021c, C-8.0); :526's instance-BOUNDARY
                               #   role (:559-570) remains scoped out on its own grounds (C-8.0b) — untouched here

src/capi/
├── capi_internal.hpp          # + session-cached table_view on fixpp_session beside dict_ (declared :493), built once at fixpp_session_open —
│                              #   the point where src/capi/session.cpp:109-111 caches dict_; non-owning pointer copied into fixpp_msg at
│                              #   create_outbound exactly as src/capi/message_write.cpp:289-291 copies dict_ into the member declared at
│                              #   :261-266. Internal struct — no exported surface (C-9.2a / C-9.3). (Copy-site citations added Gate A r3, Codex #1)
├── session.cpp                # BUILD SITE for the above — fixpp_session_open caches dict_ at :109-111; the session table_view is built there,
│                              #   once per session, and its lifetime is the session's (C-9.2a). A dictionary-less handle keeps C-9.4's
│                              #   existing no-delimiter-check behaviour unchanged. (Added to this tree at /speckit-checklist, 2026-07-31: C-9.2a
│                              #   mandates this edit and tasks.md T050 already schedules it, but the file was absent here — a plan-vs-tasks
│                              #   Consistency drift that `/speckit-checklist-audit` cannot waive.)
└── message_write.cpp          # validate_group_grammar (:710-730): thread msg_type + path through the existing :725 recursion and resolve
                               #   through the session-cached table_view — NEVER dict->as_table_view(), which is [const §XV.1]-barred on the
                               #   per-message commit path (FR-018, C-9.2 / C-9.2a). Predicate sites :157 / :812 / :923 NOT converted (C-9.5)

tests/dictionary/
├── required_scope_oracle.hpp        # ADDITIVE: new group_delims map beside group_members (D-8)
├── delimiter_census_test.cpp        # NEW — the all-ten-dictionary pin (FR-012..015) + named cases:
│                                    #   DelimiterCensus.RegisteredGroupCountMatchesCodegenFix50Sp2  (SC-005 / FR-017)
│                                    #   DelimiterCensus.NoChangeDictionariesUnchanged               (SC-008: FIX40/41/FIXT11)
│                                    #   DelimiterCensus.RedCountsReconcileWithSpecBaseline          (SC-015 / N9)
├── loader_disposition_test.cpp      # NEW — FR-006/006a both dispositions + named cases:
│                                    #   LoaderDisposition.ContextWithoutDelimiterRecordRejectedAtFinalize     (FR-023 / C-3.4)
│                                    #   LoaderDisposition.ContextWithoutDelimiterRecordTolerantModeSkipsGroup (FR-023a / C-3.4 — Gate A r2)
│                                    #   LoaderDisposition.ScalarReuseOfGroupTagIsNotACompletenessViolation    (C-3.4a !members.empty() leg — Gate A r2)
│                                    #   LoaderDisposition.DeclaredGroupWithZeroContextsDoesNotFailClosed      (FR-006d / C-6.1a)
│                                    #   LoaderDisposition.OrchestraRejectionIsOrchestraParseError             (FR-006c / C-6.1b)
└── reused_tag_census_test.cpp       # EDITED — NestedGroupDelimiterCensus re-derived under POST-FIX delimiters (FR-012a / SC-014 / C-7.2); L-063-4 disposition re-stated

tests/wire/
├── consume_group_nested_delim_test.cpp   # NEW — #208 B-2 repro (FR-007), W-1 (bare) + named cases:
│                                         #   ConsumeGroupNestedDelim.NestedDelimiterDescendsOnPopulatedContextStore (W-1a — the Phase-2 exit witness)
│                                         #   ConsumeGroupNestedDelim.NestedDelimiterAtDepthCapIsBoundedNotUnbounded   (W-3 — FR-009 / US2 scenario 4;
│                                         #     named at Gate A r3, N25(b): W-3 was the ONLY witness for FR-009 and had no case and no file)
├── delimiter_divergence_wire_test.cpp    # NEW — the FR-001 premise repro + #210's two consequences + SC-004's wire leg:
│                                         #   DelimiterDivergenceWire.PollutedContextAcceptsInjectedTagTodayRejectsAfter (FR-010a / SC-013 / #210 C1)
│                                         #   DelimiterDivergenceWire.TrailingMessageFieldNotSwallowedIntoLastInstance   (FR-007a / SC-013 / #210 C2)
│                                         #   DelimiterDivergenceWire.NamedCountTagSubsetAccepts                         (SC-004: 1677,1772,40204,41599,42060,1499,1669,1919)
├── typed_read_split_agreement_test.cpp   # NEW — W-9/W-10/W-10a (FR-021b / FR-021c / FR-021e / SC-010 / SC-016) + named cases:
│                                         #   TypedReadSplitAgreement.ValidatedInstanceCountEqualsTypedReadInstanceCount (W-9 — over BOTH fixtures:
│                                         #     (b) delimiter tag reappears at depth, (c) delimiter IS a nested group's count tag — Gate A r3)
│                                         #   TypedReadSplitAgreement.OutOfScopeWireProbesUnchanged                     (W-10 — fixture RE-STATED at
│                                         #     Gate A r3 to EXCLUDE mode (c); as written at r2 it pinned the C-8.0c defect as correct)
│                                         #   TypedReadSplitAgreement.ExtentWalkDescendsAtNestedGroupDelimiter          (W-10a — NEW Gate A r3;
│                                         #     4 legs: extent by construction, N-and-1 instance counts, agreement with validation, depth-cap RETURNS
│                                         #     IMMEDIATELY rather than burning `declared` no-op iterations (leg 4 RE-SPECIFIED at Gate A fresh loop
│                                         #     r1: fixture MUST carry declared >= 2 AT THE CAP-HITTING FRAME (not the outermost — the outermost
│                                         #     count is invariant un-mirrored too, so varying it false-greens) and the leg is decided by a
│                                         #     group_member_fn_ INVOCATION COUNT asserted INVARIANT across that frame's declared=2 vs
│                                         #     declared=8, via a counting wrapper on the existing
│                                         #     construction-time callback seam — no production change; TIMEOUT is a BACKSTOP, not the discriminator.
│                                         #     Leg 1 must be OBSERVED RED before C-8.0c lands; leg 4 cannot be — pre-fix the branch does not exist)
└── nested_group_extent_test.cpp          # EDITED — W-8 splitter characterisation (FR-021a / FR-021d), beside the existing synthetic at :511-519

tests/capi/
└── capi_group_delimiter_ctx_test.cpp     # NEW — W-11/W-11a/W-11b/W-12/W-13 (FR-018/018a, FR-019, SC-012); W-11a pins the
                                          #   config-time-only as_table_view() route ([const §XV.1] / C-9.2a) — the only witness that
                                          #   distinguishes the chosen route from the banned per-commit rebuild

tests/fuzz/
├── fuzz_wire_validator.cpp         # EDITED — nested-delimiter corpus seed (const §VII.7)
├── fuzz_dict_xml_loader.cpp        # EDITED — documented exception set comment covers FR-006's throw (:50-72)
└── fuzz_orchestra_loader.cpp       # EDITED — same, derived-type catch already covers it given FR-006c (:52-80)

bench/                            # PER-SUBSYSTEM layout — the repo's actual shape (bench/wire/, bench/core/, …), NOT a flat bench/<name>.cpp.
                                  #   (Corrected at /speckit-checklist, 2026-07-31: this tree previously wrote all three flat, disagreeing with
                                  #   tasks.md T063/T064/T065, which already use the per-subsystem homes. bench/wire/ exists; bench/capi/ is NEW.)
├── wire/
│   ├── validate_group_bench.cpp  # inbound validate path (FR-022, path 1) — alongside the existing bench/wire/validator_bench.cpp
│   └── typed_read_group_bench.cpp # NEW (Gate A r2) — typed-read group-slice split: ctx-keyed delimiter callback + nested-extent
│                                 #   recursion in group_slices_status (FR-022 path 2, C-8.1/C-8.5)
├── capi/                         # NEW directory — needs its own CMakeLists.txt, added by the same task
│   └── capi_commit_group_bench.cpp # NEW (Gate A r2) — fixpp_msg_commit group grammar: ctx-keyed lookup + path maintenance through
│                                 #   the :725 recursion, plus the one config-time as_table_view() per session (FR-022 path 3, C-9.2/C-9.2a)
└── baselines/wire/, baselines/capi/  # recorded baselines for the three benches (Article VIII §3)
```

**Structure Decision**: existing module layout is unchanged; this feature edits in place across `dict`, `wire`, and `capi` and adds **six** new test files and **three** benchmarks, and **edits six** existing test/fuzz files rather than duplicating their subsystems. No new module, no new library target, no new public header — so `check_layers.py` has no new boundary to evaluate. The only *new* runtime data structure is the per-context delimiter side table (data-model.md Entity 2), which lives on the existing metadata handle rather than in a new component; C-9.2a additionally caches one `table_view` on the **internal** `fixpp_session` struct, which is not a public or C-ABI surface. *(Gate A round 1 added: the two new contracts, the C-ABI and typed-read test files, the three fuzz harnesses, the `reused_tag_census_test.cpp` re-census, and named cases for the five success criteria that previously had no artifact — SC-004's wire leg, SC-005, SC-008, SC-010, SC-012. Gate A round 2 added: two benches for the typed-read and C-ABI hot paths, `src/capi/capi_internal.hpp` to the source tree, and three named cases — the FR-023a tolerant twin, the C-3.4a scalar-reuse exclusion, and W-11a/W-11b. **Gate A round 3 (user scope amendment) added no files and no benches** — a second edit to `src/wire/offset_table.cpp` (C-8.0c) and two named cases in test files this tree already lists: W-10a in `typed_read_split_agreement_test.cpp` and W-3 in `consume_group_nested_delim_test.cpp`. The counts below are therefore **unchanged and re-confirmed against this tree at round 3**.)*

**Counts, recounted from this tree at Gate A round 2** — round 1 said "seven test files… and edits four", and both were wrong against its own tree; this project treats a small unreconciled count as a silent-omission detector, and this paragraph is what `/speckit-tasks` reads to size the feature. New (6): `delimiter_census_test.cpp`, `loader_disposition_test.cpp`, `consume_group_nested_delim_test.cpp`, `delimiter_divergence_wire_test.cpp`, `typed_read_split_agreement_test.cpp`, `capi_group_delimiter_ctx_test.cpp`. Edited (6): `required_scope_oracle.hpp`, `reused_tag_census_test.cpp`, `nested_group_extent_test.cpp`, and the three fuzz harnesses. Benchmarks (3): `validate_group_bench.cpp`, `typed_read_group_bench.cpp`, `capi_commit_group_bench.cpp`.

## Implementation Phasing

Ordering is dictated by one hard constraint: **FR-007 (descend-at-delimiter) must land before FR-003 (recursive resolution)**, or the recursive fix converts a wrong-delimiter defect into a false rejection across 232 measured contexts plus the 30 that newly register. Phases 2 and 3 are therefore not interchangeable.

**Gates rewritten 2026-07-30 (Gate A round 1).** Every gate below was previously stated over an artifact the phase in question does not move: Phase 1 *recorded* counts without reconciling them, Phase 2's gate was a dictionary-level failure count a validator-only phase cannot change (and its only witness resolved through the bare fallback rather than the context store Phase 3 populates), Phase 3 had no wire leg so SC-004's witnesses landed nowhere, and Phase 5 scheduled edits to a branch D-8 says cannot be observed. Each gate is now an assertion over something its own phase changes.

| Phase | Content | Gate to proceed |
|---|---|---|
| **1 — RED** | Wire repro for the FR-001 premise (divergent context rejects, first-seen accepts); #208 B-2 two-instance repro; #210's two consequences (FR-010a false-accept, FR-007a swallow); the all-ten delimiter pin. Baseline the 30 unmeasured ctxMISS contexts (spec's one projected figure). **Resolve D-12** — *task re-posed at Gate A round 2 (N18)*: instrument the **`fr.type` predicate**, not "which of the two `set_group_*_ctx` call paths" (there is one production site for each and both sit behind the same `:446` gate, so that question has no discriminating answer). Log `fr.type` for tag 146 as the `src/dictionary/dictionary.cpp:446` loop over `message_fields("R")` sees it, compare against `Dictionary::field_ref("R",146).type`, with a control tag in the same dictionary. | All observed **failing** — a pin never seen red proves nothing — **and the RED counts RECONCILED against 335 / 52 / 30 / 232** (SC-015). A delta is not automatically a spec error; it must be explained in `research.md` as falsifying the probe, the pin, or the spec, and `spec.md` amended, before Phase 2 starts. From here the pin, not the deleted scratch probe, is the authority of record. **D-12 answered and recorded**: branch (a) ⇒ D-1/D-3 amended **and `group_ctx_delims.md` C-3.4a's checked set re-derived** before Phase 3 — C-3.4a's set-equality leg is explicitly conditional on branch (b); branch (b) ⇒ L-063-1 and the #196/082 handoff amended. |
| **2 — Receiver** | FR-007/008/009 descend-at-delimiter. Touches `include/fixpp/wire/validator.hpp` only. | **(i)** W-1a green — the nested-delimiter descent proven on a **populated context store**, not only the bare fallback (`consume_group.md`, rewritten gate). W-1 alone is insufficient: a hand-built `table_view` never populates `group_ctx_` (`include/fixpp/dict/table_view.hpp:346-349`), so it exercises only `:364`'s bare path, while C-4.1's descent is a context-keyed query. **(ii)** the ten dictionaries' **currently resolved delimiters are unchanged** — the real content of "must not touch resolution". *(The old gate, "no change to the delimiter pin's failure count", is deleted: a validator-only phase cannot move a dictionary-level count, so it could never fire.)* |
| **3 — Loaders** | D-1 capture + D-2 path threading, both loaders symmetrically; delete the one-level scan; repopulate the global projection (D-10); FR-006/006a/006c/006d dispositions; FR-023 completeness invariant in `finalize()`. **And C-8.0c / FR-021e — specified in `typed_read_splitter.md` and listed in Phase 4's content — must have LANDED by this exit; see gate leg (iv).** *(Duplicate mention added at Gate A fresh loop round 2, not a re-homing: `/speckit-tasks` derives ordering from this column, and emitting the C-8.0c task inside the Phase-4 block after a Phase-3 gate task requiring W-10a green would be an unsatisfiable order.)* | Delimiter pin green on all ten; FIX50SP2 502→505 with the delta accounted for by three named groups; all ten still load under the fail-closed default (C-7.1); **plus, added at round 1: (i)** SC-004's named eight-tag **wire** subset run and green *at this exit*, not deferred — otherwise the tree can sit between Phase 3 and Phase 5 in exactly the strictly-worse state the ordering constraint forbids, with every gate green; **(ii)** C-7.2 — nested/parent delimiter collisions re-censused under the **post-fix** delimiters and L-063-4's disposition re-stated (FR-012a / SC-014), with the projection write ordered before `finalize()`'s guard reads it; **(iii)** C-7.3 — all ten dictionaries confirmed to satisfy the FR-023 completeness invariant **before** that check is enabled, run as a **both-directions set diff** against the checked set C-3.4a defines (registered-with-no-record = violation; checked-set-not-registered = C-3.7 dead data, recorded). FR-023 adds a *third* new fail-closed load path, and like C-7.1's it must not meet the shipped set for the first time in CI. **(iv)** *(added at Gate A fresh loop round 1)* **W-10a green *at this exit*, not deferred to the Phase-4 exit** — which requires **C-8.0c to land by Phase 3's exit**. C-8.0c is **not blocked by** Phase 3 (its delimiter is wire-derived and it needs no per-context delimiter record — C-8.6a), so it can land any time from Phase 2 onward; pulling its witness here removes the Phase-3-to-Phase-4 window entirely, in the same shape SC-004's wire leg was pulled into this exit at round 1. This is a **gate-leg addition, not a re-homing of the contract clause**: C-8.0c stays specified in `typed_read_splitter.md` and stays listed in Phase 4's content, because a gate leg asserts the tree's state at a phase exit and does not say which phase authored the code. **Intermediate phase exits are not releasable states for this feature** — the merge unit is the single PR, and Gate B and all three tier matrices run on the PR head, not on phase boundaries. |
| **4 — Consumers** | `as_table_view()` consumes the ctx delimiter; FR-011 comment fix; C-ABI threading (FR-018, `capi_group_grammar.md`); splitter (FR-021, `typed_read_splitter.md`, characterise first); **the extent walk's delimiter-position descent (FR-021e, C-8.0c)** *(added at Gate A round 3; at Gate A fresh loop round 1 its witness **W-10a was pulled to the Phase-3 exit**, so C-8.0c lands by then — it is still listed here because this is where the rest of `offset_table.cpp`'s edits land, and a gate leg asserts a tree state rather than an authoring phase)*. | **Gate RE-STATED at Gate A round 3 — as written at round 2 it could go GREEN while SC-010 and FR-021b were false for 262 of the 365 affected contexts.** Member-set exactness falls out (FR-015); **W-11/W-11a/W-11b/W-12/W-13** green — C-ABI and validator agree in both directions on a **nested, divergent** context, the commit-side view is built once per session and never per message (W-11a, `[const §XV.1]` / C-9.2a), dict-free handles keep their pre-083 disposition (W-11b / C-9.4), the five disclosed delimiter moves are pinned, and every registered group still answers the bare predicate non-zero (C-9.5/D-10's total-regression scenario); **W-8/W-9/W-10/W-10a** green, in the corrected division of labour: **(i)** splitter characterised (W-8); **(ii)** typed-read and validation agree on count *and boundaries* on a **divergent context under a non-empty parent path**, over **both** W-9 fixtures — (b) delimiter tag reappearing at depth **and (c) delimiter that IS a nested group's count tag**; round 2 mandated (b) alone, which is the shape the extent walk already handled, so W-9 could not see the C-8.0c defect; **(iii)** the extent bound **CHANGES** on a mode-(c) shape and spans all `declared` instances, agreeing with validation, with the one-instance form still reporting one and the depth-capped form returning `err_group_too_large` **and returning immediately** rather than burning `declared` no-op iterations (**W-10a**, FR-021e / SC-016 — leg 1 observed RED before C-8.0c lands; **W-10a is already required green at the Phase-3 exit** and is re-run here, so this leg is a re-assertion rather than its first appearance; **leg 4 re-specified at Gate A fresh loop round 1** — `declared ≥ 2` **at the frame whose descent hits the depth cap** (not the outermost frame — the outermost count is invariant in the un-mirrored implementation too, so varying it would false-green) plus a `group_member_fn_` invocation count asserted invariant across two values of **that frame's** `declared`, since the error code alone passes against an implementation that omits the `if (overflow) return k;` mirror, and the failure mode is a DoS, not a hang); **(iv)** and only *then* the freeze: on a divergent context **whose delimiter is NOT a nested group's count tag**, `consume_group_extent`'s extent bound, `group()`'s `group_index` and the reserve bound are unchanged from pre-083 while the splitter's split is not (**W-10** / C-8.0 / C-8.0b). Leg (iv)'s fixture constraint is asserted inside the case, not assumed — without it leg (iv) contradicts leg (iii) and the pin certifies the defect. #180's later fix on `:559-570` is expected to update W-10 rather than be blocked by it. |
| **5 — Evidence** | **Three** benches + baselines — inbound validate, typed read, C-ABI commit (FR-022, Gate A r2 scope); B&L rows (this repo's release-note artifact) enumerating **all THREE classes** of C-ABI construction change (FR-019a/C-9.6) — (a) the five groups whose global value moves, (b) the cause-1 tags whose lookup becomes context-sensitive in divergent contexts, (c) the three unregistered groups + six children that become buildable; *(classes (b)/(c) added 2026-07-31 at the checklist audit — the previous "five delimiter moves" enumeration answered "whose **global** value moves", but the check stops reading the global at all, and SC-007's exception is (a)+(b))*; L-063-3 retired, L-063-4 re-stated under the post-fix delimiters **with THREE dispositions, not two** *(third added at Gate A round 3)* *(re-stated 2026-08-02, Gate B r1 F2, matching `tasks.md` T071's corrected three-way enumeration)* **— splitter leg (a) NOT done: DESCOPED with evidence (T054's zero-population measurement across all ten dictionaries); cap-loop leg (b) OUTSTANDING, now tracked as fixpp#214 (`#180` was already CLOSED by 072 on 2026-07-13 — its scope was the census pins + load-time guard, not this splitter fix, and it is not reopened here); and (c) the row's own claim that `consume_group_extent()` computes `group_end` correctly CORRECTED, since it is false for the delimiter-is-a-nested-count shape the row never audited and this feature makes reachable in 262 contexts (repaired by C-8.0c / FR-021e)**, L-063-1 amended if D-12 lands on branch (b); **one new `spec/behaviors-and-limitations.md` line carrying the 082 handoff** (see below — the B&L *line* is in scope here; the 082-side edits are not); interop-gate observation (FR-020); codegen parity (FR-017); ABI hygiene (W-14). | Article VIII/IX evidence complete for Gate B. The 082 handoff line **exists on this branch** and names both items; the 082-side edits it describes are explicitly not attempted here. |

**The Phase-3-to-Phase-4 window C-8.0c used to leave open — now CLOSED, by this paragraph's own named remedy** *(added Gate A round 3; closed at Gate A fresh loop round 1, on Codex's P3).*

**Why C-8.0c is not blocked by Phase 3 — the reason that actually holds.** `consume_group_extent`'s delimiter is **wire-derived** and the repair needs **no per-context delimiter record**, so nothing FR-021e requires is produced by the loader change and it can land any time from Phase 2 onward. **The round-3 reason — *"the membership sets it probes are already context-exact from 063"* — is withdrawn as false**, and it contradicted a sibling clause of the same amendment: `typed_read_splitter.md` W-10's **exclusion 2** exists precisely because those member sets **do** move at Phase 3, on the 52 polluted contexts (the `table_view.hpp:645` injection becomes a no-op) and the 30 newly registering ones. 063 made the sets context-**keyed**, not context-**exact**; that they are not exact until Phase 3 is this feature's premise. Not-blocked-by is the claim the phasing needs, and it survives.

**The window, and its closure.** Round 3 recorded that from the Phase-3 exit to the Phase-4 exit the tree would contain reachable silent instance loss on the 262 mode-(c) contexts — validation accepting (Phase 2) while the extent walk truncates — and named its own cheap remedy: *"add W-10a as a Phase-3 exit leg, in the same shape SC-004's wire leg was added at round 1."* **That remedy is applied**: the Phase-3 gate above now carries **W-10a green *at this exit*** as leg (iv), which requires C-8.0c to land by the Phase-3 exit. The window is **removed**, not merely bounded. It is a **gate-leg addition, not a re-homing of the source site**: C-8.0c remains specified in `typed_read_splitter.md` and listed in Phase 4's content, so no second scope decision is taken on the user's behalf — the round-3 objection to re-phasing is respected by changing only what a phase exit asserts, not where the clause lives.

**And, stated explicitly rather than relied on implicitly**: **intermediate phase exits are not releasable states for this feature.** The merge unit is this feature's **single PR**; Gate B and all three tier matrices run on the PR head, not on phase boundaries. No phase exit here is a shippable artifact. That is the standing disposition for every intermediate state in this plan, and it is what makes a phase gate a construction-order discipline rather than a release gate.

**082 follow-ups: a close-out handoff, not a Phase-5 deliverable** *(demoted at Gate A round 1).* D-8 rests its entire "the oracle extension must be additive" decision on 082 being *"parked on an unbuildable branch — the breakage could not be observed until 082 resumes."* Scheduling edits to 082's artifacts as a verifiable Phase-5 task relies on 082's unreachability as a design constraint while treating it as a checkable deliverable in the same bundle. Those edits cannot be built, run, or checked from this branch, so the task could only ever be marked done by inspection — the shape that produces a false-green close-out. They are therefore recorded as a **handoff**, carried on this branch by a `spec/behaviors-and-limitations.md` line (a tracked artifact 082 will read) and repeated in the phase-4 close-out record: tighten the two 082 pins currently written to tolerate the injected delimiter, and correct 082's recorded "5 of its 14 contexts" note to 2. D-12's outcome, if it lands on branch (b), is added to the same handoff.

**The handoff line itself has an owning phase — Phase 5 — and is listed in that row's content.** Demoting the *082-side edits* out of the phasing must not demote the *artifact that records them* out of it too; that would leave "recorded" meaning "recorded nowhere", which is the same defect this rewrite fixed for SC-004's named subset (a promise with no home in Project Structure and no phase to land in). Phase 5 already owns the B&L edits, so the line costs nothing extra and is checkable on this branch.

## Complexity Tracking

No Constitution Check violations — section intentionally empty.

## Gate A

- Round 1 applied 2026-07-30: Codex P1=0 P2=2 P3=2; Opus post-judging P1=3 P2=11 P3=5; orchestrator measurements refuted N14 and resolved N1b's branch point favourably. Rewrite addresses root causes **RC1 (the consumer of the per-context delimiter was never designed — new `## Consumer contract` C-3.4..C-3.7, research D-11, FR-023/023a/006d, closing N1a, N1b, N10, the Codex #3 / D-5 leg and N13's Article IX §1 leg)**, **RC2 (every phase gate stated over an artifact its phase does not move — all five gates restated, W-1a added on a populated context store, SC-004's wire leg pulled into the Phase-3 exit, C-7.2 added, Phase 1's exit made a reconciliation, 082 follow-ups demoted to a handoff; closing N2, N3, N9, N15 and N7's SC-004 leg)**, **RC3 (the two `/clarify`-widened surfaces never got design depth — new `contracts/typed_read_splitter.md` and `contracts/capi_group_grammar.md` with named test artifacts; closing Codex #1, N7's SC-010/SC-012 legs, N11 and N12)**, and **RC4 (traceability one pass behind the analysis — `## Normative References` in `spec.md` only per the 079 adjudication, the missing Article VI row, `plan.md`'s member-injection contradiction, the projection marked at all four loci, #210's two substituted witnesses restored as FR-007a/FR-010a/SC-013; closing Codex #2, Codex #3, N5, N6, N8, N9, N13)**. N14 is **not** in that list: it was refuted by measurement, the FIX42 row and the 335/52 totals stand unchanged, and its residual — an unexplained population path — is carried forward as the open question **research.md D-12** with a Phase-1 task ahead of the loader change. Reviews: research/reviews/codex_083-group-delimiter-resolution_gate_a_review.md, research/reviews/opus_083-group-delimiter-resolution_gate_a_adversarial_review.md, research/reviews/orchestrator_083-group-delimiter-resolution_gate_a_r1_measurements.md.

- Round 2 applied 2026-07-30 *(reviews dated 2026-07-30; rewrite executed 2026-07-31, which is the date carried by the in-document correction annotations)*: Codex P1=1 P2=2 P3=1; Opus post-judging P1=1 P2=6 P3=4; rewrite 2/2 addresses root causes **RC3 (the two `/clarify`-widened surfaces each still had a hole at its centre) — now CLOSED**: `typed_read_splitter.md`'s census role column is corrected at **all six loci** (`spec.md` FR-021c, `research.md` D-6, the contract's own table + C-8.0, `plan.md`'s Source Code tree, `plan.md`'s Phase-4 gate, `data-model.md` Entity 6) with the two boundary rules scoped out by the new **C-8.0b** on their own grounds and **C-8.5's "discharges part of #180" claim withdrawn** (N16, P1); `capi_group_grammar.md` gains **C-9.2a**, naming the session-cached `table_view` route, refusing the `[const §XV.1]`-barred `dict->as_table_view()`, and stating allocation/ownership/null-disposition, pinned by new **W-11a/W-11b**, recorded as new research **D-13** and new **FR-018b** (Codex #3); `data-model.md:103`'s mis-keyed `group_context_for(no_tag)` summary corrected to `stored_group_context()` with C-8.2's lead citation replaced by the evidence that actually carries it, and **W-9 constrained to a divergent context under a non-empty parent path** so the bare-global fallback cannot mask an off-by-one key (Codex #1); **C-8.4's second disjunct deleted** — the splitter cannot observe "the context did not resolve", so the disposition reduces to two observable cases (Codex #2); **C-3.4a** added, defining how `finalize()` computes the checked set including the `!members.empty()` exclusion and marking set-equality **conditional on D-12 branch (b)** (N17); **D-12's Phase-1 task re-posed to instrument the `fr.type` predicate** rather than "which of two call paths" (N18); **FR-022/SC-009 widened from one hot path to three** with two new benches and an Article XV §1 row (N19); and the P3 sweep — both stale contract headers, the Article IX §1 enumeration completed to ten branches with the FR-023a twin named, the `table_view.hpp:646` citation and the Structure Decision counts corrected, and the FIX50SP2 25,897-vs-25,927 delta reconciled as the 30 unregistered contexts (Codex #4 / N20 / N21 / N22). Reviews: research/reviews/codex_083-group-delimiter-resolution_gate_a_2_review.md, research/reviews/opus_083-group-delimiter-resolution_gate_a_2_adversarial_review.md.

- **Round 3 — EXHAUSTED, then AMENDED by the user, 2026-07-31.** Codex P1=0 P2=0 P3=1; Opus post-judging **P1=1 P2=0 P3=3**; verdict **DO NOT CONVERGE**, with the rewrite budget already spent (2/2 used at rounds 1–2), so the loop stopped and escalated rather than attempting a third rewrite. The surviving P1 was **N23**: `typed_read_splitter.md` **C-8.0b** scoped `src/wire/offset_table.cpp:454` out on the stated ground that its instance-boundary rule *"is not defective"*, and that ground is **false at source** — `consume_group_extent` consumes the instance-opening delimiter with a bare `++k` at `:475` and descends only for members scanned after it (`:485-488`, inside the inner loop at `:476`), so on the delimiter-is-a-nested-group's-count-tag shape it truncates the extent to one instance. The orchestrator verified the finding independently against `src/wire/offset_table.cpp` before escalating. **The user's decision: amend the scope and re-run Gate A as a fresh loop** — not converge over the finding, and not descope the feature. Applied: C-8.0b's `:454` bullet **withdrawn** (struck through, with the wrong reasoning preserved) and replaced by new **C-8.0c**, C-4.1's symmetric twin at the offset table, homed in `typed_read_splitter.md` because that contract owns `offset_table.cpp`, owns the withdrawn clause, owns the witness that had to be re-stated, and owns the criteria that were at risk; new **FR-021e** and **SC-016** appended (nothing renumbered); **W-10 re-stated** with its fixture constrained to exclude the shape whose extent must now change — as round 2 wrote it, W-10 pinned the defect as correct and the Phase-4 gate would have certified it green, the false-green shape RC2 was opened for; new **W-10a** with four legs including the `overflow`-return **termination** leg; **W-9 widened from one fixture to two**; the **Phase-4 gate re-stated**; Article IX §1 enumeration grown to eleven branches; new research **D-4a**; `consume_group.md` **C-4.4** added as a pointer, adding no Phase-2 work; and the Phase-3-to-Phase-4 window C-8.0c leaves open recorded above rather than resolved by re-phasing *(**SUPERSEDED IN PART, 2026-07-31, Gate A fresh loop round 1**: that window is now **CLOSED**, by the very remedy this bullet's own record named — W-10a added as a Phase-3 exit leg. The round-3 account is left as written, with this marker, per the same convention as the round-2 disagreements below. The round-3 justification for the window — *"bounded by this feature's single PR, so it never ships"* — is likewise superseded by the standing statement that intermediate phase exits are not releasable states for this feature. See the fresh-loop entry below.)*. The **`:526` cap-loop scope-out is kept as-is** — independently verified sound at rounds 2 and 3, and not reopened by the user. P3 sweep applied: Codex #1's C-ABI copy-site citations (with its `:489-493` leg **rejected** — `:493` is the exact declaration line and the judge's rejection stands), **N24**'s `dictionary.cpp` citation cluster (`:503-505`→`:507-508` for FR-011's false sentence — the leg that would have sent `/speckit-tasks` at a *correct* comment; `:508-509`→`:510-511` normalised at all four loci; `:407`→`:398`; `:437-443`→`:439-444`), and **N25**'s stale FR count plus the homeless **W-3**, now `ConsumeGroupNestedDelim.NestedDelimiterAtDepthCapIsBoundedNotUnbounded`, with the dangling **W-2** alias resolved to W-9/W-10a. **Two items found by the orchestrator while verifying the amendment, reported by no reviewer**: **(i)** W-10 needed a **second** fixture exclusion, not one. Its first re-statement excluded mode-(c) shapes but not **polluted** ones, and `consume_group_extent`'s instance-termination test at `:478` consults the per-context **member set**, which changes on the 52 polluted contexts once the delimiter injection at `include/fixpp/dict/table_view.hpp:645` becomes a no-op — so the extent bound moves there too, and that movement is #210 Consequence 2, which FR-007a / SC-013 / US1 scenario 5 / quickstart §1a all pin **as a change**. W-10's fixture is now divergent, non-polluted **and** non-mode-(c), with both exclusions asserted in the case. **(ii)** the `set_group_first_ctx` injection call is at `table_view.hpp:645` inside `:641-646`; round 1 said `:641-645` and round 2 "corrected" it to `:646` inside `:641-647` — **both off by one**, now fixed in C-3.3, D-5 and D-12. **Research D-12 remains deliberately OPEN**, with its Phase-1 `fr.type` instrumentation task intact. Reviews: research/reviews/codex_083-group-delimiter-resolution_gate_a_3_review.md, research/reviews/opus_083-group-delimiter-resolution_gate_a_3_adversarial_review.md. **The rewrite budget resets; Gate A restarts as a fresh loop over the amended bundle.**

- Fresh loop round 1 applied 2026-07-31: Codex P1=0 P2=0 P3=1; Opus post-judging P1=0 P2=1 P3=3; rewrite 1/2 corrects C-8.0c.3's transplanted spin claim (the real failure mode is unbounded no-op iteration bounded by attacker-controlled `declared`, a DoS) and re-specifies W-10a leg 4 so it can fail against an un-mirrored implementation. Reviews: research/reviews/codex_083-group-delimiter-resolution_gate_a_4_review.md, research/reviews/opus_083-group-delimiter-resolution_gate_a_4_adversarial_review.md.

- Fresh loop round 2 CONVERGED 2026-07-31: Codex P1=0 P2=0 P3=1; Opus post-judging P1=0 P2=0 P3=3. Three P3s applied as sign-off edits (callback-seam citation at 5 loci + 1 mis-pointed fixture cross-reference; W-10a leg 4's cap-frame `declared` qualifier; C-4.4 and Phase-3 Content pointers). Reviews: research/reviews/codex_083-group-delimiter-resolution_gate_a_5_review.md, research/reviews/opus_083-group-delimiter-resolution_gate_a_5_adversarial_review.md.

### Fresh loop round 1 — what was applied, by finding

- **N26 [P2] — C-8.0c.3's spin claim corrected at all EIGHT loci, and W-10a leg 4 re-specified.** The claim that an un-mirrored descent makes the `:473` loop *spin* is false at source: that loop carries `inst < declared` and `++inst` at `src/wire/offset_table.cpp:500` is unconditional, so the walk performs exactly `declared` **no-op iterations** and returns. The spin argument is correct for the sibling `consume_group` (`include/fixpp/wire/validator.hpp:357`, no instance bound) and was transplanted without re-derivation — research D-4a's own lesson recurring inside the clause that records it, and that is stated in the bundle rather than only here. Because `parse_bounded_u32` saturates `declared` at `UINT32_MAX` (`include/fixpp/wire/tag_scan.hpp:63-79`) and no instance-count cap exists, the exposure is a **DoS, not a hang**. The eight corrected loci: **(1)** `contracts/typed_read_splitter.md` C-8.0c.3; **(2)** `contracts/typed_read_splitter.md` W-10a leg 4; **(3)** `research.md` D-4a; **(4)** `spec.md` FR-021e; **(5)** `plan.md`'s Source Code tree (`src/wire/offset_table.cpp` row); **(6)** `plan.md`'s Project Structure test tree (`typed_read_split_agreement_test.cpp`); **(7)** `plan.md`'s **Phase-4 gate**, leg (iii); **(8)** `quickstart.md` §4's **W-10a verification row**. Deliberately **not** touched, per the review: `plan.md`'s round-3 historical record (preserved-superseded text by this bundle's convention) and `contracts/consume_group.md`'s *"the depth-cap branch returns a **non-advancing** index"*, which is **true as written** — the index really is non-advancing; only the inference about the loop was wrong. The consequence that made this a P2 is repaired at the witness: W-10a leg 4's error-code assertion passed against an un-mirrored implementation (`overflow` is a `bool&` reaching `group()`'s `:551` either way) and its unconstrained fixture never tripped the `TIMEOUT`, so the leg whose own text warned that *"a witness that only checked the error code would never run"* was specified as exactly that witness. Leg 4 now MUSTs **`declared` ≥ 2** and is decided by a **`group_member_fn_` invocation count asserted invariant across `declared` = 2 and `declared` = 8**, recorded by a counting wrapper on the **existing** construction-time callback seam (`include/fixpp/wire/offset_table.hpp:79-80`, `:119-121` / `:126-128`) — **no production change**, no new seam, and no hand-derived absolute constant that a later reader could not re-derive. The per-test `TIMEOUT` is retained as a **backstop, never the discriminator**. **No instance-count cap requirement was added** — the review is explicit that the MUST is unchanged and only its justification moves; adding one would be a second scope decision.
- **N27 [P3] — the "already context-exact from 063" reason withdrawn at THREE loci, conclusion untouched.** C-8.6a and C-8.0c.5 justified C-8.0c's Phase-3 independence by asserting the membership sets it probes are *"already context-exact from 063"*; W-10's own **exclusion 2**, added in the same amendment, refutes that — the sets change at Phase 3 on the 52 polluted contexts (the `include/fixpp/dict/table_view.hpp:645` injection becomes a no-op) plus the 30 newly registering ones. 063 made the sets context-**keyed**, not context-**exact**. The reason is replaced by the one that holds — C-8.0c is **not blocked by** Phase 3 because its delimiter is wire-derived and it needs no per-context delimiter record — and the phasing **conclusion is unchanged**. The review named two loci; a grep for the sentence found **three**: C-8.0c.5, C-8.6a and `plan.md`'s window paragraph. All three are rewritten as single coherent replacements (each carried both the wrong reason *and* the window statement).
- **N28 [P3] — W-10 exclusion 2 widened to the CHILD contexts.** It constrained only the fixture's own member set, but the walk also probes child contexts at `src/wire/offset_table.cpp:485-486` (and, after C-8.0c, at the delimiter position). Exclusion 2 now reads over *"the fixture's member sets, including every nested context the extent walk descends through"*. Bounded in the contract as the review bounds it: **spurious-RED risk, not false-green** — a moved extent makes W-10 *fail*, and a failing pin gets investigated. `quickstart.md`'s W-10 row is deliberately **left as-is**: it summarises exclusion 1 only and always did, and inflating an operator-facing row with a fixture sub-clause would misrepresent the severity.
- **Codex #1 [P3, ride-along] — the Phase-3→Phase-4 window is CLOSED by the plan's own named remedy.** `plan.md`'s Phase-3 gate gains leg **(iv)**: **W-10a green *at this exit*, not deferred to the Phase-4 exit**, in the same shape SC-004's wire leg was pulled into that exit at round 1. This requires C-8.0c to land by the Phase-3 exit; it is a **gate-leg addition, not a re-homing** — C-8.0c stays specified in `typed_read_splitter.md` and stays listed in Phase 4's content, because a gate leg asserts the tree's state at a phase exit and does not say which phase authored the code, so the round-3 objection (re-phasing a source site would be a second user scope decision) is respected. Codex's alternative minimum — an explicit statement — is **also** applied and not treated as the alternative: **intermediate phase exits are not releasable states for this feature** now appears in both `plan.md`'s window paragraph and C-8.0c.5, with the reason (the merge unit is the single PR; Gate B and all three tier matrices run on the PR head, not on phase boundaries).

### Fresh loop round 1 — disagreements

- **No finding in the round-4 reviews is marked Disagree, and none was dropped.** Codex's single P3 was judged CONFIRM-at-P3 (explicitly not P2) and is applied as ride-along cleanup; the three new Opus findings are applied in full. This section records only where a *remedy* was executed differently from how it was proposed.
- **W-10a leg 4's discriminator is asserted DIFFERENTIALLY, not against an absolute invocation count.** The review's remedy says *"assert a bounded `group_member_fn_` invocation count, derived from the fixture's own entry layout"*. The finding — the count is the discriminator, and it needs no production change — is applied exactly. The **form** is not: an absolute total on a shape nested at `kMaxGroupDepth` would have to account for every enclosing frame's probes — 16 levels of them (`include/fixpp/wire/offset_table.hpp:340`) — and a number whose derivation no later reader can reproduce is the *"captured from the implementation"* anti-pattern leg 1 explicitly forbids. The case instead runs the same fixture at `declared` = 2 and `declared` = 8 and asserts the totals are **equal**. This is strictly stronger on the property that matters — the mirrored implementation's count is invariant under `declared`, the un-mirrored one's grows by `declared − 1` — and it needs no hand-derived constant.
- **No instance-count cap was introduced.** Correcting the failure mode to a DoS makes an instance cap the obvious instinct; the review forecloses it (*"Keep the MUST — the requirement is unchanged, only its justification moves"*). The `UINT32_MAX`-saturating `declared` and the absence of any instance cap are stated **as facts characterising the exposure**, at the loci that describe it, and are converted into no new FR, no new contract clause and no new witness.
- **`plan.md`'s Article IX §1 branch (11) is deliberately NOT edited.** Its citation of W-10a leg 4 as the witness for C-8.0c's `overflow` early-return branch was the *reason* N26 was a P2 (coverage satisfied, discrimination absent); once leg 4 is re-specified the citation becomes true as written. Re-checked after the leg-4 rewrite and left unchanged rather than churned.

### Round 3 — notes on the amendment

- **This is a user scope decision, not a third rewrite.** N23 brought a **new source site into Phase 4** that the round-2 judge's no-re-scope instruction had excluded, and it invalidated a shipped witness. Both are decisions the loop is not authorised to take on its own, which is why round 3 escalated with a concrete minimal repair rather than applying one.
- **What was NOT reopened.** RC1, RC2 and RC4 stay closed. C-8.0b's `:526` bullet stands. C-8.0's freeze on the membership-probe role stands — C-8.0c is a **traversal** change and leaves `:454`'s wire-derived local and `:457`'s probe exactly as they are, so the round-2 census decision (N16) is untouched. The round-1 refutation of **N14** and the favourable **N1b** measurement stand unchanged: the FIX42 row, the 335/52 totals and the zero-multi-path-pairs result are not weakened anywhere in this amendment.
- **What the amendment says about RC3.** Round 3 recorded RC3 as NOT CLOSED, with the splitter contract's centre failing on inspection three rounds running — absent (r1), mis-labelled (r2), correctly labelled but wrongly reasoned (r3). D-4a records *why* the census kept missing it: the census asked "where does a delimiter come from?" and answered that well; the defect was "where does the walk descend?", and a site can be sound on the first and broken on the second. That is the generalisable lesson, and it is in the bundle rather than only in the review.
- **Codex #1's `:489-493` leg NOT applied** — the round-3 judge rejected it and the rejection is carried forward: the bundle already cites `:493`, the exact declaration line, and `:489-492` is its comment. Replacing a precise line citation with a fuzzier range is a de-improvement. The finding's other two legs *are* applied.

### Round 2 — disagreements

- **SUPERSEDED IN PART, 2026-07-31 (Gate A round 3).** The N16 bullet below states *"`consume_group_extent`'s traversal needs no change."* That sentence is **false at source** and was withdrawn by the round-3 user scope amendment; the traversal does need a change, specified as C-8.0c. The rest of the bullet — the `:526` cap-loop scope-out and its severity bound — was re-verified at round 3 and **stands**. The round-2 record is left as written, with this marker, because rewriting a superseded round's account would erase the reasoning a later reviewer needs to see.

- **No finding in the round-2 adversarial review is marked Disagree.** Every Codex finding was confirmed (one downgraded P1→P2, none dropped) and every new Opus finding was applied. This section records the two places where a *remedy* was applied differently from how a reviewer proposed it, and the one open residual the rewrite deliberately created.
- **Codex #1's proposed remedy — redefining `group_delim_fn_t` — NOT applied** (the finding itself IS applied). Codex read `data-model.md:103`'s mis-keyed summary as evidence that the callback shape is unimplementable and asked for a new signature. The judge refuted that at source: `opaque_dict_` is already a `fixpp::dict::table_view const*` (`include/fixpp/wire/parser.hpp:535` static-casts it, `:581` binds it from a `table_view const&`), and the existing membership call already passes `(ctx, no_tag, delim)` separately with `ctx = stored_group_context()` (`src/wire/offset_table.cpp:533`, `:541`) — i.e. C-8.1's sibling callback drops straight in as designed. The defect was a **doc-drift between two artifacts, one of which was already right**; `data-model.md` is corrected to match C-8.2 and the callback shape is unchanged.
- **N16's scope decision — `group()`'s cap loop (`src/wire/offset_table.cpp:559-570`) is scoped OUT, not brought in.** N16 offered both options. Bringing it in would pull a new source site into Phase 4, which is a re-scope the round-2 judge explicitly forbade ("no re-scope and no return to `/clarify`"), and the review's own severity bound supports leaving it: `group()` returns no split, so the loop's only consumer is a `max_group_entries_per_instance` check `consume_group_extent` already applies over correct, nesting-aware boundaries — the exposure is a false positive/negative on a defense-in-depth DoS cap, never wrong returned data, and `consume_group_extent`'s traversal needs no change. What is **not** waived is the consequence: C-8.0b records the residual (two disagreeing instance-boundary rules left in one file), **C-8.5 withdraws its claim to discharge part of #180**, and `spec.md`'s L-063-4 Normative Reference + Phase 5's close-out row now disposition L-063-4's two legs separately so #180 is not recorded as closed. The remaining work is named for whoever takes #180.
- **The judge's `bad_alloc` aside on C-3.4's "non-throwing" leg is explicitly not a finding** and was not acted on, per the review's own parenthetical.

### Round 1 — disagreements

- **Codex #4 (P3, `checklists/requirements.md:16` "all 3 resolved" vs the notes' "4 questions asked") NOT applied** — the Opus adversarial review adjudicated it a **disagree-and-drop**, and I concur on re-reading the file. It is not an inconsistency but an explicitly reconciled distinction: `:16` is a statement about **markers** ("no `[NEEDS CLARIFICATION]` markers remain — all 3 resolved"), while the notes heading at `:34` reads "all three markers settled in `/speckit-clarify` (**4 questions asked**)" and the fourth table row at `:41` is itself labelled "(4th question, **not a pre-existing marker**)". Both numbers appear in the same table with the discrepancy named. The second half of Codex's remedy — mark the two widened-scope clarifications as reflected in plan/research/data-model/contracts — was already satisfied (FR-021 by D-6 + Entity 6 + plan Phase 4; FR-006 by D-7 + the whole of `loader_tolerant_mode.md`), and is now satisfied more strongly by the two new contracts. Applying it would cost a convergence round for nothing.
- **Codex #2's remedy scope NOT applied as written** (the finding itself IS applied). Codex asked for Normative References sections in "the feature artifacts" (plural). That exact reading was adjudicated a false positive on the immediately preceding feature — `specs/079-required-presence-scope/plan.md:101`: *"Article VI §5's operative reading is spec.md-only, matching merged precedent 074/075/076/078 (spec.md only) and 077 (none)."* Adding the section to plan/research/data-model/contracts would re-open a settled adjudication and diverge from every merged bundle. The section is added to **`spec.md` only**.
- **N14 (P2, "the FIX42 baseline row cannot have been measured on `0539b56d`") NOT applied — refuted by measurement, not disagreed with.** The orchestrator's round-1 measurement note shows `(R,{},146)` is a real per-context entry (size 32, data `0x…2050`) and not the bare fallback (size 19, data `0x…1fb0`), with tag 382 in the same dictionary resolving as a genuine bare fallback at identical pointer and size — so the discriminator works. FIX42 yields CTX-HIT=38 / BARE-FALLBACK=0 / EMPTY=0. Consequently: the FIX42 row is **not** re-stated, the 335 and 52 totals are **not** moved, **FR-012's "no carve-out" is left exactly as written** (the note is explicit that it needs no position on the INT-typed dictionaries), and **SC-008's omission of FIX42 is left as written** — with N14 refuted, that omission is correct, since FIX42 does change (8 wrong / 4 polluted). The review's inference from that omission was evidence *for* N14 and falls with it. What is carried forward instead is the residual the note raises: research.md **D-12**.
- **N1b's re-scope branch NOT taken.** The review made N1b conditional on a probe: if any shipped dictionary declared one `NumInGroup` tag under two ancestor paths in a single message, the fix would reach `message_fields`/`append_run` — 063's store shape, outside this feature's scope and D-8's additive constraint — and require re-running from `/clarify`. Measured: **zero such pairs in all ten dictionaries.** The favourable branch. Applied instead, per the measurement note: `spec.md`'s Edge Case #1 retired as unreachable, D-3's rejection rationale rewritten (it justified path-keying by citing exactly that unreachable shape), and the `message_fields` tag-dedup recorded as a known structural limit of the store in `group_ctx_delims.md` C-3.5 — including that the sort is `std::ranges::sort`, **not** `stable_sort`, despite the comment at `src/dictionary/xml_loader.cpp:875` saying otherwise. Path keying stays; N1a's clause was owed either way and is delivered.
