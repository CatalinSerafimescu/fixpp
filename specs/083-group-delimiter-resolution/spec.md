# Feature Specification: Group Delimiter Resolution

**Feature Branch**: `083-group-delimiter-resolution`

**Created**: 2026-07-30

**Status**: Draft

**Input**: User description: "Fix the group-delimiter resolution defect family (closes fixpp#210 and fixpp#208)."

**Closes**: fixpp#210 (context-store delimiter is global-by-construction) and fixpp#208 (leading-nested-group delimiter: loader one-level component scan + `consume_group` false-rejection). The user scope decision of 2026-07-30 took the **whole delimiter axis** rather than splitting it, because a document-order delimiter regression pin cannot hold across all ten dictionaries while either half remains unfixed.

---

## Context — what a "delimiter" is and why this is one defect, not three

In FIX, a repeating group's **delimiter** is the tag that must open every instance of that group. It is defined as the group's **first member in declaration (document) order**. A receiver counts instances by counting delimiter occurrences; get the delimiter wrong and instance counting collapses.

The engine resolves a delimiter in three places, and all three are wrong in a different way:

1. **Which context the delimiter is applied to.** The runtime keys group metadata by `(msg_type, ancestor-path, no_tag)`, but the delimiter fed into that store is looked up **globally, first-seen-wins**. A `NumInGroup` tag reused across messages with different leading members therefore gets one message's delimiter applied to all of them.
2. **How the delimiter is found inside the declaration.** The loaders scan a group's first child only **one level deep** when that child is a component, so a component whose own first child is a nested group is skipped and a later scalar is chosen instead — or nothing is found at all.
3. **What the receiver does when the delimiter is itself a nested group's count tag.** The instance scanner consumes the delimiter without descending into the nested group it opens, so the nested group's body is read as if it belonged to the outer group, the scan breaks, and instance counting fails.

Fixing (2) without (3) converts a wrong-delimiter defect into a **false rejection across 232 measured contexts**. That coupling is why this is a single feature.

---

## Clarifications

### Session 2026-07-30

- Q: If a reference engine's delimiter disagrees with declaration order for a given group, which wins? → A: Declaration order always wins. The interop gate is **observational** — divergences are measured and recorded as behaviour/limitation rows and release-note entries, but never alter resolution. *(**Preserved verbatim as the `/clarify` transcript; superseded in part.** "release-note entries" here predates **FR-019b**, which established that this repo has no `CHANGELOG.md` and that `spec/behaviors-and-limitations.md` **is** the release-note artifact — so the two named artifacts are one. Marked rather than edited, per this bundle's convention for preserved-superseded text; see `plan.md`'s round-3 "SUPERSEDED IN PART" markers. Added 2026-07-31, `/speckit-analyze` re-run F8.)* No compatibility mode, no config surface, no branch on the inbound validation path.
- Q: How is the C-ABI construction-path delimiter check reconciled with the validation-path delimiter? → A: Thread the context into the construction path. `validate_group_grammar` already recurses through nested instances and the accumulator already carries the message type, so it maintains the ancestor path as it descends and uses the context-keyed lookup. Both paths then apply one identical rule; no exported ABI signature changes.
- Q: What should the loader do when a group declaration has no resolvable first member even after recursion? → A: Reject the load by default (mirroring the loader's existing fail-closed disposition), **with an explicit opt-in tolerant mode** that downgrades to warn-and-skip for callers loading untrusted or partial dictionaries. Both behaviours are tested and documented.
- Q: Is the typed-read instance splitter (wire-derived delimiter, may mis-split when the delimiter tag reappears at depth) in scope? → A: **Fully in scope** — investigated and fixed in this feature, so nothing delimiter-adjacent is left live.

### Session 2026-07-30 (Gate A round 1)

Resolutions taken during the Gate A round-1 convergence pass. None required new user input: each is a design decision forced by an existing constraint the round-1 review or the orchestrator's measurements exposed. They are recorded here in the same form as the `/clarify` session so the decision, not just its consequence, is traceable.

- Q: What happens when `as_table_view()` looks up a context in the new per-context delimiter table and finds **no record**? → A: **It cannot happen, and that is enforced at load, not at lookup.** The loaders assert a completeness invariant in `finalize()` — every context `as_table_view()` will later enumerate has an Entity-2 record — so the consumer-side miss is unreachable by construction. A silent fallback to either legacy value is forbidden outright: `group_first_field(no_tag)` is the defect this feature removes, and `members.front()` is a *worse* defect already fixed and pinned (`dictionary.cpp:498-506`; `tests/wire/validator_production_table_view_test.cpp::ValidatorProductionTableView.GroupDelimiterFromWireNotTagSortedMember`). `as_table_view()` itself **stays non-throwing**, as 072 established (L-063-4). See FR-023.
- Q: Does FR-006a's tolerant mode create such a miss? → A: No, and it must not. A tolerantly-skipped group is skipped at **load**, before any context enters the consumer's enumeration, so the invariant of FR-023 holds unchanged in both dispositions. Tolerant mode is not a licence for a half-registered context.
- Q: A group that is *declared* in the dictionary but reachable from no message expansion produces zero contexts. Is that the FR-006 fail-closed condition? → A: **No.** FR-006 fires on a group whose *first member* cannot be resolved, not on a group that no message references. A group no message references cannot affect any wire decision; its diagnostic is informational. See FR-006d — without this, FR-006's per-declaration wording and C-1.4's per-context wording disagree and C-7.1's precondition measurement has no defined answer space.
- Q: FR-006's default rejection is specified as `xml_parse_error` in both loaders. Is that symmetric? → A: **Behaviour is symmetric; the exception type is per-loader.** `OrchestraLoader` throws `orchestra_parse_error` (which derives `xml_parse_error`, `include/fixpp/dict/error.hpp:98`), mirroring the `group_delimiter_collision_error` precedent 072 set. A base `xml_parse_error` thrown from the Orchestra loader would escape `tests/fuzz/fuzz_orchestra_loader.cpp`'s documented exception set — that harness catches the *derived* type — and be rethrown as an invariant violation, crashing the fuzzer on every input with an unresolvable group. See FR-006c.
- Q: Is the "same `NumInGroup` tag under two ancestor paths in one message" edge case reachable? → A: **No — measured, zero such pairs in all ten shipped dictionaries** (orchestrator Gate A round-1 measurement, 2026-07-30). The Edge Case is retired below and the design rationale that leaned on it (research.md D-3) is rewritten. Per-context path keying stays, for the reasons D-3 now gives.

### Session 2026-07-31 (Gate A exhaustion — user scope amendment)

Gate A reached its rewrite budget at **round 3 (2/2 rewrites used)** with **P1 = 1 / P2 = 0 / P3 = 3** and a verdict of DO-NOT-CONVERGE. The surviving P1 (**N23**) was not a documentation defect: it reported that `contracts/typed_read_splitter.md` **C-8.0b** scoped `src/wire/offset_table.cpp:454` out of the feature on the stated ground that its instance-boundary rule *"is not defective"*, and that the ground is **false at source**. The orchestrator verified the finding independently against `src/wire/offset_table.cpp` before escalating. The user reviewed it and chose to **amend the scope and re-run Gate A as a fresh loop** rather than converge over the finding or descope the feature. This entry records the decision, not merely its consequence.

- Q: `consume_group_extent` consumes the instance-opening delimiter with a bare `++k` (`:475`) and descends into nested extents only for members scanned *after* it (`:485-488`, inside the inner loop at `:476`) — the identical asymmetry `contracts/consume_group.md` diagnoses in `include/fixpp/wire/validator.hpp:362` and repairs there via **C-4.1**. Is that in scope? → A: **Yes, in scope. C-8.0b's `:454` bullet is withdrawn.** The repair is C-4.1's symmetric twin, specified as **C-8.0c** in `contracts/typed_read_splitter.md` (that contract owns `offset_table.cpp`), required by new **FR-021e**, pinned by new **SC-016** and witness **W-10a**, and landing in Phase 4. The `:526` cap-loop scope-out is **kept as-is** — it was independently verified sound at rounds 2 and 3 and was not reopened.
- Q: Why does this need a user decision at all, rather than a third rewrite? → A: Because it brings a **new source site into Phase 4** that the round-2 judge's no-re-scope instruction had excluded, and because it **invalidates a shipped gate**. The alternative — converging as scoped — would ship a feature that fixes validation (FR-007) while leaving the read path truncating on **262 of the 365** affected contexts, turning today's loud rejection into **silent instance loss** through `MessageView::group<>()` and the C-ABI top-level group getter. That is not a residual that can be recorded and carried; it is a defect the feature would introduce.
- Q: What happens to **W-10**, which positively pins `consume_group_extent`'s extent bound as *"unchanged from pre-083 values"*? → A: **Re-stated, not deleted.** As written it is a pin whose passing condition is that the defect is still present — a false-green of the exact class root cause **RC2** was opened for, and the reason the Phase-4 gate would have certified this green. Its anti-class-fix purpose is real and is kept, with its **fixture constrained** to divergent contexts whose delimiter is *not* a nested group's count tag (the population C-8.0c provably does not touch). The complementary assertion — the extent bound **must change** on the delimiter-is-a-nested-count shape — is new **W-10a**, and **W-9's fixture set is widened from one shape to two**, because W-9's round-2 fixture (mode (b)) is structurally incapable of detecting this defect.
- Q: Does the amendment re-point `:454`'s wire-derived delimiter at the dictionary? → A: **No.** C-8.0's freeze on the **membership-probe** role is untouched; both `:454`'s local and `:457`'s probe stay wire-derived. The amendment adds a descent and nothing else, which is why it does not reopen the round-2 census decision.

**Rewrite budget resets.** Gate A restarts as a fresh loop over the amended bundle; rounds 1–3 remain on the record in `plan.md` `## Gate A`.

---

## Baseline measurement *(established on `main` @ `0539b56d` before any change)*

> **⚠ SUPERSEDED 2026-07-31 by T012.** The table below is the **original scratch-probe projection** and is retained for provenance only. The **authority of record** for every figure here is now the checked-in pin `tests/dictionary/delimiter_census_test.cpp` — see **"Baseline, re-measured (T012)"** immediately after this table. Four figures moved; each is accounted for by construction. Do not cite the numbers below.

Method: for every group context in every dictionary, compare the runtime's resolved delimiter and member set against an **independent** raw-document walk, across all ten shipped dictionaries (nine QuickFIX-XML + the Orchestra FIX Latest repository).

| dictionary | contexts | wrong delimiter | of those, delimiter is a nested group | polluted member sets | unregistered contexts |
|---|---:|---:|---:|---:|---:|
| FIX40 | 6 | 0 | 0 | 0 | 0 |
| FIX41 | 10 | 0 | 0 | 0 | 0 |
| FIX42 | 38 | 8 | 0 | 4 | 0 |
| FIX43 | 235 | 4 | 0 | 0 | 0 |
| FIX44 | 823 | 10 | 0 | 6 | 0 |
| FIX50 | 1114 | 10 | 0 | 6 | 0 |
| FIX50SP1 | 1309 | 12 | 0 | 6 | 0 |
| FIX50SP2 | 25897 | 261 | 232 | 14 | 30 |
| FIXT11 | 8 | 0 | 0 | 0 | 0 |
| Orchestra FIX Latest | 26806 | 30 | 0 | 16 | 0 |
| **total** | | **335** | **232** | **52** | **30** |

## Baseline, re-measured (T012) — **the authority of record**

Measured by the checked-in pin `tests/dictionary/delimiter_census_test.cpp` against the independent oracle in `tests/dictionary/required_scope_oracle.hpp`, corroborated on a documented 36-row sample by a third authority (the code generator's own walkers — T007, 34 agree / 0 differ). Every figure below is reproducible by `ctest -L dictionary -R delimiter_census`.

**Which surface is being measured matters, and the original probe measured a different one.** There are **three** delimiter stores, not two (T008):

| # | Accessor | Populated by | Gated on |
|---|---|---|---|
| 1 | `Dictionary::group_first_field(no_tag)` | the loader, from `<group>` XML structure | nothing message-specific |
| 2 | `table_view::group_first_field(no_tag)` | `dictionary.cpp:397-420`, reads store #1 | store #1 **and** the per-message `NumInGroup` type gate |
| 3 | `table_view::group_first_field(mt, path, no_tag)` | `dictionary.cpp:445-525` | the per-message `NumInGroup` type gate |

Store **#3** is what `validator.hpp` and the parser lambda query first, so it is the surface that governs behaviour, and it is what the pin measures (discriminating a context miss from a wrong answer by the span `.data()` pointer).

| dictionary | contexts | wrong | wrong(nested) | nested_total | polluted | unreg. in checked set | INT-typed, out of set |
|---|---:|---:|---:|---:|---:|---:|---:|
| FIX40 | 6 | 0 | 0 | 0 | 0 | 0 | 6 |
| FIX41 | 10 | 0 | 0 | 0 | 0 | 0 | 10 |
| FIX42 | 38 | 0 | 0 | 0 | 0 | 0 | 38 |
| FIX43 | 235 | 4 | 0 | 0 | 0 | 0 | 1 |
| FIX44 | 823 | 10 | 0 | 0 | 6 | 0 | 0 |
| FIX50 | 1114 | 10 | 0 | 0 | 6 | 0 | 0 |
| FIX50SP1 | 1309 | 12 | 0 | 0 | 6 | 0 | 0 |
| FIX50SP2 | 25927 | 264 | 235 | 240 | 14 | 0 | 0 |
| FIXT11 | 8 | 0 | 0 | 0 | 0 | 0 | 0 |
| Orchestra FIX Latest | 26806 | 30 | 0 | 245 | 16 | 0 | 0 |
| **total** | **56276** | **330** | **235** | **485** | **48** | **0** | **55** |

**Reconciliation against the superseded projection — it closes by construction, in exactly two cells.** Eight of the ten dictionaries agree cell-for-cell on the wrong-delimiter column. Only FIX42 and FIX50SP2 move:

- **wrong**: 335 − 8 (FIX42) + 3 (FIX50SP2) = **330**
- **polluted**: 52 − 4 (FIX42) = **48**
- **FIX50SP2 contexts**: 25897 → 25927 = **+30**, exactly the population the original probe skipped

**Cell 1 — FIX42's 8 wrong / 4 polluted are not 083's, and never were.** `dictionary.cpp:445-447` gates the entire context-store population on `fr.type == field_data_type::NumInGroup`. FIX 4.0/4.1/4.2 type their group-count fields `INT` (e.g. `dictionaries/FIX42.xml:2084`, the sole declaration of tag 146), so those dictionaries register **no** group contexts at all and have no resolved delimiter to be wrong about. This is **L-063-1 / L-066-1 / issue #196**, explicitly out of scope here. FIX43's single case is the same root cause in miniature — one isolated `<field number='576' type='INT'/>` in an otherwise-`NumInGroup` dictionary. These 55 contexts are now counted in their own column and **asserted at exactly 55** by `DelimiterCensus.IntTypedOutOfCheckedSetIsExactlyFiftyFive`, so that if #196 later lands the pin fails loudly rather than drifting silently.

**Cell 2 — FIX50SP2's three zero-delimiter groups are registered after all.** `dictionary.cpp:508-509` reads `delim = gr_delim != 0 ? gr_delim : members.front()`. A zero bare lookup does **not** leave the context unregistered; it registers `members.front()`, the lowest-**tag** member. Measured over the 8 affected contexts: 1499 (`CC`, `CD`) → 146 where the true delimiter is 453; 1919 (`y`) → 1918 where it is 1920; 1669 (5 msgs) → 1529, correct only because 1529 happens to be both its lowest tag and its true delimiter. Exactly **3** are wrong, and the same 3 drive both the +3 wrong and the +3 nested deltas.

**Verdict on what the delta falsifies: the PROBE, not the pin and not the requirement.** The deleted `delim_probe3.cpp` measured `Dictionary`'s bare handle accessors (store #1), not `table_view`'s registered store (#3). That single substitution accounts for both cells. The requirements are unchanged; only the figures they are scored against move.

**No completeness violation exists.** `unregistered_in_checked_set` is **0** on all ten dictionaries — every context `as_table_view()` enumerates is registered. This is the empirical half of C-7.3 and it passes.

Findings that shape the scope:

- **The wrong delimiter (330 measured; 335 as originally projected) is the primary defect; the polluted member set (48 measured; 52 projected) is a secondary symptom.** A context's member set is polluted only where the wrongly-chosen delimiter is *not already* a declared member there. Everywhere else the delimiter is still wrong and the group still mis-parses, silently.
- **No member is ever missing** (0 in every dictionary), and where pollution exists the extra tag is **exactly the wrongly-resolved delimiter, a single tag, in 100% of cases**. Correcting the delimiter therefore removes all 52 pollutions *by construction*, because a correctly-resolved delimiter is necessarily a declared member of its own context.
- **The defect is cross-loader.** Orchestra has no broken scan and no unregistered groups, yet still shows 30 wrong-delimiter contexts from cause (1). A fix applied to one loader only would be a half-restructure.
- **Affected contexts are not obscure.** Every message below is a measured wrong-delimiter context, with names resolved from the dictionary's own `<messages>` block:
  - `NoOrders(73)` — 8 FIX50SP2 messages: `NewOrderList(E)`, `AllocationInstruction(J)`, `AllocationReport(AS)`, `AllocationInstructionAlert(BM)`, `Confirmation(AK)`, `ConfirmationRequest(BH)`, `ListStatus(N)`, `TradeAggregationRequest(DW)`.
  - `NoExecs(124)` — 6 messages, the `Collateral*` family: `CollateralRequest(AX)`, `CollateralAssignment(AY)`, `CollateralResponse(AZ)`, `CollateralReport(BA)`, `CollateralInquiry(BB)`, `CollateralInquiryAck(BG)`.
  - `NoMDEntries(268)` — `MarketDataIncrementalRefresh(X)`, and additionally `MarketDataSnapshotFullRefresh(W)` on Orchestra.
  - `NoQuoteEntries(295)` — `MassQuote(i)`, `MassQuoteAck(b)`. `NoBidComponents(420)` — `BidResponse(l)`.
- **FIX43 shows 4 wrong delimiters but 0 pollution**, confirming the two symptoms are independent and that a member-set-only fix would leave the primary defect untouched.
- **The 335 figure excludes the 30 unregistered contexts.** Those contexts resolve no delimiter at all today, so they cannot be scored for delimiter *correctness* and were counted separately. Once FR-006 makes their three parent groups register, they enter the delimiter population for the first time. Their declared delimiters — `1499→453`, `1669→1529`, `1919→1920` — are **themselves nested-group count tags**, so these 30 are **additional cases of the reception defect in Story 2**, over and above the 232 already measured. Story 2 must therefore be sized against 232 *plus* these 30, and the pin's post-fix denominator is 365, not 335. This is the one figure in this table that is a projection rather than a measurement, and the first task of the feature is to convert it into one.

Affected `NumInGroup` tags, by cause:

- **Context divergence** (cause 1): 73, 124, 146, 268, 295, 420, 2428, 2474 — plus 1677 and 1772 on Orchestra.
- **Broken one-level scan** (cause 2), FIX50SP2 only: 1677, 1772, 40204, 41599, 42060 (wrong delimiter), and 1499, 1669, 1919 (no delimiter found at all → group never registers, taking nested children 1529, 1534, 1540, 1559, 1918, 1920 with them).

---

## User Scenarios & Testing *(mandatory)*

### User Story 1 - A counterparty's schema-valid message stops being rejected (Priority: P1)

An integrator runs a session against a counterparty that sends a repeating group in a message where that group's `NumInGroup` tag is also used elsewhere in the dictionary with a different leading member. Today the engine resolves the delimiter from whichever message the loader happened to read first, so it looks for the wrong opening tag, fails to count instances, and rejects a message the dictionary plainly permits. After this feature the engine resolves the delimiter from the group's own declaration in that message, and the message is accepted.

**Why this priority**: This is the user-visible defect. It produces spurious session-level rejections of legal traffic on 335 measured contexts across every shipped dictionary, in messages as common as market-data snapshots and order-list entries. Everything else in this feature exists to make this fix correct or to keep it fixed.

**Independent Test**: Build a wire message for a context whose declared delimiter differs from the dictionary-global first-seen one (FIX44 `AX` / `NoExecs(124)` is the cleanest), submit it to strict validation, and observe acceptance. Build the equivalent message for a *first-seen* context of the same `NumInGroup` tag and observe that it was already accepted — the asymmetry between those two is the entire defect, expressed in two assertions.

**Acceptance Scenarios**:

1. **Given** a dictionary where a `NumInGroup` tag is declared with different leading members in two different messages, **When** a schema-valid multi-instance message arrives for the context that is *not* the first-seen one, **Then** the engine accepts it, rather than rejecting with an unexpected-tag error.
2. **Given** the same dictionary, **When** a schema-valid message arrives for the first-seen context, **Then** the engine still accepts it — the fix must not trade one context's correctness for another's.
3. **Given** a message that is genuinely invalid because it opens a group instance with a tag that is not that context's declared delimiter, **When** it is validated, **Then** the engine still rejects it. Correct resolution must not become permissiveness.
4. *(fixpp#210 Consequence 1 — over-permissive membership, witnessed directly rather than by set cardinality)* **Given** one of the 52 measured polluted contexts, named in the witness, and a wire message carrying the injected tag **inside** that group where the dictionary does not declare it, **When** it is validated under strict inbound validation, **Then** it is **accepted today and rejected after this change**. SC-002 pins the pollution count falling 52 → 0; this scenario pins the user-visible leniency that count is a proxy for. The message is not schema-valid, so this newly-correct rejection is an enumerated exception to SC-007, not a violation of it.
5. *(fixpp#210 Consequence 2 — extent mis-parse, the RED pin the issue author sequenced first)* **Given** an outer group in a polluted context immediately followed by a message-level field whose tag equals that context's injected member, **When** the message is validated, **Then** today that trailing field is **swallowed into the last group instance** because the extent scan does not terminate at it, and after this change it is not. This is a distinct mechanism from scenario 1 — an extent-termination defect, not an instance-open defect — and must not be substituted for by the Story 1 or Story 2 witnesses. If the swallow proves unreachable, that negative result and its evidence replace the witness (FR-007a).

---

### User Story 2 - A group whose first member is itself a group parses correctly (Priority: P1)

A counterparty sends multiple instances of a repeating group whose declared first member is a nested repeating group — a shape that exists in FIX 5.0 SP2 and is already live on every Orchestra/FIX-Latest session. Today the receiver consumes the opening tag without descending into the nested group it opens, reads the nested group's body as if it belonged to the outer group, aborts the scan after one instance, and rejects on an instance-count mismatch. After this feature the receiver descends, counts every instance, and accepts.

**Why this priority**: P1 alongside Story 1, and a **blocking prerequisite** for it. Correcting how the delimiter is *found* (Story 3) makes eight `NumInGroup` tags resolve to a nested group's count tag for the first time. Without descend-at-delimiter, that correction converts a wrong-delimiter defect into a false rejection across 232 measured FIX50SP2 contexts — strictly worse than today. A further **30 contexts** join them once Story 3 makes their parent groups register, since all three of those groups' declared delimiters are nested-group count tags too. This story must land before, or atomically with, Story 3.

**Independent Test**: Construct a dictionary in which an outer group's delimiter is a nested group's count tag, then validate a two-instance message. Today it is rejected while the one-instance form is accepted; after the fix both are accepted. The single-instance case passing is precisely why this has gone unnoticed.

**Acceptance Scenarios**:

1. **Given** an outer group whose delimiter is a nested group's count tag, **When** a two-instance message arrives, **Then** it is accepted with an instance count of two.
2. **Given** the same shape, **When** a one-instance message arrives, **Then** it is still accepted — the previously-passing case must not regress.
3. **Given** the same shape, **When** a message declares a count that does not match the instances present, **Then** it is still rejected. Descending must not weaken instance-count enforcement.
4. **Given** a nesting depth at the engine's supported limit, **When** a message at that depth arrives, **Then** behaviour is well-defined and bounded rather than unbounded recursion.

---

### User Story 3 - Groups the runtime silently ignored become known to it (Priority: P2)

An integrator uses the generated typed builders to construct a message containing one of three FIX 5.0 SP2 groups whose declaration consists solely of a component that itself contains only a nested group. Today the loader finds no first member for these, records no delimiter, and silently drops the group from the runtime dictionary — so the engine's own strict validation rejects a message its own code generator produced. After this feature the loader resolves the member by recursing, the group registers, and the runtime agrees with the code generator.

**Why this priority**: P2 — it is a genuine correctness and self-consistency defect (the runtime registers 502 FIX50SP2 groups while the generator emits 505), and it is a *silent* drop with no diagnostic, but it affects three groups rather than the hundreds in Stories 1 and 2.

**Independent Test**: Count registered groups for FIX50SP2 before and after; assert the three named groups resolve a non-zero delimiter and that their nested children become reachable.

**Acceptance Scenarios**:

1. **Given** FIX50SP2, **When** the dictionary is loaded, **Then** all three previously-dropped groups register with the delimiter their declaration specifies.
2. **Given** the same load, **When** the registered-group total is compared against the code generator's emitted total, **Then** the two agree, and the size of the change is explained by naming the groups that account for it rather than by asserting a bare number.
3. **Given** a group declaration that genuinely has no resolvable member, **When** it is loaded, **Then** the loader still surfaces that condition rather than dropping it silently.

---

### User Story 4 - The delimiter cannot silently regress again (Priority: P2)

A maintainer changes the loader, the dictionary set, or the code generator. Today **nothing anywhere pins any delimiter** — which is exactly why this defect family survived across every shipped dictionary and both loaders, through a suite that includes 78 passing collision-membership cases. After this feature, a regression in delimiter resolution fails the build.

**Why this priority**: P2 — it ships no user-facing behaviour, but without it the same class of defect recurs undetected. The existing collision-membership guards must not be counted as coverage here: their discriminator tag is derived independently of the delimiter and is not necessarily the offending tag, so their green is a proxy gap rather than proof of absence.

**Independent Test**: Deliberately reintroduce a global-first-seen delimiter resolution and confirm the pin fails; restore and confirm it passes.

**Acceptance Scenarios**:

1. **Given** all ten dictionaries, **When** the pin runs, **Then** every group context's resolved delimiter equals its declaration-order first member, with no carve-out or exclusion list.
2. **Given** the pin, **When** its expected values are derived, **Then** they come from a source independent of the code under test, and a sample is corroborated against a third independent authority — so the pin cannot pass by mirroring the implementation's own logic.
3. **Given** a reintroduced global-first-seen resolution, **When** the pin runs, **Then** it fails. A pin never observed failing proves nothing.

---

### User Story 5 - Reading a group back yields the same instances that validated (Priority: P3)

An integrator validates an inbound message, then reads its repeating group through the typed-read API. Today the read path infers instance boundaries from the wire rather than from the dictionary, so where a delimiter tag legitimately reappears at greater nesting depth the two can disagree — a message that validated as N instances may read back as a different number, silently. After this feature both paths split on the same dictionary-defined boundary.

**Why this priority**: P3 — it is the last place in the engine where a delimiter is resolved by a different rule, and leaving it would mean this feature fixed every delimiter path but one. It is reachable on Orchestra sessions today. It is P3 rather than P1 because, unlike Stories 1 and 2, **no defect has yet been measured here** — fixpp#208 flagged it as adjacent and explicitly unverified. The first work in this story is therefore to measure, not to fix.

**Independent Test**: Take a shape whose delimiter tag reappears at greater depth, validate it to establish the instance count, then read it back through the typed API and compare.

**Acceptance Scenarios**:

1. **Given** a group whose delimiter tag also occurs at greater nesting depth inside its own instances, **When** the message is validated and then read back, **Then** both report the same instance count and the same instance boundaries.
2. **Given** the characterisation in FR-021a finds no reachable mis-split, **When** that conclusion is recorded, **Then** it is supported by the shapes actually tried, not asserted — the point of this story is to replace an unverified note with evidence either way.
3. *(Added 2026-07-31, Gate A round 3 — FR-021e / SC-016.)* **Given** an outer group whose **delimiter is itself a nested group's count tag** — the Story 2 shape, read back rather than validated — **When** a message with two instances is validated and then read through the typed API, **Then** the read reports **two** instances on the same boundaries, not one. This is a **distinct shape** from scenario 1, whose delimiter tag merely *reappears* at depth: scenario 1's mechanism is a mis-split *inside* a correct extent, this one's is an extent *truncated before the split runs*. Neither may be substituted for the other, and the Independent Test above exercises only the first.

---

### Edge Cases

- ~~A `NumInGroup` tag reused in the *same* message under two different ancestor paths — the ancestor path, not just the message type, must discriminate.~~ **RETIRED 2026-07-30 (Gate A round 1) — not reachable in any shipped dictionary.** Measured across all ten: **zero** `(msg_type, no_tag)` pairs are declared under more than one ancestor path (FIX40 0/6, FIX41 0/10, FIX42 0/38, FIX43 0/235, FIX44 0/823, FIX50 0/1114, FIX50SP1 0/1309, FIX50SP2 0/25927, FIXT11 0/8, Orchestra FIX Latest 0/26806 — orchestrator Gate A round-1 measurement note). **Denominator note, added 2026-07-31 (Gate A round 2, N22)**: these denominators are `(msg_type, no_tag)` pairs **declared**, whereas the Baseline table's `contexts` column counts contexts the runtime **registers**. The two differ for FIX50SP2 only — 25,927 here vs 25,897 in the Baseline table — and the difference is exactly **30**, the unregistered contexts SC-003 drives to 0. Both figures are right and neither is a typo; SC-001's population sum uses the Baseline (registered) basis, which is why it reads 56,246 rising to ~56,276. It is retired rather than merely deprioritised because the runtime **cannot represent** it: `message_fields(mt)` is sorted and `unique`d **by tag** (`src/dictionary/xml_loader.cpp:877-885`), so `as_table_view()` registers at most one context per `(msg_type, no_tag)`. This is a known structural limit of the 063 store, inherited not introduced, and is recorded as such in `contracts/group_ctx_delims.md` C-3.5. Per-context path keying is retained for the reasons in research.md D-3 — not for this shape.
- A group whose declared first member is a nested group **that is itself declared via a component** — both the recursion and the descent must compose.
- A group declaration that resolves to no member at all: must be surfaced, not silently dropped (this is what makes the current three-group drop invisible).
- A delimiter tag that legitimately reappears *inside* a group instance at greater depth — instance boundaries must not be mis-split.
- Nesting at and beyond the engine's supported depth limit — bounded, defined behaviour, no unbounded recursion.
- A context present in the independent walk but absent from the runtime store: must be distinguishable from a context that resolves to the wrong value. (The runtime falls back to a global lookup on a context miss, which makes a miss *look* like a wrong answer; the baseline measurement above had to discriminate these explicitly, and 10 of the 42 contexts originally reported in fixpp#210 were this artefact.) **The same condition exists inside the product, not only inside the probe** — the new per-context delimiter table can be queried for a context it has no record of. That is the subject of FR-023, which forbids the silent fallback that would make it invisible.
- Dictionaries with zero affected contexts (FIX40, FIX41, FIXT11) must stay at zero — the fix must not introduce churn where there was no defect.

---

## Requirements *(mandatory)*

### Functional Requirements

**Delimiter resolution**

- **FR-001**: The engine MUST resolve a repeating group's delimiter from that group's own declaration in its own context, identified by message type and ancestor group path, rather than from a dictionary-global first-seen value.
- **FR-002**: The resolved delimiter MUST be the group's first member in declaration (document) order.
- **FR-003**: Resolution MUST recurse through component references to any depth when locating the first member, rather than inspecting only a component's immediate children.
- **FR-004**: When a group's first member in declaration order is itself a repeating group, its count tag MUST be the resolved delimiter.
- **FR-005**: Both dictionary sources — the QuickFIX-XML loader and the Orchestra loader — MUST implement FR-001 through FR-004 with equivalent behaviour. A change to one without the other is not an acceptable partial delivery.
- **FR-006**: A group declaration for which no member can be resolved even after FR-003's recursion MUST NOT be silently omitted from the runtime dictionary. The default disposition is **fail-closed**: the load is rejected with a diagnostic naming the offending group, mirroring how the loader already treats every other structural violation.
- **FR-006a**: A caller loading an untrusted or partial dictionary MUST be able to opt in to a **tolerant mode** that downgrades FR-006 to warn-and-skip, leaving the group unregistered and the load successful. Tolerant mode MUST be explicit opt-in; the default MUST remain fail-closed.
- **FR-006b**: Both dispositions MUST be tested — a rejection witness under the default and a skip-with-diagnostic witness under tolerant mode — and both MUST be documented for operators. Before FR-006 is enabled, all ten shipped dictionaries MUST be confirmed to still load under the default, so the fail-closed path cannot regress the shipped set.
- **FR-006c**: FR-006's rejection MUST be **behaviourally symmetric across the two loaders but typed per loader**: `XmlLoader` throws `xml_parse_error`, `OrchestraLoader` throws `orchestra_parse_error` — which derives `xml_parse_error`, so a caller may still discriminate by either. Both loaders' fuzz harnesses enumerate a *documented exception set* and rethrow anything outside it as an invariant violation; the Orchestra harness catches the **derived** type, so a base `xml_parse_error` thrown from that loader would escape and crash the fuzzer on every input containing an unresolvable group. The new throw MUST therefore land inside each harness's existing set, and `include/fixpp/dict/orchestra_loader.hpp`'s documented exception list MUST be updated in the same change. No `core::error` enum slot is added, so the `test_020_error_completeness.cpp` slot pin is not engaged — the same route 072 took for `group_delimiter_collision_error`.
- **FR-006d**: A group that is **declared in the dictionary but reachable from no message expansion** produces zero contexts and MUST NOT trip FR-006's fail-closed default. FR-006 fires on an unresolvable *first member*, not on an unreferenced *declaration*; a group no message references cannot affect any wire decision, so its diagnostic is informational. This disposition MUST be stated at the same granularity in the requirement and in the contracts, so the FR-006b precondition measurement has a defined answer space rather than merely producing a number.

**Reception**

- **FR-007**: When an instance-opening delimiter is itself a repeating group's count tag, the receiver MUST descend into that nested group and resume the outer scan one past the nested group's extent.
- **FR-007a**: fixpp#210's *Consequence 2* — a message-level field following a group being swallowed into the last instance because an injected member prevents the extent scan from terminating — MUST be established as a named Phase-1 RED witness in its own right. The issue author sequenced it first and it is a different mechanism from the Story 1 and Story 2 witnesses (extent termination, not instance opening), so neither may be substituted for it. If measurement shows the swallow is unreachable, that negative result and the shapes tried MUST be recorded in Assumptions instead — the same standard FR-021a imposes on the splitter.
- **FR-008**: FR-007 MUST NOT weaken instance-count enforcement, required-member enforcement, or extent termination; a message whose declared count disagrees with the instances present MUST still be rejected.
- **FR-009**: Descent MUST remain bounded by the engine's existing nesting-depth limit.

**Membership**

- **FR-010**: A group context's member set MUST contain exactly the tags that context's declaration declares — no injected tag.
- **FR-010a**: fixpp#210's *Consequence 1* — a tag accepted inside a group where the dictionary does not declare it — MUST be closed by a **direct negative witness** on a named polluted context, not by SC-002's set-cardinality assertion alone. A cardinality delta is a proxy for the user-visible leniency the issue filed; one named context suffices, because SC-002 generalises it.
- **FR-011**: The load-bearing source comment asserting that the per-context member set "stays exact regardless" of divergent delimiters MUST be corrected; it is currently false and was the reason the defect was not caught by inspection.

**Regression pin**

- **FR-012**: A regression pin MUST assert, for every group context in all ten shipped dictionaries, that the resolved delimiter equals the declaration-order first member — with no carve-out, exclusion list, or per-dictionary exemption.
- **FR-013**: The pin's expected values MUST be derived independently of the implementation under test, and a documented sample MUST be corroborated against a third independent authority (the code generator's own group-order data, or the recorded cross-loader values).
- **FR-014**: The pin MUST be demonstrated to fail when the defect is reintroduced, and that demonstration MUST be recorded.
- **FR-015**: Per-context member-set exactness MUST be asserted by the same pin rather than as a separate assertion, since FR-002 makes it a consequence of FR-012 rather than an independent property.
- **FR-012a**: The load-time **nested-vs-parent delimiter collision guard** shipped by 072 for fixpp#180 (`src/dictionary/xml_loader.cpp:1016-1017` and its Orchestra twin at `src/dictionary/orchestra_loader.cpp:895-896`) reads `first_field_tag`, which this feature re-derives. Its corroborating census (`tests/dictionary/reused_tag_census_test.cpp::NestedGroupDelimiterCensus`) MUST be **re-derived under the post-fix delimiters across all ten dictionaries**, and `spec/behaviors-and-limitations.md` L-063-4's *"0 nested/parent delimiter collisions"* audit — which was performed against the pre-fix, wrong delimiters that this feature changes in 335 contexts — MUST be re-stated on the new basis before its "real-dict-unreachable" disposition may continue to be relied on. A census whose derivation and whose guard stop meaning the same thing still passes and stops pinning.
- **FR-016**: The existing collision-membership guards MUST NOT be cited as evidence of delimiter correctness.

**Consistency and disclosure**

- **FR-017**: The count of registered groups per dictionary MUST agree with the count the code generator emits for the same dictionary, and any change to that count MUST be justified by naming the groups responsible.
- **FR-018**: The C ABI's message-construction path MUST resolve a group's delimiter using the same context-keyed rule as inbound validation, so that a message the builder accepts is never rejected by the engine's own validation on delimiter grounds, and vice versa. The construction-side check already walks nested group instances recursively and already has the message type available, so it MUST carry the ancestor path through that walk rather than falling back to a context-free lookup.
- **FR-018a**: FR-018 MUST NOT change any exported C ABI signature. The ABI is GA-frozen; this is a behaviour change reachable through the existing surface, not a surface change.
- **FR-018b**: The route by which the C-ABI construction path reaches the per-context delimiter MUST be named in the design, not left to `/speckit-tasks`. *(Added 2026-07-31, Gate A round 2.)* The construction check receives a `Dictionary`, whose only `group_first_field` is the context-free one; the context-keyed overload is on `table_view`. The obvious route — building a `table_view` at the check — is **barred by `[const §XV.1]`**, which restricts that construction to config time and explicitly forbids rebuilding it on the per-message hot path; the check runs inside message commit. The route MUST therefore be a **config-time-built, session-owned** context-keyed view whose lifetime and null-disposition mirror the dictionary pointer the handle already carries, so that a handle with no dictionary keeps its existing no-delimiter-check behaviour unchanged. A dictionary-present handle that cannot reach the context-keyed view MUST fail closed rather than fall back to the context-free lookup, which would reinstate the defect FR-001 removes.
- **FR-019**: Behaviour changes visible to existing integrators MUST be recorded as operator-facing behaviour/limitation entries. The C-ABI construction change is **not one class but three**, and the disclosure MUST cover all three — enumerating only the first understates the exception SC-007 carves out. *(Classes (b) and (c) added 2026-07-31, Gate A checklist audit; see FR-019a.)*
- **FR-019a**: The three C-ABI construction classes, each with its own disposition:
  - **(a) The global value itself moves** — the five §Baseline cause-2 groups (`1677`, `1772`, `40204`, `41599`, `42060`). A client that opens these with today's tag is **accepted today, rejected after**. Enumerate by group with the old and new opening tag.
  - **(b) The lookup becomes context-sensitive** — the §Baseline cause-1 tags (`73`, `124`, `146`, `268`, `295`, `420`, `2428`, `2474`, plus `1677`/`1772` on Orchestra). The *global* value is unchanged, so this class is invisible if the disclosure is written against the global lookup alone; but FR-018 re-points the construction check at the **context-keyed** value, so a client building one of these groups in a **divergent** context must now open with that context's declared tag instead of the dictionary-global first-seen one — **accepted today, rejected after**, exactly as class (a). This class is the reason SC-007's exception cannot be stated as "five groups". Enumerate **by count tag × affected message type** with the old and new opening tag — the granularity an integrator can act on — not as 335 context rows.
  - **(c) The check activates where none ran** — the three §Baseline unregistered groups (`1499`, `1669`, `1919`) and the six nested children they take with them (`1529`, `1534`, `1540`, `1559`, `1918`, `1920`). Today `group_first_field` returns 0 for these, and **`fixpp_msg_group_begin` / `fixpp_entry_group_begin` reject them outright with `FIXPP_ERR_TYPE_MISMATCH`** (`src/capi/message_write.cpp:812`, `:923`), so they cannot be built through the C ABI at all; the commit-time delimiter check never runs either, since it is guarded on `delim != 0` (`:720`). Once FR-006 registers them these groups become **buildable**. This class is a **widening** — previously-rejected construction now accepted — so it does **not** engage SC-007, but it is integrator-visible and MUST be disclosed as such rather than omitted for being benign.
- **FR-019b**: The disclosure artifact is `spec/behaviors-and-limitations.md`. *(Corrected 2026-07-31, Gate A checklist audit — the earlier clause additionally said "and in the release notes", which named no artifact: there is no `CHANGELOG.md` or release-notes file in this repo — confirmed absent by search. **`spec/behaviors-and-limitations.md` IS this repo's release-note artifact**, exactly as resolved for the identical defect in `specs/075-live-wire-enum-validation/spec.md:140`. No second artifact is invented.)*
- **FR-020**: Because every member of the affected groups is schema-optional, **any** delimiter choice rejects some shape another engine accepts. Declaration order (FR-002) is nonetheless **authoritative and unconditional**: where an external reference engine resolves a different delimiter, that divergence MUST be recorded as an operator-facing behaviour/limitation row, and MUST NOT change the engine's resolution. The per-release interop gate is therefore **observational** for this feature — it measures and documents divergence, it does not arbitrate it.
- **FR-020a**: No compatibility or leniency mode MAY be introduced for delimiter resolution. There is to be no per-session configuration surface and no conditional branch on the inbound validation path for this behaviour, so that resolution stays derivable from the dictionary alone and FR-012's pin can remain carve-out-free.
**Typed-read instance splitting**

- **FR-021**: The typed-read path's group-instance splitter MUST divide a group's instances on the same boundaries the dictionary's per-context delimiter defines. It currently infers the boundary from the wire rather than from the dictionary, which can mis-split when the delimiter tag legitimately reappears at greater nesting depth.
- **FR-021a**: The splitter's current behaviour MUST first be characterised by measurement — a reproduction demonstrating a mis-split, or evidence that none is reachable. Whichever it is MUST be recorded, so the conclusion is not carried forward as an assumption the way it was in fixpp#208.
- **FR-021b**: Typed-read instance boundaries and validation instance boundaries MUST agree for every affected context, and that agreement MUST be pinned. A shape that validates as N instances MUST read back as N instances.
- **FR-021c**: `src/wire/offset_table.cpp` derives a delimiter from the wire at **four** sites, not one: `:454` (`consume_group_extent`), `:526` (`group(no_tag)`), `:597` (the `group_slices_` reserve estimator) and `:656` (the splitter). The census MUST record each site's **role(s)**, because a repeated idiom must be censused before a class-fix is scoped. *(Role column corrected 2026-07-31, Gate A round 2 — round 1 recorded all three out-of-scope sites as membership probes only, and two of them are not.)* Only `:597` is a membership probe alone. At `:454` and `:526` the **same** wire-derived local serves **two** roles: a membership probe *and* an instance-boundary rule — `:454`'s extent open/close and per-instance cap (`:473`, `:476`, `:496-499`), and `:526`'s post-extent cap loop (`:559-570`), which is the *"redundant flat cap loop"* `spec/behaviors-and-limitations.md` L-063-4 names alongside the splitter. **`:656` is the only site whose delimiter SOURCE changes** — the **membership-probe role** at all three other sites MUST remain wire-derived, since re-pointing *that role* at the dictionary would collapse it into a tautology and silently delete three real checks. *(Scope amended 2026-07-31, Gate A round 3 — see FR-021e.)* Of the two out-of-scope **boundary** roles round 2 recorded, **only `:526`'s survives as out of scope**: its cap loop feeds only a `max_group_entries_per_instance` check that `consume_group_extent` already applies over correct boundaries (`:496-499`), so leaving it flat risks a false positive/negative on a defense-in-depth DoS cap and never wrong returned data. **`:454`'s scope-out is WITHDRAWN**: round 2 recorded its boundary rule as "already nesting-aware (it recurses at `:485-488`) and therefore not defective", and that ground is false — the recursion is inside the inner loop and so reaches only post-delimiter members, while the instance-opening delimiter is consumed by a bare increment at `:475`. FR-021e brings that repair in scope. The remaining residual — **one** flat, wire-derived instance-boundary rule left in the file, at `:561` — MUST be recorded rather than elided, and this feature MUST NOT be described as discharging #180, whose recommended fix has a second leg (folding the flat cap loop into the same traversal) that this scoping still excludes. Additionally, `:597` sizes a reservation in the fixed 16 KiB inbound arena whose under-reserve failure mode is a *documented silent truncation* (`spec/behaviors-and-limitations.md` L-073-1, and L-065-2 for the nested twin); this feature changes the member sets that estimator consumes, so the effect of the changed values on it MUST be assessed and recorded, not assumed benign.
- **FR-021d**: The characterisation FR-021a demands MUST start from the repository's existing record of this defect, not from zero. `spec/behaviors-and-limitations.md` **L-063-4** (issue **#180**, `spec/coverage-index.md:995-999`) already names the flat splitter and the redundant cap loop by line, states the correct mechanism, records a prior measurement, and points at an existing synthetic reproduction (`tests/wire/nested_group_extent_test.cpp:511-519`). L-063-4 MUST be cited by the design and updated at close-out — and, per FR-012a, its measurement leg must in any case be re-derived under the post-fix delimiters. **L-063-4 MUST NOT be inherited uncritically** *(added 2026-07-31, Gate A round 3)*: the row also asserts that `consume_group_extent()` *"correctly computes the nesting-aware `group_end`"*, and that assertion is **false for the delimiter-is-a-nested-group's-count-tag shape** (FR-021e) — a shape L-063-4 never audited, that this feature makes reachable in **485 contexts** *(corrected 2026-08-02, Gate B r2 — superseding the earlier "262" figure; see `spec.md:342-346` / SC-016)*. *(Re-dispositioned 2026-08-02, Gate B r2, fixpp#216 P1-B — the sentence below previously read "splitter leg done, cap-loop leg outstanding", which contradicted `contracts/typed_read_splitter.md`'s C-8.5-DESCOPED banner.)* The close-out re-statement therefore carries **three** dispositions, not two: splitter leg **descoped with evidence, not delivered** (T054's zero-population measurement), cap-loop leg **outstanding** (now tracked by fixpp#214), and the `consume_group_extent` correctness claim **corrected**. `#180` itself was already closed by 072 on 2026-07-13; 083 neither closes nor reopens it.

- **FR-021e**: The **offset table's** group-extent walk MUST descend when an instance-opening delimiter is itself a nested group's count tag, resuming one past that nested group's extent — the same rule FR-007 imposes on the receiver, at the second site that implements it. *(Added 2026-07-31, Gate A round 3 — user scope amendment.)* `OffsetTable::consume_group_extent` (`src/wire/offset_table.cpp:438-503`) consumes the delimiter with a bare increment at `:475` and descends only for members scanned *after* it (`:485-488`, inside the inner loop at `:476`), so on that shape it truncates the extent to **one** instance. Because the typed-read splitter's bound is derived from that extent (`:648` ← `:550`), FR-021 cannot deliver FR-021b there however correct the split is. Left unrepaired, this feature would fix validation (FR-007) and leave the read path truncating on the **485** contexts *(corrected 2026-08-02, Gate B r2 — superseding the earlier "262 — 232 measured plus 30 newly registering"; see `spec.md:342-346` / SC-016)* whose post-fix delimiter is a nested group's count tag, converting today's loud rejection into **silent instance loss** through `MessageView::group<>()` and the C-ABI top-level group getter. The descent MUST reuse the walk's existing child context and existing depth guard (FR-009) rather than introduce a new mechanism, MUST NOT weaken the declared-count bound or extent termination, and MUST honour the walk's overflow early-return — the depth-cap branch returns its input index unchanged, so a descent that ignores overflow does not advance the walk's index. *(Corrected 2026-07-31, Gate A fresh loop round 1: this sentence previously read "the walk fails to terminate", which is false at source. The outer loop is bounded by `inst < declared` with an unconditional `++inst`, so the walk performs exactly `declared` **no-op iterations** and then returns. Because `declared` is attacker-controlled up to `UINT32_MAX` — `parse_bounded_u32` saturates — and no instance-count cap exists, the exposure is a **denial of service, not a hang**, and a prompt silent return at any benign count. The requirement is unchanged; only its justification is. See `contracts/typed_read_splitter.md` C-8.0c.3.)* This requirement does **not** re-point `:454`'s wire-derived local or its membership probe (FR-021c); it adds a descent, nothing else. The per-instance entry cap now accounts for a nested extent opened at the delimiter exactly as it already does for one opened at any later member, and whether any shipped shape can breach that cap MUST be assessed and recorded rather than assumed, because a breach would be a new rejection under SC-007.

**Consumer of the per-context delimiter**

- **FR-023**: The per-context delimiter table MUST be **complete with respect to its consumer**: every group context that `Dictionary::as_table_view()` enumerates and registers MUST have a record in it. This completeness MUST be enforced as a **load-time invariant in the loaders' `finalize()`**, where a violation is a load rejection under FR-006's disposition — *not* checked at `as_table_view()`, which is contractually **non-throwing** (established by 072, `spec/behaviors-and-limitations.md` L-063-4) and must remain so. A lookup miss at the consumer is therefore unreachable by construction. Where a miss is nonetheless observable — in a debug assertion or in verification code — it MUST NOT be resolved by a silent fallback: falling back to `group_first_field(no_tag)` silently reinstates the exact defect this feature removes for precisely the contexts the new table failed to cover, and falling back to `members.front()` reinstates a *worse* defect already fixed and pinned (`src/dictionary/dictionary.cpp:498-506`; `tests/wire/validator_production_table_view_test.cpp::ValidatorProductionTableView.GroupDelimiterFromWireNotTagSortedMember`, FIX44 `NoPartyIDs(453)`: lowest member 447, real delimiter 448).
- **FR-023b**: FR-023 creates a **new fail-closed load path**, so — exactly as FR-006b requires for FR-006 — all ten shipped dictionaries MUST be confirmed to satisfy the invariant **before** the check is enabled. This is not a formality: `as_table_view()` does not consume a recorded ancestor path, it **reconstructs** one by walking an `immediate_parent` chain built from the tag-deduped vector (`src/dictionary/dictionary.cpp:439-444`, `:489-496`), so a suppressed occurrence could in principle yield a hybrid chain that no occurrence ever had and that the loader therefore never recorded. The measurement retiring Edge Case #1 (zero multi-path pairs in all ten) is what discharges that, which means the invariant holds **because of a measurement, not by pure construction** — and a measured premise gets a precondition, not an assumption.
- **FR-023a**: FR-006a's tolerant mode MUST NOT be able to manufacture an FR-023 violation. A tolerantly-skipped group is skipped at **load**, before it enters the consumer's enumeration, so both dispositions leave the invariant intact. Tolerant mode is not a licence to register a context whose delimiter is unknown.

**Performance**

- **FR-022**: This feature changes hot-path behaviour on **three** paths, and a benchmark covering **each** MUST ship **in the same change** as the fix, together with any intentional baseline update and its rationale. A narrative assertion that the budget was met is not acceptable in place of the benchmark, on any of the three. *(Scope widened 2026-07-31 — round 1 stated only path (1); paths (2) and (3) were created by the two contracts that same round added.)* (1) **Inbound validation** — delimiter resolution is queried per received field. (2) **Typed read** — FR-021's splitter replaces an array read of `entries_[first].tag` with a context-keyed dictionary lookup and adds nested-extent recursion inside the boundary scan, on the per-group-access path reached from `MessageView::group<>()` and the C-ABI top-level group getter. (3) **C-ABI construction** — FR-018 replaces an `O(1)` bare lookup with a context-keyed lookup plus ancestor-path maintenance, executed per group instance on every message commit. The C-ABI route MUST additionally be shown to build its context-keyed view **once per session at config time, never per message** — a per-message rebuild is barred by `[const §XV.1]` and is not distinguishable from the correct route by any behavioural witness.

### Key Entities

- **Group context**: the coordinate identifying one occurrence of a repeating group — message type, ancestor group path (outermost first, excluding the group itself), and the group's own count tag. The unit at which delimiters and member sets must be exact.
- **Delimiter**: the tag that opens every instance of a group in a given context; defined as the first member in declaration order. Currently one value per count tag across the whole dictionary; must become one value per context.
- **Member set**: the tags a context's declaration declares as direct members of that group. Used to determine where a group instance ends.
- **Group declaration record**: the loader's per-group record carrying the delimiter. Currently deduplicated by count tag with first-seen-wins, which is the structural cause of the context divergence.
- **Independent document walk**: a reader of the raw dictionary documents, sharing no code with the loaders, used to derive expected delimiters and member sets for the regression pin.

---

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: Across all ten shipped dictionaries, the number of group contexts whose resolved delimiter differs from the declaration-order first member falls to **0**. The pre-fix figure is **330 measured** *(revised from 335 at T012 — see "Baseline, re-measured"; the delta is FIX42's 8, which register no contexts at all and belong to #196, plus FIX50SP2's +3)*. **The affected set is 330, not 365.** The old `365 = 335 + 30` construction is withdrawn: it assumed all 30 previously-unscored contexts would prove wrong once registered, and the measured answer is that **all 30 already register** in the context store and only **3** of them are wrong (T008) — and those 3 are already inside the 330, because the pin scores the 30 rather than excluding them. The requirement to establish the 30's baseline **before** the fix rather than asserting it after is **discharged** (T008). Separately, **55** contexts in FIX40/FIX41/FIX42/FIX43 are excluded from the checked set entirely because their count tags are `INT`-typed (#196); they are asserted at exactly 55 and are **not** part of this feature's affected set. *Terminology, fixed here and used consistently in `quickstart.md` and `contracts/group_ctx_delims.md`*: **365 is the size of the affected set, not the context population.** The context population is the Baseline table's own `contexts` column — 6+10+38+235+823+1114+1309+25897+8+26806 = **56,246**, rising to ~56,276 once the 30 register. "0 of 365" therefore reads as "0 wrong, over the 365 contexts in the affected set", never as a rate over the dictionaries.
- **SC-002**: Across all ten shipped dictionaries, the number of group contexts whose member set differs from its declaration falls from **52 to 0**.
- **SC-003**: *(Restated at T012 — the original "30 contexts absent from the runtime falls to 0" is not measurable on either surface and is withdrawn.)* Measured, the 30 are **not** absent from the runtime: all 30 register in the context store today (T008). What is genuinely absent is at the **`no_tag`** level in the **bare** store: FIX50SP2 declares **505** distinct group `no_tag`s and registers **502**, the three unresolved being **1499, 1669, 1919**. So SC-003 is: the FIX50SP2 registered-group count rises **502 → 505**, the three named groups each resolve a non-zero bare delimiter, and their six nested children (1529, 1534, 1540, 1559, 1918, 1920) become reachable — with the delta accounted for **by naming the three groups**, never asserted as a bare number (FR-017 / SC-005 / W-7). The 30 remains the correct size of the *context* population those three `no_tag`s plus their named children occupy, and its delimiter correctness is scored inside SC-001's 330.
- **SC-004**: Groups whose delimiter is a nested group's count tag parse at multiple instances. Coverage is split deliberately, because a per-context wire witness is not constructible at this scale: the **delimiter pin (FR-012) covers the full mode-(c) population by construction** — **485 contexts** *(corrected 2026-08-02, Gate B r2 — superseding the earlier "232 measured contexts plus the 30 newly-registering ones"; see `spec.md:342-346` / SC-016)* — and **wire acceptance is witnessed on a named subset** — one message per affected count tag (1677, 1772, 40204, 41599, 42060, 1499, 1669, 1919) plus the synthetic two-instance reproduction. No previously-accepted single-instance message may become rejected. Enumerating every affected context's wire witness is explicitly *not* the deliverable; a sibling feature already found that some contexts are unconstructable at all because every member is schema-optional, and that constraint applies here too.
- **SC-005**: The registered-group count for FIX 5.0 SP2 agrees with the code generator's emitted count, and the difference from today is fully accounted for by three named groups.
- **SC-006**: A deliberately reintroduced global-first-seen delimiter resolution causes the regression pin to fail, demonstrated and recorded.
- **SC-007**: No schema-valid message accepted before this change is rejected after it, except for the disclosed C-ABI construction-order change, which is enumerated and documented per **FR-019a**. *(Exception scope corrected 2026-07-31, Gate A checklist audit: the exception is **classes (a) and (b)** of FR-019a — the five groups whose global value moves **and** the cause-1 tags whose lookup becomes context-sensitive in divergent contexts. As previously written this criterion's exception read as "five groups"; class (b) is the same accepted-today-rejected-after severity and was outside it, so an SC-007 audit against the old wording would have certified green while class (b)'s rejections went unenumerated. Class (c) is a widening and does not engage this criterion.)*
- **SC-008**: Dictionaries with no affected contexts today (FIX40, FIX41, FIXT11) show no behavioural change.
- **SC-009**: Message-processing throughput stays within the project's standing regression budget against the recorded baseline on **all three** FR-022 paths — inbound validation, typed-read group access, and C-ABI commit — not on the inbound path alone. *(Qualified 2026-08-02, Gate B r1 F4, fixpp#216 IND-2 — NOT read as met by measurement on path 3: it is a **seed** baseline with no prior `bench/baselines/` entry, so `[const §VIII.2]`'s ±5% has no referent, and the same-bench pre-083 A/B measured a **+12.8%** per-message regression on the nested C-ABI commit shape. See `tasks.md` T066's dated amendment for the full disposition.)*
- **SC-010**: For every affected context, the instance count and boundaries reported by validation and by typed read agree; the pre-existing behaviour of the read path is characterised by evidence rather than left unverified. *(Round 3 note: as scoped through round 2 this criterion was **unachievable** for the **485** contexts *(corrected 2026-08-02, Gate B r2 — superseding the earlier "262"; see `spec.md:342-346` / SC-016)* whose delimiter is a nested group's count tag, because the extent the read path splits was truncated before the split ran. SC-016 is the sub-criterion that makes that majority witnessable rather than assumed.)*
- **SC-011**: A dictionary whose group declaration has no resolvable member is rejected by default with a diagnostic naming that group, and is loaded with a warning under the explicit tolerant mode. All ten shipped dictionaries continue to load under the default.
- **SC-012**: A group built through the C ABI is accepted at construction if and only if the engine's own inbound validation would accept the same delimiter placement, with no exported ABI signature changed.
- **SC-013**: On a **named** measured polluted context, a wire message carrying the injected tag inside the group is accepted before this change and rejected after it (FR-010a), and a message-level field following a group in a polluted context is swallowed into the last instance before and not after (FR-007a) — or, for either, the shapes tried and the negative result are recorded. fixpp#210's two consequences are closed by their own witnesses, not by SC-002's cardinality proxy.
- **SC-014**: Under the **post-fix** delimiters, nested-vs-parent delimiter collisions across all ten dictionaries remain **0**, re-derived rather than inherited, and `spec/behaviors-and-limitations.md` L-063-4's real-dict-unreachable disposition is re-stated on that basis (FR-012a).
- **SC-015**: The Phase-1 regression pin's observed RED counts **reconcile** with this spec's 335 / 52 / 30 / 232, or every delta is explained in `research.md` and this spec is amended before Phase 2 begins. From Phase 1 onward the checked-in pin — not the scratch probe — is the authority of record for every figure in the Baseline table.
- **SC-016**: For a group whose delimiter **is a nested group's count tag** — the post-fix shape of **485 contexts** *(measured at T012; **the earlier 262 = 232 + 30 is withdrawn as a category error**)* — a message with N > 1 instances reports **N** instances and the same instance boundaries through the typed-read path as through validation, and the offset table's group extent spans all N rather than one. *(Added 2026-07-31, Gate A round 3 — FR-021e.)* The one-instance form MUST still report exactly one, since single-instance groups are why the defect was invisible. Both the pre-repair truncation and the post-repair extent MUST be **observed**, and the expected extent MUST be derived from the fixture's own entry layout rather than captured from the implementation — a value captured after the fix pins whatever the fix did, not what it should do.

  **Why 262 was wrong, and why the correction widens the defect rather than narrowing it (T012).** The old figure was built as "232 measured + 30 newly registering", but the 232 came from a count **conditioned on the delimiter being wrong** — it was only ever computed inside `if (wrong_delimiter)`. The population SC-016 actually describes is a **shape**, not an error: every context whose delimiter *is* a nested group's count tag, whether or not that delimiter currently resolves correctly. Measured unconditioned, that population is **485** — FIX50SP2 **240**, Orchestra FIX Latest **245**, all others 0.

  The consequential part is Orchestra's **245**. Orchestra has **0** wrong delimiters, so every one of those 245 contexts resolves its delimiter **correctly today** — and was therefore invisible to a count taken inside `if (wrong)`. But C-8.0c's defect is triggered by the *shape*, not by the delimiter being wrong: `consume_group_extent` consumes the instance-opening delimiter with a bare `++k` (`src/wire/offset_table.cpp:475`) and fails to descend whenever that delimiter opens a nested group, regardless of whether its value is right. So the read-path truncation and silent instance loss are **live on Orchestra today**, on contexts this feature does not otherwise touch. The corrected population makes C-8.0c's repair (T022 / FR-021e) materially more load-bearing than "262 mode-(c) contexts" implied, and any statement elsewhere in this bundle that sizes the mode-(c) population as 262 or as "232 + 30" is superseded by this clause.

---

## Assumptions

- **Declaration order is the authority.** The FIX specification defines a group's delimiter as its first field in declaration order; where the engine's current behaviour disagrees with declaration order, the engine is wrong. This is corroborated internally by three independent authorities that already compute the declaration-order value — the Orchestra loader for the tags it covers, the code generator's group-order data, and the typed builders — and it is the premise of the entire measurement above. It is *not* corroborated against an external reference engine, and the reference engines are absent from the working copy, so that corroboration cannot be obtained during this feature. Per the 2026-07-30 clarification this does **not** hold the rule hostage: declaration order is authoritative unconditionally (FR-020), and any divergence a reference engine later shows is recorded as a documented behaviour rather than treated as a defect in this resolution.
- **Delimiter correctness subsumes member-set correctness.** Because a declaration-order first member is necessarily a declared member of its own context, and because the measurement shows no member is ever missing and the only extra tag is ever the delimiter itself, the member-set defect is a consequence rather than a peer. The feature therefore does not separately remove the delimiter's injection into the member set — it makes that injection redundant, and pins the result.
- **The claim that wrong delimiters cause mis-parsing is, at specification time, a reading of the receiver's logic rather than a wire-level measurement.** No wire reproduction was constructed during triage. The first test written for this feature closes that gap (Story 1's independent test) before any fix is made; if the reading turns out to be wrong, the feature's premise changes and this spec must be revisited.
- **FR-010a's leniency and FR-007a's swallow are NOT witnessable at the wire level — a measured negative result, recorded per FR-007a's own escape clause (T009, 2026-07-31).** Both were sought on FIX44's only polluted `NoExecs(124)` context (`CollateralRequest`/`AX`) and then confirmed unreachable **by exhaustive measurement across all 48 polluted contexts in all ten shipped dictionaries**: for every one, `table_view::field_valid_for(msg_type, injected_tag)` is **false** — zero contexts have their injected/polluted tag independently valid at message level. This is a sweep, not an argument from one example. The shape is blocked on **both** paths that could exhibit it, for two independent reasons plus one structural fact common to both:
  1. **Inbound validation** (`dictionary_driven_validator::validate()`): Step 1's message-wide valid-tag check (`include/fixpp/wire/validator.hpp:176`) rejects any frame carrying the injected tag with `wire_unexpected_tag` **before** Step 3's group-extent walk — where the leniency/swallow would have to occur — ever runs. That check is independent of group-context pollution, since `valid_tags_for` is built solely from the message's own declaration (`src/dictionary/dictionary.cpp:378-383`). Measured: `AX 124=1,17=EXEC1,32=999` → rejected, `wire_unexpected_tag`, `ref_tag=32`.
  2. **Typed read** (`OffsetTable::consume_group_extent`, `src/wire/offset_table.cpp:438-503`): measured directly, bypassing `validate()` — a trailing occurrence of the injected tag after a group whose declared count is already satisfied is **excluded** from the reported extent (`entry_count()` and `group_slices()` both stop at the declared count). The outer loop is declared-bounded — `while (k < entries_.size() && inst < declared && entries_[k].tag == delim)` (`:473`) — so it terminates before any membership check runs on the trailing field. Note the asymmetry: `validator.hpp`'s `consume_group` outer loop (`:357`) carries **no** such bound, so there a trailing delimiter occurrence is consumed as an (N+1)th instance and produces a declared-vs-actual count mismatch — also not a swallow, just a different non-swallow outcome.
  3. **Structural, holding on both layers regardless of which gate fires first**: a polluted/injected tag is *by construction* identical to that context's delimiter — `set_group_first_ctx` injects exactly the value passed to it as the delimiter (`include/fixpp/dict/table_view.hpp:641-646`). So any wire occurrence of it is intercepted by the delimiter-recurrence condition (`tag != delim_tag`) in the inner loop that `consume_group` (`validator.hpp:363`) and `consume_group_extent` (`offset_table.cpp:476`) share verbatim, before the membership check FR-010a/FR-007a's mechanisms describe is ever reached.

  Both requirements are therefore discharged by a **recorded negative result with the shapes tried** — never by silence (T079 (ii)) — and their pins (`tests/wire/delimiter_divergence_wire_test.cpp::DelimiterDivergenceWire.TrailingMessageFieldNotSwallowedIntoLastInstance` and `.PollutedContextAcceptsInjectedTagTodayRejectsAfter`) assert the unchanged rejection on both sides of the fix rather than an impossible accepted-then-rejected transition.
- **The baseline measurement's root-cause split is sound but not proven.** A wrongly-scanned delimiter that coincidentally equalled another context's declared first member would be attributed to the wrong cause. The split reproduces fixpp#208's independently-derived tag list exactly, which is strong corroboration but not proof.
- **~~The baseline figures are verified, but the mechanism by which FIX42 registers group contexts at all is an open question.~~ RESOLVED 2026-07-31 (T013/T014) — D-12 lands on a THIRD branch: FIX42 registers no group contexts at all, and the round-1 measurement that said otherwise is not reproducible.** The superseded claim was that `(R,{},146)` is a genuine per-context entry (size 32, distinct storage), that FIX42 yields CTX-HIT=38 / BARE-FALLBACK=0, and that "the 335 and 52 totals stand". Instrumenting all three of D-12's legs directly on the current tree refutes it:
  - **Leg 1** — iterating `message_fields("R")` exactly as `dictionary.cpp:445` does: tag 146 is present, `type = Int`, `group_no_tag = 0`. The gate skips it.
  - **Leg 2** — `Dictionary::field_ref("R", 146)`: present, `type = Int`. **Identical**, and identical *by construction*: `field_ref_impl` (`dictionary.cpp:67-81`) and `message_fields_impl` (`:236-243`) both call `find_msg_fields` and slice the **same** `fields_` array, and `field_ref_impl` returns `*it`. There is no composition step that could make them diverge, so **branch (b) (type promotion) is impossible**, not merely unobserved. `xml_loader.cpp:592` (`no_fr.type = nit->second.type`) assigns the count field its global declared type, and `dictionaries/FIX42.xml:2084` is the sole declaration of tag 146 — `type='INT'`.
  - **Leg 3** — control tag 382 behaves as a control should, and the discriminator was sanity-checked against FIX44's genuinely `NUMINGROUP`-typed tag 453, which reports REGISTERED with distinct non-null pointers.
  - **The entry does not exist.** Store #1 returns 46 (non-zero, structural, gate-independent); stores #2 and #3 both return `data() == nullptr, size() == 0`. Cross-validated by the checked-in pin: FIX42 is **38 unregistered of 38**.

  So **branch (a) also fails** — there is no orphan entry requiring an unaccounted population path. The disposition is **branch (c): the documented gate explains the behaviour completely; the round-1 observation measured a different surface.** One partial reconciliation was tested and is recorded as unresolved provenance: FIX44 also types 146 `NUMINGROUP` and also has a `msgtype='R'`, and `FIX44 (R,{},146)` *is* registered — but at size **100**, not 32. Plausible in direction, unconfirmed at the number. The round-1 text is preserved in `research.md` rather than overwritten.

  **Two consequences, both favourable.** (i) `contracts/group_ctx_delims.md` **C-3.4a** makes its set-equality leg conditional on branch (b) *"because `fr.type == NumInGroup` meaning the same thing on both sides is precisely the predicate D-12 shows is not understood."* That predicate is now understood — it means the same **bytes** on both sides — so the conditionality is discharged and the leg holds **unconditionally**; T040/T041 get simpler, not harder. (ii) The 335 and 52 totals do **not** stand: FIX42's 8 wrong and 4 polluted were scored against a surface that registers nothing, which is precisely the two-cell delta reconciled in "Baseline, re-measured". **L-063-1 / L-066-1 (issue #196) is broader at the `table_view` layer than any existing disposition records**: it is not a narrow "group-blind shorthand" but total — FIX40, FIX41 and FIX42 register **zero** group contexts, plus one isolated context in FIX43 (`<field number='576' type='INT'/>`, msg `AE`) that no prior L-066-1 disposition names.
- **The ten dictionaries in the repository are the full population.** No dictionary outside those shipped is considered.
- **The C ABI's exported signatures do not change.** This feature changes behaviour reachable through a frozen ABI, not the ABI surface itself; it is nonetheless treated as ABI-affecting for gate purposes, since the observable contract changes for existing callers.

## Normative References

Per `[const §VI.5]` (`.specify/constitution.md:164`), the exact coverage-index and behaviour-record entries that inform this spec. **No new OFFICIAL catalogue rows are introduced** — every requirement here is correctness of an existing row's behaviour on shipped dictionaries; traceability is via existing rows plus B&L L-row updates (`[const §VI.4]` is therefore not engaged; `[const §VI.6]`'s closure conditions are satisfied by the FR-019 B&L rows and the FR-017 count justification).

- **`[FIX50SP2 §3.2] Repeating groups (NoXxx delimiter, ordered field list, nested groups)`** — `spec/coverage-index.md:184` (catalogue rows W-006 / W-007 / D-010; the 063 context-scoped membership and 072 nesting/delimiter-guard precedent). This is the row the whole feature corrects: the *NoXxx delimiter* clause is FR-001..FR-005, the *ordered field list* clause is FR-002/FR-010, and the *nested groups* clause is FR-007..FR-009 and FR-021.
- **`[FIX50SP2 §3] Message validator — required fields, type conformance, enum values, group structure`** — `spec/coverage-index.md:189` (catalogue row W-014). The *group structure* clause is what the delimiter and member-set corrections change on the opt-in strict inbound path (FR-010, FR-010a, SC-013).
- **`[FIX50SP2 §3] Tag=Value encoding — field format, SOH delimiter, ASCII`** — `spec/coverage-index.md:181` (catalogue row W-001). The wire-side substrate the typed-read splitter divides (FR-021/021b/021c); the splitter's boundary rule is a §3.2 question resolved over a §3 encoding.
- **`[const §X.1]` / `[const §X.6]`** (`.specify/constitution.md:220`, `:225`) — the C ABI is a versioned contract, GA-frozen at 1.5.0, and an ABI-affecting feature triggers all four mandatory controls. FR-018/FR-018a and SC-012 change behaviour reachable through that frozen surface without changing a signature; the four controls are tracked in plan.md's Constitution Check.
- **`spec/behaviors-and-limitations.md` L-063-3** (`spec/behaviors-and-limitations.md:1699`) — residual (b), *the global first-seen delimiter*, recorded as STILL-LIVE and reachable on shipped dictionaries (FIX44 `NoMDEntries(268)`: `269` in `MDFullGrp` vs `279` in `MDIncGrp`). This is fixpp#210's defect, stated in the repository before the issue was filed; FR-001 closes it and this row is retired at close-out.
- **`spec/behaviors-and-limitations.md` L-063-4** (`spec/behaviors-and-limitations.md:1701`, `spec/coverage-index.md:995-999`, issue **#180**) — the flat `group_slices()` splitter and its redundant cap loop, already characterised with a measurement, a mechanism, and a synthetic reproduction. FR-021a's characterisation starts here (FR-021d), and the row's "0 collisions" measurement must be re-derived under the post-fix delimiters (FR-012a, SC-014). *(Re-dispositioned 2026-08-02, Gate B r2, fixpp#216 P1-B — the paragraph below previously read "this feature lands only the FIRST of L-063-4's two recommended legs" and "#180 therefore remains open", both false at close-out: T054's characterisation found the FIRST leg's target population unreachable on all ten shipped dictionaries, so it was descoped rather than delivered, and `#180` was already closed by 072 before this feature began.)* **This feature lands NEITHER of L-063-4's two recommended legs**: the first — *"make the splitter nesting-aware"* — is **descoped with measured evidence**; the second — *"fold the redundant flat cap loop into the same traversal"* (`offset_table.cpp:559-570`, still scoped out by FR-021c after the round-3 amendment) — is **outstanding, now tracked by fixpp#214**. **`#180` was already CLOSED by 072 on 2026-07-13; 083 neither closes nor reopens it.** The row is updated with both legs dispositioned separately, not retired. The row's re-statement carries a **third** disposition that is not one of its own legs: L-063-4 also asserts `consume_group_extent()` computes the nesting-aware `group_end` **correctly**, which is false for the delimiter-is-a-nested-group's-count-tag shape — a shape its "0 collisions" audit never covered and that this feature makes reachable in **485 contexts** *(corrected 2026-08-02, Gate B r2 — superseding the earlier "262" figure; see `spec.md:342-346` / SC-016)*. FR-021e repairs it, and the row records the correction so a later reader does not re-inherit the claim (the way FR-021d's characterisation nearly did).
- **`spec/behaviors-and-limitations.md` L-063-1 / L-066-1** (`spec/behaviors-and-limitations.md:1695`) — the FIX40/41/42 `INT`-typed `NumInGroup` carve-out (issue **#196**). Cited as *engaged but unresolved*: the FIX42 baseline row in this spec is measured and stands, yet the mechanism producing a per-context entry for an `INT`-typed tag is unexplained by these rows as written (research.md **D-12**). Whichever branch resolves it, this row is affected — either its "group-blind" shorthand is narrower than stated, or a population path exists that neither it nor this feature has accounted for.
- **`spec/behaviors-and-limitations.md` L-073-1 / L-065-2** — the fixed-arena silent-truncation family. Cited because `offset_table.cpp:597`'s reserve estimator consumes the member sets this feature makes exact (FR-021c).

## Dependencies

- Requires the independent document walk used by the existing dictionary census, extended to record declaration order (it currently records members as an unordered set, which cannot express a delimiter). **The extension must be purely additive** — a new record alongside the existing member sets, not a reshaping of them. The existing sets are consumed by census pins on a parked sibling branch that cannot currently be built or re-run, so changing their shape would break pins with no way to observe the breakage.
- Requires the per-release external-engine interop gate for **observation only** (FR-020) — it records divergence, it does not gate the resolution rule. The reference engines are currently absent from the working copy; because the gate is observational, their absence does not block this feature, but it does mean any divergence is discovered later rather than here.
- Supersedes and closes fixpp#208; its two defects are FR-003/FR-006 and FR-007 here.
- **Handoff to `082-structural-group-detection`, recorded — not a deliverable of this feature.** Two 082 pins are currently written to tolerate the injected delimiter and can be tightened once this lands, and 082's recorded note that a related test fails "5 of its 14 contexts" becomes 2. Those edits **cannot be built, run, or checked from this branch**: research.md D-8 rests its entire "the oracle extension must be additive" decision on 082 being parked on an unbuildable branch where breakage cannot be observed. Scheduling them as a verifiable task here would produce a step that can only be marked done by inspection — a false-green close-out shape. They are therefore a **close-out handoff note**, carried on this branch by a `spec/behaviors-and-limitations.md` line (a tracked artifact that exists today and that 082 will read) and repeated in the feature's phase-4 close-out record under `research/G19-fix-fpml-iso20022/phases/phase-4/` when the pipeline's MARK DONE step creates it, so whoever resumes 082 finds them rather than a stale task list.
