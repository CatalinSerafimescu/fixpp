# Interop / Business-Message Requirements-Quality Checklist: 020-g2-business-messages

**Purpose**: Validate that the REQUIREMENTS for the typed business-message surface, the send-path framing change, and the live interop are complete, clear, consistent, and measurable — before `/speckit-implement`. ("Unit tests for English", not implementation tests.)
**Created**: 2026-06-04
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [data-model.md](../data-model.md) · [contracts/business-messages.md](../contracts/business-messages.md)
**Inferred scope**: depth = release-gate (heading to Gate B); audience = reviewer; focus clusters = (1) typed builder/send-path contract, (2) live-interop / wire-conformance.

## Requirement Completeness

- [x] CHK001 Are the minimal field sets for both NewOrderSingle and ExecutionReport enumerated exhaustively (tag + type + required) so no field is left to implementer discretion? [Completeness, Spec §FR-001/§FR-002, data-model §E1/§E2] — PASS: spec §FR-001 lists 7 NOS fields (ClOrdID/11, Symbol/55, Side/54, OrderQty/38, OrdType/40, Price/44, TransactTime/60) all with tag+type+required; data-model §E1 table mirrors them exactly. spec §FR-002 lists 9 ExecRpt fields (OrderID/37, ExecID/17, ExecType/150, OrdStatus/39, Symbol/55, Side/54, LeavesQty/151, CumQty/14, AvgPx/6) with tag+type+required; data-model §E2 table mirrors them. Realizability sub-check: both builder entities are free functions (non-value-type); NOS/ExecRpt read flyweights are pointer-sized (static_assert confirmed in Messages.hpp line 6798/19230), complete over MessageView<Index>*. No latent completeness gap.
- [x] CHK002 Are the requirements for the send-path framing change stated as three distinct, independently checkable obligations (MsgType field-3, digit-only BodyLength, valid checksum)? [Completeness, Spec §FR-004a] — PASS: spec §FR-004a explicitly states (i) MsgType(35) is the third field, (ii) BodyLength(9) is digit-only/unpadded (cites `.specify/2b-wire.md`), and (iii) CheckSum(10) is valid — three independently enumerated obligations; INV-1 names the test asserting all three on captured bytes.
- [x] CHK003 Is the opaque-payload validation requirement fully enumerated for every reject trigger (empty, no leading 35=, duplicate 35=, each embedded session/trailer tag)? [Completeness, Spec §FR-016] — PASS: spec §FR-016 and data-model §INV-8 enumerate: empty payload, duplicate `35=`, and embedded session header/trailer tags (`8/9/34/49/52/56/10`) — all nine tags listed. Reject via `error::app_payload_malformed` (slot 131) with no transmit/seqnum. Named test `OpaquePayload_Malformed_RejectedNoSeqnumConsumed`.
- [x] CHK004 Are requirements defined for BOTH interop roles (fixpp-initiator and fixpp-acceptor) against EACH live engine, rather than a single direction? [Completeness, Spec §FR-010/§FR-011] — PASS: spec §FR-010 requires both roles (fixpp-initiator↔QFJ-acceptor AND fixpp-acceptor↔QFJ-initiator) for QFJ; spec §FR-011 requires the same for QFcpp; US2/US3 acceptance scenarios AC1+AC2 each address both directions. Tasks T012/T016 confirm the in-repo test covers both roles with skip-without-counterparty.
- [x] CHK005 Is the responding counterparty `Application`'s behavior specified (one ExecutionReport per NewOrderSingle, with exact field semantics) for all three responders (QFJ, QFcpp, fixpp-acceptor fixture)? [Completeness, Spec §FR-010, data-model §E3] — PASS: spec §FR-010 + data-model §E3 specify fully-filled ExecRpt semantics (ExecType=F/OrdStatus=2/LeavesQty=0/CumQty=OrderQty/AvgPx=Price) for all three responders; §E3 explicitly identifies both reference-engine responders and the fixpp-acceptor fixture and notes both are test fixtures, not production entities. Tasks T013/T014/T017 implement all three.
- [x] CHK006 Are both forward-deferral obligations recorded with a defined destination (B&L + deferred-work registry) rather than merely mentioned? [Completeness, Spec §FR-015a/§FR-015b, §SC-006] — PASS: spec §FR-015 states both deferrals must be captured in `spec/behaviors-and-limitations.md` AND the deferred-work registry (explicitly named destinations); SC-006 confirms "recorded as tracked, scheduled deferrals — discoverable in the behaviors-and-limitations catalogue and the deferred-work registry"; tasks.md T019 assigns the exact B&L labels (L-020-1/L-020-2) and registry entry. FR-015b is additionally flagged as "scheduled as a post-v1.0 obligation".
- [x] CHK007 Does the spec define what the catalogue/coverage-index update IS (gap-note / partial-evidence) and IS NOT (a done-flip)? [Completeness, Spec §FR-014] — PASS: spec §FR-014 is unambiguous: A-001/A-006 stay `backlog` with a gap-note (partial G2 interop evidence); coverage-index records a partial-evidence note (NOT a closure); explicit rationale "codegen-owned all-version rows, this hand-written minimal slice does not deliver full semantics"; plan §VI row confirms "RESOLVED (no row flip)". SC-001 further reinforces "does NOT close those all-version official rows".

## Requirement Clarity

- [x] CHK008 Is "minimal" defined by an explicit field list (not a vague "required-tag set") so scope creep vs. full-coverage is unambiguous? [Clarity, Spec §FR-001/§FR-002] — PASS: spec §FR-001/§FR-002 enumerate exact tag numbers + types + required status for every minimal field. The spec explicitly states "v1.0 is deliberately MINIMAL" and Out-of-Scope lists exactly what full coverage would add (TimeInForce/Account/Currency/ExecInst/repeating groups). No vague "required-tag set" — it is a concrete enumerated list.
- [x] CHK009 Is the numeric type and its serialization/parse API named precisely (`fixpp::decimal_t`, `format(span)` / `parse(span,mr)`) with no residual `core::Decimal`? [Clarity, Spec §FR-007] — PASS: spec §FR-007, data-model §E1/§E2, contracts/business-messages.md, and quickstart.md all use `fixpp::decimal_t` (= `core::decimal<FIXPP_DECIMAL_T>`). Clarifications note explicitly states the earlier `core::Decimal` was fiction and was corrected at Gate A round 1. Serialization API is `decimal_t::format(std::span<std::byte>)` and parse is `decimal_t::parse(std::span<const std::byte>, std::pmr::memory_resource*)` — both named with signatures in the contracts section. No residual `core::Decimal` found anywhere in the bundle.
- [x] CHK010 Is "fully-filled ExecutionReport" quantified with exact field values (ExecType=F, OrdStatus=2, LeavesQty=0, CumQty=OrderQty, AvgPx=Price)? [Clarity, Spec §FR-002] — PASS: spec §FR-002 is precise: "ExecType=Trade (`150=F`, the FIX 4.4 wire value for a fill)", "OrdStatus=Filled (`39=2`)", "LeavesQty=0", "CumQty=OrderQty", "AvgPx=Price". Also explicitly notes the FIX 4.4 wire value distinction (`F` not `2` which the 019 witness used). Data-model §E2 confirms in the "Round-trip value" column of the field table.
- [x] CHK011 Is the OrdType=Limit fixing stated as a builder invariant (not a caller parameter) for v1.0? [Clarity, Spec §FR-001, data-model §E1] — PASS: spec §FR-001 states "OrdType is fixed to Limit (`40=2`) and Price (44) is therefore always required"; data-model §E1 marks OrdType row as "(fixed `2`=Limit)" and "builder emits Limit; not a caller parameter for v1.0"; contracts/business-messages.md comment shows `build_new_order_single` signature has no OrdType parameter; quickstart comment notes "Order type is Limit (`40=2`) in v1.0". Unambiguously a builder invariant.
- [x] CHK012 Is "digit-only / unpadded BodyLength" defined against a citable contract (`.specify/2b-wire.md`) rather than left to interpretation? [Clarity, Spec §FR-004a] — PASS: spec §FR-004a explicitly cites `.specify/2b-wire.md` as the contract source, noting "`wire::Writer::commit()` already honors" digit-only BodyLength. research.md D1 further cites `[FIX50SP2 §3.3]` `Length` data type "without leading zeroes". 2b-wire.md confirmed to exist and contain "digit-only BodyLength" language at line 421. The obligation is anchored to a citable contract.
- [x] CHK013 Is the TransactTime validation requirement precise about what "well-formed UTCTimestamp" means (length + shape, fail-closed)? [Clarity, Spec §FR-008] — PASS: spec §FR-008 states "builder MUST validate it fail-closed as a well-formed UTCTimestamp (length + shape)"; data-model §E1 validation section states "ill-formed `transact_time` UTCTimestamp"; contracts/business-messages.md states "FR-008 validation"; spec US1 AS5 enumerates "ill-formed UTCTimestamp" as a named invalid-input case. The "length + shape" phrasing matches the 016 F1 lesson precedent (SendingTime); it is precise enough for a TDD-RED test to be written without further spec consultation.
- [x] CHK014 Is the builder failure contract clear about output state (returned span absent; `out` contents unspecified; caller must not inspect)? [Clarity, Spec §FR-005, data-model §INV-4] — PASS: spec §FR-005 states "On failure the returned span is absent and the caller MUST NOT inspect `out` (fail-closed; no partial/malformed emission)"; data-model §INV-4 adds "the builder builds into a local stack scratch buffer and copies into the caller `out` only on full success, so on error `out`'s contents are unspecified and the caller MUST NOT inspect them"; contracts/business-messages.md Atomicity section is identical. Three corroborating statements in different artifacts make the output-state contract unambiguous.

## Requirement Consistency

- [x] CHK015 Are the body-only builder requirement (FR-004: no 8/9/34/49/52/56/10) and the send-path stamping requirement (FR-004a: engine stamps header) consistent and non-overlapping? [Consistency, Spec §FR-004/§FR-004a] — PASS: spec §FR-004 covers what the builder MUST NOT emit (session header tags + trailer); spec §FR-004a covers what the send path MUST do (hoist MsgType to field-3, digit-only BodyLength, valid checksum). The two are defined as complementary non-overlapping halves: builder outputs body starting with `35=`; send path places that body after the engine-stamped header fields at the correct wire position. data-model §INV-2 and contracts send-path obligation section confirm the same split. No overlap or conflict.
- [x] CHK016 Do the read-side requirements (consume generated `fixpp::v44` accessors) and write-side requirements (new hand-written builders) stay consistent about what is new vs. reused? [Consistency, Spec §FR-003/§FR-006, plan Structure Decision] — PASS: spec §FR-003 (builder, new) vs §FR-006 (read, consume generated flyweights) are distinctly scoped. Plan Structure Decision explicitly labels "READ side: NO new source — consume generated `fixpp::v44` flyweights" and "WRITE side: NEW minimal hand-written builders". Complexity Tracking confirms the read path is reused codegen output; research.md D2 grounds the decision. No inconsistency: both the spec and plan agree on exactly which half is new and which is reused.
- [x] CHK017 Is the numeric type used consistently across spec, data-model, contract, and quickstart (all `fixpp::decimal_t`, value-equality comparison)? [Consistency, Spec §FR-007, data-model §INV-3] — PASS: audit across all four artifacts: spec §FR-007 uses `fixpp::decimal_t`; data-model §E1/§E2 tables use `const fixpp::decimal_t&`; data-model §INV-3 specifies `decimal_t` value-equality; contracts/business-messages.md specifies `const fixpp::decimal_t&` parameters; quickstart.md uses `decimal_t` and notes "Compare numerics by decimal_t value-equality". No `core::Decimal` or `double` anywhere in the post-Gate-A-round-1 bundle.
- [x] CHK018 Are the FR-015a/FR-015b sub-labels used consistently as first-class IDs across spec, tasks (T019), and Out-of-Scope? [Consistency, Spec §FR-015] — PASS: spec §FR-015 defines FR-015a and FR-015b as sub-labels in the FR-015 entry; Out-of-Scope section references "FR-015a" and "FR-015b" explicitly; tasks.md T019 cites "L-020-1 (minimal-field-only, full FIX 4.4 field/group coverage deferred — FR-015a)" and "L-020-2 (…FR-015b)"; T020 lists "FR-001..FR-016 (incl. FR-004a/FR-015a/FR-015b)" for the completeness audit. Consistent usage throughout.
- [x] CHK019 Does the catalogue requirement (FR-014, A-001/A-006 stay backlog) stay consistent with SC-001's "records partial G2 evidence; does NOT close the rows" wording? [Consistency, Spec §FR-014/§SC-001] — PASS: spec §FR-014 states "WITHOUT flipping catalogue rows A-001/A-006 to `done`"; SC-001 states "records partial G2 interop evidence against catalogue A-001/A-006; does NOT close those all-version official rows"; plan Structure Decision §VI row states "RESOLVED (no row flip — partial-evidence note only)". All three are consistent. CHK037 additionally confirms no contradictory flip-to-done language appears anywhere in the bundle.

## Acceptance Criteria Quality / Measurability

- [x] CHK020 Is numeric round-trip fidelity stated as objectively measurable (`decimal_t` value-equality, tolerant of trailing-zero forms) rather than byte-match? [Measurability, Spec §FR-007/§SC-002, data-model §INV-3] — PASS: spec §FR-007 states "Numeric fidelity is asserted by `decimal_t` value-equality (not byte-match)"; SC-002 states "100% value fidelity (no loss/format drift)"; data-model §INV-3 names the test `Builder_NumericFidelity_DecimalValueEquality` and explicitly says "tolerant of a counterparty's trailing-zero form (`190.5` vs `190.50`)". This is objectively decidable: pass iff `decimal_t a == decimal_t b` holds.
- [x] CHK021 Is the wire-conformance assertion specified against the CAPTURED transport/stored bytes (not the lenient `fromApp` view) so it can actually catch a field-order/padding defect? [Measurability, data-model §INV-1] — PASS: data-model §INV-1 explicitly states "Asserted on the captured `transport_send` / stored bytes (not just the `fromApp` MsgType value, which fixpp's lenient parser masks)" and names the test `SendPath_StoredFrame_Field3MsgType_UnpaddedBodyLength_ValidChecksum` with the annotation "RED before the send-path fix". plan.md D1 further emphasizes this exact distinction. The requirement is measurable against the wire-level stored frame, not the application-layer view.
- [x] CHK022 Are the goldens-normalization fields enumerated (52=/60=/34=/IDs + decimal value-form) so "matches goldens modulo non-deterministic fields" is objectively decidable? [Measurability, Spec §FR-012] — PASS: spec §FR-012 states "modulo non-deterministic fields: seqnum, sending/transact time, IDs as applicable"; tasks.md T015 enumerates explicitly: `52=`/`60=`/`34=`/IDs (`11/37/17`) for normalization AND "decimal value-form drift (`190.5`≡`190.50`, compared by `decimal_t` value, not byte)"; research.md D7 mirrors the same list. The normalization set is enumerated specifically enough to write a deterministic golden comparator.
- [x] CHK023 Is the §VIII.5 allocation claim backed by a measurable witness requirement (`counting_resource`, zero heap on the builder write path)? [Measurability, plan §VIII.5, contract] — PASS: plan §VIII.5 states "builder write path writes into caller buffer via `wire::Writer` (no heap — `counting_resource` witness)"; contracts/business-messages.md Conventions section states "an alloc-discipline witness — `counting_resource` over the builder — confirms it"; tasks.md T005 requires `Builder_NoHeap_CountingResource` — "wrap both builders in a `counting_resource` and assert zero heap allocations on the write path". The witness is a named test with a concrete assertion (count == 0).
- [x] CHK024 Is "session survives" after a business reject defined as a checkable state (stays `Active`)? [Measurability, Spec §FR-009/§SC-005, data-model §INV-5] — PASS: data-model §INV-5 states "the session stays Active (FR-009/SC-005)"; spec §FR-009 says "session survives"; SC-005 says "no malformed emission and no session crash"; the named test `InboundReject_EmitsBusinessMessageReject_SessionSurvives` in T007 is the vehicle and tasks.md names the test with "session stays `Active`". `Active` is the named FSM state from the session module, making it a checkable post-condition.

## Scenario Coverage

- [x] CHK025 Are requirements defined for the malformed-inbound scenario (user `fromApp` reject → `BusinessMessageReject(35=j)`), with the responsibility boundary (user code, not auto engine validation) stated? [Coverage, Spec §FR-009, data-model §INV-5] — PASS: spec §FR-009 and the "Malformed / required-field-missing inbound business message" edge case section both state the boundary explicitly: "This is user-`fromApp` responsibility — the engine does not auto-run the v44 validator on inbound app messages before `fromApp`". The requirement specifies the engine reuses 019's BusinessMessageReject path when the user returns a reject. data-model §INV-5 confirms and names the loopback test.
- [x] CHK026 Are requirements defined for the counterparty-rejects-our-order scenario (peer replies reject/ExecRpt-Rejected; fixpp surfaces it, session survives)? [Coverage, Spec Edge Cases] — PASS: spec Edge Cases section has "Counterparty rejects fixpp's NewOrderSingle" — states that if the live counterparty replies with `BusinessMessageReject`/`ExecutionReport(150=8 Rejected)`, fixpp "surfaces it to `fromApp` without crashing and the session survives". This is a named scenario with a defined behavior.
- [x] CHK027 Is the re-entrant send-from-inside-`fromApp` scenario covered by a requirement (named multi-threaded witness before live cells)? [Coverage, Spec Assumptions, data-model §INV-7] — PASS: spec Assumptions section requires "a named loopback test ('send-from-inside-fromApp' under a multi-threaded `io_context`) MUST prove no deadlock/UAF on this re-entrant path; if it cannot, the reply is hoisted off-strand (explicit waiver)"; data-model §INV-7 names the test `SendFromInsideFromApp_NoDeadlockNoUAF` with "multi-threaded `io_context`" qualifier and "RED-verified to exercise the exec-hop, BEFORE the live cells"; research.md D9 grounds the rationale. The requirement includes a concrete multi-threaded witness obligation plus a fallback path.
- [x] CHK028 Is the no-counterparty scenario covered (live cells skip cleanly, not fail)? [Coverage, Spec §FR-013] — PASS: spec §FR-013 states "Live interop cells MUST skip cleanly (not fail) when no reference-engine counterparty is provisioned, per the existing harness skip-without-counterparty contract"; US2 and US3 independent test descriptions both say "skip cleanly when no counterparty is provisioned (existing harness skip-without-counterparty contract)"; tasks.md T012/T016 both note "skip cleanly" requirement.
- [x] CHK029 Is the opaque-payload-from-a-non-builder-caller scenario covered (FR-016 applies to all 019 opaque callers, not just the typed builders)? [Coverage, Spec §FR-016, data-model §INV-8] — PASS: spec §FR-016 explicitly states "this applies to all 019 opaque callers, not only the typed builders"; data-model §INV-8 states "the app-send path validates the opaque `Engine::send` payload before assigning a seqnum or transmitting" — without qualifying it to the typed-builder output only; spec Edge Cases section "Malformed opaque `Engine::send` payload" edge case reiterates "because `Engine::send` still accepts arbitrary 019 opaque byte payloads (not only the typed builders' output)". The broader scope is explicitly stated.

## Edge Case Coverage

- [x] CHK030 Are body field-ordering requirements defined to satisfy BOTH reference engines' tolerance (not just one)? [Edge Case, Spec Edge Cases/§FR-004] — PASS: spec §FR-004 states "business fields in an order both reference engines accept" (plural, not one); Edge Cases "Field ordering" section states "the body field order MUST satisfy both reference engines' tolerance"; research.md D1 notes QuickFIX-cpp and QuickFIX-J both enforce the BeginString/BodyLength/MsgType first-three-fields rule and both are cited as constraints. The requirement is explicitly cross-engine.
- [x] CHK031 Are the invalid-input edge cases for the builder enumerated (empty string, out-of-range enum char, unformattable decimal, ill-formed timestamp, too-small buffer) rather than a generic "invalid input"? [Edge Case, Spec §FR-005, data-model §INV-4] — PASS: spec §FR-005 enumerates exactly five named cases: "(1) empty string for a required string field, (2) an out-of-range enum char (`side`/`exec_type`/`ord_status`), (3) an invalid/unformattable decimal, (4) an ill-formed UTCTimestamp (TransactTime), (5) or a too-small output buffer"; US1 AS5 mirrors the same five-case list; tasks.md T005 test names `Builder_InvalidField_NoUsableOutput` covers all five. Not generic — each case is named and implementer-facing.
- [x] CHK032 Is the distinction between "omitted field" (compile error) and "invalid value" (runtime fail-closed) explicitly stated so the missing-required test target is unambiguous? [Edge Case/Ambiguity, Spec §FR-005, US1 AS5] — PASS: spec §FR-005 states "A required field cannot be omitted: the positional free-function signatures make omission a compile error"; spec US1 AS5 states "A field cannot be omitted — the free-function signatures are positional, so omission is a compile error; the runtime guarantee is fail-closed-on-invalid-value"; contracts/business-messages.md Conventions section: "Omission is impossible — positional signatures make it a compile error". The distinction is triply stated and unambiguous: omit = compile error (not a runtime test target), invalid value = runtime expected_t error (the test target).

## Non-Functional Requirements

- [x] CHK033 Is the threading/concurrency requirement honest (no NEW concurrency surface; relies on 019's any-thread re-entrant `Engine::send`) rather than overclaiming? [Non-Functional, Spec Assumptions, data-model §INV-6] — PASS: spec Assumptions section states "No new engine concurrency surface is introduced by this feature. This RELIES on 019's any-thread re-entrant `Engine::send`"; data-model §INV-6 states "no new threading surface"; plan §XI.4 row: "reuses 019's strand/keepalive contract for `Engine::send`; no NEW concurrency surface". The re-entrancy risk (INV-7) is called out explicitly as something that must be demonstrated, not assumed (research D9). This is honest about what is new vs. reused.
- [x] CHK034 Is the sanitizer/coverage expectation stated for the highest-risk production edit (the `send_impl` change under ASan/UBSan/TSan + multi-threaded INV-7)? [Non-Functional, plan §IX.2, tasks Build/verify discipline] — PASS: plan Build/verify discipline section states "The send-path change (T009/T010) is the highest-risk production edit — run ASan/UBSan/TSan on `test_business_messages_roundtrip` + the live-outbound suites before sign-off"; plan §IX.2 row: "ASan/UBSan/TSan on the send-path MsgType-ordering change + interop ctest (per 018 discipline)"; tasks.md Build/verify discipline section restates the same requirement with the multi-threaded INV-7 annotation. The highest-risk edit is specifically called out for the full sanitizer matrix.

## Dependencies & Assumptions

- [x] CHK035 Is the assumption "the minimal field set is sufficient for clean QFJ/QFcpp acceptance" stated as validatable-at-bring-up (additions count as 'minimal', not scope creep)? [Assumption, Spec Assumptions] — PASS: spec Assumptions first bullet states "the minimal field sets enumerated above are sufficient for a clean single-order round-trip against QuickFIX-J and QuickFIX-cpp on FIX 4.4; any additional tag a reference engine demands for acceptance will be added to the minimal set during interop bring-up (treated as part of 'minimal', not as full-coverage scope creep)". This explicitly makes the assumption validatable and defines the boundary: bring-up additions are in-scope as minimal, while additional business fields beyond what engines require stay deferred (FR-015a).
- [x] CHK036 Are the upstream dependencies (019 Application/Engine::send + A-014; 016/018 harness seam/goldens; generated `fixpp::v44`; reference-engine versions) documented? [Dependency, Spec Dependencies] — PASS: spec Dependencies section lists: (1) 019-app-callbacks (MERGED) — Application/Engine::send/BusinessMessageReject A-014; (2) 016-interop-harness/018-interop-live-admin (MERGED) — both-role orchestration/seam/TLS/skip; (3) wire/ + dict/ (PR #68/#66/#67); (4) QuickFIX-J 3.0.1 and QuickFIX-cpp v1.16.0 with explicit version numbers; (5) generated `fixpp::v44` flyweights via the codegen. All five dependency classes are documented with merge status and/or version numbers.

## Ambiguities & Conflicts

- [x] CHK037 Is there any residual "flip A-001/A-006 to done" language anywhere in the bundle that conflicts with FR-014's backlog+gap-note decision? [Conflict, Spec §FR-014] — PASS: exhaustive search across spec.md, plan.md, tasks.md, data-model.md, contracts/business-messages.md, research.md, quickstart.md — no "flip to done" or "close A-001/A-006" language found. All occurrences uniformly state "stays `backlog`", "gap-note", "partial-evidence note", "NOT close", "no done-flip", "no row flip". The Gate A round 1 rewrite specifically removed the earlier catalogue overclaim (RC1). No conflict with FR-014.
- [x] CHK038 Is the architecture placement of `session/business_messages.hpp` resolved (precedent + `check_layers.py`, with the §4.4-amendment-or-waiver obligation captured) rather than left as a false "[arch §4.4] sanctions it" claim? [Ambiguity, plan Structure Decision, tasks §T019] — DD-DECIDED §research D3 + plan Structure Decision: the plan Structure Decision section explicitly corrects the earlier false §4.4 claim: "the placement rests on two real grounds, not a §4.4 sanction: (i) de-facto precedent — `admin_messages.hpp` already places body/frame builders in `session/`; (ii) the `tools/check_layers.py` gate enforces actual layer direction at `/implement`." The plan further states the hand-written builders "need an explicit one-line `architecture.md` amendment sanctioning `session/business_messages.hpp` as a hand-written body-builder bridge" — and tasks.md T019 captures the obligation explicitly as a Polish task: "add the one-line `.specify/architecture.md §4.4` amendment sanctioning `session/business_messages.hpp` as a hand-written body-builder bridge (per plan Structure Decision), OR record a waiver that `tools/check_layers.py` enforcement suffices". The obligation is captured in T019 as a deliverable; the false claim is corrected; the §4.4-amendment-or-waiver is not left open — it is assigned to the Polish phase with two explicit resolution paths. The ambiguity is resolved to a concrete implementer obligation.

---

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 37 |
| SPEC-FIXED | 0 |
| DD-DECIDED | 1 |
| WAIVED | 0 |
| **Total** | **38** |

### SPEC-FIXED items

(none)

### DD-DECIDED items

- CHK038 — anchor `research §D3` + `plan Structure Decision` + `tasks §T019`; rationale: the §4.4 placement ambiguity is frozen-authority in the bundle's plan Structure Decision (corrected at Gate A round 1 from the false §4.4-sanctions-it claim); the amendment-or-waiver obligation is a concrete T019 deliverable, not a spec gap.

### WAIVED items

(none)

Anchors spot-verified:
- `[const §VII.6]` — resolves to constitution.md Article VII §6 (line 92): "Interop: v1.0 includes at least one interop test against an independent FIX implementation (QuickFIX) covering Logon → NewOrderSingle → ExecutionReport → Logout."
- `[const §VIII.5]` — resolves to constitution.md Article VIII §5 (line 106): "Allocator policy on the hot path: zero `new`/`delete` between parse and `fromApp` callback."
- `[const §XV.13]` — resolves to constitution.md Article XV §13 (line 221): "Eager codegen with no runtime dictionary path. Hybrid mandated."
- `[arch §4.2]` — resolves to architecture.md §4.2 `dictionary` (line 188): codegen pipeline for `fixpp::v44::*` generated typed messages.
- `[arch §4.4]` — resolves to architecture.md §4.4 `session` (line 234): `Session/Application/MessageStore/.../Engine/SessionId` public surface.
- `.specify/2b-wire.md` — exists; contains "digit-only BodyLength" language at line 421 (Writer::commit() algorithm).
- `data-model §INV-1..INV-8` — all resolve in data-model.md Invariants section.
- `research §D1..D9` — all resolve in research.md sections D1 through D9.
- `Spec §FR-001..FR-016` (incl. §FR-004a/§FR-015a/§FR-015b) — all resolve in spec.md Functional Requirements section.
- `Spec §SC-001..SC-006` — all resolve in spec.md Success Criteria section.

All anchors resolve in the signed-off revision of the bundle (Gate A round 2, 2026-06-04).
</content>
