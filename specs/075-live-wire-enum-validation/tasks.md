# Tasks: Live-Wire Enum-Value Validation (Dictionary-Driven Enum Domain Checking)

**Input**: Design documents from `specs/075-live-wire-enum-validation/`
**Prerequisites**: plan.md ✅, spec.md ✅ (24 FRs / 11 SCs / 3 user stories), research.md ✅, data-model.md ✅, contracts/enum-domain.md ✅, quickstart.md ✅
**Gate A**: CONVERGED round 5, user-signed-off 2026-07-14 (submodule `8c8ec699`). Records: `.specify/decisions/075-live-wire-enum-validation-gatea.md` + `research/reviews/{codex,opus}_075-*_gate_a{,_2,_3,_4,_5}*.md`.

**Tests**: REQUIRED and mutation-discriminating. Every witness below names the mutation that must turn it RED (`plan.md` § Test matrix). A witness that passes under its own mutation is a defect, not a witness (`[[feedback_coverage_push_enshrines_bugs]]`).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: parallelizable (different files, no dependency on an incomplete task)
- **[Story]**: US1 / US2 / US3 — user-story phases only
- Every task carries an exact file path.

## Test-binary discipline (`[const §VII.8]`, `.specify/constitution.md:131`)

Tests join **grouped** binaries and are selected by **`ctest -L <label>`**, **never** by `-R <exe-name>`. **Isolation-sensitive** tests — global alloc/OOM interception, exact-set completeness gates, own `main()`, data-driven exact gates — stay **standalone**. Per `plan.md`'s label-discipline note: `tests/wire/` buckets carry **no `LABELS`** today (name-selected, `tests/wire/CMakeLists.txt:1-7`); that is **pre-existing and NOT 075's to fix**. 075 sets `LABELS` on the buckets and standalone targets it **adds or joins**, so every *new* test below is `-L`-selectable.

---

## Phase 0.5: The golden is a BLOCKING FIRST deliverable ⚠️ GATE

**This phase BLOCKS the entire parity leg (FR-018 / FR-019 / FR-024 / SC-009) and MUST NOT be reordered after the FRs it validates.** Gate A root cause **RC#1**: the original bundle wrote the parity claims first and designed the corpus from the same partial reading of QuickFIX's call graph it was meant to check — so the corpus was structurally blind to that reading's errors. Two such errors were found by *review*, not by the corpus (the group-member divergence DV-3; the type-arm-dependent empty value DV-1/DV-2). **The golden's measured output DEFINES the divergence register** (`contracts/enum-domain.md` C-6); it is not evidence for a claim written elsewhere. A divergence the golden reveals that is **not** an argued register row is a **defect that blocks the feature** (SC-009).

- [ ] T001 Re-measure **every `asserted: true` literal** in the FR-018 corpus against the shipped `dictionaries/*.xml` **before writing the generator**, and record the result in the FR-018 audit table (`specs/075-live-wire-enum-validation/spec.md:184-195`). Confirm: `Side(54)` = `1`–`9`,`A`–`G` (no `Z`); `ExecInst(18)` = 40 codes incl. `1`,`G`,`6` (no `ZZ`); `PossDupFlag(43)` = `Y`,`N` (no `X`); **`MatchType(574)`** = `A1`–`A5`,`AQ`,`S1`–`S5`,`M1`,`M2`,`MT`,`M3`–`M6` — **no bare `A`** (`STRING`, single-value, top-level in `TradeCaptureReport(35=AE)`); `SettlLocation(166)` on **FIX41/FIX42** = `CED`,`DTC`,`EUR`,`FED`,`ISO Country Code`,`PNY`,`PTC` (no `US`). **A `reject`-asserting row that BOTH engines ACCEPT is a defect, not a row** — it coincides with the stub (`enum_valid → true` accepts everything), passes green forever, and no mutation in this bundle can redden it. *(This is exactly how the old `277=A` row got in — `TradeCondition(277)` actually declares `A`. Gate A round 2, O2-1.)* The **accept**-asserting rows 1 and 3 are the paired positive controls against **over**-rejection and are NOT condemned by this rule.
- [ ] T002 Create the local-only golden generator `tools/quickfix_enum_golden/` (new dir: `main.cpp`, `CMakeLists.txt`) linking **real QuickFIX v1.16.0** `DataDictionary::validate`. Root = validated CMake cache variable **`FIXPP_QUICKFIX_ROOT`** (default `${CMAKE_SOURCE_DIR}/../../../reference-engines/quickfix-cpp` — the **parent** repo root, three levels up, **outside** the submodule's git boundary). Target **guarded OFF by default**; it MUST **hard-error** if the variable is set but `lib/libquickfix.so` is absent. *(That structural guard — not a gitignore claim — is what keeps CI from ever reaching the tree; the draft's "reference-engines/ is gitignored" was true only by accident. Gate A round 1, O-4.)*
- [ ] T003 Pin the generator's QuickFIX configuration **in the generator's own source, never as CLI arguments** (FR-019), so `generator_source_hash` genuinely pins it — a runtime-configurable flag would be recorded but unpinned. Pin **all four booleans**: `m_checkFieldsHaveValues` (**default `true` — this is the switch that front-ran the enum check and produced the bundle's original false parity claim**), `m_checkFieldsOutOfOrder`, `m_checkUserDefinedFields`, `AllowUnknownMsgFields`. Pin the **dictionary topology** — **single-DD / non-FIXT, `sessionDD == appDD`**, applied **per the corpus row's own dictionary** (the corpus spans FIX44 **and** FIX41/FIX42, so no single version names it). *Why topology is the bigger lever:* QuickFIX's two-DD FIXT path enum-checks `MsgType(35)` against **FIXT11**, whose `MsgType` declares **zero** `<value>` children ⇒ unconstrained — a validation topology **fixpp does not have** (one `cfg_.dictionary`; `engine.cpp:210`, `session.cpp:992`, `:1234`). A two-DD golden would measure a topology fixpp lacks and FR-015's header-parity claim would be false against it.
- [ ] T004 Implement the **12-row corpus** (FR-018), **every row a message frame driven through `validate()`**, each carrying its `asserted: true|false` discriminator: rows 1–7 + 12 `asserted: true` (in-domain single; **out-of-domain single** — the headline; multi-value all-declared; multi-value one-undeclared; degenerate whitespace; **header** `PossDupFlag(43)=X`; strict prefix `MatchType(574)=A`; `SettlLocation(166)=US` on FIX41/42) and rows 8–11 `asserted: false` (**empty × `Char`**; **empty × `String`**; **group member**; **nested group member**, depth ≥ 2). **There is deliberately NO `validate_field()` row** — QuickFIX has no context-free analogue, and the row schema has no column naming which fixpp surface drives a row, so a uniform harness would drive it through `validate()` where the engines **check in opposite order** (fixpp `field_valid_for` `validator.hpp:143` → reason **2**, never reaching `enum_valid` `:148`; QuickFIX `checkValue` `DataDictionary.cpp:172` **before** `checkIsInMessage` `:178` → reason **5**) ⇒ an `asserted: true` row red by construction, which SC-009 declares feature-blocking. *(Gate A round 4, O4-1. The FIX50SP2 store-only witness lives in T024, the FR-020 unit test.)*
- [ ] T005 Generate and **check in** the golden expectation table `tools/quickfix_enum_golden/golden.csv` (QuickFIX verdict + reject reason per row) with its embedded **manifest** (FR-024), carrying **six** fields: `quickfix_version` + soname (`1.16.0` / `libquickfix.so.17.0.0`); **`dictionary_sha1`** for **every** dictionary the corpus loads; `generator_source_hash`; `corpus_input_hash` (over the frame **inputs**); **`golden_output_hash`** (over the **output rows** — `quickfix_verdict` + `quickfix_reason` + `asserted` — computed **excluding the manifest block it lives in**, else self-referential); and the **topology recorded PER DICTIONARY** alongside each `dictionary_sha1`, plus the four boolean settings. **`golden_output_hash` is load-bearing and NOT implied by the others**: the input, generator and dictionary hashes **all stay valid** when someone hand-edits an `accept` to a `reject` in the checked-in table — without a hash over the *verdicts* the manifest gate is decorative, exactly the false-green it exists to prevent.
- [ ] T006 **DERIVE the divergence register from the golden's measured output** and reconcile `contracts/enum-domain.md` C-6 (DV-1..DV-4) against it. Expected: **DV-1** (empty × `Char` — both reject/5, but fixpp via its **type** arm and QuickFIX via its **enum** arm ⇒ parity *by coincidence*), **DV-2** (empty × `String` — fixpp **accepts**, QuickFIX rejects/5), **DV-3** (group-member enums — fixpp **checks at every depth**, QuickFIX **never** does), **DV-4** (`SettlLocation(166)=US` — both engines **agree** and both reject, but an operator-visible regression that required an argued decision ⇒ a register row under SC-009 leg (2), and it is `asserted: **true**`). **`asserted: false` ⇒ a register row, but NOT conversely.** **Any divergence the golden reveals that is not already an argued register row BLOCKS the feature** until fixed or promoted with an argued disposition + a B-row + a corpus row that measures it. **STOP and report to the user if the golden surfaces an unregistered divergence** — do not silently add a DV row.
- [ ] T007 Add the CI manifest gate `tests/wire/enum_golden_manifest_test.cpp` (**standalone** binary, `LABELS "075;golden;manifest"`), recomputing the dictionary SHA-1s + generator-source hash + corpus **input** hash **and the `golden_output_hash`** from the tree and failing on any mismatch. It runs **without** `reference-engines/`. **What it catches, exactly** *(narrowed at Gate A round 2, O2-3 — the earlier "makes a stale golden fail" was overstated)*: **(i) tree drift** (a dictionary / corpus-input / generator-source change the golden was not regenerated against) and **(ii) a careless hand-edit** of the checked-in verdicts. What it **cannot** catch is drift against a **newer QuickFIX** — `quickfix_version` is *recorded but unverifiable* in a CI that has no QuickFIX by this design's own premise; that leg is **T008's** job, not this one's.
- [ ] T008 **PROVE the manifest gate RED** (quickstart S-7 step 5): hand-edit one verdict in the checked-in `golden.csv`, run `ctest -L 075;golden;manifest`, confirm **RED**, revert, confirm **GREEN**. Record the RED output in the task notes. **A gate never proven RED proves nothing** (`[[feedback_sanitizer_canary_must_be_proven_red]]`) — and this is the single gate standing between the feature and a false-green golden.
- [ ] T009 Add the local **regeneration-and-diff** target (FR-024 item 3) that, **when `reference-engines/` is present**, rebuilds the golden and **fails on drift**; guarded OFF by default. **Bind it to an owner and a cadence, or "stale" has no one to catch it** *(Gate A round 2, O2-3)*: it is the **only** mechanism that can detect QuickFIX-version drift (T007 provably cannot), and an OFF-by-default target with no schedule may never run again after this phase. Land the binding as a **line in the release-interop checklist** (`[[project_release_interop_quickfix_fix8]]` — the per-release out-of-CI QuickFIX/Fix8 interop run, which already owns a local reference-engine tree), **not merely as a CMake target**: run the regen-and-diff on every release-interop pass and treat a diff as a **release-blocking** finding.

**Checkpoint** — the golden + manifest exist, the manifest gate is **proven RED**, and the divergence register is **derived from measured output**. The parity leg is now unblocked. ⚠️ Do not start Phase 2 before this checkpoint.

---

## Phase 1: Setup

- [ ] T010 Confirm the branch (`075-live-wire-enum-validation`), a clean tree, and a green baseline: full `ctest` on the debug preset **before** any edit, so a later RED is attributable. *(`[[feedback_stale_build_objects_false_green_masks_pins]]`: build with `--clean-first` — this feature changes header **sizes** in `table_view.hpp`, and an incremental dyndep build can yield stale-object ODR FALSE-RED sanitizer failures in unrelated code, `[[feedback_header_size_change_incremental_dyndep_stale_object_false_sanitizer]]`.)*
- [ ] T011 [P] Record the pre-change baseline for SC-006: `as_table_view()` build time + `table_view` footprint on **FIX50SP2** (668 enum-backed fields / 5565 codes — the largest case) in `bench/dictionary/`. **Measured, not asserted** (research R-10 / O-3).

---

## Phase 2: Foundational (BLOCKS all user stories)

**These carry the entire behavior change. Three production files.**

- [ ] T012 **[FR-001]** Teach `XmlLoader` to parse `<value enum="…" description="…"/>` into **074's existing** additive tag-keyed enum store — `enum_values_` / `enum_runs_` on `dict_metadata_handle` — in `src/dictionary/xml_loader.cpp`. **No new parallel storage mechanism.** The loader currently never reads these elements at all, which is why all nine QuickFIX dictionaries carry an **empty** enum store today. **Bind the `EnumValueRef` views in the EXISTING post-`shrink_to_fit()` pass** (`xml_loader.cpp:853-865`) — **binding during the parse DANGLES** on the next pool reallocation (research R-4; this is ranked risk 5).
- [ ] T013 **[FR-017]** Loader tolerance/strictness on code-set XML, matching QuickFIX, in `src/dictionary/xml_loader.cpp`: a **duplicate** `<value enum="X">` on one field is **deduped** (union semantics, no error — QuickFIX stores codes in a `std::set`, so a duplicate is idempotent for a membership test and fail-closed would buy no correctness while rejecting third-party dictionaries QuickFIX loads); a `<value>` **missing its `enum`** attribute is a **load failure** (fail-closed, mirroring QuickFIX's `ConfigError`, using this codebase's existing `xml_parse_error` family per the **catch-discriminated** 072/074 precedent); a `<value>` **missing its `description`** is **legal** and yields an empty description view (diagnostics-only — it MUST NOT fail the load). *(074's fail-closed reflex does **not** transfer: its duplicate guards were on **structural ids**, where a duplicate is a genuine which-definition-wins ambiguity, i.e. silent **loss**.)*
- [ ] T014 **[R-10]** Extend `pool_estimate` (`src/dictionary/xml_loader.cpp:646-658`) with the **code + description byte count**. It today sums **names only** and will be short by the full code+description bytes on **every** dictionary. **Reserve TIGHT, not loose** (`[[feedback_fixed_arena_over_reserve_silent_loss_larger_stl]]`: a loose reserve into a fixed null-upstream arena over-allocates, MSVC's larger STL metadata exhausts it, and the result is a **silent** empty read). Name the upstream `memory_resource` each loader is handed and state whether it is bounded.
- [ ] T015 **[FR-002/FR-005]** Project the enum-domain table into `table_view` in `src/dictionary/dictionary.cpp` (`as_table_view()`), built **once** at table-construction time and **never** rebuilt per message (`[const §XV.1]`, `[const §VIII.5]`). **The projection is STORE-DRIVEN, not `message_fields()`-driven** — iterate `dict_metadata_handle::enum_runs_` / `enum_values_` so **every** enum-backed tag gets a domain, **then** use `message_fields()` only to set `multi_value=true` where `FieldRef` data exists. ⚠️ **This is the C3-1 fix and it is the reason Gate A exhausted its first loop.** A `message_fields()`-only projection leaves the **35** message-unreachable enum-backed tags (**10 / 11 / 14** in FIX50 / FIX50SP1 / FIX50SP2 — the FIXT split; incl. `MsgType(35)`, `EncryptMethod(98)`, `MsgDirection(385)`, `ApplVerID(1128)`, `SessionStatus(1409)`) with **no domain**, so `validate_field()` — which calls `enum_valid()` as its **first statement with no `field_valid_for` precheck** (`validator.hpp:323-330`) — **silently ACCEPTS** out-of-domain values on three shipped dictionaries, through a frozen public API. That violates **FR-003, a normative MUST**. `table_view` **owns copies** of the code bytes (Complexity row 2 — aliasing `name_pool_` would convert a currently-legal `table_view`-outlives-`Dictionary` usage into a use-after-free no existing test would catch).
- [ ] T016 **[FR-005]** A store-only tag absent from `message_fields()` defaults to **`multi_value = false`**, in `src/dictionary/dictionary.cpp`. **Measured-safe today**: all **35** message-unreachable enum-backed tags are `STRING` / `BOOLEAN` / `INT` / `CHAR` — **none is a `MULTIPLE*` type** (`MsgDirection(385)` is plain `CHAR`, not `MULTIPLECHARVALUE`). This default is an **assumption**, so it is **pinned by SC-011** (T021) and goes **RED** if a future dictionary violates it, rather than silently false-rejecting a conformant multi-value field.
- [ ] T017 **[FR-003/FR-004/FR-007/FR-008/FR-009/FR-014]** Make `enum_valid()` real in `include/fixpp/dict/table_view.hpp` (replacing the `return true` stub at `:288-294`). **`noexcept`, allocation-free, no `std::string` materialization of the wire value.** Branch order matters — there are **TWO distinct accept-floors, and they are NOT the same rule**:
  1. **[FR-003] Absent tag / empty CODE SET ⇒ ACCEPT.** The anti-reject-everything floor (ranked risk 2) and the sole thing keeping **FIXT11** working at all, since its `MsgType` declares **zero** codes.
  2. **[FR-008] Empty field VALUE ⇒ `enum_valid` returns `true` UNCONDITIONALLY — the enum check is BYPASSED and `check_field_type` decides.** ⚠️ **This is a separate branch from (1), and omitting it is a silent conformant-traffic regression.** A literal byte-exact whole-token compare with no empty-value guard finds `""` absent from **every** codeset and **rejects via the enum arm** — harmless on `Char` (`Side(54)=` still reject/5, same outcome as today) but it **flips empty × `String` from accept to REJECT** (`ExecInst(18)=` would newly reject), which `contracts/enum-domain.md` C-1 explicitly promises will **not** happen. FR-008's decision **(a)** is *"exclude empty values from the enum check and leave today's disposition untouched"* — the resulting disposition is then **type-arm dependent** (`Char` ⇒ reject/5 via `check_field_type`, `validator.hpp:411-417`; `String` ⇒ **accept**, the `ft::String` arm imposes no constraint, `:419-425`), which is exactly **DV-1 / DV-2**. Do **not** route empty values through the enum check: fixpp has **no reason-4 slot**, so doing so would *manufacture* a 5-vs-4 divergence rather than achieve parity. *(Witness: T022a.)*

  Then, for a non-empty value on a tag with a non-empty codeset: single-value ⇒ binary-search the sorted codes (`MsgType(35)` has **93** codes in FIX44 and is on **every** message); multi-value ⇒ tokenize on a **single space** and require **every** token. Comparison is **byte-exact and whole-token** — no case folding, **no prefix matching** (FIX codes are case-sensitive).
- [ ] T018 **[FR-014]** The tokenizer MUST split on a **single space** and MUST **NOT** skip empty tokens, in `include/fixpp/dict/table_view.hpp` — a double space or a trailing space yields an **empty token**, which is never a declared code, and therefore **rejects**. This is **byte-for-byte QuickFIX** (`DataDictionary.h:265-275`) and is **required for interop parity, not merely permitted**. Do **not** invent a more forgiving tokenizer.
- [ ] T019 **[FR-021]** Make **`add_enum(tag, value)` REAL** in `include/fixpp/dict/table_view.hpp` (it currently discards **both** arguments at `:350-352`), populating the same owned enum-domain table `as_table_view()` builds. **If it stayed a no-op**, every builder-built `table_view` would have an empty enum table, FR-003's floor would return `true` for all of them, and **four suites would stay green while proving nothing** — `[[feedback_coverage_push_enshrines_bugs]]` verbatim. It **cannot carry the multi-value bit by itself** (FR-005), so add its **companion** — an `add_enum` overload or `set_multi_value(tag)` — **otherwise FR-004's tokenizer has NO unit-level witness that does not require a full XML load**. Update `tests/support/mock_dict_table.hpp:16` (artifact #6), which exists precisely to keep builder-surface parity with the production type.
- [ ] T020 **[FR-006]** Verify the out-of-domain value surfaces via the **existing** `wire_field_value_out_of_range(40)` slot → **SessionRejectReason 5** with `RefTagID` = the offending tag. **NO CODE CHANGE** is expected in `include/fixpp/wire/validator.hpp` — `:148` (Step-1 walk) and `:325` (`validate_field`) both simply go live. Confirm the Step-1 flat walk (`:139-158`) yields **header + trailer + body + group members at every depth** from **one** loop: `msg.begin()`→`msg.end()` builds a **dict-free `field_iterator` over the raw frame bytes** (`parser.hpp:229-233`, `advance()` at `:444`) — a linear `tag=value` scan with **no group awareness at all**. Group and header coverage is a property of **that single walk**, **not** of a second call site.

**Checkpoint** — the enum domain is live end-to-end. All user stories can now proceed.

---

## Phase 3: US1 — A strict-validating session rejects an out-of-domain enum value (P1) 🎯 MVP

**Goal**: `Side(54)=Z` on a FIX44 session is rejected with SessionRejectReason **5**, `RefTagID=54`, and never reaches the application. This is the entire point of the feature and the only story that turns the dead reject arm live.

**Independent test**: load the shipped `FIX44.xml`, build the validator, feed a frame with `54=Z` ⇒ reject/5/`RefTagID=54`; feed `54=1` ⇒ accept. Mutation: revert `enum_valid` → `return true` ⇒ the first assertion MUST fail.

- [ ] T021 [P] [US1] **[SC-010/FR-016 + SC-011/FR-022]** Dictionary census gates in `tests/dictionary/dict_enum_census_test.cpp` **(new, STANDALONE binary, `LABELS "075;dictionary;census"`)** — standalone because an **exact-set completeness gate is isolation-sensitive** per `[const §VII.8]`. Assert: **(SC-010)** every declared `<message msgtype="X">` is present in the `MsgType(35)` code set **or** that code set is **empty** — a dictionary declaring a message type while omitting it from tag 35's codeset would have that message type **rejected outright**, a self-inflicted total loss. *(**FIXT11 passes via the empty-set arm** — its `MsgType` declares **zero** `<value>` children. State that explicitly; do not special-case it. It is a **load-bearing dependency on FR-003**, not a coincidence.)* **(SC-011)** zero duplicate codes / zero `<value>` missing `enum` / zero missing `description` across the ten; the declared codes containing a **space** are **EXACTLY** `{FIX41:166:"ISO Country Code", FIX42:166:"ISO Country Code"}` — an **exact-set** predicate (`[[feedback_completeness_gate_exact_set_not_subset]]`), **GREEN today** (re-measured across all ten **including** Orchestra's 5708 `fixr:code` values — zero with a space); and **every enum-backed tag present in the store but absent from message expansion carries a non-multi-value declared type** (the T016 pin). **Any addition, removal or changed literal — or any store-only tag becoming multi-value — FAILS the build.** Assert against the **shipped XML**, never a hand-maintained expectation list.
- [ ] T022 [US1] **[SC-001/FR-006, AC US1 #1-2-3 + FR-015/SC-004]** New witnesses in `tests/wire/validator_enum_domain_test.cpp` **(new; bucket `wire_dict_tests`, `LABELS "075;wire"`)**: `54=Z` ⇒ reject/5/`RefTagID=54`; `54=1` ⇒ accept; **`ClOrdID(11)`** (zero codes) ⇒ accept regardless of content — the **absent-tag ⇒ accept floor, pinned DIRECTLY, not merely by a green suite** (SC-004); FIXT11's 8 message types all accept via the empty-set arm; and **(FR-015)** the **header** field `PossDupFlag(43)=X` ⇒ reject/5/`RefTagID=43` — a body-only implementation would **silently pass it** and break SC-009 parity. *(`43` is `BOOLEAN`, whose type arm imposes **no** constraint — `validator.hpp:419-425` — so **only the enum arm** can catch it.)* Mutations: revert `enum_valid` → `return true` (⇒ RED); make the absent-tag branch **reject** (⇒ the whole suite goes RED — that is the point); **body-only walk** (⇒ the FR-015 case goes RED).
- [ ] T022a [US1] **[FR-008 — DV-1 / DV-2 fixpp-side pin]** The **empty-value** witness in `tests/wire/validator_enum_domain_test.cpp` (bucket `wire_dict_tests`, `LABELS "075;wire"`), driven through **`validate()`**: **`Side(54)=`** (empty × **`Char`**) ⇒ **reject/5** — but via the **type** arm, not the enum arm (**DV-1**, parity with QuickFIX *by coincidence*); **`ExecInst(18)=`** (empty × **`String`**) ⇒ **ACCEPT** (**DV-2**, a genuine divergence — QuickFIX rejects/5). ⚠️ **`empty × String ⇒ accept` is the assertion that does the work, and NOTHING in the tree pins it today**: `tests/wire/validator_type_check_test.cpp:387-396` already pins empty × `Char` ⇒ reject (pre-existing, unrelated to 075), so a missing empty-value bypass would leave that green while silently regressing `ExecInst(18)=` — and T029's "full suite passes unchanged" backstop **cannot** catch it either. **Mutation: delete T017's empty-value bypass guard (branch 2) ⇒ `ExecInst(18)=` MUST flip to reject.** *(Golden corpus rows 8 and 9 are `asserted: false` — they characterize QuickFIX's side only and assert **nothing** about fixpp, so they do **not** discharge this.)*
- [ ] T023 [US1] **[SC-001/FR-006/FR-023, AC US1 #4]** The **DV-3 pin** in `tests/wire/validator_enum_domain_test.cpp` (bucket `wire_dict_tests`, `LABELS "075;wire"`): an out-of-domain enum on a **repeating-group member** *and, separately, on a **NESTED**-group member (depth ≥ 2)* ⇒ reject/5, `RefTagID` = the offending **member** tag. ⚠️ **The witness MUST drive the group path through `validate()` / the Step-1 walk — NOT through `validate_field()`**, which has no message context and is a **separate surface** (FR-020); a `validate_field()` unit test does **NOT** discharge this leg. **Mutation: restrict the Step-1 walk to top-level fields ⇒ MUST go RED.** *(This is a **declared divergence**: QuickFIX does **not** enum-check group members **at all** — closed call-site census: `isFieldValue` ← `checkValue` ← only `DataDictionary.cpp:172` inside `iterate`, which never descends into `m_groups`. fixpp is deliberately **stricter and more correct**; corpus rows 10-11 are `asserted: false`.)*
- [ ] T024 [US1] **[FR-020]** Flip **artifact #2** — `tests/wire/validator_type_check_test.cpp:257-275` (bucket `wire_pure_tests`, `LABELS "075;wire"`), which today asserts the **opposite** (*"Phase-1: enum_valid always true; validate_field must accept"*). Add `validate_field()` witnesses: in-domain accept / out-of-domain reject / multi-value / empty, **plus the FIX50SP2 store-only witness `validate_field(1128, "bogus")` ⇒ reject and `validate_field(1128, "9")` ⇒ accept** (`ApplVerID(1128)` codes are `0`–`10`; it is **declared in the dictionary but absent from message expansion**, and messages reference `RefApplVerID(1130)` instead). ⚠️ **Its second mutation is the one that matters**: *build the enum-domain table only from `message_fields()`* ⇒ store-only tags stay unconstrained ⇒ **MUST go RED**. That mutation — not the stub revert — is what discriminates the **store-driven** projection (T015), and this is the **only** witness in the bundle with power against it. *(FR-012: called out explicitly, never silently re-baselined.)*
- [ ] T025 [P] [US1] **[FR-021 artifacts #1, #3, #4]** Flip the three remaining code artifacts the real `add_enum` invalidates, each **explicitly called out** per FR-012: **#1** `tests/wire/validator_domain_test.cpp:559-592` (*"Phase-1: enum_valid always true; `Side=X` must be accepted (FR-005)"*) ⇒ **reject/5**; **#3** `tests/wire/validator_per_version_test.cpp:212-230` (`EncryptMethod(98)=9` must be **accepted**) ⇒ **reject/5** (`98`'s codes are `0`–`6`, so `9` is genuinely undeclared); **#4** `tests/dictionary/table_view_test.cpp:256-269` (*"`enum_valid` must always return true (Phase-1)"*) ⇒ **rewritten to assert the real domain check, including the absent-tag ⇒ accept floor**.
- [ ] T026 [US1] **[FR-021 artifact #5]** Update the conformance oracle row in `tests/wire/conformance/w014_validate.csv`. ⚠️ **This is DATA, not code — it will NOT fail to compile.** It fails at **runtime with an unhelpful diff**, so it must be updated **deliberately**, not discovered.

**Checkpoint** — US1 is independently testable and is the **MVP**. The dead SessionRejectReason-5 arm is live.

---

## Phase 4: US2 — Multi-value enum fields validate per token (P1, co-equal with US1)

**Goal**: `ExecInst(18)=1 G 6` (three space-separated, individually-declared codes — exactly what the FIX spec prescribes for a `MULTIPLEVALUESTRING`) is **accepted**; `18=1 ZZ 6` is rejected.

⚠️ **P1 and co-equal with US1 — NOT a refinement. US1 cannot ship without US2.** Getting this wrong does not merely under-validate: a naive whole-string domain check **false-rejects every conformant `ExecInst`, `QuoteCondition`, `TradeCondition`, `OrderRestrictions`, …** — 8 such tags in FIX44 alone. It **actively breaks working traffic**. This is ranked risk 1.

- [ ] T027 [US2] **[SC-003/FR-004/FR-014]** Multi-value witnesses in `tests/wire/validator_enum_multivalue_test.cpp` **(new; bucket `wire_dict_tests`, `LABELS "075;wire"`)**: `18=1 G 6` ⇒ **accept**; `18=1 ZZ 6` ⇒ reject/5/`RefTagID=18`; `18=1` (single token) ⇒ accept; **degenerate whitespace** `18=1  G` (double space) and `18=1 ` (trailing space) ⇒ **reject** (an empty token is never a declared code — FR-014); and a **single-value** field whose value contains a space is checked as **ONE** code with **no** tokenization (AC US2 #3 — tokenization applies **only** to fields the dictionary types as multi-value). **Table-driven over the FULL multi-value census** — FIX44's `18, 276, 277, 286, 291, 292, 529, 546`; FIX50/SP1 add `1031, 1035`; **FIX50SP2 has 9** (tag `1035` is `MULTIPLESTRINGVALUE` but declares **zero `<value>` children**, so it is not enum-backed). **Mutation: replace the tokenizer with a whole-string lookup ⇒ the ACCEPT case (`18=1 G 6`) MUST flip to reject.** *(That accept-side mutation is the discriminator — a reject-only witness set would pass under the broken tokenizer.)*

**Checkpoint** — conformant multi-token traffic is provably not broken.

---

## Phase 5: US3 — Legacy dictionaries gain enum data without changing anything else (P2)

**Goal**: all nine QuickFIX dictionaries carry their declared code sets, queryable through the same read-only accessor 074 introduced — with everything else byte-for-byte unchanged.

- [ ] T028 [US3] **[SC-002/FR-001]** Census + exposure witness in `tests/dictionary/xml_enum_codeset_test.cpp` **(new; bucket `dictionary_pure_tests`, `LABELS "075;dictionary"`)**: `enum_values(54)` on the FIX44 dictionary returns the declared `Side` codes **with their descriptions**; per-dictionary **enum-backed-field and total-code counts** match the Context census — FIX40 39/235, FIX41 53/342, FIX42 104/629, FIX43 159/1198, **FIX44 245/1708**, FIX50 290/2326, FIX50SP1 327/2640, FIX50SP2 668/5565, FIXT11 9/56. **Assert against the shipped XML, NOT a hand-maintained expectation list** — so a dictionary refresh cannot silently invalidate the gate. ⚠️ **The exact-count leg is scoped to the NINE XmlLoader dictionaries** (what **075** newly populates and what the census table covers); the **exposure** leg covers all ten. The **tenth** (Orchestra / FIX Latest) has **074's** code sets — populated by `OrchestraLoader`, untouched by 075, and pinned today only by `tests/dictionary/orchestra_loader_test.cpp`'s `OrchestraCodesets.PreservesValuesAndDescriptions`, an **`AdvSide(4)` spot-check**. **074 has NO exact-count census** — do not claim one; 075 adds no new exact-count gate over the tenth. *(Gate A round 2, O2-7; a carve-out must be **stated**, per `[[feedback_completeness_gate_exact_set_not_subset]]`.)*
- [ ] T029 [US3] **[FR-012/SC-004]** Run the **full** existing dictionary / validator / session / codegen suites and confirm every pre-existing test passes **unchanged**, except the **six** FR-021 artifacts (T024/T025/T026), each already explicitly called out. **Zero new rejections** on codeset-free fields and enum-data-free dictionaries. Any *other* test that changes ⇒ **STOP**: it is an unplanned behavior change, not a re-baseline.

**Checkpoint** — the substrate is proven; the nine legacy dictionaries carry their code sets with no collateral behavior change.

---

## Phase 6: Session-level & parity witnesses

- [ ] T030 [P] **[SC-008/FR-013]** Logon witness in `tests/session/test_enum_validation_logon.cpp` **(new; bucket `session_pure_tests`, `LABELS "075;session"`)**: an inbound **Logon** carrying an out-of-domain `EncryptMethod(98)=9` ⇒ reject/**5** **and the session does NOT establish**; conversely a Logon whose admin enums are all in-domain **establishes exactly as today** (no false-reject of the handshake). *(fixpp validates **before** `interpret_logon` on the `NotConnected` arm — `src/session/session.cpp:2060-2070` — which is **QuickFIX parity**: `Session::next` validates at `Session.cpp:1218-1229`, **before** `nextLogon` at `:1231`. Reject(3) and Logout(5) stay exempt via the pre-existing no-reject-loop guard, so enum checking cannot induce a reject loop.)* **Both halves matter** — a witness that only pins the reject would stay green under a reject-everything regression. **Mutation: revert `enum_valid` ⇒ both halves must not both stay green.**
- [ ] T031 **[SC-009/FR-018/FR-019]** The parity gate `tests/wire/enum_golden_parity_test.cpp` **(new; STANDALONE binary, `LABELS "075;golden;parity"`)** — standalone because it is a data-driven exact gate consuming the checked-in golden + manifest. fixpp's verdict matches the golden on **every `asserted: true` row**; `asserted: false` rows are **recorded, not asserted**. **On an `asserted: true` row, divergence from the reference engine is a DEFECT.** **Mutation: revert `enum_valid` → `return true` ⇒ EVERY `asserted: true` *reject* row MUST go RED.** Plus the standing per-row obligation from T001: **a `reject`-asserting row that BOTH engines ACCEPT is a defect, not a row** (it coincides with the stub and no mutation here can redden it) — while the **accept**-asserting rows 1 and 3 are the paired **positive controls against over-rejection** (row 3 reddens under the tokenizer mutation; row 1 under any absent-tag⇒reject regression) and are **not** condemned by it.
- [ ] T032 [P] **[SC-006]** Hot-path pins: **zero additional allocations per message** via the existing alloc-guard suite (**standalone**, `LABELS "alloc_guard;mallocnesia"` — global-alloc interception is isolation-sensitive per `[const §VII.8]`), plus the `bench/dictionary/` measurement against T011's baseline. **Mutation: remove the config-time table build and rebuild the enum table per message ⇒ the alloc guard MUST go RED.** *(An alloc-guard that cannot go RED under a per-message rebuild is not guarding anything — `[[feedback_tracking_pmr_resource_false_pass]]`.)*
- [ ] T033 [P] **[SC-005/SC-007/FR-011]** Confirm **zero C-ABI diff** and an unchanged exported symbol set via the existing ABI-golden gate (**standalone**, `LABELS "abi"`) — the C ABI is **GA-frozen at `1.5.0`** and this feature MUST NOT touch `include/fix/c_api*`. Confirm a session with inbound validation **disabled** is **byte-identical to `main`** (`session_pure_tests`).

---

## Phase 7: Documentation, constitution & catalogue (Article VI is BLOCKING)

⚠️ **`plan.md`'s Constitution Check marks Article VI §4 `CONDITIONAL — blocking until the coverage-index / catalogue edits land`. It is NOT a PASS until T035/T036 land.** Article VI §4 (`.specify/constitution.md:116`) requires every OFFICIAL row's coverage-index entry to land **before** the row does.

- [ ] T034 **[Article I §1 amendment, v0.6 → v0.7, MINOR]** Amend `.specify/constitution.md` Article I §1 to **narrow** the FIX-Latest post-1.0 carve-out to *typed codegen + ApplExtID(1156)=303 + session negotiation*, recording that **dictionary-driven wire validation ships generically for all supported versions** via this feature. Include a **Sync Impact Report**. **MINOR** is the right class under XX §4 (none of its enumerated triggers — banned-pattern additions, perf-budget tightening — apply). The amendment **rides this feature's branch** rather than a standalone `Constitution: amend …` PR, per the established Gate-A-fold deviation from Article XX §2's letter (**precedents: 035, 043, 068, 069**). *(Article XVIII §5 has **no residual conflict** once §1 is narrowed: §5 bars early-shipping deferred post-1.0 ***protocols***, and XVIII §2's roadmap enumerates protocols — SOFH, SBE, FIXP, FAST, JSON, GPB, MMT. **Dictionary-driven wire validation is not a protocol**, so §5 is not engaged on its own terms. Ordering matters: the **amendment** is what makes the scope cease to be deferred — not a waiver.)*
- [ ] T035 **[Article VI §4 — BLOCKING]** Land the four `spec/coverage-index.md` edits enumerated in `spec.md` § Normative References: **`:581`** strike **live-wire validation** from the post-1.0 backlog list and record it delivered generically for all ten dictionaries by 075; **`:189`** (W-014) add the 075 delivery note discharging the **`enum values`** clause + **L-041-1**; **`:68`** (§4.5.4) amend the 041 note — **L-041-1 RETIRED by 075**; reason 5's **enum arm** is now live; **`:704`** (Post-1.0 Gap Registry, D-011) strike **live-wire**. *(W-014 is an **existing OFFICIAL** row — the coverage index has listed `enum values` inside its scope **since 004-wire-codec**. 075 is the **delivery of that clause**, not a new row.)*
- [ ] T036 **[Article VI §4 — BLOCKING]** Land the two `spec/feature-catalogue.md` edits: **`:130`** (**D-011**) strike **live-wire** from *"Typed-codegen / live-wire / ApplExtID(1156)=303 / session-negotiation tiers remain post-1.0"* — **this rides the T034 amendment and MUST land in the same pass**; **`:111`** (W-014) add 075 as a delivering feature for the `enum values` clause.
- [ ] T037 **[FR-010/FR-013/FR-022/FR-023 — B-rows; L-075-1; L-041-1; L-069-1]** Update `spec/behaviors-and-limitations.md`. ⚠️ **The B-row IS the release-note artifact** — there is **no `CHANGELOG.md`** in this repo, so it MUST be written for an **OPERATOR** audience, not as an internal note, and **no second artifact is invented** (FR-010; this is root cause RC#4, which re-entered the bundle once already). Land: **(a)** the headline **B-row** — strict-validating sessions begin **rejecting out-of-domain enum values they accept today**; name the flag (`validate_inbound_messages`), the new reject reason (**5**), and the affected tags/dictionaries; **(b)** a B-row for **DV-3** (fixpp enum-checks repeating-group members at every depth; QuickFIX does not — deliberately stricter); **(c)** a B-row for **DV-4** naming **tag 166 on FIX 4.1 / 4.2 explicitly** (`166=US` now rejects — the `"ISO Country Code"` placebo codeset); **(d)** a B-row for **FR-013** (a peer sending an out-of-domain admin enum **cannot establish a session**); **(e)** **L-075-1**, which records **TWO** facts, not one — fixpp has **no reason-4 mapping** (`reject_reason_map.hpp` emits only 1/2/5/6/14) **AND** its empty-value disposition is **type-arm dependent** (`Char` ⇒ reject/5 via the *type* arm; `String` ⇒ **accept**); **(f)** **RETIRE L-041-1** (`:1478` — *"enum-value conformance is NOT validated"* — that is precisely what 075 delivers); **(g)** **RESTATE L-069-1 as STILL OPEN** — the generated `validate_<Msg>` functions for the 83 v44 typed builders ride a **different** path (typed codegen, not `table_view`) and 075 does **not** touch them. **L-069-1 is a Non-Goal and MUST NOT be silently absorbed.**
- [ ] T038 [P] **[DOC-CORRECTION]** Fix the comments 075 makes **false** — no code change in either file. `include/fixpp/wire/reject_reason_map.hpp`: **`:22-23`** *"type-arm only in Phase-1 since the enum arm is dead"*, **`:60-61`** *"Reason 5 … type arm; enum arm is dead Phase-1"*, and **`:1-19`** the same framing in the doc block. *(This file's dead-enum-arm comment is the bundle's own **headline** — leaving it stale would be the feature contradicting itself in source.)* `include/fixpp/dict/dictionary.hpp`: **`:66-67`** *"Populated only by `OrchestraLoader`"* ⇒ **false**; **`:184-189`** *"(all XmlLoader/QuickFIX dictionaries)"* carry an empty store ⇒ **false**, and *"orthogonal to `table_view::enum_valid` (unchanged Phase-1 stub)"* ⇒ **false** (`enum_valid` now **reads** it); **`:195-200`** *"all validator method calls O(1)"* ⇒ now **O(log C)**, and *"`enum_valid` is stubbed to true (FR-005)"* ⇒ **false**.
- [X] T039 [P] **[Gate A residual P3 #1 — DISCHARGED at checklist-audit 2026-07-14, CHK051 SPEC-FIXED]** The multi-value census **prose** omits FIX50SP2's count at **three** sites — `specs/075-live-wire-enum-validation/quickstart.md:35`, `spec.md:195`, and **SC-003** (`spec.md:260`) — each reading *"FIX50/SP1 add `1031, 1035`"* without stating FIX50SP2's. The **Context census table is CORRECT**: **FIX50SP2 has 9** enum-backed multi-value fields, because tag **`1035`** is `MULTIPLESTRINGVALUE` but declares **zero `<value>` children**, so it is not enum-backed. Correct all three to name FIX50SP2's **9** explicitly.
- [ ] T040 [P] **[Gate A residual P3 #2]** `specs/075-live-wire-enum-validation/plan.md:76` says spec.md has **"2 clarification sessions"**; Gate A round 4 added a **third** (`spec.md:411`). Correct to **3**.

---

## Phase 8: Polish & close-out (MANDATORY — hard `/gate-b` preconditions, Article XVII §8)

- [ ] T041 Run `/simplify` over the 075 diff, then **`/speckit-verify`** (produces `.specify/decisions/075-live-wire-enum-validation-verify.md` — **required paired evidence** for the `gate-a-done` label per `[const §XVII.8]`, alongside the existing Gate A record). ⚠️ Build **every** verify preset with **`--clean-first`**: this feature changes the **size** of `table_view`, and an incremental dyndep build yields stale-object ODR **FALSE-RED** sanitizer/test failures in unrelated code (`[[feedback_header_size_change_incremental_dyndep_stale_object_false_sanitizer]]`). *(`/speckit-verify` is **clang-only** — gcc-release and MSVC are **CI-only** jobs, `[[feedback_local_verify_clang_only_misses_gcc_release_ci_job]]`.)*
- [ ] T042 **[CLOSE-OUT 1/2 — catalogue]** **Confirm** T035's and T036's coverage-index / catalogue edits landed (do **not** re-edit them — T035/T036 own those six file:line edits; this task verifies, it does not duplicate), then **flip every 075-owned OFFICIAL `spec/feature-catalogue.md` row to `done`**: **W-014** (the `enum values` clause), the **§4.5.4 / S-007 / S-033 / S-034** reason-5 enum arm, and **D-011**'s narrowed carve-out. Confirm the **L-041-1 retirement** and the **L-069-1 still-open restatement** are both live in `spec/behaviors-and-limitations.md`.
- [ ] T043 **[CLOSE-OUT 2/2 — FEATURE-COMPLETENESS AUDIT — THIS IS THE FINAL TASK]** Audit that **tasks ↔ FR/SC ↔ catalogue** all map to a **landed test + implementation**: all **24 FRs**, all **11 SCs**, all **12** golden-corpus rows, all **4** divergence-register rows (DV-1..DV-4), and all **6** FR-021 artifacts. Record the verdict — **100%-or-waived** — in `.specify/decisions/075-live-wire-enum-validation-verify.md` under a `## Completeness` heading (or a sibling `-completeness.md`). ⚠️ **`/gate-b` pre-flight 4d HARD-BLOCKS without this record.** Explicitly confirm the **Non-Goals were not silently absorbed**: **L-069-1 remains OPEN** (codegen enum-domain validation — the 83 typed `validate_<Msg>` builders are untouched); no ApplExtID(1156)=303 / `version_registry` re-keying; no typed `fixpp::vlatest` codegen; **no outbound** enum gate; **zero** C-ABI or Python-binding surface change.

---

## Dependencies & execution order

```
Phase 0.5 (T001-T009)  ⚠️ GATE — the golden BLOCKS the parity leg. Never reorder after the FRs it validates.
        ↓  (T006 derives the divergence register from MEASURED output; T008 PROVES the manifest gate RED)
Phase 1 (T010-T011)     Setup — clean baseline, bench baseline
        ↓
Phase 2 (T012-T020)     Foundational — BLOCKS every user story
        │   T012 → T013 → T014   (all in xml_loader.cpp — sequential, same file)
        │   T015 → T016          (both in dictionary.cpp — sequential, same file)
        │   T017 → T018 → T019   (all in table_view.hpp — sequential, same file)
        │   T020                 (verification only; no code change)
        ↓
   ┌────┴──────────────┬────────────────────┐
Phase 3 (US1, P1) 🎯  Phase 4 (US2, P1)   Phase 5 (US3, P2)
T021-T026 (+T022a)    T027                T028-T029
   └────┬──────────────┴────────────────────┘
        ↓
Phase 6 (T030-T033)     Session + parity + hot-path + ABI
        ↓
Phase 7 (T034-T040)     ⚠️ Article VI is BLOCKING (T035/T036) — Constitution Check is CONDITIONAL until they land
        ↓
Phase 8 (T041-T043)     /simplify → /speckit-verify → catalogue → COMPLETENESS AUDIT (final)
```

**US1 and US2 are BOTH P1 and co-equal.** US2 is not a refinement of US1: without the tokenizer, US1's implementation false-rejects every conformant `ExecInst(18)=1 G 6`. **US1 cannot ship without US2.** US3 (P2) is the enabling substrate — without it, US1/US2 cover **FIX Latest only** and miss the real-world FIX 4.4 case entirely.

### Parallel opportunities

- **Phase 2 is three independent file-chains** — `xml_loader.cpp` (T012→T014), `dictionary.cpp` (T015→T016), `table_view.hpp` (T017→T019) — parallel **across** chains, sequential **within** each (same file).
- **Phases 3, 4, 5 run in parallel** once Phase 2 lands: US1, US2 and US3 touch disjoint test files.
- T021 [P], T025 [P] within US1; T030/T032/T033 [P] in Phase 6; T038/T039/T040 [P] in Phase 7.

### MVP scope

**US1 + US2** (they are co-equal P1 and cannot be separated) on top of Phase 0.5 + Phase 2. That delivers the headline: a strict-validating FIX 4.4 session rejects `Side(54)=Z` with reason 5 **without** false-rejecting `ExecInst(18)=1 G 6`.

---

## Traps this feature has already fallen into once — do not re-enter them

| Trap | Where it bit | The guard |
|---|---|---|
| **A witness that passes either way.** The FR-009 no-prefix-match witness was `277=A` "where only `AX` is declared" — but `TradeCondition(277)` **declares `A`**. Both engines accept ⇒ the row could never go red ⇒ FR-009 had **zero** witnesses. | Gate A round 2 (O2-1) | **T001** — re-measure **every** asserted literal against the shipped XML **before** writing the generator. A `reject`-asserting row that **both engines accept** is a **defect**. |
| **A reachability-built table missing a bypassing surface.** The projection walked `message_fields()`, so **35** enum-backed tags unreachable in FIX50/SP1/SP2 got **no domain** — and `validate_field()` has **no `field_valid_for` precheck**, so it **silently accepted** out-of-domain values through a frozen public API. Violates **FR-003**, a normative MUST. | Gate A round 3 — **this exhausted the first loop** | **T015** (store-driven projection) + **T016** (the `multi_value=false` pin) + **T024**'s `message_fields()`-only mutation, the **only** witness with power against it. |
| **A parity-corpus row for a surface the reference engine lacks.** The `validate_field(1128,…)` witness was double-booked as golden corpus row 12 (`asserted: true`) — but QuickFIX has no context-free `validate_field`, and the engines **check in opposite order**, so the row would have gone **spuriously RED** and blocked the feature. | Gate A round 4 (O4-1) — introduced **by the fix** for the previous trap | **T004** — 12 rows, **every one a message frame through `validate()`**; the witness lives in **T024**, not the corpus. |
| **A gate defined to fail.** SC-011 asserted *"no declared code contains a space"* while conceding it "fires today" on `SettlLocation(166)`. An implementer either ships a permanently-RED test that gets "fixed" under pressure, or downgrades it to a **log**. | Gate A round 2 (C2-1) | **T021** — an **exact-exception-set** predicate, **green today**, RED on any addition/removal/edit. |
| **An undischargeable artifact re-entering through prose.** FR-010 established there is **no `CHANGELOG.md`** and that **the B-row IS the release note** — and FR-022, **one FR later in the same pass**, re-invented the release note. | Gate A round 2 (C2-2 / RC#4) | **T037** — the B-row is written for an **operator** audience and **no second artifact is invented**. |
| **Two accept-floors collapsed into one.** FR-003's *empty **code set*** ⇒ accept and FR-008's *empty **field value*** ⇒ bypass-the-enum-check are **different rules**. Implementing only the first makes a byte-exact compare find `""` absent from every codeset and **reject via the enum arm**, flipping **empty × `String`** (`ExecInst(18)=`) from accept to reject — a conformant-traffic regression that empty × `Char`'s pre-existing pin leaves **green**, and that golden rows 8/9 (`asserted: false`) assert nothing about. | `/speckit-analyze` (C1), post-Gate-A | **T017** branch 2 (the explicit bypass) + **T022a** (the empty × `String` ⇒ **accept** pin, whose mutation is *delete the bypass guard*). |
| **A golden nothing can falsify.** `reference-engines/` lives **outside** the submodule and CI never runs the generator, so a checked-in golden can go stale, be hand-edited, or be built against the wrong dictionary with **nothing** catching it. | Ranked risk 7 | **T005** (`golden_output_hash` over the **verdicts** — the other five manifest fields all stay valid under a hand-edit) + **T007** (CI manifest gate) + **T008** (**prove it RED**) + **T009** (regen-and-diff with a **named owner and cadence**). |
