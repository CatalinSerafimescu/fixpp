# Process: Completeness & Traceability Requirements Quality Checklist: Wire Codec

**Purpose**: Formal release-gate validation that the completeness-gate, catalogue/coverage-index, traceability, and feature-done requirements are themselves well-specified, unambiguous, and measurable — the process surface that prevents the 001-class end-gap. Tests whether the *process requirements* are well-written, not whether the process ran.
**Created**: 2026-05-16
**Audited**: 2026-05-21 (pipeline step 9 — `/speckit-checklist-audit`, retroactive post-merge per POLICY OVERRIDE)
**Feature**: [spec.md](../spec.md) · **Plan**: [plan.md](../plan.md) · **Tasks**: [tasks.md](../tasks.md) (T057/T058)
**Audience**: Reviewer at Gate A/B

## ID Scheme & Bidirectional Traceability

- [x] CHK001 - Is a stable requirement ID scheme (FR-001..FR-018, SC-001..SC-008) established and used consistently across spec/plan/tasks? [Traceability, Spec Requirements] — **PASS**: spec.md numbers FR-001..FR-018 + SC-001..SC-008; plan.md "Phase-2 input checklist" line "Spec FR-001..018 + SC-001..008 ↔ `[2b §4]`/`[2b §6]`/`[2b §9]` mapped"; tasks.md cites the IDs throughout (T020 FR-013, T024 FR-006, T027 D-17/FR-006/SC-006(d), T046 FR-013/SC-005, T058 mechanically verifies FR-001..FR-018 + SC-001..SC-008 traceability). Stable ID scheme used consistently.
- [x] CHK002 - Is every FR- required to map to ≥1 task AND ≥1 SC-, with the bidirectional mapping an explicit obligation (not implied)? [Traceability, tasks.md T058] — **PASS**: tasks.md T058 binds "(b) every spec FR-001..FR-018 + SC-001..SC-008 maps to a landed test **and** implementation (the `/analyze` coverage table re-run against the merged tree, not the plan)". Explicit obligation; T058 is the mechanical enforcement step. T058 re-run 2026-05-17 confirms 100% bidirectional mapping on the merged tree.
- [x] CHK003 - Is every owned catalogue row (W-001..W-014, OSS-006/008) required to trace to an FR/SC and a coverage-index entry? [Traceability, Plan §VI.4 / tasks.md T057] — **PASS**: tasks.md T057 binds "Add/refresh the matching `spec/coverage-index.md` entries (bidirectional `[FIX50SP2 §3/§3.1/§3.2/§3.3]` ↔ row mapping) **before** the rows are declared done"; plan.md §VI.4 row "Owns W-001..W-014 + OSS-006/008/013 (inherited, no new catalogue row per `[2b §11]`)"; T058(c) mechanically verifies "every 004-owned catalogue row (W-001..W-014, OSS-006, OSS-008) is `done` in `spec/feature-catalogue.md` with a `coverage-index.md` entry (T057)". Conformance.md CHK023 traces every W-row to FR/SC. Required + mechanically enforced.
- [x] CHK004 - Is the FR-006 / FR-013 "covered-but-underspecified" status from `/analyze` reconciled or explicitly accepted, so traceability gaps are not silently inherited? [Consistency, /analyze report C1/U1] — **PASS**: tasks.md "Remediation decisions (post-`/analyze` 2026-05-16 — applied)" header explicitly resolves C1 (FR-013 throwing-trait trap → T020 noexcept_trap_test + trap_throw clauses in T024/T035/T046) and U1 (FR-006 general trait-decode boundary → clause added to T024 making the 2a/2c `field_traits` decode/encode boundary explicit for all W-009 types). Reconciled.

## Catalogue / Coverage-Index Update Requirements

- [x] CHK005 - Is the disposition of each owned catalogue row specified individually (which rows `backlog`/`in-progress`→`done`, which stay deferred), not as a blanket "flip all"? [Clarity, tasks.md T057] — **PASS**: tasks.md T057 enumerates per-row dispositions: "W-001..W-008, W-010..W-014 (`backlog`→`done`), W-009 (`in-progress`→`done`, 001 wire-FLOAT deferral now closed by T027), and the inherited OSS rows delivered here per `[2b §11]` — OSS-006 + OSS-008 (`backlog`→`done`). **OSS-013 stays `post-1.0 v1.2` (SBE) — NOT flipped**". Per-row specified.
- [x] CHK006 - Is OSS-013's non-flip (post-1.0 v1.2, pattern-only) stated explicitly so it is not wrongly closed by the audit? [Conflict, tasks.md T057 / `[const §XVIII.2]`] — **PASS**: tasks.md T057 explicit "**OSS-013 stays `post-1.0 v1.2` (SBE) — NOT flipped** (004 applies the flyweight *pattern* only; the SBE row is roadmap-deferred per `[const §XVIII.2]`)"; T057 closure note "OSS-013 left `post-1.0 v1.2`". Conformance.md CHK019 mirrors. Explicit.
- [x] CHK007 - Is the ordering requirement explicit — coverage-index entry exists *before* a row is declared done (`[const §VI.4]`) — rather than left to inference? [Clarity, Plan Constitution Check §VI.4] — **PASS**: tasks.md T057 "Add/refresh the matching `spec/coverage-index.md` entries ... **before** the rows are declared done. Per-row evidence (`/specify` artifact, verifying test, Gate A, Gate B) per `[const §VI.6]`". Explicit ordering with **before** in bold.
- [x] CHK008 - Is the per-row evidence set required by `[const §VI.6]` (matching `/specify` artifact, verifying tests, Gate A, Gate B) stated as a precondition to declaring a row done? [Completeness, `[const §VI.6]`] — **PASS**: tasks.md T057 "Per-row evidence (`/specify` artifact, verifying test, Gate A, Gate B) per `[const §VI.6]`"; T057 closure note "the rows are now genuinely Gate-A-converged + verifying-test-landed + verify-GREEN, Gate B converging via the active loop). Flipped in `spec/feature-catalogue.md`: **W-001..W-014** + **OSS-006/OSS-008** → `done` (004 ref + PR #68 + verifying test + Gate-A/Gate-B evidence cell)". Precondition explicit.

## Completeness Audit Definition (T058)

- [x] CHK009 - Are the three audit assertion sets (tasks `[X]`-or-waived; FR/SC ↔ landed test AND impl; catalogue ↔ coverage-index) each defined precisely enough to be objectively evaluated? [Measurability, tasks.md T058] — **PASS**: tasks.md T058 binds three precisely-defined sets: "(a) every `tasks.md` row is `[X]` (or waived with rationale); (b) every spec FR-001..FR-018 + SC-001..SC-008 maps to a landed test **and** implementation; (c) every 004-owned catalogue row (W-001..W-014, OSS-006, OSS-008) is `done` in `spec/feature-catalogue.md` with a `coverage-index.md` entry (T057)". Objectively evaluable; T058 re-run 2026-05-17 closed each set against HEAD `ce1d4d2`.
- [x] CHK010 - Is "re-checked against the merged tree, not the plan" stated explicitly, so the audit cannot pass on planned-but-absent coverage? [Clarity, tasks.md T058] — **PASS**: tasks.md T058 "(b) every spec FR-001..FR-018 + SC-001..SC-008 maps to a landed test **and** implementation (the `/analyze` coverage table re-run against the merged tree, not the plan)"; T058 re-run record "Re-run mechanically against the **merged tree @ HEAD `ce1d4d2`**". Explicit.
- [x] CHK011 - Is "100% or recorded waiver" defined — what constitutes a valid waiver (rationale format, approver), so a waiver is not an unbounded escape hatch? [Ambiguity, tasks.md T058 / `[const §XVII.2]`] — **DD-DECIDED `[const §XVII]` + tasks.md T058 + project memory `feedback_retro_pr_gate_a_waiver` + `feedback_codecov_patch_vs_lcov_da_brda_gate`**: tasks.md T058 binds "(or waived with rationale)" + the closure record names every waiver explicitly with anchor + scope + remediation note (e.g., "T055 `[~]`→`[X]` coverage discharged"; "2c/002 real-dict enum data-path dependency"; "GCC-release infra block"). Waiver format is anchored at the constitution level (`[const §IX.1]` written-justifications in verify.md §T059 precedent) and the established project pattern (gh api REST `gate-{a,b}-waived` label + verify.md body rationale; precedent PR #73 codecov/patch waiver, PR #74 row-flip waiver). The audit is not silent: every waived item is dispositioned with its rationale in `004-wire-codec-completeness.md`. Constitution + project precedent + task explicit text together bound the escape hatch.
- [x] CHK012 - Is the audit's load-bearing relationship to `/gate-b` stated (a non-100%/non-waived result blocks `/gate-b` exactly like a RED verify verdict)? [Consistency, tasks.md T058 / `[const §XVII.8]`] — **PASS**: tasks.md T058 explicit "a non-100%, non-waived result **blocks `/gate-b`** exactly like a `RED` verify verdict (`[const §XVII.8]` `/gate-b`-precondition shape)"; T058 closure "T058 `/gate-b` precondition SATISFIED (honest)". Load-bearing relationship stated.
- [x] CHK013 - Is the audit's output location specified unambiguously (a `## Completeness` section in `<feature>-verify.md` OR a sibling `<feature>-completeness.md`) so the `/gate-b` pre-flight can deterministically find it? [Clarity, tasks.md T058 / gate-b pre-flight 4d] — **PASS**: tasks.md T058 "Write the result into `.specify/decisions/004-wire-codec-verify.md` (or a sibling `-completeness.md`)"; T058 closure record "overwrites `.specify/decisions/004-wire-codec-completeness.md`". Deterministic location (one-of). The sibling-file resolution matches project memory `project_gate_records_layout` (decisions/ is gitignored local-only).

## "Feature Complete" as a Measurable State

- [x] CHK014 - Is "feature complete" defined as a measurable end-state (audit 100%/waived + verify GREEN/YELLOW + both gate records) rather than an informal judgement? [Measurability, tasks.md Notes / `[const §XVII.8]`] — **PASS**: tasks.md Notes line 269 "`/speckit-verify` GREEN (T056) **and** the T058 feature-completeness audit at 100% (or recorded waivers) are both mandatory `/gate-b` preconditions (`[const §XVII.8]`); T057 catalogue/coverage-index update is required by `[const §VI.4]`/`[const §VI.6]` before any owned row is declared `done`. T058 exists to mechanically prevent the 001-class end-gap". Three-predicate measurable end-state.
- [x] CHK015 - Is the cutover unblock claim (001 FLOAT + 003 reify) tied to a measurable success criterion (previously-2b-gated tests GREEN, zero frozen-stub-surface refs) rather than a narrative assertion? [Measurability, Spec SC-006] — **PASS**: spec SC-006 binds 4 measurable sub-clauses (a frozen-stub surface gone with grep evidence per quickstart §9; b drift guard reconciled and passes; c 003 reify round-trip executes end-to-end; d 001 FLOAT accessor executes end-to-end). Cutover.md CHK014/CHK015 mirror. Measurable.
- [x] CHK016 - Are cross-doc items D-9..D-12 explicitly excluded from 004's completeness scope (confirmations owned by 2c/2d/2e, not 004 blockers), so the audit boundary is bounded? [Clarity, research / Plan Gate A] — **PASS**: research D-9/D-10/D-11/D-12 each binds the disposition "not a 004 blocker"; plan.md Gate A row + research closing paragraph confirm. Cutover.md CHK018 mirrors. Audit boundary bounded.

## Gate Records & Evidence Pairing

- [x] CHK017 - Is the gate-label evidence-pairing rule referenced (gate-{a,b}-done requires verify GREEN + Codex convergence record) so labels are evidence, not decoration? [Traceability, `[const §XVII.8]` / quickstart §8] — **PASS**: plan.md §XVII.8 row "`/speckit-verify 004-wire-codec` → `.specify/decisions/004-wire-codec-verify.md`; `GREEN` required for `gate-b-done`"; tasks.md T056 reaffirms; project memory `feedback_codecov_patch_vs_lcov_da_brda_gate` + `project_gate_label_application` carry the established precedent. The evidence-pairing rule is anchored at the constitution level.
- [x] CHK018 - Is the tracked vs local-only record split specified (tracked = phase-4 doc + `research/reviews/`; `.specify/decisions/` gitignored), so the completeness/verify records are filed in the right place? [Clarity, Plan / project record-layout] — **DD-DECIDED project memory `project_gate_records_layout` + constitution + `.gitignore`**: project memory binds "Gate records layout — `.specify/decisions/` is gitignored (local-only); tracked record = phase-4 doc + `research/reviews/`". The `.specify/decisions/` directory carries a `.gitignore`. The split lives at the project / repo-config level (constitution + ignore rules + project memory), not 004's spec — 004 inherits the convention. The verify + completeness records correctly land in `.specify/decisions/` (local-only working memory) while gate review records (Codex + Opus) land in `research/reviews/` (tracked).
- [x] CHK019 - Is the distinction between the pre-`/tasks` Gate A *review record* and the PR-scoped gate *label* stated, so "Gate A done" is unambiguous about which event it refers to? [Ambiguity, quickstart §8 / Plan Gate A] — **PASS**: plan.md Gate A "Round 1 — applied (pre-`/tasks`)" subsection vs Gate B "Round 1 — TBD (post-`/implement`, pre-merge)" subsection make the temporal split explicit. Plan §XVII.1 row "Codex Gate A before `/tasks`" + plan §XVII.8 "`/speckit-verify` mandatory post-`/implement`" + plan Round 1 disagreement note "DISAGREE per Opus adversarial: gate-label evidence rule is PR-scoped at merge (`[const §XVII.6]`/§XVII.8 `/gate-b` precondition), not a pre-`/tasks` Gate A blocker; Gate A review record alone unblocks `/tasks`" disambiguate review-record vs PR-label semantics. Project memory `feedback_retro_pr_gate_a_waiver` carries the precedent.
- [x] CHK020 - Is the local-build approval obligation (`[const §XVII.7]` AskUserQuestion before Conan/CMake) recorded against the GREEN/Tier-1 tasks so it is not a silent assumption? [Assumption, tasks.md Notes G1] — **PASS**: tasks.md Remediation decisions G1 "GREEN/Tier-1 tasks (T031/T036/T041/T047/T055) imply local Conan/CMake builds; recorded in Notes that the `/implement` executor MUST surface the `[const §XVII.7]` `AskUserQuestion` build-approval prompt before running them"; tasks.md Notes "**(G1)** the GREEN/Tier-1 tasks (T031/T036/T041/T047/T055) and any task implying a local Conan/CMake build MUST be preceded by the `[const §XVII.7]` `AskUserQuestion` build-approval prompt at `/implement` time (the agent never auto-runs `conan install`/`cmake --build`); consistent with project memory `feedback_self_run_build_gate`". Explicit, recorded against the named tasks.

## Process Traceability

- [x] CHK021 - Does every process obligation above trace to a constitution article, a tasks.md task, or a gate-b pre-flight step, with no completeness/traceability requirement lacking an enforcement anchor? [Traceability, `[const §VI]`/`[const §XVII.8]` / tasks.md T057/T058] — **PASS**: process-obligation → enforcement-anchor matrix: ID scheme/bidirectional traceability → T058; catalogue/coverage-index ordering → T057 + `[const §VI.4]`/`[const §VI.6]`; audit definition → T058 + `[const §XVII.8]`; feature-complete state → tasks.md Notes + `[const §XVII.8]`; cutover unblock → SC-006; gate records → `[const §XVII.6]`/§XVII.8 + plan Gate A/B subsections + project memory `project_gate_records_layout`; local-build approval → tasks.md Notes G1 + `[const §XVII.7]` + project memory `feedback_self_run_build_gate`. Every obligation has an enforcement anchor.

---

## Audit Result

**Feature**: 004-wire-codec
**Checklist**: `process.md` (21 items, CHK001–CHK021)
**Design-doc anchor verified**: `.specify/2b-wire.md` Draft v0.2 (Gate A round 1 converged)
**Audit mode**: retroactive post-merge per POLICY OVERRIDE

### Per-domain tally

| Domain | PASS | SPEC-FIX-CANDIDATE | DD-DECIDED | WAIVED | Total |
|--------|------|--------------------|------------|--------|-------|
| ID Scheme & Bidirectional Traceability (CHK001–004) | 4 | 0 | 0 | 0 | 4 |
| Catalogue / Coverage-Index Update (CHK005–008) | 4 | 0 | 0 | 0 | 4 |
| Completeness Audit Definition (CHK009–013) | 4 | 0 | 1 | 0 | 5 |
| "Feature Complete" as a Measurable State (CHK014–016) | 3 | 0 | 0 | 0 | 3 |
| Gate Records & Evidence Pairing (CHK017–020) | 3 | 0 | 1 | 0 | 4 |
| Process Traceability (CHK021) | 1 | 0 | 0 | 0 | 1 |
| **Total** | **19** | **0** | **2** | **0** | **21** |

### Findings + resolutions

All 21 items dispositioned. Zero SPEC-FIX-CANDIDATEs. Two DD-DECIDED items — both anchored at the project / constitution level rather than 004's spec (CHK011 waiver-rationale format anchored in constitution `[const §XVII]` + project precedent / memory; CHK018 tracked-vs-local record split anchored in project memory `project_gate_records_layout` + repo `.gitignore`). 004 inherits these conventions consistently.

The process surface (T057 catalogue/coverage-index update + T058 mechanical completeness audit + tasks.md Notes G1 build-approval gate) is the explicit defense against the 001-class end-gap (`/speckit-taskstoissues` silently dropped rows). T058 re-run 2026-05-17 against HEAD `ce1d4d2` confirms 100% completeness with explicit recorded waivers, and `/gate-b` precondition is satisfied — this is the canonical "post-merge tasks/T057/T058 closed" state.

### Pipeline disposition

- **`/speckit-analyze` re-run required?** NO — zero SPEC-FIX-CANDIDATEs.
- **Verdict**: GREEN.
