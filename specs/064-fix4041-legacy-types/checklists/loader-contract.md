# Loader Contract-Relaxation Checklist: FIX 4.0/4.1 dictionary loader legacy-type support

**Purpose**: Unit-tests-for-requirements on the AC-L8 vocabulary relaxation — validate that the requirements around the two legacy-type aliases, the global scope, the QuickFIX-divergence recording, and the preserved fail-closed disposition are complete, clear, consistent, measurable, and traceable **before** implementation. (Requirements-quality gate; NOT an implementation test.)
**Created**: 2026-07-06
**Feature**: [spec.md](../spec.md) · [contracts/loader-vocabulary-contract.md](../contracts/loader-vocabulary-contract.md)
**Audience/Timing**: Gate-A / Gate-B reviewer, pre-`/speckit-implement`

## Requirement Completeness

- [x] CHK001 - Are the requirements for **both** legacy aliases (`TIME → UtcTimestamp` AND `DATE → LocalMktDate`) explicitly and separately stated, rather than one being implied by the other? [Completeness, Spec §FR-001/FR-002] — PASS: FR-001 and FR-002 are two independent MUST clauses, each naming its own xml type and target enum value; neither cross-references the other.
- [x] CHK002 - Is the requirement that the accepted-type set grows by **exactly two** named aliases (not a broadened/permissive rule) documented? [Completeness, Spec §SC-003, Contract §"exactly two names"] — PASS: SC-003 states "exactly two named aliases, not a hole" with a mutation intuition; contract's acceptance table states "exactly two names moving from reject → accept. No other cell changes."
- [x] CHK003 - Are the recording obligations for the relaxation enumerated as concrete artifacts (research, data-model, behaviors-and-limitations, README, catalogue) rather than a general "document it"? [Completeness, Spec §FR-006/FR-009, Contract §amendment-checklist] — PASS: FR-006 names research.md + data-model.md; FR-009 names spec/behaviors-and-limitations.md; FR-004 names dictionaries/README.md; FR-010 names spec/feature-catalogue.md; contract's 6-row amendment checklist restates all five artifacts with exact site references.
- [x] CHK004 - Is the requirement that the `field_data_type` enum stays frozen (no variant added) stated as a hard constraint, not an assumption? [Completeness, Spec §FR-003, Key Entities] — PASS: FR-003 uses hard MUST/MUST NOT language ("MUST NOT gain a variant and MUST NOT be otherwise modified"), restated in Key Entities and plan's AC-L8 Constitution-Check row.
- [x] CHK005 - Are the vendoring requirements (verbatim, pinned SHA, README listing) specified for both `FIX40.xml` and `FIX41.xml`? [Completeness, Spec §FR-004] — PASS: FR-004 names both files explicitly, the exact pinned SHA source (`dictionaries/UPSTREAM.txt`), and the README update obligation, applying identically to both.
- [x] CHK006 - Is the zero-surface-change boundary (no public header / C-ABI / wire / `error` variant) stated as an explicit requirement with an enumerated allowed-path set? [Completeness, Spec §FR-008/SC-004] — PASS: FR-008 enumerates the allowed scope (xml_loader.cpp, dictionaries/, dictionary tests); SC-004 restates it as a verifiable diff-path assertion (tightened further under CHK016 — see SPEC-FIXED note there).

## Requirement Clarity & Measurability

- [x] CHK007 - Is each mapping target named with a concrete `field_data_type` enum value (`UtcTimestamp`, `LocalMktDate`) rather than a prose category? [Clarity, Spec §FR-001/FR-002] — PASS: both FR-001/FR-002 name the exact enum value; cross-verified the values exist at `include/fixpp/dict/field_ref.hpp:50` (`UtcTimestamp`) and `:53` (`LocalMktDate`).
- [x] CHK008 - Is the term **"metadata-only"** defined with a specific, checkable mechanism (both targets collapse to `field_type::String` at a cited header location), not left as a reassuring adjective? [Clarity/Measurability, Spec §FR-009, Research R4] — PASS: FR-009 and the Clarifications session cite `include/fixpp/dict/field_type.hpp:98-114`; spot-verified by reading the file — lines 98-114 are exactly the switch-case block collapsing `UtcTimestamp` (line 105), `LocalMktDate` (line 108), `UtcDateOnly`, `DialectExtension`, and the `default` to `field_type::String`. Anchor resolves exactly as cited.
- [x] CHK009 - Is "the relaxation is **global (not version-scoped)**" defined precisely enough to be unambiguous about what a newer dictionary using `DATE`/`TIME` would do? [Clarity, Spec §FR-007, Research R5] — PASS: the Clarifications session and Edge Cases spell out the concrete consequence ("a `FIX44.xml` that used `type="DATE"` would also be accepted rather than fail-closing"), matching the existing global `TAGNUM`/`LOCALMKTTIME`/`XID`/`XIDREF` precedent cited by line number.
- [x] CHK010 - Can SC-001 (every field resolves; zero `xml_parse_error`) be objectively measured against the vendored files? [Measurability, Spec §SC-001] — PASS: objectively measurable (load succeeds / throws is a binary, testable outcome), and is task-backed by T006 (RED-before/GREEN-after).
- [x] CHK011 - Is SC-003's "exactly two aliases, not a hole" expressed as a measurable/mutation-testable criterion (delete a row → the corresponding dict re-fails)? [Measurability, Spec §SC-003, Quickstart §Mutation intuition] — PASS: SC-003's parenthetical states the mutation intuition directly; quickstart.md §"Mutation intuition (SC-003)" restates the same criterion per-row (delete DATE row / delete TIME row / delete neither), and T009 is the task that encodes it.
- [x] CHK012 - Is SC-006 stated with a defined verification **method** (static enumeration vs runtime before/after diff) that matches what the tasks actually do? [Measurability/Clarity, Spec §SC-006, Tasks T012] — **SPEC-FIXED**: found a genuine mismatch. SC-006 (as originally worded) stated the verification method as "loading them before/after and diffing resolved types" (a runtime diff), while T012 discharged it by pointing at the `AllRuntimeVersions` `VersionParam` rows staying green — but that suite (`tests/dictionary/lookup_test.cpp`, `VersionParam` struct) asserts message/group/component **structure** (msgtypes, group tags, `has_clordid`, `parties_expected`, `has_instrument`), never a per-field `field_data_type` value, so it cannot actually perform the stated "diff resolved types" check. Empirically confirmed (grep) that none of the seven already-vendored FIX 4.2+ dictionaries (`FIX42/43/44/50/50SP1/50SP2/FIXT11`) contain `type='DATE'` or `type='TIME'` — the correct, actually-checkable mechanism is a **static enumeration**, not a runtime diff. Edited `spec.md` SC-006 to state the static-enumeration method and explicitly note the `VersionParam` rows are not the discharging mechanism; edited `tasks.md` T012 to match (replaced the "byte-identical resolve" / "AllRuntimeVersions satisfies it" language with the static grep-enumeration check across the seven named files).
## Requirement Consistency

- [x] CHK013 - Do spec.md, plan.md, data-model.md (E-1), and the contract all state the **same** two mapping targets (no residual `DATE → UtcDateOnly` from the retired hypothesis anywhere)? [Consistency, Spec §SC-005, Research R3] — PASS (scoped to the four named artifacts): grep-verified `spec.md`, `plan.md`, `data-model.md`, `research.md`, and `contracts/loader-vocabulary-contract.md` all state `TIME → UtcTimestamp` / `DATE → LocalMktDate` consistently; every occurrence of the string `UtcDateOnly` in those five files is inside the explicit "retired hypothesis, NOT adopted" disclaimer (spec.md:64,177,257; research.md:61,81), never asserted as the mapping. **Note (not a CHK013 failure, flagged for visibility)**: `spec/feature-catalogue.md:120` — a pre-existing tracker row outside this feature's authored artifact set — still reads `status: blocked` and still states the retired `DATE→UtcDateOnly` hypothesis in its rationale text. This is explicitly T013's job at implement time ("drop the retired `DATE → UtcDateOnly` hypothesis currently in the row"); fixing it now would be premature (the feature hasn't shipped) and is outside CHK013's cited artifact list. SC-005's own grep-verifiable claim ("no project document claims `DATE → UtcDateOnly`") will only be fully true project-wide after T013 lands — tracked, not a gate blocker for this audit.
- [x] CHK014 - Is the AC-L8 relaxation described consistently as a *loader-acceptance* rule (not a constitution article) across spec Normative References, plan Constitution Check, and the contract? [Consistency, Plan §Constitution-Check AC-L8 row] — PASS: spec.md Normative References calls it "the recorded acceptance criterion AC-L8"; plan.md's Constitution-Check AC-L8 row explicitly states "The relaxed contract is AC-L8 (a loader acceptance rule, not a constitution article — there is no field-type-freeze article)"; the contract document opens by calling it "the loader's field-type acceptance set." Consistent across all three.
- [x] CHK015 - Are the FR-set and SC-set mutually consistent (every recording FR has a matching SC and vice versa, e.g. FR-009↔SC-005, FR-007↔SC-006, FR-010↔SC-007)? [Consistency, Spec §Requirements/Success-Criteria] — PASS: verified all pairs — FR-001/002↔SC-001, FR-005↔SC-002, FR-006↔SC-003, FR-008↔SC-004, FR-009↔SC-005, FR-007↔SC-006, FR-010↔SC-007; FR-003/FR-004 are structural constraints folded into SC-001/SC-004's verifiable diff-scope rather than a dedicated SC row, which is proportionate (they are sub-conditions of loadability/no-surface-change, not independent outcomes).
- [x] CHK016 - Is the allowed-touched-path set identical between SC-004, the quickstart scope-guard grep, and the tasks that edit `spec/` files (no path that a close-out task writes is excluded from the allowlist)? [Consistency, Spec §SC-004, Quickstart §Verify-no-surface-change, Tasks T012/T013] — **SPEC-FIXED**: found a genuine mismatch. SC-004 (as originally worded) authorized touching any file under the bare directory `spec/`, while the quickstart.md scope-guard grep and T012's enforcement only allow `spec/behaviors-and-limitations.md` and `spec/feature-catalogue.md`, and T013 explicitly states `spec/coverage-index.md` must NOT be touched ("keeps the T012 scope-guard allowlist consistent"). As originally worded, SC-004 would have authorized a diff T013 + the scope-guard reject — a real Consistency contradiction, not merely a coarser summary. Edited `spec.md` SC-004 to name the two specific `spec/` files and explicitly state `spec/coverage-index.md` is excluded, making the three allowlists (SC-004, quickstart grep, T012/T013) identical.

## Scenario & Edge Case Coverage

- [x] CHK017 - Are requirements defined for the **fail-closed-preserved** scenario (a name outside both the vocabulary and the full collapse set still throws `dict_xml_parse_failed`)? [Coverage/Exception Flow, Spec §US3/SC-003, Contract §BG-2] — PASS: US3 (P2) + SC-003 + contract BG-2 define the scenario; task-backed by T009, which keeps the existing AC-L8 witness (`negative_paths_test.cpp:230`, spot-verified to exist at that exact line) unmodified and adds the DATE/TIME-now-accepted companion.
- [x] CHK018 - Are requirements defined for the **pre-FIXT session-bearing** shape of FIX 4.0/4.1 (session msgtypes REQUIRED present in-dict, the inverse of the D-006 app-only forbidden shape)? [Coverage, Spec §US2/FR-005, Data-model §E-4] — PASS: US2 (P1) + FR-005 + data-model E-4 explicitly contrast with D-006's app-only shape; task-backed by T008.
- [x] CHK019 - Is the edge case of `DATE`/`TIME` appearing in a **newer** (FIX 4.2+) dictionary addressed, and the no-regression claim (no vendored 4.2+ file uses these names) stated? [Edge Case/Coverage, Spec §Edge-Cases, SC-006, Contract §BG-5] — PASS: Edge Cases + SC-006 (now SPEC-FIXED per CHK012) + contract BG-5 state the claim; independently re-verified empirically (grep across `dictionaries/FIX42.xml,FIX43.xml,FIX44.xml,FIX50.xml,FIX50SP1.xml,FIX50SP2.xml,FIXT11.xml`) — zero occurrences of `type='DATE'` or `type='TIME'` in any of the seven, confirming the claim is factually true, not merely asserted.
- [x] CHK020 - Is the QuickFIX-divergence scenario for `DATE` (they resolve `TYPE::Unknown`, we resolve `LocalMktDate`) addressed as a deliberate, recorded choice rather than an undocumented difference? [Coverage, Spec §FR-009, Research R3, Edge-Cases] — PASS: FR-009, Research R3 (with the QuickFIX anchor `DataDictionary.cpp:675-677/678`), and Edge Cases all record it as a deliberate user decision, task-backed by T010 (behaviors-and-limitations row).
- [x] CHK021 - Is case-sensitivity of the two new type-name matches addressed (they follow the existing table's convention; no new case-handling path)? [Edge Case, Spec §Edge-Cases] — PASS: Edge Cases states this explicitly ("the two new rows follow that exact convention (no new case-handling path)"), consistent with the actual `resolve_field_type` implementation (`std::string_view` exact-match linear scan, no case-folding, verified by reading `src/dictionary/xml_loader.cpp:107-116`).

## Dependencies & Assumptions

- [x] CHK022 - Is the assumption that `DATE`/`TIME` are the **only** out-of-vocabulary type names in FIX 4.0/4.1 stated AND given a confirm-at-implement re-enumeration obligation (guarding against fetch drift)? [Assumption/Traceability, Spec §Assumptions, Research §Confirm-at-implement #1] — PASS: spec.md Assumptions states the claim with the enumerated type lists per version; research.md "Confirm-at-implement obligations #1" restates it as a guard against fetch mismatch; task-backed exactly by T005 ("guards research R1 against a fetch mismatch").
- [x] CHK023 - Is the dependency on the pinned upstream SHA (`19ef6a4c`, `dictionaries/UPSTREAM.txt`) documented with a no-pin-drift requirement? [Dependency, Spec §Dependencies, Contract §checklist-5] — PASS: spec.md Dependencies names the SHA and source file; contract amendment-checklist row 5 requires "unchanged SHA... assert no pin drift"; task-backed by T004.
- [x] CHK024 - Is the assumption that `field_data_type::UtcTimestamp` and `::LocalMktDate` already exist in the frozen enum validated (with a source anchor), so "no enum change" is grounded? [Assumption, Spec §Assumptions, Data-model §E-2] — PASS: data-model.md E-2 cites `include/fixpp/dict/field_ref.hpp:50,53`; spot-verified by direct read — both enum values exist exactly at those lines.
- [x] CHK025 - Is the QuickFIX `DataDictionary::XMLTypeToType` reference (SHA-pinned) documented as the authoritative source for the `TIME` mapping and the `DATE` divergence? [Dependency/Traceability, Spec §Dependencies, Research R2/R3] — PASS: spec.md Dependencies and research.md R2/R3 both cite the pinned SHA and exact line numbers (`DataDictionary.cpp:675-677` for `TIME`, `:678` for the `DATE` fallthrough to `Unknown`).

## Traceability & Ambiguities

- [x] CHK026 - Does every functional requirement (FR-001..FR-010) trace to at least one user story, task, and — where it records a decision — a research/contract anchor? [Traceability, Spec §Requirements] — PASS: traced each — FR-001/002→US1/T007; FR-003→T007; FR-004→US-Foundational/T002-T004; FR-005→US2/T006,T008; FR-006→US3/T009 + research.md/data-model.md (already landed Phase-1 docs); FR-007→Research R5 (documentation-only obligation, already fulfilled); FR-008→T012 scope-guard; FR-009→T010; FR-010→T013. No orphaned FR.
- [x] CHK027 - Is the D-004 catalogue close-out requirement (flip to `done` AND correct the stale rationale text) traceable to a concrete task and evidence ref? [Traceability, Spec §FR-010/SC-007, Tasks T013] — PASS: T013 explicitly states both the status flip and the rationale-text rewrite ("Also rewrite each D-004 row's rationale/evidence text, not just the status field"), with evidence = "this PR."
- [x] CHK028 - Are there any remaining ambiguous or unquantified terms in the relaxation requirements (e.g., "narrow", "behaviorally free") that lack a cited, checkable basis? [Ambiguity, Spec §Overview/Clarifications] — PASS: grep-checked both terms — "narrow" (spec.md US3 "Why this priority") is immediately glossed as "two named additions, not a hole"; "behaviorally free" (research.md R3) is immediately followed by "See R4 for why," and R4 supplies the cited, checkable mechanism (both enum values collapse to `field_type::String` at `field_type.hpp:98-114`, spot-verified). "Unambiguous" (spec.md Clarifications, TIME mapping) is likewise backed by the two evidence axes listed in the same answer. No floating unquantified term found.

## Notes

- This checklist tests the **requirements about the relaxation**, not the loader behavior — Gate B verifies the behavior via the contract's BG-1..5 and the amendment checklist.
- Companion generic spec-quality gate: [requirements.md](./requirements.md) (from `/specify`, already ✅).
- Any item that fails here should be resolved by editing spec/plan/data-model/contract (SPEC-FIXED) or dispositioned in the step-9 checklist audit — not by relaxing the requirement.

## Audit Result

**Audited**: 2026-07-06 · pipeline.md step 9 (checklist audit gate, pre-`/speckit-implement`).

| Disposition | Count |
|---|---|
| PASS | 26 |
| SPEC-FIXED | 2 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 28 |

### SPEC-FIXED items

- **CHK012** — `spec.md` SC-006 stated its verification method as a runtime "before/after diff of resolved types," but no task performs that; the `AllRuntimeVersions` `VersionParam` rows (`tests/dictionary/lookup_test.cpp`) assert message/group/component structure only, never per-field `field_data_type`. Rewrote SC-006 to state the actual, stronger, static-enumeration method (grep the seven already-vendored FIX 4.2+ dictionaries for `type='DATE'`/`type='TIME'`; zero occurrences ⇒ resolved typing cannot change) and to explicitly disclaim the `VersionParam` rows as the discharging mechanism. Affected: `spec.md` §Success Criteria (SC-006). Also edited `tasks.md` T012 to match (replaced the "AllRuntimeVersions satisfies it" / "byte-identical resolve" language with the static grep-enumeration check naming the seven files). Empirically re-verified the underlying factual claim is true: `grep -oiE "type='DATE'|type='TIME'"` across `FIX42/43/44/50/50SP1/50SP2/FIXT11.xml` returns zero matches.
- **CHK016** — `spec.md` SC-004 authorized touching any file under the bare `spec/` directory, but the quickstart.md scope-guard grep and `tasks.md` T012/T013 only allow `spec/behaviors-and-limitations.md` and `spec/feature-catalogue.md`, with T013 explicitly forbidding `spec/coverage-index.md`. As originally worded, SC-004 would have authorized a diff that T013 + the scope-guard reject — a genuine Consistency contradiction. Tightened SC-004 to name the two specific `spec/` files and explicitly exclude `spec/coverage-index.md`, making the three allowlists identical. Affected: `spec.md` §Success Criteria (SC-004).

### DD-DECIDED items

None. This feature has no separate signed-off design-doc anchor; all cited authorities (Clarifications, research.md R1-R5, contract) are themselves part of the audited bundle, so settled decisions are recorded as PASS against their spec/research/contract anchor rather than DD-DECIDED.

### WAIVED items

None. Zero WAIVED dispositions were applied.

### Realizability sub-check

Not applicable — this feature introduces **no new value-typed entity**. It appends two rows to an existing `constexpr FieldTypeEntry[]` table (`kFieldTypeTable`) and vendors two data files; `field_data_type` (data-model.md E-2) is confirmed frozen and unchanged (spot-verified: no new enum variant in `include/fixpp/dict/field_ref.hpp`). No forward-declared-value-type completeness trap exists in this bundle.

### Anchors spot-verified (all resolve exactly as cited)

- `src/dictionary/xml_loader.cpp:97-104` — the post-canonical collapse block (`TAGNUM`/`LOCALMKTTIME`/`XID`/`XIDREF` rows + preceding comment) — resolves exactly.
- `include/fixpp/dict/field_type.hpp:98-114` — the `field_type_from_data_type` switch-case collapsing `UtcTimestamp` (line 105), `LocalMktDate` (line 108), `UtcDateOnly`, `DialectExtension`, and `default` to `field_type::String` — resolves exactly.
- `include/fixpp/dict/field_ref.hpp:50` (`UtcTimestamp`) and `:53` (`LocalMktDate`) — resolve exactly.
- `tests/dictionary/negative_paths_test.cpp:230` — `TEST(NegativePaths, AC_L8_UnknownFieldTypeThrowsXmlParseError)` — resolves exactly.
- `specs/002-dictionary-xml-loader/data-model.md:297` (AC-L8) — cited as the relaxed contract; not independently re-opened in this audit (out of this feature's scope to amend), but the citing text in spec.md/plan.md is internally consistent with how AC-L8 is characterized elsewhere in this bundle (see CHK014).
- No signed-off Phase-2 design-doc anchor applies to this feature (confirmed — spec.md carries no such anchor field; the plan.md Constitution Check + Gate A round 1 record are the closest analogues and were read in full).

### CodeGraph lookups performed

None via MCP `codegraph_*` tools — the `mcp__codegraph__*` tools were not exposed in this session's toolset. Symbol-existence and shape checks (the intended purpose of the CodeGraph step) were instead performed via direct `Read`/`Grep` against the live source — equivalent evidentiary value for this feature's narrow scope (2 enum-value references, 1 table, 1 switch-case block): `field_data_type::UtcTimestamp`/`::LocalMktDate` (`field_ref.hpp:50,53`), `kFieldTypeTable`/`resolve_field_type` (`xml_loader.cpp:65-116`), `field_type_from_data_type` (`field_type.hpp:98-114`). `codegraph status` confirmed the index is live (899 files) for future use.

### Re-run `/speckit-analyze`? — **YES**

Two `SPEC-FIXED` edits landed in `spec.md` (SC-004, SC-006) and a matching edit in `tasks.md` (T012). The prior `/speckit-analyze` drift check predates these edits and is invalidated. Re-run `/speckit-analyze` before `/speckit-implement`.

### Verdict: **GREEN**

All 28 `[ ]` boxes are dispositioned (0 un-dispositioned). All cited anchors spot-verified to resolve. Zero Completeness/Clarity/Consistency items were closed as WAIVED (0 WAIVED total). Pipeline.md step 9 is satisfied for `checklists/loader-contract.md`; step 10 (`/speckit-implement`) may proceed **after** the mandatory `/speckit-analyze` re-run triggered by the two SPEC-FIXED edits above.
