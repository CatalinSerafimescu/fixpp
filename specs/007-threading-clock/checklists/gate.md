# Requirements-Quality Release Gate Checklist: Application Threading Contract & `fixpp::core::Clock`

**Purpose**: Formal release-gate validation of requirements **quality** (completeness, clarity, consistency, measurability, coverage) across the threading/cancellation, public-API-contract, config/lifecycle, and non-functional domains — before `/speckit-implement`. This tests whether the requirements are *written* correctly, not whether the implementation works.
**Created**: 2026-05-19
**Feature**: [spec.md](../spec.md)
**Rigor**: Formal release gate (Gate-B-mandatory P1 feature; hard correctness obligations)
**Companion**: [requirements.md](requirements.md) (the `/specify` spec-quality checklist — not duplicated here)

> Scope note: 007 is a Phase-4 realization of the **signed-off, Gate-A-converged** `.specify/2d-threading.md` v0.4. Per the 004/006 precedent, design-doc/section anchors are normative traceability references to a frozen authority, not new implementation decisions; items below test whether the *spec's restatement* of those obligations is complete, unambiguous, and internally consistent.

## Requirement Completeness

- [x] CHK001 - Are all four `Clock` methods' contracts (return type, `noexcept`, awaitable shape, and the explicit "no `expected_t` return — cancellation via `operation_aborted`") fully specified? [Completeness, Spec §FR-001/Key Entities] — PASS: FR-001 + data-model E1 (all 4: return types, `noexcept` on `now`/`steady_now`, `awaitable<void> sleep_until` with the explicit "No `expected_t` here — cancellation via `operation_aborted`" note); `contracts/clock.hpp` oracle.
- [x] CHK002 - Is the `effective_clock` resolution rule specified for *every* enumerated session-scoped consumer (heartbeat, SendingTime, S-035, session-scoped LOG/OBS) and distinguished from engine-scope records? [Completeness, Spec §FR-005] — PASS: FR-005 enumerates all four + "engine-scope LOG/OBS MUST read `EngineConfig::clock` and carry `clock_scope = engine`"; I-04; US2 AC-2.
- [x] CHK003 - Are requirements defined for every callback in the 9-element set the strand must serialise (`onLogon…fromApp`, store op, clock wake, transport completion)? [Completeness, Spec §FR-008] — PASS: FR-008 / I-05 list all 9 (`onLogon,onLogout,toAdmin,fromAdmin,toApp,fromApp,store op,clock wake,transport completion`).
- [x] CHK004 - Are the phase-1 `FileStore::flush_for_session_close()` hook's invocation conditions (exactly once, ordering vs Logout `async_write`, idempotency, `terminal`-skip, mid-flush error path) fully specified? [Completeness, Spec §FR-011] — PASS: FR-011 + I-07 + research D-9 specify exactly-once, after-last-`store()`/before-Logout-`async_write`, idempotent, not-under-`terminal`, mid-flush `fdatasync` error → `expected_t::unexpected{store_io_failure}` logged.
- [x] CHK005 - Are all 9 error variants (slots 47–55) and their `FIXPP_ERR_THREAD_*`/`FIXPP_ERR_CANCELLED` coalescing groups enumerated as requirements? [Completeness, Spec §FR-018] — PASS: FR-018 lists all 9; data-model error table + research D-7 + `contracts/threading_errors.hpp` give slot↔variant↔C-ABI-group.
- [x] CHK006 - Are requirements present for the engine-fallback trace-context path when no session domain is in scope? [Completeness, Spec §FR-015/US4 AC-3] — PASS: FR-015 ("fall back to the engine's atomic `EngineConfig::engine_trace_context` snapshot"); US4 AC-3; I-12; E8.
- [x] CHK007 - Does the spec bound the minimal `Session` skeleton's 2d-owned surface explicitly against what `005` later adds, so the completeness audit has a defined boundary? [Completeness, Spec Assumptions/Clarifications 2026-05-19] — PASS: Clarifications Q1 + Assumptions "Session shell scope" + research D-4 + E10 enumerate the exact 2d-owned surface and state "`005` extends this same type".
- [x] CHK008 - Is the `Session::session_arena()` engine-internal accessor's contract (never-null resolution chain, `fixpp::session/`-only) stated as a requirement, given it has no dedicated test seam? [Gap, Spec §FR-019] — PASS: FR-019 + I-18 state `noexcept`/never-null/resolution-chain/`fixpp::session/`-only + "core/ MUST NOT back-edge"; never-null is a ctor precondition (I-18), so the [Gap] (no dedicated seam) is acceptably closed without one.

## Requirement Clarity & Measurability

- [x] CHK009 - Is "deterministic scripted test-double FSM" defined precisely enough to bound which properties it may assert (2d-owned) vs FIX FSM correctness (out of scope)? [Clarity, Spec Clarifications 2026-05-19] — PASS: Clarifications Q2 + Assumptions + research D-5 + FR-022 bound it to 2d-owned properties only (strand serialisation, `mock_clock` determinism, two-phase cancellation, alloc/latency), "never FIX FSM correctness, which is `005`'s".
- [x] CHK010 - Is each §6.3 latency ceiling a concrete numeric threshold with an explicit workload and platform qualifier? [Measurability, Spec §FR-021/SC-004] — PASS: SC-004/US6 AC-3 give concrete ns/µs per row with workload qualifiers (in-strand/cross-thread/cross-strand; 20-tag/200-tag); platform = Assumptions "Linux/Clang Tier 1".
- [x] CHK011 - Is "zero global-heap allocation" defined to clearly distinguish flagged global-heap allocation from expected (non-flagged) PMR-arena allocation? [Clarity, Spec §FR-020/SC-003] — PASS: US6 AC-1 "(PMR-arena allocations expected, not flagged)" + research D-6 "mallocnesia guard catches global-heap `new`/`delete`/`malloc` only".
- [x] CHK012 - Is the cross-thread 250 ns row's "bench-soft" status and its precise pass/fail effect on CI unambiguous? [Ambiguity, Spec §SC-004] — PASS (F-1 resolved 2026-05-19): SC-004 and US6 AC-3 now annotate the cross-thread row inline — "(**bench-soft**: regression-gated only at >5% vs the previous tagged release; the absolute 250 ns is a §10-Q4 follow-up tightening target, **not** an absolute CI hard-fail — data-model I-19)" — so the spec body and data-model I-19 agree and the precise CI pass/fail effect is now unambiguous from spec.md alone.
- [x] CHK013 - Can "no exception crosses the parse→`fromApp` window" be objectively verified as written (boundary of the window defined)? [Measurability, Spec §FR-021] — PASS: "parse→`fromApp` window" is a project-established bounded term (= the dispatch chain from message parse to `fromApp`; FR-013/FR-020/FR-021/SC-003/I-09/I-16), operationalised by the same-boundary alloc guard (`[const §VIII.5]` precedent).
- [x] CHK014 - Are the three `close()` states (already-closing / never-opened / already-closed-drained) each given a distinct, unambiguous required outcome with no overlap? [Clarity, Spec §FR-013/US3 AC-3] — PASS: FR-013 + US3 AC-3 + I-10 + error slot 52 — already-closing → same in-flight awaitable / no error / no side effects; never-opened OR already-closed(drained) → `session_already_closed`; slot 52 explicitly excludes the already-closing case (no overlap).
- [x] CHK015 - Is the `cancellable_dispatch` "≤5 ns relaxed-atomic check" stated as a measurable bound rather than a vague qualifier? [Measurability, Spec §FR-012] — PASS: FR-012 + E9 state a concrete "≤5 ns relaxed-atomic check" numeric bound.
- [x] CHK016 - Is the `trace_context` POD's size/trivial-copyability/standard-layout stated as an enforceable contract (e.g., a pinned `static_assert`) rather than prose? [Measurability, Spec Key Entities / data-model E11] — PASS: E11 mandates a pinned `static_assert(sizeof==32 && is_trivially_copyable_v && is_standard_layout_v)`; **verified present** at `contracts/trace_context.hpp:28-32` ("pinned, not advisory"), not a prose comment.

## Requirement Consistency

- [x] CHK017 - Is the backpressure naming consistent everywhere (closed nested *type* `SessionConfig::backpressure_mode` vs frozen *field* `SessionConfig::app_backpressure`) across FR-010, US5, SC-005, Key Entities? [Consistency, Spec §FR-010] — PASS: identical phrasing in FR-010, US5 AC-3, SC-005, Key Entities/SessionConfig, I-14, `contracts/session_config.hpp` (nested `backpressure_mode` enum + `app_backpressure` field).
- [x] CHK018 - Do US3 acceptance scenarios and FR-011 agree on phase-1↔phase-2 ordering and the "`terminal` skips phase 1 / hook not invoked" rule? [Consistency, Spec §US3/§FR-011] — PASS: US3 narrative + AC-1 and FR-011 + I-07 agree (hook once after last `store()` / before Logout `async_write`; root `total` only after phase 1 resolves; `terminal` skips phase 1, hook not invoked).
- [x] CHK019 - Is SC-006's third-party-`Clock` claim consistently scoped as scripted-test-double (not a real FIX FSM) everywhere it appears (US1/US2 Independent Tests, SC-006, seam-15)? [Consistency, Spec §SC-006] — PASS: US2 Independent Test, SC-006, research D-5 (seam 15), US1 Independent Test all carry the identical "via the scripted test-double FSM… not a real FIX session FSM (`005`-owned)" qualifier.
- [x] CHK020 - Does the trace-context recovery mechanism (`session_ptr()` member; NOT `any_io_executor::query`; NOT `thread_local`) read consistently across FR-015, US4, and Edge Cases? [Consistency, Spec §FR-015] — PASS: FR-015, US4 narrative/AC-2/Independent-Test (negative assertion), Edge Cases, E8, I-11, `contracts/trace_context.hpp` comment all state the same `session_ptr()`-member / not-`query` / not-`thread_local` mechanism.
- [x] CHK021 - Are the `clock_not_set` precedence rules consistent between FR-006 ("regardless of session `clock_override`s") and US2 AC-1? [Consistency, Spec §FR-006] — PASS: FR-006 "regardless of session `clock_override`s" == US2 AC-1 "even if every session supplies `clock_override`"; I-03.
- [x] CHK022 - Is the `close_timeout` ownership statement consistent (owned by `005`, **not** a `SessionConfig` field; 2d wires mechanism only) across FR-011, Assumptions, and the Key Entities/config description? [Consistency, Spec §FR-011/Assumptions] — PASS (no contradiction): spec.md FR-011 references only the timeout *mechanism* (`Clock::sleep_until` under the child state), the SessionConfig Key Entity correctly omits `close_timeout`, and `contracts/session_config.hpp` carries no such field; full ownership rationale ("owned by `005`, not a config field; 2d wires mechanism only") is in research D-9 + the contract comment. Internally consistent. *Minor (non-blocking):* the explicit ownership sentence is absent from spec.md's FR-011/Assumptions — recommend a one-line Assumptions note for traceability; does not affect correctness or this gate.

## Threading & Cancellation Scenario Coverage

- [x] CHK023 - Are requirements defined for the cancellation-racing-release/drain case via the deterministic three-case `cancellable_dispatch` contract (exactly one of granted/cancelled per waiter)? [Coverage, Spec §Edge Cases/§FR-012] — PASS: Edge Cases ("exactly one of granted/cancelled per waiter") + FR-012 three-case + US3 AC-2 + I-08 + E9.
- [x] CHK024 - Are requirements defined for `cancel_sleeps()` invoked re-entrantly from inside a `sleep_until` completion handler (idempotent, no re-entry)? [Coverage, Spec §Edge Cases] — PASS: Edge Cases ("idempotent, no re-entry §6.6") + FR-003 ("safe to call concurrently and re-entrantly") + E1.
- [x] CHK025 - Are requirements specified for `direct_executor` attested over a genuinely non-serialised executor, distinguishing debug-assert vs release documented-UB behavior? [Coverage, Edge Case, Spec §Edge Cases/US1 AC-4] — PASS: Edge Cases explicitly distinguishes "debug builds trip the strand-invariant assert… release builds are documented user-contract-violation UB"; US1 AC-4 covers the non-attested rejection path.
- [x] CHK026 - Is the `~Engine` teardown ordering with in-flight sleeps/`fromApp` fully specified as an ordered requirement (close-all → drain → `cancel_sleeps` → drain waiter list → clear `clock` last)? [Coverage, Spec §Edge Cases/US2 AC-5] — PASS: Edge Cases + US2 AC-5 + I-17 give the exact ordered sequence with `EngineConfig::clock` `shared_ptr` cleared **last**.
- [x] CHK027 - Are abort/forced-disconnect requirements defined for PMR arena exhaustion on the dispatch / `cancellable_dispatch` node? [Coverage, Exception Flow, Spec §Edge Cases] — PASS: Edge Cases ("`error::strand_dispatch_failed_oom`, forced disconnect") + E9 ("not a silent fallback") + error slot 50.
- [x] CHK028 - Are requirements defined for a coroutine resuming on a different thread than it suspended on, asserting trace-context correctness via the stable `Session*`? [Coverage, Spec §Edge Cases/US4 AC-1] — PASS: Edge Cases + US4 AC-1 + Independent Test (assert thread-id changed, re-read, byte-equality) + FR-015 + I-11.
- [x] CHK029 - Are requirements defined for the empty-slot-mid-`Session::open()` case (default `trace_context` + debug assertion)? [Edge Case, Spec §Edge Cases] — PASS: Edge Cases ("awaiter returns default-constructed `trace_context` + debug assertion") + E8.

## API Contract Quality

- [x] CHK030 - Is `make_session_executor(...)` specified as the *single* `executor_not_serialised` enforcement point with no alternative construction path? [Clarity, Spec §FR-009] — PASS: FR-009 ("the single point"), E6 ("Constructed only via the free helper… the single enforcement point"), FR-018, `contracts/session_executor.hpp` ("THIS IS THE SINGLE ENFORCEMENT POINT" + "Internal sites should not construct directly").
- [x] CHK031 - Are `session_executor` value-semantics requirements stated unambiguously (copyable but **NOT** trivially copyable) to prevent a trivial-copy assumption? [Clarity, Spec Key Entities] — PASS: Key Entities + E6 ("copyable but **NOT** trivially copyable… `std::is_trivially_copyable_v` is **false**; do not assert or imply trivial copyability"); echoed in `contracts/session_executor.hpp:48-52`.
- [x] CHK032 - Is the `Session::open()` 2d-owned shape bounded so it pins only the 2d obligations (executor resolution, `effective_clock`, slot population, rejections) without freezing `005`'s FIX FSM signature? [Clarity, Spec §FR-018] — PASS: FR-018 ("the FIX establishment FSM is `005`'s; the contract here pins only the 2d-owned obligations") + E10 ("the design doc freezes no `open()` signature, only the §4.8:994 resolution rule").
- [x] CHK033 - Is the error-slot model specified as additive, non-renumbering at slots 47–55, with the C-ABI-symbol-shape decision explicitly deferred to `2i`? [Clarity, Spec §FR-018/Assumptions] — PASS: FR-018 ("final coalescing is 2i's call"), Assumptions "C-ABI symbol shapes deferred to `2i`", research D-7, `contracts/threading_errors.hpp` (non-renumbering invariant; final coalescing 2i's call).

## Config & Lifecycle Coverage

- [x] CHK034 - Are all illegal-config rejection cases enumerated with their specific required error (null `dictionary` / null `EngineConfig::executor` / sentinel `security_profile` / `direct_executor`+`spin`)? [Completeness, Spec §FR-018/US5 AC-2] — PASS: FR-018 enumerates all four → `error::invalid_session_config`; US5 AC-2; error slot 53; `contracts/session_config.hpp` (sentinel `security_profile`, required `dictionary`).
- [x] CHK035 - Is the close-and-reopen-only reconfiguration constraint specified to apply to *every* frozen field (no mid-session reconfiguration path)? [Coverage, Spec §US5 AC-1] — PASS: US5 AC-1 ("any frozen field… mid-session reconfiguration… not supported; the only path is close-and-reopen") + FR-016 + I-13; `contracts/session_config.hpp` ("FROZEN at Session::open — close-and-reopen only").
- [x] CHK036 - Is the FIXT.1.1 `ApplVerID(1128)`-miss routing requirement unambiguous that it routes through the `[2c §6.7]` dict-layer error (not a 2d synonym) at **both** dispatch-time and `Engine::open` registry-build? [Clarity, Spec §FR-017/US5 AC-4] — PASS: FR-017 (registry built at `Engine::open`; per-message miss → `[2c §6.7] dict_no_dictionary_for_application_version`, "NOT a 2d-layer synonym") + I-15 explicitly "(dispatch-time **and** `Engine::open` registry-build)" + research D-13 + US5 AC-4.
- [x] CHK037 - Is the uniform `resolved = override.value_or(engine_anchor)` pattern specified consistently across all three axes (executor, clock, dictionary)? [Consistency, Spec §FR-016] — PASS: FR-016 ("executor, clock, and dictionary axes MUST follow one uniform… `resolved = override.value_or(engine_anchor)`") + US5 narrative + I-13 + `contracts/session_config.hpp` header comment.

## Non-Functional Requirements Quality

- [x] CHK038 - Are the allocation-guard requirements scoped to a named platform tier (Linux/Clang Tier-1) and explicit corpus sizes (10⁴ messages / 10⁴ cycles)? [Clarity, Spec §SC-003/Assumptions] — PASS: SC-003 (10⁴-message corpus / 10⁴ heartbeat cycles) + Assumptions "Linux/Clang Tier 1 is the gating platform for the `mallocnesia` allocation guards" + US6 AC-1/AC-2.
- [x] CHK039 - Is the per-`Session*` timer-slot rule ("allocated exactly once on first `sleep_until`; an idle session that never sleeps allocates none") stated measurably for **both** threading modes? [Measurability, Spec §FR-020/US6 AC-2] — PASS: FR-020 + US6 AC-2 ("allocated exactly once **on the first `sleep_until` for that session**… under both threading modes… an idle session that never sleeps allocates no slot") + E12 + research D-8.
- [x] CHK040 - Are the sanitizer obligations specified per-seam (which seams are TSan-mandatory; which additionally ASan-clean) rather than generically? [Completeness, Spec §SC-001] — PASS: SC-001 names the seam classes that are TSan-clean (strand-serialisation, sleep/cancel-race, `session_local`-lifetime, cancellation) vs additionally ASan-clean (race/leak); not a generic blanket; US2 AC-4 "(TSan+ASan-clean)".
- [x] CHK041 - Is the fuzz-harness obligation's scope unambiguous (voluntary, not a `[const §VII.7]` requirement, campaign duration, zero-deadlock/double-free/PMR-leak pass criteria)? [Clarity, Spec §SC-007/Assumptions] — PASS: SC-007 gives crisp pass criteria (zero deadlocks/double-frees/PMR leaks); research D-11 fixes the voluntary, "**not** a `[const §VII.7]`-required obligation" status; campaign duration is intentionally a "configured campaign" operational knob (quickstart-owned, same pattern as bench/corpus sizes) — the *requirement* is unambiguous.

## Dependencies, Assumptions & Conflicts

- [x] CHK042 - Is the assumption that `005`/`2e` are deferred (Session = minimal skeleton; conformance corpus 2d-scoped) validated and explicitly bounded so the feature-completeness audit passes **without** a waiver? [Assumption, Spec Assumptions/§SC-002] — PASS: Assumptions "Session shell scope" + "Conformance corpus scope" ("No full-FIX-corpus discharge is claimed by 007; recorded… so the audit passes without a waiver") + SC-002 + SC-008 + research D-5/D-11.
- [x] CHK043 - Are the consumed-not-built upstream dependencies (merged `006` `session_executor` backing, merged `003` `version_registry`) documented as requirement-relevant assumptions? [Dependency, Spec Assumptions] — PASS: Assumptions "Prerequisite ordering" + "PMR three-arena model" + Authority anchor/Normative References; research D-14/D-13 (006 declaration-only helper, 003 `version_registry`, `001/002/004` baseline).
- [x] CHK044 - Is the NFR-015 catalogue claim stated so that only the Gate-B Status-field promotion is residual (row/coverage-index/`[arch §11]`/`session_local` rename already landed), with this feature editing none of those files? [Assumption, Spec §SC-008] — PASS: SC-008 ("already present in the authority set as of `2d` v0.4 sign-off 2026-05-08… the only orchestrator step that remains is the catalogue Status-field promotion… this feature does not edit architecture.md / feature-catalogue.md / coverage-index.md directly") + Authority anchor + research D-12.
- [x] CHK045 - Is there any unresolved conflict between "minimal real `Session`, no FIX FSM" and the seams that drive FIX-shaped sequences, and is it explicitly resolved via the scripted-test-double scoping? [Conflict, Spec Clarifications 2026-05-19] — PASS: explicitly resolved — Clarifications Q2 + Assumptions + research D-5 + FR-022 route every FSM-shaped seam through the deterministic scripted test-double FSM asserting only 2d-owned properties. No residual conflict.
- [x] CHK046 - Does the spec unambiguously state that seam 11 / SC-002 claims **no** FIX-TC discharge (to avoid an inadvertent completeness-audit waiver)? [Conflict, Spec §SC-002/Assumptions] — PASS: SC-002 ("**not** the full FIX-TC corpus, which lands with `005`") + Assumptions "Conformance corpus scope" ("No full-FIX-corpus discharge is claimed by 007… so the audit passes without a waiver") + research D-5/D-11.

## Audit Result (walked 2026-05-19, pre-`/speckit-implement`)

| Domain | Items | Pass | Fail |
|---|---|---|---|
| Requirement Completeness | CHK001–008 | 8 | 0 |
| Clarity & Measurability | CHK009–016 | 8 | 0 |
| Consistency | CHK017–022 | 6 | 0 |
| Threading & Cancellation Coverage | CHK023–029 | 7 | 0 |
| API Contract Quality | CHK030–033 | 4 | 0 |
| Config & Lifecycle Coverage | CHK034–037 | 4 | 0 |
| Non-Functional Quality | CHK038–041 | 4 | 0 |
| Dependencies/Assumptions/Conflicts | CHK042–046 | 5 | 0 |
| **Total** | **46** | **46** | **0** |

**Overall: 46/46 PASS (100%) after F-1 resolution. Requirements-quality gate GREEN.**

### F-1 (CHK012) — cross-thread 250 ns "bench-soft" status under-stated in spec.md — **RESOLVED 2026-05-19**

**Defect class:** Ambiguity / measurability (requirement text, not implementation).

**Detail (as found):** The "bench-soft" qualifier existed **only** in `data-model.md` I-19. spec.md SC-004/US6 AC-3 listed `cross-thread ≤250 ns` alongside the genuinely-hard ns/µs ceilings under one uniform ">5% regression fails CI" gate; the only softening signal in the spec body was Assumptions §10-Q4 (about tightening the number later, not about CI pass/fail). A spec-only reader could not determine the precise CI effect for that row.

**Resolution applied:** SC-004 and US6 AC-3 now carry the inline annotation — *"cross-thread ≤250 ns (**bench-soft**: regression-gated only at >5% vs the previous tagged release; the absolute 250 ns is a §10-Q4 follow-up tightening target, **not** an absolute CI hard-fail — data-model I-19)"* — verbatim-consistent with `data-model.md` I-19. No design-doc decision touched (the design doc already scopes Q4 as a 2d follow-up); pure spec-restatement clarity fix per the spec's own "an inconsistency is a defect in this spec" rule. CHK012 now PASS. Gate-B precondition: requirements-quality gate is fully green.

## Notes

- Check items off as completed: `[x]`; record findings inline. An item "fails" if the *requirement text* is missing, ambiguous, unmeasurable, inconsistent, or uncovered — not if implementation is incomplete.
- Traceability: 45/46 items carry a `[Spec §…]` ref or a `[Gap]/[Ambiguity]/[Conflict]/[Assumption]` marker (≥80% satisfied).
- This is a requirements-quality gate, complementary to `/speckit-analyze` (cross-artifact consistency) and `/gate-a` (design-faithfulness). It does not re-litigate the signed-off `2d` v0.4 design decisions.
