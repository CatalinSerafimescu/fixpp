# Checklist: Correctness & ABI — requirements quality (073-nested-read-arena-failloud)

**Purpose**: Unit-test the WRITTEN requirements (spec/plan/research/data-model/contracts/tasks) for the fail-loud status logic and C-ABI safety — are they complete, clear, consistent, measurable? Not implementation verification.
**Created**: 2026-07-13
**Audience**: Gate-B reviewer / implementer.

## Status-logic completeness (the round-1 Gate-A root cause)

- [x] CHK001 - Are ALL arena-exhaustion sub-modes explicitly enumerated as origins of `alloc_failed`? [Completeness, research §D2 / data-model set-condition] — SPEC-FIXED (implement-time): originally enumerated only TWO origins — (a) `build_nested_subview → nullptr` and (b) the built sub-table's own `group_slices()` `catch(bad_alloc)`. A **third** origin surfaced at `/speckit-implement` (the same `feedback_status_origin_must_cover_all_alloc_catch_sites` census gap that Gate-A round 1 fixed for mode (b), one level deeper): (c) the sub-table builds **non-null** but its ctor's own `build()` degrades on `bad_alloc` to `status_ = out_of_memory` (`offset_table.cpp:366-370`) — a `noexcept` ctor cannot let `bad_alloc` escape — and `group_slices_status()` on that degraded table returns `{empty, false}` with NO throw at its own site (invisible to both (a) and (b)). Reconciled research §D2, data-model set-condition, contracts postconditions, and tasks T004 to enumerate three origins; surfaced via `build_status().error() == out_of_memory`, scoped to `out_of_memory` only (malformed-data degradation stays not-failed, FR-007). Verified live: `:709-711` (a), `:674-675` (b), `:366-370` + `:782-786`/`:810-813` (c). Handed loud to Gate B (origin count 2→3; verify census at `:366`/`:684`/`:722` complete).
- [x] CHK002 - Is it specified that `alloc_failed` is the OR of the two sub-modes at EVERY empty-returning exit of `nested_group_slices` (the cache-hit early return AND the final return), not just one? [Completeness, research §D2] — SPEC-FIXED: data-model.md "Set-condition (both empty-returning exits, D2)" correctly names `row.table` at the cache-hit exit and the resolved `table` at the final exit — but `tasks.md` T004 (the artifact an implementer executes verbatim) stated a single generic `table` for BOTH exits. Traced against live source (`src/wire/offset_table.cpp:742-769`): at the cache-hit early return (`:748-750`), the function-scope local `table` is unassigned (`nullptr`) for a same-`no_tag` match — the resolved pointer there is `row.table`. A literal reading of T004's formula at that exit evaluates `(table == nullptr)` as unconditionally true, producing a **false-positive `alloc_failed=true` on every warm cache-hit of a successfully-built group** — a direct FR-006 (no success-path change) / FR-007 (disjointness) violation risk, undetected by any existing cache test (they assert on `slices`, not the new bit). Edited `tasks.md` T004 to disambiguate: `row.table` at the cache-hit exit, resolved `table` at the final exit, matching data-model.md:22-24.
- [x] CHK003 - Is the requirement that the status ORIGINATE at the point of failure (not be reconstructed at a caller by re-testing a null pointer) stated unambiguously, and is the `group_slices_status` internal-split mechanism named as how it is satisfied? [Clarity, Spec §FR-002 / contracts] — PASS: Spec §FR-002 states the origination requirement; research.md §D2 "FR-002 (originate at the point of failure)" names `group_slices_status(no_tag)` explicitly; contracts/nested_slices_result.md "Internal status split" section gives the exact before/after signatures.
- [x] CHK004 - Is the repeated-read behavior specified for BOTH sub-modes (mode (a) served from the cached null row; mode (b) re-materialized from the cached non-null row → re-throws), so the second read also fails loud? [Coverage, Spec Edge Cases / research §D2] — PASS: Spec Edge Cases "Repeated read after a failed build" states the general MUST; research.md §D2 final paragraph + contracts/nested_slices_result.md postconditions spell out both sub-mode mechanics explicitly; tasks T001/T006/T008 each require a repeated-read assertion.

## Disjointness (no false positives / negatives)

- [x] CHK005 - Is the disjointness rule stated precisely: `slice_data==nullptr` (absent) → not-failed, and a NON-null table returning a genuine count-0 span WITHOUT throwing → not-failed? [Clarity, Spec §FR-007 / data-model] — PASS: Spec §FR-007; data-model.md "Invariants" section states both legs verbatim, cross-checked against the live `slice_data == nullptr` guard at `offset_table.cpp:722-724`.
- [x] CHK006 - Can the "only a present group whose sub-view could not be allocated raises the signal" requirement be objectively verified by a witness (i.e., is it measurable, with named controls)? [Measurability, Spec §SC-003] — PASS: Spec §SC-003 names the controls (absent/zero-entry, ample arena, 0 false positives); tasks T006/T008 implement the named non-failure control witnesses on both consumer paths.
- [x] CHK007 - Is the zero-length-slice case addressed (a zero-len slice still builds a non-null empty table → not-failed), so it is not mis-classified as failure? [Edge Case, research §D2 seam] — PASS: research.md "Verified seam" table row 1 states explicitly "a zero-len slice still builds a non-null empty table"; data-model.md set-condition confirms a non-null table with a non-throwing count-0 `group_slices_status()` returns `{empty, false}`.

## C-ABI / Article X safety

- [x] CHK008 - Is the exact C-ABI error code fixed (reuse `FIXPP_ERR_WIRE_LIMIT_EXCEEDED`) rather than left open, and is the "distinct from OK AND from TAG_NOT_FOUND" property stated? [Clarity, Spec §FR-008 / Clarifications] — PASS: Spec Clarifications Q1 + §FR-008 fix the code and state distinctness from both `OK` and `TAG_NOT_FOUND`; verified `FIXPP_ERR_WIRE_LIMIT_EXCEEDED = 101` at `include/fix/c_api/error.h:78` and the existing `wire_offset_table_full → 101` mapping at `src/capi/error.cpp:71-74`.
- [x] CHK009 - Is it explicitly required that NO C-ABI symbol/struct/signature/macro changes and NO `core::error` enumerator is appended, with the rationale (nested read returns `fixpp_error_t` directly) recorded? [Completeness, plan Article X / Spec §FR-008] — PASS: Spec §FR-008 states the MUST NOT explicitly with the direct-`fixpp_error_t` rationale; the analogous 072 precedent (no `core::error` append for a catch-type-discriminated new error) is a live, verified pattern in the codebase.
- [x] CHK010 - Is the C-ABI return-ordering requirement (alloc-failed → `WIRE_LIMIT_EXCEEDED` BEFORE the presence probe) unambiguous in the contract? [Clarity, contracts / tasks §T007] — PASS: contracts/nested_slices_result.md "C-ABI consumer" section and tasks T007 both place the `alloc_failed` check before the `slices.empty()` presence probe; verified against live `src/capi/message_read.cpp:492` (the exact presence-probe line the new arm must precede).
- [x] CHK011 - Is the Article X behavioral-change (formerly `OK`/nc=0 → now an error in an edge) flagged as a Gate-A-confirmed item, and is a task named to assert no ABI-golden asserts the old behavior? [Consistency, plan §Complexity / tasks §T014] — PASS: plan.md Constitution Check (Article X "PASS-with-note... Flagged for explicit Gate A confirmation") + Complexity Tracking row; Gate A rounds 1-2 recorded in plan.md §Gate A; tasks T014 names the ABI-golden no-regression assertion via `abi_symbol_golden_test.cpp` + the CI workflow.

## Scope & non-regression invariants

- [x] CHK012 - Is the top-level-reads-out-of-scope boundary (L-073-1) stated, and the requirement that the public `group_slices()` wrapper leaves every top-level caller behaviorally unchanged? [Clarity, Spec §Assumptions / research §D8] — PASS: Spec §Assumptions "Known limitation / out of scope (L-073-1)"; research.md §D8 L-073-1 paragraph; data-model.md line 28 "every top-level caller is unchanged"; verified live — the public `group_slices(no_tag)` signature stays unchanged per contracts/nested_slices_result.md, and the only two production consumers of `nested_group_slices` are `message_read.cpp:489` and the emitter (`emit_messages.cpp:263`) — no third call site exists (grep-verified across `src/`, `include/`, `tools/`).
- [x] CHK013 - Is the fail-loud-only scope (FR-009: no arena-sizing/growability change) stated as a MUST-NOT, and is it consistent across spec, plan, research, and tasks (no task touches arena sizing)? [Consistency, Spec §FR-009] — PASS: Spec §FR-009 states the MUST-remain-bounded requirement; research.md §D8 restates; plan.md Blast radius / Project Structure and tasks.md T001-T018 scanned — no task references arena capacity or growability change (only injecting a tiny-cap *test* arena, which is the witness fixture, not a change to the shipped arena policy).
- [x] CHK014 - Is the no-behavior-change-on-success/absence requirement (FR-006) measurable via a named full-suite regression gate? [Measurability, Spec §FR-006 / SC-004 / tasks §T013] — PASS: Spec §FR-006 + §SC-004; tasks T013 names the full (not narrow) ctest regression gate across debug + touched sanitizer presets.
- [x] CHK015 - Are the `group_view` invariants that must be preserved (trivial-copyability; seam #8 iter()/operator[] agreement) explicitly called out as requirements, not left implicit? [Completeness, research §D4 / data-model] — PASS: data-model.md "group_view status extension" states "Stays trivially copyable"; tasks T009 explicitly requires confirming `is_trivially_copyable_v<group_view>` AND "seam #8 (iter()/operator[] agreement)" hold. Anchor verified: `.specify/2b-wire.md:765` seam item 8 "Repeating-group equivalence — for any group-bearing message, group_view::iter() and group_view::operator[] enumerate the same entries in the same order" — resolves exactly.
- [x] CHK016 - Is the golden-regeneration obligation (emitter change → regen the read goldens, run FULL ctest for `codegen_determinism_test`) specified, including which dictionaries actually change vs no-op? [Completeness, tasks §T011] — PASS: tasks T011 names the exact regen targets and states v44 (58 sites)/v50sp2 (690 sites) change while v42/vt11 are a byte-identical no-op (expected, not a broken emitter); research.md §D7 cites the prior stale-golden hang precedent.

## Realizability sub-check (mandatory, value-typed entities)

- **`nested_slices_result`** (`fixpp::wire`, offset_table.hpp, NEW): fields are `std::span<group_slice const>` + `bool`. `group_slice` is a complete type at this point (included verbatim via `view.hpp`, `offset_table.hpp:22`), not forward-declared — `std::span<T const>` never requires `T` complete for the struct's own completeness in any case. No owning pointer to a deferred type. **Realizable — clean, no stub needed.**
- **`group_slices_result`** (internal, same file): identical shape (`span<group_slice const>` + `bool`). Same analysis. **Realizable — clean.**
- **`group_view<GroupT>` status extension**: adds one `bool alloc_failed_` NSDMI + a defaulted ctor param to an already-shipped, already-realizable trivially-copyable template (existing members: `View` base, `std::span<slice const> instances_`, `entry_context base_ctx_` — `entry_context` already carries its own `static_assert(is_trivially_copyable_v<entry_context>)` at `group_view.hpp:100-101`). Adding a `bool` cannot break trivial-copyability or completeness. **Realizable — clean.**

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 14 |
| SPEC-FIXED | 2 |
| DD-DECIDED | 0 |
| WAIVED | 0 |
| **Total** | 16 |

### SPEC-FIXED items
- CHK002 — `tasks.md` T004's crux `alloc_failed` formula used one generic `table` identifier for both the cache-hit exit and the final exit; a literal implementation reads the loop-scoped local `table` (unassigned/`nullptr` at the cache-hit exit for a same-`no_tag` match) instead of `row.table`, producing a false-positive `alloc_failed=true` on every warm cache-hit of a successfully-built group. Edited T004 to name `row.table` at the cache-hit exit and the post-build `table` at the final exit, matching data-model.md:22-24. Affected: `specs/073-nested-read-arena-failloud/tasks.md#T004`.
- CHK001 — **SPEC-FIXED at `/speckit-implement`** (not at the pre-implement audit): the bundle enumerated only two `alloc_failed` origins; a third (mode (c) — non-null sub-table whose ctor `build()` degraded to `out_of_memory`) surfaced when the T006 witness passed for the wrong reason at `kTinyCap=1024`. Reconciled research §D2, data-model, contracts, tasks T004, and this item to three origins; fixed in `offset_table.cpp` at both `nested_group_slices` exits (`build_status().error() == out_of_memory`, scoped to OOM). Handed loud to Gate B. Affected: research.md §D2, data-model.md, contracts/nested_slices_result.md, tasks.md#T004, src/wire/offset_table.cpp.

### DD-DECIDED items
None.

### WAIVED items
None.

Anchors spot-verified (all resolve in the signed-off revisions):
- `src/wire/offset_table.cpp:674-675, 709-711, 718-769, 722-724, 748-750, 768, 776-781` — exact line match against live source.
- `src/capi/message_read.cpp:489-499, 492` — exact line match.
- `tools/codegen/fixpp-codegen/emit_messages.cpp:256-286` — exact line match.
- `include/fix/c_api/error.h:78` (`FIXPP_ERR_WIRE_LIMIT_EXCEEDED = 101`) + `src/capi/error.cpp:71-74` (`wire_offset_table_full → 101` mapping) — resolve.
- `.specify/2b-wire.md:765` (seam #8, repeating-group equivalence) — resolves.
- `.specify/constitution.md:51` (Article II, C++23) and Articles VII/IX/X/XI/XV/XVI/XVII (plan.md Constitution Check table) — resolve.
- `specs/065-cabi-nested-group-membership/research.md` Decision 6 (`:91`, rejected-alternative widening) + FR-009 cross-cutting fact (`:116`, "do not widen the span primitive") — both resolve; the combined "Decision 6 / FR-009" citation in spec.md's Context & Problem narrative is grounded across the two sections, not dangling.
