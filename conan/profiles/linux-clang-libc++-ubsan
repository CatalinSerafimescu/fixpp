# Conan profile: linux-clang-libc++-ubsan
# UBSan build under libc++ — Tier-3 lane for NFR-017 (046-atomic-shared-ptr).
# Mirrors linux-clang-ubsan sanitizer blocks but uses compiler.libcxx=libc++
# so all rebuilt deps also use the libc++ runtime (ABI-consistent).
# Clang 22 matches local install; CI provisions via apt.llvm.org.
# OTel is not built on this lane (with_otel=False avoids the SDK rebuild).

[settings]
os=Linux
arch=x86_64
build_type=Debug
compiler=clang
compiler.version=22
compiler.libcxx=libc++
compiler.cppstd=23

[buildenv]
CC=clang-22
CXX=clang++-22
CXXFLAGS=-stdlib=libc++ -fsanitize=undefined -fno-omit-frame-pointer
LDFLAGS=-stdlib=libc++ -fsanitize=undefined

[conf]
tools.cmake.cmaketoolchain:generator=Ninja
tools.build:cxxflags=["-fsanitize=undefined", "-fno-omit-frame-pointer"]
tools.build:sharedlinkflags=["-fsanitize=undefined"]
tools.build:exelinkflags=["-fsanitize=undefined"]
# compiler.libcxx=libc++ is already in [settings] so it enters package_id.
# The confs line keeps asan/ubsan/tsan distinct within the libc++ family
# (same rationale as linux-clang-asan — see that profile for full explanation).
tools.info.package_id:confs=["tools.build:cxxflags", "tools.build:cflags", "tools.build:sharedlinkflags", "tools.build:exelinkflags"]
