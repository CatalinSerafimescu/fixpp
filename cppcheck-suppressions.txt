# cppcheck project-level suppressions
# Used by: cppcheck --enable=all --inline-suppr --suppressions-list=cppcheck-suppressions.txt

# Policy: prefer inline `// cppcheck-suppress <id>` comments at the offending
# site over blanket category suppressions here. Inline comments document the
# rationale next to the code and make every suppressed finding individually
# reviewable. Add a categorical entry below only when the same false positive
# fires across so many sites that inline annotation hurts readability.

# decimal_alias.hpp uses a computed #include to allow users to override the
# decimal implementation header. FIXPP_DECIMAL_USER_HEADER is intentionally
# undefined during static analysis; cppcheck cannot resolve computed includes
# without -D.
preprocessorErrorDirective:*decimal_alias.hpp

# ── chore/repo-wide-lint (2026-06-01): clear the W-2 cppcheck carry-forward ──
# Each class below was source-read across all sites; no real defect found
# (consistent with every /speckit-verify Step-1 triage). Categorical here rather
# than inline because each fires across many sites or is a whole-program limitation.

# Library public API + test-only accessors are used cross-TU / by external
# consumers; cppcheck's whole-program view can't see those callers (the W-2
# pattern, 89 sites). unusedFunction is meaningless for a published library surface.
unusedFunction

# Same whole-program-view limitation as unusedFunction, applied to struct/class
# members: cppcheck flags a member "never used" when its only readers/writers live
# in a TU outside the analyzed set (config structs populated by callers — e.g.
# FileSinkConfig/OtlpLogSinkConfig/SyslogSinkConfig/OtelConfig — and class members
# used across separate .cpp from the header, e.g. FileSink::fd_/stream_,
# SessionConfig public fields). First tripped by 017-log-otel's new sink/otel
# config headers; source-read across all 38 sites, every one a published-surface or
# cross-TU FP, none dead.
unusedStructMember

# House style deliberately uses explicit loops over <algorithm> calls; matches the
# clang-tidy posture. Pure style, fires widely.
useStlAlgorithm

# Minor-style classes the codebase deliberately does not adopt — mirrors the already-
# suppressed clang-tidy misc-const-correctness (.clang-tidy): local scope/const are a
# deliberate authoring choice here, not a defect.
variableScope
constVariablePointer
constParameterReference
constParameterPointer
shadowVariable
passedByValue

# False positives confirmed by source-read (scoped per-file to stay narrow):
#   engine.cpp: `session` is from make_unique / a guaranteed-set acceptor session;
#     cppcheck's unique_ptr::get() flow analysis is conservatively null-pessimistic.
nullPointer:src/session/engine.cpp
#   system_clock_source.cpp: `released` exists ONLY to destroy the timer slot AFTER
#     the lock is released (deliberate destroy-outside-lock RAII), not dead.
unreadVariable:src/core/system_clock_source.cpp
#   compid_authorization_policy.cpp: the SAN-accessor return is opaque to cppcheck;
#     the !empty() guards before [0] are correct and necessary.
knownConditionTrueFalse:src/session/compid_authorization_policy.cpp
#   reconnect_fsm.cpp: a correct `session_ != nullptr` guard before deref (the
#     standalone-fsm test path has no Session owner).
duplicateCondition:src/session/reconnect_fsm.cpp
#   parser.hpp: local `end` deliberately named; shadows std::end only by name.
shadowFunction:*parser.hpp
#   unknown_fields_view intentionally provides its own empty() over View::empty().
duplInheritedMember:*unknown_fields.hpp
