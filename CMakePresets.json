{
  "version": 6,
  "cmakeMinimumRequired": { "major": 3, "minor": 28, "patch": 0 },

  "configurePresets": [
    {
      "name": "_base",
      "hidden": true,
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/${presetName}",
      "cacheVariables": {
        "CMAKE_EXPORT_COMPILE_COMMANDS": "ON",
        "FIXPP_BUILD_TESTS": "ON",
        "FIXPP_WERROR": "ON"
      }
    },

    {
      "name": "linux-clang-debug",
      "displayName": "Linux / Clang / Debug",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-debug/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-release",
      "displayName": "Linux / Clang / Release",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-release/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-asan",
      "displayName": "Linux / Clang / ASan",
      "description": "#213. The fuzz targets carry -fsanitize=fuzzer,address,undefined, so this is their natural lane, and tier1's matrix selects lanes purely by preset name with no per-lane -D overrides -- a workflow flag would be the special case here, not the preset. It also keeps local reproduction of the CI lane accurate. COSTS: this adds one build+link per fuzz target and one fast ctest per corpus; measured small against the lane's own build, but a MEASUREMENT ROTS so re-derive rather than trusting a number here -- `cmake --build build/linux-clang-asan --target $(ninja -C build/linux-clang-asan -t targets | grep -o '^fuzz_[a-z_]*' | tr '\\n' ' ')` for build cost, `ctest --test-dir build/linux-clang-asan -L fuzz` for replay cost, `du -ch build/linux-clang-asan/bin/fuzz_*` for disk. No existing object's command line changes, so no compiler-cache entry is invalidated -- that one is STRUCTURAL and does not rot. ⚠️ DISK IS THE AXIS TO WATCH: this is the disk-tightest leg in the matrix and it has died with `ld: final link failed: No space left on device` before, so if that recurs the fuzz binaries are one of the things to weigh. ASYMMETRY, deliberate: linux-clang-libc++-asan does NOT set FIXPP_BUILD_FUZZ. One lane replaying the corpora is the regression guard #213 asked for; a second would duplicate the cost without covering any additional seed.",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "FIXPP_ENABLE_ASAN": "ON",
        "BUILD_SHARED_LIBS": "ON",
        "FIXPP_BUILD_FUZZ": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-asan/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-ubsan",
      "displayName": "Linux / Clang / UBSan",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "FIXPP_ENABLE_UBSAN": "ON",
        "BUILD_SHARED_LIBS": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-ubsan/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-tsan",
      "displayName": "Linux / Clang / TSan",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "FIXPP_ENABLE_TSAN": "ON",
        "BUILD_SHARED_LIBS": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-tsan/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-coverage",
      "displayName": "Linux / Clang / Coverage",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "FIXPP_ENABLE_COVERAGE": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-coverage/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-gcc-release",
      "displayName": "Linux / GCC / Release (sanity)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_C_COMPILER": "gcc",
        "CMAKE_CXX_COMPILER": "g++",
        "FIXPP_WERROR": "OFF",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-gcc-release/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-gcc-debug",
      "displayName": "Linux / GCC / Debug (sanity)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "gcc",
        "CMAKE_CXX_COMPILER": "g++",
        "FIXPP_WERROR": "OFF",
        "FIXPP_BUILD_OTEL": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-gcc-debug/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-libc++",
      "displayName": "Linux / Clang / libc++ (Tier-2 NFR-017)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "CMAKE_CXX_FLAGS": "-stdlib=libc++",
        "CMAKE_EXE_LINKER_FLAGS": "-stdlib=libc++",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-libc++/conan_toolchain.cmake",
        "FIXPP_LIBCXX_LANE": "ON",
        "FIXPP_BUILD_OTEL": "OFF"
      }
    },

    {
      "name": "linux-clang-libc++-asan",
      "displayName": "Linux / Clang / libc++ / ASan (Tier-3 NFR-017)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang-22",
        "CMAKE_CXX_COMPILER": "clang++-22",
        "CMAKE_CXX_FLAGS": "-stdlib=libc++",
        "CMAKE_EXE_LINKER_FLAGS": "-stdlib=libc++",
        "FIXPP_ENABLE_ASAN": "ON",
        "BUILD_SHARED_LIBS": "ON",
        "FIXPP_BUILD_OTEL": "OFF",
        "FIXPP_LIBCXX_LANE": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-libc++-asan/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-libc++-ubsan",
      "displayName": "Linux / Clang / libc++ / UBSan (Tier-3 NFR-017)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang-22",
        "CMAKE_CXX_COMPILER": "clang++-22",
        "CMAKE_CXX_FLAGS": "-stdlib=libc++",
        "CMAKE_EXE_LINKER_FLAGS": "-stdlib=libc++",
        "FIXPP_ENABLE_UBSAN": "ON",
        "BUILD_SHARED_LIBS": "ON",
        "FIXPP_BUILD_OTEL": "OFF",
        "FIXPP_LIBCXX_LANE": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-libc++-ubsan/conan_toolchain.cmake"
      }
    },

    {
      "name": "linux-clang-libc++-tsan",
      "displayName": "Linux / Clang / libc++ / TSan (Tier-3 NFR-017)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_C_COMPILER": "clang-22",
        "CMAKE_CXX_COMPILER": "clang++-22",
        "CMAKE_CXX_FLAGS": "-stdlib=libc++",
        "CMAKE_EXE_LINKER_FLAGS": "-stdlib=libc++",
        "FIXPP_ENABLE_TSAN": "ON",
        "BUILD_SHARED_LIBS": "ON",
        "FIXPP_BUILD_OTEL": "OFF",
        "FIXPP_LIBCXX_LANE": "ON",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/linux-clang-libc++-tsan/conan_toolchain.cmake"
      }
    },

    {
      "name": "windows-msvc-debug",
      "displayName": "Windows / MSVC / Debug",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/windows-msvc-debug/conan_toolchain.cmake"
      }
    },

    {
      "name": "windows-msvc-release",
      "displayName": "Windows / MSVC / Release",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/windows-msvc-release/conan_toolchain.cmake"
      }
    },

    {
      "name": "windows-msvc-asan",
      "displayName": "Windows / MSVC / ASan (Tier 2)",
      "inherits": "_base",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug",
        "CMAKE_MSVC_RUNTIME_LIBRARY": "MultiThreadedDebug",
        "CMAKE_TOOLCHAIN_FILE": "${sourceDir}/build/windows-msvc-asan/conan_toolchain.cmake",
        "FIXPP_ENABLE_ASAN": "ON"
      }
    }
  ],

  "buildPresets": [
    { "name": "linux-clang-debug",    "configurePreset": "linux-clang-debug" },
    { "name": "linux-clang-release",  "configurePreset": "linux-clang-release" },
    { "name": "linux-clang-asan",     "configurePreset": "linux-clang-asan" },
    { "name": "linux-clang-ubsan",    "configurePreset": "linux-clang-ubsan" },
    { "name": "linux-clang-tsan",     "configurePreset": "linux-clang-tsan" },
    { "name": "linux-clang-coverage", "configurePreset": "linux-clang-coverage" },
    { "name": "linux-gcc-release",    "configurePreset": "linux-gcc-release" },
    { "name": "linux-gcc-debug",      "configurePreset": "linux-gcc-debug" },
    { "name": "linux-clang-libc++",        "configurePreset": "linux-clang-libc++" },
    { "name": "linux-clang-libc++-asan",   "configurePreset": "linux-clang-libc++-asan" },
    { "name": "linux-clang-libc++-ubsan",  "configurePreset": "linux-clang-libc++-ubsan" },
    { "name": "linux-clang-libc++-tsan",   "configurePreset": "linux-clang-libc++-tsan" },
    { "name": "windows-msvc-debug",        "configurePreset": "windows-msvc-debug" },
    { "name": "windows-msvc-release", "configurePreset": "windows-msvc-release" },
    { "name": "windows-msvc-asan",    "configurePreset": "windows-msvc-asan" }
  ],

  "testPresets": [
    {
      "name": "linux-clang-debug",
      "configurePreset": "linux-clang-debug",
      "description": "Debug. jobs=4 — #267 phase 2, MEASURED 2.10x by an A-B-A run on ONE VM (j=1 1142.1 s, j=4 541.5 s, j=1 1135.3 s — the two serial passes agree to 0.6%, so there is no pass-order effect), with a CPU calibration between passes flat and /proc/stat steal 0 throughout. ~597 s = 10 min/run. See ci/ctest-parallelism-probe.md.",
      "output": { "outputOnFailure": true },
      "execution": { "jobs": 4 }
    },
    {
      "name": "linux-clang-release",
      "configurePreset": "linux-clang-release",
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-clang-asan",
      "configurePreset": "linux-clang-asan",
      "description": "ASan. jobs=4 — #267, same-VM A-B-A in run 33977674899: j=1 1252.4 s -> j=4 578.4 s, wall 2.17x, achieved concurrency 3.01x. Peak RSS 2.17 -> 3.32 GiB, 21.3% of a 15.61 GiB runner. ⚠️ THE TWO SERIAL PASSES DISAGREE BY 6.3%, above the 5% tolerance, so this sample would VOID on that alone even after the cleanliness fix — of the five widened lanes this one's speedup is the least trustworthy, and it wants its own re-dispatch before the number is repeated. ⚠️ NOT A CLEAN `MEASURED` FIGURE, and it must not be quoted as one alongside debug/ubsan's. That run's verdict was FORMALLY REJECTED: every pass of every lane failed fixpp::dict::codegen-build-graph-check because the driver wrote its sample into the checkout (since fixed — the driver now refuses an --out inside a work tree). The failure cost 0.10 s and hit all three passes identically, so the WALL TIMES are still same-VM and comparable; what is void is the verdict, not the clock. ⚠️ Wall speedup and achieved concurrency are DIFFERENT numbers and were conflated once — quoting the concurrency as the speedup overstates the gain by about a third. ⚠️ This lane is NOT on any tier's critical path (Tier 2 is bounded by windows-msvc-asan, Tier 3 by linux-clang-libc++-tsan), so on #267's own criterion — tier wall time = its slowest lane — this buys no TIER time. Applied on the user's explicit 2026-09-05 decision to widen the five measured lanes and observe, which is a different and stated basis. See research/G19-fix-fpml-iso20022/decisions/speckit/267-widening-decisions.md.",
      "execution": { "jobs": 4 },
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-clang-ubsan",
      "configurePreset": "linux-clang-ubsan",
      "description": "UBSan. jobs=4 — #267 phase 2, MEASURED 1.79x (1993 -> 1114 s, ~14.6 min/run) by a PAIRED same-VM run, cross-validated on a second VM to 1.9%/4.2%. Peak 0.65 -> 0.86 GiB, 5.5% of a 15.61 GiB runner. ⚠️ Do NOT copy to another lane without ITS OWN paired measurement: between-VM wall-clock variance is 27-43%, so only a paired same-VM run can decide a lane. debug was separately confirmed at 2.10x; tsan's j=2-vs-j=4 question is UNRESOLVED (its comparison was unpaired). See ci/ctest-parallelism-probe.md. || #268 — UBSAN_OPTIONS BELONGS HERE, NOT IN THE WORKFLOW. UBSan's default mode is RECOVERABLE: a finding prints 'runtime error:', execution continues, and the process exits 0 — so this lane ran the whole suite for its entire existence and could not go red on a UBSan finding, while /speckit-verify set halt_on_error=1 BY HAND at every gate. That asymmetry between CI and local IS the defect, so the fix is put where both read it: a testPresets environment reaches ctest and ctest passes it to every test, so CI, a developer's `ctest --preset`, and /speckit-verify all now get the same behaviour from one place — where a workflow `env:` would have fixed CI and left the asymmetry intact. Proven by a paired local A/B on one binary (2026-08-18, clang-22): the same signed-overflow test PASSES with ctest exiting 0 without this environment, and FAILS with it, print_stacktrace=1 yielding a real frame list. ⚠️ A preset environment value OVERRIDES the inherited one, so a hand-set UBSAN_OPTIONS no longer composes with it — add options here rather than around the invocation.",
      "output": { "outputOnFailure": true },
      "execution": { "jobs": 4 },
      "environment": { "UBSAN_OPTIONS": "halt_on_error=1:print_stacktrace=1" }
    },
    {
      "name": "linux-clang-tsan",
      "configurePreset": "linux-clang-tsan",
      "description": "TSan. jobs=2 — a CONSERVATIVE DEFAULT, not a measured optimum. ⚠️ The j=2-vs-j=4 comparison was made across TWO SEPARATE RUNS on a lane whose between-VM wall-clock spread is 43.3% (1903/2160/1376 s at an unchanged j=2), so it resolves nothing and an earlier claim of 'no gain at j=4' is WITHDRAWN. What IS solid: peak RSS reproduces to <2%, so j=4's +36% memory is real, and debug/ubsan both gain ~1.8-2.1x when measured properly. Settle this with the same paired A-B-A design before changing it.",
      "output": { "outputOnFailure": true },
      "execution": { "jobs": 2 }
    },
    {
      "name": "linux-clang-coverage",
      "configurePreset": "linux-clang-coverage",
      "description": "Coverage. jobs=4 — #267, same-VM A-B-A in run 33977674899: j=1 1280.3 s -> j=4 663.8 s, wall 1.93x, achieved concurrency 2.51x — the lowest parallel efficiency of the five, which is expected given per-test profile writing. Peak RSS 0.87 -> 1.06 GiB, 6.8% of a 15.61 GiB runner. The two serial passes agree to 2.5%. ⚠️ THE COVERAGE MECHANISM IS INTACT AT j=4 AND THE COVERAGE CONTENT IS NOT STABLE AT ANY j — two separate facts, measured in run 33977674899. Mechanism: all three passes report status=ok with profraw_count=367, so the per-PID LLVM_PROFILE_FILE scheme (%p, set as a step-level override by tier1's coverage job) loses no profile when four test binaries run at once. Content: the two SERIAL passes produced DIFFERENT merged coverage (17886 vs 17882 lines, different digests) at UNCHANGED concurrency, so this suite has run-to-run coverage nondeterminism of its own and #267 acceptance item 4 CANNOT be attributed to --parallel from this sample in either direction — the baseline moves. The j=4 pass read 17877, i.e. within the same order as the 4-line serial-to-serial gap (~0.05% of 17.9k covered lines). Do not read that as 'parallel loses coverage', and do not read this preset as having established the opposite. The nondeterminism is pre-existing, is NOT caused by this widening, and wants its own issue. ⚠️ NOT A CLEAN `MEASURED` FIGURE, and it must not be quoted as one alongside debug/ubsan's. That run's verdict was FORMALLY REJECTED: every pass of every lane failed fixpp::dict::codegen-build-graph-check because the driver wrote its sample into the checkout (since fixed — the driver now refuses an --out inside a work tree). The failure cost 0.10 s and hit all three passes identically, so the WALL TIMES are still same-VM and comparable; what is void is the verdict, not the clock. ⚠️ Wall speedup and achieved concurrency are DIFFERENT numbers and were conflated once — quoting the concurrency as the speedup overstates the gain by about a third. ⚠️ This lane is NOT on any tier's critical path (Tier 2 is bounded by windows-msvc-asan, Tier 3 by linux-clang-libc++-tsan), so on #267's own criterion — tier wall time = its slowest lane — this buys no TIER time. Applied on the user's explicit 2026-09-05 decision to widen the five measured lanes and observe, which is a different and stated basis. See research/G19-fix-fpml-iso20022/decisions/speckit/267-widening-decisions.md.",
      "execution": { "jobs": 4 },
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-gcc-release",
      "configurePreset": "linux-gcc-release",
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-gcc-debug",
      "configurePreset": "linux-gcc-debug",
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-clang-libc++",
      "configurePreset": "linux-clang-libc++",
      "description": "libc++. jobs=4 — #267, same-VM A-B-A in run 33977674899: j=1 1287.9 s -> j=4 558.5 s, wall 2.31x (the best of the five), achieved concurrency 3.09x. Peak RSS 0.62 -> 0.59 GiB — essentially flat, 3.8% of a 15.61 GiB runner. The two serial passes agree to 1.1%. ⚠️ NOT A CLEAN `MEASURED` FIGURE, and it must not be quoted as one alongside debug/ubsan's. That run's verdict was FORMALLY REJECTED: every pass of every lane failed fixpp::dict::codegen-build-graph-check because the driver wrote its sample into the checkout (since fixed — the driver now refuses an --out inside a work tree). The failure cost 0.10 s and hit all three passes identically, so the WALL TIMES are still same-VM and comparable; what is void is the verdict, not the clock. ⚠️ Wall speedup and achieved concurrency are DIFFERENT numbers and were conflated once — quoting the concurrency as the speedup overstates the gain by about a third. ⚠️ This lane is NOT on any tier's critical path (Tier 2 is bounded by windows-msvc-asan, Tier 3 by linux-clang-libc++-tsan), so on #267's own criterion — tier wall time = its slowest lane — this buys no TIER time. Applied on the user's explicit 2026-09-05 decision to widen the five measured lanes and observe, which is a different and stated basis. See research/G19-fix-fpml-iso20022/decisions/speckit/267-widening-decisions.md.",
      "execution": { "jobs": 4 },
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-clang-libc++-asan",
      "configurePreset": "linux-clang-libc++-asan",
      "description": "libc++ ASan. jobs=4 — #267, same-VM A-B-A in run 33977674899: j=1 1968.1 s -> j=4 881.2 s, wall 2.23x, achieved concurrency 3.29x. ⚠️ THE LARGEST MEMORY RISE OF THE FIVE: peak RSS 2.98 -> 4.32 GiB (+45%), 27.7% of a 15.61 GiB runner. Headroom is ample today, but this is the lane to re-measure first if the runner size ever drops or the suite grows. ⚠️ NOT A CLEAN `MEASURED` FIGURE, and it must not be quoted as one alongside debug/ubsan's. That run's verdict was FORMALLY REJECTED: every pass of every lane failed fixpp::dict::codegen-build-graph-check because the driver wrote its sample into the checkout (since fixed — the driver now refuses an --out inside a work tree). The failure cost 0.10 s and hit all three passes identically, so the WALL TIMES are still same-VM and comparable; what is void is the verdict, not the clock. ⚠️ Wall speedup and achieved concurrency are DIFFERENT numbers and were conflated once — quoting the concurrency as the speedup overstates the gain by about a third. ⚠️ This lane is NOT on any tier's critical path (Tier 2 is bounded by windows-msvc-asan, Tier 3 by linux-clang-libc++-tsan), so on #267's own criterion — tier wall time = its slowest lane — this buys no TIER time. Applied on the user's explicit 2026-09-05 decision to widen the five measured lanes and observe, which is a different and stated basis. See research/G19-fix-fpml-iso20022/decisions/speckit/267-widening-decisions.md.",
      "execution": { "jobs": 4 },
      "output": { "outputOnFailure": true }
    },
    {
      "name": "linux-clang-libc++-ubsan",
      "configurePreset": "linux-clang-libc++-ubsan",
      "description": "#268 — the Tier 3 twin, and it is NOT optional. This lane reproduces the same recoverable-mode gap and the same finding as linux-clang-ubsan: run 32024674144 job 95371515603 recorded 1 report, ran 356/356, ctest green. Fixing one ubsan lane and not the other is the enumeration trap ci/expected-sanitizer-reports.txt's header already paid for once, when an allowlist named linux-clang-asan and missed linux-clang-libc++-asan. See linux-clang-ubsan's description for the full rationale. || #267 — jobs=4, same-VM A-B-A in run 33977674899: j=1 1188.6 s -> j=4 564.2 s, wall 2.11x, achieved concurrency 3.02x. Peak RSS 0.73 -> 0.84 GiB, 5.4% of a 15.61 GiB runner. ⚠️ THE TWO SERIAL PASSES DISAGREE BY 9.8%, the worst of the five and well above the 5% tolerance, so this sample would VOID on that alone even after the cleanliness fix; re-dispatch before repeating the speedup. ⚠️ NOT A CLEAN `MEASURED` FIGURE, and it must not be quoted as one alongside debug/ubsan's. That run's verdict was FORMALLY REJECTED: every pass of every lane failed fixpp::dict::codegen-build-graph-check because the driver wrote its sample into the checkout (since fixed — the driver now refuses an --out inside a work tree). The failure cost 0.10 s and hit all three passes identically, so the WALL TIMES are still same-VM and comparable; what is void is the verdict, not the clock. ⚠️ Wall speedup and achieved concurrency are DIFFERENT numbers and were conflated once — quoting the concurrency as the speedup overstates the gain by about a third. ⚠️ This lane is NOT on any tier's critical path (Tier 2 is bounded by windows-msvc-asan, Tier 3 by linux-clang-libc++-tsan), so on #267's own criterion — tier wall time = its slowest lane — this buys no TIER time. Applied on the user's explicit 2026-09-05 decision to widen the five measured lanes and observe, which is a different and stated basis. See research/G19-fix-fpml-iso20022/decisions/speckit/267-widening-decisions.md.",
      "output": { "outputOnFailure": true },
      "execution": { "jobs": 4 },
      "environment": { "UBSAN_OPTIONS": "halt_on_error=1:print_stacktrace=1" }
    },
    {
      "name": "linux-clang-libc++-tsan",
      "configurePreset": "linux-clang-libc++-tsan",
      "description": "libc++ TSan. jobs=4 — #267, and THE ONLY WIDENING IN THIS SET THAT BUYS TIER TIME. This lane is Tier 3's critical path (87 min against a next-longest 44), and on #267's criterion a tier costs what its SLOWEST lane costs. Same-VM A-B-A in run 33977674899: j=1 4656.1 s -> j=4 2521.6 s -> j=1 4634.8 s, wall 1.84x, achieved concurrency 2.82x — about 35 minutes off the tier's bound. ⚠️ THE BEST-EVIDENCED SAMPLE OF THE SEVEN: the two serial passes agree to 0.5% (tolerance 5%), /proc/stat steal was +0 across all three intervals, calibration drift 0.5%/1.4%, all 357 tests ran in every pass matching ci/expected-eligible-tests.txt, and sanitizer reports were 0 throughout. Peak RSS 0.96 -> 1.93 GiB (+100%), 12.4% of a 15.61 GiB runner. Summed test time +53%, which is a DISCLOSURE and not an objection — runner-minutes is the axis #267 deprioritises. ⚠️ THIS DOES NOT CONTRADICT linux-clang-tsan BEING LEFT AT jobs=2, and the reason is the BASELINE, not the sanitizer: that lane's inconclusive comparison was j=2 -> j=4, this one is j=1 -> j=4. Going from serial to four is a different question from going from two to four, and only the first is answered here. ⚠️ NOT A CLEAN `MEASURED` FIGURE: that run's verdict was FORMALLY REJECTED because every pass of every lane failed fixpp::dict::codegen-build-graph-check — the driver wrote its sample into the checkout (since fixed). The failure cost 0.03-0.71 s and hit all three passes identically, so the wall times stay same-VM and comparable; the verdict is void on comparability, not on the clock. See research/G19-fix-fpml-iso20022/decisions/speckit/267-widening-decisions.md.",
      "execution": { "jobs": 4 },
      "output": { "outputOnFailure": true }
    },
    {
      "name": "windows-msvc-debug",
      "configurePreset": "windows-msvc-debug",
      "output": { "outputOnFailure": true }
    },
    {
      "name": "windows-msvc-release",
      "configurePreset": "windows-msvc-release",
      "output": { "outputOnFailure": true }
    },
    {
      "name": "windows-msvc-asan",
      "configurePreset": "windows-msvc-asan",
      "output": { "outputOnFailure": true }
    }
  ]
}
