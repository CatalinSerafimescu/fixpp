# X.509 v1 config — no extensions section, so `openssl req -x509` emits a v1
# cert (negative fixture for the verify_peer "x509_v1" rejection path).

[ req ]
distinguished_name = req_dn
prompt             = no

[ req_dn ]
CN = fixpp-leaf-x509v1-negative
