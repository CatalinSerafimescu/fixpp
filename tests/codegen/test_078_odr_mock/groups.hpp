#pragma once

// 078 R2a probe — mock DATA-ONLY groups header (mirrors data-model.md
// Entity 1: fixpp/<ns>/groups.hpp). Pure data struct, no validator traits.

namespace test078_mock {

struct GroupArgs {
    int value = 0;
};

} // namespace test078_mock
