# neg_cert_source_file_missing_leaf.toml
# [cert_source] kind="file" but cert_file (leaf_path) is absent — the get_path
# lambda returns {} for missing keys (line 246 selector_resolver.cpp).
# make_file_cert_source then fails because leaf_path is empty (line 261-268).
# This covers both lines 246 and 261-268.

[clock]
kind = "system"

[store]
kind = "memory"

[cert_source]
kind     = "file"
key_file = "leaf.key"
ca_file  = "ca.pem"

[dictionary]
kind = "path"
path = "FIX44.xml"

[[session]]
sender_comp_id = "CLIENT1"
target_comp_id = "SERVER1"
begin_string   = "FIX.4.4"
role           = "initiator"

[session.transport]
kind = "tls"
host = "fix.example.com"
port = 4321

[session.security_profile]
kind = "mtls_ca"
