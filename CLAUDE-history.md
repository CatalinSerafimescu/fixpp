# fixpp — merged-feature history (newest first)

> Moved out of `CLAUDE.md` (2026-07-10) to keep the session-loaded thin pointer thin. This is the narrative changelog; the authoritative per-feature records are `spec/feature-catalogue.md` + `spec/coverage-index.md`, the parent `phases/phase-4/<module>/<feature>.md` lifecycle docs, `spec/behaviors-and-limitations.md` (B-*/L-*), and project memory (`MEMORY.md` index).

- **`081-strict-validation-residuals`** MERGED 2026-07-29 (PR #207, squash `710e658e`; `gate-a-done` + `gate-b-done`, 0 Gate-B waivers). Closes both residuals 079 left open on the **opt-in** strict inbound-validation path (`validate_inbound_messages`, default off = byte-identical no-op). **Concern A / #203 / L-041-2** — the three empty-`<header/>` dictionaries (FIX50/FIX50SP1/FIX50SP2) defer their standard header/trailer to the FIXT.1.1 session layer, so `validate()` Step-1 rejected the very first header tag (`8`). 081 bakes a **validator-private** FIXT framing surface (`fixt_framing_tags_`/`fixt_framing_types_`, `src/dictionary/fixt_framing_table.hpp`, census-pinned to `FIXT11.xml`) consulted only by the validator's Step-1 accept + type-check arms — **accept-only** (the session FSM owns header required-presence; deliberate, recorded named-intent divergence from QuickFIX) and **type-checked** (`34=abc`/`1156=abc` reject). The parser-shared `valid_` store stays byte-identical (D-1), so **zero read-golden change**. **Concern B / #205** — a field `required='Y'` that is a direct member of an **optional** repeating group is no longer enforced per group instance; per-instance required-ness is gated on the enclosing group's own `required=` (QuickFIX `addXMLGroup`, immediate-enclosing, NOT ancestor-AND), collapsing 079's 24 divergent contexts to **0** and **superseding waiver W-204-1**. Threaded through both loaders + the census oracle + the codegen emitter, which now forks group-plan identity by enclosing-required (one vlatest fork: `NoLinesOfText` `G_33Args` → `G_33_1Args`/`G_33_2Args`). **No C-ABI change** (frozen 1.5.0); read/reify goldens byte-identical, only typed **validator** goldens change. Gate A 3 rounds / 2 rewrites (RC1: the original merge would have hit the parser-shared `valid_` store that `field_valid_for` feeds into `unknown_fields()` → redesigned validator-private; RC2: group-plan interning omitted enclosing-usage required-ness → fork plan identity). **Gate B 2 rounds, 0 waivers** — r1 Codex found a real **P1 false-accept**: the framing table maps `Length`-typed tags (9/90/93/212) but `check_field_type()` routed `ft::Length` through the no-op arm, so `90=abc` was accepted once Concern A widened those tags past Step-1; fixed **globally** with a dedicated `ft::Length` numeric arm (also closing the identical latent no-op for message-attached Length fields 95/348/350). **Waiver W-081-1** (user-approved, design-tier): 3 stricter-superset residuals on a different axis (FIX44/50/50SP1 `MassQuote`/`NoQuoteSets`/`NoQuoteEntries(295)` — QuickFIX `DataDictionary.cpp:563` hardcodes `componentRequired=false` for a `<component>` nested directly in a `<group>`); fixpp ⊇ QuickFIX, **no false-accept**, pinned as an exact carve-out (`kKnownSupersetContexts`). **Post-merge-gate CI fix** (`3e413c85`): all 17 matrix jobs failed on one pinned count — `BuilderDedupCount077.VlatestStructCountIs576` saw 577 after the Concern B plan fork; the golden had been regenerated but the numeric pin had not. Root-caused (fork complete by construction — the gated values are signature components; one fork, zero merges in the golden diff) before re-pinning. The gap was a **label-filtered ctest**: the verify selector ran the codegen *determinism* and *build-graph* gates because the emitter changed, but neither counts structs, and `-L codegen` was never in the filter ([[feedback_label_filtered_ctest_missed_changed_subsystem_count_pin]]). **Closes #203; closes #205.** See `phases/phase-4/wire/081-strict-validation-residuals.md` + [[project_081_strict_validation_residuals]].
- **`080-orchestra-runtime-load`** MERGED 2026-07-19 (PR #206, squash `8853d1bd`; `gate-a-done` + `gate-b-waived`, 1 waiver W-206-1; closed out 2026-07-19). Closes the Orchestra **runtime-load entry-point gap** left by 074: `fixpp_dict_load_from_xml` (and the TOML config path) now also accept an Orchestra `<fixr:repository>` root and load FIX Latest, via a new shared `dict::load_any` root-element **sniff-dispatch** helper — a behavioral **contract-widening** of the GA-frozen C-ABI with **no C-ABI change** (frozen `1.5.0`; the frozen `dict.h` prose is retained verbatim, B-080-1/B-080-2). The 074 loader-unit pin (`XmlLoader` rejects `fixr:repository`) stays green: only the C-API/config contract widens, not the loader unit. FR-007/FR-010's dual-dictionary coupling was **descoped at Gate A** (no config surface can name two dictionaries). L-080-1 records the `std::abort` descope. Amends catalogue `CA-011` ("widened by 080") + `coverage-index.md` D-011. Named follow-up **F4** (post-1.0, optional): factor the loaders to accept a pre-parsed `pugi::xml_document` so the sniff and the load share one parse and one root-selection semantic. See `phases/phase-4/dictionary/080-orchestra-runtime-load.md` + [[project_orchestra_runtime_load_entry_point_gap]].
- **`079-required-presence-scope`** (fixpp#201) MERGED 2026-07-19 (PR #204, squash `ea1b6b89`; `gate-a-done` + `gate-b-waived`, 1 waiver W-204-1; 3-tier CI in flight on the merged head). Corrects the message-level required-set **derivation** of the `required fields` validator clause on the opt-in strict `validate_inbound_messages` path — three over/under-require defects: **(1)** a field `required='Y'` only inside an **optional repeating group** no longer leaks into the message-level required set (was a false-reject of conforming messages omitting the group — e.g. FIX44 PositionReport w/o NoUnderlyings; FIX50SP2 TradeCaptureReport; FIX42 Allocation); **(2)** the per-instance group required-member check (`consume_group`, `validator.hpp`) is now **dynamic-width / fail-closed**, replacing the candidate's ≤64 fail-**open** skip; **(3) T020 component-AND** — a field/group `required='Y'` inside an **optional `<component required='N'>`** no longer over-requires at message level (`xml_loader.cpp`/`orchestra_loader.cpp` `component_required` running-AND, mirroring QuickFIX `DataDictionary::isRequiredField`), fixing **4 real sites** (FIX50/FIX50SP1 `AR`/552, FIX50SP1 `AB`/`AC`/555) a Phase-0 enumeration had wrongly declared vacuous. Proven by a **non-circular census** (independent raw-XML component-AND oracle ≡ `table_view::required_fields()` ≡ codegen IR `collect_top_fields`, exact-set both directions, all 10 dicts, 2 RED witnesses) cross-checked against **quickfix-cpp 1.16.0** (624-message required-set golden, 9 QuickFIX dicts) + a two-tier runtime/typed verdict-agreement test. **No C-ABI change** (frozen 1.5.0); read/reify goldens byte-identical (FR-006/008; `FieldRef.rule` untouched). Gate A 3 rounds / 2 rewrites; **Gate B 2 Sonnet rounds, no Codex fixer** — r1 caught a real latent false-reject (the component-AND fix gated the message-level set but not the per-group store → `consume_group` false-rejected a valid FIX50SP2 NoMDStatistics instance omitting a member required only inside an optional component within the group; fixed group-relative, accumulator reset at group entry, in both loaders + oracle, 2 mutation-RED tests). **Waiver W-204-1** (user-signed-off): 079's NEW per-group *required-once-present* enforcement is stricter than QuickFIX at 24/29,247 group contexts (all fixpp superset — no false-accept) → WAIVE + follow-up **#205**. **Deferred (tracked):** L-041-2/**#203** (FIX50SPx empty `<header/>` blocks a standalone full-frame `validate()` on tag 8 — 079 scopes FIX50SP2 correctness to the derivation/census tier); L-066-1/**#196** (FIX40/41/42 `INT`-typed group counts carve-out). **Closes fixpp#201.** See `phases/phase-4/wire/079-required-presence-scope.md` + [[project_201_required_presence_scope_candidate]].
- **`078-precompiled-builder-libs`** MERGED 2026-07-18 (PR #200, squash `6d487283`; `gate-a-done` + `gate-b-done`, 0 waivers; full 3-tier CI green). Restructures 077's monolithic header-only `fixpp/<ns>/Builders.hpp` into **precompiled per-version STATIC libraries** — `fixpp::{builders,validators}::{v44,v50sp2,vlatest}` — + a slim declaration surface: per-plan `groups/<Plan>.hpp` + `all.hpp` (replaces `Builders.hpp`, FR-008) + per-message header-only inline mode (`FIXPP_BUILDERS_HEADER_ONLY[_<Msg>]`). Consumer opt-in is **purely link-time** (link builders/validators/both/neither); both libs always built; disjoint per-message `.o` (SC-002/003, nm-witnessed on Linux; ODR traits COMDAT-folded). **Zero core change** (`src/`/`capi/`/`bindings/` untouched; C-ABI frozen 1.5.0); wire byte-identical / validator result-identical (SC-004); determinism byte-stable over the regenerated ~3,291-file golden set. **⚠️ SC-001 amended** (L-078-1): value-semantics `Args` makes single-message compile RSS **closure-bounded**, NOT universally order-of-magnitude below the monolith (v44 ~17× MET; v50sp2/vlatest ~2.6–7.9×) — forward-declared-`Args` (Option 3) deferred. **Constitution v0.10** (annotation-only, Article I §1 + XVIII §7). Gate A 3 rounds; **Gate B 7 rounds, 0 waivers** — 1 real P1 (install-scope coherence: the inherited 061 install rule shipped the unexported slim layout → 5 component excludes), the long tail was doc-drift from the feature's several mid-impl amendments (inline-XOR-link IFNDR, #197-retained, SC-001-restatement, vt11-absence, per-plan-`groups/` layout, stale `Builders.hpp` names) — converged via a comprehensive spec-vs-delivered audit + a mechanical name-sweep. /speckit-verify (clang-debug + gcc-release heavy-ON + MSVC, 16 GiB cap) caught+fixed **2 full-build breaks masked by narrow-target builds** (`emit_builders`→`std::vector<EmittedFile>` API-drift in `test_067_emit_builders_unit`; a `test_067_builder_validate` builder-lib mislink). CI green-up: tier timeout **90→180** (078's ~800 per-message-split TUs cold-compile past 90 min on first run — cold-cache wall-time, not OOM; local proved 16 GiB fit); MSVC `nm`-witnesses gated off (Itanium `nm -C` can't demangle MSVC COFF; SC-002/003 witnessed on Linux + MSVC link/roundtrip). **Named follow-ups:** `install(EXPORT)` for an installed external consumer; **#197 heavy-test stopgap deletion (T034)** — RETAINED, not blind-deleted; ccache-size tuning + a dumpbin-based MSVC `nm`-witness. **Closes #198; fixes #199.** See `phases/phase-4/wire/078-precompiled-builder-libs.md` + [[project_078_precompiled_builder_libs]].
- **`077-builder-args-dedup`** MERGED 2026-07-17 (PR #197, squash `df04e7df`; `gate-a-done` + `gate-b-done`, 0 waivers). Re-enables the typed `build_<Msg>`/`validate_<Msg>` **builder tier for all app-bearing versions** — `fixpp::{v44, v50sp2, vlatest}` — via a **component-identity `Args`-dedup**: each repeating group's `Args` keyed by `(no_tag, recursive structural signature)`, emitted once into `fixpp::<ns>::groups` (`G_<no_tag>Args` / ordinaled `G_<no_tag>_1..kArgs`). Collapses 076's uncompilable 53,590-struct / 137 MB `vlatest/Builders.hpp` (>21 GB RSS) to **576 shared plans / ~78 MB** (~3.7 GiB RSS/TU). Per-version: v44 88/83, v50sp2 558/156, vlatest 576/173; vt11 admin-only → none. **⚠️ v42 DESCOPED** (issue #196 / L-077-1: FIX 4.2 `NumInGroup=INT` ⇒ 0 typed groups). Codegen-tool + CMake + tests + specs only; zero `src/`/`capi/`/`python` (C-ABI frozen 1.5.0). **Constitution v0.9** (Article I §1 + XVIII §7). Gate A 3 rounds; Gate B 2 Sonnet rounds, 0 waivers. /speckit-verify caught+fixed a crash-corrupt 1-byte `v50sp2` golden + a host-OOM gtest footgun (`EXPECT_EQ` on ~75 MB headers → unbounded diff → bounded byte-compare). Post-Gate-B CI green-up: `/bigobj` (MSVC C1128 on the completeness TUs) + a Ninja job pool + **lean config-gating** (`FIXPP_BUILD_HEAVY_BUILDER_TESTS` ON only for linux-clang-debug/asan + windows-msvc-release) so the heavy builder-test TUs (~3.7-4.6 GiB RSS; ~2× under gcc) fit the 16 GB hosted runners — the linux reds were reproducible **OOM** (exit-143 "runner shutdown", no compile error), NOT infra flake. **Follow-up #198** (precompiled per-version builder/validator libs — supersedes this CI stopgap; slim header + inline mode + per-message; do BEFORE #196). See `phases/phase-4/wire/077-builder-args-dedup.md` + [[project_077_builder_args_dedup]].
- **`076-fix-latest-typed-codegen`** MERGED 2026-07-16 (PR #195, squash `535f80d6`; `gate-a-done` + `gate-b-waived`, 1 waiver W-1). Typed **read/reify/args/validator codegen tier for all 181 FIX Latest (EP303) messages** under `fixpp::vlatest`, fed by 074's `OrchestraLoader`, behind opt-in `FIXPP_CODEGEN_FIX_LATEST` (default ON). Additive — legacy `v42/v44/v50sp2/vt11` byte-identical ON/OFF. **DOMINANT change = Orchestra-native IR projection** (`populate_orchestra_projection`/`walk_orchestra_level` in `ir.cpp` + root-element-sniff loader dispatch): the `<fix>`-hardcoded `populate_group_order` NO-OPS on the Orchestra `<fixr:repository>` root, so typed classes do NOT come "for free" ([[feedback_codegen_typed_classes_not_free_second_reparse_noops_on_new_schema]]). Non-circular completeness (no QuickFIX peer): **V-1** census (independent raw-XML walker N-1 ≡ emitted manifest, mutation-RED ×4) ∘ **V-1b** (manifest ≡ shipped read classes, mutation-RED ×2) ⇒ `class ≡ raw-XML`. New `emit_manifest.cpp`. **⚠️ DESCOPE (user-decided 2026-07-16 at /implement):** the typed `build_<Msg>` **builder** tier is DEFERRED to a follow-up — the shared per-message non-deduplicated `emit_builders` produces a **137 MB / 53,590-struct uncompilable `vlatest/Builders.hpp`** (>21 GB RSS); `emit_builders` reverted to v44-only (v44 golden byte-identical, additive guarantee intact). Descopes FR-001(b)/FR-007(a)/V-2/V-2b (waived in the completeness audit). Zero `src/`/`capi/`/`python` change (C-ABI frozen 1.5.0). **Constitution v0.8** (Article I §1 both loci + Article XVIII §2). Gate B converged **4 Codex rounds** (all Sonnet fixer; P1 r1 = `FIXPP_CODEGEN_FIX_LATEST=OFF` compiled deleted vlatest headers → gate the vlatest test targets on the option): the long tail was **stale-doc-bundle drift** from the mid-impl descope → converged via authoritative-surface fixes + one **top-of-file supersession banner** per Gate-A-era design record + **W-1 waiver**, NOT per-line enumeration (which relocated the P2 each round — [[feedback_descope_stale_doc_drift_converge_by_banner_not_enumeration]]). Also closed a real vlatest run-to-run determinism gap the verify record had over-claimed (`AC-T1` was legacy-only → new `VlatestByteIdenticalAcrossRuns`); reconciled a `category→is_application` spec↔code disagreement by fixing the SPEC (`Session` = EP303's sole admin category, verified; binary rule is correct + forward-compatible). Golden = `vlatest_Messages.golden.hpp` only (003 precedent). Full sanitizer+MSVC matrix GREEN in CI (deferred from local verify — WSL2 OOM'd 5× the session). See `phases/phase-4/wire/076-fix-latest-typed-codegen.md` + [[project_076_fix_latest_typed_codegen]].
- **`075-live-wire-enum-validation`** MERGED 2026-07-15 (PR #193, squash `8fc9e68a`; `gate-a-done` + `gate-b-done`, 0 waivers). Makes `table_view::enum_valid()` **real** — a store-driven check of a field's wire value against its dictionary-declared `<value enum=…>` code set, live across **all ten** dictionaries; retires the Phase-1 `enum_valid`→`true` stub (L-041-1) and fires the dead `SessionRejectReason=5` enum arm. `XmlLoader` now parses codesets into 074's additive enum store (was EMPTY for the 9 legacy dicts). Multi-value fields tokenized on a single space (empty tokens NOT skipped, byte-for-byte QuickFIX). **FR-006:** `Validator::validate()` threads the offending tag out (required `ref_tag_out`, T020a) → validate-driven `Reject(35=3)` carries `RefTagID(371)`. Two distinct accept-floors: empty **code set** ⇒ accept (keeps FIXT11), empty field **value** ⇒ bypass enum check. Rides existing `validate_inbound_messages` flag (default false, byte-identical). **T044 (out-of-bundle):** the parity golden exposed + fixed a pre-existing 100%-inbound-rejection bug on FIX40/41 (`CHAR`-typed `BeginString`/`CheckSum`) via QuickFIX's version-conditional `CHAR`→`String` rule. Zero C-ABI change. **Constitution v0.7** (Article I §1). Parity DERIVED from a 13-row golden generated by real QuickFIX v1.16.0 (manifest + content hashes; links no QuickFIX in CI). Gate B converged 2 rounds (Sonnet 1/2, 0 waivers): one P1 = validator hot-path perf change with no benchmark (Art VIII §3) → fixed bench-only (`bench/wire/validator_bench.cpp`); the surfaced perf finding (validate() 489–986 ns pre-existing walk + 075's +16–33% enum delta) is accept+scheduled as v1.0 perf row 15c, NOT a blocker. Post-convergence CI fix `eb0bf541` = `-text` on the golden-SHA1'd files (`FIX44/41/42.xml`, `main.cpp`) — Windows CRLF broke `enum_golden_manifest_test`; local rsync-MSVC sandbox false-greened it ([[feedback_local_msvc_rsync_sandbox_false_green_on_crlf_hash]]). See `phases/phase-4/wire/075-live-wire-enum-validation.md` + [[project_075_live_wire_enum_validation]].
- **`074-orchestra-native-reader`** MERGED 2026-07-14 (PR #192, squash `5fbef34b`; `gate-a-done` + `gate-b-done`, 0 waivers). Native FIX Orchestra (`fixr:repository`) reader → **FIX Latest (EP303)** at the read/dictionary tier: `dict::OrchestraLoader` (pugixml, sibling to `XmlLoader`) → runtime `Dictionary` under new `session_version::vlatest` (wire app-version = existing `v50sp2`, no distinct ApplVerID; NO `application_version::vlatest`, NO `render_appl_ver_id` change). 181 messages; depth-7 + reused-tag group resolution via `as_table_view`. **Additive tag-keyed enum side-table** on `dict_metadata_handle` + read-only `Dictionary::enum_values(tag)` for codeset values+descriptions (the handle had none — `enum_valid` stays the Phase-1 stub; `FieldRef` byte-identical). **FR-010** fail-loud registry guard (release-effective abort when a FIX50SP2 + a FIX Latest dict collide on the shared `v50sp2` slot; L-074-1, interim — ApplExtID(1156)=303 re-keying deferred). Fail-closed `orchestra_parse_error : xml_parse_error` (catch-discriminated, 072 precedent); duplicate structural-id guards at all 4 collect sites (Gate B P2 + codeset census). Zero C-ABI change; `XmlLoader` + nine QuickFIX dicts untouched. **Constitution v0.6** (Article I §1 — first FIX version-set widening, read-tier-scoped). Gate B converged 2 rounds (Sonnet 1/2, 0 waivers); post-convergence CI fix `a7a6376a` = `dictionaries/orchestra/OrchestraFIXLatest.xml -text` (Windows CRLF broke the byte-exact `file(SHA1)` supply-chain gate — [[feedback_byte_exact_sha1_gate_needs_gitattributes_text]]). See `phases/phase-4/dictionary/074-orchestra-native-reader.md` + [[project_074_orchestra_native_reader]].
- **`072-nested-group-hardening`** MERGED 2026-07-13 (PR #190, squash `033e8d46`; `gate-a-done` + `gate-b-done`, 0 waivers). Bundles #180 (two structural census pins + a load-time reject at `XmlLoader::load` for a nested group whose delimiter == its parent's — new `dict::group_delimiter_collision_error` deriving `xml_parse_error`, reuses inherited `code()`, catch-type discriminated, NO `core::error` append) + #183 (typed depth-3 pushed membership context — the emitter view-mint pushes the nested `no_tag` onto the returned child view's stored context, reconciled UP to 065's C-ABI cursor; + a nesting-aware recursive validator Step-3 rewrite `consume_group`/`validate_group_level`, FR-010 split-trigger did NOT fire). Zero C-ABI change (frozen 1.5.0; no `capi/`/`core/error.hpp`/`test_020`). Gate B converged 3 rounds (Sonnet 2/2): r1 L-063-3 doc overclaim narrowed (residual (b) global-delimiter REMAINS — shipped-reachable e.g. FIX44 NoMDEntries(268) 269-vs-279, pre-existing/main-parity, opt-in-strict-validator-only — pinned by `PerContextDelimiterResidual_L063_3b_SkipPending`); r2 real bug — `validate_group_level` level-scanner/consumer double-duty over-ran, false-rejecting a valid multi-instance parent with a nested group ([[feedback_recursive_walk_double_duty_scanner_consumer_overruns]]). Post-Gate-B CI-fix: `codegen_determinism_test` hung on stale golden — research "no checked-in golden" premise was WRONG; regenerated v44+v50sp2 read goldens ([[feedback_codegen_golden_exists_narrow_verify_misses_it]]). B&L L-062-3/L-063-4 pinned+guarded, L-065-1/L-063-3(a) fixed. See `phases/phase-4/wire/072-nested-group-hardening.md` + [[project_072_nested_group_hardening]].
- **`069-v44-all-families`** MERGED 2026-07-12 (PR #187, squash `d1eac827`; `gate-a-done` + `gate-b-waived`, 1 P3 test-hygiene waiver). Widens the 067 writer-emitter from 33 OFFICIAL → all **83** in-scope v44 `msgcat='app'` messages (85 − {BE,BF}; 33 + 50 new), write-side + verification only (read/reify already universal). `CoverageMode {All,Official}` predicate + CMake `FIXPP_CODEGEN_V44_FAMILIES=all|official` (default all, CACHE STRING); IR `is_application` (fail-closed on missing `msgcat`). Verified: differential round-trip over all 83 vs the runtime-XML `as_table_view` path (0 skips, set-equality) + 8 QuickFIX goldens + non-circular raw-`FIX44.xml` completeness census (83). 33 OFFICIAL byte-identical. No runtime/C-ABI/Python/link-ABI change (FR-012; C-ABI frozen 1.5.0). Constitution **v0.5** (Article XVIII §7 amendment). Gate B: F1 stale-codegen regen guard FIXED (content-fingerprint + stale-tool→bootstrap-rebuild reroute, [[feedback_forced_recompute_reuses_stale_producer_hides_staleness]]); post-loop P2 fixed (witness `RUN_SERIAL`, [[feedback_tree_mutating_test_must_run_serial_vs_gitclean_gate]]); L-069-1 (unbacked enum-domain). See `phases/phase-4/wire/069-v44-all-families.md` + [[project_v44_all_families_crank_before_orchestra]].
- **`068-test-binary-grouping`** MERGED 2026-07-11 (PR #186, squash `ef9ca2bd`; `gate-a-waived` + `gate-b-done`; full `push:main` 3-tier + ABI-golden green). Whole-binary `add_test` grouping across all 23 test modules (FR-001 amended from the initially-spiked `gtest_discover_tests`, which regressed serial ctest 5.77×) — test-infra only (no src/ABI/runtime change), 454→292 test binaries (~15GB reclaimed across 6 presets); **3085 gtest cases preserved exactly** (0 dropped, verified clean before/after set-diff). 8-preset Linux + MSVC verified. Constitution **v0.4** (Article VII §8 — author tests grouped, select by `ctest -L`, never exe name). See `specs/068-test-binary-grouping/{dispositions,measurements}.md` + [[project_test_binary_grouping_disk_win]].
- **`067-codegen-writer-emitter`** MERGED 2026-07-10 (PR #185, squash `84833c8d`; gate-a-done + gate-b-done). FR-015a-lite codegen writer-emitter: `build_<Msg>` + `validate_<Msg>` for all 33 OFFICIAL FIX44 MsgTypes over `wire::body_builder`; v44 representative ns. No runtime/C-ABI/Python change (FR-009). L-067-1/2/3.
- **`065-cabi-nested-group-membership`** — PR #182 (squash `0e3589e5`), 2026-07-10, gate-a-done + gate-b-done, 0 waivers; full 3-tier CI green. **Fixes #179 / L-063-2** (RESOLVED): the C-ABI **nested** repeating-group read (`fixpp_group_get_nested_group`) used a hand-rolled membership-free **positional** scanner that closed the LAST nested instance at end-of-outer-entry-slice, absorbing a trailing outer-group member → `OK`+wrong-value instead of `TAG_NOT_FOUND` (silent wrong value on the GA-frozen 1.5.0 C-ABI; 222 FIX44 / ~20k FIX50SP2 layouts). Fix = delete the scanner, delegate to the membership-aware `OffsetTable::nested_group_slices` (062) + `consume_group_extent` (063); three non-C-ABI seams (internal `fixpp_group::group_ctx`; public out-of-line `OffsetTable::group_context_for`; `nested_group_slices` 4-arg overload); C-ABI 1.5.0 freeze byte-identical. Built on merged 066 (dict-backed dispatch prerequisite). Gate B converged r1 (0 fixer commits, 0 waivers). Depth-1 scope; deferred: **L-065-1/#183** (typed depth-≥2 unpushed-context gap — emitter regen), **L-065-2/#184** (nested-read arena-exhaustion silent-truncation, parity with the shipped typed path — fail-loud). See `phases/phase-4/c-api/065-cabi-nested-group-membership.md` + [[project_065_cabi_nested_group_membership]] / [[feedback_delegation_to_arena_primitive_shifts_stack_read_into_fixed_arena]].
- **`066-dict-backed-inbound-parse`** — PR #181 (squash `4e3be8e5`), 2026-07-09, gate-a-done + gate-b-done. The **shipped** inbound receive parse (`Session::parse_and_dispatch_`) built its `wire::Parser` **dict-free**, so no inbound-dispatched `MessageView` (C-ABI or C++ typed) carried group membership — the context-scoped membership 062/063 delivered was never consulted on a real dispatched message (only on the unit-tier `Parser<Index>{dict}` those features' tests used). The real root cause of #179, broader than #179 stated. Fix: `inbound_tv_` built once at `open()`, parse with `Parser{*inbound_tv_}`, membership propagated into `fixpp_msg_clone`/`dict::reify` (FR-007) via a new internal `MessageView::membership_copy()` (owned `table_view`, no public/codegen change). Strict in-group membership (QuickFIX/J-aligned, permissive→strict, B-066-1); scoped to the 6 group-registering dicts (FIX40/41/42 group-blind, L-066-1). Does NOT fix the C-ABI **nested** positional last-instance defect (L-063-2 / #179 / feature 065). Gate B converged (0 waivers). Post-Gate-B CI fixes in the squash (native-MSVC + local-libc++): OOM-witness libstdc++ gate (`2a78f31e`), MSVC-debug `_Container_proxy` grouped-read skip (`9d67b028`), and a **real production fix** — `OffsetTable::group_slices()` reserve tightened to the group-instance bound so a large inbound group can't silently read empty when MSVC's STL metadata exhausts the fixed 16 KiB null-upstream parse arena (`cc169700`). Amends W-006/W-007/CA-010 + coverage-index §3.2. See `phases/phase-4/session/066-dict-backed-inbound-parse.md` + [[project_066_dict_backed_inbound_parse]] / [[feedback_fixed_arena_over_reserve_silent_loss_larger_stl]].
- **`061-typed-app-messages` (061-slim)** — PR #178 (squash `7f7124d3`), 2026-07-08, gate-a-done + gate-b-done. Typed application-message **write shape-oracle**: 5 hand-written exemplar builders (NewOrderSingle 35=D, ExecutionReport 35=8, OrderCancelReject 35=9, NewOrderList 35=E [3-level nested], AllocationReport 35=AS — all `fixpp::v44`) on a NEW `wire::body_builder` primitive (body-only FIX serializer: LIFO group API, reallocation-safe index-path handles, zero-global-heap fixed arena, count-precedence serialization, INV-2/3/4/5 at `commit()`) + FR-008 header install + external-consumer install-witness; each exemplar byte-anchored to a QuickFIX-authored golden + dict-aware round-trip/read witness — the prerequisite for the FR-015a codegen writer-emitter, NOT its implementation. Gate B 4 rounds (Sonnet 2/2 + Codex 1 fix + 1 re-review, 0 waivers; 5 mutation-proven RC fixes: MessageView move-only, body_builder handle-lifecycle owner+innermost-open guards, grouped empty-required guards, msg_type framing-injection validation). Post-convergence CI-surfaced fixes in the squash (`fef351e1`, test/harness/CI only): consumer-witness build-type + portable-MSVC-link + `.exe`; MSVC-debug alloc-guard `_ITERATOR_DEBUG_LEVEL` skips; tier1 ccache 500M→2G + timeout 45→60. B-061-1..4 / L-061-1..2. See [[project_061_typed_app_messages]].
- **`063-nested-group-parse-correctness`** — PR #176 (squash `ed8d2494`), 2026-07-08, gate-a-done + gate-b-done. Prereq for 061 + 062-real-dict. **Defect A** (loader membership collision, first-seen-wins global `no_tag` key) → **exact context-scoped membership** keyed `(msg_type, bounded parent-no_tag-path, no_tag)`, built in `dictionary.cpp::as_table_view()`, carried on `OffsetTable` as `group_context` (Gate A Option A; Option B union rejected as unsound for the byte-slicer). **Defect B** (non-nesting-aware `OffsetTable::group()` extent) → alloc-free depth-capped (K=16) nesting-aware `consume_group_extent` walk. FR-005 held: 062 slicing/caching algorithm + codegen accessors untouched; C-ABI byte-identical. Gate A converged 3 rounds (dual-store amendment `0caafd23`; L-063-3 validator-delimiter fix). Gate B converged 2 rounds (Sonnet 2/2): **RC#1** = exact-context membership seeded only lazily on the typed path → C-ABI/`group_slices()` top-level reads ran under empty `{msg_type=""}` context (Defect-A reintroduced) + `no_tag`-keyed cache-poisoned the typed read → seed root `group_context` in the dict-aware `MessageView` ctor (`a5e0b310`, 2 mutation-proven tests); Codex #2 flat-splitter downgraded to L-063-4 (real-dict-unreachable). Post-convergence CI-surfaced fix (`b9648eac`): GCC `-Wchanges-meaning` on member `entry_context::group_context` (clang-silent, gcc-release CI-only) → renamed `group_ctx`, verified locally under gcc-13. Retires L-062-1/2; adds L-063-1 (FIX40/41/42 INT-typed counts), L-063-2 (C-ABI nested positional), L-063-3, L-063-4. See `phases/phase-4/wire/063-nested-group-parse-correctness.md` + [[project_061_typed_app_messages]] / [[feedback_shared_context_seeded_lazily_one_path_leaves_siblings]] / [[feedback_member_named_same_as_type_gcc_changes_meaning]].
- **`169-toplevel-group-metadata-trap` (W-1 / issue #169)** — PR #175 (squash `d3cb1d49`), 2026-07-07, gate-a-waived + gate-b-waived. Non–Spec-Kit close-out of the 062 W-1 deferral: arms the debug generation trap on the top-level group-metadata reads left unguarded by #168 (which armed only the new `nested_group_slices` path). `OffsetTable::group()/group_slices()/find()` + `group_view::size()/operator[]/end()` each `check_alive()` (debug-only retained `gen_` token, alloc-free zero-length liveness check mirroring `nested_group_slices`); `#ifndef NDEBUG` → zero release/ABI change; frozen top-level read surface (062 FR-007) preserved. Gate B converged in 2 rounds (Sonnet 1/2): **RC#1** = census-missed `find()`, the shared metadata-locate primitive (absent-tag stale-read reachable via C-ABI `fixpp_msg_get_group` / `MessageView::get` / `field_bytes`) → one-line entry guard closes all three (`7e8ed546`); **RC#2** = source-correct but untested held-`group_view` guard B → 3 discriminating, mutation-proven death tests (`b5949795`). P3 `group()`-entry-pin waived (transitive via `group_slices()`). Full debug ctest 557/557; all 4 new `EXPECT_DEATH` mutation-proven. Fix delegated to Codex (`gpt-5.5` investigate/review, `gpt-5.4` fix) under Opus orchestration. See `phases/phase-4/wire/169-toplevel-group-metadata-trap.md`.
- **`064-fix4041-legacy-types` (A-5 / D-004)** — PR #172 (squash `da84d81b`), 2026-07-06, gate-a-done + gate-b-waived. Loads the last two runtime-XML versions (FIX 4.0/4.1), closing `[const §I.1]`'s all-nine commitment. Production change = **two** `kFieldTypeTable` collapse rows in `src/dictionary/xml_loader.cpp`: `TIME → UtcTimestamp` (agrees with QuickFIX `XMLTypeToType`) and `DATE → LocalMktDate` (deliberate stronger-typing over QuickFIX `TYPE::Unknown`; metadata-only — both collapse to `field_type::String`, so no message QuickFIX accepts is newly rejected). `field_data_type` enum unchanged; AC-L8 relaxed by exactly two global aliases; zero public/C-ABI/wire/error/layout surface. Vendored `dictionaries/FIX40.xml`/`FIX41.xml` (quickfix @ `19ef6a4c`, MIT). Gate B 1 fix-round (Sonnet `f170f91b`): pinned the metadata-only `LocalMktDate → field_type::String` invariant with a direct `table_view_test` regression assert. B-064-1/L-064-1 (QuickFIX `DATE` divergence, metadata-only). See [[project_a5_dictionary_xmls_d004_loader_gap]].
- **D-005/006 dictionary XMLs** — PR #171 (squash `1c9e44eb`), 2026-07-05, gate-a-waived + gate-b-done. Non-Spec-Kit data+tests closure: vendored `dictionaries/FIX43.xml`, `FIX50.xml`, `FIX50SP1.xml` (verbatim from quickfix/quickfix at the UPSTREAM.txt-pinned SHA) + 3 new headline `VersionParam` rows in `tests/dictionary/lookup_test.cpp` (suite `FourShippedVersions`→`AllRuntimeVersions`, 7 params). FIX 5.0/5.0SP1 are app-layer only (session→FIXT.1.1). Gate B 1 round (Codex P2: README refresh recipe listed FIX40/41 contradicting the D-004 deferral → scoped to the 7 bundled files, `05ded926`). **D-004 (FIX 4.0/4.1) NOT shipped** — loader `[FIX50SP2 §3.3]` vocabulary fail-closes on their legacy `DATE`/`TIME` types → own Gate-A'd loader feature (→ 064). See [[project_a5_dictionary_xmls_d004_loader_gap]].
- **TC-018 zero-count repeating group** — PR #170 (squash `3ce30162`), 2026-07-05, gate-a-waived + gate-b-done. Witness-only conformance closure (QuickFIX AT 21, `RepeatingGroupSpecifierWithValueOfZero`): a `NoXXX=0` present-but-empty group is accepted, not rejected. Zero production change. Added `ZeroCountGroupAccepted` (validator, 4 wire versions, mutation-proven RED on deleting the short-circuit) + `ZeroCountGroupPreservedByteIdentical` (writer round-trip) + B&L `B-004-7`. See [[feedback_planning_explore_existence_claims_unreliable]].
- **`062-grouped-typed-read-fix`** — PR #168 (squash `9d5ac6cb`), 2026-07-05, gate-a-done + gate-b-waived. Prereq for 061: fixes the verified compile blocker where typed reads of repeating-group ENTRIES didn't compile (`group_view::operator[]` span-ctor vs generated `G_<n>` `MessageView`-ctor). HYBRID mechanism: entry carries `entry_context` (span + mr + dict + group_member_fn + generation_token + ROOT cache owner); one-level scalar reads span-scan the slice (zero-alloc, FR-004a); nested descent builds a dict-aware sub-`OffsetTable` over slice-scoped `data,len+1`, cached once in a ROOT-owned flat cache keyed by `(slice_data ptr, nested_no_tag)`. Gate B 2 rounds (RC#1 warm nested-cache-hit generation-trap bypass `7bb3509e`; RC#2 emitter-comment `285f5b9d`) + Tier-2 MSVC-debug-STL alloc-injection test skip (`99a18c53`). W-1 top-level group-metadata stale-read → **RESOLVED** by #175. **L-062**: single-entry nested proven; multi-entry nested + real-dict correctness → prereq **063**. See [[project_061_typed_app_messages]].
- **`store-memory-reset-guard` (S-P2-2)** — PR #167 (squash `128fcd82`), 2026-07-05, gate-a-waived + gate-b-done. Non-Spec-Kit bug-fix batch (Cluster-3 residual): `MemoryStore::retrieve()` mirrors FileStore's `generation_` reset-during-traversal guard (fail-closed `store_io_failure` on a mid-walk `reset()`, both policies) + a reused member `retrieve_scratch_` (no per-call monotonic-arena burn). Gate B 2 rounds. No public/C-ABI/wire/error/layout change. B-008-7. See [[project_next_task_sp2_2_memorystore_guard]].
- **`060-int128-decimal-compare`** — PR #165 (squash `122c95b6`), 2026-07-04, gate-a-done + gate-b-done. Cluster-2 residual **C1**: exact wide-integer cross-exponent decimal compare (`k≥19` dominance + `mul_u64_wide`), default-path swap, bit-identical `strong_ordering`, `decimal.hpp` byte-identical; reverses the 2a Gate-A `no-__int128` decision. MSVC `_umul128` lane Tier-2-only (L-060-1). See [[project_060_int128_decimal_compare]].
- **`059-outbound-store-fail-closed`** — PR #163 (squash `b48bcfd`), 2026-07-04, gate-a-done + gate-b-waived. Cluster-3 flagship (S-P1-1/S-P2-1): outbound frame-store failure now **fatal-when-persistent** + best-effort outbound-only reconcile-from-durable. Gate B 3 rounds. See [[project_059_outbound_store_fail_closed]] / [[feedback_mirror_existing_failclosed_disposition]] / [[feedback_classify_disposition_by_provenance_not_error_value]].
- **`058-async-mutex-hardening`** — PR #162 (squash `edad623`), 2026-07-03, gate-a/b-done. Cluster-4 hardening: 7 async_mutex defects + T-1..T-7 test-validity gaps; header-only. Gate-B Codex also found+fixed a pre-existing 048 acquisition livelock. Post-merge: portable `set_terminate` death-test marker (libstdc++-only regex on Tier-2/3). See [[project_async_mutex_cluster4_hardening]] / [[feedback_death_test_terminate_banner_not_portable]].
- **`057-behavioral-reify-unblock`** — PR #161 (squash `5a7a944`), 2026-07-02, gate-a/b-done. Live `dict::reify()`/`reify_as<Msg>()` typed owning handles via a build-tree dispatch-bridge; lifts R6 / L-003-1; NO type-erasure (T059-deferred); zero new surface. See [[project_057_behavioral_reify_unblock]].
- **`056-python-wheel-packaging`** (PY-005) — PR #158 (`e9d3763`), 2026-07-01. One `cp310-abi3` wheel + Tier-1 `python-wheel` gate; closes the PY-001..005 arc. [[project_056_python_wheel_packaging_gatea]].
- **#160 `cabi-1.5-ga-freeze`** — PR #160 (`61edae6`). C-ABI MAJOR 0→1, MINOR preserved → **`1.5.0`** (the `0→1` GA freeze). [[project_cabi_ga_freeze_1_5_0]] / [[feedback_ga_major_bump_preserve_minor_downgrade_frame]].
- **#159 `python-bindings-hardening`** — PR #159 (`723da02`). 4th `python-bindings` UBSan Tier-1 leg (closes L-054-2) + `msg_get_string` non-UTF-8 decode witness.
- **Earlier:** 053–055 (Python PY-001..004) · 049–052 (C-ABI Features A–C + Python-readiness) · 046 (libc++ atomic_shared_ptr) · 035–045 (Fable F-f tail + config/transport) · 001–034 (core/dictionary/wire/session/transport/log/otel foundations). See `phases/phase-4.md` + `spec/feature-catalogue.md`; older lifecycle close-outs in `MEMORY-archive.md`.
