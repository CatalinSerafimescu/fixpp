// SPDX-License-Identifier: AGPL-3.0-or-later
// tools/codegen/fixpp-codegen/emit_messages.cpp
//
// 003-dictionary-codegen — T024 — <vXX>/Messages.hpp typed flyweights incl. price(mr) v1.4 decimal accessor (data-model Entity 1; AC-G1..G12).
// Foundational-checkpoint SCAFFOLDING STUB: returns "" (no artifact emitted)
// so the host tool builds and links. The full deterministic emitter body is
// implemented by the named US1+ task; until then main.cpp writes no file for
// this artifact (write_file() skips empty content).
#include "emit.hpp"

namespace fixpp::codegen {

std::string emit_messages(VersionIR const& ir) {
    (void)ir;
    return {};  // TODO(T024 — <vXX>/Messages.hpp typed flyweights incl. price(mr) v1.4 decimal accessor (data-model Entity 1; AC-G1..G12)): emit per the contract oracle + data-model entity
}

}  // namespace fixpp::codegen
