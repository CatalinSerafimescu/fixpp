// SPDX-License-Identifier: AGPL-3.0-or-later
// tools/codegen/fixpp-codegen/emit_reify.cpp
//
// 003-dictionary-codegen — T032 — <vXX>/Reify.hpp owning_<Msg> + owning_message_traits<Msg> specialisation (data-model Entity 4; AC-G7a/AC-R2).
// Foundational-checkpoint SCAFFOLDING STUB: returns "" (no artifact emitted)
// so the host tool builds and links. The full deterministic emitter body is
// implemented by the named US1+ task; until then main.cpp writes no file for
// this artifact (write_file() skips empty content).
#include "emit.hpp"

namespace fixpp::codegen {

std::string emit_reify(VersionIR const& ir) {
    (void)ir;
    return {};  // TODO(T032 — <vXX>/Reify.hpp owning_<Msg> + owning_message_traits<Msg> specialisation (data-model Entity 4; AC-G7a/AC-R2)): emit per the contract oracle + data-model entity
}

}  // namespace fixpp::codegen
