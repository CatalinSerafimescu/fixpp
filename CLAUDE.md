<!-- SPECKIT START -->
**Active Spec-Kit feature**: `033-fixt-fix50sp2-session` — **G4a, implement-ready** (pre-implement pipeline complete: Gate A rd 3 signed-off → tasks T001–T036 → analyze [2 HIGH remediated: FR-010 witness T035, §VII.7 fuzz-seed T036] → checklist [establishment/security/interop, 50 items] → checklist-audit GREEN 50/50 PASS, 0 SPEC-FIXED; next `/speckit-implement`). FIXT.1.1 / FIX 5.0 SP2 session establishment: `8=FIXT.1.1` begin-string split, `DefaultApplVerID(1137)` negotiation, optional `Username(553)`/`Password(554)` Logon auth, un-defer the `HP-fixt11-fix50sp2-cells` interop axis (5.0SP2 + a FIXT-carrying-4.4 family, both roles × QFcpp/QFJ). Flips catalogue S-020 FIXT-half + S-025 + S-022; S-026 (per-message `ApplVerID(1128)`) stays deferred (tolerate-only). Builds on the already-FIXT-aware dict/version layer (`version_profile`/`version_registry`/`reify`); work is in the session establishment path. Clarify (2026-06-12) ratified: any-ApplVerID general design, missing-1137→`Reject(35=3)` RequiredTagMissing, creds parse+surface only (validation a future config-gated feature, FR-008a).

Last merged: `032-initiator-reset-outbound-advance` — **MERGED 2026-06-11** (PR #115 squash `9417454`, gate-a-done + gate-b-done; amends catalogue **S-017** + **S-032**). Conformance fix for the live-found **L-024-2**: a reset-knob INITIATOR emits `Logon(141=Y,34=1)` (outbound→2), then on the peer's `141=Y` echo at the `peer_ack_sent_reset_flag` arm rebases outbound 2→1 (the missing OUTBOUND twin of `030`'s inbound restore) → duplicate `34=1` → QFcpp/QFJ reject. Fix = latch the emit-time fact `own_logon_sent_reset_flag_` (unconditional assign at the shared `emit_initiator_logon_` point), restore outbound to `2` iff `latch && reset_before_send` (new `SeqnumManager::set_next_outbound` public-on-internal-class + `Session::persist_outbound_advance_` private, fatal-when-persistent), `we_initiated = latch` alone (C1 restore / C4 label are DISTINCT gates, diverge on `bilateral_strict`-at-N). Gate B: 2 rounds, NO production defect (all 3 findings doc-accuracy). **L-024-2 → RESOLVED + B-032-1.** **T021/SC-003 live close-out DONE (2026-06-12):** the 2 `RL-*-init` cells now pass live both engines (QFcpp + QFJ), both golden layers banked; manifest flipped to `live`/`pass`, schema-check 9/9. Predecessors `031-acceptor-789-resend-boundary` MERGED 2026-06-11 (PR #114 squash `7477fe2`, amends S-031) + `030-received-reset-inbound-advance` MERGED 2026-06-10 (PR #113 squash `3c5ee65`). NEXT (per `../REMAINING-WORK.md` Tier-1 Item 1): 025 `StandbyRehydrate` demo cell (harness, non-SC, net-new FileStore plumbing) · optional direct-mismatch injector cells · C-103 → G4. Per [[project_031_acceptor_789_resend_boundary_merged]] / [[project_live_quickfix_hostile_scenarios]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
