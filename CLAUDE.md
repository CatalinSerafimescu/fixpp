<!-- SPECKIT START -->
**Active feature:** _none — between features._ `010-session-cfg-lifetime` shipped at library/main `25cf09d` (PR #83, squash-merged 2026-05-23, `gate-a-done` + `gate-b-waived`). The next feature is TBD; `/speckit-specify <slug>` will scaffold it and refresh this pointer.

Recently merged (`main` history): [`010-session-cfg-lifetime`](specs/010-session-cfg-lifetime/plan.md) PR #83 (squash `25cf09d`) — closed the 7 residual PR #82 Gate B waivers (W-5 by-value `SessionConfig cfg_;` + F-04 LogonReceived observability + F-05 admin-distinct-now + F-06 49-cell FSM matrix witness + F-07/E1 dedicated `error::session_invalid_state_for_send` + F-11 initiator transport-throw + RC#G admin-emit mixed-path) plus in-slice F4 (`is_session_admin` dropped `"A"`/`"2"`/`"4"` per Codex + QuickFIX-cpp + QuickFIX/J 2nd-opinion survey converging on Position A) and /simplify Wave 1-5 (13th `[[feedback_simplify_pass_catches_9th_burn]]` instance — 4 P1 + 5 P2 + 4 P3 binding-contract drifts). Gate B converged 2 rounds (Sonnet 1/2, Codex fixer 0/2 NOT consumed); 1 P3 waiver (F-16 no >256-transition test, arithmetically correct by construction) + 4 inherited 009 W-1..W-4 Codecov DA/BRDA carry-forwards per `[[feedback_codecov_patch_vs_lcov_da_brda_gate]]` + PR #73 precedent.

Earlier merged: [`009-session-fsm-finalize`](specs/009-session-fsm-finalize/plan.md) PR #82 (squash `ba2222d`) — drift closure of 005 across PR #81 (paused) → PR #82 (merged). Gate A inherited from 005; Gate B converged 4 rounds (Sonnet 2/2 + Codex 1/2). Earlier: [`008-message-store`](specs/008-message-store/plan.md) PR #77+#78, [`007-threading-clock`](specs/007-threading-clock/plan.md) PR #74+#75, [`006-async-mutex`](specs/006-async-mutex/plan.md) PR #73, [`004-wire-codec`](specs/004-wire-codec/plan.md) PR #68, [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) PR #67, [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) PR #66, [`001-core-decimal`](specs/001-core-decimal/plan.md) Gate A/B converged 2026-05-12/13. [`005-session-establishment-fsm`](specs/005-session-establishment-fsm/plan.md) shipped via the bundled PR #82 with 009.

**Next-feature candidates** (deferred work surfaced during 010):
- **Per-release interop gate** — run fixpp against QuickFIX-cpp + Fix8 as both initiator and acceptor + sweep their issue trackers and re-run fixpp against their historical thorny bugs. 005 FSM (Logon/Heartbeat/Resend/Reset/Logout) is now fully shipped — the prior blocker is removed. See `[[project_release_interop_quickfix_fix8]]`.
- **Deferred session-recovery feature** — will upgrade 010's F4 closure cells `"2"` (ResendRequest) and `"4"` (SequenceReset) from Reject → Process (gap-fill via message store), matching QuickFIX-cpp `Session::nextResendRequest` / `Session::nextSequenceReset` and QuickFIX/J counterparts. The dup-Logon cell stays as Reject per 005's intentional defensive divergence. Catalogue row 400 in `library/spec/coverage-index.md`; 4 traceability markers (source TODO + test TODO + ledger + verify-record).
<!-- SPECKIT END -->
