<!-- SPECKIT START -->
**Last merged FEATURE: `088-firstframe-budget-timer-lifetime`** (PR #239, squash `27922ec0`) — **closes #233** (and **#236**). Two production defects on the acceptor pre-session path, both in `read_first_frame_bounded`, neither a regression: (1) the byte budget rejected at **equality** (`>=` where FR-014 says *exceeds*) **and** rejected **before** `framer.feed`, so a chunk already holding a complete first frame was discarded — a valid Logon coalesced to exactly 4096 B was dropped. The two wrongs compose, so the fix is **atomic**: frame-first-then-budget (D-1) with the carry buffer sized `max_bytes + 1` (D-1a). (2) the deadline timer handler could **outlive the coroutine frame** it captured (heap-use-after-free, reproduced under ASan) and its `transport.cancel()` also tore down sessions that had just **established** — replaced by an `operator||` join retiring both arms (D-2) plus a **shared** `timer_epoch_state` counter block (D-4.1) guarding all three transport sites. Gate A round 2 caught that the join itself **never completed under `Engine::stop()` on TLS** — an *unbounded hang on the default accept path*, strictly worse than the defect it replaced — closed by a transport-side two-argument `reset_cancellation_state` OUT map with a **real-TLS** witness (a mock cannot substitute: `mock_transport`'s read is a `steady_timer`, which honours `total` exactly where a real `ssl::stream` hangs). Gate A converged **`P1=0 P2=0`** after 4 rounds + 4 amendments (**every headline defect found by the Opus judge or an orchestrator measurement, none by Codex**); `/speckit-verify` **GREEN** (32 PASS / 0 FAIL / 2 waived / 5 N/A, 50/50 tasks, **13 witness cells each RED against its named mutant and each RED shown to *terminate***, 100 % coverage on the core file); Gate B converged in **2 rounds with ZERO Gate B waivers**. **Two waivers carried from verify**, both judged sound at Gate B: **W-1** the MSVC leg was not run — the cost stated plainly rather than as *"CI covers it"*, because **all three tier matrices skip until both gate labels land**, so this merged with *zero* MSVC signal; **W-2** the epoch guard's suppression half has no witness by construction (D-6.8's deliberately-empty `guard omitted` column, shipped as **L-088-2**). Rebased twice (onto 087's `60dc56f0`, then 238's `9c1b6226`); both times all 29 files stayed byte-identical, so no measured anchor moved. **Post-rebase CI was cancelled by operator decision** — the identical content was 30/30 green pre-rebase and the only intervening change touched CI workflow files. See `phases/phase-4/session/088-firstframe-budget-timer-lifetime.md`.

**Previously: `087-system-include-binding`** (PR #235, squash `60dc56f0`) — **closes #234**. Binds `INTERFACE_SYSTEM_INCLUDE_DIRECTORIES` at the installed-package consumer: the **fourth** usage requirement `$<LINK_ONLY:>` withholds, and the one 086 recorded against itself as a knowing scope limit (its C-3). Non-trivial because there is **no documented collected consumer property**, so a fourth leg written like the other three would be empty *by construction rather than by measurement* — an assertion that cannot fail, which is why 086 declined to write it. Settled by measuring the **real** consumer against a **real** staged install on **both** platforms before any artifact prescribed a mechanism: `fixpp::capi` → **1** root, `fixpp::service` → **2**, `fixpp::fixpp` → **7**, identical on Linux/clang and MSVC. Compiler built-ins never appear, so exact **set equality** is cheap to state; the **1-vs-7 contrast is the discrimination**. Every decisive Gate A catch was **anti-vacuity — the defect class the feature exists to remove, found four times inside its own gate** (a comparator deletable while everything stayed green; a token vocabulary with no assigning algorithm; a carrier rule jointly unsatisfiable with its own demonstrations; a named fault cause carried nowhere). Gate A instance 1 **EXHAUSTED**; a Codex-fixer escalation then a fresh instance converged. Gate B round 5, **0 waivers**, and **no P1 was ever raised against the implementation** — the loop found a chain of *evidence-record* defects instead, whose recurring mechanism (a fix correcting some sites of a claim and missing another, three rounds running, because the fixer paraphrased instead of copying canonical wording and the verification grep could not see the site it missed) is the lesson worth carrying. **CI 29/29 — the first-ever full matrix run**, discharging the gcc, libc++ and MSVC-comparator residuals. Built entirely in `~/Work/Programming/fixpp-parallel`. See `phases/phase-4/c-api/087-system-include-binding.md`. *(Changelog entry backfilled 2026-08-07 during 088's close-out — 087's own close-out wrote none; reconstructed from its complete lifecycle doc + all three decision records, which remain authoritative.)*

**Previously: `086-capi-include-isolation`** (PR #230, **merge commit** `0839d71e` — not a squash; parents `7caba872` + `773a5231`) — **closes #218**. `fixpp::capi`'s *installed* interface is now narrowed to `include/capi` and `fixpp::service` to `include/service-iface`, both **additively**, so no include spelling changes and a bare `-I<prefix>/include` still resolves. #218's proposed `INTERFACE_INCLUDE_DIRECTORIES = include/fix/` was **not implementable** — every C-ABI header carries `fix/` in its own spelling, so that root resolves `<fix/c_api.h>` to `include/fix/fix/c_api.h`. Gate A round 3; Gate B **GREEN at round 6, 12 P1, 0 waivers**, plus **a 7th round after sign-off forced by CI** (`try_compile` cannot resolve Conan's imported-target closure → all three negative probes failed for a non-isolation reason on every MSVC Debug run; under the pre-r1 probe polarity that would have been a *silent false green* there). Follow-up **#234** carried, not closed. See `phases/phase-4/c-api/086-capi-include-isolation.md`. Full merged-feature changelog: [`CLAUDE-history.md`](./CLAUDE-history.md) (features only — non-feature merges are deliberately not listed there).

**`main` has moved past it — several non-feature merges since, listed below.** None is a Spec-Kit feature, so none appears in `CLAUDE-history.md`. ⚠️ **No count is written here on purpose** — the previous wording said "two" and was already stale when #247 landed; derive it from the list, or from `git log --oneline <088-tag>..main`.
**AFTER 088:**
- **PR #251** (squash `a4925126`) — CI correctness, **closes #243**. The three `python-bindings` sanitizer legs ran TSan/ASan/UBSan against an **UNINSTRUMENTED dependency closure**: the library was instrumented, the deps were not — ~5 h/run reporting green while structurally unable to see a whole layer. All four legs ran `conan install -pr conan/profiles/linux-clang-debug`; each now uses its own profile, and the GHCR restore moves with it (`conan_cache_key` hashes the profile FILE, so leaving the restore behind would have handed back the packages the profile no longer asks for and `--build=missing` would have rebuilt the closure from source **every run** instead of failing visibly). The workflow comment asserting the opposite — *"the sanitizer is a CMake-level flag (`FIXPP_ENABLE_{ASAN,TSAN}`), not a Conan setting"* — is **true of the library and false of the dependencies**, which is why nobody looked. **Acceptance measured on run `31311577828`, 36/36 checks green**: all four legs `conan-cache HIT` on the profile-matched tag (`linux-clang-{debug-364354fed040bdd2,asan-bb202b79d135804e,ubsan-9bbe8bd17fc82594,tsan-a78eea0dbd49aa08}`) — predicted from the branch *before* merge and matched against the live GHCR listing — so the restore is a HIT rather than a from-source rebuild and **zero bytes** were added to the package; the install line shows `-pr conan/profiles/linux-clang-tsan`; **zero new sanitizer reports**. ⚠️ That last result is **bounded, not a clean bill**: the C dependencies are still uninstrumented and a sanitizer reports only what an exercised path hits. ⚠️ **The PR's own headline claim was REFUTED by the review**: *"13 of 16 host-context packages change id per profile"* conflated **package-id-differs** with **is-instrumented** — `tools.build:cxxflags` sits in `tools.info.package_id:confs`, so the id moves even for a package compiling no C++ at all. Measured on the archives: `libcrypto.a` **50,830 symbols, 0 `__tsan_*`**, same for zlib/curl/civetweb, because the profiles set no `CFLAGS`. Filed as **#252** — pre-existing, and **equal on the Tier 1 matrix legs**, so fixing it there would invalidate six jobs' seeded Conan caches with no PR-side reseed lever. Gate A waived (CI-only, precedent #227/#245/#247/#250); **Gate B converged in 3 rounds (Sonnet 2/2, Codex 1/2), P1=0, 3 P2 waivers**, each filed as a tracked issue rather than left in prose (**#252**, **#248** the `run:`-block truth table on the *location* rule, **#253** the pin's name-keyed step lookup). **The blocker CI could never have surfaced**: `tier1-required`'s `needs:` omitted all three python jobs, so the four legs this PR changes could fail while the required check went green — in a PR that explicitly *expects* new sanitizer findings on those legs. The loop's lesson repeats #247's and #250's — **the PR's substance never moved**; the subject went one level away from the product each round (r1 the PR, r2 the pin's coverage, r3 the pin's self-description), and **two of nine root causes were introduced by the fix for the round before**: r2's exact-set census made r1's suffix rule unreachable while the script still named a mutant as its witness, and r3's C1/C2 split left the summary counting five mutants where six ran — the second caught by orchestrator verification *after* the fixer reported done, not by a review round. Also lands **`ci/test-tier1-python-policy.sh`** — six mutants, each proven RED for its own stated reason, with `cmp`-based no-op guards so a mutation that fails to apply cannot read as a pass — wired into `ci-script-pins`. See `decisions/speckit/pr251-python-sanitizer-profiles-gateb.md` and `research/reviews/{codex,opus}_pr251_*` (7 artifacts, in the parent repo).
- **PR #250** (squash `14792b07`) — CI caching, **#240**; **closes #249**; explicitly **does NOT close #248**. Moves Tier 3's four `linux-clang-libc++*` ccaches off the repo-wide 10 GB Actions pool onto GHCR, replacing `hendrikmuhs/ccache-action` with `ci/{ccache-cache-key,restore-ccache,seed-ccache,ccache-stats,prune-ccache}.sh` — the ccache siblings of the sccache offload proven on Tier 2. **Tier 1 and Tier 2 are untouched**: no re-seed, no invalidation. Pool arithmetic **measured** from `actions/cache/usage`, not projected: 8.49 GiB live over 15 caches → 7.61 GiB once `cache-cleanup`'s keep-newest sweep converges → **5.82 GiB** after tier3's `1,932,445,241 B` (1.80 GiB) moves, leaving ~4.2 GiB — the room #241's coverage cache is budgeted against. ⚠️ **That 1.80 GiB is NOT reclaimed by the move.** Keep-newest only deletes what something *superseded*, so a key nothing writes any more has an **immortal** newest entry and the sweep protects it forever — a one-time `gh cache delete` is required or the reduction silently does not happen. Recorded at the sweep itself, because the trap belongs to the rule. **Post-merge follow-up (2026-08-09): the four keys are gone, and no sweep run deleted one** — every `cache-cleanup` run was read; the only one that deleted anything swept 7 entries, all `ccache-tier1-*`. They went to **GitHub's own over-cap LRU eviction** (the pool peaked ≈14 GB while a superseded Tier 1 set was still resident). A 7-day access reaper is ruled out by dating Tier 3's previous `push:main` at 14:37 the same afternoon. **This does not weaken the rule — it is a worse mechanism that happened to land well**: eviction only fires once you are *already* over cap and selects by recency rather than by retirement, so the same event could have taken a live lane. Pool now **6.04 GiB over 9 caches** against the 5.82 GiB projection (delta = a regenerated `conan-wheel` cache). Second surprise: **`fixpp-ccache` was created `public`**, so the "a package first created by `GITHUB_TOKEN` is PRIVATE" claim is **wrong as stated for this repo** — corrected to the observation, since nothing measured *why*. **Cold-run acceptance, measured on the merge run (all four legs identical):** `MISS` (the package does not exist until the first `push:main` seeds it — `oras push` is what creates it), **1478 cacheable calls at 0 %**, so liveness is proven and the launcher is wired; **648–883 MiB on disk, `cleanups_this_run=0`, 34–46 % of the 2G cap**. That **retires the circular `~460 MB` figure**: it measured what the old 500M cap *allowed* and was then cited as evidence the cap sufficed, when true demand is **1.4–1.9× larger** — which is the thrash mechanism behind Tier 3's 7–27 % hit rate, now measured rather than inferred. **✅ Warm acceptance MET on `workflow_dispatch` run `31301539507` (2026-08-09) — #240 CLOSED.** All four legs `restore HIT` **and** **100 % over 1478 cacheable calls**, `cleanups_this_run=0`, so the 2G cap does not bind; **Tier 3 wall-clock 404 min → 174 min (−57 %)**. Both runs took a `conan-cache` HIT on the identical key, so the delta is ccache's and not Conan's. ⚠️ **TSan's 82 min is test time, not a weak cache** — its rate is the same 100 %; quoting a per-lane speed-up as a cache figure would repeat #231's observational-AC2 error. The seed step **correctly skipped** on all four (`changed == 0`), which is the withhold half of the publish predicate executing for the first time — the cold run had only ever proved the seed half. It also first-exercised `restore-ccache.sh` against a **populated** package (pull → staged extract → same-volume swap → `--zero-stats`), previously covered only by the local harness. ⚠️ Read the acceptance as the **pair** — a `HIT` with 0 % exits 0 by design, so neither half alone is evidence. The tag is coarse and **rolling** (`ccache-<preset,'+'→'x'>-clang<major>-<sha8 of the compiler's self-reported identity>`) so a PR can pull what main published — which makes the `github.ref` seed guard *more* load-bearing than the Conan one, not less, since every branch computes the **identical** tag whatever it changed. `prune-sccache.sh` collapses into a generic `prune-compiler-cache.sh` that requires the tag regex be anchored at **both** ends: the old `startswith()` form was safe on Tier 2 only *by accident of naming*, and `linux-clang-libcxx` **is** a prefix of `linux-clang-libcxx-asan`, so a prefix match run for the plain lane would have classified all three sanitizer lanes' **live** caches as reclaimable. Verified safe for Tier 2 by measurement first — all three live `fixpp-sccache` tags match the end-anchored form. Also new: **`ci-script-pins`**, an ungated ~2.5 s job running both `ci/test-*.sh` harnesses (47 + conan assertions) and wired into `tier1-required` — the first and only **pre-merge** signal these scripts have ever had, since the matrices skip until both gate labels land. Gate A waived (CI-only, precedent #227/#245/#247); **Gate B converged in 2 rounds, 1 waiver** (F5, integer overflow — unreachable below a corrupted stats body, and the equivalent was declined twice on #247 as H2). ⚠️ **The reviewer's only P1 was refuted by MEASUREMENT, not argument**: it claimed a permission-denied GHCR DELETE returns 404 and is swallowed as a racing leg's deletion, but that endpoint returns **403** for a scope failure — verified against a live-API 403 recorded in this very diff, the repo's probe procedure (which *depends* on `403 ≠ 404`), and a CI run where every refusal landed in the `REFUSED` branch; the branch was also **pre-existing on base**. Recorded as a disagreement, not a waiver. The loop's lesson repeats #247's exactly — **the PR's substance never moved** (the storage design, tag grammar, install ordering, ref guard and cap decision survived both hostile passes untouched) and every finding was in the **instrument and its prose**; three of round 1's six defects were in work the PR had already declared *verified*, including a **hardcoded** test fixture that would have let a tag-grammar change leave every assertion green while the pruner silently matched nothing, and an assertion that **could never go RED**. Root cause worth carrying: **`cache_miss == 0` does NOT prove ccache wrote nothing** — reproduced on 4.9.1, a same-line-count comment edit yields `direct_cache_miss=1, preprocessed_cache_hit=1, cache_miss=0` with a real manifest write, so the publish gate now reads `local_storage_write`. See `decisions/speckit/pr250-tier3-ccache-ghcr-gateb.md` and `research/reviews/{codex,opus}_pr250_*` (4 artifacts, in the parent repo).
- **PR #247** (squash `1ea7ebe4`) — CI caching, **#244 part 1**; #244 stays open for part 2. One thesis: *no CI job writes into a compiler-cache namespace it does not own, and the leg that only reads gets to read.* `python-bindings (none)` built in `build/linux-clang-debug-py`; the `-py` suffix prevented no collision (separate runners) but defeated ccache twice — ~56 % of compile commands carry an absolute `…/build/<dir>/_codegen/include` path, and `hash_dir` (default true under `-g`) folds the CWD in besides — so the leg recompiled ~1457 TUs from cold on **every** `push:main`, 89–102 min, since `python_touched` is unconditional off a PR. Drop the suffix, restore the matrix debug leg's cache **`save: false`**. **Measured on run 31256807981: `ccache-restore HIT … 759 MiB`, 1461 hits / 1 miss, 99 % over 1462 cacheable calls, job wall 7m31s** — the single miss is the SWIG wrapper edge, the exact delta the local 1457/1457 command comparison predicted. Zero bytes added to the 10 GB Actions pool. Also folded in: `\.github/workflows/tier1\.yml$` added to `python_touched`'s `PY_RE`, because the python jobs' **build recipe sat outside their own relevance filter** — a `tier1.yml`-only PR left every python job SKIPPED, so a change to *how they build* had no PR-side signal and was first exercised post-merge; and a `github.ref == 'refs/heads/main'` guard on tier2's **sccache** save gate, the last dispatch-gated compiler-cache publisher without one (worse than the Conan equivalent #245 fixed, because the sccache tag carries **no content hash** — every branch computes the identical tag). Gate A waived (CI-only, precedent #227/#245); **Gate B converged at round 5, 1 waiver** (F1 → **#248**, no harness for workflow-embedded shell). ⚠️ **Read the acceptance as a PAIR — `restore HIT` AND a high hit rate.** A HIT with **0 %** exits 0 by design (the only failure path is zero-cacheable-calls) and is the thesis *failing*; **#244 part 2 must not close on "CI was green"**. Three things the gate caught that CI could not: Actions runs a `run:` block as a **bash SCRIPT FILE** (`bash -e {0}`), where an arithmetic-expansion error is **non-fatal** — the step exits **0** printing no disposition at all, while `bash -c` on the same input exits 1, so a probe exercised that way shows a false RED and hides the defect; `hendrikmuhs/ccache-action@v1.2.23` **swallows Actions-cache service errors** (`dist/restore/index.js` — non-5xx warns, 5xx `core.error`s, then reports "No cache found"), so a failed download is indistinguishable from an empty cache at the probe; and **`uncacheable_call` does not exist** in ccache 4.9.1, so round 1's prescription would have written a counter that never fires. The loop's own lesson: **the PR's substance never moved** — the build-dir rename, `save: false`, the `-D` launcher, the `PY_RE` widening and the ref-guard survived all five hostile passes untouched, and every one of the 13 root causes was in the **instrument and its prose**, in two classes (*a path that exits without an attributed disposition*; *a statement asserting a cause the step did not measure*) that recurred because each fix was scoped to the line the review cited. Round 4 replaced the grep with an enumeration and round 5 proved **that census false three ways** — frozen before the fix it was accepting, hand-drawn (its one written-down exclusion is contradicted by the diff hunk header), and file-scoped, so the PR body was never walked. Rule recorded: a completeness claim is admissible only when its population is **machine-derived from the diff** at the head being accepted, **re-derived after the last fix commit**, and spanning **every artifact carrying the claim**. Also spun off **#249** — #245's regression pin replays captured output indented, and the runner strips leading whitespace before parsing `::` commands, so it re-emits **five fake `conan-cache` annotations** on every `linux-gcc-release` run, camouflaging the very diagnostic #222 exists for. See `decisions/speckit/pr247-python-bindings-none-ccache-gateb.md` and `research/reviews/{codex,opus}_pr247_*` (12 artifacts, in the parent repo).
- **PR #245** (squash `5e881f84`) — CI diagnostics, **closes #222**. `conan_cache_key` hashes **three** inputs (`conanfile.py` + the profile file + on MSVC the toolset), but the MISS annotation blamed only the toolset and prescribed a remediation that is a **no-op for the common case**: a branch touching `conan/profiles/**` cannot match main's republished key and needs no action — it self-heals on the first `push:main`. It was also **MSVC-only** while the profile file is hashed everywhere, so one commit missed on all fourteen lanes and two said so above `echo` level. Now per-input sha8 digests are logged on **hit and miss** (the diagnostic *is* a diff between the two), the annotation fires on every lane, and tier1/tier3's Conan save gates accept `workflow_dispatch` so "dispatch on main to reseed" is true everywhere. Also fixes the **GHCR prune** (follow-up to #231): the DELETE status was discarded and every failure printed a hardcoded guess; `tee /dev/stderr` **fails on the windows runner**, losing every per-version line; and `prune-conan-cache.sh` still carried the silent-empty `2>/dev/null | jq` shape under which a **failed LIST and a clean package both print "no stale tags"** — which is how a scope-less token went unnoticed for a week. **Gate B: 5 rounds, 0 waivers, P1 = 0 from round 3**; Gate A waived (CI-only, precedent PR #227). Two threads ran the whole loop and are the lesson: **a PR written to stop CI emitting confident, wrong diagnostics kept being caught shipping its own** (four of them — including one *added by round 1's fix* that contradicted the bullet above it, one blaming a published artifact for what may be a full disk, and the silent-empty shape surviving **inside its own fix**, since `jq -s 'add // []'` maps empty, `null` and `{}` all onto `[]`); and **a regression pin that could not fail** — rounds 2–5 each found another surviving mutant, the final harness killing all seven (`cleanup-trap`, `wrong-tag`, `wrong-image`, `bogus-conan-opt`, `no-annotation`, `profile-hardcode`, and `origin/main`'s pre-fix script). The conan cache key is **byte-for-byte unchanged**, re-verified tag-for-tag against `origin/main` after every round — a moved key misses every lane in all three tiers at once. ⚠️ **#229 is NOT closed**: the peak-memory instrument landed but its probe **FAILED on the runner** — `/sys/fs/cgroup/memory.peak` is unreadable there — so criterion 4 stays UNMET and `ctest --parallel` must not widen. The step emitted its loud `NOT MEASURED` warning rather than a silent nothing or a fake number, which is exactly the case it was designed for. See `research/reviews/{codex,opus}_pr245_*` (10 artifacts, in the parent repo).
- **`6b3164e1` + `d8a1ac33`** (direct to `main`) — the GHCR prune, finally working: `prune: deleted 4` on [run 31247987579], reclaiming the exact version id that had logged `REFUSED` the run before. **The cause was never the token.** Under Git Bash on the windows runner, MSYS rewrites a leading-slash argument into a Windows path, so `/user/packages/...` reached `gh` as `C:/Program Files/Git/user/packages/...` and every DELETE was rejected *before leaving the runner*. It read as a permissions problem for two rounds of fixes because the conversion is **shape-dependent**: the LIST endpoint carries `?per_page=100` and is *not* converted, so the prune listed the package correctly, classified the right versions, tried to delete them, and failed — indistinguishable from a scope-less token, and #245's predecessor then printed a hardcoded `token lacks delete:packages` that confirmed the wrong theory. **What found it was #245's instrumentation**: printing the API response instead of a guess made the runner say `invalid API endpoint: "C:/Program Files/Git/..."` verbatim. Same MSYS behaviour `winpath()` in `ci/conan-cache-key.sh` had documented all along (*"sometimes mangles such arguments and sometimes does not; it depends on the argument's shape"*) — one file away, never applied to the prune scripts. Fixed by dropping the leading slash from **all four** endpoints (both prune scripts, LIST and DELETE), not just the one observed failing, since relying on a query string to suppress the conversion is the fragile shape the bug is made of. Second commit: a **404 on DELETE is success, not a refusal** — the three tier2 legs run in parallel over the same pool of untagged versions and race to reclaim them, so the losers get 404; counting that as PENDING would have made every successful cleanup cry wolf on 2 of 3 legs. Found by dry-running against the live package before enabling deletion for the first time. Package is now **3 tags / 0 untagged**, the steady state it had never once reached.

[run 31247987579]: https://github.com/CatalinSerafimescu/fixpp/actions/runs/31247987579

**AFTER 086:**
- **PR #232** (squash `21f1cb67`) — tests-only, **closes #228**. `engine_firstframe_test.cpp` asserted a **proxy**: its raw-TCP probes are rejected at Step 2 (the 1500 ms `tls_handshake_timeout`) and their 2 s self-deadline sits *below* the 5 s `kFirstFrameDeadline`, so a Step-3 close would have FAILED the test — both Step-3 legs were **unobservable**, and `read_first_frame_bounded` had **zero** coverage from this binary. Three post-handshake mTLS witnesses added, asserting an **elapsed band** (a bare "closed" is true on every rejection leg, so it discriminates nothing). Mutation-proven across **8 cells**, each mutant killing exactly one witness — including a **two-byte** budget widening (`4096 → 4098`) and `continue → co_return` in the Step-3 reject arm, which before this PR left *every* test green. Gate A **waived** (precedent PR #226/#164), Gate B converged in 2 rounds, **0 waivers**; spun off **#233** (two live production defects in `read_first_frame_bounded`). Records: `.specify/decisions/228-firstframe-bounded-window-witness-{verify,gateb}.md`. Net **faster** despite the added 5 s wait — `run_until()` slicing replaced `ioc.run_for(cap)`, which never returned early: 6 tests / 10.1 s vs 3 / 12.2 s.

**BETWEEN 085 and 086:**
- **PR #226** (squash `2419f9bb`) — tests-only, **closes #221**. Three hostile-frame tests covering the two `offset_table.cpp` guards Article IX §1 would not let 085 waive: the W-P2-1a Length/Data overrun guard and the `group_slices_reserve_bound()` clamp. Coverage 38→32 uncovered, newly covered exactly `[275,276,277,278,647,648]`, zero regressions; three mutations run. Gate A **waived** (precedent PR #164/#166), Gate B done in 3 rounds. Records: `.specify/decisions/221-offset-table-guard-coverage-{verify,gateb}.md`.
- **PR #227** (squash `80ccb782`) — `ctest` parallelism probe, `execution.jobs: 2` on the **`linux-clang-tsan` preset only**, plus `RUN_SERIAL` on `log_file_fsync`. **Measured −43.8% on that lane** (3319 s → 1867 s mean, three runs). Gate A waived, Gate B done in 3 rounds (round 1 **BLOCKed** on a real P1). **Do not copy `execution.jobs` to other lanes**: acceptance criterion 4 (peak RSS / cgroup `memory.peak`) is still unmet — see `ci/ctest-parallelism-probe.md`.
- **`f88c560f`** — `cache-cleanup.yml`: escaped `+` in the `workflow_run` selector. `on.workflow_run.workflows` entries are parsed as **filter patterns**, so the unescaped `libc++` was malformed and the whole workflow failed to BUILD for that event — emitting 15–20 "(Unknown event)" `startup_failure` runs per push to `main` while `push`/`pull_request_target` kept working. The tier-completion sweep had **never fired once** since `a5ae671f`. Single quotes are load-bearing: `'Tier 3 — Linux libc\+\+ CI'`.

**Decision records moved (2026-08-04):** `.specify/decisions/` is now a **symlink** to the parent research repo at `research/G19-fix-fpml-iso20022/decisions/speckit/`, where 306 gate/verify records are **tracked and pushed** instead of living gitignored on one machine. Every tool path resolves unchanged. The symlink needs its own `.gitignore` entry (`decisions/` has a trailing slash → directories only; git treats a symlink as a file).

**Open issues: 13** (re-derived 2026-08-04 after PR #232 via `gh issue list --state open`, which returned **13**: 196, 209, 211, 213, 215, 217, 220, 222, 225, 229, 231, 233, 234. **#228 closed with PR #232**; **#218 closed with 086/PR #230**; **#221 closed with PR #226**; #214 closed with 085. Two NEW: **#233** (filed by #232's Gate B) and **#234** (filed by 086's Gate B). **Derive this count, never copy it** — it has now gone stale three times; earlier revisions said "6", "9" and "11"):
**#196** v42 typed builder tier (blocked on L-063-1 structural group detection — that is what in-flight `082-structural-group-detection` delivers) · **#215** 083 `/simplify` follow-ups (duplicate `as_table_view` per session + 4 cleanups) · **#213** no fuzz corpus replayed in CI (~198 seeds, zero ctest registrations) · **#211** FileSink backpressure/drop untested · **#209** `compile_time_bench` flat 3s ceiling is not a meaningful gate · **#217** packaging needs a libc++ tier (standard-library dimension in the artifact name first) · **#220** dict-free per-instance cap counts trailing top-level fields → spurious `wire_group_too_large` under a tightened `Config` (filed by 085; recorded as L-085-1, unreachable under default configuration) · **#222** conan-cache MISS warning names the wrong cause (and is MSVC-only) · **#225** Article IX §1's wording is narrower than eight features of enforced practice — per-line *assessment* is the gate, the percentage is a *target* (waived at 085 Gate B as P3-2) · **#229** CI test-time investigation + the #227 probe record: the top-2 slowest tests (150 min aggregate, 38% of all test wall time) are a `std::system()` launcher and a CMake reconfigure script, so **no compiler flag reaches them**; ranked remediations · **#231** Tier 2 (MSVC) has no compiler cache — ~4.3 h of recompilation per run; try sccache · **#233** `read_first_frame_bounded` — TWO production defects surfaced by #232's Gate B and deliberately deferred (behaviour changes would have voided that PR's tests-only Gate A waiver): the byte budget rejects at **equality** (`buf.size() >= max_bytes`) while FR-014 says *"exceeds"*, and it rejects **before** `framer.feed`, so a chunk already holding a complete first frame is discarded — a valid Logon coalesced to exactly 4096 B is dropped; and the deadline `timer.async_wait` handler captures coroutine-frame locals **by reference**, so a late-firing handler can write through a dead frame *and* fire `transport.cancel()` on a transport already moved into a live `Session`, tearing down a just-established session · **#234** C-3 does not bind `INTERFACE_SYSTEM_INCLUDE_DIRECTORIES` (filed by 086's Gate B).
Also open, no issue filed: **ApplExtID(1156)=303** differentiation + `version_registry` re-keying (deferred by 074's L-074-1).

Orchestra read/dictionary tier DONE via 074 + runtime-load entry point via 080; typed **read** tier DONE via 076; typed **builder** tier DONE via 077 + split into per-version libs via 078 (**closed #198**); dictionary census hardening DONE via 072 (**closed #180**); live-wire validation DONE via 075, required-presence scope via 079, strict-validation residuals via 081 (**closed #203 + #205**), per-context group-delimiter resolution via 083 (**closed #210 + #208 + #212**), L-063-4 leg 2 via 085 (**closed #214**). C-ABI GA-frozen at `1.5.0`; Python PY-001..005 COMPLETE.

**This file is a THIN POINTER — do not paste per-feature history here.** The authoritative records:
- **Merged-feature changelog (newest first):** [`CLAUDE-history.md`](./CLAUDE-history.md).
- **Per-feature status + evidence:** `spec/feature-catalogue.md` + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/<module>/<feature>.md` (+ `phases/phase-4.md` dashboard, `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*` / `feedback_*` (index `MEMORY.md`; older close-outs `MEMORY-archive.md`).

**CI procedure (per [[project_ci_run_tier_labels]]):** branch protection REQUIRES `tier{1,2,3}-required` + `Gate A`/`Gate B` labels; all three tiers run on `pull_request`, gated on `(gate-a-done ∧ gate-b-done)`. Turn a PR green by satisfying both gates via `/gate-a` + `/gate-b` — the gate-label event co-fires all three matrices in pull_request context. Known exception: stale duplicate `tierN-required` check-runs (`[FAILURE,…,SUCCESS]`) can wedge the merge box at latest-green → merge `--admin` (PRs #177, #182; `required_approving_review_count` is 0). `push:main` re-runs all three post-merge (badge + gate).

**Live deferred work:** session-recovery catalogue row 400 (010 F4 ResendRequest/SequenceReset Reject→Process — largely shipped via 013 + S-023; residual = v1.0 traceability confirm) · live-interop golden-capture + C-103 chunked-resend → G4 ([[project_release_interop_quickfix_fix8]]).
<!-- SPECKIT END -->
