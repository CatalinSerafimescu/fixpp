<!-- SPECKIT START -->
**Active feature:** `031-acceptor-789-resend-boundary` — conformance bug fix (acceptor `NextExpectedMsgSeqNum(789)` resend-range boundary), plan at `specs/031-acceptor-789-resend-boundary/plan.md` (Phase 1 done; Gate A next). When fixpp is an ACCEPTOR with the 789 knob on, `honor_peer_next_expected_` (`session.cpp:4509`) compares the peer's advertised 789 against `peek_outbound()` — but on the acceptor arm honor runs AFTER the reply Logon consumed a seqnum (027 RC#4 ordering `:2023-2030`), so that value is the POST-reply `N_post = N_pre+1`. The peer's initial Logon advertises `789 = N_pre` (in-sync), so `N_pre < N_post` fires a spurious `SequenceReset-GapFill` at the seq the reply Logon already used → duplicate-MsgSeqNum → peer Logout ("MsgSeqNum too low") → session fails to establish. Found via the 027 SC-005 live acceptor interop cell (`NE-*-acc`) vs QFcpp v1.16.0; invisible to in-process 027 unit tests; parallels 030. **Oracle (both engines, research.md R2/R3):** the peer's 789 is compared against the PRE-reply next-outbound (QFcpp `Session.cpp:228/230` evaluate `< / > getExpectedSenderNum()` before `generateLogon(reply)` at `:244`; QFJ `Session.java:2250/2312` use `nextSenderMsgNumAtLogonReceived`, a pre-reply snapshot). In-sync `X==N_pre` ⇒ no resend; too-high `X>N_pre` ⇒ Logout; genuine-gap range `[X, N_pre]` — and fixpp's range `[x789, peek_outbound()-1]` ALREADY equals `[X, N_pre]`, so **the range is correct; only the comparison threshold is wrong**. **Fix (role-aware, comprehensive per /clarify):** parameterize `honor_peer_next_expected_` with `next_outbound_ref` used for all three comparisons; the ACCEPTOR call site (`:2027`) captures the PRE-reply outbound (`peek_outbound()` before the reply emit at `:2015`) and passes it; the INITIATOR call site (`:3322`) passes current `peek_outbound()` — byte-identical (its peer-reply `789=target+1` already matches; `NE-*-init` cells pass both engines). The resend range stays unchanged. Discriminating witnesses (research.md R8, avoid the `drive_to_active` trap): W1 in-sync ⇒ zero SequenceReset/ResendRequest + strict-monotonic seqnums (currently RED); W2 genuine-gap range non-regression; W3 too-high `X=N_pre+1` ⇒ Logout; W4 initiator non-regression; W5 invalid-789. No new wire/error/codegen/C-ABI/config/store surface; amends catalogue **S-031**. Predecessor `030-received-reset-inbound-advance` MERGED 2026-06-10 (PR #113 squash `3c5ee65`, gate-a-done + gate-b-waived [SC-001 live, discharged by Item-1]). Per [[project_live_quickfix_hostile_scenarios]] CHECKPOINT 9 / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **015 down-peer cause #2** — a mid-connect `transport::async_connect` is not promptly cancelled by `cancellation_type::total` (~30 s teardown latency on a never-established initiator). Real, bounded, Gate-A-class concurrency change; tracked in `spec/behaviors-and-limitations.md` (L2). Cause #1 (busy-spin) fixed in 016.
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
