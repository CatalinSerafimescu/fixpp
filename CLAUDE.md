<!-- SPECKIT START -->
**Active feature:** `010-session-cfg-lifetime` — close the residual PR #82 Gate B P2/P3 waivers (W-5 + F-04 / F-05 / F-06 / F-07 + E1 / F-11 / RC#G mixed-path) bundled around the same `session.cpp` / `session.hpp` / `tests/session/` touch surface. **Primary subject (W-5):** eliminate the pre-existing 005-baseline ASan stack-use-after-scope on `Session::cfg_` (declared at `include/fixpp/session/session.hpp:312` as `const SessionConfig& cfg_;`, dangling-dereferenced at `src/session/session.cpp:116` once the fixture-local `SessionConfig` leaves scope). `/speckit-clarify` (2026-05-23) settled the mechanism: **Option A — by-value `SessionConfig cfg_;`** (ctor copies the caller's config into a same-typed member; caller may freely drop or mutate the config after the ctor returns). The ride-along deferrals close the F-04 / F-05 / F-06 / F-07-E1 / F-11 / RC#G mixed-path gaps logged in the PR #82 Gate B decision record (`library/.specify/decisions/009-session-fsm-finalize-gateb.md`).

For technologies, project structure, build/test commands, and gate status, read the current plan: [`specs/010-session-cfg-lifetime/plan.md`](specs/010-session-cfg-lifetime/plan.md). **The 005 design is binding and unchanged** — the slice does not amend `005/spec.md` / `data-model.md` / `contracts/*.hpp`. **Gate A is NOT re-run for 010** (inherited from 005 Gate A round 3, 2026-05-18, with a 010-specific addendum citing /clarify + research D-1/D-2/D-3 produced at the Gate B precondition step per FR-012 option ii). Design anchors: every existing 005 anchor carries forward as-is (`.specify/2e-msgstore.md` v0.4, `.specify/2d-threading.md` v0.4, `.specify/2f-async-mutex.md` v1.5, `[FIX-SL]` + `[FIX-TC]`).

**Branch base:** `010-session-cfg-lifetime` is rooted on the post-PR-#82-merge `main` of the library submodule (`ba2222d`). No 005 / 009 branch carry-overs; the 005 + 009 work is fully merged. At Gate B convergence the slice merges directly to `main`; the PR closes the W-5 waiver row in `009-session-fsm-finalize-gateb.md` (SC-009).

`/clarify` decisions: Q1 (2026-05-23) — FR-001 W-5 mechanism = Option A (by-value `SessionConfig cfg_;`). Q2-Q5 unnecessary (the deferred items — F-04 seam mechanism, F-07/E1 variant name, Gate A inheritance — are plan-level micro-decisions resolved in research.md D-1/D-2/D-3, not /clarify questions).

Companion artifacts in the same directory:
- [`spec.md`](specs/010-session-cfg-lifetime/spec.md) — 12 FRs (FR-001..FR-012) tracing to W-5 + the 6 bundled deferrals + 3 hygiene rows; 4 user stories (US1 P1 W-5 lifetime; US2 P2 F-04 + F-07-E1; US3 P2 F-06 + F-05 + RC#G; US4 P3 F-11); 9 measurable SCs.
- [`research.md`](specs/010-session-cfg-lifetime/research.md) — Phase 0: 4 plan-level micro-decisions (D-1 by-value cfg copy + copyability gate, D-2 FSM visit-history ring-buffer seam, D-3 `session_invalid_state_for_send = 77` variant + slot, D-4 FSM matrix cell-count enumeration). No design space opened.
- [`data-model.md`](specs/010-session-cfg-lifetime/data-model.md) — **no new entities;** modifies E1 `Session` (cfg_ by-value + ring-buffer + accessor + I-25 invariant), E2 `SessionConfig` (add `static_assert(is_copy_constructible)` gate), E3 `error` enum (one new slot at 77).
- [`contracts/session_error_state_for_send.hpp`](specs/010-session-cfg-lifetime/contracts/session_error_state_for_send.hpp) — the ONLY new shape oracle; documents the binding contract for the new variant + FIXPP_ERR_SESSION_REJECT C-ABI prefix-group mapping.
- [`quickstart.md`](specs/010-session-cfg-lifetime/quickstart.md) — build / sanitizer matrix / test commands / acceptance gates per FR / `/speckit-verify` (test-bodies discipline) / `/gate-b` / merge bookkeeping.

**PR #82 closure tracking:** the W-5 row in `library/.specify/decisions/009-session-fsm-finalize-gateb.md` will be annotated `CLOSED — see PR #<N>` at 010 Gate B convergence (SC-009). The ASan-only test-skip in `tests/session/CMakeLists.txt` (added in PR #82 as the W-5 carry-forward) is removed by FR-003.

Previous feature (merged): [`009-session-fsm-finalize`](specs/009-session-fsm-finalize/plan.md) — drift closure of 005 across PR #81 (paused) → PR #82 (merged, squash `ba2222d`). Gate A inherited from 005; Gate B converged 4 rounds (Sonnet 2/2 + Codex 1/2). Earlier: [`008-message-store`](specs/008-message-store/plan.md) PR #77+#78, [`007-threading-clock`](specs/007-threading-clock/plan.md) PR #74+#75, [`006-async-mutex`](specs/006-async-mutex/plan.md) PR #73, [`004-wire-codec`](specs/004-wire-codec/plan.md) PR #68, [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) PR #67, [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) PR #66, [`001-core-decimal`](specs/001-core-decimal/plan.md) Gate A/B converged 2026-05-12/13. [`005-session-establishment-fsm`](specs/005-session-establishment-fsm/plan.md) shipped via the bundled PR #82 with 009.
<!-- SPECKIT END -->
