<!-- SPECKIT START -->
**Active Spec-Kit feature**: `034-credential-store-redaction` — plan at `specs/034-credential-store-redaction/plan.md` (security hardening, Fable F-c). Mask `Password(554)` same-length/zero-alloc at the single `store_then_emit` store boundary; wire frame untouched; no new wire/error/config/codegen/C-ABI surface. Awaiting Gate A + user `/plan` sign-off (Appendix A: security → all four controls).

Last merged: `033-fixt-fix50sp2-session` — **MERGED 2026-06-12** (PR #116 squash `799aa75`, gate-a-done + gate-b-waived; amends catalogue **S-020** FIXT-half + **S-022** + **S-025**; S-026/1128 stays deferred tolerate-only). G4a FIXT.1.1 / FIX 5.0 SP2 establishment: `8=FIXT.1.1` transport split from app version via `DefaultApplVerID(1137)`; both roles establish; missing-1137→`Reject(373=1)`, unserviceable→`Reject(373=5)` (acceptor-only, FR-004a); negotiated version recorded + exposed via `Session::negotiated_version_profile()`; optional `Username(553)`/`Password(554)` emit+parse+surface via `CompIdAuthorizationPolicy::authorize_logon` seam; `554` redacted. Additive behind default-off `SessionConfig::default_appl_ver_id`; FIX.4.x byte-identical (W4). **US3 live SC-004/SC-006 PROVEN**: all 8 `HP-{QFcpp,QFj}-{init,acc}-fixt11-{fix50sp2,fix44}` cells pass live both engines/roles (1137=9 / 1137=6), goldens banked, manifest off `deferred:fixt-routing`, schema 9/9. **Gate B (2 rounds, Sonnet-only): 2 P1 + 3 P2 all REAL** — hardened the establishment path (US3 live had no `src/` change): FQ-1 open()-time FIXT config validation fail-closed; FQ-2 `authorize_logon` throw-safety inside the noexcept body; FQ-3 credential SOH/`=` injection floor; FQ-4 doc. **Waiver:** #4 `redact_tag554` RawData(96) length-awareness → L-033-4. **Live-found L-033-5** (acceptor needs the app-dict in the engine `version_registry` to service a configured version — spec-correct footgun; open Q whether establishment should self-register `cfg.default_appl_ver_id`). Predecessor `032-initiator-reset-outbound-advance` MERGED 2026-06-11 (PR #115 squash `9417454`). NEXT: 025 `StandbyRehydrate` demo cell (non-SC) · C-103 → G4 tail. Per [[project_033_fixt_fix50sp2_session_merged]] / [[project_live_quickfix_hostile_scenarios]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
