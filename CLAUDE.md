<!-- SPECKIT START -->
**Active feature:** `030-received-reset-inbound-advance` — conformance bug fix (acceptor 141=Y off-by-one), plan at `specs/030-received-reset-inbound-advance/plan.md` (Phase 1 done; Gate A next). On the received-141 path (peer `Logon(141=Y)`, `reset_on_logon` knob OFF), `reset_seqnums_to_one_durable()` clobbers the consumed reset-Logon's inbound advance back to 1, leaving next-expected-inbound=1 instead of 2 → spurious `ResendRequest` on the peer's seq-2 message + reply advertises `789=1` not `2` (027-on). Found via a failed live acceptor interop cell vs QFcpp/QFJ. Oracle grounded in source: both engines **reset-then-increment** (net 2); fixpp increments-then-resets with no re-increment (QFcpp `nextLogon` ~206/265/710; QFJ 2202-2204/2215/2278/2303). **Fix (Gate A round-1 outcome — persist-to-2, BOTH arms):** after the durable reset, restore next-expected-inbound to `seqnum_min+1` in BOTH the in-memory manager (`set_next_inbound`) AND the durable store (one `next_seqnum(inbound,increment=true)` write-through) → `store == manager == 2` (INV-H1 holds with **equality**; the consumed seq-1 reset Logon is a *surviving net-advance*, NOT 029 over-persist). Manager-only (store left at 1) was REJECTED at Gate A — the knob-off Logon gate has no ResendRequest arm, so it would fatal-disconnect-on-restart. **Gate A round-2 refinement (user-approved):** the received-141 durable reset is made **fatal when the store is persistent** (`store_is_persistent_ ? fatal : logged`) on both arms, so persist-to-2 only runs after a guaranteed-successful reset (closes the swallowed-`logged`-reset-failure over-persist hole) — this **amends 024's FR-001/C2.6 I-07 contract** (persistent received-141 reset failure now DISCONNECTS, was stay-Active). Guarded on `logon_inbound_advanced`; the restore+persist lands between the reset (`:1942`) and the 789 read (`:1974`) so 789 reflects the corrected counter. Applied symmetrically to the **acceptor** received-141 arm AND the **initiator** Logon-ack `peer_ack_sent_reset_flag` arm (`:3162`, consolidated onto the shared helper) — separate code paths, identical clobber, both reachable (FR-009; [[feedback_half_restructure_symmetric_api]]). Outbound reply MsgSeqNum stays seq 1 (byte-identical); only 789 *content* corrects 1→2 (acceptor-reply only). Discriminating witness (acceptor) = the **triple** (next_inbound==2 AND reply.MsgSeqNum==1 AND reply.789==2, 027-on); initiator = next_inbound==2 + harm-repro; + a fault-injection witness (persistent store seeded N=37, reset failure → disconnect, store retains N=37). Blast radius = **7 pins** across 013/024/027/029 (6 value-pins next_inbound/789 1→2, incl. the initiator `BilateralStrict_Initiator_CountersResetToOne` + the 029 hydrate W9b's 2 sub-asserts; + 1 contract-witness split: 024 witness (5) → persistent-Disconnect / non-persistent-stay-Active). Prototyped + harm-confirmed on `fix/acceptor-received-141-next-inbound` (not merged); this pipeline re-derives TDD-first. Predecessor `025-refresh-on-logon` MERGED 2026-06-10 (PR #112 squash `357f5ab`, gate-b-done). Per [[project_live_quickfix_hostile_scenarios]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **015 down-peer cause #2** — a mid-connect `transport::async_connect` is not promptly cancelled by `cancellation_type::total` (~30 s teardown latency on a never-established initiator). Real, bounded, Gate-A-class concurrency change; tracked in `spec/behaviors-and-limitations.md` (L2). Cause #1 (busy-spin) fixed in 016.
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
