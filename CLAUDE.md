<!-- SPECKIT START -->
**Active Spec-Kit feature**: _(none)_ — `035-filestore-io-offload` MERGED 2026-06-14 (see "Last merged" below). Next deferred work: 025 `StandbyRehydrate` demo cell (non-SC) · C-103 chunked-resend · live interop golden-capture (Item 1) → G4.

Last merged: `035-filestore-io-offload` — **MERGED 2026-06-14** (PR #119 squash `90c8037`, gate-a-done + gate-b-waived). Real `file_io_executor` offload for `FileStore`: replaced the inert `co_await asio::post(file_io_executor, use_awaitable)` (D-18) at store/next_seqnum/reset + `flush_for_session_close` with nested `co_spawn(pool, syscall, use_awaitable)` so `pwrite`/`fdatasync`/`rename`/`datasync` run off the session strand — fixes the live `[const §XV.4]` violation (3-region split: strand-prepare → pool raw-syscall lambda capturing only fd-value/PODs/span → strand-commit; mutex held across the hop; `impl_` mutation strand-confined). `retrieve()`'s `pread` stays on the strand; `generation_` epoch guards a mid-walk `reset()` → `store_io_failure` (no new variant; FR-021 10-variant freeze static_assert). Binary §6.1.4 cancellation: cancel at mutex-acquire → `store_cancelled`/0-change; after offload → durable, unconditional `operation_aborted`→durable catch. Shipped on the `[const §XV.1]` v0.2 §XV.4-offload exemption (≤1 bounded O(1) frame/op; MemoryStore zero — by-construction + probe). Conformance fix to 2e/008 (FR-024/§4.3.2/SC-006); no public signature/error/config/wire change (3 `FIXPP_TEST_HOOKS`-gated runtime test seams). **Gate B 3 rounds** (R1 Codex 4 P1+3 P2 → triage P1=3/P2=3 → Sonnet; R2 1 P2+1 P3; R3 converged P1=0/P2=0): fixed reset() post-rename fd-handling (poison on reopen-fail + durable-abort fd recovery — a silent-loss P1 pre-existing in 008), cancellation arm-(a) determinism (io_context controller, no SUCCEED escape), SC-007 spec drift (Engine::stop is terminal-only → split C5a code-analysis + C5b discriminating `g_flush_datasync_count` witness), bench rebaseline (+5.5% real_time/−23% CPU). One §IX.1 waiver (store/next_seqnum benign defensive catch). Post-merge CI ASan flake fixed (`Reset_Syscall_OnPoolThread`: strand shared the file pool → dedicated strand thread). New B-035-1 + L-035-1/L-035-2; S-013 amended. Predecessor `034-credential-store-redaction` MERGED 2026-06-13 (PR #117 squash `f5bee19`). NEXT: 025 `StandbyRehydrate` demo cell (non-SC) · C-103 → G4 tail. Per [[project_035_filestore_io_offload_merged]] / [[project_034_credential_store_redaction_merged]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
