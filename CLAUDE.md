<!-- SPECKIT START -->
**Active feature:** [`013-session-reconnect-binding`](specs/013-session-reconnect-binding/plan.md) — session-Phase-4 surface: reconnect FSM driver on top of 005's 6-state `fsm_state` + recovery sub-protocol (FR-009..FR-016 — amends 005 FR-008 fatal-disconnect → recovery-active; discharges the 2e-recovery upgrade obligation per `[[project_2e_recovery_v1_upgrade_obligation]]`) + CompID↔TLS-identity binding (T-041 full row; FR-019..FR-025; allow-list-only `CompIdAuthorizationPolicy` per Clarifications Q3=A; canonical-fixed CN→SAN-DNS→SAN-URI→SHA-256-fingerprint principal extraction per Clarifications Q2=A) + TLS validation outcome→SessionEvent (T-039 second half; FR-026..FR-029; surfaces the 6-cell `error::tls_*` master-enum — `tls_handshake_failed` GROUPING per `[2g §6.6]` + 5 specifics — plus the thread-local `last_handshake_sub_reason()` diagnostic string as `SessionEvent::tls_validation_failed{code, sub_reason, peer_endpoint, reason_string}`) + in-process `reload_credentials` (T-040 second half; FR-030..FR-034; atomic swap on the factory-internal `cert_source_slot_` per Clarifications Q4=A; `TransportFactory::reload_credentials` is the new pure-virtual — count 1→2 still under 5/5 cap — that the factory IS the symmetric authority for both initiator and acceptor halves). 4 new `error::session_*` slots 116..119 (slot 119 `session_invalid_argument`, renumbered from 121 per F1/D1 2026-05-28; plus reuse of 005-era slots 73 `session_logout_timeout` + 74 `session_test_request_unanswered` for the same FIX-SL §4.5 events — reference-engine sweep confirmed zero precedent for typed code-level discrimination). Consumes 012's `Transport` + `TransportFactory` + `ReconnectPolicy` + `handshake_result.peer_id`, 011's `verify_peer` 6-master-enum surface + thread-local `last_handshake_sub_reason()` diagnostic + `peer_identity`, 010's `fsm_visit_history()` accessor (UNCHANGED; 013 introduces a NEW `SessionEvent` variant union + `Session::recent_events()` accessor) + `SessionConfig` (4 new fields appended per FR-008/FR-017/FR-023/FR-030). `/speckit-specify` + `/speckit-clarify` (5 Qs resolved 2026-05-28 with QFC/QFJ/Fix8 reference-engine sweep per `[[feedback_always_invoke_speckit_clarify]]`) + `/speckit-plan` (2026-05-28) shipped; next per `[[feedback_speckit_pipeline_order_gate_a_before_tasks]]` is `/gate-a 013-session-reconnect-binding` (Phase-4 Gate A) → `/speckit-tasks` → `/speckit-analyze` → `/speckit-checklist` → `/speckit-checklist-audit` → `/speckit-implement` → `/simplify` → `/speckit-verify` → `/gate-b` → merge.

Recently merged (`main` history): [`012-2h-transport`](specs/012-2h-transport/plan.md) PR #85 (squash `53e25b1`) — TCP + TLS transport surface (`Transport` interface + `TlsTransport` sub-interface, `asio_tls_transport` OpenSSL impl, `asio_listener` acceptor, `ReconnectPolicy` schedule-array shape Q2=C, `TransportFactory` with cached SSL_CTX + credentials Q1=B, `mock_transport` test seam, 22-variant `error::transport_*` envelope at slots 94..115). Gate B converged 4 rounds (Sonnet 2/2 + Codex 2/2 attempts) with 3 documented carry-forward waivers (RC#C PMR-OOM witness depth + RC#G handshake bench scaffold + RC#I fuzz scope re-label). 011 Gate B F-1 cancellation-seam #13 witness discharged in 012 via `tests/transport/test_load_credentials_seam13_witness.cpp` (8 cells × 4 sanitizers). Three new burn-class anti-pattern library candidates surfaced for codification (half-restructure / weak_ptr cache lifetime / trampoline witness surface-cardinality). See `[[project_012_2h_transport_closed]]`.

Earlier merged: [`011-tls-policy`](specs/011-tls-policy/plan.md) PR #84 (squash `67448c0`) — TLS validation surface (cert_source + Pinset + verify_peer + CipherPolicy + SecurityProfile + SslCtxConfig + peer_identity); F-1 carryover discharged in 012. [`010-session-cfg-lifetime`](specs/010-session-cfg-lifetime/plan.md) PR #83 (squash `25cf09d`). [`009-session-fsm-finalize`](specs/009-session-fsm-finalize/plan.md) PR #82 (squash `ba2222d`); [`008-message-store`](specs/008-message-store/plan.md) PR #77+#78; [`007-threading-clock`](specs/007-threading-clock/plan.md) PR #74+#75; [`006-async-mutex`](specs/006-async-mutex/plan.md) PR #73; [`004-wire-codec`](specs/004-wire-codec/plan.md) PR #68; [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) PR #67; [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) PR #66; [`001-core-decimal`](specs/001-core-decimal/plan.md) Gate A/B converged 2026-05-12/13. [`005-session-establishment-fsm`](specs/005-session-establishment-fsm/plan.md) shipped via bundled PR #82 with 009.

**Deferred-work registry** (still applicable after 012 ships):
- **Session-module Phase-4** (next planned) — Logon/Heartbeat/Resend/Reset/Logout reconnect FSM + CompID-to-TLS-identity binding policy half of T-041. Consumes 012's `Transport` + `TlsTransport` + `ReconnectPolicy` + `handshake_result.peer_id`. Unblocks `[[project_release_interop_quickfix_fix8]]` per-release interop gate.
- **Per-release interop gate** — run fixpp against QuickFIX-cpp + Fix8 as both initiator and acceptor + sweep their issue trackers and re-run fixpp against their historical thorny bugs. 005 FSM shipped + 011 TLS validation surface shipped + 012 transport surface shipped; awaits session-Phase-4 for the full transport-active matrix. Per `[[project_release_interop_quickfix_fix8]]`.
- **Deferred session-recovery feature** — will upgrade 010's F4 closure cells `"2"` (ResendRequest) and `"4"` (SequenceReset) from Reject → Process (gap-fill via message store), matching QuickFIX-cpp / QuickFIX/J. Dup-Logon cell stays as Reject per 005 defensive divergence. Catalogue row 400; 4 traceability markers tracked.
- **012 Gate B carry-forward slice** — RC#C PMR-OOM witness depth (multi-SAN cert fixture + trampoline-targeted fault-injection cell per `[[feedback_trap_throw_pmr_witness_enumerate_sites]]`) + RC#G handshake bench live fixture (post-cache shape stable now — measurable) + RC#I fuzz scope catalogue re-label. Follow-on PR off post-012 `main`.
- **Repo-wide clang-format / clang-tidy / iwyu / coverage cleanup** — separate `chore/` branch off post-012 `main` (deferred per user direction during 012 Gate B). Pre-existing W-format (425 violations) + W-tidy (75 diagnostics) + W-2 cppcheck + W-3 iwyu carry-forwards from prior PRs.
<!-- SPECKIT END -->
