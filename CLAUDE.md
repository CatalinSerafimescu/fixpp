<!-- SPECKIT START -->
**IN FLIGHT: `064-fix4041-legacy-types` (A-5 / D-004)** — plan `specs/064-fix4041-legacy-types/plan.md` (Phase 1 done; Gate A next). Adds two loader collapse-table rows (`TIME→UtcTimestamp`, `DATE→LocalMktDate`) + vendors FIX40/41 XMLs so the last two runtime-XML versions load; relaxes AC-L8 for exactly two named legacy types (global, metadata-only — validator collapses both to `field_type::String`). Full Spec-Kit + Gate A per user (2026-07-05). See [[project_a5_dictionary_xmls_d004_loader_gap]].

Last merged (newest first): **D-005/006 dictionary XMLs** (PR #171, squash `1c9e44eb`, 2026-07-05, gate-a-waived + gate-b-done) — vendored FIX 4.3/5.0/5.0SP1 runtime-XML dicts + headline tests (pure data+tests, no loader code); **D-004 (FIX 4.0/4.1) BLOCKED** on a Gate-A'd loader follow-up (loader fail-closes on their legacy `DATE`/`TIME` field types — see [[project_a5_dictionary_xmls_d004_loader_gap]]). **TC-018 zero-count repeating group** (PR #170, squash `3ce30162`, 2026-07-05, gate-a-waived + gate-b-done) — witness-only conformance closure (`NoXXX=0` accepted as empty group; B-004-7). Before those, **`062-grouped-typed-read-fix`** (PR #168, squash `9d5ac6cb`, gate-a-done + gate-b-waived) — the repeating-group ENTRY typed-read path fix (`group_view::operator[]` now builds generated `G_<n>` entries via an `entry_context` ctor; span-scan scalars + lazy dict-aware nested sub-view); unblocks `061-typed-app-messages` (paused). **Next: resume 061** — rebase onto main → re-run `/speckit-plan` against the working group-read API → Gate A → tasks → implement → verify → Gate B — and/or the prereq **`063` "nested group-parse correctness"** (per-context group registration + recursive nesting-aware boundary walk; `research/findings/dict-group-tag-collision-2026-07-05.md`), required by BOTH 062-real-dict AND 061 (L-062: single-entry nested proven; multi-instance nested + real-dict deferred). See [[project_061_typed_app_messages]]. C-ABI GA-frozen at `1.5.0`; Python PY-001..005 COMPLETE. **CI merge caveat (per [[project_ci_run_tier_labels]]):** branch protection REQUIRES `tier{1,2,3}-required` + Gate A/B; `run-tierN` labels are one-shot on the fresh label event and sequential labels can never make all 3 green-latest at once — apply the 3 labels once and don't touch them, or verify all 3 matrices built green on the head SHA and `gh pr merge --admin`.

**This file is a THIN POINTER.** Per-feature history is NOT duplicated here — it lives in the authoritative trackers:
- **Per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/<module>/<feature>.md` (+ `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*` / `feedback_*` notes (index in `MEMORY.md`; older lifecycle close-outs in `MEMORY-archive.md`).

**Recently merged (newest first — full detail in the phase-4 lifecycle docs + memory):**
- **D-005/006 dictionary XMLs** — PR #171 (squash `1c9e44eb`), 2026-07-05, gate-a-waived + gate-b-done. Non-Spec-Kit data+tests closure: vendored `dictionaries/FIX43.xml`, `FIX50.xml`, `FIX50SP1.xml` (verbatim from quickfix/quickfix at the UPSTREAM.txt-pinned SHA) + 3 new headline `VersionParam` rows in `tests/dictionary/lookup_test.cpp` (suite `FourShippedVersions`→`AllRuntimeVersions`, 7 params). FIX 5.0/5.0SP1 are app-layer only (session→FIXT.1.1): headlines are app msgs D/8/W, session msgs A/0 asserted forbidden, and the `RequiredFieldsAndLengthPairs` app-only Logon carve-out extended v50sp2→v50/v50sp1. Gate B 1 round (Codex P2: README refresh recipe listed FIX40/41 contradicting the D-004 deferral → scoped to the 7 bundled files, `05ded926`). **D-004 (FIX 4.0/4.1) NOT shipped** — loader `[FIX50SP2 §3.3]` vocabulary fail-closes on their legacy `DATE`/`TIME` types → own Gate-A'd loader feature. See [[project_a5_dictionary_xmls_d004_loader_gap]].
- **TC-018 zero-count repeating group** — PR #170 (squash `3ce30162`), 2026-07-05, gate-a-waived + gate-b-done. Witness-only conformance closure (QuickFIX AT 21, `RepeatingGroupSpecifierWithValueOfZero`): a `NoXXX=0` present-but-empty group is accepted, not rejected. Zero production change — behavior already correct (validator short-circuits at `declared_count==0`; parser yields empty span). Added `ZeroCountGroupAccepted` (validator, 4 wire versions, mutation-proven RED on deleting the short-circuit) + `ZeroCountGroupPreservedByteIdentical` (writer round-trip) + B&L `B-004-7`. Gate B 1 round (Codex P3 = inherited out-of-scope commit, rebased out). See [[feedback_planning_explore_existence_claims_unreliable]].
- **`062-grouped-typed-read-fix`** — PR #168 (squash `9d5ac6cb`), 2026-07-05, gate-a-done + gate-b-waived. Prereq for 061: fixes the verified compile blocker where typed reads of repeating-group ENTRIES didn't compile (`group_view::operator[]` span-ctor vs generated `G_<n>` `MessageView`-ctor). HYBRID mechanism: entry carries `entry_context` (span + mr + dict + group_member_fn + generation_token + ROOT cache owner); one-level scalar reads span-scan the slice (zero-alloc, FR-004a); nested descent builds a dict-aware sub-`OffsetTable` over slice-scoped `data,len+1`, cached once in a ROOT-owned flat cache keyed by `(slice_data ptr, nested_no_tag)`. Gate B 2 rounds (RC#1 warm nested-cache-hit generation-trap bypass `7bb3509e`; RC#2 emitter-comment `285f5b9d`) + Tier-2 MSVC-debug-STL alloc-injection test skip (`99a18c53`). W-1 top-level group-metadata stale-read → follow-up #169. **L-062**: single-entry nested proven; multi-entry nested + real-dict correctness → prereq **063**. See [[project_061_typed_app_messages]].
- **`store-memory-reset-guard` (S-P2-2)** — PR #167 (squash `128fcd82`), 2026-07-05, gate-a-waived + gate-b-done. Non-Spec-Kit bug-fix batch (Cluster-3 residual): `MemoryStore::retrieve()` mirrors FileStore's `generation_` reset-during-traversal guard (fail-closed `store_io_failure` on a mid-walk `reset()`, both policies — unbounded previously silent-replayed) + a reused member `retrieve_scratch_` (no per-call monotonic-arena burn). Gate B 2 rounds (Codex off-mutex-read P1 refuted = accepted FileStore strand-confined Decision-3; `frame_scratch` arena-burn P2 fixed via member promotion). No public/C-ABI/wire/error/layout change. `phase-4/session/store-memory-reset-guard.md` + `behaviors-and-limitations.md` B-008-7. See [[project_next_task_sp2_2_memorystore_guard]].
- **`060-int128-decimal-compare`** — PR #165 (squash `122c95b6`), 2026-07-04, gate-a-done + gate-b-done. Cluster-2 residual **C1**: exact wide-integer cross-exponent decimal compare (`k≥19` dominance + `mul_u64_wide`), default-path swap, bit-identical `strong_ordering`, `decimal.hpp` byte-identical; reverses the 2a Gate-A `no-__int128` decision (5-site amendment: 3 in-submodule landed in-PR, 2 parent-repo in the post-merge close-out). MSVC `#elif` `_umul128` lane Tier-2-only (L-060-1) — discharged locally + CI-green. `phase-4/core/060-int128-decimal-compare.md`. See [[project_060_int128_decimal_compare]].
- **`059-outbound-store-fail-closed`** — PR #163 (squash `b48bcfd`), 2026-07-04, gate-a-done + gate-b-waived. Cluster-3 flagship (S-P1-1/S-P2-1): outbound frame-store failure now **fatal-when-persistent** (mirrors the transport-write disposition — `store_then_emit` returns `store_r.error()`, caller-owned transition) + best-effort outbound-only reconcile-from-durable; closes the FileStore transient-I/O retention-freeze + restart-desync. Gate B 3 rounds (RC#1 fatal-class range-predicate `[56,65)`; RC#2 provenance-flag vs value-coercion); no public/C-ABI/wire/error/layout change. `phase-4/session/059-outbound-store-fail-closed.md`. See [[project_059_outbound_store_fail_closed]] / [[feedback_mirror_existing_failclosed_disposition]] / [[feedback_classify_disposition_by_provenance_not_error_value]].
- **`058-async-mutex-hardening`** — PR #162 (squash `edad623`), 2026-07-03, gate-a/b-done, 0 blocker-waivers. Cluster-4 hardening: 7 async_mutex defects (AM-P1 ABA/reuse + P2 drain/destroy HB + P3 impossible-state traps) + T-1..T-7 test-validity gaps; header-only, no public/C-ABI/layout change. Gate-B Codex also found+fixed a pre-existing 048 acquisition livelock in-PR. Post-merge: portable `set_terminate` death-test marker fixed a libstdc++-only `"terminate called"` regex on Tier-2/3. `phase-4/core/058-async-mutex-hardening.md`. See [[project_async_mutex_cluster4_hardening]] / [[feedback_death_test_terminate_banner_not_portable]].
- **`057-behavioral-reify-unblock`** — PR #161 (squash `5a7a944`), 2026-07-02, gate-a/b-done, 0 waivers. Live `dict::reify()`/`reify_as<Msg>()` typed owning handles (app single/multi-char + FIXT-admin) via a build-tree dispatch-bridge; lifts R6 / L-003-1; NO type-erasure (`as<Msg>()` stays T059-deferred); zero new public/wire/error/C-ABI surface (NFR-003-8 held). `phase-4/dictionary/057-behavioral-reify-unblock.md`. See [[project_057_behavioral_reify_unblock]].
- **`056-python-wheel-packaging`** (PY-005) — PR #158 (`e9d3763`), 2026-07-01. One `cp310-abi3` wheel + Tier-1 `python-wheel` gate; closes the PY-001..005 arc. [[project_056_python_wheel_packaging_gatea]].
- **#160 `cabi-1.5-ga-freeze`** — PR #160 (`61edae6`). C-ABI MAJOR 0→1, MINOR preserved → **`1.5.0`** (the `0→1` GA freeze). [[project_cabi_ga_freeze_1_5_0]] / [[feedback_ga_major_bump_preserve_minor_downgrade_frame]].
- **#159 `python-bindings-hardening`** — PR #159 (`723da02`). 4th `python-bindings` UBSan Tier-1 leg (closes L-054-2) + `msg_get_string` non-UTF-8 decode witness.
- **Earlier:** 053–055 (Python PY-001..004) · 049–052 (C-ABI Features A–C + Python-readiness) · 046 (libc++ atomic_shared_ptr) · 035–045 (Fable F-f tail + config/transport). See `phases/phase-4.md` + `spec/feature-catalogue.md`.

**Live deferred work** (everything else is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
- **Next candidates** (value-ordered, per parent `REMAINING-WORK.md`): live-interop golden-capture (Tier-1 Item-1, incl. the 025 demo cell) + C-103 chunked-resend (Tier-2, P3) → **G4**. Per [[project_release_interop_quickfix_fix8]].
<!-- SPECKIT END -->
