<!-- SPECKIT START -->
**Active Spec-Kit feature**: _none — 034 merged 2026-06-13; next feature TBD._

Last merged: `034-credential-store-redaction` — **MERGED 2026-06-13** (PR #117 squash `f5bee19`, gate-a-done + gate-b-done; amends catalogue **S-022** at-rest half). Security hardening (Fable F-c / 5.2): masks the `Password(554)` value **same-length / zero-alloc / in-place** in a private copy at the single `Session::store_then_emit` store boundary BEFORE persistence (both roles, all store backends), gated to a genuine `554` AND `MsgType(35)=="A"`; wire frame transmitted UNMASKED. New `mask_tag554_same_length_inplace` + `frame_has_genuine_tag554` (`logon_credentials.hpp`). Over-bound branch production-unreachable (`build_logon` fails closed at the 256-capped callers); open()-time `cred_len >= kMaxMaskableLogonBytes` sufficient-condition floor (both roles). Closes the at-rest cleartext exposure the 033 `redact_tag554` claim missed (the 008 store persisted cleartext `554`); L-033-6 → **B-034-1** mitigation + forward-constraint **L-034-1**. No new wire/error/config/codegen/C-ABI/store surface; FIX.4.x byte-identical. **Verify GREEN** (6-preset matrix; full session suite 73/73; all 034-new lines 100% DA/BRDA incl. over-bound both arms via T010). **Gate B (3 Sonnet rounds, P1=0/P2=0/P3=0, zero waivers)** — all fixes doc-only; Codex's lone round-1 P1 (open()-guard "incomplete") was a vindicated misread (build_logon is the real ceiling). **Verify-phase TSan finding** (TEST-ONLY, not production): the credential harness co_spawned `close()` on a bare 2-thread pool, violating close()'s v1.0 serialisation-domain precondition (session.hpp:140; production dispatches close() on the session_strand, engine.cpp:1174) — fixed by confining the harness to one long-lived `any_io_executor` strand + `pool.join()` ([[feedback_strand_in_any_executor_refcount_race]]). Predecessor `033-fixt-fix50sp2-session` MERGED 2026-06-12 (PR #116 squash `799aa75`, gate-a-done + gate-b-waived). NEXT: 025 `StandbyRehydrate` demo cell (non-SC) · C-103 → G4 tail. Per [[project_034_credential_store_redaction_merged]] / [[project_033_fixt_fix50sp2_session_merged]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
