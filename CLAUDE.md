<!-- SPECKIT START -->
**Active Spec-Kit feature**: **`037-resend-reply-possdup-tags`** (Fable **F-e**) — plan at `specs/037-resend-reply-possdup-tags/plan.md`. Pure wire-output correction in the two resend-reply emitters: (1) `build_sequence_reset_gapfill` gains `43=Y` + `122=<own 52>` (default-path wire change → golden re-bake + live re-run); (2) `build_replay_frame` skips stored `43`/`122` so the `allow_pos_dup=true` retain case replays with exactly one each. `122=52` + `43-requires-122` + map-based-order-safety verified vs QFcpp/QFJ. No new wire field / error slot / config / codegen / C-ABI (FR-007). **Status:** spec + clarify + plan + Gate A (converged r2) + tasks + analyze + checklist-audit (GREEN) + **`/speckit-implement` DONE** (5 commits, branch `037-resend-reply-possdup-tags`, NOT pushed). Both builder edits live + witnessed: GapFill 43/122 (Cell 1, `test_resend_reply_possdup.cpp`) + replay dedup under `allow_pos_dup` (Cell 2/3 in `test_send_allow_pos_dup_strip.cpp`). ASan/UBSan clean on touched suites; FR-007 surface gate PASSES (only a doc-comment in `include/`); prod 256B GapFill buffer headroom proven via `recovery_admin_span_gapfill` #239. **SC-004 live conformance is fully deferred/waived** — QFJ arm DEFERRED to Item-1 live-golden-capture (L-037-2, unblock-conditioned), QFcpp waived (L-021-3); byte-level rests on Cell 1 + the (not-yet-captured) golden. T009 golden profile switched ({52,10,122}); T010 verified no committed golden captures a 35=4 (no re-bake). **Next: `/speckit-simplify` → `/speckit-verify` (full 6-preset Tier-1 matrix) → Gate B.** Next deferred work after F-e: **F-f** (resend-bypasses-toApp / acceptor inbound-Logon SendingTime) · 025 `StandbyRehydrate` demo cell (non-SC) · C-103 chunked-resend · live interop golden-capture (Item 1) → G4.

Last merged: `036-admin-emit-toadmin-coverage` — **MERGED 2026-06-14** (PR #120 squash `5b82f9c`, gate-a-done + gate-b-done, **zero waivers**). Fable review **F-d** / assessment 2.4 DRIFT #1: closed the FR-008 toAdmin/toApp observation gap — 10 of 25 engine-originated `build_*` emit sites bypassed the `Application` callback (half-restructure across 019/027/033). **ARM 1 (9 admin)** → `fire_to_admin_` (C1: after `assign_outbound`, before `store_then_emit`; throw→`app_callback_threw`+`record_state_transition_(Disconnected)`): the shared `emit_session_reject_` helper (covers fromAdmin-veto + no-app callers) + 7 inline `Reject(35=3)` sites + the initiator Guard-3 Logon-ack `Logout`. **ARM 2 (1 app)** → `BusinessMessageReject(35=j)` via `toApp` (C2: before `assign_outbound`): veto (`app_do_not_send`) → suppress emit + no outbound seqnum, but **FALL THROUGH to `persist_inbound_advance_()`** (under-persist guard); throw → `close(terminal)`. No new wire/error/config/codegen/C-ABI surface; amends FR-008 (019), A-014→toApp. Witness `tests/session/test_admin_emit_toadmin_coverage.cpp` (23 cells: exact-count `toAdmin_calls == admin-frames-on-wire` + per-site throw + BMR veto-persist) + the **durable SC-002 static gate** `tools/check_admin_emit_observation.py` (ctest `session_admin_emit_observation_gate`; comment-strip-hardened, negative-tested). **Gate B 2 rounds Sonnet-only**: production control flow confirmed UNCHANGED+sound by hostile Codex+Opus (`git diff ..-- src/` empty); all 4 findings test/verification-completeness (SC-002 static gate, SC-005 byte-identity witness, windowed wire checks, gate comment-strip). CI Tier-1 all-green incl. codecov/patch. New B-036-1; APP-001 + coverage-index §4.5.4 amended. Predecessor `035-filestore-io-offload` MERGED 2026-06-14 (PR #119 squash `90c8037`). NEXT: F-e/F-f Fable fixes · 025 demo · C-103 → G4 tail. Per [[project_036_admin_emit_toadmin_coverage_merged]] / [[project_035_filestore_io_offload_merged]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
