<!-- SPECKIT START -->
**Active feature:** [`012-2h-transport`](specs/012-2h-transport/plan.md) — scaffolded 2026-05-27, branch `012-2h-transport` (parent-of-main `dd42c79` → submodule `67448c0`). Bound by signed-off design doc `.specify/2h-transport.md` v0.3 (Phase-2 Gate A round 2 converged Phase A 2026-05-09). `/speckit-specify` → `/speckit-clarify` (5 Qs resolved 2026-05-27) → **`/speckit-plan` complete** (this rotation). Next gate: **Phase-4 Gate A** per `[const §XVII.1]` / `[pipeline.md step 4]` — reviews the `specs/012-2h-transport/` bundle before `/speckit-tasks`. Ships catalogue rows T-001/T-002/T-003/T-004/T-005 + T-009/T-010 (TLS 1.3 + 1.2 wire via `asio_tls_transport` OpenSSL config); cross-cuts T-039/T-040 wiring half (with 011-tls-policy) + T-041 handoff (into session/ Phase-4). Establishes the new `include/fixpp/transport/` + `src/transport/` module from scratch. **011 Gate B F-1 carryover BINDING** for 012 Gate B per `[[project_011_tls_policy_closed]]` — FR-033 + SC-008 codify the 8-cell `cancellable_dispatch`-recipe witness (4 deterministic cases × 2 executor modes; Clarifications Q3=C); cannot defer further.

Recently merged (`main` history): [`011-tls-policy`](specs/011-tls-policy/plan.md) PR #84 (squash `67448c0`) — TLS validation surface (cert_source + Pinset + verify_peer + CipherPolicy + SecurityProfile + SslCtxConfig + peer_identity). Gate B converged 3 rounds (Sonnet 2/2 used + verification-only round 3; Codex fixer 0/2 NOT consumed); F-1 cancellation-seam #13 full production witness WAIVED to 2h-transport Gate B carryover per `[[project_011_tls_policy_closed]]`. W-2 cppcheck + W-3 iwyu inherited carry-forwards remain applicable.

Earlier merged: [`010-session-cfg-lifetime`](specs/010-session-cfg-lifetime/plan.md) PR #83 (squash `25cf09d`) — closed the 7 residual PR #82 Gate B waivers + 6 in-slice F4 closures + /simplify Wave 1-5 (13th `[[feedback_simplify_pass_catches_9th_burn]]` instance). Gate B converged 2 rounds (Sonnet 1/2 clean). [`009-session-fsm-finalize`](specs/009-session-fsm-finalize/plan.md) PR #82 (squash `ba2222d`); [`008-message-store`](specs/008-message-store/plan.md) PR #77+#78; [`007-threading-clock`](specs/007-threading-clock/plan.md) PR #74+#75; [`006-async-mutex`](specs/006-async-mutex/plan.md) PR #73; [`004-wire-codec`](specs/004-wire-codec/plan.md) PR #68; [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) PR #67; [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) PR #66; [`001-core-decimal`](specs/001-core-decimal/plan.md) Gate A/B converged 2026-05-12/13. [`005-session-establishment-fsm`](specs/005-session-establishment-fsm/plan.md) shipped via bundled PR #82 with 009.

**Deferred-work registry** (still applicable after 012 ships):
- **Per-release interop gate** — run fixpp against QuickFIX-cpp + Fix8 as both initiator and acceptor + sweep their issue trackers and re-run fixpp against their historical thorny bugs. 005 FSM shipped + 011 TLS validation surface shipped; 012 unblocks the full transport-active matrix. Deferred to pre-v1.0 per `[[project_release_interop_quickfix_fix8]]` — escalate at first interop run once 012 + session-Phase-4 ship.
- **Deferred session-recovery feature** — will upgrade 010's F4 closure cells `"2"` (ResendRequest) and `"4"` (SequenceReset) from Reject → Process (gap-fill via message store), matching QuickFIX-cpp / QuickFIX/J. The dup-Logon cell stays as Reject per 005's intentional defensive divergence. Catalogue row 400 in `library/spec/coverage-index.md`; 4 traceability markers tracked.
- **Session-module Phase-4** — the Logon/Heartbeat/Resend/Reset/Logout reconnect FSM + CompID-to-TLS-identity binding policy half of T-041. Consumes 012's `Transport` + `TlsTransport` + `ReconnectPolicy` + `handshake_result.peer_id`. Next Phase-4 feature after 012 ships.
<!-- SPECKIT END -->
