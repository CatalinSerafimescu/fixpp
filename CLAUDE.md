<!-- SPECKIT START -->
**Active feature:** `008-message-store` — the `MessageStore` plugin interface + two default impls (`MemoryStore` in-memory test/embedded; `FileStore` single-append-only-log production with per-record CRC32 + atomic-rename `reset()` + platform durability primitives Linux parent-dir `fsync` / Windows `MOVEFILE_WRITE_THROUGH`) + `MessageStoreFactory` `unique_ptr` ownership (extends 007's minimal stub in place) + awaitable `retrieve_visitor` (root cause #2) + `direction_t` + placeholder `seqnum_t` (uint32, canonical type Phase-4-owned) + `FileStorePolicy` (`commit_per_message` / `commit_batched(N)` / `commit_interval(ms)`) + concept-shaped `has_flush_for_session_close` engine-internal graceful-close hook + `quickfix_compat::cfg_loader` (config translation; **NO** runtime adapter — Path A retired v0.3). 10 new store error variants at slots **56–65**; `EngineConfig::max_store_memory_per_session` field (1 GiB default); per-instance `file_io_executor`. Third and final of `2f→2d→2e` prerequisites the deferred `005` consumes. 21 named test seams; TSan mandatory; libFuzzer voluntary. Owns S-011 (NEW done), S-012 (NEW done), S-013 (NEW done), S-014 (store-side only — FSM half stays `backlog` for `005`); discharges OSS-002 at Path B verdict; updates COM-009 with forward-compat note.

For technologies, project structure, build/test commands, and gate status, read the current plan: [`specs/008-message-store/plan.md`](specs/008-message-store/plan.md). Design anchor: `.specify/2e-msgstore.md` **v0.4** (Gate-A-converged round 3, post-cap line-edit pass; incl. Appendix D cross-doc amendments to `[2d §4.5]` D.1 + `[2d §4.7]` D.2 **already applied at 007 merge**) — on conflict the design doc wins.

Prerequisite/consumed (merged, not modified): `001` `core::error`/`expected_t`/`trap_throw`; `004`/`2b` `wire::Writer::commit` post-commit span + `wire::Framer` `frame_view::bytes()`; `006`/`2f` `async_mutex` (per-instance writer mutex on every method); `007`/`2d` `session_executor` / `Session::session_arena()` / `cancellable_dispatch` / two-phase `Session::close` / `SessionConfig::store_factory` (`unique_ptr` field at `session_config.hpp:106` per `[2d §4.5]` Appendix D §D.1) / the minimal `MessageStoreFactory` polymorphic-bind-target stub at `message_store_factory.hpp` / `[2d §4.7]` per-mode effect-table row at `2d-threading.md:853`. Downstream unblocked by this PR: `005-session-establishment-fsm` (the FIX FSM that consumes the 4-method seam), then the rest of `2g`–`2m`. Cross-doc structural edits **shipped in this feature's merge bundle** (FR-037/038/039: `coverage-index.md:76`, `feature-catalogue.md` OSS-002 + COM-009, `architecture.md:598` Q3 disposition flip) PLUS the `EngineConfig::max_store_memory_per_session` field (FR-014a). Orchestrator step-19 at Gate-B merge promotes catalogue Status `backlog → done` for S-011/S-012/S-013 per `pipeline.md` + 007 NFR-015 precedent (`research.md` D-12).

Companion artifacts in the same directory:
- [`research.md`](specs/008-message-store/research.md) — Phase 0 D-1..D-15 (D-1 = `seqnum.hpp` fresh authoring; D-4 = FSM-dependent seam scripted test-double; D-5 = `[const §VII.5]` store-side applicability + `[const §VII.6]` N/A; D-6 = error slots 56–65 + no C-ABI; D-8 = `[2d]` amendments pre-applied; D-9 = `[arch]` / catalogue / coverage-index edits + `EngineConfig` field owed by this feature; D-11 = concept-shaped non-virtual `flush_for_session_close()` dispatch; D-12 = catalogue Status promotion is orchestrator step 19; D-14 = cross-doc edit tracking)
- [`data-model.md`](specs/008-message-store/data-model.md) — E1..E12 entities, invariants I-01..I-22, error slots 56–65
- [`contracts/`](specs/008-message-store/contracts/) — 11 shape-oracle headers: `message_store` / `retrieve_visitor` / `memory_store` / `file_store` / `message_store_factory` (extends 007's stub) / `memory_store_factory` / `file_store_factory` / `direction` / `seqnum` / `cfg_loader` / `store_errors` / `has_flush_for_session_close`.hpp
- [`quickstart.md`](specs/008-message-store/quickstart.md) — build / test / TSan (mandatory) / ASan+UBSan / alloc-guard / libFuzzer (voluntary) / bench / coverage / `/speckit-verify` / `/gate-a` / `/gate-b` / Windows Tier-2 recipes

**Deferred feature (005-session-establishment-fsm):** Gate A converged (round 1 complete); implementation **unblocked by 008's Gate-B merge** (`2f`/`006` merged, `2d`/`007` merged, `2e`/`008` is the third and final prerequisite). `005` owns the FIX FSM that extends 007's minimal `Session` skeleton + the FIX-TC TC-001..TC-017 corpus `tests/conformance/`; 005's seams replace 008's scripted test-double FSM with the real FSM at the same seam (research D-4).

Previous feature (merged): [`007-threading-clock`](specs/007-threading-clock/plan.md) — `fixpp::core::Clock` plugin + threading contract; PR #74 on `main` (Gate B converged 2026-05-20). Earlier merged: [`006-async-mutex`](specs/006-async-mutex/plan.md) (PR #73, 2026-05-17), [`004-wire-codec`](specs/004-wire-codec/plan.md) (PR #68, 2026-05-17), [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) (PR #67), [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) (PR #66), [`001-core-decimal`](specs/001-core-decimal/plan.md).
<!-- SPECKIT END -->
