<!-- SPECKIT START -->
**Active feature:** **`016-interop-harness`** — [`plan`](specs/016-interop-harness/plan.md) (2026-06-01): `/speckit-specify`+`/speckit-clarify` (3 Qs: SUT-tests-in-lib/orchestration-in-parent; full v1.0 corpus sweep; TLS-logon cells in v1.0) + `/speckit-plan` (Phase 0/1 done) COMPLETE; **next = Phase-4 Gate A** (`[const §XVII.1]`). Realizes the Phase-9 per-release interop gate as a **tests-only feature** (`tests/interop/`, near-zero prod surface): live fixpp-SUT vs QuickFIX-cpp/J both roles (session-only badge), thorny-corpus replay, parity-GAP closure. **One Gate-A adjudication carried in the plan:** `[const §VII.6]`'s `NewOrderSingle→ExecutionReport` interop clause vs the session-only scope (R7 — stays an open v1.0-GA item, not closed by this badge). Per `[[project_release_interop_quickfix_fix8]]`. **`015-runtime-engine` MERGED 2026-06-01 (PR #88 squash `d7e215b`, `gate-b-waived`).** Public Initiator/Acceptor runtime engine (accept/connect loops, `SessionConfig`-keyed registry, programmatic multi-session lifecycle, acceptor accept→Session-create→byte-feed production path) + full **T-041** closure (both-role live CompID↔TLS-identity binding + `logon_peer_identity_override` seam removed from prod+tests) + new error slot `session_unknown_acceptor_session = 121`. Gate B converged **4 fixer rounds** (Sonnet 2/2 → flag commit → Codex 2/2) + 1 confirmatory re-review (final P1=0/P2=0/P3=0) — the whole loop centred on the live-outbound write path + teardown (unified `write_gate_` async-mutex channel, liveness-drain TOCTOU, emit-failure FSM, terminal+graceful `close()` socket-close). Waivers: carried verify-YELLOW (repo-wide static-analysis → chore branch; engine.cpp cov <90/80 justified) + coverage-index P3. Post-merge CI (TSan) caught + fixed a **test-only** span-UAF in `live_outbound_serialized` (`6eff4ea`, in the squash); the owed full-matrix ASan/TSan re-run of the live-write path was **DISCHARGED 2026-06-01** on merged HEAD `9539570` — ASan + TSan each **349/349, 0 findings**; `live_outbound_serialized` 9/9 under both (evidence: `015-runtime-engine-verify.md` "Owed sanitizer re-run — DISCHARGED" addendum). **Next:** repo-wide lint chore branch → Phase-9 interop matrix (**session-only badge** per the post-015 refresh) per `[[project_release_interop_quickfix_fix8]]`; pre-interop refresh + unit-test-parity GAP shortlist in `research/.../phases/phase-9/{REFRESH-2026-06-01-post-015,unit-test-parity-matrix}.md` (1 genuine correctness gap: integer-convertor overflow bounds). See `[[project_015_runtime_engine_inflight]]`.

**Previously merged — `014-transport-active-binding` MERGED 2026-05-30 (PR #87 squash `1848439`, `gate-b-done`, ZERO waivers).** Per-session live wiring shipped: `ReconnectFsm::drive_reconnect_attempt` realized (bounded reconnect loop, `transport_reconnect_limit_exceeded` at cap), live `handshake_result.peer_id → authorize()` on the initiator Logon gate (4-arm guard; fabricated payload removed), real `session_event_credentials_rotated` (real leaf SHA-256, on-strand via injected callback), + 013/012 carry-forward witnesses (Ed25519 sigalg, multi-SAN PMR-OOM depth, load_credentials counter, handshake bench, fuzz re-label) + slot-120 `session_seqnum_too_high`. Two public-surface deltas: error slot 120 + `TransportFactory::cert_source_snapshot()` concrete→pure-virtual (2/5→3/5). **Pipeline:** `/simplify` (`8fbea4f`) → `/speckit-verify` YELLOW (6/6 build, TSan 0-reports, completeness 100%) → **Gate B converged 2 rounds (Sonnet 2/2, zero waivers)** fixing a dangling-`.cn` UAF on the live success arm + a wrong terminal error code; a user-requested post-Gate-B sanitizer re-verify then caught a regression from an orphan round-1 over-correction (`19ccbbd` gated the seam arm on `is_mtls`, broke 3 merged 013 non-mTLS-profile+override tests) — **reverted (`d5c1987`)**, full ASan+UBSan+TSan matrix GREEN (339/339×3, 0 findings) before merge. **T-041 stays `implementing`** — initiator live path done; acceptor live-binding + `logon_peer_identity_override` seam removal + full both-role fail-CLOSED closure carved to **`015`** per `[[project_014_015_split]]`. Roadmap: **015 (runtime engine + full T-041) → chore (repo-wide lint) → interop matrix** per `[[project_release_interop_quickfix_fix8]]`. Lifecycle: `research/.../phases/phase-4/session/014-transport-active-binding.md`; local Gate-B evidence `library/.specify/decisions/014-transport-active-binding-{verify,gateb}.md` (gitignored); reviews `research/reviews/{codex,opus}_pr87_*`. **Standing lesson:** never brief a Gate-B fixer from anticipated findings before the review lands, and always run the FULL sanitizer ctest before sign-off (the gate-b loop's scoped re-reviews missed the `19ccbbd` regression in adjacent merged tests; only the full matrix caught it).

Recently merged (`main` history): [`013-session-reconnect-binding`](specs/013-session-reconnect-binding/plan.md) PR #86 (squash `bd84e08`) — session-Phase-4: reconnect FSM driver (`ReconnectFsm` on 005's 6-state `fsm_state` + AwaitingResend transient) + recovery sub-protocol (ResendRequest issue+reply store-walk replay, SequenceReset-GapFill, ResetSeqNumFlag(141) 3-mode matrix — amends 005 FR-008 fatal-disconnect → recovery-active, discharging the 2e-recovery obligation) + CompID↔TLS-identity binding policy half (allow-list `CompIdAuthorizationPolicy` + canonical CN→SAN-DNS→SAN-URI→SHA-256 extraction + fail-CLOSED-when-mTLS Logon gate) + TLS-validation-outcome `SessionEvent` + in-process `reload_credentials` atomic swap. 4 new `error::session_*` slots 116..119 + dual-emission of 005-era 73/74. New pure-virtual `TransportFactory::reload_credentials` (count 1→2, under 5/5 cap). Catalogue **S-005/S-006/S-014/S-024 → done**, **T-039/T-040 → done**; **T-041 stays `implementing`** (production peer_id source wiring → 014). Gate B converged 3 rounds (Sonnet 2/2, gate-b-waived); post-merge fix `8e2d362` closed a `[const §XV.9]` corpus regression (session.hpp awaitable closure). See `[[project_013_carryforwards_to_014]]`.

Earlier merged: [`012-2h-transport`](specs/012-2h-transport/plan.md) PR #85 (squash `53e25b1`) — TCP + TLS transport surface (`Transport` interface + `TlsTransport` sub-interface, `asio_tls_transport` OpenSSL impl, `asio_listener` acceptor, `ReconnectPolicy` schedule-array shape Q2=C, `TransportFactory` with cached SSL_CTX + credentials Q1=B, `mock_transport` test seam, 22-variant `error::transport_*` envelope at slots 94..115). Gate B converged 4 rounds (Sonnet 2/2 + Codex 2/2 attempts) with 3 documented carry-forward waivers (RC#C PMR-OOM witness depth + RC#G handshake bench scaffold + RC#I fuzz scope re-label). 011 Gate B F-1 cancellation-seam #13 witness discharged in 012 via `tests/transport/test_load_credentials_seam13_witness.cpp` (8 cells × 4 sanitizers). Three new burn-class anti-pattern library candidates surfaced for codification (half-restructure / weak_ptr cache lifetime / trampoline witness surface-cardinality). See `[[project_012_2h_transport_closed]]`.

Earlier merged: [`011-tls-policy`](specs/011-tls-policy/plan.md) PR #84 (squash `67448c0`) — TLS validation surface (cert_source + Pinset + verify_peer + CipherPolicy + SecurityProfile + SslCtxConfig + peer_identity); F-1 carryover discharged in 012. [`010-session-cfg-lifetime`](specs/010-session-cfg-lifetime/plan.md) PR #83 (squash `25cf09d`). [`009-session-fsm-finalize`](specs/009-session-fsm-finalize/plan.md) PR #82 (squash `ba2222d`); [`008-message-store`](specs/008-message-store/plan.md) PR #77+#78; [`007-threading-clock`](specs/007-threading-clock/plan.md) PR #74+#75; [`006-async-mutex`](specs/006-async-mutex/plan.md) PR #73; [`004-wire-codec`](specs/004-wire-codec/plan.md) PR #68; [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) PR #67; [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) PR #66; [`001-core-decimal`](specs/001-core-decimal/plan.md) Gate A/B converged 2026-05-12/13. [`005-session-establishment-fsm`](specs/005-session-establishment-fsm/plan.md) shipped via bundled PR #82 with 009.

**Deferred-work registry** (ordering: 014 → 015 → chore → interop):
- **`014-transport-active-binding`** (ACTIVE) — per-session live wiring: initiator `drive_reconnect_attempt` real loop + live `handshake_result.peer_id → authorize()` + real `credentials_rotated` (013 FR-032) + 013/012 carry-forwards. T-041 advances (live initiator path) but may not fully close in 014. See `[[project_013_carryforwards_to_014]]` + `[[project_014_015_split]]`.
- **`015` — Initiator/Acceptor runtime engine** (next planned, after 014) — public multi-session engine: accept/connect loops, `SessionConfig`-keyed session registry, programmatic multi-session lifecycle, the acceptor accept→Session-create→byte-feed production path, removal of the test-only `logon_peer_identity_override` seam, and **full T-041 production fail-CLOSED closure for both roles**. Bounded BELOW the Phase-5 service wrapper (no config-file parsing / Application callbacks / store-log factories / C ABI). Carve-out decided 2026-05-29 per `[[project_014_015_split]]`. Unblocks `[[project_release_interop_quickfix_fix8]]`.
- **Per-release interop gate** — run fixpp against QuickFIX-cpp + Fix8 as both initiator and acceptor + sweep their issue trackers and re-run fixpp against their historical thorny bugs. 005 FSM + 011 TLS + 012 transport shipped; awaits **014 + 015** (the full transport-active path) before the matrix is runnable. Per `[[project_release_interop_quickfix_fix8]]`.
- **Deferred session-recovery feature** — will upgrade 010's F4 closure cells `"2"` (ResendRequest) and `"4"` (SequenceReset) from Reject → Process (gap-fill via message store), matching QuickFIX-cpp / QuickFIX/J. Dup-Logon cell stays as Reject per 005 defensive divergence. Catalogue row 400; 4 traceability markers tracked.
  - **Slot-74 too-high stand-in cleanup — DISCHARGED (2026-06-02 triage; substantive fix shipped in 014).** `SeqnumManager::check_inbound`'s too-high branch now returns the dedicated `session_seqnum_too_high` (slot 120, minted 014 FR-016/E-4): `src/session/seqnum_manager.cpp:81` returns it and the T4 assertion `tests/session/seqnum_manager_test.cpp:144` already asserts it; reconnect_fsm loop-exhaustion returns `transport_reconnect_limit_exceeded` (`reconnect_fsm.cpp:319`). The slot-74 misnomer the original carry-forward described was superseded by 014; only a stale `.cpp` doc-block comment lagged, corrected in the 2026-06-02 debt-triage doc PR. The remaining `session_test_request_unanswered` (slot 74) refs in `reconnect_fsm.hpp` / `session.hpp` are its **legitimate** TestRequest-unanswered-liveness use, not the stand-in. No further code action; remains a v1.0-release taxonomy-hygiene checklist *confirm* only.
- **015 down-peer initiator teardown — cause #1 FIXED (016 T008); only cause #2 residual (2026-06-02 triage).** Original: a runtime-engine initiator pointed at a **down/unreachable peer** is not promptly torn down by `Engine::stop()`, from two coupled causes. **(1) Busy-spin — FIXED in 016 T008:** `SessionConfig::reconnect_policy` (optional `ReconnectPolicy`, `session_config.hpp:253`) now exists and `resolve_reconnect_policy()` (`session.cpp`) defaults to `defaults_quickfix_compat()` (single 30 s, NON-ZERO backoff), replacing the hard-coded empty `ReconnectPolicy{}` 0-backoff schedule that busy-spun at ~100% CPU. **(2) RESIDUAL:** the in-flight `transport::Transport::async_connect` is still not promptly cancelled by `cancellation_type::total` (~30 s single-connect-timeout latency before `stop()` join completes on a *mid-connect, never-established* initiator). 015's `stop()` cleanly tears down *established* sessions (closes the live socket — T018). The residual is a real but bounded production concurrency change (bounded/cancellable connect on the engine path) → would **re-trigger Gate A** per `[const §XVII.1]`; tracked in `spec/behaviors-and-limitations.md` (L2). NOT a chore — fix as its own Gate-A'd item or fold into the recovery feature.
- **012 Gate B carry-forward slice — mostly landed (2026-06-02 triage).** Seam-13 witness (`tests/transport/test_load_credentials_seam13_witness.cpp`), the RC#C PMR-OOM witness (`test_verify_peer_pmr_oom.cpp`), and the fuzz targets (`fuzz_transport_handshake.cpp`, `fuzz_transport_read_path.cpp`) all exist. Possible residual: RC#C witness *depth* (multi-SAN cert fixture + trampoline-targeted fault-injection cell per `[[feedback_trap_throw_pmr_witness_enumerate_sites]]`) + RC#G handshake *bench* fixture — both **tests/bench-only**, low-risk, optional. RC#I fuzz re-label is a catalogue label only.
- **Repo-wide clang-format / clang-tidy / iwyu / coverage cleanup** — branch `chore/repo-wide-lint` (2026-06-01). **W-format CLEARED** (whole-tree `clang-format`, 308 files; 3 wire tests carry `clang-format off/on` guards for the load-bearing `mock_dict_table.hpp`→`validator.hpp` seam). **W-tidy CLEARED for production** (`src/` + `include/fixpp` headers: 405→0 via hybrid autofix + documented `.clang-tidy` suppressions + 4 targeted fixes/NOLINTs). **W-2 cppcheck CLEARED** (153→0; documented `cppcheck-suppressions.txt` — all FP/style, source-read). **W-3 iwyu RESOLVED as advisory-only** (user-directed 2026-06-01): not enforced — its 335-change include-graph churn conflicts with the deliberate fwd-decl seams + the `[const §XV.9]` awaitable-include invariant for ~zero correctness value, and it was never a gate. **`tests/` clang-tidy OUT OF SCOPE** (user-directed): ~1.5–2k findings are test-scaffold idioms; production code is the binding gate. Coverage cleanup not pursued here. Static-analysis triage found ZERO real defects (consistent with every prior /speckit-verify Step-1).
<!-- SPECKIT END -->
