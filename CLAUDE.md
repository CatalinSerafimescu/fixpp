<!-- SPECKIT START -->
**Active Spec-Kit feature (in-flight, NOT merged)**: **`049-c-abi-handles-errors`** — C ABI Feature A (CA-001..004: opaque handle catalogue, full `fixpp_error_t` master enum + `fixpp_strerror`, per-symbol reentrancy contract, `fixpp_version()`/`fixpp_library_version()` + macros). Plan: `specs/049-c-abi-handles-errors/plan.md` (+ spec/research/data-model/contracts/quickstart). Clarify-locked: stay 0.x (GA does 0→1), full `[2i §4.3]` enum now, pure `translate()` downgrade (engine_create→Feature B, L-049-1), re-number provisional decimal codes 3/10/11→6/800/801. Design note (research D-8, resolved in-feature): `session_*`/`log_*`/`otel_*`/`app_*` C++ variants have no published `[2i §4.3]` code and no Feature-A producer → mapped to `FIXPP_ERR_UNKNOWN` now (total `translate()` switch); publishing the `FIXPP_ERR_SESSION_*` block + `[2i]` amendment is a Feature-B item (L-049-2). Pipeline next: Gate A → /tasks. ABI-affecting ⇒ all four Article X §6 controls mandatory.

**Last-merged Spec-Kit feature**: **`046-atomic-shared-ptr`** (NFR-017 — libc++ portability fallback integration) — **MERGED 2026-06-22, PR #142 (squash `b36ef16`), gate-a-done + gate-b-done, 0 waivers.** Rebased onto merged 048 (PR #144) → consumer set **4→3** (048's strand-local reap removed async_mutex `drain_latch_ptr_`; async_mutex.hpp byte-identical to main; drain-latch witness deleted). Original full pipeline (Gate A r3 + Gate B 3 rounds P1=P2=0) converged pre-048; **focused post-rebase Gate B CONVERGED P1=P2=0** — proven byte-identical surviving C++ + libc++-portability-delta review (`utc_time_point`→ns, native-path-neutral) + independent Codex pass → **2 CI P2s fixed** (one-shot tier2/3 run-trigger + digest-pinned instrumented-libc++ image `sha256:65106764…`). **Tier 1 + Tier 3 GREEN on HEAD** (#37 libc++ lane now a green merge-gate; #25 use_future TSan = proven instrumentation artifact, eliminated at the lane via the GHCR instrumented-libc++ package). CI run-tierN on-demand labels standardised — **run ALL 4 tiers before v1.0; Tier 4/macOS workflow still TBD.** See [[project_libcxx_atomic_shared_ptr_followup]] / [[project_ci_run_tier_labels]] / [[project_libcxx_tsan_usefuture_artifact_proven]]. Spec: `specs/046-atomic-shared-ptr/spec.md` (3 US, FR-001..015 + FR-011a, SC-001..006); plan + research.md D-1..D-9 + data-model.md E-1..E-7 + contracts/{atomic-shared-ptr-api, type-erased-lock-and-awaitable-cleanliness, census-regrowth-guard} + quickstart.md + **tasks.md (T001..T029, 6 phases)**. **KEY decision — NO constitution amendment** (settled at Gate A): the fallback `std::mutex` is **type-erased into `src/core/sync/atomic_shared_ptr.cpp`** behind an opaque RAII `detail::shard_guard` (headers under `include/fixpp/core/sync/detail/`, single `std::size_t shard_index_` member, no `<mutex>`), so Article XI §3 / XV §9 stay satisfied as-is on both libstdc++ and libc++ — verified token-scoped/syntactic; chosen over a §XI.3 exemption. **`fixpp_sync` promotes INTERFACE→STATIC and ALWAYS compiles+ships the guard TU** (always-ship-guard force-mode contract); native path is a zero-overhead alias. **OTel-ENABLED-under-libc++ is the PRIMARY acceptance** (non-OTel `fixpp:with_otel=False` + `FIXPP_BUILD_OTEL=OFF` paired toggle only a documented fallback). XV §1 PASS correct (heap-alloc-scoped, not locking). **Scope**: integrate the proven `fixpp::sync::atomic_shared_ptr<T>` (harness 18/18, design LOCKED) + rewire **3** census'd consumers (pinset.hpp `snapshot_`, transport_factory.hpp `cert_source_slot_`, **engine.hpp `reader_snapshot_`** — added by 023) — _was 4; async_mutex.hpp `drain_latch_ptr_` dropped on the 048 rebase_ + a Tier-2 `linux-clang-libc++` lane + census-regrowth guard + **reverses 023's CHK046** (FR-012/FR-013, objection *removed* not overridden). Build caps: max `-j2`, sanitizer presets ONE AT A TIME (WSL2 OOM). See [[project_libcxx_atomic_shared_ptr_followup]].

**CORRECTIONS 2026-06-20** (parent REMAINING-WORK.md item 6 + phase-4 next-gate note updated): **"G4a — FIXT.1.1 / FIX 5.0 SP2 session establishment" already SHIPPED in `033` (PR #116, MERGED 2026-06-12)** — begin-string split, `1137` negotiation both roles, `553/554` + `authorize_logon` seam, and the un-deferred `deferred:fixt-routing` cells (8 live, goldens banked; S-020 + S-025 done). The earlier "G4a is next / 033 laid groundwork" wording was stale. The only residual FIXT slice, **S-026 per-message `ApplVerID(1128)` routing, was DEFERRED 2026-06-20 (user)** as a thin/ergonomic documented known-limitation (already consumer-reachable via `negotiated_version_profile()` + `MessageView::get(1128)` + `dict::resolve_application_version`). _Other candidates (deferred):_ coverage-honesty gap (README ≥90% vs main ~75.7%, GA-prep) · mallocnesia broad-window gate re-scoping (debt) · G4 live-interop golden-capture + C-103 (Tier-1/2 optional, low value). Tracer/meter TOML config stays backlog (OTLP export pipeline unimplemented).

**Earlier merge**: **`045-observability-config`** (item 14b — step 2 of the 044 rollout) — **MERGED 2026-06-20, PR #141 (squash `e96b520`), gate-a-done + gate-b-waived (1 waiver W-1).** TOML logging-leg: extends the 044 loader to hydrate the existing `fixpp::log::Logger` (file/syslog/OTLP-log sinks) from `[logger]`/`[[logger.sinks]]` → one additive `EngineEstablishment.logger` shared_ptr + per-session via `SessionConfig::logger_override`; side-effect-free load-time preflight (dir exists+writable, OTLP cert readable+PEM, endpoint present); construct-on-clean-accumulator (fail-closed); credential redaction; `logger` flipped deferred→supported; tracer/meter export-pipeline-gated → backlog. No new dep/public-type/module/wire/error/`reason_class`/`fixpp_error_t`/codegen/C-ABI (FR-024). Gate A converged round 3 (Security+Threading trigger). /speckit-verify **GREEN** (6-preset matrix; src/config all touched files ≥90 line/≥80 branch; fuzz zero crashes; **completeness 100% — the audit was run at Gate B pre-flight, having been skipped by 045's verify**). Gate B converged **3 rounds, 1 waiver** (r1 Sonnet: noexcept-boundary escape + non-table-`logger` silent-drop + the full 16-field optional **wrong-type→`malformed_value`** class [a round-2 `directory=123` finding triggered a census showing it only 7/16 closed] + sink-local collect-ALL + negative-numeric guards + default-dir base_dir resolution + `#ifndef _WIN32` compile guard; **standout: `cert_source` wrong-type → silent plain-HTTP TLS fail-open**; r2–3 fixed orchestrator-inline). **Waiver W-1: Windows file-sink directory-writability (FR-014)** — no Windows CI gates the PR (tier2 windows-msvc is opt-in via a `windows` label), the runtime `FileSink` is itself POSIX-only, degrades to the named L-045-1 counted-disable. Lessons: reaffirmed [[feedback_census_all_handrolled_scanners_before_scoping_parse_fix]] (the wrong-type class was the tip of a systemic gap) + [[feedback_subagent_phase_verification_two_traps]] (the r1 fixer fabricated a peer-file anchor in a shipped comment). Catalogue **T-044** (DESIGN). Lifecycle: `phase-4/config/045-observability-config.md`. Per [[project_release_interop_quickfix_fix8]].

**Earlier merge**: **`044-toml-session-config`** (item 14a — step 1 of a 2-step rollout) — **MERGED 2026-06-20, PR #140 (squash `95283fe`), gate-a-done + gate-b-done, 0 waivers.** Native TOML config-file loader → fully-validated `ConfigBundle` (engine-establishment slice + `[]SessionConfig`), **fail-closed before `Session::open`**; per-key collect-ALL diagnostics; `tomlplusplus/3.4.0` PRIVATE in a NEW opt-in `fixpp_config_toml` target (FR-004). No new wire/error/codegen/C-ABI surface (FR-013/FR-022). **T039 (Gate-B blocker) RESOLVED**: a `src/config/toml_include.hpp` ODR-safe shim (`#undef NDEBUG` save/restore + throwing `TOML_ASSERT`) converts toml++'s `TOML_ASSERT_ASSUME` abort(debug)/UB(release) on malformed input (`[ [key]]`) into a catchable `parse_error` (FR-012/rule-9); validated RED→GREEN both modes + ≥10-min fuzz both modes zero crashes. Gate A converged round 4 (Security trigger: TLS/cert-source). /speckit-verify **GREEN** (7-preset matrix; src/config **97.04% line / 84.42% branch**; fuzz both modes zero crashes; completeness 100%). Gate B converged **4 rounds, 0 waivers** (Sonnet r1–2: noexcept-UB [null-resource/duration-overflow/"us"] + per-session `transport.kind` fail-closed [data-model E-3 drift — a session diverging from session[0]'s engine-default ran silently] + collect-ALL truncation + `TOML_ASSERT` macro leak; r2: `security_profile.kind="unset"` accept + collect-ALL decouple; **Codex** r3: minting-loop collect-ALL; r4 CONVERGED). **New lesson [[feedback_coverage_push_enshrines_bugs]]** — a verify-stage coverage push enshrined 2 bugs (silent-TLS-default + `kind="unset"` positives), surfaced only by the hostile Gate B review. Catalogue **T-043** (DESIGN, design-blessed). Lifecycle: `phase-4/config/044-toml-session-config.md`. Per [[project_release_interop_quickfix_fix8]].

**Earlier merge**: **`043-plaintext-tcp-transport`** (Tier-2 item 3b) — **MERGED 2026-06-18, PR #134 (squash `95aa1c3`), gate-a-done + gate-b-waived.** Plaintext TCP transport (`asio_plain_transport`, an encryption-agnostic sibling to `asio_tls_transport`) gated behind the new `SecurityProfile::kind::insecure_plain_tcp` (**constitution §XII.5 amended v0.3**; Gate A folded the amendment review per Article XX). Plain transport = 5 base `Transport` virtuals (no handshake) + credential-free factory sibling (+ `make_accepted()`) + defaulted `TransportFactory::kind()` (pure count stays 3); engine **auto-derives** the plaintext factory; `Session::open()` resolves the effective factory **once, pre-mutation, fail-closed** on an explicit profile↔factory `kind()` mismatch; reconnect-FSM + the three-site acceptor accept-loop **skip the handshake**; `live_peer_id_` stays `nullopt`; loud `[[deprecated]]` friction. **Also folded in (T030, user-ratified): inbound `EncryptMethod(98)≠"0"` reject, all profiles/both roles, present-but-malformed fails closed (sticky-flag — closes the §XII.7 false-green + S-021).** No new wire/error/codegen/C-ABI surface (FR-013). Catalogue **T-042**. Gate A converged round 3 (2 rewrites); /speckit-verify **YELLOW** (6-preset matrix green, zero sanitizer findings; §IX.1 plaintext-reconnect gap witnessed, fault-injection arms waived; TSan single-threaded-harness on the 043 witnesses noted); Gate B converged **round 4** (Sonnet r1-2: §XII.7 last-wins fail-open + untested Logon→Logout + open()-reject-after-state-mutation; Codex r3: residual factory-creation-after-state-mutation; r4 APPROVE). **1 Gate-B waiver (W-1)** + 2 carried verify waivers. New lesson: [[feedback_config_reject_after_state_mutation_leaves_is_open]]; reaffirmed [[feedback_forged_token_before_real_tag_self_heals_witness]]. Lifecycle: `phase-4/transport/043-plaintext-tcp-transport.md`. Per [[project_release_interop_quickfix_fix8]].

**Earlier merge**: **`042-fixt-version-serviceability-guard`** — **MERGED 2026-06-17, PR #133 (squash `5d99d1a`), gate-a-done + gate-b-done, 0 waivers.** Closes **L-033-5 "A+"** (the LAST Fable F-f release-gate tail bullet → **F-f fully discharged**). Role-agnostic `Session::open()` config-load guard: a FIXT session whose configured `default_appl_ver_id` cannot be served by the engine `version_registry` fails closed with `error::invalid_session_config` (a third disjunct on the FQ-1 FIXT guard in `session.cpp`), instead of opening then silently rejecting every inbound FIXT Logon. Inbound peer-`1137` reject (033 FR-004a) stays live; no new wire/error/config/codegen/C-ABI surface (FR-007). Catalogue row **S-043**. Gate A converged round 2 (1 rewrite — root causes: bundle mis-modeled the inherited inbound witnesses; Constitution Check false-PASS on Normative Refs); /speckit-verify GREEN (6-preset matrix, §IX.1 new branch `[True:2,False:14]`); Gate B converged round 1 (Codex APPROVE P1=0/P2=0; 2 P3s fixed — W1/W2 → full FR-002 postcondition + CLAUDE.md drift; new lesson reaffirmed [[feedback_witness_asserts_named_postcondition_not_proxy]]). Per [[project_release_interop_quickfix_fix8]].

**Earlier merge**: **`041-validation-gate-wiring`** (item 3c) — **MERGED 2026-06-17, PR #132 (squash `8c9f6c3`), gate-a-done + gate-b-waived.** Wires the two specified-but-unwired validation gates: (A) opt-in dictionary-driven inbound validation — `SessionConfig::validate_inbound_messages` (default OFF = byte-identical no-op) feeds `wire::dictionary_driven_validator` from a production `dict::table_view` + `Dictionary::as_table_view()` (5/6 inputs; `enum_valid`→true Phase-1, enum 2c-deferred L-041-1), inserted **validate-first per FSM arm**, failures → `Reject(35=3, 373∈{14,2,1,5})`; (B) Engine clock gate — `Engine::start()` `void`→`[[nodiscard]] expected_t<void>`, rejects null clock (`clock_not_set`). Resolves B-004-1/B-005-7 (under opt-in) + B-007-2. **1 waiver: reason-6 (`wire_field_value_truncated`/`373=6`) unreachable with default `pod_decimal`** (cross-traits-only; forward-looking guard, witnessed substitute reason-5; **L-041-3**). Gate B 3 rounds (Sonnet r1–2, r3 clean Codex confirm) — fixed a `table_view` `noexcept` per-msg heap-alloc P1 (transparent heterogeneous `find`), 19 raw `Engine::start()` `[[nodiscard]]`-drop sites, + an alloc-witness/ASan-incompat. New lessons [[feedback_noexcept_hotpath_string_key_alloc]] / [[feedback_operator_new_witness_breaks_sanitizers]] / [[feedback_nodiscard_migration_misses_raw_callsites]]. B&L 041 section (B-041-1/2, L-041-1/2/3); amends catalogue W-014 + NFR-015. Per [[project_fix_tc_coverage_gaps_and_unwired_validator]]. (Separate: TSan `store_fifo_fair` flake → PR #131 MERGED 2026-06-16.)

The Fable **F-f release-gate tail** is **nearly complete** — 039+040 closed 4 of the 6 post-038 F-f bullets, and the **resend-bypasses-`toApp`** bullet was closed 2026-06-15 as a documented limitation (**L-019-4**: `toApp` is an originate-path tap; ResendRequest retransmissions are not surfaced to `toApp` and cannot be app-vetoed/gap-filled — the wire output is FIX-conformant; full QuickFIX-parity `DoNotSend→GapFill`-on-resend is a deferred Phase-5 slice). The **last** F-f bullet, **L-033-5 "A+"**, is now closed by **042** (role-agnostic `open()` serviceability guard; PR #133 squash `5d99d1a`, MERGED 2026-06-17) — so **F-f is fully discharged**. Next candidates (value-ordered, see parent `REMAINING-WORK.md`): the **live-interop golden-capture (Tier-1 Item-1, incl. the 025 demo cell)** + **C-103** chunked-resend (Tier-2, P3) → **G4**. (The 001–014 B&L back-fill is already MERGED — PR #129.) Per [[project_release_interop_quickfix_fix8]]. Start the next slice with `/speckit-specify`.

**Earlier merge**: **`039-ff-tail-hardening`** — plan at `specs/039-ff-tail-hardening/plan.md`. The residual **F-f release-gate tail** (LOW), **US2–US5** (original US1 split to the merged 040): **US2** pinned the ratified frozen-ABI `_checked` `INT64_MIN`-sentinel `OK`+0 behavior with a test + **comment-only** `decimal.cpp` diff (mutation-discrimination RED-confirmed; the Fable "reject the sentinel" ask contradicts the AC-C6/D-12/`[const §X.1]` frozen-ABI decision); **US3** coverage remediation — `set_next_outbound:188` lock-fail (via `drain()`) + `negotiated_version_profile` Unknown-fallback (absent-`1137`-ack initiator witness) WITNESSED, `OsFile` move-ctor re-measured **WAIVED** (production-unreachable + TU-local; re-confirms 008), 033 FIXT lines re-measured COVERED; **US4** extended the §XV.9 no-std-mutex corpus gate **+7** session-side awaitable headers; **US5** resolved L-033-3 + documented the absent/unserviceable `1137`-ack initiator case. **No production behavior change** (FR-014); whole-PR production touch = 4 comment lines. **Status: MERGED 2026-06-15** — PR #127 squash `4465bfe`, **gate-a-waived (NOT REQUIRED, FR-014 + Article XVII §6) + gate-b-waived** (1 waiver: `OsFile` move-ctor §IX.1 coverage). Gate B **1 round, 0 code-fix rounds** (Codex P1=0/P2=0/P3=1 + Opus triage re-confirmed all 5 risk classes; 2 P3 doc-staleness fixed orchestrator-side; **no production code changed by Gate B**). /speckit-verify GREEN (debug/ASan/UBSan/TSan + §XV.9 corpus gate; US3 coverage re-measured 100% on the FIXT funcs). 039 owns no catalogue row. → [[project_040_inbound_tag_overflow_split_and_039_rescope]] / [[project_release_interop_quickfix_fix8]]. **F-f is nearly closed — resend-toApp documented as L-019-4 (2026-06-15); 1 LOW bullet remains (L-033-5 "A+"); see the ACTIVE line above.**

Earlier merges: `040-inbound-tag-overflow-hardening` — **MERGED 2026-06-15** (PR #126 squash `a0f84be`, gate-a-done + gate-b-done, 0 waivers; TLS-auth-bounded **security fix** split from 039: a forged multi-digit tag overflows uint32 and wraps to alias a security tag at 5 live-inbound hand-rolled scanners — one shared `constexpr` `0xFFFF`-bound helper `fixpp::wire::accumulate_tag_digit`; `scan_frame_header` shipped a DEFECTIVE `>429496729U` guard; Gate B P2 was a non-discriminating witness → [[feedback_forged_token_before_real_tag_self_heals_witness]]). `038-acceptor-sendingtime-guard` — **MERGED 2026-06-15** (PR #122 squash `9f2cda9`, gate-a/b-done, 0 waivers; Fable F-f session subset: acceptor first-Logon SendingTime(52) MaxLatency guard + ReconnectFsm callback-seam hardening + FIXT 1137 witnesses; amends S-019). `037-resend-reply-possdup-tags` — **MERGED 2026-06-14** (PR #121 squash `795cde1`, gate-a-done + gate-b-done, **zero waivers**). Fable review **F-e** / assessment 2.4 DRIFT #2: resend-reply GapFill now stamps `PossDupFlag(43)=Y` + `OrigSendingTime(122)=<own 52>` (`build_sequence_reset_gapfill`); `build_replay_frame` dedups stored `43`/`122` under `allow_pos_dup=true` (one each). Gate B 2 rounds Sonnet-only — the two wire edits sound; the one P1 was a **pre-existing fail-closed defect the PR aggravated** (`emit_gapfill_async` `co_return true` silent-success on a 256B GapFill overflow with uncapped CompIDs → silent recovery data-loss) fixed `co_return false` + near-cap RED witness → [[feedback_fixed_buffer_build_failure_silent_success]]. Amends S-005/S-006/S-010; live SC-004/FR-008 QFJ arm deferred (L-037-2, unblock-conditioned), QFcpp waived (L-021-3). Predecessors `036-admin-emit-toadmin-coverage` (PR #120 `5b82f9c`) + `035-filestore-io-offload` (PR #119 `90c8037`) both MERGED 2026-06-14. NEXT: F-f session subset = **038 (active)**; then F-f tail (release-gate) · 025 demo · C-103 → G4. Per [[project_037_resend_reply_possdup_merged]] / [[project_036_admin_emit_toadmin_coverage_merged]] / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
