<!-- SPECKIT START -->
**Active feature:** `009-session-fsm-finalize` — drift closure against the Gate-A-converged 005-session-establishment-fsm design. Addresses the 7 binding-contract drift gaps found by PR #81 round-1 Gate B Codex review (Opus triage confirmed all 7 @ P1; the 005 Phase-8 T067 completeness audit was a false-PASS on 5 FRs — see `[[project_005_phase8_completeness_false_pass]]`). The 7 RCs: (1) wire `Session::send` outbound pipeline (assign + stamp + store + emit); (2) add acceptor `session_role` branch on `Session::open` so the `NotConnected→LogonReceived→Active` matrix row is reachable from production; (3) correct the `NotConnected × refused-Logon` matrix cell to `Disconnected`; (4) thread negotiated `BeginString` + `effective_clock.now()` through every admin builder; (5) reject missing/malformed inbound `SendingTime` per contract (LogonSent-special logout-with-error per D-3); (6) move TestReqID counter from process-global static to per-session state; (7) drain `SeqnumManager` in `Session::close` before destruction.

For technologies, project structure, build/test commands, and gate status, read the current plan: [`specs/009-session-fsm-finalize/plan.md`](specs/009-session-fsm-finalize/plan.md). **The 005 design is binding and unchanged** — the slice does not amend `005/spec.md` / `data-model.md` / `contracts/*.hpp`. **Gate A is NOT re-run for 009** (the design was already approved by 005's Gate A round 3, 2026-05-18). Design anchors: every existing 005 anchor carries forward as-is (`.specify/2e-msgstore.md` v0.4, `.specify/2d-threading.md` v0.4, `.specify/2f-async-mutex.md` v1.5, `[FIX-SL]` + `[FIX-TC]`).

**Branch base:** `009-session-fsm-finalize` is rooted on `005-session-establishment-fsm` HEAD `4e621e1`, inheriting the 25 prior commits (Phase 1–8 + /simplify + /speckit-verify). At Gate B convergence the slice merges back into `005-session-establishment-fsm` (refreshing PR #81's draft branch) OR directly to `main` (retiring 005's branch and PR #81) — choice deferred to merge time.

`/clarify` decisions: **N/A** (inherited from 005 Q1/Q2/Q3 + Session-2026-05-18 Gate A round 1 re-clarification). No new design question opens.

Companion artifacts in the same directory:
- [`spec.md`](specs/009-session-fsm-finalize/spec.md) — 13 FRs (FR-001..FR-013) traced to the 7 PR #81 RCs and to existing 005 contract anchors; 4 user stories (US1+US2 P1, US3+US4 P2); 7 measurable SCs.
- [`research.md`](specs/009-session-fsm-finalize/research.md) — Phase 0: design inheritance from 005 D-1..D-13 + 3 micro-decisions (D-1 `session_role` enum placement, D-2 drain-failure policy, D-3 TestReqID counter overflow).
- [`data-model.md`](specs/009-session-fsm-finalize/data-model.md) — **no new entities;** modifies existing 005 entities E1 `Session` (member + 6 method-semantics) + E2 `SessionConfig` (new `role` field) + admin builders (2 new params).
- [`contracts/session_role.hpp`](specs/009-session-fsm-finalize/contracts/session_role.hpp) — the ONLY new shape oracle; the rest of the contracts are 005's unchanged.
- [`quickstart.md`](specs/009-session-fsm-finalize/quickstart.md) — build / test / TSan / coverage / `/speckit-verify` / feature-completeness audit (test-bodies discipline per `[[project_005_phase8_completeness_false_pass]]`) / `/gate-b` / merge bookkeeping.

**PR #81 state:** open as **draft** on branch `005-session-establishment-fsm` (commit `4e621e1`). Round-1 reviews on disk: `research/G19-fix-fpml-iso20022/research/reviews/codex_pr81_review.md` + `opus_pr81_1_triage.md` + `opus_pr81_paused.md`. Decision: PR #81 will either refresh post-009-merge or be superseded by a fresh PR for 009 — user picks at merge time.

Previous feature (merged): [`004-wire-codec`](specs/004-wire-codec/plan.md) — `fixpp::wire` module + the 2b cutover. Gate A + Gate B converged; PR #68 on `main`. Earlier: [`008-message-store`](specs/008-message-store/plan.md) PR #77+#78, [`007-threading-clock`](specs/007-threading-clock/plan.md) PR #74+#75, [`006-async-mutex`](specs/006-async-mutex/plan.md) PR #73, [`003-dictionary-codegen`](specs/003-dictionary-codegen/plan.md) PR #67, [`002-dictionary-xml-loader`](specs/002-dictionary-xml-loader/plan.md) PR #66, [`001-core-decimal`](specs/001-core-decimal/plan.md) Gate A/B converged 2026-05-12/13.
<!-- SPECKIT END -->
