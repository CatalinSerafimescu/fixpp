<!-- SPECKIT START -->
**Active Spec-Kit feature: `032-initiator-reset-outbound-advance`** — plan: `specs/032-initiator-reset-outbound-advance/plan.md` (Gate A PENDING, next). Conformance fix for the live-found **L-024-2**: a `reset_on_logon` initiator emits `Logon(141=Y,34=1)` (outbound→2), then on the peer's `141=Y` echo at the `peer_ack_sent_reset_flag` arm (`session.cpp:3185`) rebases outbound 2→1 (the missing OUTBOUND twin of `030`'s inbound restore at `:3210`) → duplicate `34=1` → QFcpp/QFJ reject. Fix = capture pre-reset `n_pre_outbound`, restore outbound to `2` iff `n_pre_outbound==seqnum_min+1` (reset-before-send observable; covers all reset knobs, excludes `bilateral_strict`-at-N by construction) + widen `we_initiated` (`:3224`) to the `initr_reset_seqnum` predicate (fix `by_peer_request`). Amends catalogue **S-017**. Mechanism (restore vs skip-reset) flagged for Gate A.

Last merged: `031-acceptor-789-resend-boundary` — **MERGED 2026-06-11** (PR #114 squash `7477fe2`, gate-a-done + gate-b-done; amends catalogue **S-031**). The acceptor `NextExpectedMsgSeqNum(789)` honor now compares the peer's advertised 789 against the **pre-reply** next-outbound `N_pre` (parameterized `honor_peer_next_expected_(.., next_outbound_ref)`: acceptor passes hoisted `n_pre_outbound` = `reply_seq` captured before the reply emit; initiator passes current `peek_outbound()` — byte-identical). Closed the live-found CHECKPOINT-9 conformance bug (spurious in-sync `SequenceReset-GapFill` → QFcpp/QFJ reject); all 4 `NE-*-fix44-next-expected` cells now pass live. Predecessor `030-received-reset-inbound-advance` MERGED 2026-06-10 (PR #113 squash `3c5ee65`). NEXT (per `../REMAINING-WORK.md` Tier-1 Item 1): 025 `StandbyRehydrate` advanced-store demo cell (harness work, non-SC) · PD-replay peer · 028 frame-injection · C-103 → G4. Per [[project_031_acceptor_789_resend_boundary_merged]] / [[project_live_quickfix_hostile_scenarios]] CHECKPOINT 10 / [[project_release_interop_quickfix_fix8]].

**This file stays a thin pointer — shipped-feature history is tracked elsewhere, not duplicated here:**
- **Authoritative per-feature status + evidence:** `spec/feature-catalogue.md` (status / evidence_pr / tests per row) + `spec/coverage-index.md`.
- **Per-feature Gate A/B convergence + sign-off:** parent `research/G19-fix-fpml-iso20022/phases/phase-4/session/<feature>.md` (+ the `phases/phase-4.md` status dashboard / `phases/phase-4/cross-module-decisions.md`).
- **Behaviours & limitations (operator-facing):** `spec/behaviors-and-limitations.md` (B-* / L-* rows).
- **Cross-session state + lessons:** project memory `project_*_merged` / `feedback_*` notes (index in `MEMORY.md`).

**Live deferred work** (everything else in the old registry is DISCHARGED or tracked in the catalogue / B&L above):
- **Deferred session-recovery (catalogue row 400)** — upgrade 010 F4 cells "2"/"4" (ResendRequest / SequenceReset) from Reject→Process; largely shipped via 013 + S-023; residual = traceability-marker confirm at the v1.0 gate.
<!-- SPECKIT END -->
